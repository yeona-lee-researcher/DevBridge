import {
  ChannelSearchSource,
  ErrorFromResponse,
  FixedSizeQueueCache,
  LinkPreviewsManager,
  LiveLocationManager,
  MessageComposer,
  MessageSearchSource,
  SearchController,
  StateStore,
  StreamChat,
  UserSearchSource,
  VotingVisibility,
  find,
  formatMessage,
  isAudioAttachment,
  isFileAttachment,
  isGiphyAttachment,
  isImageAttachment,
  isLocalAttachment,
  isLocalAudioAttachment,
  isLocalFileAttachment,
  isLocalImageAttachment,
  isLocalVideoAttachment,
  isLocalVoiceRecordingAttachment,
  isScrapedContent,
  isSharedLocationResponse,
  isVideoAttachment,
  isVoiceRecordingAttachment,
  isVoteAnswer,
  localMessageToNewMessagePayload,
  test
} from "./chunk-QNRSLZ5V.js";
import "./chunk-4TT6YGON.js";
import {
  require_jsx_runtime
} from "./chunk-JPS3FM2S.js";
import {
  require_react_dom
} from "./chunk-Y4FVOAAS.js";
import {
  require_react
} from "./chunk-N7QNPNI7.js";
import {
  __commonJS,
  __esm,
  __export,
  __toCommonJS,
  __toESM
} from "./chunk-V4OQ3NZ2.js";

// node_modules/lodash.throttle/index.js
var require_lodash = __commonJS({
  "node_modules/lodash.throttle/index.js"(exports, module) {
    var FUNC_ERROR_TEXT = "Expected a function";
    var NAN = 0 / 0;
    var symbolTag = "[object Symbol]";
    var reTrim = /^\s+|\s+$/g;
    var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
    var reIsBinary = /^0b[01]+$/i;
    var reIsOctal = /^0o[0-7]+$/i;
    var freeParseInt = parseInt;
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root4 = freeGlobal || freeSelf || Function("return this")();
    var objectProto = Object.prototype;
    var objectToString = objectProto.toString;
    var nativeMax = Math.max;
    var nativeMin = Math.min;
    var now = function() {
      return root4.Date.now();
    };
    function debounce3(func, wait, options) {
      var lastArgs, lastThis, maxWait, result, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      wait = toNumber(wait) || 0;
      if (isObject2(options)) {
        leading = !!options.leading;
        maxing = "maxWait" in options;
        maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      function invokeFunc(time) {
        var args = lastArgs, thisArg = lastThis;
        lastArgs = lastThis = void 0;
        lastInvokeTime = time;
        result = func.apply(thisArg, args);
        return result;
      }
      function leadingEdge(time) {
        lastInvokeTime = time;
        timerId = setTimeout(timerExpired, wait);
        return leading ? invokeFunc(time) : result;
      }
      function remainingWait(time) {
        var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime, result2 = wait - timeSinceLastCall;
        return maxing ? nativeMin(result2, maxWait - timeSinceLastInvoke) : result2;
      }
      function shouldInvoke(time) {
        var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime;
        return lastCallTime === void 0 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
      }
      function timerExpired() {
        var time = now();
        if (shouldInvoke(time)) {
          return trailingEdge(time);
        }
        timerId = setTimeout(timerExpired, remainingWait(time));
      }
      function trailingEdge(time) {
        timerId = void 0;
        if (trailing && lastArgs) {
          return invokeFunc(time);
        }
        lastArgs = lastThis = void 0;
        return result;
      }
      function cancel() {
        if (timerId !== void 0) {
          clearTimeout(timerId);
        }
        lastInvokeTime = 0;
        lastArgs = lastCallTime = lastThis = timerId = void 0;
      }
      function flush() {
        return timerId === void 0 ? result : trailingEdge(now());
      }
      function debounced() {
        var time = now(), isInvoking = shouldInvoke(time);
        lastArgs = arguments;
        lastThis = this;
        lastCallTime = time;
        if (isInvoking) {
          if (timerId === void 0) {
            return leadingEdge(lastCallTime);
          }
          if (maxing) {
            timerId = setTimeout(timerExpired, wait);
            return invokeFunc(lastCallTime);
          }
        }
        if (timerId === void 0) {
          timerId = setTimeout(timerExpired, wait);
        }
        return result;
      }
      debounced.cancel = cancel;
      debounced.flush = flush;
      return debounced;
    }
    function throttle3(func, wait, options) {
      var leading = true, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      if (isObject2(options)) {
        leading = "leading" in options ? !!options.leading : leading;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      return debounce3(func, wait, {
        "leading": leading,
        "maxWait": wait,
        "trailing": trailing
      });
    }
    function isObject2(value) {
      var type = typeof value;
      return !!value && (type == "object" || type == "function");
    }
    function isObjectLike(value) {
      return !!value && typeof value == "object";
    }
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && objectToString.call(value) == symbolTag;
    }
    function toNumber(value) {
      if (typeof value == "number") {
        return value;
      }
      if (isSymbol(value)) {
        return NAN;
      }
      if (isObject2(value)) {
        var other = typeof value.valueOf == "function" ? value.valueOf() : value;
        value = isObject2(other) ? other + "" : other;
      }
      if (typeof value != "string") {
        return value === 0 ? value : +value;
      }
      value = value.replace(reTrim, "");
      var isBinary = reIsBinary.test(value);
      return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
    }
    module.exports = throttle3;
  }
});

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
var require_use_sync_external_store_shim_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
    "use strict";
    (function() {
      function is2(x2, y2) {
        return x2 === y2 && (0 !== x2 || 1 / x2 === 1 / y2) || x2 !== x2 && y2 !== y2;
      }
      function useSyncExternalStore$2(subscribe2, getSnapshot) {
        didWarnOld18Alpha || void 0 === React10.startTransition || (didWarnOld18Alpha = true, console.error(
          "You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."
        ));
        var value = getSnapshot();
        if (!didWarnUncachedGetSnapshot) {
          var cachedValue = getSnapshot();
          objectIs(value, cachedValue) || (console.error(
            "The result of getSnapshot should be cached to avoid an infinite loop"
          ), didWarnUncachedGetSnapshot = true);
        }
        cachedValue = useState8({
          inst: { value, getSnapshot }
        });
        var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
        useLayoutEffect8(
          function() {
            inst.value = value;
            inst.getSnapshot = getSnapshot;
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
          },
          [subscribe2, value, getSnapshot]
        );
        useEffect8(
          function() {
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            return subscribe2(function() {
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            });
          },
          [subscribe2]
        );
        useDebugValue(value);
        return value;
      }
      function checkIfSnapshotChanged(inst) {
        var latestGetSnapshot = inst.getSnapshot;
        inst = inst.value;
        try {
          var nextValue = latestGetSnapshot();
          return !objectIs(inst, nextValue);
        } catch (error2) {
          return true;
        }
      }
      function useSyncExternalStore$1(subscribe2, getSnapshot) {
        return getSnapshot();
      }
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
      var React10 = require_react(), objectIs = "function" === typeof Object.is ? Object.is : is2, useState8 = React10.useState, useEffect8 = React10.useEffect, useLayoutEffect8 = React10.useLayoutEffect, useDebugValue = React10.useDebugValue, didWarnOld18Alpha = false, didWarnUncachedGetSnapshot = false, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
      exports.useSyncExternalStore = void 0 !== React10.useSyncExternalStore ? React10.useSyncExternalStore : shim;
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    })();
  }
});

// node_modules/use-sync-external-store/shim/index.js
var require_shim = __commonJS({
  "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_use_sync_external_store_shim_development();
    }
  }
});

// node_modules/lodash.debounce/index.js
var require_lodash2 = __commonJS({
  "node_modules/lodash.debounce/index.js"(exports, module) {
    var FUNC_ERROR_TEXT = "Expected a function";
    var NAN = 0 / 0;
    var symbolTag = "[object Symbol]";
    var reTrim = /^\s+|\s+$/g;
    var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
    var reIsBinary = /^0b[01]+$/i;
    var reIsOctal = /^0o[0-7]+$/i;
    var freeParseInt = parseInt;
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root4 = freeGlobal || freeSelf || Function("return this")();
    var objectProto = Object.prototype;
    var objectToString = objectProto.toString;
    var nativeMax = Math.max;
    var nativeMin = Math.min;
    var now = function() {
      return root4.Date.now();
    };
    function debounce3(func, wait, options) {
      var lastArgs, lastThis, maxWait, result, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      wait = toNumber(wait) || 0;
      if (isObject2(options)) {
        leading = !!options.leading;
        maxing = "maxWait" in options;
        maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      function invokeFunc(time) {
        var args = lastArgs, thisArg = lastThis;
        lastArgs = lastThis = void 0;
        lastInvokeTime = time;
        result = func.apply(thisArg, args);
        return result;
      }
      function leadingEdge(time) {
        lastInvokeTime = time;
        timerId = setTimeout(timerExpired, wait);
        return leading ? invokeFunc(time) : result;
      }
      function remainingWait(time) {
        var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime, result2 = wait - timeSinceLastCall;
        return maxing ? nativeMin(result2, maxWait - timeSinceLastInvoke) : result2;
      }
      function shouldInvoke(time) {
        var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime;
        return lastCallTime === void 0 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
      }
      function timerExpired() {
        var time = now();
        if (shouldInvoke(time)) {
          return trailingEdge(time);
        }
        timerId = setTimeout(timerExpired, remainingWait(time));
      }
      function trailingEdge(time) {
        timerId = void 0;
        if (trailing && lastArgs) {
          return invokeFunc(time);
        }
        lastArgs = lastThis = void 0;
        return result;
      }
      function cancel() {
        if (timerId !== void 0) {
          clearTimeout(timerId);
        }
        lastInvokeTime = 0;
        lastArgs = lastCallTime = lastThis = timerId = void 0;
      }
      function flush() {
        return timerId === void 0 ? result : trailingEdge(now());
      }
      function debounced() {
        var time = now(), isInvoking = shouldInvoke(time);
        lastArgs = arguments;
        lastThis = this;
        lastCallTime = time;
        if (isInvoking) {
          if (timerId === void 0) {
            return leadingEdge(lastCallTime);
          }
          if (maxing) {
            timerId = setTimeout(timerExpired, wait);
            return invokeFunc(lastCallTime);
          }
        }
        if (timerId === void 0) {
          timerId = setTimeout(timerExpired, wait);
        }
        return result;
      }
      debounced.cancel = cancel;
      debounced.flush = flush;
      return debounced;
    }
    function isObject2(value) {
      var type = typeof value;
      return !!value && (type == "object" || type == "function");
    }
    function isObjectLike(value) {
      return !!value && typeof value == "object";
    }
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && objectToString.call(value) == symbolTag;
    }
    function toNumber(value) {
      if (typeof value == "number") {
        return value;
      }
      if (isSymbol(value)) {
        return NAN;
      }
      if (isObject2(value)) {
        var other = typeof value.valueOf == "function" ? value.valueOf() : value;
        value = isObject2(other) ? other + "" : other;
      }
      if (typeof value != "string") {
        return value === 0 ? value : +value;
      }
      value = value.replace(reTrim, "");
      var isBinary = reIsBinary.test(value);
      return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
    }
    module.exports = debounce3;
  }
});

// node_modules/dayjs/dayjs.min.js
var require_dayjs_min = __commonJS({
  "node_modules/dayjs/dayjs.min.js"(exports, module) {
    !(function(t2, e) {
      "object" == typeof exports && "undefined" != typeof module ? module.exports = e() : "function" == typeof define && define.amd ? define(e) : (t2 = "undefined" != typeof globalThis ? globalThis : t2 || self).dayjs = e();
    })(exports, (function() {
      "use strict";
      var t2 = 1e3, e = 6e4, n2 = 36e5, r3 = "millisecond", i2 = "second", s2 = "minute", u3 = "hour", a2 = "day", o2 = "week", c2 = "month", f2 = "quarter", h2 = "year", d2 = "date", l2 = "Invalid Date", $2 = /^(\d{4})[-/]?(\d{1,2})?[-/]?(\d{0,2})[Tt\s]*(\d{1,2})?:?(\d{1,2})?:?(\d{1,2})?[.:]?(\d+)?$/, y2 = /\[([^\]]+)]|Y{1,4}|M{1,4}|D{1,2}|d{1,4}|H{1,2}|h{1,2}|a|A|m{1,2}|s{1,2}|Z{1,2}|SSS/g, M2 = { name: "en", weekdays: "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), months: "January_February_March_April_May_June_July_August_September_October_November_December".split("_"), ordinal: function(t3) {
        var e2 = ["th", "st", "nd", "rd"], n3 = t3 % 100;
        return "[" + t3 + (e2[(n3 - 20) % 10] || e2[n3] || e2[0]) + "]";
      } }, m2 = function(t3, e2, n3) {
        var r4 = String(t3);
        return !r4 || r4.length >= e2 ? t3 : "" + Array(e2 + 1 - r4.length).join(n3) + t3;
      }, v2 = { s: m2, z: function(t3) {
        var e2 = -t3.utcOffset(), n3 = Math.abs(e2), r4 = Math.floor(n3 / 60), i3 = n3 % 60;
        return (e2 <= 0 ? "+" : "-") + m2(r4, 2, "0") + ":" + m2(i3, 2, "0");
      }, m: function t3(e2, n3) {
        if (e2.date() < n3.date()) return -t3(n3, e2);
        var r4 = 12 * (n3.year() - e2.year()) + (n3.month() - e2.month()), i3 = e2.clone().add(r4, c2), s3 = n3 - i3 < 0, u4 = e2.clone().add(r4 + (s3 ? -1 : 1), c2);
        return +(-(r4 + (n3 - i3) / (s3 ? i3 - u4 : u4 - i3)) || 0);
      }, a: function(t3) {
        return t3 < 0 ? Math.ceil(t3) || 0 : Math.floor(t3);
      }, p: function(t3) {
        return { M: c2, y: h2, w: o2, d: a2, D: d2, h: u3, m: s2, s: i2, ms: r3, Q: f2 }[t3] || String(t3 || "").toLowerCase().replace(/s$/, "");
      }, u: function(t3) {
        return void 0 === t3;
      } }, g2 = "en", D2 = {};
      D2[g2] = M2;
      var p2 = "$isDayjsObject", S2 = function(t3) {
        return t3 instanceof _2 || !(!t3 || !t3[p2]);
      }, w2 = function t3(e2, n3, r4) {
        var i3;
        if (!e2) return g2;
        if ("string" == typeof e2) {
          var s3 = e2.toLowerCase();
          D2[s3] && (i3 = s3), n3 && (D2[s3] = n3, i3 = s3);
          var u4 = e2.split("-");
          if (!i3 && u4.length > 1) return t3(u4[0]);
        } else {
          var a3 = e2.name;
          D2[a3] = e2, i3 = a3;
        }
        return !r4 && i3 && (g2 = i3), i3 || !r4 && g2;
      }, O2 = function(t3, e2) {
        if (S2(t3)) return t3.clone();
        var n3 = "object" == typeof e2 ? e2 : {};
        return n3.date = t3, n3.args = arguments, new _2(n3);
      }, b2 = v2;
      b2.l = w2, b2.i = S2, b2.w = function(t3, e2) {
        return O2(t3, { locale: e2.$L, utc: e2.$u, x: e2.$x, $offset: e2.$offset });
      };
      var _2 = (function() {
        function M3(t3) {
          this.$L = w2(t3.locale, null, true), this.parse(t3), this.$x = this.$x || t3.x || {}, this[p2] = true;
        }
        var m3 = M3.prototype;
        return m3.parse = function(t3) {
          this.$d = (function(t4) {
            var e2 = t4.date, n3 = t4.utc;
            if (null === e2) return /* @__PURE__ */ new Date(NaN);
            if (b2.u(e2)) return /* @__PURE__ */ new Date();
            if (e2 instanceof Date) return new Date(e2);
            if ("string" == typeof e2 && !/Z$/i.test(e2)) {
              var r4 = e2.match($2);
              if (r4) {
                var i3 = r4[2] - 1 || 0, s3 = (r4[7] || "0").substring(0, 3);
                return n3 ? new Date(Date.UTC(r4[1], i3, r4[3] || 1, r4[4] || 0, r4[5] || 0, r4[6] || 0, s3)) : new Date(r4[1], i3, r4[3] || 1, r4[4] || 0, r4[5] || 0, r4[6] || 0, s3);
              }
            }
            return new Date(e2);
          })(t3), this.init();
        }, m3.init = function() {
          var t3 = this.$d;
          this.$y = t3.getFullYear(), this.$M = t3.getMonth(), this.$D = t3.getDate(), this.$W = t3.getDay(), this.$H = t3.getHours(), this.$m = t3.getMinutes(), this.$s = t3.getSeconds(), this.$ms = t3.getMilliseconds();
        }, m3.$utils = function() {
          return b2;
        }, m3.isValid = function() {
          return !(this.$d.toString() === l2);
        }, m3.isSame = function(t3, e2) {
          var n3 = O2(t3);
          return this.startOf(e2) <= n3 && n3 <= this.endOf(e2);
        }, m3.isAfter = function(t3, e2) {
          return O2(t3) < this.startOf(e2);
        }, m3.isBefore = function(t3, e2) {
          return this.endOf(e2) < O2(t3);
        }, m3.$g = function(t3, e2, n3) {
          return b2.u(t3) ? this[e2] : this.set(n3, t3);
        }, m3.unix = function() {
          return Math.floor(this.valueOf() / 1e3);
        }, m3.valueOf = function() {
          return this.$d.getTime();
        }, m3.startOf = function(t3, e2) {
          var n3 = this, r4 = !!b2.u(e2) || e2, f3 = b2.p(t3), l3 = function(t4, e3) {
            var i3 = b2.w(n3.$u ? Date.UTC(n3.$y, e3, t4) : new Date(n3.$y, e3, t4), n3);
            return r4 ? i3 : i3.endOf(a2);
          }, $3 = function(t4, e3) {
            return b2.w(n3.toDate()[t4].apply(n3.toDate("s"), (r4 ? [0, 0, 0, 0] : [23, 59, 59, 999]).slice(e3)), n3);
          }, y3 = this.$W, M4 = this.$M, m4 = this.$D, v3 = "set" + (this.$u ? "UTC" : "");
          switch (f3) {
            case h2:
              return r4 ? l3(1, 0) : l3(31, 11);
            case c2:
              return r4 ? l3(1, M4) : l3(0, M4 + 1);
            case o2:
              var g3 = this.$locale().weekStart || 0, D3 = (y3 < g3 ? y3 + 7 : y3) - g3;
              return l3(r4 ? m4 - D3 : m4 + (6 - D3), M4);
            case a2:
            case d2:
              return $3(v3 + "Hours", 0);
            case u3:
              return $3(v3 + "Minutes", 1);
            case s2:
              return $3(v3 + "Seconds", 2);
            case i2:
              return $3(v3 + "Milliseconds", 3);
            default:
              return this.clone();
          }
        }, m3.endOf = function(t3) {
          return this.startOf(t3, false);
        }, m3.$set = function(t3, e2) {
          var n3, o3 = b2.p(t3), f3 = "set" + (this.$u ? "UTC" : ""), l3 = (n3 = {}, n3[a2] = f3 + "Date", n3[d2] = f3 + "Date", n3[c2] = f3 + "Month", n3[h2] = f3 + "FullYear", n3[u3] = f3 + "Hours", n3[s2] = f3 + "Minutes", n3[i2] = f3 + "Seconds", n3[r3] = f3 + "Milliseconds", n3)[o3], $3 = o3 === a2 ? this.$D + (e2 - this.$W) : e2;
          if (o3 === c2 || o3 === h2) {
            var y3 = this.clone().set(d2, 1);
            y3.$d[l3]($3), y3.init(), this.$d = y3.set(d2, Math.min(this.$D, y3.daysInMonth())).$d;
          } else l3 && this.$d[l3]($3);
          return this.init(), this;
        }, m3.set = function(t3, e2) {
          return this.clone().$set(t3, e2);
        }, m3.get = function(t3) {
          return this[b2.p(t3)]();
        }, m3.add = function(r4, f3) {
          var d3, l3 = this;
          r4 = Number(r4);
          var $3 = b2.p(f3), y3 = function(t3) {
            var e2 = O2(l3);
            return b2.w(e2.date(e2.date() + Math.round(t3 * r4)), l3);
          };
          if ($3 === c2) return this.set(c2, this.$M + r4);
          if ($3 === h2) return this.set(h2, this.$y + r4);
          if ($3 === a2) return y3(1);
          if ($3 === o2) return y3(7);
          var M4 = (d3 = {}, d3[s2] = e, d3[u3] = n2, d3[i2] = t2, d3)[$3] || 1, m4 = this.$d.getTime() + r4 * M4;
          return b2.w(m4, this);
        }, m3.subtract = function(t3, e2) {
          return this.add(-1 * t3, e2);
        }, m3.format = function(t3) {
          var e2 = this, n3 = this.$locale();
          if (!this.isValid()) return n3.invalidDate || l2;
          var r4 = t3 || "YYYY-MM-DDTHH:mm:ssZ", i3 = b2.z(this), s3 = this.$H, u4 = this.$m, a3 = this.$M, o3 = n3.weekdays, c3 = n3.months, f3 = n3.meridiem, h3 = function(t4, n4, i4, s4) {
            return t4 && (t4[n4] || t4(e2, r4)) || i4[n4].slice(0, s4);
          }, d3 = function(t4) {
            return b2.s(s3 % 12 || 12, t4, "0");
          }, $3 = f3 || function(t4, e3, n4) {
            var r5 = t4 < 12 ? "AM" : "PM";
            return n4 ? r5.toLowerCase() : r5;
          };
          return r4.replace(y2, (function(t4, r5) {
            return r5 || (function(t5) {
              switch (t5) {
                case "YY":
                  return String(e2.$y).slice(-2);
                case "YYYY":
                  return b2.s(e2.$y, 4, "0");
                case "M":
                  return a3 + 1;
                case "MM":
                  return b2.s(a3 + 1, 2, "0");
                case "MMM":
                  return h3(n3.monthsShort, a3, c3, 3);
                case "MMMM":
                  return h3(c3, a3);
                case "D":
                  return e2.$D;
                case "DD":
                  return b2.s(e2.$D, 2, "0");
                case "d":
                  return String(e2.$W);
                case "dd":
                  return h3(n3.weekdaysMin, e2.$W, o3, 2);
                case "ddd":
                  return h3(n3.weekdaysShort, e2.$W, o3, 3);
                case "dddd":
                  return o3[e2.$W];
                case "H":
                  return String(s3);
                case "HH":
                  return b2.s(s3, 2, "0");
                case "h":
                  return d3(1);
                case "hh":
                  return d3(2);
                case "a":
                  return $3(s3, u4, true);
                case "A":
                  return $3(s3, u4, false);
                case "m":
                  return String(u4);
                case "mm":
                  return b2.s(u4, 2, "0");
                case "s":
                  return String(e2.$s);
                case "ss":
                  return b2.s(e2.$s, 2, "0");
                case "SSS":
                  return b2.s(e2.$ms, 3, "0");
                case "Z":
                  return i3;
              }
              return null;
            })(t4) || i3.replace(":", "");
          }));
        }, m3.utcOffset = function() {
          return 15 * -Math.round(this.$d.getTimezoneOffset() / 15);
        }, m3.diff = function(r4, d3, l3) {
          var $3, y3 = this, M4 = b2.p(d3), m4 = O2(r4), v3 = (m4.utcOffset() - this.utcOffset()) * e, g3 = this - m4, D3 = function() {
            return b2.m(y3, m4);
          };
          switch (M4) {
            case h2:
              $3 = D3() / 12;
              break;
            case c2:
              $3 = D3();
              break;
            case f2:
              $3 = D3() / 3;
              break;
            case o2:
              $3 = (g3 - v3) / 6048e5;
              break;
            case a2:
              $3 = (g3 - v3) / 864e5;
              break;
            case u3:
              $3 = g3 / n2;
              break;
            case s2:
              $3 = g3 / e;
              break;
            case i2:
              $3 = g3 / t2;
              break;
            default:
              $3 = g3;
          }
          return l3 ? $3 : b2.a($3);
        }, m3.daysInMonth = function() {
          return this.endOf(c2).$D;
        }, m3.$locale = function() {
          return D2[this.$L];
        }, m3.locale = function(t3, e2) {
          if (!t3) return this.$L;
          var n3 = this.clone(), r4 = w2(t3, e2, true);
          return r4 && (n3.$L = r4), n3;
        }, m3.clone = function() {
          return b2.w(this.$d, this);
        }, m3.toDate = function() {
          return new Date(this.valueOf());
        }, m3.toJSON = function() {
          return this.isValid() ? this.toISOString() : null;
        }, m3.toISOString = function() {
          return this.$d.toISOString();
        }, m3.toString = function() {
          return this.$d.toUTCString();
        }, M3;
      })(), k2 = _2.prototype;
      return O2.prototype = k2, [["$ms", r3], ["$s", i2], ["$m", s2], ["$H", u3], ["$W", a2], ["$M", c2], ["$y", h2], ["$D", d2]].forEach((function(t3) {
        k2[t3[1]] = function(e2) {
          return this.$g(e2, t3[0], t3[1]);
        };
      })), O2.extend = function(t3, e2) {
        return t3.$i || (t3(e2, _2, O2), t3.$i = true), O2;
      }, O2.locale = w2, O2.isDayjs = S2, O2.unix = function(t3) {
        return O2(1e3 * t3);
      }, O2.en = D2[g2], O2.Ls = D2, O2.p = {}, O2;
    }));
  }
});

// node_modules/react-fast-compare/index.js
var require_react_fast_compare = __commonJS({
  "node_modules/react-fast-compare/index.js"(exports, module) {
    var hasElementType = typeof Element !== "undefined";
    var hasMap = typeof Map === "function";
    var hasSet = typeof Set === "function";
    var hasArrayBuffer = typeof ArrayBuffer === "function" && !!ArrayBuffer.isView;
    function equal(a2, b2) {
      if (a2 === b2) return true;
      if (a2 && b2 && typeof a2 == "object" && typeof b2 == "object") {
        if (a2.constructor !== b2.constructor) return false;
        var length, i2, keys3;
        if (Array.isArray(a2)) {
          length = a2.length;
          if (length != b2.length) return false;
          for (i2 = length; i2-- !== 0; )
            if (!equal(a2[i2], b2[i2])) return false;
          return true;
        }
        var it3;
        if (hasMap && a2 instanceof Map && b2 instanceof Map) {
          if (a2.size !== b2.size) return false;
          it3 = a2.entries();
          while (!(i2 = it3.next()).done)
            if (!b2.has(i2.value[0])) return false;
          it3 = a2.entries();
          while (!(i2 = it3.next()).done)
            if (!equal(i2.value[1], b2.get(i2.value[0]))) return false;
          return true;
        }
        if (hasSet && a2 instanceof Set && b2 instanceof Set) {
          if (a2.size !== b2.size) return false;
          it3 = a2.entries();
          while (!(i2 = it3.next()).done)
            if (!b2.has(i2.value[0])) return false;
          return true;
        }
        if (hasArrayBuffer && ArrayBuffer.isView(a2) && ArrayBuffer.isView(b2)) {
          length = a2.length;
          if (length != b2.length) return false;
          for (i2 = length; i2-- !== 0; )
            if (a2[i2] !== b2[i2]) return false;
          return true;
        }
        if (a2.constructor === RegExp) return a2.source === b2.source && a2.flags === b2.flags;
        if (a2.valueOf !== Object.prototype.valueOf && typeof a2.valueOf === "function" && typeof b2.valueOf === "function") return a2.valueOf() === b2.valueOf();
        if (a2.toString !== Object.prototype.toString && typeof a2.toString === "function" && typeof b2.toString === "function") return a2.toString() === b2.toString();
        keys3 = Object.keys(a2);
        length = keys3.length;
        if (length !== Object.keys(b2).length) return false;
        for (i2 = length; i2-- !== 0; )
          if (!Object.prototype.hasOwnProperty.call(b2, keys3[i2])) return false;
        if (hasElementType && a2 instanceof Element) return false;
        for (i2 = length; i2-- !== 0; ) {
          if ((keys3[i2] === "_owner" || keys3[i2] === "__v" || keys3[i2] === "__o") && a2.$$typeof) {
            continue;
          }
          if (!equal(a2[keys3[i2]], b2[keys3[i2]])) return false;
        }
        return true;
      }
      return a2 !== a2 && b2 !== b2;
    }
    module.exports = function isEqual2(a2, b2) {
      try {
        return equal(a2, b2);
      } catch (error2) {
        if ((error2.message || "").match(/stack|recursion/i)) {
          console.warn("react-fast-compare cannot handle circular refs");
          return false;
        }
        throw error2;
      }
    };
  }
});

// node_modules/emoji-regex/index.js
var require_emoji_regex = __commonJS({
  "node_modules/emoji-regex/index.js"(exports, module) {
    "use strict";
    module.exports = function() {
      return /\uD83C\uDFF4\uDB40\uDC67\uDB40\uDC62(?:\uDB40\uDC77\uDB40\uDC6C\uDB40\uDC73|\uDB40\uDC73\uDB40\uDC63\uDB40\uDC74|\uDB40\uDC65\uDB40\uDC6E\uDB40\uDC67)\uDB40\uDC7F|(?:\uD83E\uDDD1\uD83C\uDFFF\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFF\u200D\uD83E\uDD1D\u200D(?:\uD83D[\uDC68\uDC69]))(?:\uD83C[\uDFFB-\uDFFE])|(?:\uD83E\uDDD1\uD83C\uDFFE\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFE\u200D\uD83E\uDD1D\u200D(?:\uD83D[\uDC68\uDC69]))(?:\uD83C[\uDFFB-\uDFFD\uDFFF])|(?:\uD83E\uDDD1\uD83C\uDFFD\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFD\u200D\uD83E\uDD1D\u200D(?:\uD83D[\uDC68\uDC69]))(?:\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|(?:\uD83E\uDDD1\uD83C\uDFFC\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFC\u200D\uD83E\uDD1D\u200D(?:\uD83D[\uDC68\uDC69]))(?:\uD83C[\uDFFB\uDFFD-\uDFFF])|(?:\uD83E\uDDD1\uD83C\uDFFB\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFB\u200D\uD83E\uDD1D\u200D(?:\uD83D[\uDC68\uDC69]))(?:\uD83C[\uDFFC-\uDFFF])|\uD83D\uDC68(?:\uD83C\uDFFB(?:\u200D(?:\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFF])|\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFF]))|\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFC-\uDFFF])|[\u2695\u2696\u2708]\uFE0F|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD]))?|(?:\uD83C[\uDFFC-\uDFFF])\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFF])|\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFF]))|\u200D(?:\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83D\uDC68|(?:\uD83D[\uDC68\uDC69])\u200D(?:\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67]))|\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67])|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFF\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFE])|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFE\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFD\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFC\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|(?:\uD83C\uDFFF\u200D[\u2695\u2696\u2708]|\uD83C\uDFFE\u200D[\u2695\u2696\u2708]|\uD83C\uDFFD\u200D[\u2695\u2696\u2708]|\uD83C\uDFFC\u200D[\u2695\u2696\u2708]|\u200D[\u2695\u2696\u2708])\uFE0F|\u200D(?:(?:\uD83D[\uDC68\uDC69])\u200D(?:\uD83D[\uDC66\uDC67])|\uD83D[\uDC66\uDC67])|\uD83C\uDFFF|\uD83C\uDFFE|\uD83C\uDFFD|\uD83C\uDFFC)?|(?:\uD83D\uDC69(?:\uD83C\uDFFB\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D(?:\uD83D[\uDC68\uDC69])|\uD83D[\uDC68\uDC69])|(?:\uD83C[\uDFFC-\uDFFF])\u200D\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D(?:\uD83D[\uDC68\uDC69])|\uD83D[\uDC68\uDC69]))|\uD83E\uDDD1(?:\uD83C[\uDFFB-\uDFFF])\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1)(?:\uD83C[\uDFFB-\uDFFF])|\uD83D\uDC69\u200D\uD83D\uDC69\u200D(?:\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67]))|\uD83D\uDC69(?:\u200D(?:\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D(?:\uD83D[\uDC68\uDC69])|\uD83D[\uDC68\uDC69])|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFF\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFE\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFD\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFC\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFB\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD]))|\uD83E\uDDD1(?:\u200D(?:\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFF\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFE\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFD\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFC\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFB\u200D(?:\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD]))|\uD83D\uDC69\u200D\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC69\u200D\uD83D\uDC69\u200D(?:\uD83D[\uDC66\uDC67])|\uD83D\uDC69\u200D\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67])|(?:\uD83D\uDC41\uFE0F\u200D\uD83D\uDDE8|\uD83E\uDDD1(?:\uD83C\uDFFF\u200D[\u2695\u2696\u2708]|\uD83C\uDFFE\u200D[\u2695\u2696\u2708]|\uD83C\uDFFD\u200D[\u2695\u2696\u2708]|\uD83C\uDFFC\u200D[\u2695\u2696\u2708]|\uD83C\uDFFB\u200D[\u2695\u2696\u2708]|\u200D[\u2695\u2696\u2708])|\uD83D\uDC69(?:\uD83C\uDFFF\u200D[\u2695\u2696\u2708]|\uD83C\uDFFE\u200D[\u2695\u2696\u2708]|\uD83C\uDFFD\u200D[\u2695\u2696\u2708]|\uD83C\uDFFC\u200D[\u2695\u2696\u2708]|\uD83C\uDFFB\u200D[\u2695\u2696\u2708]|\u200D[\u2695\u2696\u2708])|\uD83D\uDE36\u200D\uD83C\uDF2B|\uD83C\uDFF3\uFE0F\u200D\u26A7|\uD83D\uDC3B\u200D\u2744|(?:(?:\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC70\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD35\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD4\uDDD6-\uDDDD])(?:\uD83C[\uDFFB-\uDFFF])|\uD83D\uDC6F|\uD83E[\uDD3C\uDDDE\uDDDF])\u200D[\u2640\u2642]|(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)(?:\uFE0F|\uD83C[\uDFFB-\uDFFF])\u200D[\u2640\u2642]|\uD83C\uDFF4\u200D\u2620|(?:\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC70\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD35\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD4\uDDD6-\uDDDD])\u200D[\u2640\u2642]|[\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u2328\u23CF\u23ED-\u23EF\u23F1\u23F2\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB\u25FC\u2600-\u2604\u260E\u2611\u2618\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u2692\u2694-\u2697\u2699\u269B\u269C\u26A0\u26A7\u26B0\u26B1\u26C8\u26CF\u26D1\u26D3\u26E9\u26F0\u26F1\u26F4\u26F7\u26F8\u2702\u2708\u2709\u270F\u2712\u2714\u2716\u271D\u2721\u2733\u2734\u2744\u2747\u2763\u27A1\u2934\u2935\u2B05-\u2B07\u3030\u303D\u3297\u3299]|\uD83C[\uDD70\uDD71\uDD7E\uDD7F\uDE02\uDE37\uDF21\uDF24-\uDF2C\uDF36\uDF7D\uDF96\uDF97\uDF99-\uDF9B\uDF9E\uDF9F\uDFCD\uDFCE\uDFD4-\uDFDF\uDFF5\uDFF7]|\uD83D[\uDC3F\uDCFD\uDD49\uDD4A\uDD6F\uDD70\uDD73\uDD76-\uDD79\uDD87\uDD8A-\uDD8D\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA\uDECB\uDECD-\uDECF\uDEE0-\uDEE5\uDEE9\uDEF0\uDEF3])\uFE0F|\uD83C\uDFF3\uFE0F\u200D\uD83C\uDF08|\uD83D\uDC69\u200D\uD83D\uDC67|\uD83D\uDC69\u200D\uD83D\uDC66|\uD83D\uDE35\u200D\uD83D\uDCAB|\uD83D\uDE2E\u200D\uD83D\uDCA8|\uD83D\uDC15\u200D\uD83E\uDDBA|\uD83E\uDDD1(?:\uD83C\uDFFF|\uD83C\uDFFE|\uD83C\uDFFD|\uD83C\uDFFC|\uD83C\uDFFB)?|\uD83D\uDC69(?:\uD83C\uDFFF|\uD83C\uDFFE|\uD83C\uDFFD|\uD83C\uDFFC|\uD83C\uDFFB)?|\uD83C\uDDFD\uD83C\uDDF0|\uD83C\uDDF6\uD83C\uDDE6|\uD83C\uDDF4\uD83C\uDDF2|\uD83D\uDC08\u200D\u2B1B|\u2764\uFE0F\u200D(?:\uD83D\uDD25|\uD83E\uDE79)|\uD83D\uDC41\uFE0F|\uD83C\uDFF3\uFE0F|\uD83C\uDDFF(?:\uD83C[\uDDE6\uDDF2\uDDFC])|\uD83C\uDDFE(?:\uD83C[\uDDEA\uDDF9])|\uD83C\uDDFC(?:\uD83C[\uDDEB\uDDF8])|\uD83C\uDDFB(?:\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA])|\uD83C\uDDFA(?:\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF])|\uD83C\uDDF9(?:\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF])|\uD83C\uDDF8(?:\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF])|\uD83C\uDDF7(?:\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC])|\uD83C\uDDF5(?:\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE])|\uD83C\uDDF3(?:\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF])|\uD83C\uDDF2(?:\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF])|\uD83C\uDDF1(?:\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE])|\uD83C\uDDF0(?:\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF])|\uD83C\uDDEF(?:\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5])|\uD83C\uDDEE(?:\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9])|\uD83C\uDDED(?:\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA])|\uD83C\uDDEC(?:\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE])|\uD83C\uDDEB(?:\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7])|\uD83C\uDDEA(?:\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA])|\uD83C\uDDE9(?:\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF])|\uD83C\uDDE8(?:\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF5\uDDF7\uDDFA-\uDDFF])|\uD83C\uDDE7(?:\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF])|\uD83C\uDDE6(?:\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF])|[#\*0-9]\uFE0F\u20E3|\u2764\uFE0F|(?:\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC70\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD35\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD4\uDDD6-\uDDDD])(?:\uD83C[\uDFFB-\uDFFF])|(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)(?:\uFE0F|\uD83C[\uDFFB-\uDFFF])|\uD83C\uDFF4|(?:[\u270A\u270B]|\uD83C[\uDF85\uDFC2\uDFC7]|\uD83D[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDC8F\uDC91\uDCAA\uDD7A\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC]|\uD83E[\uDD0C\uDD0F\uDD18-\uDD1C\uDD1E\uDD1F\uDD30-\uDD34\uDD36\uDD77\uDDB5\uDDB6\uDDBB\uDDD2\uDDD3\uDDD5])(?:\uD83C[\uDFFB-\uDFFF])|(?:[\u261D\u270C\u270D]|\uD83D[\uDD74\uDD90])(?:\uFE0F|\uD83C[\uDFFB-\uDFFF])|[\u270A\u270B]|\uD83C[\uDF85\uDFC2\uDFC7]|\uD83D[\uDC08\uDC15\uDC3B\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDC8F\uDC91\uDCAA\uDD7A\uDD95\uDD96\uDE2E\uDE35\uDE36\uDE4C\uDE4F\uDEC0\uDECC]|\uD83E[\uDD0C\uDD0F\uDD18-\uDD1C\uDD1E\uDD1F\uDD30-\uDD34\uDD36\uDD77\uDDB5\uDDB6\uDDBB\uDDD2\uDDD3\uDDD5]|\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC70\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD35\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD4\uDDD6-\uDDDD]|\uD83D\uDC6F|\uD83E[\uDD3C\uDDDE\uDDDF]|[\u231A\u231B\u23E9-\u23EC\u23F0\u23F3\u25FD\u25FE\u2614\u2615\u2648-\u2653\u267F\u2693\u26A1\u26AA\u26AB\u26BD\u26BE\u26C4\u26C5\u26CE\u26D4\u26EA\u26F2\u26F3\u26F5\u26FA\u26FD\u2705\u2728\u274C\u274E\u2753-\u2755\u2757\u2795-\u2797\u27B0\u27BF\u2B1B\u2B1C\u2B50\u2B55]|\uD83C[\uDC04\uDCCF\uDD8E\uDD91-\uDD9A\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF7C\uDF7E-\uDF84\uDF86-\uDF93\uDFA0-\uDFC1\uDFC5\uDFC6\uDFC8\uDFC9\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF8-\uDFFF]|\uD83D[\uDC00-\uDC07\uDC09-\uDC14\uDC16-\uDC3A\uDC3C-\uDC3E\uDC40\uDC44\uDC45\uDC51-\uDC65\uDC6A\uDC79-\uDC7B\uDC7D-\uDC80\uDC84\uDC88-\uDC8E\uDC90\uDC92-\uDCA9\uDCAB-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDDA4\uDDFB-\uDE2D\uDE2F-\uDE34\uDE37-\uDE44\uDE48-\uDE4A\uDE80-\uDEA2\uDEA4-\uDEB3\uDEB7-\uDEBF\uDEC1-\uDEC5\uDED0-\uDED2\uDED5-\uDED7\uDEEB\uDEEC\uDEF4-\uDEFC\uDFE0-\uDFEB]|\uD83E[\uDD0D\uDD0E\uDD10-\uDD17\uDD1D\uDD20-\uDD25\uDD27-\uDD2F\uDD3A\uDD3F-\uDD45\uDD47-\uDD76\uDD78\uDD7A-\uDDB4\uDDB7\uDDBA\uDDBC-\uDDCB\uDDD0\uDDE0-\uDDFF\uDE70-\uDE74\uDE78-\uDE7A\uDE80-\uDE86\uDE90-\uDEA8\uDEB0-\uDEB6\uDEC0-\uDEC2\uDED0-\uDED6]|(?:[\u231A\u231B\u23E9-\u23EC\u23F0\u23F3\u25FD\u25FE\u2614\u2615\u2648-\u2653\u267F\u2693\u26A1\u26AA\u26AB\u26BD\u26BE\u26C4\u26C5\u26CE\u26D4\u26EA\u26F2\u26F3\u26F5\u26FA\u26FD\u2705\u270A\u270B\u2728\u274C\u274E\u2753-\u2755\u2757\u2795-\u2797\u27B0\u27BF\u2B1B\u2B1C\u2B50\u2B55]|\uD83C[\uDC04\uDCCF\uDD8E\uDD91-\uDD9A\uDDE6-\uDDFF\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF7C\uDF7E-\uDF93\uDFA0-\uDFCA\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF4\uDFF8-\uDFFF]|\uD83D[\uDC00-\uDC3E\uDC40\uDC42-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDD7A\uDD95\uDD96\uDDA4\uDDFB-\uDE4F\uDE80-\uDEC5\uDECC\uDED0-\uDED2\uDED5-\uDED7\uDEEB\uDEEC\uDEF4-\uDEFC\uDFE0-\uDFEB]|\uD83E[\uDD0C-\uDD3A\uDD3C-\uDD45\uDD47-\uDD78\uDD7A-\uDDCB\uDDCD-\uDDFF\uDE70-\uDE74\uDE78-\uDE7A\uDE80-\uDE86\uDE90-\uDEA8\uDEB0-\uDEB6\uDEC0-\uDEC2\uDED0-\uDED6])|(?:[#\*0-9\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23E9-\u23F3\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB-\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u261D\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692-\u2697\u2699\u269B\u269C\u26A0\u26A1\u26A7\u26AA\u26AB\u26B0\u26B1\u26BD\u26BE\u26C4\u26C5\u26C8\u26CE\u26CF\u26D1\u26D3\u26D4\u26E9\u26EA\u26F0-\u26F5\u26F7-\u26FA\u26FD\u2702\u2705\u2708-\u270D\u270F\u2712\u2714\u2716\u271D\u2721\u2728\u2733\u2734\u2744\u2747\u274C\u274E\u2753-\u2755\u2757\u2763\u2764\u2795-\u2797\u27A1\u27B0\u27BF\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B50\u2B55\u3030\u303D\u3297\u3299]|\uD83C[\uDC04\uDCCF\uDD70\uDD71\uDD7E\uDD7F\uDD8E\uDD91-\uDD9A\uDDE6-\uDDFF\uDE01\uDE02\uDE1A\uDE2F\uDE32-\uDE3A\uDE50\uDE51\uDF00-\uDF21\uDF24-\uDF93\uDF96\uDF97\uDF99-\uDF9B\uDF9E-\uDFF0\uDFF3-\uDFF5\uDFF7-\uDFFF]|\uD83D[\uDC00-\uDCFD\uDCFF-\uDD3D\uDD49-\uDD4E\uDD50-\uDD67\uDD6F\uDD70\uDD73-\uDD7A\uDD87\uDD8A-\uDD8D\uDD90\uDD95\uDD96\uDDA4\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA-\uDE4F\uDE80-\uDEC5\uDECB-\uDED2\uDED5-\uDED7\uDEE0-\uDEE5\uDEE9\uDEEB\uDEEC\uDEF0\uDEF3-\uDEFC\uDFE0-\uDFEB]|\uD83E[\uDD0C-\uDD3A\uDD3C-\uDD45\uDD47-\uDD78\uDD7A-\uDDCB\uDDCD-\uDDFF\uDE70-\uDE74\uDE78-\uDE7A\uDE80-\uDE86\uDE90-\uDEA8\uDEB0-\uDEB6\uDEC0-\uDEC2\uDED0-\uDED6])\uFE0F|(?:[\u261D\u26F9\u270A-\u270D]|\uD83C[\uDF85\uDFC2-\uDFC4\uDFC7\uDFCA-\uDFCC]|\uD83D[\uDC42\uDC43\uDC46-\uDC50\uDC66-\uDC78\uDC7C\uDC81-\uDC83\uDC85-\uDC87\uDC8F\uDC91\uDCAA\uDD74\uDD75\uDD7A\uDD90\uDD95\uDD96\uDE45-\uDE47\uDE4B-\uDE4F\uDEA3\uDEB4-\uDEB6\uDEC0\uDECC]|\uD83E[\uDD0C\uDD0F\uDD18-\uDD1F\uDD26\uDD30-\uDD39\uDD3C-\uDD3E\uDD77\uDDB5\uDDB6\uDDB8\uDDB9\uDDBB\uDDCD-\uDDCF\uDDD1-\uDDDD])/g;
    };
  }
});

// node_modules/@braintree/sanitize-url/dist/index.js
var require_dist = __commonJS({
  "node_modules/@braintree/sanitize-url/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.sanitizeUrl = exports.BLANK_URL = void 0;
    var invalidProtocolRegex = /^([^\w]*)(javascript|data|vbscript)/im;
    var htmlEntitiesRegex = /&#(\w+)(^\w|;)?/g;
    var htmlCtrlEntityRegex = /&(newline|tab);/gi;
    var ctrlCharactersRegex = /[\u0000-\u001F\u007F-\u009F\u2000-\u200D\uFEFF]/gim;
    var urlSchemeRegex = /^.+(:|&colon;)/gim;
    var relativeFirstCharacters = [".", "/"];
    exports.BLANK_URL = "about:blank";
    function isRelativeUrlWithoutProtocol(url) {
      return relativeFirstCharacters.indexOf(url[0]) > -1;
    }
    function decodeHtmlCharacters(str) {
      var removedNullByte = str.replace(ctrlCharactersRegex, "");
      return removedNullByte.replace(htmlEntitiesRegex, function(match, dec) {
        return String.fromCharCode(dec);
      });
    }
    function sanitizeUrl2(url) {
      if (!url) {
        return exports.BLANK_URL;
      }
      var sanitizedUrl = decodeHtmlCharacters(url).replace(htmlCtrlEntityRegex, "").replace(ctrlCharactersRegex, "").trim();
      if (!sanitizedUrl) {
        return exports.BLANK_URL;
      }
      if (isRelativeUrlWithoutProtocol(sanitizedUrl)) {
        return sanitizedUrl;
      }
      var urlSchemeParseResults = sanitizedUrl.match(urlSchemeRegex);
      if (!urlSchemeParseResults) {
        return sanitizedUrl;
      }
      var urlScheme = urlSchemeParseResults[0];
      if (invalidProtocolRegex.test(urlScheme)) {
        return exports.BLANK_URL;
      }
      return sanitizedUrl;
    }
    exports.sanitizeUrl = sanitizeUrl2;
  }
});

// node_modules/load-script/index.js
var require_load_script = __commonJS({
  "node_modules/load-script/index.js"(exports, module) {
    module.exports = function load(src, opts, cb) {
      var head = document.head || document.getElementsByTagName("head")[0];
      var script = document.createElement("script");
      if (typeof opts === "function") {
        cb = opts;
        opts = {};
      }
      opts = opts || {};
      cb = cb || function() {
      };
      script.type = opts.type || "text/javascript";
      script.charset = opts.charset || "utf8";
      script.async = "async" in opts ? !!opts.async : true;
      script.src = src;
      if (opts.attrs) {
        setAttributes(script, opts.attrs);
      }
      if (opts.text) {
        script.text = "" + opts.text;
      }
      var onend = "onload" in script ? stdOnEnd : ieOnEnd;
      onend(script, cb);
      if (!script.onload) {
        stdOnEnd(script, cb);
      }
      head.appendChild(script);
    };
    function setAttributes(script, attrs) {
      for (var attr2 in attrs) {
        script.setAttribute(attr2, attrs[attr2]);
      }
    }
    function stdOnEnd(script, cb) {
      script.onload = function() {
        this.onerror = this.onload = null;
        cb(null, script);
      };
      script.onerror = function() {
        this.onerror = this.onload = null;
        cb(new Error("Failed to load " + this.src), script);
      };
    }
    function ieOnEnd(script, cb) {
      script.onreadystatechange = function() {
        if (this.readyState != "complete" && this.readyState != "loaded") return;
        this.onreadystatechange = null;
        cb(null, script);
      };
    }
  }
});

// node_modules/deepmerge/dist/cjs.js
var require_cjs = __commonJS({
  "node_modules/deepmerge/dist/cjs.js"(exports, module) {
    "use strict";
    var isMergeableObject = function isMergeableObject2(value) {
      return isNonNullObject(value) && !isSpecial(value);
    };
    function isNonNullObject(value) {
      return !!value && typeof value === "object";
    }
    function isSpecial(value) {
      var stringValue = Object.prototype.toString.call(value);
      return stringValue === "[object RegExp]" || stringValue === "[object Date]" || isReactElement(value);
    }
    var canUseSymbol = typeof Symbol === "function" && Symbol.for;
    var REACT_ELEMENT_TYPE = canUseSymbol ? /* @__PURE__ */ Symbol.for("react.element") : 60103;
    function isReactElement(value) {
      return value.$$typeof === REACT_ELEMENT_TYPE;
    }
    function emptyTarget(val) {
      return Array.isArray(val) ? [] : {};
    }
    function cloneUnlessOtherwiseSpecified(value, options) {
      return options.clone !== false && options.isMergeableObject(value) ? deepmerge(emptyTarget(value), value, options) : value;
    }
    function defaultArrayMerge(target, source, options) {
      return target.concat(source).map(function(element4) {
        return cloneUnlessOtherwiseSpecified(element4, options);
      });
    }
    function getMergeFunction(key, options) {
      if (!options.customMerge) {
        return deepmerge;
      }
      var customMerge = options.customMerge(key);
      return typeof customMerge === "function" ? customMerge : deepmerge;
    }
    function getEnumerableOwnPropertySymbols(target) {
      return Object.getOwnPropertySymbols ? Object.getOwnPropertySymbols(target).filter(function(symbol) {
        return Object.propertyIsEnumerable.call(target, symbol);
      }) : [];
    }
    function getKeys(target) {
      return Object.keys(target).concat(getEnumerableOwnPropertySymbols(target));
    }
    function propertyIsOnObject(object, property) {
      try {
        return property in object;
      } catch (_2) {
        return false;
      }
    }
    function propertyIsUnsafe(target, key) {
      return propertyIsOnObject(target, key) && !(Object.hasOwnProperty.call(target, key) && Object.propertyIsEnumerable.call(target, key));
    }
    function mergeObject(target, source, options) {
      var destination = {};
      if (options.isMergeableObject(target)) {
        getKeys(target).forEach(function(key) {
          destination[key] = cloneUnlessOtherwiseSpecified(target[key], options);
        });
      }
      getKeys(source).forEach(function(key) {
        if (propertyIsUnsafe(target, key)) {
          return;
        }
        if (propertyIsOnObject(target, key) && options.isMergeableObject(source[key])) {
          destination[key] = getMergeFunction(key, options)(target[key], source[key], options);
        } else {
          destination[key] = cloneUnlessOtherwiseSpecified(source[key], options);
        }
      });
      return destination;
    }
    function deepmerge(target, source, options) {
      options = options || {};
      options.arrayMerge = options.arrayMerge || defaultArrayMerge;
      options.isMergeableObject = options.isMergeableObject || isMergeableObject;
      options.cloneUnlessOtherwiseSpecified = cloneUnlessOtherwiseSpecified;
      var sourceIsArray = Array.isArray(source);
      var targetIsArray = Array.isArray(target);
      var sourceAndTargetTypesMatch = sourceIsArray === targetIsArray;
      if (!sourceAndTargetTypesMatch) {
        return cloneUnlessOtherwiseSpecified(source, options);
      } else if (sourceIsArray) {
        return options.arrayMerge(target, source, options);
      } else {
        return mergeObject(target, source, options);
      }
    }
    deepmerge.all = function deepmergeAll(array, options) {
      if (!Array.isArray(array)) {
        throw new Error("first argument should be an array");
      }
      return array.reduce(function(prev, next) {
        return deepmerge(prev, next, options);
      }, {});
    };
    var deepmerge_1 = deepmerge;
    module.exports = deepmerge_1;
  }
});

// node_modules/react-player/lib/utils.js
var require_utils = __commonJS({
  "node_modules/react-player/lib/utils.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.parseStartTime = parseStartTime;
    exports.parseEndTime = parseEndTime;
    exports.randomString = randomString;
    exports.queryString = queryString;
    exports.getSDK = getSDK;
    exports.getConfig = getConfig;
    exports.omit = omit2;
    exports.callPlayer = callPlayer;
    exports.isMediaStream = isMediaStream;
    exports.isBlobUrl = isBlobUrl;
    exports.supportsWebKitPresentationMode = supportsWebKitPresentationMode;
    var _loadScript = _interopRequireDefault(require_load_script());
    var _deepmerge = _interopRequireDefault(require_cjs());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    function _slicedToArray3(arr, i2) {
      return _arrayWithHoles3(arr) || _iterableToArrayLimit3(arr, i2) || _unsupportedIterableToArray4(arr, i2) || _nonIterableRest3();
    }
    function _nonIterableRest3() {
      throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    function _unsupportedIterableToArray4(o2, minLen) {
      if (!o2) return;
      if (typeof o2 === "string") return _arrayLikeToArray4(o2, minLen);
      var n2 = Object.prototype.toString.call(o2).slice(8, -1);
      if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
      if (n2 === "Map" || n2 === "Set") return Array.from(o2);
      if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray4(o2, minLen);
    }
    function _arrayLikeToArray4(arr, len) {
      if (len == null || len > arr.length) len = arr.length;
      for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) {
        arr2[i2] = arr[i2];
      }
      return arr2;
    }
    function _iterableToArrayLimit3(arr, i2) {
      if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return;
      var _arr = [];
      var _n = true;
      var _d = false;
      var _e2 = void 0;
      try {
        for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
          _arr.push(_s.value);
          if (i2 && _arr.length === i2) break;
        }
      } catch (err) {
        _d = true;
        _e2 = err;
      } finally {
        try {
          if (!_n && _i["return"] != null) _i["return"]();
        } finally {
          if (_d) throw _e2;
        }
      }
      return _arr;
    }
    function _arrayWithHoles3(arr) {
      if (Array.isArray(arr)) return arr;
    }
    var MATCH_START_QUERY = /[?&#](?:start|t)=([0-9hms]+)/;
    var MATCH_END_QUERY = /[?&#]end=([0-9hms]+)/;
    var MATCH_START_STAMP = /(\d+)(h|m|s)/g;
    var MATCH_NUMERIC = /^\d+$/;
    function parseTimeParam(url, pattern) {
      if (url instanceof Array) {
        return void 0;
      }
      var match = url.match(pattern);
      if (match) {
        var stamp = match[1];
        if (stamp.match(MATCH_START_STAMP)) {
          return parseTimeString(stamp);
        }
        if (MATCH_NUMERIC.test(stamp)) {
          return parseInt(stamp);
        }
      }
      return void 0;
    }
    function parseTimeString(stamp) {
      var seconds = 0;
      var array = MATCH_START_STAMP.exec(stamp);
      while (array !== null) {
        var _array = array, _array2 = _slicedToArray3(_array, 3), count2 = _array2[1], period = _array2[2];
        if (period === "h") seconds += parseInt(count2, 10) * 60 * 60;
        if (period === "m") seconds += parseInt(count2, 10) * 60;
        if (period === "s") seconds += parseInt(count2, 10);
        array = MATCH_START_STAMP.exec(stamp);
      }
      return seconds;
    }
    function parseStartTime(url) {
      return parseTimeParam(url, MATCH_START_QUERY);
    }
    function parseEndTime(url) {
      return parseTimeParam(url, MATCH_END_QUERY);
    }
    function randomString() {
      return Math.random().toString(36).substr(2, 5);
    }
    function queryString(object) {
      return Object.keys(object).map(function(key) {
        return "".concat(key, "=").concat(object[key]);
      }).join("&");
    }
    function getGlobal(key) {
      if (window[key]) {
        return window[key];
      }
      if (window.exports && window.exports[key]) {
        return window.exports[key];
      }
      if (window.module && window.module.exports && window.module.exports[key]) {
        return window.module.exports[key];
      }
      return null;
    }
    var requests = {};
    function getSDK(url, sdkGlobal) {
      var sdkReady = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
      var isLoaded = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : function() {
        return true;
      };
      var fetchScript = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : _loadScript["default"];
      var existingGlobal = getGlobal(sdkGlobal);
      if (existingGlobal && isLoaded(existingGlobal)) {
        return Promise.resolve(existingGlobal);
      }
      return new Promise(function(resolve, reject) {
        if (requests[url]) {
          requests[url].push({
            resolve,
            reject
          });
          return;
        }
        requests[url] = [{
          resolve,
          reject
        }];
        var onLoaded = function onLoaded2(sdk) {
          requests[url].forEach(function(request) {
            return request.resolve(sdk);
          });
        };
        if (sdkReady) {
          var previousOnReady = window[sdkReady];
          window[sdkReady] = function() {
            if (previousOnReady) previousOnReady();
            onLoaded(getGlobal(sdkGlobal));
          };
        }
        fetchScript(url, function(err) {
          if (err) {
            requests[url].forEach(function(request) {
              return request.reject(err);
            });
            requests[url] = null;
          } else if (!sdkReady) {
            onLoaded(getGlobal(sdkGlobal));
          }
        });
      });
    }
    function getConfig(props, defaultProps2) {
      return (0, _deepmerge["default"])(defaultProps2.config, props.config);
    }
    function omit2(object) {
      var _ref;
      for (var _len = arguments.length, arrays = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        arrays[_key - 1] = arguments[_key];
      }
      var omitKeys = (_ref = []).concat.apply(_ref, arrays);
      var output = {};
      var keys3 = Object.keys(object);
      for (var _i2 = 0, _keys = keys3; _i2 < _keys.length; _i2++) {
        var key = _keys[_i2];
        if (omitKeys.indexOf(key) === -1) {
          output[key] = object[key];
        }
      }
      return output;
    }
    function callPlayer(method) {
      var _this$player;
      if (!this.player || !this.player[method]) {
        var message = "ReactPlayer: ".concat(this.constructor.displayName, " player could not call %c").concat(method, "%c – ");
        if (!this.player) {
          message += "The player was not available";
        } else if (!this.player[method]) {
          message += "The method was not available";
        }
        console.warn(message, "font-weight: bold", "");
        return null;
      }
      for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
        args[_key2 - 1] = arguments[_key2];
      }
      return (_this$player = this.player)[method].apply(_this$player, args);
    }
    function isMediaStream(url) {
      return typeof window !== "undefined" && typeof window.MediaStream !== "undefined" && url instanceof window.MediaStream;
    }
    function isBlobUrl(url) {
      return /^blob:/.test(url);
    }
    function supportsWebKitPresentationMode() {
      var video = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : document.createElement("video");
      var notMobile = /iPhone|iPod/.test(navigator.userAgent) === false;
      return video.webkitSupportsPresentationMode && typeof video.webkitSetPresentationMode === "function" && notMobile;
    }
  }
});

// node_modules/react-player/lib/patterns.js
var require_patterns = __commonJS({
  "node_modules/react-player/lib/patterns.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.canPlay = exports.FLV_EXTENSIONS = exports.DASH_EXTENSIONS = exports.HLS_EXTENSIONS = exports.VIDEO_EXTENSIONS = exports.AUDIO_EXTENSIONS = exports.MATCH_URL_KALTURA = exports.MATCH_URL_VIDYARD = exports.MATCH_URL_MIXCLOUD = exports.MATCH_URL_DAILYMOTION = exports.MATCH_URL_TWITCH_CHANNEL = exports.MATCH_URL_TWITCH_VIDEO = exports.MATCH_URL_WISTIA = exports.MATCH_URL_STREAMABLE = exports.MATCH_URL_FACEBOOK_WATCH = exports.MATCH_URL_FACEBOOK = exports.MATCH_URL_VIMEO = exports.MATCH_URL_SOUNDCLOUD = exports.MATCH_URL_YOUTUBE = void 0;
    var _utils = require_utils();
    function _createForOfIteratorHelper(o2, allowArrayLike) {
      var it3;
      if (typeof Symbol === "undefined" || o2[Symbol.iterator] == null) {
        if (Array.isArray(o2) || (it3 = _unsupportedIterableToArray4(o2)) || allowArrayLike && o2 && typeof o2.length === "number") {
          if (it3) o2 = it3;
          var i2 = 0;
          var F2 = function F3() {
          };
          return { s: F2, n: function n2() {
            if (i2 >= o2.length) return { done: true };
            return { done: false, value: o2[i2++] };
          }, e: function e(_e2) {
            throw _e2;
          }, f: F2 };
        }
        throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
      }
      var normalCompletion = true, didErr = false, err;
      return { s: function s2() {
        it3 = o2[Symbol.iterator]();
      }, n: function n2() {
        var step = it3.next();
        normalCompletion = step.done;
        return step;
      }, e: function e(_e2) {
        didErr = true;
        err = _e2;
      }, f: function f2() {
        try {
          if (!normalCompletion && it3["return"] != null) it3["return"]();
        } finally {
          if (didErr) throw err;
        }
      } };
    }
    function _unsupportedIterableToArray4(o2, minLen) {
      if (!o2) return;
      if (typeof o2 === "string") return _arrayLikeToArray4(o2, minLen);
      var n2 = Object.prototype.toString.call(o2).slice(8, -1);
      if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
      if (n2 === "Map" || n2 === "Set") return Array.from(o2);
      if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray4(o2, minLen);
    }
    function _arrayLikeToArray4(arr, len) {
      if (len == null || len > arr.length) len = arr.length;
      for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) {
        arr2[i2] = arr[i2];
      }
      return arr2;
    }
    var MATCH_URL_YOUTUBE = /(?:youtu\.be\/|youtube(?:-nocookie)?\.com\/(?:embed\/|v\/|watch\/|watch\?v=|watch\?.+&v=|shorts\/))((\w|-){11})|youtube\.com\/playlist\?list=|youtube\.com\/user\//;
    exports.MATCH_URL_YOUTUBE = MATCH_URL_YOUTUBE;
    var MATCH_URL_SOUNDCLOUD = /(?:soundcloud\.com|snd\.sc)\/[^.]+$/;
    exports.MATCH_URL_SOUNDCLOUD = MATCH_URL_SOUNDCLOUD;
    var MATCH_URL_VIMEO = /vimeo\.com\/(?!progressive_redirect).+/;
    exports.MATCH_URL_VIMEO = MATCH_URL_VIMEO;
    var MATCH_URL_FACEBOOK = /^https?:\/\/(www\.)?facebook\.com.*\/(video(s)?|watch|story)(\.php?|\/).+$/;
    exports.MATCH_URL_FACEBOOK = MATCH_URL_FACEBOOK;
    var MATCH_URL_FACEBOOK_WATCH = /^https?:\/\/fb\.watch\/.+$/;
    exports.MATCH_URL_FACEBOOK_WATCH = MATCH_URL_FACEBOOK_WATCH;
    var MATCH_URL_STREAMABLE = /streamable\.com\/([a-z0-9]+)$/;
    exports.MATCH_URL_STREAMABLE = MATCH_URL_STREAMABLE;
    var MATCH_URL_WISTIA = /(?:wistia\.(?:com|net)|wi\.st)\/(?:medias|embed)\/(?:iframe\/)?(.*)$/;
    exports.MATCH_URL_WISTIA = MATCH_URL_WISTIA;
    var MATCH_URL_TWITCH_VIDEO = /(?:www\.|go\.)?twitch\.tv\/videos\/(\d+)($|\?)/;
    exports.MATCH_URL_TWITCH_VIDEO = MATCH_URL_TWITCH_VIDEO;
    var MATCH_URL_TWITCH_CHANNEL = /(?:www\.|go\.)?twitch\.tv\/([a-zA-Z0-9_]+)($|\?)/;
    exports.MATCH_URL_TWITCH_CHANNEL = MATCH_URL_TWITCH_CHANNEL;
    var MATCH_URL_DAILYMOTION = /^(?:(?:https?):)?(?:\/\/)?(?:www\.)?(?:(?:dailymotion\.com(?:\/embed)?\/video)|dai\.ly)\/([a-zA-Z0-9]+)(?:_[\w_-]+)?$/;
    exports.MATCH_URL_DAILYMOTION = MATCH_URL_DAILYMOTION;
    var MATCH_URL_MIXCLOUD = /mixcloud\.com\/([^/]+\/[^/]+)/;
    exports.MATCH_URL_MIXCLOUD = MATCH_URL_MIXCLOUD;
    var MATCH_URL_VIDYARD = /vidyard.com\/(?:watch\/)?([a-zA-Z0-9-_]+)/;
    exports.MATCH_URL_VIDYARD = MATCH_URL_VIDYARD;
    var MATCH_URL_KALTURA = /^https?:\/\/[a-zA-Z]+\.kaltura.(com|org)\/p\/([0-9]+)\/sp\/([0-9]+)00\/embedIframeJs\/uiconf_id\/([0-9]+)\/partner_id\/([0-9]+)(.*)entry_id.([a-zA-Z0-9-_].*)$/;
    exports.MATCH_URL_KALTURA = MATCH_URL_KALTURA;
    var AUDIO_EXTENSIONS = /\.(m4a|m4b|mp4a|mpga|mp2|mp2a|mp3|m2a|m3a|wav|weba|aac|oga|spx)($|\?)/i;
    exports.AUDIO_EXTENSIONS = AUDIO_EXTENSIONS;
    var VIDEO_EXTENSIONS = /\.(mp4|og[gv]|webm|mov|m4v)(#t=[,\d+]+)?($|\?)/i;
    exports.VIDEO_EXTENSIONS = VIDEO_EXTENSIONS;
    var HLS_EXTENSIONS = /\.(m3u8)($|\?)/i;
    exports.HLS_EXTENSIONS = HLS_EXTENSIONS;
    var DASH_EXTENSIONS = /\.(mpd)($|\?)/i;
    exports.DASH_EXTENSIONS = DASH_EXTENSIONS;
    var FLV_EXTENSIONS = /\.(flv)($|\?)/i;
    exports.FLV_EXTENSIONS = FLV_EXTENSIONS;
    var canPlayFile = function canPlayFile2(url) {
      if (url instanceof Array) {
        var _iterator = _createForOfIteratorHelper(url), _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done; ) {
            var item = _step.value;
            if (typeof item === "string" && canPlayFile2(item)) {
              return true;
            }
            if (canPlayFile2(item.src)) {
              return true;
            }
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
        return false;
      }
      if ((0, _utils.isMediaStream)(url) || (0, _utils.isBlobUrl)(url)) {
        return true;
      }
      return AUDIO_EXTENSIONS.test(url) || VIDEO_EXTENSIONS.test(url) || HLS_EXTENSIONS.test(url) || DASH_EXTENSIONS.test(url) || FLV_EXTENSIONS.test(url);
    };
    var canPlay = {
      youtube: function youtube(url) {
        if (url instanceof Array) {
          return url.every(function(item) {
            return MATCH_URL_YOUTUBE.test(item);
          });
        }
        return MATCH_URL_YOUTUBE.test(url);
      },
      soundcloud: function soundcloud(url) {
        return MATCH_URL_SOUNDCLOUD.test(url) && !AUDIO_EXTENSIONS.test(url);
      },
      vimeo: function vimeo(url) {
        return MATCH_URL_VIMEO.test(url) && !VIDEO_EXTENSIONS.test(url) && !HLS_EXTENSIONS.test(url);
      },
      facebook: function facebook(url) {
        return MATCH_URL_FACEBOOK.test(url) || MATCH_URL_FACEBOOK_WATCH.test(url);
      },
      streamable: function streamable(url) {
        return MATCH_URL_STREAMABLE.test(url);
      },
      wistia: function wistia(url) {
        return MATCH_URL_WISTIA.test(url);
      },
      twitch: function twitch(url) {
        return MATCH_URL_TWITCH_VIDEO.test(url) || MATCH_URL_TWITCH_CHANNEL.test(url);
      },
      dailymotion: function dailymotion(url) {
        return MATCH_URL_DAILYMOTION.test(url);
      },
      mixcloud: function mixcloud(url) {
        return MATCH_URL_MIXCLOUD.test(url);
      },
      vidyard: function vidyard(url) {
        return MATCH_URL_VIDYARD.test(url);
      },
      kaltura: function kaltura(url) {
        return MATCH_URL_KALTURA.test(url);
      },
      file: canPlayFile
    };
    exports.canPlay = canPlay;
  }
});

// node_modules/react-player/lib/players/YouTube.js
var require_YouTube = __commonJS({
  "node_modules/react-player/lib/players/YouTube.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _slicedToArray3(arr, i2) {
      return _arrayWithHoles3(arr) || _iterableToArrayLimit3(arr, i2) || _unsupportedIterableToArray4(arr, i2) || _nonIterableRest3();
    }
    function _nonIterableRest3() {
      throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    function _unsupportedIterableToArray4(o2, minLen) {
      if (!o2) return;
      if (typeof o2 === "string") return _arrayLikeToArray4(o2, minLen);
      var n2 = Object.prototype.toString.call(o2).slice(8, -1);
      if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
      if (n2 === "Map" || n2 === "Set") return Array.from(o2);
      if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray4(o2, minLen);
    }
    function _arrayLikeToArray4(arr, len) {
      if (len == null || len > arr.length) len = arr.length;
      for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) {
        arr2[i2] = arr[i2];
      }
      return arr2;
    }
    function _iterableToArrayLimit3(arr, i2) {
      if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return;
      var _arr = [];
      var _n = true;
      var _d = false;
      var _e2 = void 0;
      try {
        for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
          _arr.push(_s.value);
          if (i2 && _arr.length === i2) break;
        }
      } catch (err) {
        _d = true;
        _e2 = err;
      } finally {
        try {
          if (!_n && _i["return"] != null) _i["return"]();
        } finally {
          if (_d) throw _e2;
        }
      }
      return _arr;
    }
    function _arrayWithHoles3(arr) {
      if (Array.isArray(arr)) return arr;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://www.youtube.com/iframe_api";
    var SDK_GLOBAL = "YT";
    var SDK_GLOBAL_READY = "onYouTubeIframeAPIReady";
    var MATCH_PLAYLIST = /[?&](?:list|channel)=([a-zA-Z0-9_-]+)/;
    var MATCH_USER_UPLOADS = /user\/([a-zA-Z0-9_-]+)\/?/;
    var MATCH_NOCOOKIE = /youtube-nocookie\.com/;
    var NOCOOKIE_HOST = "https://www.youtube-nocookie.com";
    var YouTube = (function(_Component) {
      _inherits(YouTube2, _Component);
      var _super = _createSuper(YouTube2);
      function YouTube2() {
        var _this;
        _classCallCheck(this, YouTube2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "parsePlaylist", function(url) {
          if (url instanceof Array) {
            return {
              listType: "playlist",
              playlist: url.map(_this.getID).join(",")
            };
          }
          if (MATCH_PLAYLIST.test(url)) {
            var _url$match = url.match(MATCH_PLAYLIST), _url$match2 = _slicedToArray3(_url$match, 2), playlistId = _url$match2[1];
            return {
              listType: "playlist",
              list: playlistId.replace(/^UC/, "UU")
            };
          }
          if (MATCH_USER_UPLOADS.test(url)) {
            var _url$match3 = url.match(MATCH_USER_UPLOADS), _url$match4 = _slicedToArray3(_url$match3, 2), username = _url$match4[1];
            return {
              listType: "user_uploads",
              list: username
            };
          }
          return {};
        });
        _defineProperty3(_assertThisInitialized(_this), "onStateChange", function(event) {
          var data = event.data;
          var _this$props = _this.props, onPlay = _this$props.onPlay, onPause = _this$props.onPause, onBuffer = _this$props.onBuffer, onBufferEnd = _this$props.onBufferEnd, onEnded = _this$props.onEnded, onReady = _this$props.onReady, loop = _this$props.loop, _this$props$config = _this$props.config, playerVars = _this$props$config.playerVars, onUnstarted = _this$props$config.onUnstarted;
          var _window$SDK_GLOBAL$Pl = window[SDK_GLOBAL].PlayerState, UNSTARTED = _window$SDK_GLOBAL$Pl.UNSTARTED, PLAYING = _window$SDK_GLOBAL$Pl.PLAYING, PAUSED = _window$SDK_GLOBAL$Pl.PAUSED, BUFFERING = _window$SDK_GLOBAL$Pl.BUFFERING, ENDED = _window$SDK_GLOBAL$Pl.ENDED, CUED = _window$SDK_GLOBAL$Pl.CUED;
          if (data === UNSTARTED) onUnstarted();
          if (data === PLAYING) {
            onPlay();
            onBufferEnd();
          }
          if (data === PAUSED) onPause();
          if (data === BUFFERING) onBuffer();
          if (data === ENDED) {
            var isPlaylist = !!_this.callPlayer("getPlaylist");
            if (loop && !isPlaylist) {
              if (playerVars.start) {
                _this.seekTo(playerVars.start);
              } else {
                _this.play();
              }
            }
            onEnded();
          }
          if (data === CUED) onReady();
        });
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.callPlayer("mute");
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.callPlayer("unMute");
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(container) {
          _this.container = container;
        });
        return _this;
      }
      _createClass(YouTube2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "getID",
        value: function getID(url) {
          if (!url || url instanceof Array || MATCH_PLAYLIST.test(url)) {
            return null;
          }
          return url.match(_patterns.MATCH_URL_YOUTUBE)[1];
        }
      }, {
        key: "load",
        value: function load(url, isReady) {
          var _this2 = this;
          var _this$props2 = this.props, playing = _this$props2.playing, muted = _this$props2.muted, playsinline = _this$props2.playsinline, controls = _this$props2.controls, loop = _this$props2.loop, config = _this$props2.config, _onError = _this$props2.onError;
          var playerVars = config.playerVars, embedOptions = config.embedOptions;
          var id3 = this.getID(url);
          if (isReady) {
            if (MATCH_PLAYLIST.test(url) || MATCH_USER_UPLOADS.test(url) || url instanceof Array) {
              this.player.loadPlaylist(this.parsePlaylist(url));
              return;
            }
            this.player.cueVideoById({
              videoId: id3,
              startSeconds: (0, _utils.parseStartTime)(url) || playerVars.start,
              endSeconds: (0, _utils.parseEndTime)(url) || playerVars.end
            });
            return;
          }
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL, SDK_GLOBAL_READY, function(YT) {
            return YT.loaded;
          }).then(function(YT) {
            if (!_this2.container) return;
            _this2.player = new YT.Player(_this2.container, _objectSpread3({
              width: "100%",
              height: "100%",
              videoId: id3,
              playerVars: _objectSpread3(_objectSpread3({
                autoplay: playing ? 1 : 0,
                mute: muted ? 1 : 0,
                controls: controls ? 1 : 0,
                start: (0, _utils.parseStartTime)(url),
                end: (0, _utils.parseEndTime)(url),
                origin: window.location.origin,
                playsinline: playsinline ? 1 : 0
              }, _this2.parsePlaylist(url)), playerVars),
              events: {
                onReady: function onReady() {
                  if (loop) {
                    _this2.player.setLoop(true);
                  }
                  _this2.props.onReady();
                },
                onPlaybackRateChange: function onPlaybackRateChange(event) {
                  return _this2.props.onPlaybackRateChange(event.data);
                },
                onStateChange: _this2.onStateChange,
                onError: function onError(event) {
                  return _onError(event.data);
                }
              },
              host: MATCH_NOCOOKIE.test(url) ? NOCOOKIE_HOST : void 0
            }, embedOptions));
          }, _onError);
          if (embedOptions.events) {
            console.warn("Using `embedOptions.events` will likely break things. Use ReactPlayer’s callback props instead, eg onReady, onPlay, onPause");
          }
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("playVideo");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pauseVideo");
        }
      }, {
        key: "stop",
        value: function stop() {
          if (!document.body.contains(this.callPlayer("getIframe"))) return;
          this.callPlayer("stopVideo");
        }
      }, {
        key: "seekTo",
        value: function seekTo(amount) {
          this.callPlayer("seekTo", amount);
          if (!this.props.playing) {
            this.pause();
          }
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction * 100);
        }
      }, {
        key: "setPlaybackRate",
        value: function setPlaybackRate(rate) {
          this.callPlayer("setPlaybackRate", rate);
        }
      }, {
        key: "setLoop",
        value: function setLoop(loop) {
          this.callPlayer("setLoop", loop);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.callPlayer("getDuration");
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.callPlayer("getCurrentTime");
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return this.callPlayer("getVideoLoadedFraction") * this.getDuration();
        }
      }, {
        key: "render",
        value: function render() {
          var display = this.props.display;
          var style = {
            width: "100%",
            height: "100%",
            display
          };
          return _react["default"].createElement("div", {
            style
          }, _react["default"].createElement("div", {
            ref: this.ref
          }));
        }
      }]);
      return YouTube2;
    })(_react.Component);
    exports["default"] = YouTube;
    _defineProperty3(YouTube, "displayName", "YouTube");
    _defineProperty3(YouTube, "canPlay", _patterns.canPlay.youtube);
  }
});

// node_modules/react-player/lib/players/SoundCloud.js
var require_SoundCloud = __commonJS({
  "node_modules/react-player/lib/players/SoundCloud.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://w.soundcloud.com/player/api.js";
    var SDK_GLOBAL = "SC";
    var SoundCloud = (function(_Component) {
      _inherits(SoundCloud2, _Component);
      var _super = _createSuper(SoundCloud2);
      function SoundCloud2() {
        var _this;
        _classCallCheck(this, SoundCloud2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "duration", null);
        _defineProperty3(_assertThisInitialized(_this), "currentTime", null);
        _defineProperty3(_assertThisInitialized(_this), "fractionLoaded", null);
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.setVolume(0);
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          if (_this.props.volume !== null) {
            _this.setVolume(_this.props.volume);
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(iframe) {
          _this.iframe = iframe;
        });
        return _this;
      }
      _createClass(SoundCloud2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url, isReady) {
          var _this2 = this;
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL).then(function(SC) {
            if (!_this2.iframe) return;
            var _SC$Widget$Events = SC.Widget.Events, PLAY = _SC$Widget$Events.PLAY, PLAY_PROGRESS = _SC$Widget$Events.PLAY_PROGRESS, PAUSE = _SC$Widget$Events.PAUSE, FINISH = _SC$Widget$Events.FINISH, ERROR2 = _SC$Widget$Events.ERROR;
            if (!isReady) {
              _this2.player = SC.Widget(_this2.iframe);
              _this2.player.bind(PLAY, _this2.props.onPlay);
              _this2.player.bind(PAUSE, function() {
                var remaining = _this2.duration - _this2.currentTime;
                if (remaining < 0.05) {
                  return;
                }
                _this2.props.onPause();
              });
              _this2.player.bind(PLAY_PROGRESS, function(e) {
                _this2.currentTime = e.currentPosition / 1e3;
                _this2.fractionLoaded = e.loadedProgress;
              });
              _this2.player.bind(FINISH, function() {
                return _this2.props.onEnded();
              });
              _this2.player.bind(ERROR2, function(e) {
                return _this2.props.onError(e);
              });
            }
            _this2.player.load(url, _objectSpread3(_objectSpread3({}, _this2.props.config.options), {}, {
              callback: function callback() {
                _this2.player.getDuration(function(duration2) {
                  _this2.duration = duration2 / 1e3;
                  _this2.props.onReady();
                });
              }
            }));
          });
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("seekTo", seconds * 1e3);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction * 100);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.duration;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.currentTime;
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return this.fractionLoaded * this.duration;
        }
      }, {
        key: "render",
        value: function render() {
          var display = this.props.display;
          var style = {
            width: "100%",
            height: "100%",
            display
          };
          return _react["default"].createElement("iframe", {
            ref: this.ref,
            src: "https://w.soundcloud.com/player/?url=".concat(encodeURIComponent(this.props.url)),
            style,
            frameBorder: 0,
            allow: "autoplay"
          });
        }
      }]);
      return SoundCloud2;
    })(_react.Component);
    exports["default"] = SoundCloud;
    _defineProperty3(SoundCloud, "displayName", "SoundCloud");
    _defineProperty3(SoundCloud, "canPlay", _patterns.canPlay.soundcloud);
    _defineProperty3(SoundCloud, "loopOnEnded", true);
  }
});

// node_modules/react-player/lib/players/Vimeo.js
var require_Vimeo = __commonJS({
  "node_modules/react-player/lib/players/Vimeo.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://player.vimeo.com/api/player.js";
    var SDK_GLOBAL = "Vimeo";
    var Vimeo = (function(_Component) {
      _inherits(Vimeo2, _Component);
      var _super = _createSuper(Vimeo2);
      function Vimeo2() {
        var _this;
        _classCallCheck(this, Vimeo2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "duration", null);
        _defineProperty3(_assertThisInitialized(_this), "currentTime", null);
        _defineProperty3(_assertThisInitialized(_this), "secondsLoaded", null);
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.setVolume(0);
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          if (_this.props.volume !== null) {
            _this.setVolume(_this.props.volume);
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(container) {
          _this.container = container;
        });
        return _this;
      }
      _createClass(Vimeo2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          this.duration = null;
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL).then(function(Vimeo3) {
            if (!_this2.container) return;
            var _this2$props$config = _this2.props.config, playerOptions = _this2$props$config.playerOptions, title = _this2$props$config.title;
            _this2.player = new Vimeo3.Player(_this2.container, _objectSpread3({
              url,
              autoplay: _this2.props.playing,
              muted: _this2.props.muted,
              loop: _this2.props.loop,
              playsinline: _this2.props.playsinline,
              controls: _this2.props.controls
            }, playerOptions));
            _this2.player.ready().then(function() {
              var iframe = _this2.container.querySelector("iframe");
              iframe.style.width = "100%";
              iframe.style.height = "100%";
              if (title) {
                iframe.title = title;
              }
            })["catch"](_this2.props.onError);
            _this2.player.on("loaded", function() {
              _this2.props.onReady();
              _this2.refreshDuration();
            });
            _this2.player.on("play", function() {
              _this2.props.onPlay();
              _this2.refreshDuration();
            });
            _this2.player.on("pause", _this2.props.onPause);
            _this2.player.on("seeked", function(e) {
              return _this2.props.onSeek(e.seconds);
            });
            _this2.player.on("ended", _this2.props.onEnded);
            _this2.player.on("error", _this2.props.onError);
            _this2.player.on("timeupdate", function(_ref) {
              var seconds = _ref.seconds;
              _this2.currentTime = seconds;
            });
            _this2.player.on("progress", function(_ref2) {
              var seconds = _ref2.seconds;
              _this2.secondsLoaded = seconds;
            });
            _this2.player.on("bufferstart", _this2.props.onBuffer);
            _this2.player.on("bufferend", _this2.props.onBufferEnd);
          }, this.props.onError);
        }
      }, {
        key: "refreshDuration",
        value: function refreshDuration() {
          var _this3 = this;
          this.player.getDuration().then(function(duration2) {
            _this3.duration = duration2;
          });
        }
      }, {
        key: "play",
        value: function play() {
          var promise = this.callPlayer("play");
          if (promise) {
            promise["catch"](this.props.onError);
          }
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
          this.callPlayer("unload");
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("setCurrentTime", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction);
        }
      }, {
        key: "setLoop",
        value: function setLoop(loop) {
          this.callPlayer("setLoop", loop);
        }
      }, {
        key: "setPlaybackRate",
        value: function setPlaybackRate(rate) {
          this.callPlayer("setPlaybackRate", rate);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.duration;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.currentTime;
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return this.secondsLoaded;
        }
      }, {
        key: "render",
        value: function render() {
          var display = this.props.display;
          var style = {
            width: "100%",
            height: "100%",
            overflow: "hidden",
            display
          };
          return _react["default"].createElement("div", {
            key: this.props.url,
            ref: this.ref,
            style
          });
        }
      }]);
      return Vimeo2;
    })(_react.Component);
    exports["default"] = Vimeo;
    _defineProperty3(Vimeo, "displayName", "Vimeo");
    _defineProperty3(Vimeo, "canPlay", _patterns.canPlay.vimeo);
    _defineProperty3(Vimeo, "forceLoad", true);
  }
});

// node_modules/react-player/lib/players/Facebook.js
var require_Facebook = __commonJS({
  "node_modules/react-player/lib/players/Facebook.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function _extends2() {
      _extends2 = Object.assign || function(target) {
        for (var i2 = 1; i2 < arguments.length; i2++) {
          var source = arguments[i2];
          for (var key in source) {
            if (Object.prototype.hasOwnProperty.call(source, key)) {
              target[key] = source[key];
            }
          }
        }
        return target;
      };
      return _extends2.apply(this, arguments);
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://connect.facebook.net/en_US/sdk.js";
    var SDK_GLOBAL = "FB";
    var SDK_GLOBAL_READY = "fbAsyncInit";
    var PLAYER_ID_PREFIX = "facebook-player-";
    var Facebook = (function(_Component) {
      _inherits(Facebook2, _Component);
      var _super = _createSuper(Facebook2);
      function Facebook2() {
        var _this;
        _classCallCheck(this, Facebook2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "playerID", _this.props.config.playerId || "".concat(PLAYER_ID_PREFIX).concat((0, _utils.randomString)()));
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.callPlayer("mute");
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.callPlayer("unmute");
        });
        return _this;
      }
      _createClass(Facebook2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url, isReady) {
          var _this2 = this;
          if (isReady) {
            (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL, SDK_GLOBAL_READY).then(function(FB) {
              return FB.XFBML.parse();
            });
            return;
          }
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL, SDK_GLOBAL_READY).then(function(FB) {
            FB.init({
              appId: _this2.props.config.appId,
              xfbml: true,
              version: _this2.props.config.version
            });
            FB.Event.subscribe("xfbml.render", function(msg) {
              _this2.props.onLoaded();
            });
            FB.Event.subscribe("xfbml.ready", function(msg) {
              if (msg.type === "video" && msg.id === _this2.playerID) {
                _this2.player = msg.instance;
                _this2.player.subscribe("startedPlaying", _this2.props.onPlay);
                _this2.player.subscribe("paused", _this2.props.onPause);
                _this2.player.subscribe("finishedPlaying", _this2.props.onEnded);
                _this2.player.subscribe("startedBuffering", _this2.props.onBuffer);
                _this2.player.subscribe("finishedBuffering", _this2.props.onBufferEnd);
                _this2.player.subscribe("error", _this2.props.onError);
                if (_this2.props.muted) {
                  _this2.callPlayer("mute");
                } else {
                  _this2.callPlayer("unmute");
                }
                _this2.props.onReady();
                document.getElementById(_this2.playerID).querySelector("iframe").style.visibility = "visible";
              }
            });
          });
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("seek", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.callPlayer("getDuration");
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.callPlayer("getCurrentPosition");
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return null;
        }
      }, {
        key: "render",
        value: function render() {
          var attributes = this.props.config.attributes;
          var style = {
            width: "100%",
            height: "100%"
          };
          return _react["default"].createElement("div", _extends2({
            style,
            id: this.playerID,
            className: "fb-video",
            "data-href": this.props.url,
            "data-autoplay": this.props.playing ? "true" : "false",
            "data-allowfullscreen": "true",
            "data-controls": this.props.controls ? "true" : "false"
          }, attributes));
        }
      }]);
      return Facebook2;
    })(_react.Component);
    exports["default"] = Facebook;
    _defineProperty3(Facebook, "displayName", "Facebook");
    _defineProperty3(Facebook, "canPlay", _patterns.canPlay.facebook);
    _defineProperty3(Facebook, "loopOnEnded", true);
  }
});

// node_modules/react-player/lib/players/Streamable.js
var require_Streamable = __commonJS({
  "node_modules/react-player/lib/players/Streamable.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://cdn.embed.ly/player-0.1.0.min.js";
    var SDK_GLOBAL = "playerjs";
    var Streamable = (function(_Component) {
      _inherits(Streamable2, _Component);
      var _super = _createSuper(Streamable2);
      function Streamable2() {
        var _this;
        _classCallCheck(this, Streamable2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "duration", null);
        _defineProperty3(_assertThisInitialized(_this), "currentTime", null);
        _defineProperty3(_assertThisInitialized(_this), "secondsLoaded", null);
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.callPlayer("mute");
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.callPlayer("unmute");
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(iframe) {
          _this.iframe = iframe;
        });
        return _this;
      }
      _createClass(Streamable2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL).then(function(playerjs) {
            if (!_this2.iframe) return;
            _this2.player = new playerjs.Player(_this2.iframe);
            _this2.player.setLoop(_this2.props.loop);
            _this2.player.on("ready", _this2.props.onReady);
            _this2.player.on("play", _this2.props.onPlay);
            _this2.player.on("pause", _this2.props.onPause);
            _this2.player.on("seeked", _this2.props.onSeek);
            _this2.player.on("ended", _this2.props.onEnded);
            _this2.player.on("error", _this2.props.onError);
            _this2.player.on("timeupdate", function(_ref) {
              var duration2 = _ref.duration, seconds = _ref.seconds;
              _this2.duration = duration2;
              _this2.currentTime = seconds;
            });
            _this2.player.on("buffered", function(_ref2) {
              var percent = _ref2.percent;
              if (_this2.duration) {
                _this2.secondsLoaded = _this2.duration * percent;
              }
            });
            if (_this2.props.muted) {
              _this2.player.mute();
            }
          }, this.props.onError);
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("setCurrentTime", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction * 100);
        }
      }, {
        key: "setLoop",
        value: function setLoop(loop) {
          this.callPlayer("setLoop", loop);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.duration;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.currentTime;
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return this.secondsLoaded;
        }
      }, {
        key: "render",
        value: function render() {
          var id3 = this.props.url.match(_patterns.MATCH_URL_STREAMABLE)[1];
          var style = {
            width: "100%",
            height: "100%"
          };
          return _react["default"].createElement("iframe", {
            ref: this.ref,
            src: "https://streamable.com/o/".concat(id3),
            frameBorder: "0",
            scrolling: "no",
            style,
            allow: "encrypted-media; autoplay; fullscreen;"
          });
        }
      }]);
      return Streamable2;
    })(_react.Component);
    exports["default"] = Streamable;
    _defineProperty3(Streamable, "displayName", "Streamable");
    _defineProperty3(Streamable, "canPlay", _patterns.canPlay.streamable);
  }
});

// node_modules/react-player/lib/players/Wistia.js
var require_Wistia = __commonJS({
  "node_modules/react-player/lib/players/Wistia.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://fast.wistia.com/assets/external/E-v1.js";
    var SDK_GLOBAL = "Wistia";
    var PLAYER_ID_PREFIX = "wistia-player-";
    var Wistia = (function(_Component) {
      _inherits(Wistia2, _Component);
      var _super = _createSuper(Wistia2);
      function Wistia2() {
        var _this;
        _classCallCheck(this, Wistia2);
        for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
          _args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(_args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "playerID", _this.props.config.playerId || "".concat(PLAYER_ID_PREFIX).concat((0, _utils.randomString)()));
        _defineProperty3(_assertThisInitialized(_this), "onPlay", function() {
          var _this$props;
          return (_this$props = _this.props).onPlay.apply(_this$props, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onPause", function() {
          var _this$props2;
          return (_this$props2 = _this.props).onPause.apply(_this$props2, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onSeek", function() {
          var _this$props3;
          return (_this$props3 = _this.props).onSeek.apply(_this$props3, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onEnded", function() {
          var _this$props4;
          return (_this$props4 = _this.props).onEnded.apply(_this$props4, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onPlaybackRateChange", function() {
          var _this$props5;
          return (_this$props5 = _this.props).onPlaybackRateChange.apply(_this$props5, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.callPlayer("mute");
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.callPlayer("unmute");
        });
        return _this;
      }
      _createClass(Wistia2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          var _this$props6 = this.props, playing = _this$props6.playing, muted = _this$props6.muted, controls = _this$props6.controls, _onReady = _this$props6.onReady, config = _this$props6.config, onError = _this$props6.onError;
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL).then(function(Wistia3) {
            if (config.customControls) {
              config.customControls.forEach(function(control) {
                return Wistia3.defineControl(control);
              });
            }
            window._wq = window._wq || [];
            window._wq.push({
              id: _this2.playerID,
              options: _objectSpread3({
                autoPlay: playing,
                silentAutoPlay: "allow",
                muted,
                controlsVisibleOnLoad: controls,
                fullscreenButton: controls,
                playbar: controls,
                playbackRateControl: controls,
                qualityControl: controls,
                volumeControl: controls,
                settingsControl: controls,
                smallPlayButton: controls
              }, config.options),
              onReady: function onReady(player) {
                _this2.player = player;
                _this2.unbind();
                _this2.player.bind("play", _this2.onPlay);
                _this2.player.bind("pause", _this2.onPause);
                _this2.player.bind("seek", _this2.onSeek);
                _this2.player.bind("end", _this2.onEnded);
                _this2.player.bind("playbackratechange", _this2.onPlaybackRateChange);
                _onReady();
              }
            });
          }, onError);
        }
      }, {
        key: "unbind",
        value: function unbind() {
          this.player.unbind("play", this.onPlay);
          this.player.unbind("pause", this.onPause);
          this.player.unbind("seek", this.onSeek);
          this.player.unbind("end", this.onEnded);
          this.player.unbind("playbackratechange", this.onPlaybackRateChange);
        }
        // Proxy methods to prevent listener leaks
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
          this.unbind();
          this.callPlayer("remove");
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("time", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("volume", fraction);
        }
      }, {
        key: "setPlaybackRate",
        value: function setPlaybackRate(rate) {
          this.callPlayer("playbackRate", rate);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.callPlayer("duration");
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.callPlayer("time");
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return null;
        }
      }, {
        key: "render",
        value: function render() {
          var url = this.props.url;
          var videoID = url && url.match(_patterns.MATCH_URL_WISTIA)[1];
          var className = "wistia_embed wistia_async_".concat(videoID);
          var style = {
            width: "100%",
            height: "100%"
          };
          return _react["default"].createElement("div", {
            id: this.playerID,
            key: videoID,
            className,
            style
          });
        }
      }]);
      return Wistia2;
    })(_react.Component);
    exports["default"] = Wistia;
    _defineProperty3(Wistia, "displayName", "Wistia");
    _defineProperty3(Wistia, "canPlay", _patterns.canPlay.wistia);
    _defineProperty3(Wistia, "loopOnEnded", true);
  }
});

// node_modules/react-player/lib/players/Twitch.js
var require_Twitch = __commonJS({
  "node_modules/react-player/lib/players/Twitch.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://player.twitch.tv/js/embed/v1.js";
    var SDK_GLOBAL = "Twitch";
    var PLAYER_ID_PREFIX = "twitch-player-";
    var Twitch = (function(_Component) {
      _inherits(Twitch2, _Component);
      var _super = _createSuper(Twitch2);
      function Twitch2() {
        var _this;
        _classCallCheck(this, Twitch2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "playerID", _this.props.config.playerId || "".concat(PLAYER_ID_PREFIX).concat((0, _utils.randomString)()));
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.callPlayer("setMuted", true);
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.callPlayer("setMuted", false);
        });
        return _this;
      }
      _createClass(Twitch2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url, isReady) {
          var _this2 = this;
          var _this$props = this.props, playsinline = _this$props.playsinline, onError = _this$props.onError, config = _this$props.config, controls = _this$props.controls;
          var isChannel = _patterns.MATCH_URL_TWITCH_CHANNEL.test(url);
          var id3 = isChannel ? url.match(_patterns.MATCH_URL_TWITCH_CHANNEL)[1] : url.match(_patterns.MATCH_URL_TWITCH_VIDEO)[1];
          if (isReady) {
            if (isChannel) {
              this.player.setChannel(id3);
            } else {
              this.player.setVideo("v" + id3);
            }
            return;
          }
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL).then(function(Twitch3) {
            _this2.player = new Twitch3.Player(_this2.playerID, _objectSpread3({
              video: isChannel ? "" : id3,
              channel: isChannel ? id3 : "",
              height: "100%",
              width: "100%",
              playsinline,
              autoplay: _this2.props.playing,
              muted: _this2.props.muted,
              // https://github.com/CookPete/react-player/issues/733#issuecomment-549085859
              controls: isChannel ? true : controls,
              time: (0, _utils.parseStartTime)(url)
            }, config.options));
            var _Twitch$Player = Twitch3.Player, READY = _Twitch$Player.READY, PLAYING = _Twitch$Player.PLAYING, PAUSE = _Twitch$Player.PAUSE, ENDED = _Twitch$Player.ENDED, ONLINE = _Twitch$Player.ONLINE, OFFLINE = _Twitch$Player.OFFLINE, SEEK = _Twitch$Player.SEEK;
            _this2.player.addEventListener(READY, _this2.props.onReady);
            _this2.player.addEventListener(PLAYING, _this2.props.onPlay);
            _this2.player.addEventListener(PAUSE, _this2.props.onPause);
            _this2.player.addEventListener(ENDED, _this2.props.onEnded);
            _this2.player.addEventListener(SEEK, _this2.props.onSeek);
            _this2.player.addEventListener(ONLINE, _this2.props.onLoaded);
            _this2.player.addEventListener(OFFLINE, _this2.props.onLoaded);
          }, onError);
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
          this.callPlayer("pause");
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("seek", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.callPlayer("getDuration");
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.callPlayer("getCurrentTime");
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return null;
        }
      }, {
        key: "render",
        value: function render() {
          var style = {
            width: "100%",
            height: "100%"
          };
          return _react["default"].createElement("div", {
            style,
            id: this.playerID
          });
        }
      }]);
      return Twitch2;
    })(_react.Component);
    exports["default"] = Twitch;
    _defineProperty3(Twitch, "displayName", "Twitch");
    _defineProperty3(Twitch, "canPlay", _patterns.canPlay.twitch);
    _defineProperty3(Twitch, "loopOnEnded", true);
  }
});

// node_modules/react-player/lib/players/DailyMotion.js
var require_DailyMotion = __commonJS({
  "node_modules/react-player/lib/players/DailyMotion.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _slicedToArray3(arr, i2) {
      return _arrayWithHoles3(arr) || _iterableToArrayLimit3(arr, i2) || _unsupportedIterableToArray4(arr, i2) || _nonIterableRest3();
    }
    function _nonIterableRest3() {
      throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    function _unsupportedIterableToArray4(o2, minLen) {
      if (!o2) return;
      if (typeof o2 === "string") return _arrayLikeToArray4(o2, minLen);
      var n2 = Object.prototype.toString.call(o2).slice(8, -1);
      if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
      if (n2 === "Map" || n2 === "Set") return Array.from(o2);
      if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray4(o2, minLen);
    }
    function _arrayLikeToArray4(arr, len) {
      if (len == null || len > arr.length) len = arr.length;
      for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) {
        arr2[i2] = arr[i2];
      }
      return arr2;
    }
    function _iterableToArrayLimit3(arr, i2) {
      if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return;
      var _arr = [];
      var _n = true;
      var _d = false;
      var _e2 = void 0;
      try {
        for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
          _arr.push(_s.value);
          if (i2 && _arr.length === i2) break;
        }
      } catch (err) {
        _d = true;
        _e2 = err;
      } finally {
        try {
          if (!_n && _i["return"] != null) _i["return"]();
        } finally {
          if (_d) throw _e2;
        }
      }
      return _arr;
    }
    function _arrayWithHoles3(arr) {
      if (Array.isArray(arr)) return arr;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://api.dmcdn.net/all.js";
    var SDK_GLOBAL = "DM";
    var SDK_GLOBAL_READY = "dmAsyncInit";
    var DailyMotion = (function(_Component) {
      _inherits(DailyMotion2, _Component);
      var _super = _createSuper(DailyMotion2);
      function DailyMotion2() {
        var _this;
        _classCallCheck(this, DailyMotion2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "onDurationChange", function() {
          var duration2 = _this.getDuration();
          _this.props.onDuration(duration2);
        });
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.callPlayer("setMuted", true);
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.callPlayer("setMuted", false);
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(container) {
          _this.container = container;
        });
        return _this;
      }
      _createClass(DailyMotion2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          var _this$props = this.props, controls = _this$props.controls, config = _this$props.config, onError = _this$props.onError, playing = _this$props.playing;
          var _url$match = url.match(_patterns.MATCH_URL_DAILYMOTION), _url$match2 = _slicedToArray3(_url$match, 2), id3 = _url$match2[1];
          if (this.player) {
            this.player.load(id3, {
              start: (0, _utils.parseStartTime)(url),
              autoplay: playing
            });
            return;
          }
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL, SDK_GLOBAL_READY, function(DM) {
            return DM.player;
          }).then(function(DM) {
            if (!_this2.container) return;
            var Player = DM.player;
            _this2.player = new Player(_this2.container, {
              width: "100%",
              height: "100%",
              video: id3,
              params: _objectSpread3({
                controls,
                autoplay: _this2.props.playing,
                mute: _this2.props.muted,
                start: (0, _utils.parseStartTime)(url),
                origin: window.location.origin
              }, config.params),
              events: {
                apiready: _this2.props.onReady,
                seeked: function seeked() {
                  return _this2.props.onSeek(_this2.player.currentTime);
                },
                video_end: _this2.props.onEnded,
                durationchange: _this2.onDurationChange,
                pause: _this2.props.onPause,
                playing: _this2.props.onPlay,
                waiting: _this2.props.onBuffer,
                error: function error2(event) {
                  return onError(event);
                }
              }
            });
          }, onError);
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("seek", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.player.duration || null;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.player.currentTime;
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return this.player.bufferedTime;
        }
      }, {
        key: "render",
        value: function render() {
          var display = this.props.display;
          var style = {
            width: "100%",
            height: "100%",
            display
          };
          return _react["default"].createElement("div", {
            style
          }, _react["default"].createElement("div", {
            ref: this.ref
          }));
        }
      }]);
      return DailyMotion2;
    })(_react.Component);
    exports["default"] = DailyMotion;
    _defineProperty3(DailyMotion, "displayName", "DailyMotion");
    _defineProperty3(DailyMotion, "canPlay", _patterns.canPlay.dailymotion);
    _defineProperty3(DailyMotion, "loopOnEnded", true);
  }
});

// node_modules/react-player/lib/players/Mixcloud.js
var require_Mixcloud = __commonJS({
  "node_modules/react-player/lib/players/Mixcloud.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://widget.mixcloud.com/media/js/widgetApi.js";
    var SDK_GLOBAL = "Mixcloud";
    var Mixcloud = (function(_Component) {
      _inherits(Mixcloud2, _Component);
      var _super = _createSuper(Mixcloud2);
      function Mixcloud2() {
        var _this;
        _classCallCheck(this, Mixcloud2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "duration", null);
        _defineProperty3(_assertThisInitialized(_this), "currentTime", null);
        _defineProperty3(_assertThisInitialized(_this), "secondsLoaded", null);
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(iframe) {
          _this.iframe = iframe;
        });
        return _this;
      }
      _createClass(Mixcloud2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL).then(function(Mixcloud3) {
            _this2.player = Mixcloud3.PlayerWidget(_this2.iframe);
            _this2.player.ready.then(function() {
              _this2.player.events.play.on(_this2.props.onPlay);
              _this2.player.events.pause.on(_this2.props.onPause);
              _this2.player.events.ended.on(_this2.props.onEnded);
              _this2.player.events.error.on(_this2.props.error);
              _this2.player.events.progress.on(function(seconds, duration2) {
                _this2.currentTime = seconds;
                _this2.duration = duration2;
              });
              _this2.props.onReady();
            });
          }, this.props.onError);
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("seek", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.duration;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.currentTime;
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return null;
        }
      }, {
        key: "render",
        value: function render() {
          var _this$props = this.props, url = _this$props.url, config = _this$props.config;
          var id3 = url.match(_patterns.MATCH_URL_MIXCLOUD)[1];
          var style = {
            width: "100%",
            height: "100%"
          };
          var query = (0, _utils.queryString)(_objectSpread3(_objectSpread3({}, config.options), {}, {
            feed: "/".concat(id3, "/")
          }));
          return _react["default"].createElement("iframe", {
            key: id3,
            ref: this.ref,
            style,
            src: "https://www.mixcloud.com/widget/iframe/?".concat(query),
            frameBorder: "0"
          });
        }
      }]);
      return Mixcloud2;
    })(_react.Component);
    exports["default"] = Mixcloud;
    _defineProperty3(Mixcloud, "displayName", "Mixcloud");
    _defineProperty3(Mixcloud, "canPlay", _patterns.canPlay.mixcloud);
    _defineProperty3(Mixcloud, "loopOnEnded", true);
  }
});

// node_modules/react-player/lib/players/Vidyard.js
var require_Vidyard = __commonJS({
  "node_modules/react-player/lib/players/Vidyard.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://play.vidyard.com/embed/v4.js";
    var SDK_GLOBAL = "VidyardV4";
    var SDK_GLOBAL_READY = "onVidyardAPI";
    var Vidyard = (function(_Component) {
      _inherits(Vidyard2, _Component);
      var _super = _createSuper(Vidyard2);
      function Vidyard2() {
        var _this;
        _classCallCheck(this, Vidyard2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.setVolume(0);
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          if (_this.props.volume !== null) {
            _this.setVolume(_this.props.volume);
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(container) {
          _this.container = container;
        });
        return _this;
      }
      _createClass(Vidyard2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          var _this$props = this.props, playing = _this$props.playing, config = _this$props.config, onError = _this$props.onError, onDuration = _this$props.onDuration;
          var id3 = url && url.match(_patterns.MATCH_URL_VIDYARD)[1];
          if (this.player) {
            this.stop();
          }
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL, SDK_GLOBAL_READY).then(function(Vidyard3) {
            if (!_this2.container) return;
            Vidyard3.api.addReadyListener(function(data, player) {
              if (_this2.player) {
                return;
              }
              _this2.player = player;
              _this2.player.on("ready", _this2.props.onReady);
              _this2.player.on("play", _this2.props.onPlay);
              _this2.player.on("pause", _this2.props.onPause);
              _this2.player.on("seek", _this2.props.onSeek);
              _this2.player.on("playerComplete", _this2.props.onEnded);
            }, id3);
            Vidyard3.api.renderPlayer(_objectSpread3({
              uuid: id3,
              container: _this2.container,
              autoplay: playing ? 1 : 0
            }, config.options));
            Vidyard3.api.getPlayerMetadata(id3).then(function(meta) {
              _this2.duration = meta.length_in_seconds;
              onDuration(meta.length_in_seconds);
            });
          }, onError);
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
          window.VidyardV4.api.destroyPlayer(this.player);
        }
      }, {
        key: "seekTo",
        value: function seekTo(amount) {
          this.callPlayer("seek", amount);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction);
        }
      }, {
        key: "setPlaybackRate",
        value: function setPlaybackRate(rate) {
          this.callPlayer("setPlaybackSpeed", rate);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.duration;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.callPlayer("currentTime");
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return null;
        }
      }, {
        key: "render",
        value: function render() {
          var display = this.props.display;
          var style = {
            width: "100%",
            height: "100%",
            display
          };
          return _react["default"].createElement("div", {
            style
          }, _react["default"].createElement("div", {
            ref: this.ref
          }));
        }
      }]);
      return Vidyard2;
    })(_react.Component);
    exports["default"] = Vidyard;
    _defineProperty3(Vidyard, "displayName", "Vidyard");
    _defineProperty3(Vidyard, "canPlay", _patterns.canPlay.vidyard);
  }
});

// node_modules/react-player/lib/players/Kaltura.js
var require_Kaltura = __commonJS({
  "node_modules/react-player/lib/players/Kaltura.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SDK_URL = "https://cdn.embed.ly/player-0.1.0.min.js";
    var SDK_GLOBAL = "playerjs";
    var Kaltura = (function(_Component) {
      _inherits(Kaltura2, _Component);
      var _super = _createSuper(Kaltura2);
      function Kaltura2() {
        var _this;
        _classCallCheck(this, Kaltura2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "callPlayer", _utils.callPlayer);
        _defineProperty3(_assertThisInitialized(_this), "duration", null);
        _defineProperty3(_assertThisInitialized(_this), "currentTime", null);
        _defineProperty3(_assertThisInitialized(_this), "secondsLoaded", null);
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.callPlayer("mute");
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.callPlayer("unmute");
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(iframe) {
          _this.iframe = iframe;
        });
        return _this;
      }
      _createClass(Kaltura2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          (0, _utils.getSDK)(SDK_URL, SDK_GLOBAL).then(function(playerjs) {
            if (!_this2.iframe) return;
            _this2.player = new playerjs.Player(_this2.iframe);
            _this2.player.on("ready", function() {
              setTimeout(function() {
                _this2.player.isReady = true;
                _this2.player.setLoop(_this2.props.loop);
                if (_this2.props.muted) {
                  _this2.player.mute();
                }
                _this2.addListeners(_this2.player, _this2.props);
                _this2.props.onReady();
              }, 500);
            });
          }, this.props.onError);
        }
      }, {
        key: "addListeners",
        value: function addListeners(player, props) {
          var _this3 = this;
          player.on("play", props.onPlay);
          player.on("pause", props.onPause);
          player.on("ended", props.onEnded);
          player.on("error", props.onError);
          player.on("timeupdate", function(_ref) {
            var duration2 = _ref.duration, seconds = _ref.seconds;
            _this3.duration = duration2;
            _this3.currentTime = seconds;
          });
        }
      }, {
        key: "play",
        value: function play() {
          this.callPlayer("play");
        }
      }, {
        key: "pause",
        value: function pause() {
          this.callPlayer("pause");
        }
      }, {
        key: "stop",
        value: function stop() {
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.callPlayer("setCurrentTime", seconds);
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.callPlayer("setVolume", fraction);
        }
      }, {
        key: "setLoop",
        value: function setLoop(loop) {
          this.callPlayer("setLoop", loop);
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          return this.duration;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          return this.currentTime;
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          return this.secondsLoaded;
        }
      }, {
        key: "render",
        value: function render() {
          var style = {
            width: "100%",
            height: "100%"
          };
          return _react["default"].createElement("iframe", {
            ref: this.ref,
            src: this.props.url,
            frameBorder: "0",
            scrolling: "no",
            style,
            allow: "encrypted-media; autoplay; fullscreen;",
            referrerPolicy: "no-referrer-when-downgrade"
          });
        }
      }]);
      return Kaltura2;
    })(_react.Component);
    exports["default"] = Kaltura;
    _defineProperty3(Kaltura, "displayName", "Kaltura");
    _defineProperty3(Kaltura, "canPlay", _patterns.canPlay.kaltura);
  }
});

// node_modules/react-player/lib/players/FilePlayer.js
var require_FilePlayer = __commonJS({
  "node_modules/react-player/lib/players/FilePlayer.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function _extends2() {
      _extends2 = Object.assign || function(target) {
        for (var i2 = 1; i2 < arguments.length; i2++) {
          var source = arguments[i2];
          for (var key in source) {
            if (Object.prototype.hasOwnProperty.call(source, key)) {
              target[key] = source[key];
            }
          }
        }
        return target;
      };
      return _extends2.apply(this, arguments);
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var HAS_NAVIGATOR = typeof navigator !== "undefined";
    var IS_IPAD_PRO = HAS_NAVIGATOR && navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1;
    var IS_IOS = HAS_NAVIGATOR && (/iPad|iPhone|iPod/.test(navigator.userAgent) || IS_IPAD_PRO) && !window.MSStream;
    var HLS_SDK_URL = "https://cdn.jsdelivr.net/npm/hls.js@VERSION/dist/hls.min.js";
    var HLS_GLOBAL = "Hls";
    var DASH_SDK_URL = "https://cdnjs.cloudflare.com/ajax/libs/dashjs/VERSION/dash.all.min.js";
    var DASH_GLOBAL = "dashjs";
    var FLV_SDK_URL = "https://cdn.jsdelivr.net/npm/flv.js@VERSION/dist/flv.min.js";
    var FLV_GLOBAL = "flvjs";
    var MATCH_DROPBOX_URL = /www\.dropbox\.com\/.+/;
    var MATCH_CLOUDFLARE_STREAM = /https:\/\/watch\.cloudflarestream\.com\/([a-z0-9]+)/;
    var REPLACE_CLOUDFLARE_STREAM = "https://videodelivery.net/{id}/manifest/video.m3u8";
    var FilePlayer = (function(_Component) {
      _inherits(FilePlayer2, _Component);
      var _super = _createSuper(FilePlayer2);
      function FilePlayer2() {
        var _this;
        _classCallCheck(this, FilePlayer2);
        for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
          _args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(_args));
        _defineProperty3(_assertThisInitialized(_this), "onReady", function() {
          var _this$props;
          return (_this$props = _this.props).onReady.apply(_this$props, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onPlay", function() {
          var _this$props2;
          return (_this$props2 = _this.props).onPlay.apply(_this$props2, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onBuffer", function() {
          var _this$props3;
          return (_this$props3 = _this.props).onBuffer.apply(_this$props3, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onBufferEnd", function() {
          var _this$props4;
          return (_this$props4 = _this.props).onBufferEnd.apply(_this$props4, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onPause", function() {
          var _this$props5;
          return (_this$props5 = _this.props).onPause.apply(_this$props5, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onEnded", function() {
          var _this$props6;
          return (_this$props6 = _this.props).onEnded.apply(_this$props6, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onError", function() {
          var _this$props7;
          return (_this$props7 = _this.props).onError.apply(_this$props7, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onPlayBackRateChange", function(event) {
          return _this.props.onPlaybackRateChange(event.target.playbackRate);
        });
        _defineProperty3(_assertThisInitialized(_this), "onEnablePIP", function() {
          var _this$props8;
          return (_this$props8 = _this.props).onEnablePIP.apply(_this$props8, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "onDisablePIP", function(e) {
          var _this$props9 = _this.props, onDisablePIP = _this$props9.onDisablePIP, playing = _this$props9.playing;
          onDisablePIP(e);
          if (playing) {
            _this.play();
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "onPresentationModeChange", function(e) {
          if (_this.player && (0, _utils.supportsWebKitPresentationMode)(_this.player)) {
            var webkitPresentationMode = _this.player.webkitPresentationMode;
            if (webkitPresentationMode === "picture-in-picture") {
              _this.onEnablePIP(e);
            } else if (webkitPresentationMode === "inline") {
              _this.onDisablePIP(e);
            }
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "onSeek", function(e) {
          _this.props.onSeek(e.target.currentTime);
        });
        _defineProperty3(_assertThisInitialized(_this), "mute", function() {
          _this.player.muted = true;
        });
        _defineProperty3(_assertThisInitialized(_this), "unmute", function() {
          _this.player.muted = false;
        });
        _defineProperty3(_assertThisInitialized(_this), "renderSourceElement", function(source, index6) {
          if (typeof source === "string") {
            return _react["default"].createElement("source", {
              key: index6,
              src: source
            });
          }
          return _react["default"].createElement("source", _extends2({
            key: index6
          }, source));
        });
        _defineProperty3(_assertThisInitialized(_this), "renderTrack", function(track2, index6) {
          return _react["default"].createElement("track", _extends2({
            key: index6
          }, track2));
        });
        _defineProperty3(_assertThisInitialized(_this), "ref", function(player) {
          if (_this.player) {
            _this.prevPlayer = _this.player;
          }
          _this.player = player;
        });
        return _this;
      }
      _createClass(FilePlayer2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.props.onMount && this.props.onMount(this);
          this.addListeners(this.player);
          if (IS_IOS) {
            this.player.load();
          }
        }
      }, {
        key: "componentDidUpdate",
        value: function componentDidUpdate(prevProps) {
          if (this.shouldUseAudio(this.props) !== this.shouldUseAudio(prevProps)) {
            this.removeListeners(this.prevPlayer, prevProps.url);
            this.addListeners(this.player);
          }
          if (this.props.url !== prevProps.url && !(0, _utils.isMediaStream)(this.props.url)) {
            this.player.srcObject = null;
          }
        }
      }, {
        key: "componentWillUnmount",
        value: function componentWillUnmount() {
          this.removeListeners(this.player);
          if (this.hls) {
            this.hls.destroy();
          }
        }
      }, {
        key: "addListeners",
        value: function addListeners(player) {
          var _this$props10 = this.props, url = _this$props10.url, playsinline = _this$props10.playsinline;
          player.addEventListener("play", this.onPlay);
          player.addEventListener("waiting", this.onBuffer);
          player.addEventListener("playing", this.onBufferEnd);
          player.addEventListener("pause", this.onPause);
          player.addEventListener("seeked", this.onSeek);
          player.addEventListener("ended", this.onEnded);
          player.addEventListener("error", this.onError);
          player.addEventListener("ratechange", this.onPlayBackRateChange);
          player.addEventListener("enterpictureinpicture", this.onEnablePIP);
          player.addEventListener("leavepictureinpicture", this.onDisablePIP);
          player.addEventListener("webkitpresentationmodechanged", this.onPresentationModeChange);
          if (!this.shouldUseHLS(url)) {
            player.addEventListener("canplay", this.onReady);
          }
          if (playsinline) {
            player.setAttribute("playsinline", "");
            player.setAttribute("webkit-playsinline", "");
            player.setAttribute("x5-playsinline", "");
          }
        }
      }, {
        key: "removeListeners",
        value: function removeListeners(player, url) {
          player.removeEventListener("canplay", this.onReady);
          player.removeEventListener("play", this.onPlay);
          player.removeEventListener("waiting", this.onBuffer);
          player.removeEventListener("playing", this.onBufferEnd);
          player.removeEventListener("pause", this.onPause);
          player.removeEventListener("seeked", this.onSeek);
          player.removeEventListener("ended", this.onEnded);
          player.removeEventListener("error", this.onError);
          player.removeEventListener("ratechange", this.onPlayBackRateChange);
          player.removeEventListener("enterpictureinpicture", this.onEnablePIP);
          player.removeEventListener("leavepictureinpicture", this.onDisablePIP);
          player.removeEventListener("webkitpresentationmodechanged", this.onPresentationModeChange);
          if (!this.shouldUseHLS(url)) {
            player.removeEventListener("canplay", this.onReady);
          }
        }
        // Proxy methods to prevent listener leaks
      }, {
        key: "shouldUseAudio",
        value: function shouldUseAudio(props) {
          if (props.config.forceVideo) {
            return false;
          }
          if (props.config.attributes.poster) {
            return false;
          }
          return _patterns.AUDIO_EXTENSIONS.test(props.url) || props.config.forceAudio;
        }
      }, {
        key: "shouldUseHLS",
        value: function shouldUseHLS(url) {
          if (this.props.config.forceHLS) {
            return true;
          }
          if (IS_IOS) {
            return false;
          }
          return _patterns.HLS_EXTENSIONS.test(url) || MATCH_CLOUDFLARE_STREAM.test(url);
        }
      }, {
        key: "shouldUseDASH",
        value: function shouldUseDASH(url) {
          return _patterns.DASH_EXTENSIONS.test(url) || this.props.config.forceDASH;
        }
      }, {
        key: "shouldUseFLV",
        value: function shouldUseFLV(url) {
          return _patterns.FLV_EXTENSIONS.test(url) || this.props.config.forceFLV;
        }
      }, {
        key: "load",
        value: function load(url) {
          var _this2 = this;
          var _this$props$config = this.props.config, hlsVersion = _this$props$config.hlsVersion, hlsOptions = _this$props$config.hlsOptions, dashVersion = _this$props$config.dashVersion, flvVersion = _this$props$config.flvVersion;
          if (this.hls) {
            this.hls.destroy();
          }
          if (this.dash) {
            this.dash.reset();
          }
          if (this.shouldUseHLS(url)) {
            (0, _utils.getSDK)(HLS_SDK_URL.replace("VERSION", hlsVersion), HLS_GLOBAL).then(function(Hls) {
              _this2.hls = new Hls(hlsOptions);
              _this2.hls.on(Hls.Events.MANIFEST_PARSED, function() {
                _this2.props.onReady();
              });
              _this2.hls.on(Hls.Events.ERROR, function(e, data) {
                _this2.props.onError(e, data, _this2.hls, Hls);
              });
              if (MATCH_CLOUDFLARE_STREAM.test(url)) {
                var id3 = url.match(MATCH_CLOUDFLARE_STREAM)[1];
                _this2.hls.loadSource(REPLACE_CLOUDFLARE_STREAM.replace("{id}", id3));
              } else {
                _this2.hls.loadSource(url);
              }
              _this2.hls.attachMedia(_this2.player);
              _this2.props.onLoaded();
            });
          }
          if (this.shouldUseDASH(url)) {
            (0, _utils.getSDK)(DASH_SDK_URL.replace("VERSION", dashVersion), DASH_GLOBAL).then(function(dashjs) {
              _this2.dash = dashjs.MediaPlayer().create();
              _this2.dash.initialize(_this2.player, url, _this2.props.playing);
              _this2.dash.on("error", _this2.props.onError);
              if (parseInt(dashVersion) < 3) {
                _this2.dash.getDebug().setLogToBrowserConsole(false);
              } else {
                _this2.dash.updateSettings({
                  debug: {
                    logLevel: dashjs.Debug.LOG_LEVEL_NONE
                  }
                });
              }
              _this2.props.onLoaded();
            });
          }
          if (this.shouldUseFLV(url)) {
            (0, _utils.getSDK)(FLV_SDK_URL.replace("VERSION", flvVersion), FLV_GLOBAL).then(function(flvjs) {
              _this2.flv = flvjs.createPlayer({
                type: "flv",
                url
              });
              _this2.flv.attachMediaElement(_this2.player);
              _this2.flv.on(flvjs.Events.ERROR, function(e, data) {
                _this2.props.onError(e, data, _this2.flv, flvjs);
              });
              _this2.flv.load();
              _this2.props.onLoaded();
            });
          }
          if (url instanceof Array) {
            this.player.load();
          } else if ((0, _utils.isMediaStream)(url)) {
            try {
              this.player.srcObject = url;
            } catch (e) {
              this.player.src = window.URL.createObjectURL(url);
            }
          }
        }
      }, {
        key: "play",
        value: function play() {
          var promise = this.player.play();
          if (promise) {
            promise["catch"](this.props.onError);
          }
        }
      }, {
        key: "pause",
        value: function pause() {
          this.player.pause();
        }
      }, {
        key: "stop",
        value: function stop() {
          this.player.removeAttribute("src");
          if (this.dash) {
            this.dash.reset();
          }
        }
      }, {
        key: "seekTo",
        value: function seekTo(seconds) {
          this.player.currentTime = seconds;
        }
      }, {
        key: "setVolume",
        value: function setVolume(fraction) {
          this.player.volume = fraction;
        }
      }, {
        key: "enablePIP",
        value: function enablePIP() {
          if (this.player.requestPictureInPicture && document.pictureInPictureElement !== this.player) {
            this.player.requestPictureInPicture();
          } else if ((0, _utils.supportsWebKitPresentationMode)(this.player) && this.player.webkitPresentationMode !== "picture-in-picture") {
            this.player.webkitSetPresentationMode("picture-in-picture");
          }
        }
      }, {
        key: "disablePIP",
        value: function disablePIP() {
          if (document.exitPictureInPicture && document.pictureInPictureElement === this.player) {
            document.exitPictureInPicture();
          } else if ((0, _utils.supportsWebKitPresentationMode)(this.player) && this.player.webkitPresentationMode !== "inline") {
            this.player.webkitSetPresentationMode("inline");
          }
        }
      }, {
        key: "setPlaybackRate",
        value: function setPlaybackRate(rate) {
          try {
            this.player.playbackRate = rate;
          } catch (error2) {
            this.props.onError(error2);
          }
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          if (!this.player) return null;
          var _this$player = this.player, duration2 = _this$player.duration, seekable = _this$player.seekable;
          if (duration2 === Infinity && seekable.length > 0) {
            return seekable.end(seekable.length - 1);
          }
          return duration2;
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          if (!this.player) return null;
          return this.player.currentTime;
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          if (!this.player) return null;
          var buffered = this.player.buffered;
          if (buffered.length === 0) {
            return 0;
          }
          var end = buffered.end(buffered.length - 1);
          var duration2 = this.getDuration();
          if (end > duration2) {
            return duration2;
          }
          return end;
        }
      }, {
        key: "getSource",
        value: function getSource(url) {
          var useHLS = this.shouldUseHLS(url);
          var useDASH = this.shouldUseDASH(url);
          var useFLV = this.shouldUseFLV(url);
          if (url instanceof Array || (0, _utils.isMediaStream)(url) || useHLS || useDASH || useFLV) {
            return void 0;
          }
          if (MATCH_DROPBOX_URL.test(url)) {
            return url.replace("www.dropbox.com", "dl.dropboxusercontent.com");
          }
          return url;
        }
      }, {
        key: "render",
        value: function render() {
          var _this$props11 = this.props, url = _this$props11.url, playing = _this$props11.playing, loop = _this$props11.loop, controls = _this$props11.controls, muted = _this$props11.muted, config = _this$props11.config, width = _this$props11.width, height = _this$props11.height;
          var useAudio = this.shouldUseAudio(this.props);
          var Element2 = useAudio ? "audio" : "video";
          var style = {
            width: width === "auto" ? width : "100%",
            height: height === "auto" ? height : "100%"
          };
          return _react["default"].createElement(Element2, _extends2({
            ref: this.ref,
            src: this.getSource(url),
            style,
            preload: "auto",
            autoPlay: playing || void 0,
            controls,
            muted,
            loop
          }, config.attributes), url instanceof Array && url.map(this.renderSourceElement), config.tracks.map(this.renderTrack));
        }
      }]);
      return FilePlayer2;
    })(_react.Component);
    exports["default"] = FilePlayer;
    _defineProperty3(FilePlayer, "displayName", "FilePlayer");
    _defineProperty3(FilePlayer, "canPlay", _patterns.canPlay.file);
  }
});

// node_modules/react-player/lib/players/index.js
var require_players = __commonJS({
  "node_modules/react-player/lib/players/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = require_react();
    var _utils = require_utils();
    var _patterns = require_patterns();
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    var _default = [{
      key: "youtube",
      name: "YouTube",
      canPlay: _patterns.canPlay.youtube,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_YouTube());
        });
      })
    }, {
      key: "soundcloud",
      name: "SoundCloud",
      canPlay: _patterns.canPlay.soundcloud,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_SoundCloud());
        });
      })
    }, {
      key: "vimeo",
      name: "Vimeo",
      canPlay: _patterns.canPlay.vimeo,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Vimeo());
        });
      })
    }, {
      key: "facebook",
      name: "Facebook",
      canPlay: _patterns.canPlay.facebook,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Facebook());
        });
      })
    }, {
      key: "streamable",
      name: "Streamable",
      canPlay: _patterns.canPlay.streamable,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Streamable());
        });
      })
    }, {
      key: "wistia",
      name: "Wistia",
      canPlay: _patterns.canPlay.wistia,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Wistia());
        });
      })
    }, {
      key: "twitch",
      name: "Twitch",
      canPlay: _patterns.canPlay.twitch,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Twitch());
        });
      })
    }, {
      key: "dailymotion",
      name: "DailyMotion",
      canPlay: _patterns.canPlay.dailymotion,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_DailyMotion());
        });
      })
    }, {
      key: "mixcloud",
      name: "Mixcloud",
      canPlay: _patterns.canPlay.mixcloud,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Mixcloud());
        });
      })
    }, {
      key: "vidyard",
      name: "Vidyard",
      canPlay: _patterns.canPlay.vidyard,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Vidyard());
        });
      })
    }, {
      key: "kaltura",
      name: "Kaltura",
      canPlay: _patterns.canPlay.kaltura,
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_Kaltura());
        });
      })
    }, {
      key: "file",
      name: "FilePlayer",
      canPlay: _patterns.canPlay.file,
      canEnablePIP: function canEnablePIP(url) {
        return _patterns.canPlay.file(url) && (document.pictureInPictureEnabled || (0, _utils.supportsWebKitPresentationMode)()) && !_patterns.AUDIO_EXTENSIONS.test(url);
      },
      lazyPlayer: (0, _react.lazy)(function() {
        return Promise.resolve().then(function() {
          return _interopRequireWildcard(require_FilePlayer());
        });
      })
    }];
    exports["default"] = _default;
  }
});

// node_modules/memoize-one/dist/memoize-one.esm.js
var memoize_one_esm_exports = {};
__export(memoize_one_esm_exports, {
  default: () => memoize_one_esm_default
});
function isEqual(first, second) {
  if (first === second) {
    return true;
  }
  if (safeIsNaN(first) && safeIsNaN(second)) {
    return true;
  }
  return false;
}
function areInputsEqual(newInputs, lastInputs) {
  if (newInputs.length !== lastInputs.length) {
    return false;
  }
  for (var i2 = 0; i2 < newInputs.length; i2++) {
    if (!isEqual(newInputs[i2], lastInputs[i2])) {
      return false;
    }
  }
  return true;
}
function memoizeOne(resultFn, isEqual2) {
  if (isEqual2 === void 0) {
    isEqual2 = areInputsEqual;
  }
  var lastThis;
  var lastArgs = [];
  var lastResult;
  var calledOnce = false;
  function memoized() {
    var newArgs = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      newArgs[_i] = arguments[_i];
    }
    if (calledOnce && lastThis === this && isEqual2(newArgs, lastArgs)) {
      return lastResult;
    }
    lastResult = resultFn.apply(this, newArgs);
    calledOnce = true;
    lastThis = this;
    lastArgs = newArgs;
    return lastResult;
  }
  return memoized;
}
var safeIsNaN, memoize_one_esm_default;
var init_memoize_one_esm = __esm({
  "node_modules/memoize-one/dist/memoize-one.esm.js"() {
    safeIsNaN = Number.isNaN || function ponyfill(value) {
      return typeof value === "number" && value !== value;
    };
    memoize_one_esm_default = memoizeOne;
  }
});

// node_modules/react-is/cjs/react-is.development.js
var require_react_is_development = __commonJS({
  "node_modules/react-is/cjs/react-is.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var hasSymbol = typeof Symbol === "function" && Symbol.for;
        var REACT_ELEMENT_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.element") : 60103;
        var REACT_PORTAL_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.portal") : 60106;
        var REACT_FRAGMENT_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.fragment") : 60107;
        var REACT_STRICT_MODE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.strict_mode") : 60108;
        var REACT_PROFILER_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.profiler") : 60114;
        var REACT_PROVIDER_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.provider") : 60109;
        var REACT_CONTEXT_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.context") : 60110;
        var REACT_ASYNC_MODE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.async_mode") : 60111;
        var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.concurrent_mode") : 60111;
        var REACT_FORWARD_REF_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.forward_ref") : 60112;
        var REACT_SUSPENSE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.suspense") : 60113;
        var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.suspense_list") : 60120;
        var REACT_MEMO_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.memo") : 60115;
        var REACT_LAZY_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.lazy") : 60116;
        var REACT_BLOCK_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.block") : 60121;
        var REACT_FUNDAMENTAL_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.fundamental") : 60117;
        var REACT_RESPONDER_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.responder") : 60118;
        var REACT_SCOPE_TYPE = hasSymbol ? /* @__PURE__ */ Symbol.for("react.scope") : 60119;
        function isValidElementType(type) {
          return typeof type === "string" || typeof type === "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
          type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
        }
        function typeOf2(object) {
          if (typeof object === "object" && object !== null) {
            var $$typeof = object.$$typeof;
            switch ($$typeof) {
              case REACT_ELEMENT_TYPE:
                var type = object.type;
                switch (type) {
                  case REACT_ASYNC_MODE_TYPE:
                  case REACT_CONCURRENT_MODE_TYPE:
                  case REACT_FRAGMENT_TYPE:
                  case REACT_PROFILER_TYPE:
                  case REACT_STRICT_MODE_TYPE:
                  case REACT_SUSPENSE_TYPE:
                    return type;
                  default:
                    var $$typeofType = type && type.$$typeof;
                    switch ($$typeofType) {
                      case REACT_CONTEXT_TYPE:
                      case REACT_FORWARD_REF_TYPE:
                      case REACT_LAZY_TYPE:
                      case REACT_MEMO_TYPE:
                      case REACT_PROVIDER_TYPE:
                        return $$typeofType;
                      default:
                        return $$typeof;
                    }
                }
              case REACT_PORTAL_TYPE:
                return $$typeof;
            }
          }
          return void 0;
        }
        var AsyncMode = REACT_ASYNC_MODE_TYPE;
        var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
        var ContextConsumer = REACT_CONTEXT_TYPE;
        var ContextProvider = REACT_PROVIDER_TYPE;
        var Element2 = REACT_ELEMENT_TYPE;
        var ForwardRef = REACT_FORWARD_REF_TYPE;
        var Fragment6 = REACT_FRAGMENT_TYPE;
        var Lazy = REACT_LAZY_TYPE;
        var Memo = REACT_MEMO_TYPE;
        var Portal2 = REACT_PORTAL_TYPE;
        var Profiler = REACT_PROFILER_TYPE;
        var StrictMode = REACT_STRICT_MODE_TYPE;
        var Suspense = REACT_SUSPENSE_TYPE;
        var hasWarnedAboutDeprecatedIsAsyncMode = false;
        function isAsyncMode(object) {
          {
            if (!hasWarnedAboutDeprecatedIsAsyncMode) {
              hasWarnedAboutDeprecatedIsAsyncMode = true;
              console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
            }
          }
          return isConcurrentMode(object) || typeOf2(object) === REACT_ASYNC_MODE_TYPE;
        }
        function isConcurrentMode(object) {
          return typeOf2(object) === REACT_CONCURRENT_MODE_TYPE;
        }
        function isContextConsumer(object) {
          return typeOf2(object) === REACT_CONTEXT_TYPE;
        }
        function isContextProvider(object) {
          return typeOf2(object) === REACT_PROVIDER_TYPE;
        }
        function isElement3(object) {
          return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
        }
        function isForwardRef(object) {
          return typeOf2(object) === REACT_FORWARD_REF_TYPE;
        }
        function isFragment(object) {
          return typeOf2(object) === REACT_FRAGMENT_TYPE;
        }
        function isLazy(object) {
          return typeOf2(object) === REACT_LAZY_TYPE;
        }
        function isMemo(object) {
          return typeOf2(object) === REACT_MEMO_TYPE;
        }
        function isPortal(object) {
          return typeOf2(object) === REACT_PORTAL_TYPE;
        }
        function isProfiler(object) {
          return typeOf2(object) === REACT_PROFILER_TYPE;
        }
        function isStrictMode(object) {
          return typeOf2(object) === REACT_STRICT_MODE_TYPE;
        }
        function isSuspense(object) {
          return typeOf2(object) === REACT_SUSPENSE_TYPE;
        }
        exports.AsyncMode = AsyncMode;
        exports.ConcurrentMode = ConcurrentMode;
        exports.ContextConsumer = ContextConsumer;
        exports.ContextProvider = ContextProvider;
        exports.Element = Element2;
        exports.ForwardRef = ForwardRef;
        exports.Fragment = Fragment6;
        exports.Lazy = Lazy;
        exports.Memo = Memo;
        exports.Portal = Portal2;
        exports.Profiler = Profiler;
        exports.StrictMode = StrictMode;
        exports.Suspense = Suspense;
        exports.isAsyncMode = isAsyncMode;
        exports.isConcurrentMode = isConcurrentMode;
        exports.isContextConsumer = isContextConsumer;
        exports.isContextProvider = isContextProvider;
        exports.isElement = isElement3;
        exports.isForwardRef = isForwardRef;
        exports.isFragment = isFragment;
        exports.isLazy = isLazy;
        exports.isMemo = isMemo;
        exports.isPortal = isPortal;
        exports.isProfiler = isProfiler;
        exports.isStrictMode = isStrictMode;
        exports.isSuspense = isSuspense;
        exports.isValidElementType = isValidElementType;
        exports.typeOf = typeOf2;
      })();
    }
  }
});

// node_modules/react-is/index.js
var require_react_is = __commonJS({
  "node_modules/react-is/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_is_development();
    }
  }
});

// node_modules/object-assign/index.js
var require_object_assign = __commonJS({
  "node_modules/object-assign/index.js"(exports, module) {
    "use strict";
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var hasOwnProperty3 = Object.prototype.hasOwnProperty;
    var propIsEnumerable = Object.prototype.propertyIsEnumerable;
    function toObject(val) {
      if (val === null || val === void 0) {
        throw new TypeError("Object.assign cannot be called with null or undefined");
      }
      return Object(val);
    }
    function shouldUseNative() {
      try {
        if (!Object.assign) {
          return false;
        }
        var test1 = new String("abc");
        test1[5] = "de";
        if (Object.getOwnPropertyNames(test1)[0] === "5") {
          return false;
        }
        var test2 = {};
        for (var i2 = 0; i2 < 10; i2++) {
          test2["_" + String.fromCharCode(i2)] = i2;
        }
        var order2 = Object.getOwnPropertyNames(test2).map(function(n2) {
          return test2[n2];
        });
        if (order2.join("") !== "0123456789") {
          return false;
        }
        var test3 = {};
        "abcdefghijklmnopqrst".split("").forEach(function(letter) {
          test3[letter] = letter;
        });
        if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") {
          return false;
        }
        return true;
      } catch (err) {
        return false;
      }
    }
    module.exports = shouldUseNative() ? Object.assign : function(target, source) {
      var from;
      var to = toObject(target);
      var symbols;
      for (var s2 = 1; s2 < arguments.length; s2++) {
        from = Object(arguments[s2]);
        for (var key in from) {
          if (hasOwnProperty3.call(from, key)) {
            to[key] = from[key];
          }
        }
        if (getOwnPropertySymbols) {
          symbols = getOwnPropertySymbols(from);
          for (var i2 = 0; i2 < symbols.length; i2++) {
            if (propIsEnumerable.call(from, symbols[i2])) {
              to[symbols[i2]] = from[symbols[i2]];
            }
          }
        }
      }
      return to;
    };
  }
});

// node_modules/prop-types/lib/ReactPropTypesSecret.js
var require_ReactPropTypesSecret = __commonJS({
  "node_modules/prop-types/lib/ReactPropTypesSecret.js"(exports, module) {
    "use strict";
    var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
    module.exports = ReactPropTypesSecret;
  }
});

// node_modules/prop-types/lib/has.js
var require_has = __commonJS({
  "node_modules/prop-types/lib/has.js"(exports, module) {
    module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
  }
});

// node_modules/prop-types/checkPropTypes.js
var require_checkPropTypes = __commonJS({
  "node_modules/prop-types/checkPropTypes.js"(exports, module) {
    "use strict";
    var printWarning = function() {
    };
    if (true) {
      ReactPropTypesSecret = require_ReactPropTypesSecret();
      loggedTypeFailures = {};
      has = require_has();
      printWarning = function(text8) {
        var message = "Warning: " + text8;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x2) {
        }
      };
    }
    var ReactPropTypesSecret;
    var loggedTypeFailures;
    var has;
    function checkPropTypes(typeSpecs, values2, location2, componentName, getStack) {
      if (true) {
        for (var typeSpecName in typeSpecs) {
          if (has(typeSpecs, typeSpecName)) {
            var error2;
            try {
              if (typeof typeSpecs[typeSpecName] !== "function") {
                var err = Error(
                  (componentName || "React class") + ": " + location2 + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
                );
                err.name = "Invariant Violation";
                throw err;
              }
              error2 = typeSpecs[typeSpecName](values2, typeSpecName, componentName, location2, null, ReactPropTypesSecret);
            } catch (ex) {
              error2 = ex;
            }
            if (error2 && !(error2 instanceof Error)) {
              printWarning(
                (componentName || "React class") + ": type specification of " + location2 + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error2 + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
              );
            }
            if (error2 instanceof Error && !(error2.message in loggedTypeFailures)) {
              loggedTypeFailures[error2.message] = true;
              var stack = getStack ? getStack() : "";
              printWarning(
                "Failed " + location2 + " type: " + error2.message + (stack != null ? stack : "")
              );
            }
          }
        }
      }
    }
    checkPropTypes.resetWarningCache = function() {
      if (true) {
        loggedTypeFailures = {};
      }
    };
    module.exports = checkPropTypes;
  }
});

// node_modules/prop-types/factoryWithTypeCheckers.js
var require_factoryWithTypeCheckers = __commonJS({
  "node_modules/prop-types/factoryWithTypeCheckers.js"(exports, module) {
    "use strict";
    var ReactIs = require_react_is();
    var assign = require_object_assign();
    var ReactPropTypesSecret = require_ReactPropTypesSecret();
    var has = require_has();
    var checkPropTypes = require_checkPropTypes();
    var printWarning = function() {
    };
    if (true) {
      printWarning = function(text8) {
        var message = "Warning: " + text8;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x2) {
        }
      };
    }
    function emptyFunctionThatReturnsNull() {
      return null;
    }
    module.exports = function(isValidElement, throwOnDirectAccess) {
      var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
      var FAUX_ITERATOR_SYMBOL = "@@iterator";
      function getIteratorFn(maybeIterable) {
        var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
        if (typeof iteratorFn === "function") {
          return iteratorFn;
        }
      }
      var ANONYMOUS = "<<anonymous>>";
      var ReactPropTypes = {
        array: createPrimitiveTypeChecker("array"),
        bigint: createPrimitiveTypeChecker("bigint"),
        bool: createPrimitiveTypeChecker("boolean"),
        func: createPrimitiveTypeChecker("function"),
        number: createPrimitiveTypeChecker("number"),
        object: createPrimitiveTypeChecker("object"),
        string: createPrimitiveTypeChecker("string"),
        symbol: createPrimitiveTypeChecker("symbol"),
        any: createAnyTypeChecker(),
        arrayOf: createArrayOfTypeChecker,
        element: createElementTypeChecker(),
        elementType: createElementTypeTypeChecker(),
        instanceOf: createInstanceTypeChecker,
        node: createNodeChecker(),
        objectOf: createObjectOfTypeChecker,
        oneOf: createEnumTypeChecker,
        oneOfType: createUnionTypeChecker,
        shape: createShapeTypeChecker,
        exact: createStrictShapeTypeChecker
      };
      function is2(x2, y2) {
        if (x2 === y2) {
          return x2 !== 0 || 1 / x2 === 1 / y2;
        } else {
          return x2 !== x2 && y2 !== y2;
        }
      }
      function PropTypeError(message, data) {
        this.message = message;
        this.data = data && typeof data === "object" ? data : {};
        this.stack = "";
      }
      PropTypeError.prototype = Error.prototype;
      function createChainableTypeChecker(validate) {
        if (true) {
          var manualPropTypeCallCache = {};
          var manualPropTypeWarningCount = 0;
        }
        function checkType(isRequired, props, propName, componentName, location2, propFullName, secret) {
          componentName = componentName || ANONYMOUS;
          propFullName = propFullName || propName;
          if (secret !== ReactPropTypesSecret) {
            if (throwOnDirectAccess) {
              var err = new Error(
                "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
              );
              err.name = "Invariant Violation";
              throw err;
            } else if (typeof console !== "undefined") {
              var cacheKey = componentName + ":" + propName;
              if (!manualPropTypeCallCache[cacheKey] && // Avoid spamming the console because they are often not actionable except for lib authors
              manualPropTypeWarningCount < 3) {
                printWarning(
                  "You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
                );
                manualPropTypeCallCache[cacheKey] = true;
                manualPropTypeWarningCount++;
              }
            }
          }
          if (props[propName] == null) {
            if (isRequired) {
              if (props[propName] === null) {
                return new PropTypeError("The " + location2 + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
              }
              return new PropTypeError("The " + location2 + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
            }
            return null;
          } else {
            return validate(props, propName, componentName, location2, propFullName);
          }
        }
        var chainedCheckType = checkType.bind(null, false);
        chainedCheckType.isRequired = checkType.bind(null, true);
        return chainedCheckType;
      }
      function createPrimitiveTypeChecker(expectedType) {
        function validate(props, propName, componentName, location2, propFullName, secret) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== expectedType) {
            var preciseType = getPreciseType(propValue);
            return new PropTypeError(
              "Invalid " + location2 + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."),
              { expectedType }
            );
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createAnyTypeChecker() {
        return createChainableTypeChecker(emptyFunctionThatReturnsNull);
      }
      function createArrayOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location2, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
          }
          var propValue = props[propName];
          if (!Array.isArray(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
          }
          for (var i2 = 0; i2 < propValue.length; i2++) {
            var error2 = typeChecker(propValue, i2, componentName, location2, propFullName + "[" + i2 + "]", ReactPropTypesSecret);
            if (error2 instanceof Error) {
              return error2;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeChecker() {
        function validate(props, propName, componentName, location2, propFullName) {
          var propValue = props[propName];
          if (!isValidElement(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createElementTypeTypeChecker() {
        function validate(props, propName, componentName, location2, propFullName) {
          var propValue = props[propName];
          if (!ReactIs.isValidElementType(propValue)) {
            var propType = getPropType(propValue);
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createInstanceTypeChecker(expectedClass) {
        function validate(props, propName, componentName, location2, propFullName) {
          if (!(props[propName] instanceof expectedClass)) {
            var expectedClassName = expectedClass.name || ANONYMOUS;
            var actualClassName = getClassName(props[propName]);
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createEnumTypeChecker(expectedValues) {
        if (!Array.isArray(expectedValues)) {
          if (true) {
            if (arguments.length > 1) {
              printWarning(
                "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
              );
            } else {
              printWarning("Invalid argument supplied to oneOf, expected an array.");
            }
          }
          return emptyFunctionThatReturnsNull;
        }
        function validate(props, propName, componentName, location2, propFullName) {
          var propValue = props[propName];
          for (var i2 = 0; i2 < expectedValues.length; i2++) {
            if (is2(propValue, expectedValues[i2])) {
              return null;
            }
          }
          var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
            var type = getPreciseType(value);
            if (type === "symbol") {
              return String(value);
            }
            return value;
          });
          return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createObjectOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location2, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
          }
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
          }
          for (var key in propValue) {
            if (has(propValue, key)) {
              var error2 = typeChecker(propValue, key, componentName, location2, propFullName + "." + key, ReactPropTypesSecret);
              if (error2 instanceof Error) {
                return error2;
              }
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createUnionTypeChecker(arrayOfTypeCheckers) {
        if (!Array.isArray(arrayOfTypeCheckers)) {
          true ? printWarning("Invalid argument supplied to oneOfType, expected an instance of array.") : void 0;
          return emptyFunctionThatReturnsNull;
        }
        for (var i2 = 0; i2 < arrayOfTypeCheckers.length; i2++) {
          var checker = arrayOfTypeCheckers[i2];
          if (typeof checker !== "function") {
            printWarning(
              "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i2 + "."
            );
            return emptyFunctionThatReturnsNull;
          }
        }
        function validate(props, propName, componentName, location2, propFullName) {
          var expectedTypes = [];
          for (var i3 = 0; i3 < arrayOfTypeCheckers.length; i3++) {
            var checker2 = arrayOfTypeCheckers[i3];
            var checkerResult = checker2(props, propName, componentName, location2, propFullName, ReactPropTypesSecret);
            if (checkerResult == null) {
              return null;
            }
            if (checkerResult.data && has(checkerResult.data, "expectedType")) {
              expectedTypes.push(checkerResult.data.expectedType);
            }
          }
          var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
          return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
        }
        return createChainableTypeChecker(validate);
      }
      function createNodeChecker() {
        function validate(props, propName, componentName, location2, propFullName) {
          if (!isNode2(props[propName])) {
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function invalidValidatorError(componentName, location2, propFullName, key, type) {
        return new PropTypeError(
          (componentName || "React class") + ": " + location2 + " type `" + propFullName + "." + key + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`."
        );
      }
      function createShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location2, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          for (var key in shapeTypes) {
            var checker = shapeTypes[key];
            if (typeof checker !== "function") {
              return invalidValidatorError(componentName, location2, propFullName, key, getPreciseType(checker));
            }
            var error2 = checker(propValue, key, componentName, location2, propFullName + "." + key, ReactPropTypesSecret);
            if (error2) {
              return error2;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function createStrictShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location2, propFullName) {
          var propValue = props[propName];
          var propType = getPropType(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location2 + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          var allKeys2 = assign({}, props[propName], shapeTypes);
          for (var key in allKeys2) {
            var checker = shapeTypes[key];
            if (has(shapeTypes, key) && typeof checker !== "function") {
              return invalidValidatorError(componentName, location2, propFullName, key, getPreciseType(checker));
            }
            if (!checker) {
              return new PropTypeError(
                "Invalid " + location2 + " `" + propFullName + "` key `" + key + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  ")
              );
            }
            var error2 = checker(propValue, key, componentName, location2, propFullName + "." + key, ReactPropTypesSecret);
            if (error2) {
              return error2;
            }
          }
          return null;
        }
        return createChainableTypeChecker(validate);
      }
      function isNode2(propValue) {
        switch (typeof propValue) {
          case "number":
          case "string":
          case "undefined":
            return true;
          case "boolean":
            return !propValue;
          case "object":
            if (Array.isArray(propValue)) {
              return propValue.every(isNode2);
            }
            if (propValue === null || isValidElement(propValue)) {
              return true;
            }
            var iteratorFn = getIteratorFn(propValue);
            if (iteratorFn) {
              var iterator = iteratorFn.call(propValue);
              var step;
              if (iteratorFn !== propValue.entries) {
                while (!(step = iterator.next()).done) {
                  if (!isNode2(step.value)) {
                    return false;
                  }
                }
              } else {
                while (!(step = iterator.next()).done) {
                  var entry = step.value;
                  if (entry) {
                    if (!isNode2(entry[1])) {
                      return false;
                    }
                  }
                }
              }
            } else {
              return false;
            }
            return true;
          default:
            return false;
        }
      }
      function isSymbol(propType, propValue) {
        if (propType === "symbol") {
          return true;
        }
        if (!propValue) {
          return false;
        }
        if (propValue["@@toStringTag"] === "Symbol") {
          return true;
        }
        if (typeof Symbol === "function" && propValue instanceof Symbol) {
          return true;
        }
        return false;
      }
      function getPropType(propValue) {
        var propType = typeof propValue;
        if (Array.isArray(propValue)) {
          return "array";
        }
        if (propValue instanceof RegExp) {
          return "object";
        }
        if (isSymbol(propType, propValue)) {
          return "symbol";
        }
        return propType;
      }
      function getPreciseType(propValue) {
        if (typeof propValue === "undefined" || propValue === null) {
          return "" + propValue;
        }
        var propType = getPropType(propValue);
        if (propType === "object") {
          if (propValue instanceof Date) {
            return "date";
          } else if (propValue instanceof RegExp) {
            return "regexp";
          }
        }
        return propType;
      }
      function getPostfixForTypeWarning(value) {
        var type = getPreciseType(value);
        switch (type) {
          case "array":
          case "object":
            return "an " + type;
          case "boolean":
          case "date":
          case "regexp":
            return "a " + type;
          default:
            return type;
        }
      }
      function getClassName(propValue) {
        if (!propValue.constructor || !propValue.constructor.name) {
          return ANONYMOUS;
        }
        return propValue.constructor.name;
      }
      ReactPropTypes.checkPropTypes = checkPropTypes;
      ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
      ReactPropTypes.PropTypes = ReactPropTypes;
      return ReactPropTypes;
    };
  }
});

// node_modules/prop-types/index.js
var require_prop_types = __commonJS({
  "node_modules/prop-types/index.js"(exports, module) {
    if (true) {
      ReactIs = require_react_is();
      throwOnDirectAccess = true;
      module.exports = require_factoryWithTypeCheckers()(ReactIs.isElement, throwOnDirectAccess);
    } else {
      module.exports = null();
    }
    var ReactIs;
    var throwOnDirectAccess;
  }
});

// node_modules/react-player/lib/props.js
var require_props = __commonJS({
  "node_modules/react-player/lib/props.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.defaultProps = exports.propTypes = void 0;
    var _propTypes = _interopRequireDefault(require_prop_types());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    var string3 = _propTypes["default"].string;
    var bool = _propTypes["default"].bool;
    var number2 = _propTypes["default"].number;
    var array = _propTypes["default"].array;
    var oneOfType = _propTypes["default"].oneOfType;
    var shape = _propTypes["default"].shape;
    var object = _propTypes["default"].object;
    var func = _propTypes["default"].func;
    var node2 = _propTypes["default"].node;
    var propTypes = {
      url: oneOfType([string3, array, object]),
      playing: bool,
      loop: bool,
      controls: bool,
      volume: number2,
      muted: bool,
      playbackRate: number2,
      width: oneOfType([string3, number2]),
      height: oneOfType([string3, number2]),
      style: object,
      progressInterval: number2,
      playsinline: bool,
      pip: bool,
      stopOnUnmount: bool,
      light: oneOfType([bool, string3]),
      playIcon: node2,
      previewTabIndex: number2,
      fallback: node2,
      oEmbedUrl: string3,
      wrapper: oneOfType([string3, func, shape({
        render: func.isRequired
      })]),
      config: shape({
        soundcloud: shape({
          options: object
        }),
        youtube: shape({
          playerVars: object,
          embedOptions: object,
          onUnstarted: func
        }),
        facebook: shape({
          appId: string3,
          version: string3,
          playerId: string3,
          attributes: object
        }),
        dailymotion: shape({
          params: object
        }),
        vimeo: shape({
          playerOptions: object,
          title: string3
        }),
        file: shape({
          attributes: object,
          tracks: array,
          forceVideo: bool,
          forceAudio: bool,
          forceHLS: bool,
          forceDASH: bool,
          forceFLV: bool,
          hlsOptions: object,
          hlsVersion: string3,
          dashVersion: string3,
          flvVersion: string3
        }),
        wistia: shape({
          options: object,
          playerId: string3,
          customControls: array
        }),
        mixcloud: shape({
          options: object
        }),
        twitch: shape({
          options: object,
          playerId: string3
        }),
        vidyard: shape({
          options: object
        })
      }),
      onReady: func,
      onStart: func,
      onPlay: func,
      onPause: func,
      onBuffer: func,
      onBufferEnd: func,
      onEnded: func,
      onError: func,
      onDuration: func,
      onSeek: func,
      onPlaybackRateChange: func,
      onProgress: func,
      onClickPreview: func,
      onEnablePIP: func,
      onDisablePIP: func
    };
    exports.propTypes = propTypes;
    var noop12 = function noop13() {
    };
    var defaultProps2 = {
      playing: false,
      loop: false,
      controls: false,
      volume: null,
      muted: false,
      playbackRate: 1,
      width: "640px",
      height: "360px",
      style: {},
      progressInterval: 1e3,
      playsinline: false,
      pip: false,
      stopOnUnmount: true,
      light: false,
      fallback: null,
      wrapper: "div",
      previewTabIndex: 0,
      oEmbedUrl: "https://noembed.com/embed?url={url}",
      config: {
        soundcloud: {
          options: {
            visual: true,
            // Undocumented, but makes player fill container and look better
            buying: false,
            liking: false,
            download: false,
            sharing: false,
            show_comments: false,
            show_playcount: false
          }
        },
        youtube: {
          playerVars: {
            playsinline: 1,
            showinfo: 0,
            rel: 0,
            iv_load_policy: 3,
            modestbranding: 1
          },
          embedOptions: {},
          onUnstarted: noop12
        },
        facebook: {
          appId: "1309697205772819",
          version: "v3.3",
          playerId: null,
          attributes: {}
        },
        dailymotion: {
          params: {
            api: 1,
            "endscreen-enable": false
          }
        },
        vimeo: {
          playerOptions: {
            autopause: false,
            byline: false,
            portrait: false,
            title: false
          },
          title: null
        },
        file: {
          attributes: {},
          tracks: [],
          forceVideo: false,
          forceAudio: false,
          forceHLS: false,
          forceDASH: false,
          forceFLV: false,
          hlsOptions: {},
          hlsVersion: "1.1.4",
          dashVersion: "3.1.3",
          flvVersion: "1.5.0"
        },
        wistia: {
          options: {},
          playerId: null,
          customControls: null
        },
        mixcloud: {
          options: {
            hide_cover: 1
          }
        },
        twitch: {
          options: {},
          playerId: null
        },
        vidyard: {
          options: {}
        }
      },
      onReady: noop12,
      onStart: noop12,
      onPlay: noop12,
      onPause: noop12,
      onBuffer: noop12,
      onBufferEnd: noop12,
      onEnded: noop12,
      onError: noop12,
      onDuration: noop12,
      onSeek: noop12,
      onPlaybackRateChange: noop12,
      onProgress: noop12,
      onClickPreview: noop12,
      onEnablePIP: noop12,
      onDisablePIP: noop12
    };
    exports.defaultProps = defaultProps2;
  }
});

// node_modules/react-player/lib/Player.js
var require_Player = __commonJS({
  "node_modules/react-player/lib/Player.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _reactFastCompare = _interopRequireDefault(require_react_fast_compare());
    var _props = require_props();
    var _utils = require_utils();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    function _extends2() {
      _extends2 = Object.assign || function(target) {
        for (var i2 = 1; i2 < arguments.length; i2++) {
          var source = arguments[i2];
          for (var key in source) {
            if (Object.prototype.hasOwnProperty.call(source, key)) {
              target[key] = source[key];
            }
          }
        }
        return target;
      };
      return _extends2.apply(this, arguments);
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var SEEK_ON_PLAY_EXPIRY = 5e3;
    var Player = (function(_Component) {
      _inherits(Player2, _Component);
      var _super = _createSuper(Player2);
      function Player2() {
        var _this;
        _classCallCheck(this, Player2);
        for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
          _args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(_args));
        _defineProperty3(_assertThisInitialized(_this), "mounted", false);
        _defineProperty3(_assertThisInitialized(_this), "isReady", false);
        _defineProperty3(_assertThisInitialized(_this), "isPlaying", false);
        _defineProperty3(_assertThisInitialized(_this), "isLoading", true);
        _defineProperty3(_assertThisInitialized(_this), "loadOnReady", null);
        _defineProperty3(_assertThisInitialized(_this), "startOnPlay", true);
        _defineProperty3(_assertThisInitialized(_this), "seekOnPlay", null);
        _defineProperty3(_assertThisInitialized(_this), "onDurationCalled", false);
        _defineProperty3(_assertThisInitialized(_this), "handlePlayerMount", function(player) {
          if (_this.player) return;
          _this.player = player;
          _this.player.load(_this.props.url);
          _this.progress();
        });
        _defineProperty3(_assertThisInitialized(_this), "getInternalPlayer", function(key) {
          if (!_this.player) return null;
          return _this.player[key];
        });
        _defineProperty3(_assertThisInitialized(_this), "progress", function() {
          if (_this.props.url && _this.player && _this.isReady) {
            var playedSeconds = _this.getCurrentTime() || 0;
            var loadedSeconds = _this.getSecondsLoaded();
            var duration2 = _this.getDuration();
            if (duration2) {
              var progress = {
                playedSeconds,
                played: playedSeconds / duration2
              };
              if (loadedSeconds !== null) {
                progress.loadedSeconds = loadedSeconds;
                progress.loaded = loadedSeconds / duration2;
              }
              if (progress.playedSeconds !== _this.prevPlayed || progress.loadedSeconds !== _this.prevLoaded) {
                _this.props.onProgress(progress);
              }
              _this.prevPlayed = progress.playedSeconds;
              _this.prevLoaded = progress.loadedSeconds;
            }
          }
          _this.progressTimeout = setTimeout(_this.progress, _this.props.progressFrequency || _this.props.progressInterval);
        });
        _defineProperty3(_assertThisInitialized(_this), "handleReady", function() {
          if (!_this.mounted) return;
          _this.isReady = true;
          _this.isLoading = false;
          var _this$props = _this.props, onReady = _this$props.onReady, playing = _this$props.playing, volume = _this$props.volume, muted = _this$props.muted;
          onReady();
          if (!muted && volume !== null) {
            _this.player.setVolume(volume);
          }
          if (_this.loadOnReady) {
            _this.player.load(_this.loadOnReady, true);
            _this.loadOnReady = null;
          } else if (playing) {
            _this.player.play();
          }
          _this.handleDurationCheck();
        });
        _defineProperty3(_assertThisInitialized(_this), "handlePlay", function() {
          _this.isPlaying = true;
          _this.isLoading = false;
          var _this$props2 = _this.props, onStart = _this$props2.onStart, onPlay = _this$props2.onPlay, playbackRate = _this$props2.playbackRate;
          if (_this.startOnPlay) {
            if (_this.player.setPlaybackRate && playbackRate !== 1) {
              _this.player.setPlaybackRate(playbackRate);
            }
            onStart();
            _this.startOnPlay = false;
          }
          onPlay();
          if (_this.seekOnPlay) {
            _this.seekTo(_this.seekOnPlay);
            _this.seekOnPlay = null;
          }
          _this.handleDurationCheck();
        });
        _defineProperty3(_assertThisInitialized(_this), "handlePause", function(e) {
          _this.isPlaying = false;
          if (!_this.isLoading) {
            _this.props.onPause(e);
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "handleEnded", function() {
          var _this$props3 = _this.props, activePlayer = _this$props3.activePlayer, loop = _this$props3.loop, onEnded = _this$props3.onEnded;
          if (activePlayer.loopOnEnded && loop) {
            _this.seekTo(0);
          }
          if (!loop) {
            _this.isPlaying = false;
            onEnded();
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "handleError", function() {
          var _this$props4;
          _this.isLoading = false;
          (_this$props4 = _this.props).onError.apply(_this$props4, arguments);
        });
        _defineProperty3(_assertThisInitialized(_this), "handleDurationCheck", function() {
          clearTimeout(_this.durationCheckTimeout);
          var duration2 = _this.getDuration();
          if (duration2) {
            if (!_this.onDurationCalled) {
              _this.props.onDuration(duration2);
              _this.onDurationCalled = true;
            }
          } else {
            _this.durationCheckTimeout = setTimeout(_this.handleDurationCheck, 100);
          }
        });
        _defineProperty3(_assertThisInitialized(_this), "handleLoaded", function() {
          _this.isLoading = false;
        });
        return _this;
      }
      _createClass(Player2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.mounted = true;
        }
      }, {
        key: "componentWillUnmount",
        value: function componentWillUnmount() {
          clearTimeout(this.progressTimeout);
          clearTimeout(this.durationCheckTimeout);
          if (this.isReady && this.props.stopOnUnmount) {
            this.player.stop();
            if (this.player.disablePIP) {
              this.player.disablePIP();
            }
          }
          this.mounted = false;
        }
      }, {
        key: "componentDidUpdate",
        value: function componentDidUpdate(prevProps) {
          var _this2 = this;
          if (!this.player) {
            return;
          }
          var _this$props5 = this.props, url = _this$props5.url, playing = _this$props5.playing, volume = _this$props5.volume, muted = _this$props5.muted, playbackRate = _this$props5.playbackRate, pip = _this$props5.pip, loop = _this$props5.loop, activePlayer = _this$props5.activePlayer, disableDeferredLoading = _this$props5.disableDeferredLoading;
          if (!(0, _reactFastCompare["default"])(prevProps.url, url)) {
            if (this.isLoading && !activePlayer.forceLoad && !disableDeferredLoading && !(0, _utils.isMediaStream)(url)) {
              console.warn("ReactPlayer: the attempt to load ".concat(url, " is being deferred until the player has loaded"));
              this.loadOnReady = url;
              return;
            }
            this.isLoading = true;
            this.startOnPlay = true;
            this.onDurationCalled = false;
            this.player.load(url, this.isReady);
          }
          if (!prevProps.playing && playing && !this.isPlaying) {
            this.player.play();
          }
          if (prevProps.playing && !playing && this.isPlaying) {
            this.player.pause();
          }
          if (!prevProps.pip && pip && this.player.enablePIP) {
            this.player.enablePIP();
          }
          if (prevProps.pip && !pip && this.player.disablePIP) {
            this.player.disablePIP();
          }
          if (prevProps.volume !== volume && volume !== null) {
            this.player.setVolume(volume);
          }
          if (prevProps.muted !== muted) {
            if (muted) {
              this.player.mute();
            } else {
              this.player.unmute();
              if (volume !== null) {
                setTimeout(function() {
                  return _this2.player.setVolume(volume);
                });
              }
            }
          }
          if (prevProps.playbackRate !== playbackRate && this.player.setPlaybackRate) {
            this.player.setPlaybackRate(playbackRate);
          }
          if (prevProps.loop !== loop && this.player.setLoop) {
            this.player.setLoop(loop);
          }
        }
      }, {
        key: "getDuration",
        value: function getDuration() {
          if (!this.isReady) return null;
          return this.player.getDuration();
        }
      }, {
        key: "getCurrentTime",
        value: function getCurrentTime() {
          if (!this.isReady) return null;
          return this.player.getCurrentTime();
        }
      }, {
        key: "getSecondsLoaded",
        value: function getSecondsLoaded() {
          if (!this.isReady) return null;
          return this.player.getSecondsLoaded();
        }
      }, {
        key: "seekTo",
        value: function seekTo(amount, type) {
          var _this3 = this;
          if (!this.isReady) {
            if (amount !== 0) {
              this.seekOnPlay = amount;
              setTimeout(function() {
                _this3.seekOnPlay = null;
              }, SEEK_ON_PLAY_EXPIRY);
            }
            return;
          }
          var isFraction = !type ? amount > 0 && amount < 1 : type === "fraction";
          if (isFraction) {
            var duration2 = this.player.getDuration();
            if (!duration2) {
              console.warn("ReactPlayer: could not seek using fraction – duration not yet available");
              return;
            }
            this.player.seekTo(duration2 * amount);
            return;
          }
          this.player.seekTo(amount);
        }
      }, {
        key: "render",
        value: function render() {
          var Player3 = this.props.activePlayer;
          if (!Player3) {
            return null;
          }
          return _react["default"].createElement(Player3, _extends2({}, this.props, {
            onMount: this.handlePlayerMount,
            onReady: this.handleReady,
            onPlay: this.handlePlay,
            onPause: this.handlePause,
            onEnded: this.handleEnded,
            onLoaded: this.handleLoaded,
            onError: this.handleError
          }));
        }
      }]);
      return Player2;
    })(_react.Component);
    exports["default"] = Player;
    _defineProperty3(Player, "displayName", "Player");
    _defineProperty3(Player, "propTypes", _props.propTypes);
    _defineProperty3(Player, "defaultProps", _props.defaultProps);
  }
});

// node_modules/react-player/lib/Preview.js
var require_Preview = __commonJS({
  "node_modules/react-player/lib/Preview.js"(exports) {
    "use strict";
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _react = _interopRequireWildcard(require_react());
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache2 = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache2;
      };
      return cache2;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache2 = _getRequireWildcardCache();
      if (cache2 && cache2.has(obj)) {
        return cache2.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache2) {
        cache2.set(obj, newObj);
      }
      return newObj;
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    var ICON_SIZE = "64px";
    var cache = {};
    var Preview = (function(_Component) {
      _inherits(Preview2, _Component);
      var _super = _createSuper(Preview2);
      function Preview2() {
        var _this;
        _classCallCheck(this, Preview2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _super.call.apply(_super, [this].concat(args));
        _defineProperty3(_assertThisInitialized(_this), "mounted", false);
        _defineProperty3(_assertThisInitialized(_this), "state", {
          image: null
        });
        _defineProperty3(_assertThisInitialized(_this), "handleKeyPress", function(e) {
          if (e.key === "Enter" || e.key === " ") {
            _this.props.onClick();
          }
        });
        return _this;
      }
      _createClass(Preview2, [{
        key: "componentDidMount",
        value: function componentDidMount() {
          this.mounted = true;
          this.fetchImage(this.props);
        }
      }, {
        key: "componentDidUpdate",
        value: function componentDidUpdate(prevProps) {
          var _this$props = this.props, url = _this$props.url, light = _this$props.light;
          if (prevProps.url !== url || prevProps.light !== light) {
            this.fetchImage(this.props);
          }
        }
      }, {
        key: "componentWillUnmount",
        value: function componentWillUnmount() {
          this.mounted = false;
        }
      }, {
        key: "fetchImage",
        value: function fetchImage(_ref) {
          var _this2 = this;
          var url = _ref.url, light = _ref.light, oEmbedUrl = _ref.oEmbedUrl;
          if (typeof light === "string") {
            this.setState({
              image: light
            });
            return;
          }
          if (cache[url]) {
            this.setState({
              image: cache[url]
            });
            return;
          }
          this.setState({
            image: null
          });
          return window.fetch(oEmbedUrl.replace("{url}", url)).then(function(response) {
            return response.json();
          }).then(function(data) {
            if (data.thumbnail_url && _this2.mounted) {
              var image3 = data.thumbnail_url.replace("height=100", "height=480");
              _this2.setState({
                image: image3
              });
              cache[url] = image3;
            }
          });
        }
      }, {
        key: "render",
        value: function render() {
          var _this$props2 = this.props, onClick = _this$props2.onClick, playIcon = _this$props2.playIcon, previewTabIndex = _this$props2.previewTabIndex;
          var image3 = this.state.image;
          var flexCenter = {
            display: "flex",
            alignItems: "center",
            justifyContent: "center"
          };
          var styles = {
            preview: _objectSpread3({
              width: "100%",
              height: "100%",
              backgroundImage: image3 ? "url(".concat(image3, ")") : void 0,
              backgroundSize: "cover",
              backgroundPosition: "center",
              cursor: "pointer"
            }, flexCenter),
            shadow: _objectSpread3({
              background: "radial-gradient(rgb(0, 0, 0, 0.3), rgba(0, 0, 0, 0) 60%)",
              borderRadius: ICON_SIZE,
              width: ICON_SIZE,
              height: ICON_SIZE
            }, flexCenter),
            playIcon: {
              borderStyle: "solid",
              borderWidth: "16px 0 16px 26px",
              borderColor: "transparent transparent transparent white",
              marginLeft: "7px"
            }
          };
          var defaultPlayIcon = _react["default"].createElement("div", {
            style: styles.shadow,
            className: "react-player__shadow"
          }, _react["default"].createElement("div", {
            style: styles.playIcon,
            className: "react-player__play-icon"
          }));
          return _react["default"].createElement("div", {
            style: styles.preview,
            className: "react-player__preview",
            onClick,
            tabIndex: previewTabIndex,
            onKeyPress: this.handleKeyPress
          }, playIcon || defaultPlayIcon);
        }
      }]);
      return Preview2;
    })(_react.Component);
    exports["default"] = Preview;
  }
});

// node_modules/react-player/lib/ReactPlayer.js
var require_ReactPlayer = __commonJS({
  "node_modules/react-player/lib/ReactPlayer.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.createReactPlayer = void 0;
    var _react = _interopRequireWildcard(require_react());
    var _deepmerge = _interopRequireDefault(require_cjs());
    var _memoizeOne = _interopRequireDefault((init_memoize_one_esm(), __toCommonJS(memoize_one_esm_exports)));
    var _reactFastCompare = _interopRequireDefault(require_react_fast_compare());
    var _props = require_props();
    var _utils = require_utils();
    var _Player3 = _interopRequireDefault(require_Player());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    function _typeof(obj) {
      "@babel/helpers - typeof";
      if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
        _typeof = function _typeof2(obj2) {
          return typeof obj2;
        };
      } else {
        _typeof = function _typeof2(obj2) {
          return obj2 && typeof Symbol === "function" && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
        };
      }
      return _typeof(obj);
    }
    function ownKeys3(object, enumerableOnly) {
      var keys3 = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        });
        keys3.push.apply(keys3, symbols);
      }
      return keys3;
    }
    function _objectSpread3(target) {
      for (var i2 = 1; i2 < arguments.length; i2++) {
        var source = arguments[i2] != null ? arguments[i2] : {};
        if (i2 % 2) {
          ownKeys3(Object(source), true).forEach(function(key) {
            _defineProperty3(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys3(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    function _extends2() {
      _extends2 = Object.assign || function(target) {
        for (var i2 = 1; i2 < arguments.length; i2++) {
          var source = arguments[i2];
          for (var key in source) {
            if (Object.prototype.hasOwnProperty.call(source, key)) {
              target[key] = source[key];
            }
          }
        }
        return target;
      };
      return _extends2.apply(this, arguments);
    }
    function _toConsumableArray3(arr) {
      return _arrayWithoutHoles3(arr) || _iterableToArray3(arr) || _unsupportedIterableToArray4(arr) || _nonIterableSpread3();
    }
    function _nonIterableSpread3() {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    function _unsupportedIterableToArray4(o2, minLen) {
      if (!o2) return;
      if (typeof o2 === "string") return _arrayLikeToArray4(o2, minLen);
      var n2 = Object.prototype.toString.call(o2).slice(8, -1);
      if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
      if (n2 === "Map" || n2 === "Set") return Array.from(o2);
      if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray4(o2, minLen);
    }
    function _iterableToArray3(iter) {
      if (typeof Symbol !== "undefined" && Symbol.iterator in Object(iter)) return Array.from(iter);
    }
    function _arrayWithoutHoles3(arr) {
      if (Array.isArray(arr)) return _arrayLikeToArray4(arr);
    }
    function _arrayLikeToArray4(arr, len) {
      if (len == null || len > arr.length) len = arr.length;
      for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) {
        arr2[i2] = arr[i2];
      }
      return arr2;
    }
    function _classCallCheck(instance2, Constructor) {
      if (!(instance2 instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _defineProperties(target, props) {
      for (var i2 = 0; i2 < props.length; i2++) {
        var descriptor = props[i2];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
      }
    }
    function _createClass(Constructor, protoProps, staticProps) {
      if (protoProps) _defineProperties(Constructor.prototype, protoProps);
      if (staticProps) _defineProperties(Constructor, staticProps);
      return Constructor;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } });
      if (superClass) _setPrototypeOf(subClass, superClass);
    }
    function _setPrototypeOf(o2, p2) {
      _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf2(o3, p3) {
        o3.__proto__ = p3;
        return o3;
      };
      return _setPrototypeOf(o2, p2);
    }
    function _createSuper(Derived) {
      var hasNativeReflectConstruct = _isNativeReflectConstruct();
      return function _createSuperInternal() {
        var Super = _getPrototypeOf(Derived), result;
        if (hasNativeReflectConstruct) {
          var NewTarget = _getPrototypeOf(this).constructor;
          result = Reflect.construct(Super, arguments, NewTarget);
        } else {
          result = Super.apply(this, arguments);
        }
        return _possibleConstructorReturn(this, result);
      };
    }
    function _possibleConstructorReturn(self2, call2) {
      if (call2 && (_typeof(call2) === "object" || typeof call2 === "function")) {
        return call2;
      }
      return _assertThisInitialized(self2);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _isNativeReflectConstruct() {
      if (typeof Reflect === "undefined" || !Reflect.construct) return false;
      if (Reflect.construct.sham) return false;
      if (typeof Proxy === "function") return true;
      try {
        Date.prototype.toString.call(Reflect.construct(Date, [], function() {
        }));
        return true;
      } catch (e) {
        return false;
      }
    }
    function _getPrototypeOf(o2) {
      _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function _getPrototypeOf2(o3) {
        return o3.__proto__ || Object.getPrototypeOf(o3);
      };
      return _getPrototypeOf(o2);
    }
    function _defineProperty3(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    function _getRequireWildcardCache() {
      if (typeof WeakMap !== "function") return null;
      var cache = /* @__PURE__ */ new WeakMap();
      _getRequireWildcardCache = function _getRequireWildcardCache2() {
        return cache;
      };
      return cache;
    }
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      }
      if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") {
        return { "default": obj };
      }
      var cache = _getRequireWildcardCache();
      if (cache && cache.has(obj)) {
        return cache.get(obj);
      }
      var newObj = {};
      var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
      for (var key in obj) {
        if (Object.prototype.hasOwnProperty.call(obj, key)) {
          var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
          if (desc && (desc.get || desc.set)) {
            Object.defineProperty(newObj, key, desc);
          } else {
            newObj[key] = obj[key];
          }
        }
      }
      newObj["default"] = obj;
      if (cache) {
        cache.set(obj, newObj);
      }
      return newObj;
    }
    var Preview = (0, _react.lazy)(function() {
      return Promise.resolve().then(function() {
        return _interopRequireWildcard(require_Preview());
      });
    });
    var IS_BROWSER = typeof window !== "undefined" && window.document;
    var IS_GLOBAL = typeof global !== "undefined" && global.window && global.window.document;
    var SUPPORTED_PROPS = Object.keys(_props.propTypes);
    var UniversalSuspense = IS_BROWSER || IS_GLOBAL ? _react.Suspense : function() {
      return null;
    };
    var customPlayers = [];
    var createReactPlayer = function createReactPlayer2(players, fallback) {
      var _class, _temp;
      return _temp = _class = (function(_Component) {
        _inherits(ReactPlayer2, _Component);
        var _super = _createSuper(ReactPlayer2);
        function ReactPlayer2() {
          var _this;
          _classCallCheck(this, ReactPlayer2);
          for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
            args[_key] = arguments[_key];
          }
          _this = _super.call.apply(_super, [this].concat(args));
          _defineProperty3(_assertThisInitialized(_this), "state", {
            showPreview: !!_this.props.light
          });
          _defineProperty3(_assertThisInitialized(_this), "references", {
            wrapper: function wrapper(_wrapper) {
              _this.wrapper = _wrapper;
            },
            player: function player(_player) {
              _this.player = _player;
            }
          });
          _defineProperty3(_assertThisInitialized(_this), "handleClickPreview", function(e) {
            _this.setState({
              showPreview: false
            });
            _this.props.onClickPreview(e);
          });
          _defineProperty3(_assertThisInitialized(_this), "showPreview", function() {
            _this.setState({
              showPreview: true
            });
          });
          _defineProperty3(_assertThisInitialized(_this), "getDuration", function() {
            if (!_this.player) return null;
            return _this.player.getDuration();
          });
          _defineProperty3(_assertThisInitialized(_this), "getCurrentTime", function() {
            if (!_this.player) return null;
            return _this.player.getCurrentTime();
          });
          _defineProperty3(_assertThisInitialized(_this), "getSecondsLoaded", function() {
            if (!_this.player) return null;
            return _this.player.getSecondsLoaded();
          });
          _defineProperty3(_assertThisInitialized(_this), "getInternalPlayer", function() {
            var key = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "player";
            if (!_this.player) return null;
            return _this.player.getInternalPlayer(key);
          });
          _defineProperty3(_assertThisInitialized(_this), "seekTo", function(fraction, type) {
            if (!_this.player) return null;
            _this.player.seekTo(fraction, type);
          });
          _defineProperty3(_assertThisInitialized(_this), "handleReady", function() {
            _this.props.onReady(_assertThisInitialized(_this));
          });
          _defineProperty3(_assertThisInitialized(_this), "getActivePlayer", (0, _memoizeOne["default"])(function(url) {
            for (var _i = 0, _arr = [].concat(customPlayers, _toConsumableArray3(players)); _i < _arr.length; _i++) {
              var player = _arr[_i];
              if (player.canPlay(url)) {
                return player;
              }
            }
            if (fallback) {
              return fallback;
            }
            return null;
          }));
          _defineProperty3(_assertThisInitialized(_this), "getConfig", (0, _memoizeOne["default"])(function(url, key) {
            var config = _this.props.config;
            return _deepmerge["default"].all([_props.defaultProps.config, _props.defaultProps.config[key] || {}, config, config[key] || {}]);
          }));
          _defineProperty3(_assertThisInitialized(_this), "getAttributes", (0, _memoizeOne["default"])(function(url) {
            return (0, _utils.omit)(_this.props, SUPPORTED_PROPS);
          }));
          _defineProperty3(_assertThisInitialized(_this), "renderActivePlayer", function(url) {
            if (!url) return null;
            var player = _this.getActivePlayer(url);
            if (!player) return null;
            var config = _this.getConfig(url, player.key);
            return _react["default"].createElement(_Player3["default"], _extends2({}, _this.props, {
              key: player.key,
              ref: _this.references.player,
              config,
              activePlayer: player.lazyPlayer || player,
              onReady: _this.handleReady
            }));
          });
          return _this;
        }
        _createClass(ReactPlayer2, [{
          key: "shouldComponentUpdate",
          value: function shouldComponentUpdate(nextProps, nextState) {
            return !(0, _reactFastCompare["default"])(this.props, nextProps) || !(0, _reactFastCompare["default"])(this.state, nextState);
          }
        }, {
          key: "componentDidUpdate",
          value: function componentDidUpdate(prevProps) {
            var light = this.props.light;
            if (!prevProps.light && light) {
              this.setState({
                showPreview: true
              });
            }
            if (prevProps.light && !light) {
              this.setState({
                showPreview: false
              });
            }
          }
        }, {
          key: "renderPreview",
          value: function renderPreview(url) {
            if (!url) return null;
            var _this$props = this.props, light = _this$props.light, playIcon = _this$props.playIcon, previewTabIndex = _this$props.previewTabIndex, oEmbedUrl = _this$props.oEmbedUrl;
            return _react["default"].createElement(Preview, {
              url,
              light,
              playIcon,
              previewTabIndex,
              oEmbedUrl,
              onClick: this.handleClickPreview
            });
          }
        }, {
          key: "render",
          value: function render() {
            var _this$props2 = this.props, url = _this$props2.url, style = _this$props2.style, width = _this$props2.width, height = _this$props2.height, fallback2 = _this$props2.fallback, Wrapper = _this$props2.wrapper;
            var showPreview = this.state.showPreview;
            var attributes = this.getAttributes(url);
            return _react["default"].createElement(Wrapper, _extends2({
              ref: this.references.wrapper,
              style: _objectSpread3(_objectSpread3({}, style), {}, {
                width,
                height
              })
            }, attributes), _react["default"].createElement(UniversalSuspense, {
              fallback: fallback2
            }, showPreview ? this.renderPreview(url) : this.renderActivePlayer(url)));
          }
        }]);
        return ReactPlayer2;
      })(_react.Component), _defineProperty3(_class, "displayName", "ReactPlayer"), _defineProperty3(_class, "propTypes", _props.propTypes), _defineProperty3(_class, "defaultProps", _props.defaultProps), _defineProperty3(_class, "addCustomPlayer", function(player) {
        customPlayers.push(player);
      }), _defineProperty3(_class, "removeCustomPlayers", function() {
        customPlayers.length = 0;
      }), _defineProperty3(_class, "canPlay", function(url) {
        for (var _i2 = 0, _arr2 = [].concat(customPlayers, _toConsumableArray3(players)); _i2 < _arr2.length; _i2++) {
          var _Player = _arr2[_i2];
          if (_Player.canPlay(url)) {
            return true;
          }
        }
        return false;
      }), _defineProperty3(_class, "canEnablePIP", function(url) {
        for (var _i3 = 0, _arr3 = [].concat(customPlayers, _toConsumableArray3(players)); _i3 < _arr3.length; _i3++) {
          var _Player2 = _arr3[_i3];
          if (_Player2.canEnablePIP && _Player2.canEnablePIP(url)) {
            return true;
          }
        }
        return false;
      }), _temp;
    };
    exports.createReactPlayer = createReactPlayer;
  }
});

// node_modules/react-player/lib/index.js
var require_lib = __commonJS({
  "node_modules/react-player/lib/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports["default"] = void 0;
    var _players = _interopRequireDefault(require_players());
    var _ReactPlayer = require_ReactPlayer();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    var fallback = _players["default"][_players["default"].length - 1];
    var _default = (0, _ReactPlayer.createReactPlayer)(_players["default"], fallback);
    exports["default"] = _default;
  }
});

// node_modules/inline-style-parser/cjs/index.js
var require_cjs2 = __commonJS({
  "node_modules/inline-style-parser/cjs/index.js"(exports, module) {
    "use strict";
    var COMMENT_REGEX = /\/\*[^*]*\*+([^/*][^*]*\*+)*\//g;
    var NEWLINE_REGEX = /\n/g;
    var WHITESPACE_REGEX = /^\s*/;
    var PROPERTY_REGEX = /^(\*?[-#/*\\\w]+(\[[0-9a-z_-]+\])?)\s*/;
    var COLON_REGEX = /^:\s*/;
    var VALUE_REGEX = /^((?:'(?:\\'|.)*?'|"(?:\\"|.)*?"|\([^)]*?\)|[^};])+)/;
    var SEMICOLON_REGEX = /^[;\s]*/;
    var TRIM_REGEX = /^\s+|\s+$/g;
    var NEWLINE = "\n";
    var FORWARD_SLASH = "/";
    var ASTERISK = "*";
    var EMPTY_STRING = "";
    var TYPE_COMMENT = "comment";
    var TYPE_DECLARATION = "declaration";
    function index6(style, options) {
      if (typeof style !== "string") {
        throw new TypeError("First argument must be a string");
      }
      if (!style) return [];
      options = options || {};
      var lineno = 1;
      var column = 1;
      function updatePosition(str) {
        var lines = str.match(NEWLINE_REGEX);
        if (lines) lineno += lines.length;
        var i2 = str.lastIndexOf(NEWLINE);
        column = ~i2 ? str.length - i2 : column + str.length;
      }
      function position3() {
        var start2 = { line: lineno, column };
        return function(node2) {
          node2.position = new Position(start2);
          whitespace2();
          return node2;
        };
      }
      function Position(start2) {
        this.start = start2;
        this.end = { line: lineno, column };
        this.source = options.source;
      }
      Position.prototype.content = style;
      function error2(msg) {
        var err = new Error(
          options.source + ":" + lineno + ":" + column + ": " + msg
        );
        err.reason = msg;
        err.filename = options.source;
        err.line = lineno;
        err.column = column;
        err.source = style;
        if (options.silent) ;
        else {
          throw err;
        }
      }
      function match(re3) {
        var m2 = re3.exec(style);
        if (!m2) return;
        var str = m2[0];
        updatePosition(str);
        style = style.slice(str.length);
        return m2;
      }
      function whitespace2() {
        match(WHITESPACE_REGEX);
      }
      function comments(rules) {
        var c2;
        rules = rules || [];
        while (c2 = comment()) {
          if (c2 !== false) {
            rules.push(c2);
          }
        }
        return rules;
      }
      function comment() {
        var pos = position3();
        if (FORWARD_SLASH != style.charAt(0) || ASTERISK != style.charAt(1)) return;
        var i2 = 2;
        while (EMPTY_STRING != style.charAt(i2) && (ASTERISK != style.charAt(i2) || FORWARD_SLASH != style.charAt(i2 + 1))) {
          ++i2;
        }
        i2 += 2;
        if (EMPTY_STRING === style.charAt(i2 - 1)) {
          return error2("End of comment missing");
        }
        var str = style.slice(2, i2 - 2);
        column += 2;
        updatePosition(str);
        style = style.slice(i2);
        column += 2;
        return pos({
          type: TYPE_COMMENT,
          comment: str
        });
      }
      function declaration() {
        var pos = position3();
        var prop = match(PROPERTY_REGEX);
        if (!prop) return;
        comment();
        if (!match(COLON_REGEX)) return error2("property missing ':'");
        var val = match(VALUE_REGEX);
        var ret = pos({
          type: TYPE_DECLARATION,
          property: trim(prop[0].replace(COMMENT_REGEX, EMPTY_STRING)),
          value: val ? trim(val[0].replace(COMMENT_REGEX, EMPTY_STRING)) : EMPTY_STRING
        });
        match(SEMICOLON_REGEX);
        return ret;
      }
      function declarations() {
        var decls = [];
        comments(decls);
        var decl;
        while (decl = declaration()) {
          if (decl !== false) {
            decls.push(decl);
            comments(decls);
          }
        }
        return decls;
      }
      whitespace2();
      return declarations();
    }
    function trim(str) {
      return str ? str.replace(TRIM_REGEX, EMPTY_STRING) : EMPTY_STRING;
    }
    module.exports = index6;
  }
});

// node_modules/style-to-object/cjs/index.js
var require_cjs3 = __commonJS({
  "node_modules/style-to-object/cjs/index.js"(exports) {
    "use strict";
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.default = StyleToObject;
    var inline_style_parser_1 = __importDefault(require_cjs2());
    function StyleToObject(style, iterator) {
      let styleObject = null;
      if (!style || typeof style !== "string") {
        return styleObject;
      }
      const declarations = (0, inline_style_parser_1.default)(style);
      const hasIterator = typeof iterator === "function";
      declarations.forEach((declaration) => {
        if (declaration.type !== "declaration") {
          return;
        }
        const { property, value } = declaration;
        if (hasIterator) {
          iterator(property, value, declaration);
        } else if (value) {
          styleObject = styleObject || {};
          styleObject[property] = value;
        }
      });
      return styleObject;
    }
  }
});

// node_modules/style-to-js/cjs/utilities.js
var require_utilities = __commonJS({
  "node_modules/style-to-js/cjs/utilities.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.camelCase = void 0;
    var CUSTOM_PROPERTY_REGEX = /^--[a-zA-Z0-9_-]+$/;
    var HYPHEN_REGEX = /-([a-z])/g;
    var NO_HYPHEN_REGEX = /^[^-]+$/;
    var VENDOR_PREFIX_REGEX = /^-(webkit|moz|ms|o|khtml)-/;
    var MS_VENDOR_PREFIX_REGEX = /^-(ms)-/;
    var skipCamelCase = function(property) {
      return !property || NO_HYPHEN_REGEX.test(property) || CUSTOM_PROPERTY_REGEX.test(property);
    };
    var capitalize = function(match, character) {
      return character.toUpperCase();
    };
    var trimHyphen = function(match, prefix) {
      return "".concat(prefix, "-");
    };
    var camelCase = function(property, options) {
      if (options === void 0) {
        options = {};
      }
      if (skipCamelCase(property)) {
        return property;
      }
      property = property.toLowerCase();
      if (options.reactCompat) {
        property = property.replace(MS_VENDOR_PREFIX_REGEX, trimHyphen);
      } else {
        property = property.replace(VENDOR_PREFIX_REGEX, trimHyphen);
      }
      return property.replace(HYPHEN_REGEX, capitalize);
    };
    exports.camelCase = camelCase;
  }
});

// node_modules/style-to-js/cjs/index.js
var require_cjs4 = __commonJS({
  "node_modules/style-to-js/cjs/index.js"(exports, module) {
    "use strict";
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    var style_to_object_1 = __importDefault(require_cjs3());
    var utilities_1 = require_utilities();
    function StyleToJS(style, options) {
      var output = {};
      if (!style || typeof style !== "string") {
        return output;
      }
      (0, style_to_object_1.default)(style, function(property, value) {
        if (property && value) {
          output[(0, utilities_1.camelCase)(property, options)] = value;
        }
      });
      return output;
    }
    StyleToJS.default = StyleToJS;
    module.exports = StyleToJS;
  }
});

// node_modules/ms/index.js
var require_ms = __commonJS({
  "node_modules/ms/index.js"(exports, module) {
    var s2 = 1e3;
    var m2 = s2 * 60;
    var h2 = m2 * 60;
    var d2 = h2 * 24;
    var w2 = d2 * 7;
    var y2 = d2 * 365.25;
    module.exports = function(val, options) {
      options = options || {};
      var type = typeof val;
      if (type === "string" && val.length > 0) {
        return parse2(val);
      } else if (type === "number" && isFinite(val)) {
        return options.long ? fmtLong(val) : fmtShort(val);
      }
      throw new Error(
        "val is not a non-empty string or a valid number. val=" + JSON.stringify(val)
      );
    };
    function parse2(str) {
      str = String(str);
      if (str.length > 100) {
        return;
      }
      var match = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(
        str
      );
      if (!match) {
        return;
      }
      var n2 = parseFloat(match[1]);
      var type = (match[2] || "ms").toLowerCase();
      switch (type) {
        case "years":
        case "year":
        case "yrs":
        case "yr":
        case "y":
          return n2 * y2;
        case "weeks":
        case "week":
        case "w":
          return n2 * w2;
        case "days":
        case "day":
        case "d":
          return n2 * d2;
        case "hours":
        case "hour":
        case "hrs":
        case "hr":
        case "h":
          return n2 * h2;
        case "minutes":
        case "minute":
        case "mins":
        case "min":
        case "m":
          return n2 * m2;
        case "seconds":
        case "second":
        case "secs":
        case "sec":
        case "s":
          return n2 * s2;
        case "milliseconds":
        case "millisecond":
        case "msecs":
        case "msec":
        case "ms":
          return n2;
        default:
          return void 0;
      }
    }
    function fmtShort(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d2) {
        return Math.round(ms / d2) + "d";
      }
      if (msAbs >= h2) {
        return Math.round(ms / h2) + "h";
      }
      if (msAbs >= m2) {
        return Math.round(ms / m2) + "m";
      }
      if (msAbs >= s2) {
        return Math.round(ms / s2) + "s";
      }
      return ms + "ms";
    }
    function fmtLong(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d2) {
        return plural(ms, msAbs, d2, "day");
      }
      if (msAbs >= h2) {
        return plural(ms, msAbs, h2, "hour");
      }
      if (msAbs >= m2) {
        return plural(ms, msAbs, m2, "minute");
      }
      if (msAbs >= s2) {
        return plural(ms, msAbs, s2, "second");
      }
      return ms + " ms";
    }
    function plural(ms, msAbs, n2, name2) {
      var isPlural = msAbs >= n2 * 1.5;
      return Math.round(ms / n2) + " " + name2 + (isPlural ? "s" : "");
    }
  }
});

// node_modules/debug/src/common.js
var require_common = __commonJS({
  "node_modules/debug/src/common.js"(exports, module) {
    function setup(env2) {
      createDebug2.debug = createDebug2;
      createDebug2.default = createDebug2;
      createDebug2.coerce = coerce;
      createDebug2.disable = disable2;
      createDebug2.enable = enable;
      createDebug2.enabled = enabled;
      createDebug2.humanize = require_ms();
      createDebug2.destroy = destroy;
      Object.keys(env2).forEach((key) => {
        createDebug2[key] = env2[key];
      });
      createDebug2.names = [];
      createDebug2.skips = [];
      createDebug2.formatters = {};
      function selectColor(namespace) {
        let hash = 0;
        for (let i2 = 0; i2 < namespace.length; i2++) {
          hash = (hash << 5) - hash + namespace.charCodeAt(i2);
          hash |= 0;
        }
        return createDebug2.colors[Math.abs(hash) % createDebug2.colors.length];
      }
      createDebug2.selectColor = selectColor;
      function createDebug2(namespace) {
        let prevTime;
        let enableOverride = null;
        let namespacesCache;
        let enabledCache;
        function debug2(...args) {
          if (!debug2.enabled) {
            return;
          }
          const self2 = debug2;
          const curr = Number(/* @__PURE__ */ new Date());
          const ms = curr - (prevTime || curr);
          self2.diff = ms;
          self2.prev = prevTime;
          self2.curr = curr;
          prevTime = curr;
          args[0] = createDebug2.coerce(args[0]);
          if (typeof args[0] !== "string") {
            args.unshift("%O");
          }
          let index6 = 0;
          args[0] = args[0].replace(/%([a-zA-Z%])/g, (match, format) => {
            if (match === "%%") {
              return "%";
            }
            index6++;
            const formatter = createDebug2.formatters[format];
            if (typeof formatter === "function") {
              const val = args[index6];
              match = formatter.call(self2, val);
              args.splice(index6, 1);
              index6--;
            }
            return match;
          });
          createDebug2.formatArgs.call(self2, args);
          const logFn = self2.log || createDebug2.log;
          logFn.apply(self2, args);
        }
        debug2.namespace = namespace;
        debug2.useColors = createDebug2.useColors();
        debug2.color = createDebug2.selectColor(namespace);
        debug2.extend = extend2;
        debug2.destroy = createDebug2.destroy;
        Object.defineProperty(debug2, "enabled", {
          enumerable: true,
          configurable: false,
          get: () => {
            if (enableOverride !== null) {
              return enableOverride;
            }
            if (namespacesCache !== createDebug2.namespaces) {
              namespacesCache = createDebug2.namespaces;
              enabledCache = createDebug2.enabled(namespace);
            }
            return enabledCache;
          },
          set: (v2) => {
            enableOverride = v2;
          }
        });
        if (typeof createDebug2.init === "function") {
          createDebug2.init(debug2);
        }
        return debug2;
      }
      function extend2(namespace, delimiter) {
        const newDebug = createDebug2(this.namespace + (typeof delimiter === "undefined" ? ":" : delimiter) + namespace);
        newDebug.log = this.log;
        return newDebug;
      }
      function enable(namespaces) {
        createDebug2.save(namespaces);
        createDebug2.namespaces = namespaces;
        createDebug2.names = [];
        createDebug2.skips = [];
        const split = (typeof namespaces === "string" ? namespaces : "").trim().replace(/\s+/g, ",").split(",").filter(Boolean);
        for (const ns of split) {
          if (ns[0] === "-") {
            createDebug2.skips.push(ns.slice(1));
          } else {
            createDebug2.names.push(ns);
          }
        }
      }
      function matchesTemplate(search2, template) {
        let searchIndex = 0;
        let templateIndex = 0;
        let starIndex = -1;
        let matchIndex = 0;
        while (searchIndex < search2.length) {
          if (templateIndex < template.length && (template[templateIndex] === search2[searchIndex] || template[templateIndex] === "*")) {
            if (template[templateIndex] === "*") {
              starIndex = templateIndex;
              matchIndex = searchIndex;
              templateIndex++;
            } else {
              searchIndex++;
              templateIndex++;
            }
          } else if (starIndex !== -1) {
            templateIndex = starIndex + 1;
            matchIndex++;
            searchIndex = matchIndex;
          } else {
            return false;
          }
        }
        while (templateIndex < template.length && template[templateIndex] === "*") {
          templateIndex++;
        }
        return templateIndex === template.length;
      }
      function disable2() {
        const namespaces = [
          ...createDebug2.names,
          ...createDebug2.skips.map((namespace) => "-" + namespace)
        ].join(",");
        createDebug2.enable("");
        return namespaces;
      }
      function enabled(name2) {
        for (const skip2 of createDebug2.skips) {
          if (matchesTemplate(name2, skip2)) {
            return false;
          }
        }
        for (const ns of createDebug2.names) {
          if (matchesTemplate(name2, ns)) {
            return true;
          }
        }
        return false;
      }
      function coerce(val) {
        if (val instanceof Error) {
          return val.stack || val.message;
        }
        return val;
      }
      function destroy() {
        console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
      }
      createDebug2.enable(createDebug2.load());
      return createDebug2;
    }
    module.exports = setup;
  }
});

// node_modules/debug/src/browser.js
var require_browser = __commonJS({
  "node_modules/debug/src/browser.js"(exports, module) {
    exports.formatArgs = formatArgs;
    exports.save = save;
    exports.load = load;
    exports.useColors = useColors;
    exports.storage = localstorage();
    exports.destroy = /* @__PURE__ */ (() => {
      let warned = false;
      return () => {
        if (!warned) {
          warned = true;
          console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
        }
      };
    })();
    exports.colors = [
      "#0000CC",
      "#0000FF",
      "#0033CC",
      "#0033FF",
      "#0066CC",
      "#0066FF",
      "#0099CC",
      "#0099FF",
      "#00CC00",
      "#00CC33",
      "#00CC66",
      "#00CC99",
      "#00CCCC",
      "#00CCFF",
      "#3300CC",
      "#3300FF",
      "#3333CC",
      "#3333FF",
      "#3366CC",
      "#3366FF",
      "#3399CC",
      "#3399FF",
      "#33CC00",
      "#33CC33",
      "#33CC66",
      "#33CC99",
      "#33CCCC",
      "#33CCFF",
      "#6600CC",
      "#6600FF",
      "#6633CC",
      "#6633FF",
      "#66CC00",
      "#66CC33",
      "#9900CC",
      "#9900FF",
      "#9933CC",
      "#9933FF",
      "#99CC00",
      "#99CC33",
      "#CC0000",
      "#CC0033",
      "#CC0066",
      "#CC0099",
      "#CC00CC",
      "#CC00FF",
      "#CC3300",
      "#CC3333",
      "#CC3366",
      "#CC3399",
      "#CC33CC",
      "#CC33FF",
      "#CC6600",
      "#CC6633",
      "#CC9900",
      "#CC9933",
      "#CCCC00",
      "#CCCC33",
      "#FF0000",
      "#FF0033",
      "#FF0066",
      "#FF0099",
      "#FF00CC",
      "#FF00FF",
      "#FF3300",
      "#FF3333",
      "#FF3366",
      "#FF3399",
      "#FF33CC",
      "#FF33FF",
      "#FF6600",
      "#FF6633",
      "#FF9900",
      "#FF9933",
      "#FFCC00",
      "#FFCC33"
    ];
    function useColors() {
      if (typeof window !== "undefined" && window.process && (window.process.type === "renderer" || window.process.__nwjs)) {
        return true;
      }
      if (typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)) {
        return false;
      }
      let m2;
      return typeof document !== "undefined" && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance || // Is firebug? http://stackoverflow.com/a/398120/376773
      typeof window !== "undefined" && window.console && (window.console.firebug || window.console.exception && window.console.table) || // Is firefox >= v31?
      // https://developer.mozilla.org/en-US/docs/Tools/Web_Console#Styling_messages
      typeof navigator !== "undefined" && navigator.userAgent && (m2 = navigator.userAgent.toLowerCase().match(/firefox\/(\d+)/)) && parseInt(m2[1], 10) >= 31 || // Double check webkit in userAgent just in case we are in a worker
      typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/);
    }
    function formatArgs(args) {
      args[0] = (this.useColors ? "%c" : "") + this.namespace + (this.useColors ? " %c" : " ") + args[0] + (this.useColors ? "%c " : " ") + "+" + module.exports.humanize(this.diff);
      if (!this.useColors) {
        return;
      }
      const c2 = "color: " + this.color;
      args.splice(1, 0, c2, "color: inherit");
      let index6 = 0;
      let lastC = 0;
      args[0].replace(/%[a-zA-Z%]/g, (match) => {
        if (match === "%%") {
          return;
        }
        index6++;
        if (match === "%c") {
          lastC = index6;
        }
      });
      args.splice(lastC, 0, c2);
    }
    exports.log = console.debug || console.log || (() => {
    });
    function save(namespaces) {
      try {
        if (namespaces) {
          exports.storage.setItem("debug", namespaces);
        } else {
          exports.storage.removeItem("debug");
        }
      } catch (error2) {
      }
    }
    function load() {
      let r3;
      try {
        r3 = exports.storage.getItem("debug") || exports.storage.getItem("DEBUG");
      } catch (error2) {
      }
      if (!r3 && typeof process !== "undefined" && "env" in process) {
        r3 = process.env.DEBUG;
      }
      return r3;
    }
    function localstorage() {
      try {
        return localStorage;
      } catch (error2) {
      }
    }
    module.exports = require_common()(exports);
    var { formatters } = module.exports;
    formatters.j = function(v2) {
      try {
        return JSON.stringify(v2);
      } catch (error2) {
        return "[UnexpectedJSONParseError]: " + error2.message;
      }
    };
  }
});

// node_modules/extend/index.js
var require_extend = __commonJS({
  "node_modules/extend/index.js"(exports, module) {
    "use strict";
    var hasOwn = Object.prototype.hasOwnProperty;
    var toStr = Object.prototype.toString;
    var defineProperty = Object.defineProperty;
    var gOPD = Object.getOwnPropertyDescriptor;
    var isArray = function isArray2(arr) {
      if (typeof Array.isArray === "function") {
        return Array.isArray(arr);
      }
      return toStr.call(arr) === "[object Array]";
    };
    var isPlainObject2 = function isPlainObject3(obj) {
      if (!obj || toStr.call(obj) !== "[object Object]") {
        return false;
      }
      var hasOwnConstructor = hasOwn.call(obj, "constructor");
      var hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn.call(obj.constructor.prototype, "isPrototypeOf");
      if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) {
        return false;
      }
      var key;
      for (key in obj) {
      }
      return typeof key === "undefined" || hasOwn.call(obj, key);
    };
    var setProperty = function setProperty2(target, options) {
      if (defineProperty && options.name === "__proto__") {
        defineProperty(target, options.name, {
          enumerable: true,
          configurable: true,
          value: options.newValue,
          writable: true
        });
      } else {
        target[options.name] = options.newValue;
      }
    };
    var getProperty = function getProperty2(obj, name2) {
      if (name2 === "__proto__") {
        if (!hasOwn.call(obj, name2)) {
          return void 0;
        } else if (gOPD) {
          return gOPD(obj, name2).value;
        }
      }
      return obj[name2];
    };
    module.exports = function extend2() {
      var options, name2, src, copy2, copyIsArray, clone;
      var target = arguments[0];
      var i2 = 1;
      var length = arguments.length;
      var deep = false;
      if (typeof target === "boolean") {
        deep = target;
        target = arguments[1] || {};
        i2 = 2;
      }
      if (target == null || typeof target !== "object" && typeof target !== "function") {
        target = {};
      }
      for (; i2 < length; ++i2) {
        options = arguments[i2];
        if (options != null) {
          for (name2 in options) {
            src = getProperty(target, name2);
            copy2 = getProperty(options, name2);
            if (target !== copy2) {
              if (deep && copy2 && (isPlainObject2(copy2) || (copyIsArray = isArray(copy2)))) {
                if (copyIsArray) {
                  copyIsArray = false;
                  clone = src && isArray(src) ? src : [];
                } else {
                  clone = src && isPlainObject2(src) ? src : {};
                }
                setProperty(target, { name: name2, newValue: extend2(deep, clone, copy2) });
              } else if (typeof copy2 !== "undefined") {
                setProperty(target, { name: name2, newValue: copy2 });
              }
            }
          }
        }
      }
      return target;
    };
  }
});

// node_modules/lodash.uniqby/index.js
var require_lodash3 = __commonJS({
  "node_modules/lodash.uniqby/index.js"(exports, module) {
    var LARGE_ARRAY_SIZE = 200;
    var FUNC_ERROR_TEXT = "Expected a function";
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    var UNORDERED_COMPARE_FLAG = 1;
    var PARTIAL_COMPARE_FLAG = 2;
    var INFINITY = 1 / 0;
    var MAX_SAFE_INTEGER = 9007199254740991;
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var funcTag = "[object Function]";
    var genTag = "[object GeneratorFunction]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var objectTag = "[object Object]";
    var promiseTag = "[object Promise]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var symbolTag = "[object Symbol]";
    var weakMapTag = "[object WeakMap]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/;
    var reIsPlainProp = /^\w*$/;
    var reLeadingDot = /^\./;
    var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
    var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
    var reEscapeChar = /\\(\\)?/g;
    var reIsHostCtor = /^\[object .+?Constructor\]$/;
    var reIsUint = /^(?:0|[1-9]\d*)$/;
    var typedArrayTags = {};
    typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
    typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root4 = freeGlobal || freeSelf || Function("return this")();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var freeProcess = moduleExports && freeGlobal.process;
    var nodeUtil = (function() {
      try {
        return freeProcess && freeProcess.binding("util");
      } catch (e) {
      }
    })();
    var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
    function arrayIncludes(array, value) {
      var length = array ? array.length : 0;
      return !!length && baseIndexOf(array, value, 0) > -1;
    }
    function arrayIncludesWith(array, value, comparator) {
      var index6 = -1, length = array ? array.length : 0;
      while (++index6 < length) {
        if (comparator(value, array[index6])) {
          return true;
        }
      }
      return false;
    }
    function arraySome(array, predicate) {
      var index6 = -1, length = array ? array.length : 0;
      while (++index6 < length) {
        if (predicate(array[index6], index6, array)) {
          return true;
        }
      }
      return false;
    }
    function baseFindIndex(array, predicate, fromIndex, fromRight) {
      var length = array.length, index6 = fromIndex + (fromRight ? 1 : -1);
      while (fromRight ? index6-- : ++index6 < length) {
        if (predicate(array[index6], index6, array)) {
          return index6;
        }
      }
      return -1;
    }
    function baseIndexOf(array, value, fromIndex) {
      if (value !== value) {
        return baseFindIndex(array, baseIsNaN, fromIndex);
      }
      var index6 = fromIndex - 1, length = array.length;
      while (++index6 < length) {
        if (array[index6] === value) {
          return index6;
        }
      }
      return -1;
    }
    function baseIsNaN(value) {
      return value !== value;
    }
    function baseProperty(key) {
      return function(object) {
        return object == null ? void 0 : object[key];
      };
    }
    function baseTimes(n2, iteratee) {
      var index6 = -1, result = Array(n2);
      while (++index6 < n2) {
        result[index6] = iteratee(index6);
      }
      return result;
    }
    function baseUnary(func) {
      return function(value) {
        return func(value);
      };
    }
    function cacheHas(cache, key) {
      return cache.has(key);
    }
    function getValue2(object, key) {
      return object == null ? void 0 : object[key];
    }
    function isHostObject(value) {
      var result = false;
      if (value != null && typeof value.toString != "function") {
        try {
          result = !!(value + "");
        } catch (e) {
        }
      }
      return result;
    }
    function mapToArray(map4) {
      var index6 = -1, result = Array(map4.size);
      map4.forEach(function(value, key) {
        result[++index6] = [key, value];
      });
      return result;
    }
    function overArg(func, transform2) {
      return function(arg) {
        return func(transform2(arg));
      };
    }
    function setToArray(set) {
      var index6 = -1, result = Array(set.size);
      set.forEach(function(value) {
        result[++index6] = value;
      });
      return result;
    }
    var arrayProto = Array.prototype;
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var coreJsData = root4["__core-js_shared__"];
    var maskSrcKey = (function() {
      var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
      return uid ? "Symbol(src)_1." + uid : "";
    })();
    var funcToString = funcProto.toString;
    var hasOwnProperty3 = objectProto.hasOwnProperty;
    var objectToString = objectProto.toString;
    var reIsNative = RegExp(
      "^" + funcToString.call(hasOwnProperty3).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
    );
    var Symbol2 = root4.Symbol;
    var Uint8Array2 = root4.Uint8Array;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var splice2 = arrayProto.splice;
    var nativeKeys = overArg(Object.keys, Object);
    var DataView2 = getNative(root4, "DataView");
    var Map2 = getNative(root4, "Map");
    var Promise2 = getNative(root4, "Promise");
    var Set2 = getNative(root4, "Set");
    var WeakMap2 = getNative(root4, "WeakMap");
    var nativeCreate = getNative(Object, "create");
    var dataViewCtorString = toSource(DataView2);
    var mapCtorString = toSource(Map2);
    var promiseCtorString = toSource(Promise2);
    var setCtorString = toSource(Set2);
    var weakMapCtorString = toSource(WeakMap2);
    var symbolProto = Symbol2 ? Symbol2.prototype : void 0;
    var symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
    var symbolToString = symbolProto ? symbolProto.toString : void 0;
    function Hash(entries) {
      var index6 = -1, length = entries ? entries.length : 0;
      this.clear();
      while (++index6 < length) {
        var entry = entries[index6];
        this.set(entry[0], entry[1]);
      }
    }
    function hashClear() {
      this.__data__ = nativeCreate ? nativeCreate(null) : {};
    }
    function hashDelete(key) {
      return this.has(key) && delete this.__data__[key];
    }
    function hashGet(key) {
      var data = this.__data__;
      if (nativeCreate) {
        var result = data[key];
        return result === HASH_UNDEFINED ? void 0 : result;
      }
      return hasOwnProperty3.call(data, key) ? data[key] : void 0;
    }
    function hashHas(key) {
      var data = this.__data__;
      return nativeCreate ? data[key] !== void 0 : hasOwnProperty3.call(data, key);
    }
    function hashSet(key, value) {
      var data = this.__data__;
      data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
      return this;
    }
    Hash.prototype.clear = hashClear;
    Hash.prototype["delete"] = hashDelete;
    Hash.prototype.get = hashGet;
    Hash.prototype.has = hashHas;
    Hash.prototype.set = hashSet;
    function ListCache(entries) {
      var index6 = -1, length = entries ? entries.length : 0;
      this.clear();
      while (++index6 < length) {
        var entry = entries[index6];
        this.set(entry[0], entry[1]);
      }
    }
    function listCacheClear() {
      this.__data__ = [];
    }
    function listCacheDelete(key) {
      var data = this.__data__, index6 = assocIndexOf(data, key);
      if (index6 < 0) {
        return false;
      }
      var lastIndex = data.length - 1;
      if (index6 == lastIndex) {
        data.pop();
      } else {
        splice2.call(data, index6, 1);
      }
      return true;
    }
    function listCacheGet(key) {
      var data = this.__data__, index6 = assocIndexOf(data, key);
      return index6 < 0 ? void 0 : data[index6][1];
    }
    function listCacheHas(key) {
      return assocIndexOf(this.__data__, key) > -1;
    }
    function listCacheSet(key, value) {
      var data = this.__data__, index6 = assocIndexOf(data, key);
      if (index6 < 0) {
        data.push([key, value]);
      } else {
        data[index6][1] = value;
      }
      return this;
    }
    ListCache.prototype.clear = listCacheClear;
    ListCache.prototype["delete"] = listCacheDelete;
    ListCache.prototype.get = listCacheGet;
    ListCache.prototype.has = listCacheHas;
    ListCache.prototype.set = listCacheSet;
    function MapCache(entries) {
      var index6 = -1, length = entries ? entries.length : 0;
      this.clear();
      while (++index6 < length) {
        var entry = entries[index6];
        this.set(entry[0], entry[1]);
      }
    }
    function mapCacheClear() {
      this.__data__ = {
        "hash": new Hash(),
        "map": new (Map2 || ListCache)(),
        "string": new Hash()
      };
    }
    function mapCacheDelete(key) {
      return getMapData(this, key)["delete"](key);
    }
    function mapCacheGet(key) {
      return getMapData(this, key).get(key);
    }
    function mapCacheHas(key) {
      return getMapData(this, key).has(key);
    }
    function mapCacheSet(key, value) {
      getMapData(this, key).set(key, value);
      return this;
    }
    MapCache.prototype.clear = mapCacheClear;
    MapCache.prototype["delete"] = mapCacheDelete;
    MapCache.prototype.get = mapCacheGet;
    MapCache.prototype.has = mapCacheHas;
    MapCache.prototype.set = mapCacheSet;
    function SetCache(values2) {
      var index6 = -1, length = values2 ? values2.length : 0;
      this.__data__ = new MapCache();
      while (++index6 < length) {
        this.add(values2[index6]);
      }
    }
    function setCacheAdd(value) {
      this.__data__.set(value, HASH_UNDEFINED);
      return this;
    }
    function setCacheHas(value) {
      return this.__data__.has(value);
    }
    SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
    SetCache.prototype.has = setCacheHas;
    function Stack(entries) {
      this.__data__ = new ListCache(entries);
    }
    function stackClear() {
      this.__data__ = new ListCache();
    }
    function stackDelete(key) {
      return this.__data__["delete"](key);
    }
    function stackGet(key) {
      return this.__data__.get(key);
    }
    function stackHas(key) {
      return this.__data__.has(key);
    }
    function stackSet(key, value) {
      var cache = this.__data__;
      if (cache instanceof ListCache) {
        var pairs = cache.__data__;
        if (!Map2 || pairs.length < LARGE_ARRAY_SIZE - 1) {
          pairs.push([key, value]);
          return this;
        }
        cache = this.__data__ = new MapCache(pairs);
      }
      cache.set(key, value);
      return this;
    }
    Stack.prototype.clear = stackClear;
    Stack.prototype["delete"] = stackDelete;
    Stack.prototype.get = stackGet;
    Stack.prototype.has = stackHas;
    Stack.prototype.set = stackSet;
    function arrayLikeKeys(value, inherited) {
      var result = isArray(value) || isArguments(value) ? baseTimes(value.length, String) : [];
      var length = result.length, skipIndexes = !!length;
      for (var key in value) {
        if ((inherited || hasOwnProperty3.call(value, key)) && !(skipIndexes && (key == "length" || isIndex(key, length)))) {
          result.push(key);
        }
      }
      return result;
    }
    function assocIndexOf(array, key) {
      var length = array.length;
      while (length--) {
        if (eq(array[length][0], key)) {
          return length;
        }
      }
      return -1;
    }
    function baseGet(object, path2) {
      path2 = isKey(path2, object) ? [path2] : castPath(path2);
      var index6 = 0, length = path2.length;
      while (object != null && index6 < length) {
        object = object[toKey(path2[index6++])];
      }
      return index6 && index6 == length ? object : void 0;
    }
    function baseGetTag(value) {
      return objectToString.call(value);
    }
    function baseHasIn(object, key) {
      return object != null && key in Object(object);
    }
    function baseIsEqual(value, other, customizer, bitmask, stack) {
      if (value === other) {
        return true;
      }
      if (value == null || other == null || !isObject2(value) && !isObjectLike(other)) {
        return value !== value && other !== other;
      }
      return baseIsEqualDeep(value, other, baseIsEqual, customizer, bitmask, stack);
    }
    function baseIsEqualDeep(object, other, equalFunc, customizer, bitmask, stack) {
      var objIsArr = isArray(object), othIsArr = isArray(other), objTag = arrayTag, othTag = arrayTag;
      if (!objIsArr) {
        objTag = getTag(object);
        objTag = objTag == argsTag ? objectTag : objTag;
      }
      if (!othIsArr) {
        othTag = getTag(other);
        othTag = othTag == argsTag ? objectTag : othTag;
      }
      var objIsObj = objTag == objectTag && !isHostObject(object), othIsObj = othTag == objectTag && !isHostObject(other), isSameTag = objTag == othTag;
      if (isSameTag && !objIsObj) {
        stack || (stack = new Stack());
        return objIsArr || isTypedArray(object) ? equalArrays(object, other, equalFunc, customizer, bitmask, stack) : equalByTag(object, other, objTag, equalFunc, customizer, bitmask, stack);
      }
      if (!(bitmask & PARTIAL_COMPARE_FLAG)) {
        var objIsWrapped = objIsObj && hasOwnProperty3.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty3.call(other, "__wrapped__");
        if (objIsWrapped || othIsWrapped) {
          var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
          stack || (stack = new Stack());
          return equalFunc(objUnwrapped, othUnwrapped, customizer, bitmask, stack);
        }
      }
      if (!isSameTag) {
        return false;
      }
      stack || (stack = new Stack());
      return equalObjects(object, other, equalFunc, customizer, bitmask, stack);
    }
    function baseIsMatch(object, source, matchData, customizer) {
      var index6 = matchData.length, length = index6, noCustomizer = !customizer;
      if (object == null) {
        return !length;
      }
      object = Object(object);
      while (index6--) {
        var data = matchData[index6];
        if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
          return false;
        }
      }
      while (++index6 < length) {
        data = matchData[index6];
        var key = data[0], objValue = object[key], srcValue = data[1];
        if (noCustomizer && data[2]) {
          if (objValue === void 0 && !(key in object)) {
            return false;
          }
        } else {
          var stack = new Stack();
          if (customizer) {
            var result = customizer(objValue, srcValue, key, object, source, stack);
          }
          if (!(result === void 0 ? baseIsEqual(srcValue, objValue, customizer, UNORDERED_COMPARE_FLAG | PARTIAL_COMPARE_FLAG, stack) : result)) {
            return false;
          }
        }
      }
      return true;
    }
    function baseIsNative(value) {
      if (!isObject2(value) || isMasked(value)) {
        return false;
      }
      var pattern = isFunction(value) || isHostObject(value) ? reIsNative : reIsHostCtor;
      return pattern.test(toSource(value));
    }
    function baseIsTypedArray(value) {
      return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[objectToString.call(value)];
    }
    function baseIteratee(value) {
      if (typeof value == "function") {
        return value;
      }
      if (value == null) {
        return identity;
      }
      if (typeof value == "object") {
        return isArray(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
      }
      return property(value);
    }
    function baseKeys(object) {
      if (!isPrototype(object)) {
        return nativeKeys(object);
      }
      var result = [];
      for (var key in Object(object)) {
        if (hasOwnProperty3.call(object, key) && key != "constructor") {
          result.push(key);
        }
      }
      return result;
    }
    function baseMatches(source) {
      var matchData = getMatchData(source);
      if (matchData.length == 1 && matchData[0][2]) {
        return matchesStrictComparable(matchData[0][0], matchData[0][1]);
      }
      return function(object) {
        return object === source || baseIsMatch(object, source, matchData);
      };
    }
    function baseMatchesProperty(path2, srcValue) {
      if (isKey(path2) && isStrictComparable(srcValue)) {
        return matchesStrictComparable(toKey(path2), srcValue);
      }
      return function(object) {
        var objValue = get2(object, path2);
        return objValue === void 0 && objValue === srcValue ? hasIn(object, path2) : baseIsEqual(srcValue, objValue, void 0, UNORDERED_COMPARE_FLAG | PARTIAL_COMPARE_FLAG);
      };
    }
    function basePropertyDeep(path2) {
      return function(object) {
        return baseGet(object, path2);
      };
    }
    function baseToString(value) {
      if (typeof value == "string") {
        return value;
      }
      if (isSymbol(value)) {
        return symbolToString ? symbolToString.call(value) : "";
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    function baseUniq(array, iteratee, comparator) {
      var index6 = -1, includes = arrayIncludes, length = array.length, isCommon = true, result = [], seen = result;
      if (comparator) {
        isCommon = false;
        includes = arrayIncludesWith;
      } else if (length >= LARGE_ARRAY_SIZE) {
        var set = iteratee ? null : createSet(array);
        if (set) {
          return setToArray(set);
        }
        isCommon = false;
        includes = cacheHas;
        seen = new SetCache();
      } else {
        seen = iteratee ? [] : result;
      }
      outer:
        while (++index6 < length) {
          var value = array[index6], computed = iteratee ? iteratee(value) : value;
          value = comparator || value !== 0 ? value : 0;
          if (isCommon && computed === computed) {
            var seenIndex = seen.length;
            while (seenIndex--) {
              if (seen[seenIndex] === computed) {
                continue outer;
              }
            }
            if (iteratee) {
              seen.push(computed);
            }
            result.push(value);
          } else if (!includes(seen, computed, comparator)) {
            if (seen !== result) {
              seen.push(computed);
            }
            result.push(value);
          }
        }
      return result;
    }
    function castPath(value) {
      return isArray(value) ? value : stringToPath(value);
    }
    var createSet = !(Set2 && 1 / setToArray(new Set2([, -0]))[1] == INFINITY) ? noop12 : function(values2) {
      return new Set2(values2);
    };
    function equalArrays(array, other, equalFunc, customizer, bitmask, stack) {
      var isPartial = bitmask & PARTIAL_COMPARE_FLAG, arrLength = array.length, othLength = other.length;
      if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
        return false;
      }
      var stacked = stack.get(array);
      if (stacked && stack.get(other)) {
        return stacked == other;
      }
      var index6 = -1, result = true, seen = bitmask & UNORDERED_COMPARE_FLAG ? new SetCache() : void 0;
      stack.set(array, other);
      stack.set(other, array);
      while (++index6 < arrLength) {
        var arrValue = array[index6], othValue = other[index6];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, arrValue, index6, other, array, stack) : customizer(arrValue, othValue, index6, array, other, stack);
        }
        if (compared !== void 0) {
          if (compared) {
            continue;
          }
          result = false;
          break;
        }
        if (seen) {
          if (!arraySome(other, function(othValue2, othIndex) {
            if (!seen.has(othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, customizer, bitmask, stack))) {
              return seen.add(othIndex);
            }
          })) {
            result = false;
            break;
          }
        } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, customizer, bitmask, stack))) {
          result = false;
          break;
        }
      }
      stack["delete"](array);
      stack["delete"](other);
      return result;
    }
    function equalByTag(object, other, tag, equalFunc, customizer, bitmask, stack) {
      switch (tag) {
        case dataViewTag:
          if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
            return false;
          }
          object = object.buffer;
          other = other.buffer;
        case arrayBufferTag:
          if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array2(object), new Uint8Array2(other))) {
            return false;
          }
          return true;
        case boolTag:
        case dateTag:
        case numberTag:
          return eq(+object, +other);
        case errorTag:
          return object.name == other.name && object.message == other.message;
        case regexpTag:
        case stringTag:
          return object == other + "";
        case mapTag:
          var convert2 = mapToArray;
        case setTag:
          var isPartial = bitmask & PARTIAL_COMPARE_FLAG;
          convert2 || (convert2 = setToArray);
          if (object.size != other.size && !isPartial) {
            return false;
          }
          var stacked = stack.get(object);
          if (stacked) {
            return stacked == other;
          }
          bitmask |= UNORDERED_COMPARE_FLAG;
          stack.set(object, other);
          var result = equalArrays(convert2(object), convert2(other), equalFunc, customizer, bitmask, stack);
          stack["delete"](object);
          return result;
        case symbolTag:
          if (symbolValueOf) {
            return symbolValueOf.call(object) == symbolValueOf.call(other);
          }
      }
      return false;
    }
    function equalObjects(object, other, equalFunc, customizer, bitmask, stack) {
      var isPartial = bitmask & PARTIAL_COMPARE_FLAG, objProps = keys3(object), objLength = objProps.length, othProps = keys3(other), othLength = othProps.length;
      if (objLength != othLength && !isPartial) {
        return false;
      }
      var index6 = objLength;
      while (index6--) {
        var key = objProps[index6];
        if (!(isPartial ? key in other : hasOwnProperty3.call(other, key))) {
          return false;
        }
      }
      var stacked = stack.get(object);
      if (stacked && stack.get(other)) {
        return stacked == other;
      }
      var result = true;
      stack.set(object, other);
      stack.set(other, object);
      var skipCtor = isPartial;
      while (++index6 < objLength) {
        key = objProps[index6];
        var objValue = object[key], othValue = other[key];
        if (customizer) {
          var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
        }
        if (!(compared === void 0 ? objValue === othValue || equalFunc(objValue, othValue, customizer, bitmask, stack) : compared)) {
          result = false;
          break;
        }
        skipCtor || (skipCtor = key == "constructor");
      }
      if (result && !skipCtor) {
        var objCtor = object.constructor, othCtor = other.constructor;
        if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
          result = false;
        }
      }
      stack["delete"](object);
      stack["delete"](other);
      return result;
    }
    function getMapData(map4, key) {
      var data = map4.__data__;
      return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
    }
    function getMatchData(object) {
      var result = keys3(object), length = result.length;
      while (length--) {
        var key = result[length], value = object[key];
        result[length] = [key, value, isStrictComparable(value)];
      }
      return result;
    }
    function getNative(object, key) {
      var value = getValue2(object, key);
      return baseIsNative(value) ? value : void 0;
    }
    var getTag = baseGetTag;
    if (DataView2 && getTag(new DataView2(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap2 && getTag(new WeakMap2()) != weakMapTag) {
      getTag = function(value) {
        var result = objectToString.call(value), Ctor = result == objectTag ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : void 0;
        if (ctorString) {
          switch (ctorString) {
            case dataViewCtorString:
              return dataViewTag;
            case mapCtorString:
              return mapTag;
            case promiseCtorString:
              return promiseTag;
            case setCtorString:
              return setTag;
            case weakMapCtorString:
              return weakMapTag;
          }
        }
        return result;
      };
    }
    function hasPath(object, path2, hasFunc) {
      path2 = isKey(path2, object) ? [path2] : castPath(path2);
      var result, index6 = -1, length = path2.length;
      while (++index6 < length) {
        var key = toKey(path2[index6]);
        if (!(result = object != null && hasFunc(object, key))) {
          break;
        }
        object = object[key];
      }
      if (result) {
        return result;
      }
      var length = object ? object.length : 0;
      return !!length && isLength(length) && isIndex(key, length) && (isArray(object) || isArguments(object));
    }
    function isIndex(value, length) {
      length = length == null ? MAX_SAFE_INTEGER : length;
      return !!length && (typeof value == "number" || reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
    }
    function isKey(value, object) {
      if (isArray(value)) {
        return false;
      }
      var type = typeof value;
      if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
        return true;
      }
      return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
    }
    function isKeyable(value) {
      var type = typeof value;
      return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
    }
    function isMasked(func) {
      return !!maskSrcKey && maskSrcKey in func;
    }
    function isPrototype(value) {
      var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
      return value === proto;
    }
    function isStrictComparable(value) {
      return value === value && !isObject2(value);
    }
    function matchesStrictComparable(key, srcValue) {
      return function(object) {
        if (object == null) {
          return false;
        }
        return object[key] === srcValue && (srcValue !== void 0 || key in Object(object));
      };
    }
    var stringToPath = memoize(function(string3) {
      string3 = toString3(string3);
      var result = [];
      if (reLeadingDot.test(string3)) {
        result.push("");
      }
      string3.replace(rePropName, function(match, number2, quote, string4) {
        result.push(quote ? string4.replace(reEscapeChar, "$1") : number2 || match);
      });
      return result;
    });
    function toKey(value) {
      if (typeof value == "string" || isSymbol(value)) {
        return value;
      }
      var result = value + "";
      return result == "0" && 1 / value == -INFINITY ? "-0" : result;
    }
    function toSource(func) {
      if (func != null) {
        try {
          return funcToString.call(func);
        } catch (e) {
        }
        try {
          return func + "";
        } catch (e) {
        }
      }
      return "";
    }
    function uniqBy2(array, iteratee) {
      return array && array.length ? baseUniq(array, baseIteratee(iteratee, 2)) : [];
    }
    function memoize(func, resolver2) {
      if (typeof func != "function" || resolver2 && typeof resolver2 != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      var memoized = function() {
        var args = arguments, key = resolver2 ? resolver2.apply(this, args) : args[0], cache = memoized.cache;
        if (cache.has(key)) {
          return cache.get(key);
        }
        var result = func.apply(this, args);
        memoized.cache = cache.set(key, result);
        return result;
      };
      memoized.cache = new (memoize.Cache || MapCache)();
      return memoized;
    }
    memoize.Cache = MapCache;
    function eq(value, other) {
      return value === other || value !== value && other !== other;
    }
    function isArguments(value) {
      return isArrayLikeObject(value) && hasOwnProperty3.call(value, "callee") && (!propertyIsEnumerable.call(value, "callee") || objectToString.call(value) == argsTag);
    }
    var isArray = Array.isArray;
    function isArrayLike(value) {
      return value != null && isLength(value.length) && !isFunction(value);
    }
    function isArrayLikeObject(value) {
      return isObjectLike(value) && isArrayLike(value);
    }
    function isFunction(value) {
      var tag = isObject2(value) ? objectToString.call(value) : "";
      return tag == funcTag || tag == genTag;
    }
    function isLength(value) {
      return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
    }
    function isObject2(value) {
      var type = typeof value;
      return !!value && (type == "object" || type == "function");
    }
    function isObjectLike(value) {
      return !!value && typeof value == "object";
    }
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike(value) && objectToString.call(value) == symbolTag;
    }
    var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
    function toString3(value) {
      return value == null ? "" : baseToString(value);
    }
    function get2(object, path2, defaultValue) {
      var result = object == null ? void 0 : baseGet(object, path2);
      return result === void 0 ? defaultValue : result;
    }
    function hasIn(object, path2) {
      return object != null && hasPath(object, path2, baseHasIn);
    }
    function keys3(object) {
      return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
    }
    function identity(value) {
      return value;
    }
    function noop12() {
    }
    function property(path2) {
      return isKey(path2) ? baseProperty(toKey(path2)) : basePropertyDeep(path2);
    }
    module.exports = uniqBy2;
  }
});

// node_modules/fix-webm-duration/fix-webm-duration.js
var require_fix_webm_duration = __commonJS({
  "node_modules/fix-webm-duration/fix-webm-duration.js"(exports, module) {
    (function(name2, definition3) {
      if (typeof define === "function" && define.amd) {
        define(definition3);
      } else if (typeof module !== "undefined" && module.exports) {
        module.exports = definition3();
      } else {
        window.ysFixWebmDuration = definition3();
      }
    })("fix-webm-duration", function() {
      var sections = {
        172351395: { name: "EBML", type: "Container" },
        646: { name: "EBMLVersion", type: "Uint" },
        759: { name: "EBMLReadVersion", type: "Uint" },
        754: { name: "EBMLMaxIDLength", type: "Uint" },
        755: { name: "EBMLMaxSizeLength", type: "Uint" },
        642: { name: "DocType", type: "String" },
        647: { name: "DocTypeVersion", type: "Uint" },
        645: { name: "DocTypeReadVersion", type: "Uint" },
        108: { name: "Void", type: "Binary" },
        63: { name: "CRC-32", type: "Binary" },
        190023271: { name: "SignatureSlot", type: "Container" },
        16010: { name: "SignatureAlgo", type: "Uint" },
        16026: { name: "SignatureHash", type: "Uint" },
        16037: { name: "SignaturePublicKey", type: "Binary" },
        16053: { name: "Signature", type: "Binary" },
        15963: { name: "SignatureElements", type: "Container" },
        15995: { name: "SignatureElementList", type: "Container" },
        9522: { name: "SignedElement", type: "Binary" },
        139690087: { name: "Segment", type: "Container" },
        21863284: { name: "SeekHead", type: "Container" },
        3515: { name: "Seek", type: "Container" },
        5035: { name: "SeekID", type: "Binary" },
        5036: { name: "SeekPosition", type: "Uint" },
        88713574: { name: "Info", type: "Container" },
        13220: { name: "SegmentUID", type: "Binary" },
        13188: { name: "SegmentFilename", type: "String" },
        1882403: { name: "PrevUID", type: "Binary" },
        1868715: { name: "PrevFilename", type: "String" },
        2013475: { name: "NextUID", type: "Binary" },
        1999803: { name: "NextFilename", type: "String" },
        1092: { name: "SegmentFamily", type: "Binary" },
        10532: { name: "ChapterTranslate", type: "Container" },
        10748: { name: "ChapterTranslateEditionUID", type: "Uint" },
        10687: { name: "ChapterTranslateCodec", type: "Uint" },
        10661: { name: "ChapterTranslateID", type: "Binary" },
        710577: { name: "TimecodeScale", type: "Uint" },
        1161: { name: "Duration", type: "Float" },
        1121: { name: "DateUTC", type: "Date" },
        15273: { name: "Title", type: "String" },
        3456: { name: "MuxingApp", type: "String" },
        5953: { name: "WritingApp", type: "String" },
        // 0xf43b675: { name: 'Cluster', type: 'Container' },
        103: { name: "Timecode", type: "Uint" },
        6228: { name: "SilentTracks", type: "Container" },
        6359: { name: "SilentTrackNumber", type: "Uint" },
        39: { name: "Position", type: "Uint" },
        43: { name: "PrevSize", type: "Uint" },
        35: { name: "SimpleBlock", type: "Binary" },
        32: { name: "BlockGroup", type: "Container" },
        33: { name: "Block", type: "Binary" },
        34: { name: "BlockVirtual", type: "Binary" },
        13729: { name: "BlockAdditions", type: "Container" },
        38: { name: "BlockMore", type: "Container" },
        110: { name: "BlockAddID", type: "Uint" },
        37: { name: "BlockAdditional", type: "Binary" },
        27: { name: "BlockDuration", type: "Uint" },
        122: { name: "ReferencePriority", type: "Uint" },
        123: { name: "ReferenceBlock", type: "Int" },
        125: { name: "ReferenceVirtual", type: "Int" },
        36: { name: "CodecState", type: "Binary" },
        13730: { name: "DiscardPadding", type: "Int" },
        14: { name: "Slices", type: "Container" },
        104: { name: "TimeSlice", type: "Container" },
        76: { name: "LaceNumber", type: "Uint" },
        77: { name: "FrameNumber", type: "Uint" },
        75: { name: "BlockAdditionID", type: "Uint" },
        78: { name: "Delay", type: "Uint" },
        79: { name: "SliceDuration", type: "Uint" },
        72: { name: "ReferenceFrame", type: "Container" },
        73: { name: "ReferenceOffset", type: "Uint" },
        74: { name: "ReferenceTimeCode", type: "Uint" },
        47: { name: "EncryptedBlock", type: "Binary" },
        106212971: { name: "Tracks", type: "Container" },
        46: { name: "TrackEntry", type: "Container" },
        87: { name: "TrackNumber", type: "Uint" },
        13253: { name: "TrackUID", type: "Uint" },
        3: { name: "TrackType", type: "Uint" },
        57: { name: "FlagEnabled", type: "Uint" },
        8: { name: "FlagDefault", type: "Uint" },
        5546: { name: "FlagForced", type: "Uint" },
        28: { name: "FlagLacing", type: "Uint" },
        11751: { name: "MinCache", type: "Uint" },
        11768: { name: "MaxCache", type: "Uint" },
        254851: { name: "DefaultDuration", type: "Uint" },
        216698: { name: "DefaultDecodedFieldDuration", type: "Uint" },
        209231: { name: "TrackTimecodeScale", type: "Float" },
        4991: { name: "TrackOffset", type: "Int" },
        5614: { name: "MaxBlockAdditionID", type: "Uint" },
        4974: { name: "Name", type: "String" },
        177564: { name: "Language", type: "String" },
        6: { name: "CodecID", type: "String" },
        9122: { name: "CodecPrivate", type: "Binary" },
        362120: { name: "CodecName", type: "String" },
        13382: { name: "AttachmentLink", type: "Uint" },
        1742487: { name: "CodecSettings", type: "String" },
        1785920: { name: "CodecInfoURL", type: "String" },
        438848: { name: "CodecDownloadURL", type: "String" },
        42: { name: "CodecDecodeAll", type: "Uint" },
        12203: { name: "TrackOverlay", type: "Uint" },
        5802: { name: "CodecDelay", type: "Uint" },
        5819: { name: "SeekPreRoll", type: "Uint" },
        9764: { name: "TrackTranslate", type: "Container" },
        9980: { name: "TrackTranslateEditionUID", type: "Uint" },
        9919: { name: "TrackTranslateCodec", type: "Uint" },
        9893: { name: "TrackTranslateTrackID", type: "Binary" },
        96: { name: "Video", type: "Container" },
        26: { name: "FlagInterlaced", type: "Uint" },
        5048: { name: "StereoMode", type: "Uint" },
        5056: { name: "AlphaMode", type: "Uint" },
        5049: { name: "OldStereoMode", type: "Uint" },
        48: { name: "PixelWidth", type: "Uint" },
        58: { name: "PixelHeight", type: "Uint" },
        5290: { name: "PixelCropBottom", type: "Uint" },
        5307: { name: "PixelCropTop", type: "Uint" },
        5324: { name: "PixelCropLeft", type: "Uint" },
        5341: { name: "PixelCropRight", type: "Uint" },
        5296: { name: "DisplayWidth", type: "Uint" },
        5306: { name: "DisplayHeight", type: "Uint" },
        5298: { name: "DisplayUnit", type: "Uint" },
        5299: { name: "AspectRatioType", type: "Uint" },
        963876: { name: "ColourSpace", type: "Binary" },
        1029411: { name: "GammaValue", type: "Float" },
        230371: { name: "FrameRate", type: "Float" },
        97: { name: "Audio", type: "Container" },
        53: { name: "SamplingFrequency", type: "Float" },
        14517: { name: "OutputSamplingFrequency", type: "Float" },
        31: { name: "Channels", type: "Uint" },
        15739: { name: "ChannelPositions", type: "Binary" },
        8804: { name: "BitDepth", type: "Uint" },
        98: { name: "TrackOperation", type: "Container" },
        99: { name: "TrackCombinePlanes", type: "Container" },
        100: { name: "TrackPlane", type: "Container" },
        101: { name: "TrackPlaneUID", type: "Uint" },
        102: { name: "TrackPlaneType", type: "Uint" },
        105: { name: "TrackJoinBlocks", type: "Container" },
        109: { name: "TrackJoinUID", type: "Uint" },
        64: { name: "TrickTrackUID", type: "Uint" },
        65: { name: "TrickTrackSegmentUID", type: "Binary" },
        70: { name: "TrickTrackFlag", type: "Uint" },
        71: { name: "TrickMasterTrackUID", type: "Uint" },
        68: { name: "TrickMasterTrackSegmentUID", type: "Binary" },
        11648: { name: "ContentEncodings", type: "Container" },
        8768: { name: "ContentEncoding", type: "Container" },
        4145: { name: "ContentEncodingOrder", type: "Uint" },
        4146: { name: "ContentEncodingScope", type: "Uint" },
        4147: { name: "ContentEncodingType", type: "Uint" },
        4148: { name: "ContentCompression", type: "Container" },
        596: { name: "ContentCompAlgo", type: "Uint" },
        597: { name: "ContentCompSettings", type: "Binary" },
        4149: { name: "ContentEncryption", type: "Container" },
        2017: { name: "ContentEncAlgo", type: "Uint" },
        2018: { name: "ContentEncKeyID", type: "Binary" },
        2019: { name: "ContentSignature", type: "Binary" },
        2020: { name: "ContentSigKeyID", type: "Binary" },
        2021: { name: "ContentSigAlgo", type: "Uint" },
        2022: { name: "ContentSigHashAlgo", type: "Uint" },
        206814059: { name: "Cues", type: "Container" },
        59: { name: "CuePoint", type: "Container" },
        51: { name: "CueTime", type: "Uint" },
        55: { name: "CueTrackPositions", type: "Container" },
        119: { name: "CueTrack", type: "Uint" },
        113: { name: "CueClusterPosition", type: "Uint" },
        112: { name: "CueRelativePosition", type: "Uint" },
        50: { name: "CueDuration", type: "Uint" },
        4984: { name: "CueBlockNumber", type: "Uint" },
        106: { name: "CueCodecState", type: "Uint" },
        91: { name: "CueReference", type: "Container" },
        22: { name: "CueRefTime", type: "Uint" },
        23: { name: "CueRefCluster", type: "Uint" },
        4959: { name: "CueRefNumber", type: "Uint" },
        107: { name: "CueRefCodecState", type: "Uint" },
        155296873: { name: "Attachments", type: "Container" },
        8615: { name: "AttachedFile", type: "Container" },
        1662: { name: "FileDescription", type: "String" },
        1646: { name: "FileName", type: "String" },
        1632: { name: "FileMimeType", type: "String" },
        1628: { name: "FileData", type: "Binary" },
        1710: { name: "FileUID", type: "Uint" },
        1653: { name: "FileReferral", type: "Binary" },
        1633: { name: "FileUsedStartTime", type: "Uint" },
        1634: { name: "FileUsedEndTime", type: "Uint" },
        4433776: { name: "Chapters", type: "Container" },
        1465: { name: "EditionEntry", type: "Container" },
        1468: { name: "EditionUID", type: "Uint" },
        1469: { name: "EditionFlagHidden", type: "Uint" },
        1499: { name: "EditionFlagDefault", type: "Uint" },
        1501: { name: "EditionFlagOrdered", type: "Uint" },
        54: { name: "ChapterAtom", type: "Container" },
        13252: { name: "ChapterUID", type: "Uint" },
        5716: { name: "ChapterStringUID", type: "String" },
        17: { name: "ChapterTimeStart", type: "Uint" },
        18: { name: "ChapterTimeEnd", type: "Uint" },
        24: { name: "ChapterFlagHidden", type: "Uint" },
        1432: { name: "ChapterFlagEnabled", type: "Uint" },
        11879: { name: "ChapterSegmentUID", type: "Binary" },
        11964: { name: "ChapterSegmentEditionUID", type: "Uint" },
        9155: { name: "ChapterPhysicalEquiv", type: "Uint" },
        15: { name: "ChapterTrack", type: "Container" },
        9: { name: "ChapterTrackNumber", type: "Uint" },
        0: { name: "ChapterDisplay", type: "Container" },
        5: { name: "ChapString", type: "String" },
        892: { name: "ChapLanguage", type: "String" },
        894: { name: "ChapCountry", type: "String" },
        10564: { name: "ChapProcess", type: "Container" },
        10581: { name: "ChapProcessCodecID", type: "Uint" },
        1293: { name: "ChapProcessPrivate", type: "Binary" },
        10513: { name: "ChapProcessCommand", type: "Container" },
        10530: { name: "ChapProcessTime", type: "Uint" },
        10547: { name: "ChapProcessData", type: "Binary" },
        39109479: { name: "Tags", type: "Container" },
        13171: { name: "Tag", type: "Container" },
        9152: { name: "Targets", type: "Container" },
        10442: { name: "TargetTypeValue", type: "Uint" },
        9162: { name: "TargetType", type: "String" },
        9157: { name: "TagTrackUID", type: "Uint" },
        9161: { name: "TagEditionUID", type: "Uint" },
        9156: { name: "TagChapterUID", type: "Uint" },
        9158: { name: "TagAttachmentUID", type: "Uint" },
        10184: { name: "SimpleTag", type: "Container" },
        1443: { name: "TagName", type: "String" },
        1146: { name: "TagLanguage", type: "String" },
        1156: { name: "TagDefault", type: "Uint" },
        1159: { name: "TagString", type: "String" },
        1157: { name: "TagBinary", type: "Binary" }
      };
      function doInherit(newClass, baseClass) {
        newClass.prototype = Object.create(baseClass.prototype);
        newClass.prototype.constructor = newClass;
      }
      function WebmBase(name2, type) {
        this.name = name2 || "Unknown";
        this.type = type || "Unknown";
      }
      WebmBase.prototype.updateBySource = function() {
      };
      WebmBase.prototype.setSource = function(source) {
        this.source = source;
        this.updateBySource();
      };
      WebmBase.prototype.updateByData = function() {
      };
      WebmBase.prototype.setData = function(data) {
        this.data = data;
        this.updateByData();
      };
      function WebmUint(name2, type) {
        WebmBase.call(this, name2, type || "Uint");
      }
      doInherit(WebmUint, WebmBase);
      function padHex(hex) {
        return hex.length % 2 === 1 ? "0" + hex : hex;
      }
      WebmUint.prototype.updateBySource = function() {
        this.data = "";
        for (var i2 = 0; i2 < this.source.length; i2++) {
          var hex = this.source[i2].toString(16);
          this.data += padHex(hex);
        }
      };
      WebmUint.prototype.updateByData = function() {
        var length = this.data.length / 2;
        this.source = new Uint8Array(length);
        for (var i2 = 0; i2 < length; i2++) {
          var hex = this.data.substr(i2 * 2, 2);
          this.source[i2] = parseInt(hex, 16);
        }
      };
      WebmUint.prototype.getValue = function() {
        return parseInt(this.data, 16);
      };
      WebmUint.prototype.setValue = function(value) {
        this.setData(padHex(value.toString(16)));
      };
      function WebmFloat(name2, type) {
        WebmBase.call(this, name2, type || "Float");
      }
      doInherit(WebmFloat, WebmBase);
      WebmFloat.prototype.getFloatArrayType = function() {
        return this.source && this.source.length === 4 ? Float32Array : Float64Array;
      };
      WebmFloat.prototype.updateBySource = function() {
        var byteArray = this.source.reverse();
        var floatArrayType = this.getFloatArrayType();
        var floatArray = new floatArrayType(byteArray.buffer);
        this.data = floatArray[0];
      };
      WebmFloat.prototype.updateByData = function() {
        var floatArrayType = this.getFloatArrayType();
        var floatArray = new floatArrayType([this.data]);
        var byteArray = new Uint8Array(floatArray.buffer);
        this.source = byteArray.reverse();
      };
      WebmFloat.prototype.getValue = function() {
        return this.data;
      };
      WebmFloat.prototype.setValue = function(value) {
        this.setData(value);
      };
      function WebmContainer(name2, type) {
        WebmBase.call(this, name2, type || "Container");
      }
      doInherit(WebmContainer, WebmBase);
      WebmContainer.prototype.readByte = function() {
        return this.source[this.offset++];
      };
      WebmContainer.prototype.readUint = function() {
        var firstByte = this.readByte();
        var bytes = 8 - firstByte.toString(2).length;
        var value = firstByte - (1 << 7 - bytes);
        for (var i2 = 0; i2 < bytes; i2++) {
          value *= 256;
          value += this.readByte();
        }
        return value;
      };
      WebmContainer.prototype.updateBySource = function() {
        this.data = [];
        for (this.offset = 0; this.offset < this.source.length; this.offset = end) {
          var id3 = this.readUint();
          var len = this.readUint();
          var end = Math.min(this.offset + len, this.source.length);
          var data = this.source.slice(this.offset, end);
          var info = sections[id3] || { name: "Unknown", type: "Unknown" };
          var ctr = WebmBase;
          switch (info.type) {
            case "Container":
              ctr = WebmContainer;
              break;
            case "Uint":
              ctr = WebmUint;
              break;
            case "Float":
              ctr = WebmFloat;
              break;
          }
          var section = new ctr(info.name, info.type);
          section.setSource(data);
          this.data.push({
            id: id3,
            idHex: id3.toString(16),
            data: section
          });
        }
      };
      WebmContainer.prototype.writeUint = function(x2, draft) {
        for (var bytes = 1, flag = 128; x2 >= flag && bytes < 8; bytes++, flag *= 128) {
        }
        if (!draft) {
          var value = flag + x2;
          for (var i2 = bytes - 1; i2 >= 0; i2--) {
            var c2 = value % 256;
            this.source[this.offset + i2] = c2;
            value = (value - c2) / 256;
          }
        }
        this.offset += bytes;
      };
      WebmContainer.prototype.writeSections = function(draft) {
        this.offset = 0;
        for (var i2 = 0; i2 < this.data.length; i2++) {
          var section = this.data[i2], content3 = section.data.source, contentLength = content3.length;
          this.writeUint(section.id, draft);
          this.writeUint(contentLength, draft);
          if (!draft) {
            this.source.set(content3, this.offset);
          }
          this.offset += contentLength;
        }
        return this.offset;
      };
      WebmContainer.prototype.updateByData = function() {
        var length = this.writeSections("draft");
        this.source = new Uint8Array(length);
        this.writeSections();
      };
      WebmContainer.prototype.getSectionById = function(id3) {
        for (var i2 = 0; i2 < this.data.length; i2++) {
          var section = this.data[i2];
          if (section.id === id3) {
            return section.data;
          }
        }
        return null;
      };
      function WebmFile(source) {
        WebmContainer.call(this, "File", "File");
        this.setSource(source);
      }
      doInherit(WebmFile, WebmContainer);
      WebmFile.prototype.fixDuration = function(duration2, options) {
        var logger = options && options.logger;
        if (logger === void 0) {
          logger = function(message) {
            console.log(message);
          };
        } else if (!logger) {
          logger = function() {
          };
        }
        var segmentSection = this.getSectionById(139690087);
        if (!segmentSection) {
          logger("[fix-webm-duration] Segment section is missing");
          return false;
        }
        var infoSection = segmentSection.getSectionById(88713574);
        if (!infoSection) {
          logger("[fix-webm-duration] Info section is missing");
          return false;
        }
        var timeScaleSection = infoSection.getSectionById(710577);
        if (!timeScaleSection) {
          logger("[fix-webm-duration] TimecodeScale section is missing");
          return false;
        }
        var durationSection = infoSection.getSectionById(1161);
        if (durationSection) {
          if (durationSection.getValue() <= 0) {
            logger(`[fix-webm-duration] Duration section is present, but the value is ${durationSection.getValue()}`);
            durationSection.setValue(duration2);
          } else {
            logger(`[fix-webm-duration] Duration section is present, and the value is ${durationSection.getValue()}`);
            return false;
          }
        } else {
          logger("[fix-webm-duration] Duration section is missing");
          durationSection = new WebmFloat("Duration", "Float");
          durationSection.setValue(duration2);
          infoSection.data.push({
            id: 1161,
            data: durationSection
          });
        }
        timeScaleSection.setValue(1e6);
        infoSection.updateByData();
        segmentSection.updateByData();
        this.updateByData();
        return true;
      };
      WebmFile.prototype.toBlob = function(mimeType) {
        return new Blob([this.source.buffer], { type: mimeType || "video/webm" });
      };
      function fixWebmDuration2(blob, duration2, callback, options) {
        if (typeof callback === "object") {
          options = callback;
          callback = void 0;
        }
        if (!callback) {
          return new Promise(function(resolve) {
            fixWebmDuration2(blob, duration2, resolve, options);
          });
        }
        try {
          var reader = new FileReader();
          reader.onloadend = function() {
            try {
              var file = new WebmFile(new Uint8Array(reader.result));
              if (file.fixDuration(duration2, options)) {
                blob = file.toBlob(blob.type);
              }
            } catch (ex) {
            }
            callback(blob);
          };
          reader.readAsArrayBuffer(blob);
        } catch (ex) {
          callback(blob);
        }
      }
      fixWebmDuration2.default = fixWebmDuration2;
      return fixWebmDuration2;
    });
  }
});

// node_modules/lodash.mergewith/index.js
var require_lodash4 = __commonJS({
  "node_modules/lodash.mergewith/index.js"(exports, module) {
    var LARGE_ARRAY_SIZE = 200;
    var HASH_UNDEFINED = "__lodash_hash_undefined__";
    var HOT_COUNT = 800;
    var HOT_SPAN = 16;
    var MAX_SAFE_INTEGER = 9007199254740991;
    var argsTag = "[object Arguments]";
    var arrayTag = "[object Array]";
    var asyncTag = "[object AsyncFunction]";
    var boolTag = "[object Boolean]";
    var dateTag = "[object Date]";
    var errorTag = "[object Error]";
    var funcTag = "[object Function]";
    var genTag = "[object GeneratorFunction]";
    var mapTag = "[object Map]";
    var numberTag = "[object Number]";
    var nullTag = "[object Null]";
    var objectTag = "[object Object]";
    var proxyTag = "[object Proxy]";
    var regexpTag = "[object RegExp]";
    var setTag = "[object Set]";
    var stringTag = "[object String]";
    var undefinedTag = "[object Undefined]";
    var weakMapTag = "[object WeakMap]";
    var arrayBufferTag = "[object ArrayBuffer]";
    var dataViewTag = "[object DataView]";
    var float32Tag = "[object Float32Array]";
    var float64Tag = "[object Float64Array]";
    var int8Tag = "[object Int8Array]";
    var int16Tag = "[object Int16Array]";
    var int32Tag = "[object Int32Array]";
    var uint8Tag = "[object Uint8Array]";
    var uint8ClampedTag = "[object Uint8ClampedArray]";
    var uint16Tag = "[object Uint16Array]";
    var uint32Tag = "[object Uint32Array]";
    var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
    var reIsHostCtor = /^\[object .+?Constructor\]$/;
    var reIsUint = /^(?:0|[1-9]\d*)$/;
    var typedArrayTags = {};
    typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
    typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
    var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    var freeSelf = typeof self == "object" && self && self.Object === Object && self;
    var root4 = freeGlobal || freeSelf || Function("return this")();
    var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
    var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var freeProcess = moduleExports && freeGlobal.process;
    var nodeUtil = (function() {
      try {
        var types2 = freeModule && freeModule.require && freeModule.require("util").types;
        if (types2) {
          return types2;
        }
        return freeProcess && freeProcess.binding && freeProcess.binding("util");
      } catch (e) {
      }
    })();
    var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
    function apply(func, thisArg, args) {
      switch (args.length) {
        case 0:
          return func.call(thisArg);
        case 1:
          return func.call(thisArg, args[0]);
        case 2:
          return func.call(thisArg, args[0], args[1]);
        case 3:
          return func.call(thisArg, args[0], args[1], args[2]);
      }
      return func.apply(thisArg, args);
    }
    function baseTimes(n2, iteratee) {
      var index6 = -1, result = Array(n2);
      while (++index6 < n2) {
        result[index6] = iteratee(index6);
      }
      return result;
    }
    function baseUnary(func) {
      return function(value) {
        return func(value);
      };
    }
    function getValue2(object, key) {
      return object == null ? void 0 : object[key];
    }
    function overArg(func, transform2) {
      return function(arg) {
        return func(transform2(arg));
      };
    }
    var arrayProto = Array.prototype;
    var funcProto = Function.prototype;
    var objectProto = Object.prototype;
    var coreJsData = root4["__core-js_shared__"];
    var funcToString = funcProto.toString;
    var hasOwnProperty3 = objectProto.hasOwnProperty;
    var maskSrcKey = (function() {
      var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
      return uid ? "Symbol(src)_1." + uid : "";
    })();
    var nativeObjectToString = objectProto.toString;
    var objectCtorString = funcToString.call(Object);
    var reIsNative = RegExp(
      "^" + funcToString.call(hasOwnProperty3).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
    );
    var Buffer = moduleExports ? root4.Buffer : void 0;
    var Symbol2 = root4.Symbol;
    var Uint8Array2 = root4.Uint8Array;
    var allocUnsafe = Buffer ? Buffer.allocUnsafe : void 0;
    var getPrototype = overArg(Object.getPrototypeOf, Object);
    var objectCreate = Object.create;
    var propertyIsEnumerable = objectProto.propertyIsEnumerable;
    var splice2 = arrayProto.splice;
    var symToStringTag = Symbol2 ? Symbol2.toStringTag : void 0;
    var defineProperty = (function() {
      try {
        var func = getNative(Object, "defineProperty");
        func({}, "", {});
        return func;
      } catch (e) {
      }
    })();
    var nativeIsBuffer = Buffer ? Buffer.isBuffer : void 0;
    var nativeMax = Math.max;
    var nativeNow = Date.now;
    var Map2 = getNative(root4, "Map");
    var nativeCreate = getNative(Object, "create");
    var baseCreate = /* @__PURE__ */ (function() {
      function object() {
      }
      return function(proto) {
        if (!isObject2(proto)) {
          return {};
        }
        if (objectCreate) {
          return objectCreate(proto);
        }
        object.prototype = proto;
        var result = new object();
        object.prototype = void 0;
        return result;
      };
    })();
    function Hash(entries) {
      var index6 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index6 < length) {
        var entry = entries[index6];
        this.set(entry[0], entry[1]);
      }
    }
    function hashClear() {
      this.__data__ = nativeCreate ? nativeCreate(null) : {};
      this.size = 0;
    }
    function hashDelete(key) {
      var result = this.has(key) && delete this.__data__[key];
      this.size -= result ? 1 : 0;
      return result;
    }
    function hashGet(key) {
      var data = this.__data__;
      if (nativeCreate) {
        var result = data[key];
        return result === HASH_UNDEFINED ? void 0 : result;
      }
      return hasOwnProperty3.call(data, key) ? data[key] : void 0;
    }
    function hashHas(key) {
      var data = this.__data__;
      return nativeCreate ? data[key] !== void 0 : hasOwnProperty3.call(data, key);
    }
    function hashSet(key, value) {
      var data = this.__data__;
      this.size += this.has(key) ? 0 : 1;
      data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
      return this;
    }
    Hash.prototype.clear = hashClear;
    Hash.prototype["delete"] = hashDelete;
    Hash.prototype.get = hashGet;
    Hash.prototype.has = hashHas;
    Hash.prototype.set = hashSet;
    function ListCache(entries) {
      var index6 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index6 < length) {
        var entry = entries[index6];
        this.set(entry[0], entry[1]);
      }
    }
    function listCacheClear() {
      this.__data__ = [];
      this.size = 0;
    }
    function listCacheDelete(key) {
      var data = this.__data__, index6 = assocIndexOf(data, key);
      if (index6 < 0) {
        return false;
      }
      var lastIndex = data.length - 1;
      if (index6 == lastIndex) {
        data.pop();
      } else {
        splice2.call(data, index6, 1);
      }
      --this.size;
      return true;
    }
    function listCacheGet(key) {
      var data = this.__data__, index6 = assocIndexOf(data, key);
      return index6 < 0 ? void 0 : data[index6][1];
    }
    function listCacheHas(key) {
      return assocIndexOf(this.__data__, key) > -1;
    }
    function listCacheSet(key, value) {
      var data = this.__data__, index6 = assocIndexOf(data, key);
      if (index6 < 0) {
        ++this.size;
        data.push([key, value]);
      } else {
        data[index6][1] = value;
      }
      return this;
    }
    ListCache.prototype.clear = listCacheClear;
    ListCache.prototype["delete"] = listCacheDelete;
    ListCache.prototype.get = listCacheGet;
    ListCache.prototype.has = listCacheHas;
    ListCache.prototype.set = listCacheSet;
    function MapCache(entries) {
      var index6 = -1, length = entries == null ? 0 : entries.length;
      this.clear();
      while (++index6 < length) {
        var entry = entries[index6];
        this.set(entry[0], entry[1]);
      }
    }
    function mapCacheClear() {
      this.size = 0;
      this.__data__ = {
        "hash": new Hash(),
        "map": new (Map2 || ListCache)(),
        "string": new Hash()
      };
    }
    function mapCacheDelete(key) {
      var result = getMapData(this, key)["delete"](key);
      this.size -= result ? 1 : 0;
      return result;
    }
    function mapCacheGet(key) {
      return getMapData(this, key).get(key);
    }
    function mapCacheHas(key) {
      return getMapData(this, key).has(key);
    }
    function mapCacheSet(key, value) {
      var data = getMapData(this, key), size4 = data.size;
      data.set(key, value);
      this.size += data.size == size4 ? 0 : 1;
      return this;
    }
    MapCache.prototype.clear = mapCacheClear;
    MapCache.prototype["delete"] = mapCacheDelete;
    MapCache.prototype.get = mapCacheGet;
    MapCache.prototype.has = mapCacheHas;
    MapCache.prototype.set = mapCacheSet;
    function Stack(entries) {
      var data = this.__data__ = new ListCache(entries);
      this.size = data.size;
    }
    function stackClear() {
      this.__data__ = new ListCache();
      this.size = 0;
    }
    function stackDelete(key) {
      var data = this.__data__, result = data["delete"](key);
      this.size = data.size;
      return result;
    }
    function stackGet(key) {
      return this.__data__.get(key);
    }
    function stackHas(key) {
      return this.__data__.has(key);
    }
    function stackSet(key, value) {
      var data = this.__data__;
      if (data instanceof ListCache) {
        var pairs = data.__data__;
        if (!Map2 || pairs.length < LARGE_ARRAY_SIZE - 1) {
          pairs.push([key, value]);
          this.size = ++data.size;
          return this;
        }
        data = this.__data__ = new MapCache(pairs);
      }
      data.set(key, value);
      this.size = data.size;
      return this;
    }
    Stack.prototype.clear = stackClear;
    Stack.prototype["delete"] = stackDelete;
    Stack.prototype.get = stackGet;
    Stack.prototype.has = stackHas;
    Stack.prototype.set = stackSet;
    function arrayLikeKeys(value, inherited) {
      var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result = skipIndexes ? baseTimes(value.length, String) : [], length = result.length;
      for (var key in value) {
        if ((inherited || hasOwnProperty3.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
        (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
        isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
        isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
        isIndex(key, length)))) {
          result.push(key);
        }
      }
      return result;
    }
    function assignMergeValue(object, key, value) {
      if (value !== void 0 && !eq(object[key], value) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    function assignValue(object, key, value) {
      var objValue = object[key];
      if (!(hasOwnProperty3.call(object, key) && eq(objValue, value)) || value === void 0 && !(key in object)) {
        baseAssignValue(object, key, value);
      }
    }
    function assocIndexOf(array, key) {
      var length = array.length;
      while (length--) {
        if (eq(array[length][0], key)) {
          return length;
        }
      }
      return -1;
    }
    function baseAssignValue(object, key, value) {
      if (key == "__proto__" && defineProperty) {
        defineProperty(object, key, {
          "configurable": true,
          "enumerable": true,
          "value": value,
          "writable": true
        });
      } else {
        object[key] = value;
      }
    }
    var baseFor = createBaseFor();
    function baseGetTag(value) {
      if (value == null) {
        return value === void 0 ? undefinedTag : nullTag;
      }
      return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
    }
    function baseIsArguments(value) {
      return isObjectLike(value) && baseGetTag(value) == argsTag;
    }
    function baseIsNative(value) {
      if (!isObject2(value) || isMasked(value)) {
        return false;
      }
      var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
      return pattern.test(toSource(value));
    }
    function baseIsTypedArray(value) {
      return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
    }
    function baseKeysIn(object) {
      if (!isObject2(object)) {
        return nativeKeysIn(object);
      }
      var isProto = isPrototype(object), result = [];
      for (var key in object) {
        if (!(key == "constructor" && (isProto || !hasOwnProperty3.call(object, key)))) {
          result.push(key);
        }
      }
      return result;
    }
    function baseMerge(object, source, srcIndex, customizer, stack) {
      if (object === source) {
        return;
      }
      baseFor(source, function(srcValue, key) {
        stack || (stack = new Stack());
        if (isObject2(srcValue)) {
          baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
        } else {
          var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source, stack) : void 0;
          if (newValue === void 0) {
            newValue = srcValue;
          }
          assignMergeValue(object, key, newValue);
        }
      }, keysIn);
    }
    function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
      var objValue = safeGet(object, key), srcValue = safeGet(source, key), stacked = stack.get(srcValue);
      if (stacked) {
        assignMergeValue(object, key, stacked);
        return;
      }
      var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source, stack) : void 0;
      var isCommon = newValue === void 0;
      if (isCommon) {
        var isArr = isArray(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
        newValue = srcValue;
        if (isArr || isBuff || isTyped) {
          if (isArray(objValue)) {
            newValue = objValue;
          } else if (isArrayLikeObject(objValue)) {
            newValue = copyArray(objValue);
          } else if (isBuff) {
            isCommon = false;
            newValue = cloneBuffer(srcValue, true);
          } else if (isTyped) {
            isCommon = false;
            newValue = cloneTypedArray(srcValue, true);
          } else {
            newValue = [];
          }
        } else if (isPlainObject2(srcValue) || isArguments(srcValue)) {
          newValue = objValue;
          if (isArguments(objValue)) {
            newValue = toPlainObject(objValue);
          } else if (!isObject2(objValue) || isFunction(objValue)) {
            newValue = initCloneObject(srcValue);
          }
        } else {
          isCommon = false;
        }
      }
      if (isCommon) {
        stack.set(srcValue, newValue);
        mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
        stack["delete"](srcValue);
      }
      assignMergeValue(object, key, newValue);
    }
    function baseRest(func, start2) {
      return setToString(overRest(func, start2, identity), func + "");
    }
    var baseSetToString = !defineProperty ? identity : function(func, string3) {
      return defineProperty(func, "toString", {
        "configurable": true,
        "enumerable": false,
        "value": constant(string3),
        "writable": true
      });
    };
    function cloneBuffer(buffer, isDeep) {
      if (isDeep) {
        return buffer.slice();
      }
      var length = buffer.length, result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);
      buffer.copy(result);
      return result;
    }
    function cloneArrayBuffer(arrayBuffer) {
      var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
      new Uint8Array2(result).set(new Uint8Array2(arrayBuffer));
      return result;
    }
    function cloneTypedArray(typedArray, isDeep) {
      var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
      return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
    }
    function copyArray(source, array) {
      var index6 = -1, length = source.length;
      array || (array = Array(length));
      while (++index6 < length) {
        array[index6] = source[index6];
      }
      return array;
    }
    function copyObject(source, props, object, customizer) {
      var isNew = !object;
      object || (object = {});
      var index6 = -1, length = props.length;
      while (++index6 < length) {
        var key = props[index6];
        var newValue = customizer ? customizer(object[key], source[key], key, object, source) : void 0;
        if (newValue === void 0) {
          newValue = source[key];
        }
        if (isNew) {
          baseAssignValue(object, key, newValue);
        } else {
          assignValue(object, key, newValue);
        }
      }
      return object;
    }
    function createAssigner(assigner) {
      return baseRest(function(object, sources) {
        var index6 = -1, length = sources.length, customizer = length > 1 ? sources[length - 1] : void 0, guard = length > 2 ? sources[2] : void 0;
        customizer = assigner.length > 3 && typeof customizer == "function" ? (length--, customizer) : void 0;
        if (guard && isIterateeCall(sources[0], sources[1], guard)) {
          customizer = length < 3 ? void 0 : customizer;
          length = 1;
        }
        object = Object(object);
        while (++index6 < length) {
          var source = sources[index6];
          if (source) {
            assigner(object, source, index6, customizer);
          }
        }
        return object;
      });
    }
    function createBaseFor(fromRight) {
      return function(object, iteratee, keysFunc) {
        var index6 = -1, iterable = Object(object), props = keysFunc(object), length = props.length;
        while (length--) {
          var key = props[fromRight ? length : ++index6];
          if (iteratee(iterable[key], key, iterable) === false) {
            break;
          }
        }
        return object;
      };
    }
    function getMapData(map4, key) {
      var data = map4.__data__;
      return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
    }
    function getNative(object, key) {
      var value = getValue2(object, key);
      return baseIsNative(value) ? value : void 0;
    }
    function getRawTag(value) {
      var isOwn = hasOwnProperty3.call(value, symToStringTag), tag = value[symToStringTag];
      try {
        value[symToStringTag] = void 0;
        var unmasked = true;
      } catch (e) {
      }
      var result = nativeObjectToString.call(value);
      if (unmasked) {
        if (isOwn) {
          value[symToStringTag] = tag;
        } else {
          delete value[symToStringTag];
        }
      }
      return result;
    }
    function initCloneObject(object) {
      return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
    }
    function isIndex(value, length) {
      var type = typeof value;
      length = length == null ? MAX_SAFE_INTEGER : length;
      return !!length && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
    }
    function isIterateeCall(value, index6, object) {
      if (!isObject2(object)) {
        return false;
      }
      var type = typeof index6;
      if (type == "number" ? isArrayLike(object) && isIndex(index6, object.length) : type == "string" && index6 in object) {
        return eq(object[index6], value);
      }
      return false;
    }
    function isKeyable(value) {
      var type = typeof value;
      return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
    }
    function isMasked(func) {
      return !!maskSrcKey && maskSrcKey in func;
    }
    function isPrototype(value) {
      var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
      return value === proto;
    }
    function nativeKeysIn(object) {
      var result = [];
      if (object != null) {
        for (var key in Object(object)) {
          result.push(key);
        }
      }
      return result;
    }
    function objectToString(value) {
      return nativeObjectToString.call(value);
    }
    function overRest(func, start2, transform2) {
      start2 = nativeMax(start2 === void 0 ? func.length - 1 : start2, 0);
      return function() {
        var args = arguments, index6 = -1, length = nativeMax(args.length - start2, 0), array = Array(length);
        while (++index6 < length) {
          array[index6] = args[start2 + index6];
        }
        index6 = -1;
        var otherArgs = Array(start2 + 1);
        while (++index6 < start2) {
          otherArgs[index6] = args[index6];
        }
        otherArgs[start2] = transform2(array);
        return apply(func, this, otherArgs);
      };
    }
    function safeGet(object, key) {
      if (key === "constructor" && typeof object[key] === "function") {
        return;
      }
      if (key == "__proto__") {
        return;
      }
      return object[key];
    }
    var setToString = shortOut(baseSetToString);
    function shortOut(func) {
      var count2 = 0, lastCalled = 0;
      return function() {
        var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
        lastCalled = stamp;
        if (remaining > 0) {
          if (++count2 >= HOT_COUNT) {
            return arguments[0];
          }
        } else {
          count2 = 0;
        }
        return func.apply(void 0, arguments);
      };
    }
    function toSource(func) {
      if (func != null) {
        try {
          return funcToString.call(func);
        } catch (e) {
        }
        try {
          return func + "";
        } catch (e) {
        }
      }
      return "";
    }
    function eq(value, other) {
      return value === other || value !== value && other !== other;
    }
    var isArguments = baseIsArguments(/* @__PURE__ */ (function() {
      return arguments;
    })()) ? baseIsArguments : function(value) {
      return isObjectLike(value) && hasOwnProperty3.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
    };
    var isArray = Array.isArray;
    function isArrayLike(value) {
      return value != null && isLength(value.length) && !isFunction(value);
    }
    function isArrayLikeObject(value) {
      return isObjectLike(value) && isArrayLike(value);
    }
    var isBuffer = nativeIsBuffer || stubFalse;
    function isFunction(value) {
      if (!isObject2(value)) {
        return false;
      }
      var tag = baseGetTag(value);
      return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
    }
    function isLength(value) {
      return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
    }
    function isObject2(value) {
      var type = typeof value;
      return value != null && (type == "object" || type == "function");
    }
    function isObjectLike(value) {
      return value != null && typeof value == "object";
    }
    function isPlainObject2(value) {
      if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
        return false;
      }
      var proto = getPrototype(value);
      if (proto === null) {
        return true;
      }
      var Ctor = hasOwnProperty3.call(proto, "constructor") && proto.constructor;
      return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
    }
    var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
    function toPlainObject(value) {
      return copyObject(value, keysIn(value));
    }
    function keysIn(object) {
      return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
    }
    var mergeWith2 = createAssigner(function(object, source, srcIndex, customizer) {
      baseMerge(object, source, srcIndex, customizer);
    });
    function constant(value) {
      return function() {
        return value;
      };
    }
    function identity(value) {
      return value;
    }
    function stubFalse() {
      return false;
    }
    module.exports = mergeWith2;
  }
});

// node_modules/attr-accept/dist/es/index.js
var require_es = __commonJS({
  "node_modules/attr-accept/dist/es/index.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.default = function(file, acceptedFiles) {
      if (file && acceptedFiles) {
        var acceptedFilesArray = Array.isArray(acceptedFiles) ? acceptedFiles : acceptedFiles.split(",");
        if (acceptedFilesArray.length === 0) {
          return true;
        }
        var fileName = file.name || "";
        var mimeType = (file.type || "").toLowerCase();
        var baseMimeType = mimeType.replace(/\/.*$/, "");
        return acceptedFilesArray.some(function(type) {
          var validType = type.trim().toLowerCase();
          if (validType.charAt(0) === ".") {
            return fileName.toLowerCase().endsWith(validType);
          } else if (validType.endsWith("/*")) {
            return baseMimeType === validType.replace(/\/.*$/, "");
          }
          return mimeType === validType;
        });
      }
      return true;
    };
  }
});

// node_modules/stream-chat-react/dist/es/index.mjs
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);

// node_modules/clsx/dist/clsx.mjs
function r(e) {
  var t2, f2, n2 = "";
  if ("string" == typeof e || "number" == typeof e) n2 += e;
  else if ("object" == typeof e) if (Array.isArray(e)) {
    var o2 = e.length;
    for (t2 = 0; t2 < o2; t2++) e[t2] && (f2 = r(e[t2])) && (n2 && (n2 += " "), n2 += f2);
  } else for (f2 in e) e[f2] && (n2 && (n2 += " "), n2 += f2);
  return n2;
}
function clsx() {
  for (var e, t2, f2 = 0, n2 = "", o2 = arguments.length; f2 < o2; f2++) (e = arguments[f2]) && (t2 = r(e)) && (n2 && (n2 += " "), n2 += t2);
  return n2;
}
var clsx_default = clsx;

// node_modules/nanoid/index.browser.js
var nanoid = (size4 = 21) => crypto.getRandomValues(new Uint8Array(size4)).reduce((id3, byte) => {
  byte &= 63;
  if (byte < 36) {
    id3 += byte.toString(36);
  } else if (byte < 62) {
    id3 += (byte - 26).toString(36).toUpperCase();
  } else if (byte > 62) {
    id3 += "-";
  } else {
    id3 += "_";
  }
  return id3;
}, "");

// node_modules/stream-chat-react/dist/es/index.mjs
var import_react29 = __toESM(require_react(), 1);

// node_modules/stream-chat-react/dist/es/audioProcessing.21cb49e1.mjs
var import_react = __toESM(require_react(), 1);
var useHandleFileChangeWrapper = (resetOnChange = false, handler) => (0, import_react.useCallback)(
  ({ currentTarget }) => {
    const { files } = currentTarget;
    if (!files) return;
    try {
      handler?.(Array.from(files));
    } catch (error2) {
      console.error(error2);
    }
    if (resetOnChange) currentTarget.value = "";
  },
  [handler, resetOnChange]
);
async function dataTransferItemsToFiles(items) {
  if (!items || !items.length) {
    return [];
  }
  const fileLikes = getFileLikes(items);
  if (fileLikes.length) {
    return fileLikes;
  }
  const blobPromises = [];
  for (const item of items) {
    if (item.type === "text/html") {
      blobPromises.push(
        new Promise((accept) => {
          item.getAsString(async (s2) => {
            const imagePromises = extractImageSources(s2).map(
              (src) => getImageSource(fileLikes, src)
            );
            await Promise.all(imagePromises);
            accept();
          });
        })
      );
    }
  }
  await Promise.all(blobPromises);
  return fileLikes;
}
function getFileLikes(items) {
  const fileLikes = [];
  for (const item of items) {
    if (item.kind === "file") {
      const file = item.getAsFile();
      if (file) {
        fileLikes.push(file);
      }
    }
  }
  return fileLikes;
}
async function getImageSource(fileLikes, src) {
  let res;
  try {
    res = await fetch(src);
  } catch (e) {
    return;
  }
  const contentType = res.headers.get("Content-type") || "application/octet-stream";
  const buf = await res.arrayBuffer();
  const blob = new Blob([buf], { type: contentType });
  fileLikes.push(blob);
}
var extractImageSources = (s2) => {
  const imageTags = new DOMParser().parseFromString(s2, "text/html").getElementsByTagName("img");
  return Array.from(imageTags, (tag) => tag.src).filter((tag) => tag);
};
var createFileFromBlobs = ({
  blobsArray,
  fileName,
  mimeType
}) => {
  const concatenatedBlob = new Blob(blobsArray, { type: mimeType });
  return new File([concatenatedBlob], fileName, { type: concatenatedBlob.type });
};
var getExtensionFromMimeType = (mimeType) => {
  const match = mimeType.match(/\/([^/;]+)/);
  return match && match[1];
};
var getRecordedMediaTypeFromMimeType = (mimeType) => {
  const match = mimeType.match(/^(audio|video)\/.*$/);
  return match && match[1];
};
var readFileAsArrayBuffer = (file) => new Promise((resolve, reject) => {
  const fileReader = new FileReader();
  fileReader.onload = () => {
    resolve(fileReader.result);
  };
  fileReader.onerror = () => {
    reject(fileReader.error);
  };
  fileReader.readAsArrayBuffer(file);
});
var toAudioBuffer = async (file) => {
  const audioCtx = new AudioContext();
  const arrayBuffer = await readFileAsArrayBuffer(file);
  const decodedData = await audioCtx.decodeAudioData(arrayBuffer);
  if (audioCtx.state !== "closed") await audioCtx.close();
  return decodedData;
};
var renderAudio = async (audioBuffer, sampleRate) => {
  const offlineAudioCtx = new OfflineAudioContext(
    audioBuffer.numberOfChannels,
    audioBuffer.duration * sampleRate,
    sampleRate
  );
  const source = offlineAudioCtx.createBufferSource();
  source.buffer = audioBuffer;
  source.connect(offlineAudioCtx.destination);
  source.start();
  return await offlineAudioCtx.startRendering();
};

// node_modules/stream-chat-react/dist/es/WithAudioPlayback.610fdf2c.mjs
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react4 = __toESM(require_react(), 1);

// node_modules/@floating-ui/react/dist/floating-ui.react.mjs
var React3 = __toESM(require_react(), 1);

// node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
function hasWindow() {
  return typeof window !== "undefined";
}
function getNodeName(node2) {
  if (isNode(node2)) {
    return (node2.nodeName || "").toLowerCase();
  }
  return "#document";
}
function getWindow(node2) {
  var _node$ownerDocument;
  return (node2 == null || (_node$ownerDocument = node2.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node2) {
  var _ref;
  return (_ref = (isNode(node2) ? node2.ownerDocument : node2.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Node || value instanceof getWindow(value).Node;
}
function isElement(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Element || value instanceof getWindow(value).Element;
}
function isHTMLElement(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function isShadowRoot(value) {
  if (!hasWindow() || typeof ShadowRoot === "undefined") {
    return false;
  }
  return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
}
function isOverflowElement(element4) {
  const {
    overflow,
    overflowX,
    overflowY,
    display
  } = getComputedStyle2(element4);
  return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
}
function isTableElement(element4) {
  return /^(table|td|th)$/.test(getNodeName(element4));
}
function isTopLayer(element4) {
  try {
    if (element4.matches(":popover-open")) {
      return true;
    }
  } catch (_e2) {
  }
  try {
    return element4.matches(":modal");
  } catch (_e2) {
    return false;
  }
}
var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
var containRe = /paint|layout|strict|content/;
var isNotNone = (value) => !!value && value !== "none";
var isWebKitValue;
function isContainingBlock(elementOrCss) {
  const css = isElement(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
  return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
}
function getContainingBlock(element4) {
  let currentNode = getParentNode(element4);
  while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
    if (isContainingBlock(currentNode)) {
      return currentNode;
    } else if (isTopLayer(currentNode)) {
      return null;
    }
    currentNode = getParentNode(currentNode);
  }
  return null;
}
function isWebKit() {
  if (isWebKitValue == null) {
    isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
  }
  return isWebKitValue;
}
function isLastTraversableNode(node2) {
  return /^(html|body|#document)$/.test(getNodeName(node2));
}
function getComputedStyle2(element4) {
  return getWindow(element4).getComputedStyle(element4);
}
function getNodeScroll(element4) {
  if (isElement(element4)) {
    return {
      scrollLeft: element4.scrollLeft,
      scrollTop: element4.scrollTop
    };
  }
  return {
    scrollLeft: element4.scrollX,
    scrollTop: element4.scrollY
  };
}
function getParentNode(node2) {
  if (getNodeName(node2) === "html") {
    return node2;
  }
  const result = (
    // Step into the shadow DOM of the parent of a slotted node.
    node2.assignedSlot || // DOM Element detected.
    node2.parentNode || // ShadowRoot detected.
    isShadowRoot(node2) && node2.host || // Fallback.
    getDocumentElement(node2)
  );
  return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node2) {
  const parentNode = getParentNode(node2);
  if (isLastTraversableNode(parentNode)) {
    return node2.ownerDocument ? node2.ownerDocument.body : node2.body;
  }
  if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
    return parentNode;
  }
  return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node2, list4, traverseIframes) {
  var _node$ownerDocument2;
  if (list4 === void 0) {
    list4 = [];
  }
  if (traverseIframes === void 0) {
    traverseIframes = true;
  }
  const scrollableAncestor = getNearestOverflowAncestor(node2);
  const isBody = scrollableAncestor === ((_node$ownerDocument2 = node2.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
  const win = getWindow(scrollableAncestor);
  if (isBody) {
    const frameElement = getFrameElement(win);
    return list4.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
  } else {
    return list4.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
  }
}
function getFrameElement(win) {
  return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}

// node_modules/@floating-ui/react/dist/floating-ui.react.utils.mjs
var React = __toESM(require_react(), 1);
var import_react2 = __toESM(require_react(), 1);

// node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
var sides = ["top", "right", "bottom", "left"];
var alignments = ["start", "end"];
var placements = sides.reduce((acc, side) => acc.concat(side, side + "-" + alignments[0], side + "-" + alignments[1]), []);
var min = Math.min;
var max = Math.max;
var round = Math.round;
var floor = Math.floor;
var createCoords = (v2) => ({
  x: v2,
  y: v2
});
var oppositeSideMap = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function clamp(start2, value, end) {
  return max(start2, min(value, end));
}
function evaluate(value, param) {
  return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
  return placement.split("-")[0];
}
function getAlignment(placement) {
  return placement.split("-")[1];
}
function getOppositeAxis(axis) {
  return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
  return axis === "y" ? "height" : "width";
}
function getSideAxis(placement) {
  const firstChar = placement[0];
  return firstChar === "t" || firstChar === "b" ? "y" : "x";
}
function getAlignmentAxis(placement) {
  return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
  if (rtl === void 0) {
    rtl = false;
  }
  const alignment = getAlignment(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const length = getAxisLength(alignmentAxis);
  let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
  if (rects.reference[length] > rects.floating[length]) {
    mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
  }
  return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
  const oppositePlacement = getOppositePlacement(placement);
  return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
}
function getOppositeAlignmentPlacement(placement) {
  return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
}
var lrPlacement = ["left", "right"];
var rlPlacement = ["right", "left"];
var tbPlacement = ["top", "bottom"];
var btPlacement = ["bottom", "top"];
function getSideList(side, isStart, rtl) {
  switch (side) {
    case "top":
    case "bottom":
      if (rtl) return isStart ? rlPlacement : lrPlacement;
      return isStart ? lrPlacement : rlPlacement;
    case "left":
    case "right":
      return isStart ? tbPlacement : btPlacement;
    default:
      return [];
  }
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
  const alignment = getAlignment(placement);
  let list4 = getSideList(getSide(placement), direction === "start", rtl);
  if (alignment) {
    list4 = list4.map((side) => side + "-" + alignment);
    if (flipAlignment) {
      list4 = list4.concat(list4.map(getOppositeAlignmentPlacement));
    }
  }
  return list4;
}
function getOppositePlacement(placement) {
  const side = getSide(placement);
  return oppositeSideMap[side] + placement.slice(side.length);
}
function expandPaddingObject(padding) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...padding
  };
}
function getPaddingObject(padding) {
  return typeof padding !== "number" ? expandPaddingObject(padding) : {
    top: padding,
    right: padding,
    bottom: padding,
    left: padding
  };
}
function rectToClientRect(rect) {
  const {
    x: x2,
    y: y2,
    width,
    height
  } = rect;
  return {
    width,
    height,
    top: y2,
    left: x2,
    right: x2 + width,
    bottom: y2 + height,
    x: x2,
    y: y2
  };
}

// node_modules/tabbable/dist/index.esm.js
var candidateSelectors = ["input:not([inert]):not([inert] *)", "select:not([inert]):not([inert] *)", "textarea:not([inert]):not([inert] *)", "a[href]:not([inert]):not([inert] *)", "button:not([inert]):not([inert] *)", "[tabindex]:not(slot):not([inert]):not([inert] *)", "audio[controls]:not([inert]):not([inert] *)", "video[controls]:not([inert]):not([inert] *)", '[contenteditable]:not([contenteditable="false"]):not([inert]):not([inert] *)', "details>summary:first-of-type:not([inert]):not([inert] *)", "details:not([inert]):not([inert] *)"];
var candidateSelector = candidateSelectors.join(",");
var NoElement = typeof Element === "undefined";
var matches = NoElement ? function() {
} : Element.prototype.matches || Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector;
var getRootNode = !NoElement && Element.prototype.getRootNode ? function(element4) {
  var _element$getRootNode;
  return element4 === null || element4 === void 0 ? void 0 : (_element$getRootNode = element4.getRootNode) === null || _element$getRootNode === void 0 ? void 0 : _element$getRootNode.call(element4);
} : function(element4) {
  return element4 === null || element4 === void 0 ? void 0 : element4.ownerDocument;
};
var focusableCandidateSelector = candidateSelectors.concat("iframe:not([inert]):not([inert] *)").join(",");

// node_modules/@floating-ui/react/dist/floating-ui.react.utils.mjs
function getPlatform() {
  const uaData = navigator.userAgentData;
  if (uaData != null && uaData.platform) {
    return uaData.platform;
  }
  return navigator.platform;
}
function isSafari() {
  return /apple/i.test(navigator.vendor);
}
var ARROW_LEFT = "ArrowLeft";
var ARROW_RIGHT = "ArrowRight";
var ARROW_UP = "ArrowUp";
var ARROW_DOWN = "ArrowDown";
function stopEvent(event) {
  event.preventDefault();
  event.stopPropagation();
}
var isClient = typeof document !== "undefined";
var noop = function noop2() {
};
var index = isClient ? import_react2.useLayoutEffect : noop;
var SafeReact = {
  ...React
};
var useInsertionEffect = SafeReact.useInsertionEffect;
var useSafeInsertionEffect = useInsertionEffect || ((fn) => fn());
function useEffectEvent(callback) {
  const ref = React.useRef(() => {
    if (true) {
      throw new Error("Cannot call an event handler while rendering.");
    }
  });
  useSafeInsertionEffect(() => {
    ref.current = callback;
  });
  return React.useCallback(function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return ref.current == null ? void 0 : ref.current(...args);
  }, []);
}
function isDifferentGridRow(index6, cols, prevRow) {
  return Math.floor(index6 / cols) !== prevRow;
}
function isIndexOutOfListBounds(listRef, index6) {
  return index6 < 0 || index6 >= listRef.current.length;
}
function getMinListIndex(listRef, disabledIndices) {
  return findNonDisabledListIndex(listRef, {
    disabledIndices
  });
}
function getMaxListIndex(listRef, disabledIndices) {
  return findNonDisabledListIndex(listRef, {
    decrement: true,
    startingIndex: listRef.current.length,
    disabledIndices
  });
}
function findNonDisabledListIndex(listRef, _temp) {
  let {
    startingIndex = -1,
    decrement = false,
    disabledIndices,
    amount = 1
  } = _temp === void 0 ? {} : _temp;
  let index6 = startingIndex;
  do {
    index6 += decrement ? -amount : amount;
  } while (index6 >= 0 && index6 <= listRef.current.length - 1 && isListIndexDisabled(listRef, index6, disabledIndices));
  return index6;
}
function getGridNavigatedIndex(listRef, _ref) {
  let {
    event,
    orientation,
    loop,
    rtl,
    cols,
    disabledIndices,
    minIndex,
    maxIndex,
    prevIndex,
    stopEvent: stop = false
  } = _ref;
  let nextIndex = prevIndex;
  if (event.key === ARROW_UP) {
    stop && stopEvent(event);
    if (prevIndex === -1) {
      nextIndex = maxIndex;
    } else {
      nextIndex = findNonDisabledListIndex(listRef, {
        startingIndex: nextIndex,
        amount: cols,
        decrement: true,
        disabledIndices
      });
      if (loop && (prevIndex - cols < minIndex || nextIndex < 0)) {
        const col = prevIndex % cols;
        const maxCol = maxIndex % cols;
        const offset4 = maxIndex - (maxCol - col);
        if (maxCol === col) {
          nextIndex = maxIndex;
        } else {
          nextIndex = maxCol > col ? offset4 : offset4 - cols;
        }
      }
    }
    if (isIndexOutOfListBounds(listRef, nextIndex)) {
      nextIndex = prevIndex;
    }
  }
  if (event.key === ARROW_DOWN) {
    stop && stopEvent(event);
    if (prevIndex === -1) {
      nextIndex = minIndex;
    } else {
      nextIndex = findNonDisabledListIndex(listRef, {
        startingIndex: prevIndex,
        amount: cols,
        disabledIndices
      });
      if (loop && prevIndex + cols > maxIndex) {
        nextIndex = findNonDisabledListIndex(listRef, {
          startingIndex: prevIndex % cols - cols,
          amount: cols,
          disabledIndices
        });
      }
    }
    if (isIndexOutOfListBounds(listRef, nextIndex)) {
      nextIndex = prevIndex;
    }
  }
  if (orientation === "both") {
    const prevRow = floor(prevIndex / cols);
    if (event.key === (rtl ? ARROW_LEFT : ARROW_RIGHT)) {
      stop && stopEvent(event);
      if (prevIndex % cols !== cols - 1) {
        nextIndex = findNonDisabledListIndex(listRef, {
          startingIndex: prevIndex,
          disabledIndices
        });
        if (loop && isDifferentGridRow(nextIndex, cols, prevRow)) {
          nextIndex = findNonDisabledListIndex(listRef, {
            startingIndex: prevIndex - prevIndex % cols - 1,
            disabledIndices
          });
        }
      } else if (loop) {
        nextIndex = findNonDisabledListIndex(listRef, {
          startingIndex: prevIndex - prevIndex % cols - 1,
          disabledIndices
        });
      }
      if (isDifferentGridRow(nextIndex, cols, prevRow)) {
        nextIndex = prevIndex;
      }
    }
    if (event.key === (rtl ? ARROW_RIGHT : ARROW_LEFT)) {
      stop && stopEvent(event);
      if (prevIndex % cols !== 0) {
        nextIndex = findNonDisabledListIndex(listRef, {
          startingIndex: prevIndex,
          decrement: true,
          disabledIndices
        });
        if (loop && isDifferentGridRow(nextIndex, cols, prevRow)) {
          nextIndex = findNonDisabledListIndex(listRef, {
            startingIndex: prevIndex + (cols - prevIndex % cols),
            decrement: true,
            disabledIndices
          });
        }
      } else if (loop) {
        nextIndex = findNonDisabledListIndex(listRef, {
          startingIndex: prevIndex + (cols - prevIndex % cols),
          decrement: true,
          disabledIndices
        });
      }
      if (isDifferentGridRow(nextIndex, cols, prevRow)) {
        nextIndex = prevIndex;
      }
    }
    const lastRow = floor(maxIndex / cols) === prevRow;
    if (isIndexOutOfListBounds(listRef, nextIndex)) {
      if (loop && lastRow) {
        nextIndex = event.key === (rtl ? ARROW_RIGHT : ARROW_LEFT) ? maxIndex : findNonDisabledListIndex(listRef, {
          startingIndex: prevIndex - prevIndex % cols - 1,
          disabledIndices
        });
      } else {
        nextIndex = prevIndex;
      }
    }
  }
  return nextIndex;
}
function createGridCellMap(sizes, cols, dense) {
  const cellMap = [];
  let startIndex = 0;
  sizes.forEach((_ref2, index6) => {
    let {
      width,
      height
    } = _ref2;
    if (width > cols) {
      if (true) {
        throw new Error("[Floating UI]: Invalid grid - item width at index " + index6 + " is greater than grid columns");
      }
    }
    let itemPlaced = false;
    if (dense) {
      startIndex = 0;
    }
    while (!itemPlaced) {
      const targetCells = [];
      for (let i2 = 0; i2 < width; i2++) {
        for (let j2 = 0; j2 < height; j2++) {
          targetCells.push(startIndex + i2 + j2 * cols);
        }
      }
      if (startIndex % cols + width <= cols && targetCells.every((cell) => cellMap[cell] == null)) {
        targetCells.forEach((cell) => {
          cellMap[cell] = index6;
        });
        itemPlaced = true;
      } else {
        startIndex++;
      }
    }
  });
  return [...cellMap];
}
function getGridCellIndexOfCorner(index6, sizes, cellMap, cols, corner) {
  if (index6 === -1) return -1;
  const firstCellIndex = cellMap.indexOf(index6);
  const sizeItem = sizes[index6];
  switch (corner) {
    case "tl":
      return firstCellIndex;
    case "tr":
      if (!sizeItem) {
        return firstCellIndex;
      }
      return firstCellIndex + sizeItem.width - 1;
    case "bl":
      if (!sizeItem) {
        return firstCellIndex;
      }
      return firstCellIndex + (sizeItem.height - 1) * cols;
    case "br":
      return cellMap.lastIndexOf(index6);
  }
}
function getGridCellIndices(indices, cellMap) {
  return cellMap.flatMap((index6, cellIndex) => indices.includes(index6) ? [cellIndex] : []);
}
function isListIndexDisabled(listRef, index6, disabledIndices) {
  if (typeof disabledIndices === "function") {
    return disabledIndices(index6);
  } else if (disabledIndices) {
    return disabledIndices.includes(index6);
  }
  const element4 = listRef.current[index6];
  return element4 == null || element4.hasAttribute("disabled") || element4.getAttribute("aria-disabled") === "true";
}

// node_modules/@floating-ui/react/dist/floating-ui.react.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var ReactDOM2 = __toESM(require_react_dom(), 1);

// node_modules/@floating-ui/core/dist/floating-ui.core.mjs
function computeCoordsFromPlacement(_ref, placement, rtl) {
  let {
    reference,
    floating
  } = _ref;
  const sideAxis = getSideAxis(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const alignLength = getAxisLength(alignmentAxis);
  const side = getSide(placement);
  const isVertical = sideAxis === "y";
  const commonX = reference.x + reference.width / 2 - floating.width / 2;
  const commonY = reference.y + reference.height / 2 - floating.height / 2;
  const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
  let coords;
  switch (side) {
    case "top":
      coords = {
        x: commonX,
        y: reference.y - floating.height
      };
      break;
    case "bottom":
      coords = {
        x: commonX,
        y: reference.y + reference.height
      };
      break;
    case "right":
      coords = {
        x: reference.x + reference.width,
        y: commonY
      };
      break;
    case "left":
      coords = {
        x: reference.x - floating.width,
        y: commonY
      };
      break;
    default:
      coords = {
        x: reference.x,
        y: reference.y
      };
  }
  switch (getAlignment(placement)) {
    case "start":
      coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
      break;
    case "end":
      coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
      break;
  }
  return coords;
}
async function detectOverflow(state, options) {
  var _await$platform$isEle;
  if (options === void 0) {
    options = {};
  }
  const {
    x: x2,
    y: y2,
    platform: platform2,
    rects,
    elements,
    strategy
  } = state;
  const {
    boundary = "clippingAncestors",
    rootBoundary = "viewport",
    elementContext = "floating",
    altBoundary = false,
    padding = 0
  } = evaluate(options, state);
  const paddingObject = getPaddingObject(padding);
  const altContext = elementContext === "floating" ? "reference" : "floating";
  const element4 = elements[altBoundary ? altContext : elementContext];
  const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
    element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element4))) != null ? _await$platform$isEle : true) ? element4 : element4.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
    boundary,
    rootBoundary,
    strategy
  }));
  const rect = elementContext === "floating" ? {
    x: x2,
    y: y2,
    width: rects.floating.width,
    height: rects.floating.height
  } : rects.reference;
  const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
  const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  };
  const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
    elements,
    rect,
    offsetParent,
    strategy
  }) : rect);
  return {
    top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
    bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
    left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
    right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
  };
}
var MAX_RESET_COUNT = 50;
var computePosition = async (reference, floating, config) => {
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware = [],
    platform: platform2
  } = config;
  const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
    ...platform2,
    detectOverflow
  };
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
  let rects = await platform2.getElementRects({
    reference,
    floating,
    strategy
  });
  let {
    x: x2,
    y: y2
  } = computeCoordsFromPlacement(rects, placement, rtl);
  let statefulPlacement = placement;
  let resetCount = 0;
  const middlewareData = {};
  for (let i2 = 0; i2 < middleware.length; i2++) {
    const currentMiddleware = middleware[i2];
    if (!currentMiddleware) {
      continue;
    }
    const {
      name: name2,
      fn
    } = currentMiddleware;
    const {
      x: nextX,
      y: nextY,
      data,
      reset: reset2
    } = await fn({
      x: x2,
      y: y2,
      initialPlacement: placement,
      placement: statefulPlacement,
      strategy,
      middlewareData,
      rects,
      platform: platformWithDetectOverflow,
      elements: {
        reference,
        floating
      }
    });
    x2 = nextX != null ? nextX : x2;
    y2 = nextY != null ? nextY : y2;
    middlewareData[name2] = {
      ...middlewareData[name2],
      ...data
    };
    if (reset2 && resetCount < MAX_RESET_COUNT) {
      resetCount++;
      if (typeof reset2 === "object") {
        if (reset2.placement) {
          statefulPlacement = reset2.placement;
        }
        if (reset2.rects) {
          rects = reset2.rects === true ? await platform2.getElementRects({
            reference,
            floating,
            strategy
          }) : reset2.rects;
        }
        ({
          x: x2,
          y: y2
        } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
      }
      i2 = -1;
    }
  }
  return {
    x: x2,
    y: y2,
    placement: statefulPlacement,
    strategy,
    middlewareData
  };
};
function getPlacementList(alignment, autoAlignment, allowedPlacements) {
  const allowedPlacementsSortedByAlignment = alignment ? [...allowedPlacements.filter((placement) => getAlignment(placement) === alignment), ...allowedPlacements.filter((placement) => getAlignment(placement) !== alignment)] : allowedPlacements.filter((placement) => getSide(placement) === placement);
  return allowedPlacementsSortedByAlignment.filter((placement) => {
    if (alignment) {
      return getAlignment(placement) === alignment || (autoAlignment ? getOppositeAlignmentPlacement(placement) !== placement : false);
    }
    return true;
  });
}
var autoPlacement = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "autoPlacement",
    options,
    async fn(state) {
      var _middlewareData$autoP, _middlewareData$autoP2, _placementsThatFitOnE;
      const {
        rects,
        middlewareData,
        placement,
        platform: platform2,
        elements
      } = state;
      const {
        crossAxis = false,
        alignment,
        allowedPlacements = placements,
        autoAlignment = true,
        ...detectOverflowOptions
      } = evaluate(options, state);
      const placements$1 = alignment !== void 0 || allowedPlacements === placements ? getPlacementList(alignment || null, autoAlignment, allowedPlacements) : allowedPlacements;
      const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
      const currentIndex = ((_middlewareData$autoP = middlewareData.autoPlacement) == null ? void 0 : _middlewareData$autoP.index) || 0;
      const currentPlacement = placements$1[currentIndex];
      if (currentPlacement == null) {
        return {};
      }
      const alignmentSides = getAlignmentSides(currentPlacement, rects, await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)));
      if (placement !== currentPlacement) {
        return {
          reset: {
            placement: placements$1[0]
          }
        };
      }
      const currentOverflows = [overflow[getSide(currentPlacement)], overflow[alignmentSides[0]], overflow[alignmentSides[1]]];
      const allOverflows = [...((_middlewareData$autoP2 = middlewareData.autoPlacement) == null ? void 0 : _middlewareData$autoP2.overflows) || [], {
        placement: currentPlacement,
        overflows: currentOverflows
      }];
      const nextPlacement = placements$1[currentIndex + 1];
      if (nextPlacement) {
        return {
          data: {
            index: currentIndex + 1,
            overflows: allOverflows
          },
          reset: {
            placement: nextPlacement
          }
        };
      }
      const placementsSortedByMostSpace = allOverflows.map((d2) => {
        const alignment2 = getAlignment(d2.placement);
        return [d2.placement, alignment2 && crossAxis ? (
          // Check along the mainAxis and main crossAxis side.
          d2.overflows.slice(0, 2).reduce((acc, v2) => acc + v2, 0)
        ) : (
          // Check only the mainAxis.
          d2.overflows[0]
        ), d2.overflows];
      }).sort((a2, b2) => a2[1] - b2[1]);
      const placementsThatFitOnEachSide = placementsSortedByMostSpace.filter((d2) => d2[2].slice(
        0,
        // Aligned placements should not check their opposite crossAxis
        // side.
        getAlignment(d2[0]) ? 2 : 3
      ).every((v2) => v2 <= 0));
      const resetPlacement = ((_placementsThatFitOnE = placementsThatFitOnEachSide[0]) == null ? void 0 : _placementsThatFitOnE[0]) || placementsSortedByMostSpace[0][0];
      if (resetPlacement !== placement) {
        return {
          data: {
            index: currentIndex + 1,
            overflows: allOverflows
          },
          reset: {
            placement: resetPlacement
          }
        };
      }
      return {};
    }
  };
};
var flip = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "flip",
    options,
    async fn(state) {
      var _middlewareData$arrow, _middlewareData$flip;
      const {
        placement,
        middlewareData,
        rects,
        initialPlacement,
        platform: platform2,
        elements
      } = state;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = true,
        fallbackPlacements: specifiedFallbackPlacements,
        fallbackStrategy = "bestFit",
        fallbackAxisSideDirection = "none",
        flipAlignment = true,
        ...detectOverflowOptions
      } = evaluate(options, state);
      if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
        return {};
      }
      const side = getSide(placement);
      const initialSideAxis = getSideAxis(initialPlacement);
      const isBasePlacement = getSide(initialPlacement) === initialPlacement;
      const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
      const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
      const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
      if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
        fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
      }
      const placements2 = [initialPlacement, ...fallbackPlacements];
      const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
      const overflows = [];
      let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
      if (checkMainAxis) {
        overflows.push(overflow[side]);
      }
      if (checkCrossAxis) {
        const sides2 = getAlignmentSides(placement, rects, rtl);
        overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
      }
      overflowsData = [...overflowsData, {
        placement,
        overflows
      }];
      if (!overflows.every((side2) => side2 <= 0)) {
        var _middlewareData$flip2, _overflowsData$filter;
        const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
        const nextPlacement = placements2[nextIndex];
        if (nextPlacement) {
          const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
          if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
          // overflows the main axis.
          overflowsData.every((d2) => getSideAxis(d2.placement) === initialSideAxis ? d2.overflows[0] > 0 : true)) {
            return {
              data: {
                index: nextIndex,
                overflows: overflowsData
              },
              reset: {
                placement: nextPlacement
              }
            };
          }
        }
        let resetPlacement = (_overflowsData$filter = overflowsData.filter((d2) => d2.overflows[0] <= 0).sort((a2, b2) => a2.overflows[1] - b2.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
        if (!resetPlacement) {
          switch (fallbackStrategy) {
            case "bestFit": {
              var _overflowsData$filter2;
              const placement2 = (_overflowsData$filter2 = overflowsData.filter((d2) => {
                if (hasFallbackAxisSideDirection) {
                  const currentSideAxis = getSideAxis(d2.placement);
                  return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                  // reading directions favoring greater width.
                  currentSideAxis === "y";
                }
                return true;
              }).map((d2) => [d2.placement, d2.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a2, b2) => a2[1] - b2[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
              if (placement2) {
                resetPlacement = placement2;
              }
              break;
            }
            case "initialPlacement":
              resetPlacement = initialPlacement;
              break;
          }
        }
        if (placement !== resetPlacement) {
          return {
            reset: {
              placement: resetPlacement
            }
          };
        }
      }
      return {};
    }
  };
};
var originSides = /* @__PURE__ */ new Set(["left", "top"]);
async function convertValueToCoords(state, options) {
  const {
    placement,
    platform: platform2,
    elements
  } = state;
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
  const side = getSide(placement);
  const alignment = getAlignment(placement);
  const isVertical = getSideAxis(placement) === "y";
  const mainAxisMulti = originSides.has(side) ? -1 : 1;
  const crossAxisMulti = rtl && isVertical ? -1 : 1;
  const rawValue = evaluate(options, state);
  let {
    mainAxis,
    crossAxis,
    alignmentAxis
  } = typeof rawValue === "number" ? {
    mainAxis: rawValue,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: rawValue.mainAxis || 0,
    crossAxis: rawValue.crossAxis || 0,
    alignmentAxis: rawValue.alignmentAxis
  };
  if (alignment && typeof alignmentAxis === "number") {
    crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
  }
  return isVertical ? {
    x: crossAxis * crossAxisMulti,
    y: mainAxis * mainAxisMulti
  } : {
    x: mainAxis * mainAxisMulti,
    y: crossAxis * crossAxisMulti
  };
}
var offset = function(options) {
  if (options === void 0) {
    options = 0;
  }
  return {
    name: "offset",
    options,
    async fn(state) {
      var _middlewareData$offse, _middlewareData$arrow;
      const {
        x: x2,
        y: y2,
        placement,
        middlewareData
      } = state;
      const diffCoords = await convertValueToCoords(state, options);
      if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
        return {};
      }
      return {
        x: x2 + diffCoords.x,
        y: y2 + diffCoords.y,
        data: {
          ...diffCoords,
          placement
        }
      };
    }
  };
};
var shift = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "shift",
    options,
    async fn(state) {
      const {
        x: x2,
        y: y2,
        placement,
        platform: platform2
      } = state;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = false,
        limiter = {
          fn: (_ref) => {
            let {
              x: x3,
              y: y3
            } = _ref;
            return {
              x: x3,
              y: y3
            };
          }
        },
        ...detectOverflowOptions
      } = evaluate(options, state);
      const coords = {
        x: x2,
        y: y2
      };
      const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
      const crossAxis = getSideAxis(getSide(placement));
      const mainAxis = getOppositeAxis(crossAxis);
      let mainAxisCoord = coords[mainAxis];
      let crossAxisCoord = coords[crossAxis];
      if (checkMainAxis) {
        const minSide = mainAxis === "y" ? "top" : "left";
        const maxSide = mainAxis === "y" ? "bottom" : "right";
        const min2 = mainAxisCoord + overflow[minSide];
        const max2 = mainAxisCoord - overflow[maxSide];
        mainAxisCoord = clamp(min2, mainAxisCoord, max2);
      }
      if (checkCrossAxis) {
        const minSide = crossAxis === "y" ? "top" : "left";
        const maxSide = crossAxis === "y" ? "bottom" : "right";
        const min2 = crossAxisCoord + overflow[minSide];
        const max2 = crossAxisCoord - overflow[maxSide];
        crossAxisCoord = clamp(min2, crossAxisCoord, max2);
      }
      const limitedCoords = limiter.fn({
        ...state,
        [mainAxis]: mainAxisCoord,
        [crossAxis]: crossAxisCoord
      });
      return {
        ...limitedCoords,
        data: {
          x: limitedCoords.x - x2,
          y: limitedCoords.y - y2,
          enabled: {
            [mainAxis]: checkMainAxis,
            [crossAxis]: checkCrossAxis
          }
        }
      };
    }
  };
};
var size = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "size",
    options,
    async fn(state) {
      var _state$middlewareData, _state$middlewareData2;
      const {
        placement,
        rects,
        platform: platform2,
        elements
      } = state;
      const {
        apply = () => {
        },
        ...detectOverflowOptions
      } = evaluate(options, state);
      const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
      const side = getSide(placement);
      const alignment = getAlignment(placement);
      const isYAxis = getSideAxis(placement) === "y";
      const {
        width,
        height
      } = rects.floating;
      let heightSide;
      let widthSide;
      if (side === "top" || side === "bottom") {
        heightSide = side;
        widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
      } else {
        widthSide = side;
        heightSide = alignment === "end" ? "top" : "bottom";
      }
      const maximumClippingHeight = height - overflow.top - overflow.bottom;
      const maximumClippingWidth = width - overflow.left - overflow.right;
      const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
      const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
      const noShift = !state.middlewareData.shift;
      let availableHeight = overflowAvailableHeight;
      let availableWidth = overflowAvailableWidth;
      if ((_state$middlewareData = state.middlewareData.shift) != null && _state$middlewareData.enabled.x) {
        availableWidth = maximumClippingWidth;
      }
      if ((_state$middlewareData2 = state.middlewareData.shift) != null && _state$middlewareData2.enabled.y) {
        availableHeight = maximumClippingHeight;
      }
      if (noShift && !alignment) {
        const xMin = max(overflow.left, 0);
        const xMax = max(overflow.right, 0);
        const yMin = max(overflow.top, 0);
        const yMax = max(overflow.bottom, 0);
        if (isYAxis) {
          availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
        } else {
          availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
        }
      }
      await apply({
        ...state,
        availableWidth,
        availableHeight
      });
      const nextDimensions = await platform2.getDimensions(elements.floating);
      if (width !== nextDimensions.width || height !== nextDimensions.height) {
        return {
          reset: {
            rects: true
          }
        };
      }
      return {};
    }
  };
};

// node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
function getCssDimensions(element4) {
  const css = getComputedStyle2(element4);
  let width = parseFloat(css.width) || 0;
  let height = parseFloat(css.height) || 0;
  const hasOffset = isHTMLElement(element4);
  const offsetWidth = hasOffset ? element4.offsetWidth : width;
  const offsetHeight = hasOffset ? element4.offsetHeight : height;
  const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
  if (shouldFallback) {
    width = offsetWidth;
    height = offsetHeight;
  }
  return {
    width,
    height,
    $: shouldFallback
  };
}
function unwrapElement(element4) {
  return !isElement(element4) ? element4.contextElement : element4;
}
function getScale(element4) {
  const domElement = unwrapElement(element4);
  if (!isHTMLElement(domElement)) {
    return createCoords(1);
  }
  const rect = domElement.getBoundingClientRect();
  const {
    width,
    height,
    $: $2
  } = getCssDimensions(domElement);
  let x2 = ($2 ? round(rect.width) : rect.width) / width;
  let y2 = ($2 ? round(rect.height) : rect.height) / height;
  if (!x2 || !Number.isFinite(x2)) {
    x2 = 1;
  }
  if (!y2 || !Number.isFinite(y2)) {
    y2 = 1;
  }
  return {
    x: x2,
    y: y2
  };
}
var noOffsets = createCoords(0);
function getVisualOffsets(element4) {
  const win = getWindow(element4);
  if (!isWebKit() || !win.visualViewport) {
    return noOffsets;
  }
  return {
    x: win.visualViewport.offsetLeft,
    y: win.visualViewport.offsetTop
  };
}
function shouldAddVisualOffsets(element4, isFixed, floatingOffsetParent) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element4)) {
    return false;
  }
  return isFixed;
}
function getBoundingClientRect(element4, includeScale, isFixedStrategy, offsetParent) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  const clientRect = element4.getBoundingClientRect();
  const domElement = unwrapElement(element4);
  let scale = createCoords(1);
  if (includeScale) {
    if (offsetParent) {
      if (isElement(offsetParent)) {
        scale = getScale(offsetParent);
      }
    } else {
      scale = getScale(element4);
    }
  }
  const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
  let x2 = (clientRect.left + visualOffsets.x) / scale.x;
  let y2 = (clientRect.top + visualOffsets.y) / scale.y;
  let width = clientRect.width / scale.x;
  let height = clientRect.height / scale.y;
  if (domElement) {
    const win = getWindow(domElement);
    const offsetWin = offsetParent && isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
    let currentWin = win;
    let currentIFrame = getFrameElement(currentWin);
    while (currentIFrame && offsetParent && offsetWin !== currentWin) {
      const iframeScale = getScale(currentIFrame);
      const iframeRect = currentIFrame.getBoundingClientRect();
      const css = getComputedStyle2(currentIFrame);
      const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
      const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
      x2 *= iframeScale.x;
      y2 *= iframeScale.y;
      width *= iframeScale.x;
      height *= iframeScale.y;
      x2 += left;
      y2 += top;
      currentWin = getWindow(currentIFrame);
      currentIFrame = getFrameElement(currentWin);
    }
  }
  return rectToClientRect({
    width,
    height,
    x: x2,
    y: y2
  });
}
function getWindowScrollBarX(element4, rect) {
  const leftScroll = getNodeScroll(element4).scrollLeft;
  if (!rect) {
    return getBoundingClientRect(getDocumentElement(element4)).left + leftScroll;
  }
  return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll) {
  const htmlRect = documentElement.getBoundingClientRect();
  const x2 = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
  const y2 = htmlRect.top + scroll.scrollTop;
  return {
    x: x2,
    y: y2
  };
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
  let {
    elements,
    rect,
    offsetParent,
    strategy
  } = _ref;
  const isFixed = strategy === "fixed";
  const documentElement = getDocumentElement(offsetParent);
  const topLayer = elements ? isTopLayer(elements.floating) : false;
  if (offsetParent === documentElement || topLayer && isFixed) {
    return rect;
  }
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  let scale = createCoords(1);
  const offsets = createCoords(0);
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isOffsetParentAnElement) {
      const offsetRect = getBoundingClientRect(offsetParent);
      scale = getScale(offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    }
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
  return {
    width: rect.width * scale.x,
    height: rect.height * scale.y,
    x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
    y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
  };
}
function getClientRects(element4) {
  return Array.from(element4.getClientRects());
}
function getDocumentRect(element4) {
  const html5 = getDocumentElement(element4);
  const scroll = getNodeScroll(element4);
  const body = element4.ownerDocument.body;
  const width = max(html5.scrollWidth, html5.clientWidth, body.scrollWidth, body.clientWidth);
  const height = max(html5.scrollHeight, html5.clientHeight, body.scrollHeight, body.clientHeight);
  let x2 = -scroll.scrollLeft + getWindowScrollBarX(element4);
  const y2 = -scroll.scrollTop;
  if (getComputedStyle2(body).direction === "rtl") {
    x2 += max(html5.clientWidth, body.clientWidth) - width;
  }
  return {
    width,
    height,
    x: x2,
    y: y2
  };
}
var SCROLLBAR_MAX = 25;
function getViewportRect(element4, strategy) {
  const win = getWindow(element4);
  const html5 = getDocumentElement(element4);
  const visualViewport = win.visualViewport;
  let width = html5.clientWidth;
  let height = html5.clientHeight;
  let x2 = 0;
  let y2 = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    const visualViewportBased = isWebKit();
    if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
      x2 = visualViewport.offsetLeft;
      y2 = visualViewport.offsetTop;
    }
  }
  const windowScrollbarX = getWindowScrollBarX(html5);
  if (windowScrollbarX <= 0) {
    const doc = html5.ownerDocument;
    const body = doc.body;
    const bodyStyles = getComputedStyle(body);
    const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
    const clippingStableScrollbarWidth = Math.abs(html5.clientWidth - body.clientWidth - bodyMarginInline);
    if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) {
      width -= clippingStableScrollbarWidth;
    }
  } else if (windowScrollbarX <= SCROLLBAR_MAX) {
    width += windowScrollbarX;
  }
  return {
    width,
    height,
    x: x2,
    y: y2
  };
}
function getInnerBoundingClientRect(element4, strategy) {
  const clientRect = getBoundingClientRect(element4, true, strategy === "fixed");
  const top = clientRect.top + element4.clientTop;
  const left = clientRect.left + element4.clientLeft;
  const scale = isHTMLElement(element4) ? getScale(element4) : createCoords(1);
  const width = element4.clientWidth * scale.x;
  const height = element4.clientHeight * scale.y;
  const x2 = left * scale.x;
  const y2 = top * scale.y;
  return {
    width,
    height,
    x: x2,
    y: y2
  };
}
function getClientRectFromClippingAncestor(element4, clippingAncestor, strategy) {
  let rect;
  if (clippingAncestor === "viewport") {
    rect = getViewportRect(element4, strategy);
  } else if (clippingAncestor === "document") {
    rect = getDocumentRect(getDocumentElement(element4));
  } else if (isElement(clippingAncestor)) {
    rect = getInnerBoundingClientRect(clippingAncestor, strategy);
  } else {
    const visualOffsets = getVisualOffsets(element4);
    rect = {
      x: clippingAncestor.x - visualOffsets.x,
      y: clippingAncestor.y - visualOffsets.y,
      width: clippingAncestor.width,
      height: clippingAncestor.height
    };
  }
  return rectToClientRect(rect);
}
function hasFixedPositionAncestor(element4, stopNode) {
  const parentNode = getParentNode(element4);
  if (parentNode === stopNode || !isElement(parentNode) || isLastTraversableNode(parentNode)) {
    return false;
  }
  return getComputedStyle2(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
}
function getClippingElementAncestors(element4, cache) {
  const cachedResult = cache.get(element4);
  if (cachedResult) {
    return cachedResult;
  }
  let result = getOverflowAncestors(element4, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
  let currentContainingBlockComputedStyle = null;
  const elementIsFixed = getComputedStyle2(element4).position === "fixed";
  let currentNode = elementIsFixed ? getParentNode(element4) : element4;
  while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
    const computedStyle = getComputedStyle2(currentNode);
    const currentNodeIsContaining = isContainingBlock(currentNode);
    if (!currentNodeIsContaining && computedStyle.position === "fixed") {
      currentContainingBlockComputedStyle = null;
    }
    const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && (currentContainingBlockComputedStyle.position === "absolute" || currentContainingBlockComputedStyle.position === "fixed") || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element4, currentNode);
    if (shouldDropCurrentNode) {
      result = result.filter((ancestor) => ancestor !== currentNode);
    } else {
      currentContainingBlockComputedStyle = computedStyle;
    }
    currentNode = getParentNode(currentNode);
  }
  cache.set(element4, result);
  return result;
}
function getClippingRect(_ref) {
  let {
    element: element4,
    boundary,
    rootBoundary,
    strategy
  } = _ref;
  const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element4) ? [] : getClippingElementAncestors(element4, this._c) : [].concat(boundary);
  const clippingAncestors = [...elementClippingAncestors, rootBoundary];
  const firstRect = getClientRectFromClippingAncestor(element4, clippingAncestors[0], strategy);
  let top = firstRect.top;
  let right = firstRect.right;
  let bottom = firstRect.bottom;
  let left = firstRect.left;
  for (let i2 = 1; i2 < clippingAncestors.length; i2++) {
    const rect = getClientRectFromClippingAncestor(element4, clippingAncestors[i2], strategy);
    top = max(rect.top, top);
    right = min(rect.right, right);
    bottom = min(rect.bottom, bottom);
    left = max(rect.left, left);
  }
  return {
    width: right - left,
    height: bottom - top,
    x: left,
    y: top
  };
}
function getDimensions(element4) {
  const {
    width,
    height
  } = getCssDimensions(element4);
  return {
    width,
    height
  };
}
function getRectRelativeToOffsetParent(element4, offsetParent, strategy) {
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  const documentElement = getDocumentElement(offsetParent);
  const isFixed = strategy === "fixed";
  const rect = getBoundingClientRect(element4, true, isFixed, offsetParent);
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const offsets = createCoords(0);
  function setLeftRTLScrollbarOffset() {
    offsets.x = getWindowScrollBarX(documentElement);
  }
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isOffsetParentAnElement) {
      const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    } else if (documentElement) {
      setLeftRTLScrollbarOffset();
    }
  }
  if (isFixed && !isOffsetParentAnElement && documentElement) {
    setLeftRTLScrollbarOffset();
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
  const x2 = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
  const y2 = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
  return {
    x: x2,
    y: y2,
    width: rect.width,
    height: rect.height
  };
}
function isStaticPositioned(element4) {
  return getComputedStyle2(element4).position === "static";
}
function getTrueOffsetParent(element4, polyfill) {
  if (!isHTMLElement(element4) || getComputedStyle2(element4).position === "fixed") {
    return null;
  }
  if (polyfill) {
    return polyfill(element4);
  }
  let rawOffsetParent = element4.offsetParent;
  if (getDocumentElement(element4) === rawOffsetParent) {
    rawOffsetParent = rawOffsetParent.ownerDocument.body;
  }
  return rawOffsetParent;
}
function getOffsetParent(element4, polyfill) {
  const win = getWindow(element4);
  if (isTopLayer(element4)) {
    return win;
  }
  if (!isHTMLElement(element4)) {
    let svgOffsetParent = getParentNode(element4);
    while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
      if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
        return svgOffsetParent;
      }
      svgOffsetParent = getParentNode(svgOffsetParent);
    }
    return win;
  }
  let offsetParent = getTrueOffsetParent(element4, polyfill);
  while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
    offsetParent = getTrueOffsetParent(offsetParent, polyfill);
  }
  if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
    return win;
  }
  return offsetParent || getContainingBlock(element4) || win;
}
var getElementRects = async function(data) {
  const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
  const getDimensionsFn = this.getDimensions;
  const floatingDimensions = await getDimensionsFn(data.floating);
  return {
    reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
    floating: {
      x: 0,
      y: 0,
      width: floatingDimensions.width,
      height: floatingDimensions.height
    }
  };
};
function isRTL(element4) {
  return getComputedStyle2(element4).direction === "rtl";
}
var platform = {
  convertOffsetParentRelativeRectToViewportRelativeRect,
  getDocumentElement,
  getClippingRect,
  getOffsetParent,
  getElementRects,
  getClientRects,
  getDimensions,
  getScale,
  isElement,
  isRTL
};
function rectsAreEqual(a2, b2) {
  return a2.x === b2.x && a2.y === b2.y && a2.width === b2.width && a2.height === b2.height;
}
function observeMove(element4, onMove) {
  let io = null;
  let timeoutId;
  const root4 = getDocumentElement(element4);
  function cleanup2() {
    var _io;
    clearTimeout(timeoutId);
    (_io = io) == null || _io.disconnect();
    io = null;
  }
  function refresh(skip2, threshold) {
    if (skip2 === void 0) {
      skip2 = false;
    }
    if (threshold === void 0) {
      threshold = 1;
    }
    cleanup2();
    const elementRectForRootMargin = element4.getBoundingClientRect();
    const {
      left,
      top,
      width,
      height
    } = elementRectForRootMargin;
    if (!skip2) {
      onMove();
    }
    if (!width || !height) {
      return;
    }
    const insetTop = floor(top);
    const insetRight = floor(root4.clientWidth - (left + width));
    const insetBottom = floor(root4.clientHeight - (top + height));
    const insetLeft = floor(left);
    const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
    const options = {
      rootMargin,
      threshold: max(0, min(1, threshold)) || 1
    };
    let isFirstUpdate = true;
    function handleObserve(entries) {
      const ratio = entries[0].intersectionRatio;
      if (ratio !== threshold) {
        if (!isFirstUpdate) {
          return refresh();
        }
        if (!ratio) {
          timeoutId = setTimeout(() => {
            refresh(false, 1e-7);
          }, 1e3);
        } else {
          refresh(false, ratio);
        }
      }
      if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element4.getBoundingClientRect())) {
        refresh();
      }
      isFirstUpdate = false;
    }
    try {
      io = new IntersectionObserver(handleObserve, {
        ...options,
        // Handle <iframe>s
        root: root4.ownerDocument
      });
    } catch (_e2) {
      io = new IntersectionObserver(handleObserve, options);
    }
    io.observe(element4);
  }
  refresh(true);
  return cleanup2;
}
function autoUpdate(reference, floating, update, options) {
  if (options === void 0) {
    options = {};
  }
  const {
    ancestorScroll = true,
    ancestorResize = true,
    elementResize = typeof ResizeObserver === "function",
    layoutShift = typeof IntersectionObserver === "function",
    animationFrame = false
  } = options;
  const referenceEl = unwrapElement(reference);
  const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
  ancestors.forEach((ancestor) => {
    ancestorScroll && ancestor.addEventListener("scroll", update, {
      passive: true
    });
    ancestorResize && ancestor.addEventListener("resize", update);
  });
  const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
  let reobserveFrame = -1;
  let resizeObserver = null;
  if (elementResize) {
    resizeObserver = new ResizeObserver((_ref) => {
      let [firstEntry] = _ref;
      if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
        resizeObserver.unobserve(floating);
        cancelAnimationFrame(reobserveFrame);
        reobserveFrame = requestAnimationFrame(() => {
          var _resizeObserver;
          (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
        });
      }
      update();
    });
    if (referenceEl && !animationFrame) {
      resizeObserver.observe(referenceEl);
    }
    if (floating) {
      resizeObserver.observe(floating);
    }
  }
  let frameId;
  let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
  if (animationFrame) {
    frameLoop();
  }
  function frameLoop() {
    const nextRefRect = getBoundingClientRect(reference);
    if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
      update();
    }
    prevRefRect = nextRefRect;
    frameId = requestAnimationFrame(frameLoop);
  }
  update();
  return () => {
    var _resizeObserver2;
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.removeEventListener("scroll", update);
      ancestorResize && ancestor.removeEventListener("resize", update);
    });
    cleanupIo == null || cleanupIo();
    (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
    resizeObserver = null;
    if (animationFrame) {
      cancelAnimationFrame(frameId);
    }
  };
}
var offset2 = offset;
var autoPlacement2 = autoPlacement;
var shift2 = shift;
var flip2 = flip;
var size2 = size;
var computePosition2 = (reference, floating, options) => {
  const cache = /* @__PURE__ */ new Map();
  const mergedOptions = {
    platform,
    ...options
  };
  const platformWithCache = {
    ...mergedOptions.platform,
    _c: cache
  };
  return computePosition(reference, floating, {
    ...mergedOptions,
    platform: platformWithCache
  });
};

// node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
var React2 = __toESM(require_react(), 1);
var import_react3 = __toESM(require_react(), 1);
var ReactDOM = __toESM(require_react_dom(), 1);
var isClient2 = typeof document !== "undefined";
var noop3 = function noop4() {
};
var index2 = isClient2 ? import_react3.useLayoutEffect : noop3;
function deepEqual(a2, b2) {
  if (a2 === b2) {
    return true;
  }
  if (typeof a2 !== typeof b2) {
    return false;
  }
  if (typeof a2 === "function" && a2.toString() === b2.toString()) {
    return true;
  }
  let length;
  let i2;
  let keys3;
  if (a2 && b2 && typeof a2 === "object") {
    if (Array.isArray(a2)) {
      length = a2.length;
      if (length !== b2.length) return false;
      for (i2 = length; i2-- !== 0; ) {
        if (!deepEqual(a2[i2], b2[i2])) {
          return false;
        }
      }
      return true;
    }
    keys3 = Object.keys(a2);
    length = keys3.length;
    if (length !== Object.keys(b2).length) {
      return false;
    }
    for (i2 = length; i2-- !== 0; ) {
      if (!{}.hasOwnProperty.call(b2, keys3[i2])) {
        return false;
      }
    }
    for (i2 = length; i2-- !== 0; ) {
      const key = keys3[i2];
      if (key === "_owner" && a2.$$typeof) {
        continue;
      }
      if (!deepEqual(a2[key], b2[key])) {
        return false;
      }
    }
    return true;
  }
  return a2 !== a2 && b2 !== b2;
}
function getDPR(element4) {
  if (typeof window === "undefined") {
    return 1;
  }
  const win = element4.ownerDocument.defaultView || window;
  return win.devicePixelRatio || 1;
}
function roundByDPR(element4, value) {
  const dpr = getDPR(element4);
  return Math.round(value * dpr) / dpr;
}
function useLatestRef(value) {
  const ref = React2.useRef(value);
  index2(() => {
    ref.current = value;
  });
  return ref;
}
function useFloating(options) {
  if (options === void 0) {
    options = {};
  }
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware = [],
    platform: platform2,
    elements: {
      reference: externalReference,
      floating: externalFloating
    } = {},
    transform: transform2 = true,
    whileElementsMounted,
    open
  } = options;
  const [data, setData] = React2.useState({
    x: 0,
    y: 0,
    strategy,
    placement,
    middlewareData: {},
    isPositioned: false
  });
  const [latestMiddleware, setLatestMiddleware] = React2.useState(middleware);
  if (!deepEqual(latestMiddleware, middleware)) {
    setLatestMiddleware(middleware);
  }
  const [_reference, _setReference] = React2.useState(null);
  const [_floating, _setFloating] = React2.useState(null);
  const setReference = React2.useCallback((node2) => {
    if (node2 !== referenceRef.current) {
      referenceRef.current = node2;
      _setReference(node2);
    }
  }, []);
  const setFloating = React2.useCallback((node2) => {
    if (node2 !== floatingRef.current) {
      floatingRef.current = node2;
      _setFloating(node2);
    }
  }, []);
  const referenceEl = externalReference || _reference;
  const floatingEl = externalFloating || _floating;
  const referenceRef = React2.useRef(null);
  const floatingRef = React2.useRef(null);
  const dataRef = React2.useRef(data);
  const hasWhileElementsMounted = whileElementsMounted != null;
  const whileElementsMountedRef = useLatestRef(whileElementsMounted);
  const platformRef = useLatestRef(platform2);
  const openRef = useLatestRef(open);
  const update = React2.useCallback(() => {
    if (!referenceRef.current || !floatingRef.current) {
      return;
    }
    const config = {
      placement,
      strategy,
      middleware: latestMiddleware
    };
    if (platformRef.current) {
      config.platform = platformRef.current;
    }
    computePosition2(referenceRef.current, floatingRef.current, config).then((data2) => {
      const fullData = {
        ...data2,
        // The floating element's position may be recomputed while it's closed
        // but still mounted (such as when transitioning out). To ensure
        // `isPositioned` will be `false` initially on the next open, avoid
        // setting it to `true` when `open === false` (must be specified).
        isPositioned: openRef.current !== false
      };
      if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
        dataRef.current = fullData;
        ReactDOM.flushSync(() => {
          setData(fullData);
        });
      }
    });
  }, [latestMiddleware, placement, strategy, platformRef, openRef]);
  index2(() => {
    if (open === false && dataRef.current.isPositioned) {
      dataRef.current.isPositioned = false;
      setData((data2) => ({
        ...data2,
        isPositioned: false
      }));
    }
  }, [open]);
  const isMountedRef = React2.useRef(false);
  index2(() => {
    isMountedRef.current = true;
    return () => {
      isMountedRef.current = false;
    };
  }, []);
  index2(() => {
    if (referenceEl) referenceRef.current = referenceEl;
    if (floatingEl) floatingRef.current = floatingEl;
    if (referenceEl && floatingEl) {
      if (whileElementsMountedRef.current) {
        return whileElementsMountedRef.current(referenceEl, floatingEl, update);
      }
      update();
    }
  }, [referenceEl, floatingEl, update, whileElementsMountedRef, hasWhileElementsMounted]);
  const refs = React2.useMemo(() => ({
    reference: referenceRef,
    floating: floatingRef,
    setReference,
    setFloating
  }), [setReference, setFloating]);
  const elements = React2.useMemo(() => ({
    reference: referenceEl,
    floating: floatingEl
  }), [referenceEl, floatingEl]);
  const floatingStyles = React2.useMemo(() => {
    const initialStyles = {
      position: strategy,
      left: 0,
      top: 0
    };
    if (!elements.floating) {
      return initialStyles;
    }
    const x2 = roundByDPR(elements.floating, data.x);
    const y2 = roundByDPR(elements.floating, data.y);
    if (transform2) {
      return {
        ...initialStyles,
        transform: "translate(" + x2 + "px, " + y2 + "px)",
        ...getDPR(elements.floating) >= 1.5 && {
          willChange: "transform"
        }
      };
    }
    return {
      position: strategy,
      left: x2,
      top: y2
    };
  }, [strategy, transform2, elements.floating, data.x, data.y]);
  return React2.useMemo(() => ({
    ...data,
    update,
    refs,
    elements,
    floatingStyles
  }), [data, update, refs, elements, floatingStyles]);
}
var offset3 = (options, deps) => {
  const result = offset2(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
var shift3 = (options, deps) => {
  const result = shift2(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
var flip3 = (options, deps) => {
  const result = flip2(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
var size3 = (options, deps) => {
  const result = size2(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
var autoPlacement3 = (options, deps) => {
  const result = autoPlacement2(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};

// node_modules/@floating-ui/react/dist/floating-ui.react.mjs
function useMergeRefs(refs) {
  const cleanupRef = React3.useRef(void 0);
  const refEffect = React3.useCallback((instance2) => {
    const cleanups = refs.map((ref) => {
      if (ref == null) {
        return;
      }
      if (typeof ref === "function") {
        const refCallback = ref;
        const refCleanup = refCallback(instance2);
        return typeof refCleanup === "function" ? refCleanup : () => {
          refCallback(null);
        };
      }
      ref.current = instance2;
      return () => {
        ref.current = null;
      };
    });
    return () => {
      cleanups.forEach((refCleanup) => refCleanup == null ? void 0 : refCleanup());
    };
  }, refs);
  return React3.useMemo(() => {
    if (refs.every((ref) => ref == null)) {
      return null;
    }
    return (value) => {
      if (cleanupRef.current) {
        cleanupRef.current();
        cleanupRef.current = void 0;
      }
      if (value != null) {
        cleanupRef.current = refEffect(value);
      }
    };
  }, refs);
}
function sortByDocumentPosition(a2, b2) {
  const position3 = a2.compareDocumentPosition(b2);
  if (position3 & Node.DOCUMENT_POSITION_FOLLOWING || position3 & Node.DOCUMENT_POSITION_CONTAINED_BY) {
    return -1;
  }
  if (position3 & Node.DOCUMENT_POSITION_PRECEDING || position3 & Node.DOCUMENT_POSITION_CONTAINS) {
    return 1;
  }
  return 0;
}
var FloatingListContext = React3.createContext({
  register: () => {
  },
  unregister: () => {
  },
  map: /* @__PURE__ */ new Map(),
  elementsRef: {
    current: []
  }
});
function FloatingList(props) {
  const {
    children,
    elementsRef,
    labelsRef
  } = props;
  const [nodes, setNodes] = React3.useState(() => /* @__PURE__ */ new Set());
  const register = React3.useCallback((node2) => {
    setNodes((prevSet) => new Set(prevSet).add(node2));
  }, []);
  const unregister = React3.useCallback((node2) => {
    setNodes((prevSet) => {
      const set = new Set(prevSet);
      set.delete(node2);
      return set;
    });
  }, []);
  const map4 = React3.useMemo(() => {
    const newMap = /* @__PURE__ */ new Map();
    const sortedNodes = Array.from(nodes.keys()).sort(sortByDocumentPosition);
    sortedNodes.forEach((node2, index6) => {
      newMap.set(node2, index6);
    });
    return newMap;
  }, [nodes]);
  return (0, import_jsx_runtime.jsx)(FloatingListContext.Provider, {
    value: React3.useMemo(() => ({
      register,
      unregister,
      map: map4,
      elementsRef,
      labelsRef
    }), [register, unregister, map4, elementsRef, labelsRef]),
    children
  });
}
function useListItem(props) {
  if (props === void 0) {
    props = {};
  }
  const {
    label
  } = props;
  const {
    register,
    unregister,
    map: map4,
    elementsRef,
    labelsRef
  } = React3.useContext(FloatingListContext);
  const [index6, setIndex] = React3.useState(null);
  const componentRef = React3.useRef(null);
  const ref = React3.useCallback((node2) => {
    componentRef.current = node2;
    if (index6 !== null) {
      elementsRef.current[index6] = node2;
      if (labelsRef) {
        var _node$textContent;
        const isLabelDefined = label !== void 0;
        labelsRef.current[index6] = isLabelDefined ? label : (_node$textContent = node2 == null ? void 0 : node2.textContent) != null ? _node$textContent : null;
      }
    }
  }, [index6, elementsRef, labelsRef, label]);
  index(() => {
    const node2 = componentRef.current;
    if (node2) {
      register(node2);
      return () => {
        unregister(node2);
      };
    }
  }, [register, unregister]);
  index(() => {
    const index7 = componentRef.current ? map4.get(componentRef.current) : null;
    if (index7 != null) {
      setIndex(index7);
    }
  }, [map4]);
  return React3.useMemo(() => ({
    ref,
    index: index6 == null ? -1 : index6
  }), [index6, ref]);
}
var ARROW_LEFT2 = "ArrowLeft";
var ARROW_RIGHT2 = "ArrowRight";
var ARROW_UP2 = "ArrowUp";
var ARROW_DOWN2 = "ArrowDown";
function renderJsx(render, computedProps) {
  if (typeof render === "function") {
    return render(computedProps);
  }
  if (render) {
    return React3.cloneElement(render, computedProps);
  }
  return (0, import_jsx_runtime.jsx)("div", {
    ...computedProps
  });
}
var CompositeContext = React3.createContext({
  activeIndex: 0,
  onNavigate: () => {
  }
});
var horizontalKeys = [ARROW_LEFT2, ARROW_RIGHT2];
var verticalKeys = [ARROW_UP2, ARROW_DOWN2];
var allKeys = [...horizontalKeys, ...verticalKeys];
var Composite = React3.forwardRef(function Composite2(props, forwardedRef) {
  const {
    render,
    orientation = "both",
    loop = true,
    rtl = false,
    cols = 1,
    disabledIndices,
    activeIndex: externalActiveIndex,
    onNavigate: externalSetActiveIndex,
    itemSizes,
    dense = false,
    ...domProps
  } = props;
  const [internalActiveIndex, internalSetActiveIndex] = React3.useState(0);
  const activeIndex = externalActiveIndex != null ? externalActiveIndex : internalActiveIndex;
  const onNavigate = useEffectEvent(externalSetActiveIndex != null ? externalSetActiveIndex : internalSetActiveIndex);
  const elementsRef = React3.useRef([]);
  const renderElementProps = render && typeof render !== "function" ? render.props : {};
  const contextValue = React3.useMemo(() => ({
    activeIndex,
    onNavigate
  }), [activeIndex, onNavigate]);
  const isGrid = cols > 1;
  function handleKeyDown(event) {
    if (!allKeys.includes(event.key)) return;
    let nextIndex = activeIndex;
    const minIndex = getMinListIndex(elementsRef, disabledIndices);
    const maxIndex = getMaxListIndex(elementsRef, disabledIndices);
    const horizontalEndKey = rtl ? ARROW_LEFT2 : ARROW_RIGHT2;
    const horizontalStartKey = rtl ? ARROW_RIGHT2 : ARROW_LEFT2;
    if (isGrid) {
      const sizes = itemSizes || Array.from({
        length: elementsRef.current.length
      }, () => ({
        width: 1,
        height: 1
      }));
      const cellMap = createGridCellMap(sizes, cols, dense);
      const minGridIndex = cellMap.findIndex((index6) => index6 != null && !isListIndexDisabled(elementsRef, index6, disabledIndices));
      const maxGridIndex = cellMap.reduce((foundIndex, index6, cellIndex) => index6 != null && !isListIndexDisabled(elementsRef, index6, disabledIndices) ? cellIndex : foundIndex, -1);
      const maybeNextIndex = cellMap[getGridNavigatedIndex({
        current: cellMap.map((itemIndex) => itemIndex ? elementsRef.current[itemIndex] : null)
      }, {
        event,
        orientation,
        loop,
        rtl,
        cols,
        // treat undefined (empty grid spaces) as disabled indices so we
        // don't end up in them
        disabledIndices: getGridCellIndices([...(typeof disabledIndices !== "function" ? disabledIndices : null) || elementsRef.current.map((_2, index6) => isListIndexDisabled(elementsRef, index6, disabledIndices) ? index6 : void 0), void 0], cellMap),
        minIndex: minGridIndex,
        maxIndex: maxGridIndex,
        prevIndex: getGridCellIndexOfCorner(
          activeIndex > maxIndex ? minIndex : activeIndex,
          sizes,
          cellMap,
          cols,
          // use a corner matching the edge closest to the direction we're
          // moving in so we don't end up in the same item. Prefer
          // top/left over bottom/right.
          event.key === ARROW_DOWN2 ? "bl" : event.key === horizontalEndKey ? "tr" : "tl"
        )
      })];
      if (maybeNextIndex != null) {
        nextIndex = maybeNextIndex;
      }
    }
    const toEndKeys = {
      horizontal: [horizontalEndKey],
      vertical: [ARROW_DOWN2],
      both: [horizontalEndKey, ARROW_DOWN2]
    }[orientation];
    const toStartKeys = {
      horizontal: [horizontalStartKey],
      vertical: [ARROW_UP2],
      both: [horizontalStartKey, ARROW_UP2]
    }[orientation];
    const preventedKeys = isGrid ? allKeys : {
      horizontal: horizontalKeys,
      vertical: verticalKeys,
      both: allKeys
    }[orientation];
    if (nextIndex === activeIndex && [...toEndKeys, ...toStartKeys].includes(event.key)) {
      if (loop && nextIndex === maxIndex && toEndKeys.includes(event.key)) {
        nextIndex = minIndex;
      } else if (loop && nextIndex === minIndex && toStartKeys.includes(event.key)) {
        nextIndex = maxIndex;
      } else {
        nextIndex = findNonDisabledListIndex(elementsRef, {
          startingIndex: nextIndex,
          decrement: toStartKeys.includes(event.key),
          disabledIndices
        });
      }
    }
    if (nextIndex !== activeIndex && !isIndexOutOfListBounds(elementsRef, nextIndex)) {
      var _elementsRef$current$;
      event.stopPropagation();
      if (preventedKeys.includes(event.key)) {
        event.preventDefault();
      }
      onNavigate(nextIndex);
      (_elementsRef$current$ = elementsRef.current[nextIndex]) == null || _elementsRef$current$.focus();
    }
  }
  const computedProps = {
    ...domProps,
    ...renderElementProps,
    ref: forwardedRef,
    "aria-orientation": orientation === "both" ? void 0 : orientation,
    onKeyDown(e) {
      domProps.onKeyDown == null || domProps.onKeyDown(e);
      renderElementProps.onKeyDown == null || renderElementProps.onKeyDown(e);
      handleKeyDown(e);
    }
  };
  return (0, import_jsx_runtime.jsx)(CompositeContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime.jsx)(FloatingList, {
      elementsRef,
      children: renderJsx(render, computedProps)
    })
  });
});
var CompositeItem = React3.forwardRef(function CompositeItem2(props, forwardedRef) {
  const {
    render,
    ...domProps
  } = props;
  const renderElementProps = render && typeof render !== "function" ? render.props : {};
  const {
    activeIndex,
    onNavigate
  } = React3.useContext(CompositeContext);
  const {
    ref,
    index: index6
  } = useListItem();
  const mergedRef = useMergeRefs([ref, forwardedRef, renderElementProps.ref]);
  const isActive = activeIndex === index6;
  const computedProps = {
    ...domProps,
    ...renderElementProps,
    ref: mergedRef,
    tabIndex: isActive ? 0 : -1,
    "data-active": isActive ? "" : void 0,
    onFocus(e) {
      domProps.onFocus == null || domProps.onFocus(e);
      renderElementProps.onFocus == null || renderElementProps.onFocus(e);
      onNavigate(index6);
    }
  };
  return renderJsx(render, computedProps);
});
var SafeReact2 = {
  ...React3
};
var serverHandoffComplete = false;
var count = 0;
var genId = () => (
  // Ensure the id is unique with multiple independent versions of Floating UI
  // on <React 18
  "floating-ui-" + Math.random().toString(36).slice(2, 6) + count++
);
function useFloatingId() {
  const [id3, setId] = React3.useState(() => serverHandoffComplete ? genId() : void 0);
  index(() => {
    if (id3 == null) {
      setId(genId());
    }
  }, []);
  React3.useEffect(() => {
    serverHandoffComplete = true;
  }, []);
  return id3;
}
var useReactId = SafeReact2.useId;
var useId = useReactId || useFloatingId;
var devMessageSet;
if (true) {
  devMessageSet = /* @__PURE__ */ new Set();
}
function warn() {
  var _devMessageSet;
  for (var _len = arguments.length, messages = new Array(_len), _key = 0; _key < _len; _key++) {
    messages[_key] = arguments[_key];
  }
  const message = "Floating UI: " + messages.join(" ");
  if (!((_devMessageSet = devMessageSet) != null && _devMessageSet.has(message))) {
    var _devMessageSet2;
    (_devMessageSet2 = devMessageSet) == null || _devMessageSet2.add(message);
    console.warn(message);
  }
}
function error() {
  var _devMessageSet3;
  for (var _len2 = arguments.length, messages = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    messages[_key2] = arguments[_key2];
  }
  const message = "Floating UI: " + messages.join(" ");
  if (!((_devMessageSet3 = devMessageSet) != null && _devMessageSet3.has(message))) {
    var _devMessageSet4;
    (_devMessageSet4 = devMessageSet) == null || _devMessageSet4.add(message);
    console.error(message);
  }
}
var FloatingArrow = React3.forwardRef(function FloatingArrow2(props, ref) {
  const {
    context: {
      placement,
      elements: {
        floating
      },
      middlewareData: {
        arrow: arrow4,
        shift: shift4
      }
    },
    width = 14,
    height = 7,
    tipRadius = 0,
    strokeWidth = 0,
    staticOffset,
    stroke,
    d: d2,
    style: {
      transform: transform2,
      ...restStyle
    } = {},
    ...rest
  } = props;
  if (true) {
    if (!ref) {
      warn("The `ref` prop is required for `FloatingArrow`.");
    }
  }
  const clipPathId = useId();
  const [isRTL2, setIsRTL] = React3.useState(false);
  index(() => {
    if (!floating) return;
    const isRTL3 = getComputedStyle2(floating).direction === "rtl";
    if (isRTL3) {
      setIsRTL(true);
    }
  }, [floating]);
  if (!floating) {
    return null;
  }
  const [side, alignment] = placement.split("-");
  const isVerticalSide = side === "top" || side === "bottom";
  let computedStaticOffset = staticOffset;
  if (isVerticalSide && shift4 != null && shift4.x || !isVerticalSide && shift4 != null && shift4.y) {
    computedStaticOffset = null;
  }
  const computedStrokeWidth = strokeWidth * 2;
  const halfStrokeWidth = computedStrokeWidth / 2;
  const svgX = width / 2 * (tipRadius / -8 + 1);
  const svgY = height / 2 * tipRadius / 4;
  const isCustomShape = !!d2;
  const yOffsetProp = computedStaticOffset && alignment === "end" ? "bottom" : "top";
  let xOffsetProp = computedStaticOffset && alignment === "end" ? "right" : "left";
  if (computedStaticOffset && isRTL2) {
    xOffsetProp = alignment === "end" ? "left" : "right";
  }
  const arrowX = (arrow4 == null ? void 0 : arrow4.x) != null ? computedStaticOffset || arrow4.x : "";
  const arrowY = (arrow4 == null ? void 0 : arrow4.y) != null ? computedStaticOffset || arrow4.y : "";
  const dValue = d2 || "M0,0" + (" H" + width) + (" L" + (width - svgX) + "," + (height - svgY)) + (" Q" + width / 2 + "," + height + " " + svgX + "," + (height - svgY)) + " Z";
  const rotation = {
    top: isCustomShape ? "rotate(180deg)" : "",
    left: isCustomShape ? "rotate(90deg)" : "rotate(-90deg)",
    bottom: isCustomShape ? "" : "rotate(180deg)",
    right: isCustomShape ? "rotate(-90deg)" : "rotate(90deg)"
  }[side];
  return (0, import_jsx_runtime.jsxs)("svg", {
    ...rest,
    "aria-hidden": true,
    ref,
    width: isCustomShape ? width : width + computedStrokeWidth,
    height: width,
    viewBox: "0 0 " + width + " " + (height > width ? height : width),
    style: {
      position: "absolute",
      pointerEvents: "none",
      [xOffsetProp]: arrowX,
      [yOffsetProp]: arrowY,
      [side]: isVerticalSide || isCustomShape ? "100%" : "calc(100% - " + computedStrokeWidth / 2 + "px)",
      transform: [rotation, transform2].filter((t2) => !!t2).join(" "),
      ...restStyle
    },
    children: [computedStrokeWidth > 0 && (0, import_jsx_runtime.jsx)("path", {
      clipPath: "url(#" + clipPathId + ")",
      fill: "none",
      stroke,
      strokeWidth: computedStrokeWidth + (d2 ? 0 : 1),
      d: dValue
    }), (0, import_jsx_runtime.jsx)("path", {
      stroke: computedStrokeWidth && !d2 ? rest.fill : "none",
      d: dValue
    }), (0, import_jsx_runtime.jsx)("clipPath", {
      id: clipPathId,
      children: (0, import_jsx_runtime.jsx)("rect", {
        x: -halfStrokeWidth,
        y: halfStrokeWidth * (isCustomShape ? -1 : 1),
        width: width + computedStrokeWidth,
        height: width
      })
    })]
  });
});
function createEventEmitter() {
  const map4 = /* @__PURE__ */ new Map();
  return {
    emit(event, data) {
      var _map$get;
      (_map$get = map4.get(event)) == null || _map$get.forEach((listener) => listener(data));
    },
    on(event, listener) {
      if (!map4.has(event)) {
        map4.set(event, /* @__PURE__ */ new Set());
      }
      map4.get(event).add(listener);
    },
    off(event, listener) {
      var _map$get2;
      (_map$get2 = map4.get(event)) == null || _map$get2.delete(listener);
    }
  };
}
var FloatingNodeContext = React3.createContext(null);
var FloatingTreeContext = React3.createContext(null);
var useFloatingParentNodeId = () => {
  var _React$useContext;
  return ((_React$useContext = React3.useContext(FloatingNodeContext)) == null ? void 0 : _React$useContext.id) || null;
};
var useFloatingTree = () => React3.useContext(FloatingTreeContext);
function createAttribute(name2) {
  return "data-floating-ui-" + name2;
}
var safePolygonIdentifier = createAttribute("safe-polygon");
var NOOP = () => {
};
var FloatingDelayGroupContext = React3.createContext({
  delay: 0,
  initialDelay: 0,
  timeoutMs: 0,
  currentId: null,
  setCurrentId: NOOP,
  setState: NOOP,
  isInstantPhase: false
});
var NextFloatingDelayGroupContext = React3.createContext({
  hasProvider: false,
  timeoutMs: 0,
  delayRef: {
    current: 0
  },
  initialDelayRef: {
    current: 0
  },
  timeoutIdRef: {
    current: -1
  },
  currentIdRef: {
    current: null
  },
  currentContextRef: {
    current: null
  }
});
var HIDDEN_STYLES = {
  border: 0,
  clip: "rect(0 0 0 0)",
  height: "1px",
  margin: "-1px",
  overflow: "hidden",
  padding: 0,
  position: "fixed",
  whiteSpace: "nowrap",
  width: "1px",
  top: 0,
  left: 0
};
var FocusGuard = React3.forwardRef(function FocusGuard2(props, ref) {
  const [role, setRole] = React3.useState();
  index(() => {
    if (isSafari()) {
      setRole("button");
    }
  }, []);
  const restProps = {
    ref,
    tabIndex: 0,
    // Role is only for VoiceOver
    role,
    "aria-hidden": role ? void 0 : true,
    [createAttribute("focus-guard")]: "",
    style: HIDDEN_STYLES
  };
  return (0, import_jsx_runtime.jsx)("span", {
    ...props,
    ...restProps
  });
});
var PortalContext = React3.createContext(null);
var attr = createAttribute("portal");
var VisuallyHiddenDismiss = React3.forwardRef(function VisuallyHiddenDismiss2(props, ref) {
  return (0, import_jsx_runtime.jsx)("button", {
    ...props,
    type: "button",
    ref,
    tabIndex: -1,
    style: HIDDEN_STYLES
  });
});
var lockCount = 0;
var scrollbarProperty = "--floating-ui-scrollbar-width";
function enableScrollLock() {
  const platform2 = getPlatform();
  const isIOS = /iP(hone|ad|od)|iOS/.test(platform2) || // iPads can claim to be MacIntel
  platform2 === "MacIntel" && navigator.maxTouchPoints > 1;
  const bodyStyle = document.body.style;
  const scrollbarX = Math.round(document.documentElement.getBoundingClientRect().left) + document.documentElement.scrollLeft;
  const paddingProp = scrollbarX ? "paddingLeft" : "paddingRight";
  const scrollbarWidth = window.innerWidth - document.documentElement.clientWidth;
  const scrollX = bodyStyle.left ? parseFloat(bodyStyle.left) : window.scrollX;
  const scrollY = bodyStyle.top ? parseFloat(bodyStyle.top) : window.scrollY;
  bodyStyle.overflow = "hidden";
  bodyStyle.setProperty(scrollbarProperty, scrollbarWidth + "px");
  if (scrollbarWidth) {
    bodyStyle[paddingProp] = scrollbarWidth + "px";
  }
  if (isIOS) {
    var _window$visualViewpor, _window$visualViewpor2;
    const offsetLeft = ((_window$visualViewpor = window.visualViewport) == null ? void 0 : _window$visualViewpor.offsetLeft) || 0;
    const offsetTop = ((_window$visualViewpor2 = window.visualViewport) == null ? void 0 : _window$visualViewpor2.offsetTop) || 0;
    Object.assign(bodyStyle, {
      position: "fixed",
      top: -(scrollY - Math.floor(offsetTop)) + "px",
      left: -(scrollX - Math.floor(offsetLeft)) + "px",
      right: "0"
    });
  }
  return () => {
    Object.assign(bodyStyle, {
      overflow: "",
      [paddingProp]: ""
    });
    bodyStyle.removeProperty(scrollbarProperty);
    if (isIOS) {
      Object.assign(bodyStyle, {
        position: "",
        top: "",
        left: "",
        right: ""
      });
      window.scrollTo(scrollX, scrollY);
    }
  };
}
var cleanup = () => {
};
var FloatingOverlay = React3.forwardRef(function FloatingOverlay2(props, ref) {
  const {
    lockScroll = false,
    ...rest
  } = props;
  index(() => {
    if (!lockScroll) return;
    lockCount++;
    if (lockCount === 1) {
      cleanup = enableScrollLock();
    }
    return () => {
      lockCount--;
      if (lockCount === 0) {
        cleanup();
      }
    };
  }, [lockScroll]);
  return (0, import_jsx_runtime.jsx)("div", {
    ref,
    ...rest,
    style: {
      position: "fixed",
      overflow: "auto",
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      ...rest.style
    }
  });
});
function useFloatingRootContext(options) {
  const {
    open = false,
    onOpenChange: onOpenChangeProp,
    elements: elementsProp
  } = options;
  const floatingId = useId();
  const dataRef = React3.useRef({});
  const [events] = React3.useState(() => createEventEmitter());
  const nested = useFloatingParentNodeId() != null;
  if (true) {
    const optionDomReference = elementsProp.reference;
    if (optionDomReference && !isElement(optionDomReference)) {
      error("Cannot pass a virtual element to the `elements.reference` option,", "as it must be a real DOM element. Use `refs.setPositionReference()`", "instead.");
    }
  }
  const [positionReference, setPositionReference] = React3.useState(elementsProp.reference);
  const onOpenChange = useEffectEvent((open2, event, reason) => {
    dataRef.current.openEvent = open2 ? event : void 0;
    events.emit("openchange", {
      open: open2,
      event,
      reason,
      nested
    });
    onOpenChangeProp == null || onOpenChangeProp(open2, event, reason);
  });
  const refs = React3.useMemo(() => ({
    setPositionReference
  }), []);
  const elements = React3.useMemo(() => ({
    reference: positionReference || elementsProp.reference || null,
    floating: elementsProp.floating || null,
    domReference: elementsProp.reference
  }), [positionReference, elementsProp.reference, elementsProp.floating]);
  return React3.useMemo(() => ({
    dataRef,
    open,
    onOpenChange,
    elements,
    events,
    floatingId,
    refs
  }), [open, onOpenChange, elements, events, floatingId, refs]);
}
function useFloating2(options) {
  if (options === void 0) {
    options = {};
  }
  const {
    nodeId
  } = options;
  const internalRootContext = useFloatingRootContext({
    ...options,
    elements: {
      reference: null,
      floating: null,
      ...options.elements
    }
  });
  const rootContext = options.rootContext || internalRootContext;
  const computedElements = rootContext.elements;
  const [_domReference, setDomReference] = React3.useState(null);
  const [positionReference, _setPositionReference] = React3.useState(null);
  const optionDomReference = computedElements == null ? void 0 : computedElements.domReference;
  const domReference = optionDomReference || _domReference;
  const domReferenceRef = React3.useRef(null);
  const tree = useFloatingTree();
  index(() => {
    if (domReference) {
      domReferenceRef.current = domReference;
    }
  }, [domReference]);
  const position3 = useFloating({
    ...options,
    elements: {
      ...computedElements,
      ...positionReference && {
        reference: positionReference
      }
    }
  });
  const setPositionReference = React3.useCallback((node2) => {
    const computedPositionReference = isElement(node2) ? {
      getBoundingClientRect: () => node2.getBoundingClientRect(),
      getClientRects: () => node2.getClientRects(),
      contextElement: node2
    } : node2;
    _setPositionReference(computedPositionReference);
    position3.refs.setReference(computedPositionReference);
  }, [position3.refs]);
  const setReference = React3.useCallback((node2) => {
    if (isElement(node2) || node2 === null) {
      domReferenceRef.current = node2;
      setDomReference(node2);
    }
    if (isElement(position3.refs.reference.current) || position3.refs.reference.current === null || // Don't allow setting virtual elements using the old technique back to
    // `null` to support `positionReference` + an unstable `reference`
    // callback ref.
    node2 !== null && !isElement(node2)) {
      position3.refs.setReference(node2);
    }
  }, [position3.refs]);
  const refs = React3.useMemo(() => ({
    ...position3.refs,
    setReference,
    setPositionReference,
    domReference: domReferenceRef
  }), [position3.refs, setReference, setPositionReference]);
  const elements = React3.useMemo(() => ({
    ...position3.elements,
    domReference
  }), [position3.elements, domReference]);
  const context = React3.useMemo(() => ({
    ...position3,
    ...rootContext,
    refs,
    elements,
    nodeId
  }), [position3, refs, elements, nodeId, rootContext]);
  index(() => {
    rootContext.dataRef.current.floatingContext = context;
    const node2 = tree == null ? void 0 : tree.nodesRef.current.find((node3) => node3.id === nodeId);
    if (node2) {
      node2.context = context;
    }
  });
  return React3.useMemo(() => ({
    ...position3,
    context,
    refs,
    elements
  }), [position3, refs, elements, context]);
}

// node_modules/stream-chat-react/dist/es/WithAudioPlayback.610fdf2c.mjs
var import_lodash = __toESM(require_lodash(), 1);
var import_shim = __toESM(require_shim(), 1);
var import_lodash2 = __toESM(require_lodash2(), 1);
var import_dayjs = __toESM(require_dayjs_min(), 1);
var import_react_fast_compare = __toESM(require_react_fast_compare(), 1);
var import_emoji_regex = __toESM(require_emoji_regex(), 1);
var ChannelActionContext = import_react4.default.createContext(void 0);
var ChannelActionProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(ChannelActionContext.Provider, { value, children });
var useChannelActionContext = (componentName) => {
  const contextValue = (0, import_react4.useContext)(ChannelActionContext);
  if (!contextValue) {
    console.warn(
      `The useChannelActionContext hook was called outside of the ChannelActionContext provider. Make sure this hook is called within a child of the Channel component. The errored call is located in the ${componentName} component.`
    );
    return {};
  }
  return contextValue;
};
var ChannelListContext = (0, import_react4.createContext)(
  void 0
);
var ChannelListContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(ChannelListContext.Provider, { value, children });
var useChannelListContext = () => {
  const contextValue = (0, import_react4.useContext)(ChannelListContext);
  if (!contextValue) {
    return {};
  }
  return contextValue;
};
var ChannelStateContext = import_react4.default.createContext(void 0);
var ChannelStateProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(ChannelStateContext.Provider, { value, children });
var remainingWarningCount = 1;
var useChannelStateContext = (componentName) => {
  const contextValue = (0, import_react4.useContext)(ChannelStateContext);
  if (!contextValue) {
    if (componentName && remainingWarningCount > 0) {
      console.warn(
        `The useChannelStateContext hook was called outside of the ChannelStateContext provider. Make sure this hook is called within a child of the Channel component. The errored call is located in the ${componentName} component.`
      );
      remainingWarningCount -= 1;
    }
    return {};
  }
  return contextValue;
};
var ChatContext = import_react4.default.createContext(void 0);
var ChatProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(ChatContext.Provider, { value, children });
var useChatContext = (componentName) => {
  const contextValue = (0, import_react4.useContext)(ChatContext);
  if (!contextValue) {
    console.warn(
      `The useChatContext hook was called outside of the ChatContext provider. Make sure this hook is called within a child of the Chat component. The errored call is located in the ${componentName} component.`
    );
    return {};
  }
  return contextValue;
};
var ComponentContext = import_react4.default.createContext({});
var ComponentProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(ComponentContext.Provider, { value, children });
var useComponentContext = (_componentName) => (0, import_react4.useContext)(ComponentContext);
var noop5 = () => {
};
function useStateStore(store, selector2) {
  const wrappedSubscription = (0, import_react4.useCallback)(
    (onStoreChange) => {
      const unsubscribe = store?.subscribeWithSelector(selector2, onStoreChange);
      return unsubscribe ?? noop5;
    },
    [store, selector2]
  );
  const wrappedSnapshot = (0, import_react4.useMemo)(() => {
    let cachedTuple;
    return () => {
      const currentValue = store?.getLatestValue();
      if (!currentValue) return void 0;
      if (cachedTuple && cachedTuple[0] === currentValue) {
        return cachedTuple[1];
      }
      const newlySelected = selector2(currentValue);
      if (cachedTuple) {
        let selectededAreEqualToCached = true;
        for (const key in cachedTuple[1]) {
          if (cachedTuple[1][key] === newlySelected[key]) continue;
          selectededAreEqualToCached = false;
          break;
        }
        if (selectededAreEqualToCached) return cachedTuple[1];
      }
      cachedTuple = [currentValue, newlySelected];
      return cachedTuple[1];
    };
  }, [store, selector2]);
  const state = (0, import_shim.useSyncExternalStore)(wrappedSubscription, wrappedSnapshot);
  return state;
}
var MessageComposerContext = (0, import_react4.createContext)(void 0);
var MessageComposerContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(
  MessageComposerContext.Provider,
  {
    value,
    children
  }
);
var useMessageComposerContext = (componentName) => {
  const contextValue = (0, import_react4.useContext)(MessageComposerContext);
  if (!contextValue) {
    return {};
  }
  return contextValue;
};
function getDefaultExportFromCjs(x2) {
  return x2 && x2.__esModule && Object.prototype.hasOwnProperty.call(x2, "default") ? x2["default"] : x2;
}
var calendar$2 = { exports: {} };
var calendar$1 = calendar$2.exports;
var hasRequiredCalendar;
function requireCalendar() {
  if (hasRequiredCalendar) return calendar$2.exports;
  hasRequiredCalendar = 1;
  (function(module, exports$1) {
    !(function(e, t2) {
      module.exports = t2();
    })(calendar$1, function() {
      return function(e, t2, a2) {
        var n2 = "h:mm A", d2 = { lastDay: "[Yesterday at] " + n2, sameDay: "[Today at] " + n2, nextDay: "[Tomorrow at] " + n2, nextWeek: "dddd [at] " + n2, lastWeek: "[Last] dddd [at] " + n2, sameElse: "MM/DD/YYYY" };
        t2.prototype.calendar = function(e2, t22) {
          var n22 = t22 || this.$locale().calendar || d2, s2 = a2(e2 || void 0).startOf("d"), o2 = this.diff(s2, "d", true), i2 = o2 < -6 ? "sameElse" : o2 < -1 ? "lastWeek" : o2 < 0 ? "lastDay" : o2 < 1 ? "sameDay" : o2 < 2 ? "nextDay" : o2 < 7 ? "nextWeek" : "sameElse", f2 = n22[i2] || d2[i2];
          return "function" == typeof f2 ? f2.call(this, a2()) : this.format(f2);
        };
      };
    });
  })(calendar$2);
  return calendar$2.exports;
}
var calendarExports = requireCalendar();
var calendar = getDefaultExportFromCjs(calendarExports);
var localizedFormat$1 = { exports: {} };
var localizedFormat = localizedFormat$1.exports;
var hasRequiredLocalizedFormat;
function requireLocalizedFormat() {
  if (hasRequiredLocalizedFormat) return localizedFormat$1.exports;
  hasRequiredLocalizedFormat = 1;
  (function(module, exports$1) {
    !(function(e, t2) {
      module.exports = t2();
    })(localizedFormat, function() {
      var e = { LTS: "h:mm:ss A", LT: "h:mm A", L: "MM/DD/YYYY", LL: "MMMM D, YYYY", LLL: "MMMM D, YYYY h:mm A", LLLL: "dddd, MMMM D, YYYY h:mm A" };
      return function(t2, n2, o2) {
        var r3 = n2.prototype, M2 = r3.format;
        o2.en.formats = e, r3.format = function(t22) {
          void 0 === t22 && (t22 = "YYYY-MM-DDTHH:mm:ssZ");
          var n22 = this.$locale().formats, o22 = (function(t3, n3) {
            return t3.replace(/(\[[^\]]+])|(LTS?|l{1,4}|L{1,4})/g, function(t4, o3, r22) {
              var M22 = r22 && r22.toUpperCase();
              return o3 || n3[r22] || e[r22] || n3[M22].replace(/(\[[^\]]+])|(MMMM|MM|DD|dddd)/g, function(e2, t5, n4) {
                return t5 || n4.slice(1);
              });
            });
          })(t22, void 0 === n22 ? {} : n22);
          return M2.call(this, o22);
        };
      };
    });
  })(localizedFormat$1);
  return localizedFormat$1.exports;
}
var localizedFormatExports = requireLocalizedFormat();
var LocalizedFormat = getDefaultExportFromCjs(localizedFormatExports);
var isNumberOrString = (output) => typeof output === "string" || typeof output === "number";
var isDayOrMoment = (output) => !!output?.isSame;
var isDate = (output) => output !== null && typeof output === "object" && typeof output.getTime === "function";
var DEFAULT_RELATIVE_COMPACT_MAX_DAYS = 6;
var DEFAULT_RELATIVE_COMPACT_MAX_WEEKS = 3;
function getRelativeCompactDateString(messageCreatedAt, t2, tDateTimeParser, maxDays = DEFAULT_RELATIVE_COMPACT_MAX_DAYS, maxWeeks = DEFAULT_RELATIVE_COMPACT_MAX_WEEKS) {
  const then = tDateTimeParser(messageCreatedAt);
  if (!isDayOrMoment(then)) return null;
  const now = tDateTimeParser((/* @__PURE__ */ new Date()).toISOString());
  if (!isDayOrMoment(now)) return null;
  const diffDays = now.startOf("day").diff(then.startOf("day"), "day");
  if (diffDays < 0) {
    return then.format("DD/MM/YY");
  }
  if (diffDays === 0) return t2("timestamp/relativeToday");
  if (diffDays === 1) return t2("timestamp/relativeYesterday");
  if (diffDays >= 2 && diffDays <= maxDays)
    return t2("timestamp/relativeDaysAgo", { count: diffDays });
  if (maxWeeks > 0) {
    const maxDaysForWeeks = maxWeeks * 7;
    if (diffDays >= 7 && diffDays <= maxDaysForWeeks) {
      const weeks = Math.ceil(diffDays / 7);
      return t2("timestamp/relativeWeeksAgo", { count: weeks });
    }
  }
  return then.format("DD/MM/YY");
}
function getDateString({
  calendar: calendar2,
  calendarFormats,
  format,
  formatDate,
  messageCreatedAt,
  relativeCompact,
  relativeCompactMaxDays,
  relativeCompactMaxWeeks,
  t: t2,
  tDateTimeParser,
  timestampTranslationKey
}) {
  if (!messageCreatedAt || typeof messageCreatedAt === "string" && !Date.parse(messageCreatedAt)) {
    return null;
  }
  if (typeof formatDate === "function") {
    return formatDate(new Date(messageCreatedAt));
  }
  if (relativeCompact && t2 && tDateTimeParser) {
    const maxDays = typeof relativeCompactMaxDays === "number" ? relativeCompactMaxDays : typeof relativeCompactMaxDays === "string" ? parseInt(relativeCompactMaxDays, 10) : DEFAULT_RELATIVE_COMPACT_MAX_DAYS;
    const maxWeeks = typeof relativeCompactMaxWeeks === "number" ? relativeCompactMaxWeeks : typeof relativeCompactMaxWeeks === "string" ? parseInt(relativeCompactMaxWeeks, 10) : DEFAULT_RELATIVE_COMPACT_MAX_WEEKS;
    const result = getRelativeCompactDateString(
      messageCreatedAt,
      t2,
      tDateTimeParser,
      Number.isNaN(maxDays) ? DEFAULT_RELATIVE_COMPACT_MAX_DAYS : maxDays,
      Number.isNaN(maxWeeks) ? DEFAULT_RELATIVE_COMPACT_MAX_WEEKS : maxWeeks
    );
    if (result) return result;
  }
  if (t2 && timestampTranslationKey) {
    const options = {};
    if (typeof calendar2 !== "undefined" && calendar2 !== null) options.calendar = calendar2;
    if (typeof calendarFormats !== "undefined" && calendarFormats !== null)
      options.calendarFormats = calendarFormats;
    if (typeof format !== "undefined" && format !== null) options.format = format;
    const translatedTimestamp = t2(timestampTranslationKey, {
      ...options,
      timestamp: new Date(messageCreatedAt)
    });
    const translationKeyFound = timestampTranslationKey !== translatedTimestamp;
    if (translationKeyFound) return translatedTimestamp;
  }
  if (!tDateTimeParser) {
    return null;
  }
  const parsedTime = tDateTimeParser(messageCreatedAt);
  if (isDayOrMoment(parsedTime)) {
    return calendar2 && parsedTime.calendar ? parsedTime.calendar(void 0, calendarFormats || void 0) : parsedTime.format(format || void 0);
  }
  if (isDate(parsedTime)) {
    return parsedTime.toDateString();
  }
  if (isNumberOrString(parsedTime)) {
    return parsedTime;
  }
  return null;
}
var predefinedFormatters = {
  durationFormatter: (streamI18n) => (value, _2, { format, withSuffix }) => {
    if (format && import_dayjs.default.isDayjs(streamI18n.DateTimeParser)) {
      return streamI18n.DateTimeParser.duration(value).format(
        format
      );
    }
    return streamI18n.DateTimeParser.duration(value).humanize(!!withSuffix);
  },
  timestampFormatter: (streamI18n) => (value, _2, {
    calendarFormats,
    ...options
  }) => {
    let parsedCalendarFormats;
    try {
      if (!options.calendar) {
        parsedCalendarFormats = {};
      } else if (typeof calendarFormats === "string") {
        parsedCalendarFormats = JSON.parse(calendarFormats);
      } else if (typeof calendarFormats === "object") {
        parsedCalendarFormats = calendarFormats;
      }
    } catch (e) {
      console.error("[TIMESTAMP FORMATTER]", e);
    }
    const result = getDateString({
      ...options,
      calendarFormats: parsedCalendarFormats,
      messageCreatedAt: value,
      t: streamI18n.t,
      tDateTimeParser: streamI18n.tDateTimeParser
    });
    if (!result || typeof result === "number") {
      return JSON.stringify(value);
    }
    return result;
  }
};
var defaultTranslatorFunction = ((key) => key);
var defaultDateTimeParser = (input) => (0, import_dayjs.default)(input);
var isLanguageSupported = (language) => {
  const translations = [
    "de",
    "en",
    "es",
    "fr",
    "hi",
    "it",
    "ja",
    "ko",
    "nl",
    "pt",
    "ru",
    "tr"
  ];
  return translations.some((translation) => language === translation);
};
import_dayjs.default.extend(calendar);
import_dayjs.default.extend(LocalizedFormat);
var TranslationContext = import_react4.default.createContext({
  t: defaultTranslatorFunction,
  tDateTimeParser: defaultDateTimeParser,
  userLanguage: "en"
});
var TranslationProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(TranslationContext.Provider, { value, children });
var useTranslationContext = (componentName) => {
  const contextValue = (0, import_react4.useContext)(TranslationContext);
  if (!contextValue) {
    console.warn(
      `The useTranslationContext hook was called outside of the TranslationContext provider. Make sure this hook is called within a child of the Chat component. The errored call is located in the ${componentName} component.`
    );
    return {};
  }
  return contextValue;
};
var TypingContext = import_react4.default.createContext(
  void 0
);
var TypingProvider = ({
  children,
  value
}) => (0, import_jsx_runtime2.jsx)(TypingContext.Provider, { value, children });
var useTypingContext = (componentName) => {
  const contextValue = (0, import_react4.useContext)(TypingContext);
  if (!contextValue) {
    console.warn(
      `The useTypingContext hook was called outside of the TypingContext provider. Make sure this hook is called within a child of the Channel component. The errored call is located in the ${componentName} component.`
    );
    return {};
  }
  return contextValue;
};
var BaseIcon = ({ className, ...props }) => (0, import_jsx_runtime2.jsx)(
  "svg",
  {
    viewBox: "0 0 20 20",
    xmlns: "http://www.w3.org/2000/svg",
    ...props,
    className: clsx_default("str-chat__icon", className)
  }
);
function toIconClass(name2) {
  return "str-chat__icon--" + name2.replace(/^Icon/, "").replace(/([a-z])([A-Z])/g, "$1-$2").replace(/([A-Za-z])(\d)/g, "$1-$2").replace(/(\d)([A-Za-z])/g, "$1-$2").replace(/_/g, "-").toLowerCase();
}
function createIcon(name2, content3, defaultProps2) {
  const iconClass = toIconClass(name2);
  const Icon = ({ className, ...props }) => (0, import_jsx_runtime2.jsx)(BaseIcon, { ...defaultProps2, ...props, className: clsx_default(iconClass, className), children: content3 });
  Icon.displayName = name2;
  return Icon;
}
var IconLeave = createIcon(
  "IconLeave",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M8.75 3.125H3.75V16.875H8.75M8.75 10H17.5M17.5 10L14.375 6.875M17.5 10L14.375 13.125",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconArrowDown = createIcon(
  "IconArrowDown",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M10 3.125V16.875M10 16.875L4.375 11.25M10 16.875L15.625 11.25",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconArrowDownCircle = createIcon(
  "IconArrowDownCircle",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.5 10.625L10 13.125M10 13.125L12.5 10.625M10 13.125V6.875M17.5 10C17.5 14.1421 14.1421 17.5 10 17.5C5.85786 17.5 2.5 14.1421 2.5 10C2.5 5.85786 5.85786 2.5 10 2.5C14.1421 2.5 17.5 5.85786 17.5 10Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconArrowLeft = createIcon(
  "IconArrowLeft",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M16.875 10H3.125M3.125 10L8.75 4.375M3.125 10L8.75 15.625",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconArrowUpRight = createIcon(
  "IconArrowUpRight",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M5 15L15 5M15 5H6.875M15 5V13.125",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconRetry = createIcon(
  "IconRetry",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M14.3751 8.12481H18.1251M18.1251 8.12481V4.37481M18.1251 8.12481L14.8618 5.13809C13.9063 4.18263 12.6904 3.52991 11.3661 3.26151C10.0418 2.9931 8.66771 3.12091 7.41561 3.62896C6.1635 4.137 5.08887 5.00276 4.32599 6.11806C3.5631 7.23336 3.1458 8.54875 3.12621 9.89986C3.10662 11.251 3.48562 12.5779 4.21585 13.7148C4.94609 14.8518 5.99517 15.7483 7.23202 16.2925C8.46887 16.8366 9.83865 17.0042 11.1702 16.7743C12.5018 16.5444 13.736 15.9272 14.7188 14.9998",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconRefresh = createIcon(
  "IconRefresh",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M13.125 7.49978H16.875M16.875 7.49978V3.74978M16.875 7.49978L14.6656 5.29041C13.3861 4.01095 11.6538 3.2875 9.84437 3.27697C8.03494 3.26644 6.29431 3.96968 5 5.23416M6.875 12.4998H3.125M3.125 12.4998V16.2498M3.125 12.4998L5.33437 14.7092C6.61388 15.9886 8.34621 16.7121 10.1556 16.7226C11.9651 16.7331 13.7057 16.0299 15 14.7654",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconReply = createIcon(
  "IconReply",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M17.5812 15.525C16.2953 14.1555 12.9195 11.25 8.12265 11.25V15L1.87265 8.75L8.12265 2.5V6.25C12.2476 6.25 17.5359 10.1914 18.1226 15.2766C18.1308 15.3424 18.1177 15.4091 18.0854 15.4671C18.0531 15.525 18.0031 15.5712 17.9428 15.5988C17.8825 15.6265 17.815 15.6343 17.75 15.621C17.685 15.6077 17.6259 15.5741 17.5812 15.525Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconArrowUp = createIcon(
  "IconArrowUp",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M10 16.875V3.125M10 3.125L4.375 8.75M10 3.125L15.625 8.75",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconBell = createIcon(
  "IconBell",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.50015 15C7.50015 15.663 7.76354 16.2989 8.23238 16.7678C8.70122 17.2366 9.33711 17.5 10.0001 17.5C10.6632 17.5 11.2991 17.2366 11.7679 16.7678C12.2368 16.2989 12.5001 15.663 12.5001 15M4.37515 8.125C4.37515 6.63316 4.96778 5.20242 6.02267 4.14752C7.07756 3.09263 8.5083 2.5 10.0001 2.5C11.492 2.5 12.9227 3.09263 13.9776 4.14752C15.0325 5.20242 15.6251 6.63316 15.6251 8.125C15.6251 10.9234 16.2736 13.1719 16.7892 14.0625C16.844 14.1574 16.8728 14.2649 16.8729 14.3745C16.873 14.484 16.8444 14.5916 16.7898 14.6865C16.7352 14.7815 16.6566 14.8604 16.5619 14.9154C16.4672 14.9705 16.3597 14.9996 16.2501 15H3.75015C3.64076 14.9993 3.53345 14.97 3.43896 14.9149C3.34448 14.8597 3.26611 14.7808 3.2117 14.6859C3.15729 14.591 3.12874 14.4835 3.12891 14.3741C3.12907 14.2647 3.15795 14.1572 3.21265 14.0625C3.72749 13.1719 4.37515 10.9227 4.37515 8.125Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconBellOff = createIcon(
  "IconBellOff",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M3.75015 3.12492L10.0001 9.99992C10.0001 9.99992 13.8094 14.1901 16.2501 16.8749M7.50015 14.9999C7.50015 15.663 7.76354 16.2988 8.23238 16.7677C8.70122 17.2365 9.33711 17.4999 10.0001 17.4999C10.6632 17.4999 11.2991 17.2365 11.7679 16.7677C12.2368 16.2988 12.5001 15.663 12.5001 14.9999M7.22515 3.23117C8.0809 2.74583 9.04899 2.49341 10.0328 2.49912C11.0166 2.50482 11.9817 2.76845 12.8317 3.26369C13.6818 3.75893 14.3871 4.46846 14.8773 5.32147C15.3674 6.17447 15.6253 7.14111 15.6251 8.12492C15.6251 10.3984 16.0533 12.3093 16.49 13.4218M14.5455 14.9999H3.75015C3.64076 14.9993 3.53345 14.9699 3.43896 14.9148C3.34448 14.8597 3.26611 14.7807 3.2117 14.6858C3.15729 14.5909 3.12874 14.4834 3.12891 14.374C3.12907 14.2646 3.15795 14.1572 3.21265 14.0624C3.72749 13.1718 4.37515 10.9226 4.37515 8.12492C4.37327 6.97806 4.72371 5.85829 5.37905 4.91711",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconBookmark = createIcon(
  "IconBookmark",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M15 17.5L10 14.375L5 17.5V3.75C5 3.58424 5.06585 3.42527 5.18306 3.30806C5.30027 3.19085 5.45924 3.125 5.625 3.125H14.375C14.5408 3.125 14.6997 3.19085 14.8169 3.30806C14.9342 3.42527 15 3.58424 15 3.75V17.5Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconBookmarkRemove = createIcon(
  "IconBookmarkRemove",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M5 6.49074V17.5L10 14.375L15 17.5V16M5.125 3.125H14.375C14.5408 3.125 14.6997 3.19085 14.8169 3.30806C14.9342 3.42527 15 3.58424 15 3.75V13M5.125 3.125L3.5 1.5M5.125 3.125L15 13M15 13L17.5 15.5",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconMessageBubbles = createIcon(
  "IconMessageBubbles",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M12.8076 6.25836C13.7525 6.30974 14.6692 6.59868 15.4728 7.09845C16.2764 7.59821 16.941 8.29265 17.4049 9.11746C17.8688 9.94227 18.1172 10.8708 18.1269 11.8171C18.1367 12.7634 17.9075 13.6968 17.4607 14.531L18.0982 16.6998C18.1298 16.8075 18.1319 16.9217 18.1041 17.0304C18.0764 17.1392 18.0198 17.2385 17.9405 17.3178C17.8611 17.3972 17.7618 17.4537 17.6531 17.4815C17.5443 17.5092 17.4301 17.5072 17.3224 17.4756L15.156 16.8357C14.4404 17.2184 13.6504 17.4419 12.8404 17.4907C12.0303 17.5396 11.2192 17.4127 10.4628 17.1188C9.70632 16.8249 9.02237 16.3708 8.45781 15.7878C7.89325 15.2049 7.46143 14.5067 7.19195 13.7412M2.53882 10.781C1.90215 9.59158 1.71418 8.21294 2.00918 6.89647C2.30419 5.58 3.06253 4.41341 4.14591 3.60942C5.22929 2.80543 6.56555 2.4176 7.91103 2.51665C9.2565 2.6157 10.5216 3.19503 11.4755 4.149C12.4295 5.10297 13.0088 6.36803 13.1079 7.7135C13.2069 9.05898 12.8191 10.3952 12.0151 11.4786C11.2111 12.562 10.0445 13.3203 8.72806 13.6153C7.41159 13.9104 6.03295 13.7224 4.84351 13.0857L2.67476 13.7232C2.56707 13.7548 2.45285 13.7569 2.3441 13.7291C2.23535 13.7014 2.13608 13.6448 2.05671 13.5655C1.97735 13.4861 1.92082 13.3868 1.89307 13.2781C1.86531 13.1693 1.86735 13.0551 1.89898 12.9474L2.53882 10.781Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconMessageBubble = createIcon(
  "IconMessageBubble",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M6.24431 16.4932C7.81972 17.405 9.67297 17.7127 11.4585 17.359C13.2441 17.0053 14.84 16.0143 15.9489 14.5708C17.0578 13.1273 17.6038 11.3298 17.4852 9.51341C17.3667 7.69704 16.5916 5.98577 15.3045 4.69866C14.0174 3.41156 12.3061 2.63646 10.4897 2.51789C8.67333 2.39932 6.87582 2.94537 5.43231 4.05422C3.9888 5.16308 2.99781 6.75906 2.64412 8.54461C2.29042 10.3302 2.59815 12.1834 3.50993 13.7588L2.53259 16.6768C2.49586 16.7869 2.49053 16.9051 2.5172 17.0181C2.54386 17.131 2.60146 17.2344 2.68355 17.3165C2.76563 17.3985 2.86895 17.4561 2.98194 17.4828C3.09492 17.5095 3.21309 17.5041 3.32321 17.4674L6.24431 16.4932Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconMessageBubbleFill = createIcon(
  "IconMessageBubbleFill",
  (0, import_jsx_runtime2.jsx)("path", { d: "M18.125 10.0001C18.1253 11.4028 17.7624 12.7818 17.0717 14.0027C16.381 15.2236 15.3859 16.2449 14.1834 16.9671C12.9808 17.6894 11.6118 18.088 10.2095 18.1242C8.80719 18.1603 7.41942 17.8328 6.18125 17.1735L3.52109 18.0602C3.30085 18.1337 3.0645 18.1444 2.83854 18.091C2.61257 18.0377 2.40593 17.9225 2.24176 17.7583C2.07759 17.5942 1.96239 17.3875 1.90906 17.1615C1.85573 16.9356 1.86639 16.6992 1.93984 16.479L2.82656 13.8188C2.24699 12.7292 1.92328 11.5218 1.88 10.2883C1.83672 9.0549 2.075 7.8278 2.57677 6.70019C3.07854 5.57258 3.8306 4.57411 4.77587 3.78055C5.72114 2.98699 6.83478 2.41921 8.03224 2.1203C9.22971 1.82139 10.4795 1.79922 11.6868 2.05545C12.8942 2.31169 14.0272 2.8396 15.0001 3.59912C15.9729 4.35865 16.7599 5.32981 17.3014 6.43891C17.8428 7.548 18.1245 8.76588 18.125 10.0001Z" })
);
var IconThread = createIcon(
  "IconThread",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.5 8.75H12.8125M7.5 11.25H12.8125M10.3125 16.875H3.75C3.58424 16.875 3.42527 16.8092 3.30806 16.6919C3.19085 16.5747 3.125 16.4158 3.125 16.25V9.6875C3.125 7.78126 3.88225 5.95309 5.23017 4.60517C6.57809 3.25725 8.40626 2.5 10.3125 2.5C11.2564 2.5 12.191 2.68591 13.063 3.04712C13.9351 3.40832 14.7274 3.93775 15.3948 4.60517C16.0623 5.27259 16.5917 6.06493 16.9529 6.93696C17.3141 7.80899 17.5 8.74362 17.5 9.6875C17.5 10.6314 17.3141 11.566 16.9529 12.438C16.5917 13.3101 16.0623 14.1024 15.3948 14.7698C14.7274 15.4373 13.9351 15.9667 13.063 16.3279C12.191 16.6891 11.2564 16.875 10.3125 16.875Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconThreadFill = createIcon(
  "IconThreadFill",
  (0, import_jsx_runtime2.jsx)("path", { d: "M10.3125 1.875C8.24119 1.87727 6.25538 2.70111 4.79074 4.16574C3.32611 5.63038 2.50227 7.61619 2.5 9.6875V16.25C2.5 16.5815 2.6317 16.8995 2.86612 17.1339C3.10054 17.3683 3.41848 17.5 3.75 17.5H10.3125C12.3845 17.5 14.3716 16.6769 15.8368 15.2118C17.3019 13.7466 18.125 11.7595 18.125 9.6875C18.125 7.6155 17.3019 5.62836 15.8368 4.16323C14.3716 2.6981 12.3845 1.875 10.3125 1.875ZM12.8125 11.875H7.5C7.33424 11.875 7.17527 11.8092 7.05806 11.6919C6.94085 11.5747 6.875 11.4158 6.875 11.25C6.875 11.0842 6.94085 10.9253 7.05806 10.8081C7.17527 10.6908 7.33424 10.625 7.5 10.625H12.8125C12.9783 10.625 13.1372 10.6908 13.2544 10.8081C13.3717 10.9253 13.4375 11.0842 13.4375 11.25C13.4375 11.4158 13.3717 11.5747 13.2544 11.6919C13.1372 11.8092 12.9783 11.875 12.8125 11.875ZM12.8125 9.375H7.5C7.33424 9.375 7.17527 9.30915 7.05806 9.19194C6.94085 9.07473 6.875 8.91576 6.875 8.75C6.875 8.58424 6.94085 8.42527 7.05806 8.30806C7.17527 8.19085 7.33424 8.125 7.5 8.125H12.8125C12.9783 8.125 13.1372 8.19085 13.2544 8.30806C13.3717 8.42527 13.4375 8.58424 13.4375 8.75C13.4375 8.91576 13.3717 9.07473 13.2544 9.19194C13.1372 9.30915 12.9783 9.375 12.8125 9.375Z" })
);
var IconNotification = createIcon(
  "IconNotification",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M16.25 10V16.25C16.25 16.4158 16.1842 16.5747 16.0669 16.6919C15.9497 16.8092 15.7908 16.875 15.625 16.875H3.75C3.58424 16.875 3.42527 16.8092 3.30806 16.6919C3.19085 16.5747 3.125 16.4158 3.125 16.25V4.375C3.125 4.20924 3.19085 4.05027 3.30806 3.93306C3.42527 3.81585 3.58424 3.75 3.75 3.75H10M17.5 4.6875C17.5 5.89562 16.5206 6.875 15.3125 6.875C14.1044 6.875 13.125 5.89562 13.125 4.6875C13.125 3.47938 14.1044 2.5 15.3125 2.5C16.5206 2.5 17.5 3.47938 17.5 4.6875Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconCamera = createIcon(
  "IconCamera",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M16.25 16.25H3.75C3.41848 16.25 3.10054 16.1183 2.86612 15.8839C2.6317 15.6495 2.5 15.3315 2.5 15V6.25C2.5 5.91848 2.6317 5.60054 2.86612 5.36612C3.10054 5.1317 3.41848 5 3.75 5H6.25L7.5 3.125H12.5L13.75 5H16.25C16.5815 5 16.8995 5.1317 17.1339 5.36612C17.3683 5.60054 17.5 5.91848 17.5 6.25V15C17.5 15.3315 17.3683 15.6495 17.1339 15.8839C16.8995 16.1183 16.5815 16.25 16.25 16.25Z",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    ),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M10 13.125C11.5533 13.125 12.8125 11.8658 12.8125 10.3125C12.8125 8.7592 11.5533 7.5 10 7.5C8.4467 7.5 7.1875 8.7592 7.1875 10.3125C7.1875 11.8658 8.4467 13.125 10 13.125Z",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    )
  ] })
);
var IconLink = createIcon(
  "IconLink",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.49989 12.4999L12.4999 7.49989M8.74989 5.94598L11.0983 3.60223C11.8035 2.90843 12.7543 2.52139 13.7436 2.52542C14.7328 2.52945 15.6804 2.92422 16.3799 3.62374C17.0795 4.32326 17.4742 5.27086 17.4783 6.26012C17.4823 7.24938 17.0953 8.20016 16.4015 8.90536L14.053 11.2499M5.94598 8.74989L3.60223 11.0983C2.90843 11.8035 2.52139 12.7543 2.52542 13.7436C2.52945 14.7328 2.92422 15.6804 3.62374 16.3799C4.32326 17.0795 5.27086 17.4742 6.26012 17.4783C7.24938 17.4823 8.20016 17.0953 8.90536 16.4015L11.2499 14.053",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconPoll = createIcon(
  "IconPoll",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M3.75 16.25V10.625H7.5M17.5 16.25H2.5M7.5 16.25V6.875H11.875M11.875 16.25V3.125H16.25V16.25",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconCheckmark1Small = createIcon(
  "IconCheckmark1Small",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M3.125 11.25L7.5 15.625L17.5 5.625",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconCheckmark = createIcon(
  "IconCheckmark",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M3.125 11.25L7.5 15.625L17.5 5.625",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconChevronDown = createIcon(
  "IconChevronDown",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M16.25 7.5L10 13.75L3.75 7.5",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconChevronLeft = createIcon(
  "IconChevronLeft",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M12.5 16.25L6.25 10L12.5 3.75",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconChevronRight = createIcon(
  "IconChevronRight",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.5 3.75L13.75 10L7.5 16.25",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconNoSign = createIcon(
  "IconNoSign",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M15.3031 15.3031L4.69688 4.69688M17.5 10C17.5 14.1421 14.1421 17.5 10 17.5C5.85786 17.5 2.5 14.1421 2.5 10C2.5 5.85786 5.85786 2.5 10 2.5C14.1421 2.5 17.5 5.85786 17.5 10Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeMiterlimit: "10",
      strokeWidth: "1.5"
    }
  )
);
var IconMinusCircle = createIcon(
  "IconMinusCircle",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M6.875 10H13.125M17.5 10C17.5 14.1421 14.1421 17.5 10 17.5C5.85786 17.5 2.5 14.1421 2.5 10C2.5 5.85786 5.85786 2.5 10 2.5C14.1421 2.5 17.5 5.85786 17.5 10Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeMiterlimit: "10",
      strokeWidth: "1.5"
    }
  )
);
var IconMinus = createIcon(
  "IconMinus",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M16.875 9.40039C17.2064 9.40039 17.4746 9.66863 17.4746 10C17.4746 10.3314 17.2064 10.5996 16.875 10.5996H3.125C2.79363 10.5996 2.52539 10.3314 2.52539 10C2.52539 9.66863 2.79363 9.40039 3.125 9.40039H16.875Z",
      fill: "currentColor"
    }
  )
);
var IconXCircle = createIcon(
  "IconXCircle",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M12.5 7.5L7.5 12.5M7.5 7.5L12.5 12.5M17.5 10C17.5 14.1421 14.1421 17.5 10 17.5C5.85786 17.5 2.5 14.1421 2.5 10C2.5 5.85786 5.85786 2.5 10 2.5C14.1421 2.5 17.5 5.85786 17.5 10Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconClock = createIcon(
  "IconClock",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M10 5.625V10H14.375M17.5 10C17.5 14.1421 14.1421 17.5 10 17.5C5.85786 17.5 2.5 14.1421 2.5 10C2.5 5.85786 5.85786 2.5 10 2.5C14.1421 2.5 17.5 5.85786 17.5 10Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconQuote = createIcon(
  "IconQuote",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M8.4375 11.25H3.125C2.95924 11.25 2.80027 11.1842 2.68306 11.0669C2.56585 10.9497 2.5 10.7908 2.5 10.625V5.625C2.5 5.45924 2.56585 5.30027 2.68306 5.18306C2.80027 5.06585 2.95924 5 3.125 5H7.8125C7.97826 5 8.13723 5.06585 8.25444 5.18306C8.37165 5.30027 8.4375 5.45924 8.4375 5.625V12.5C8.4375 13.3288 8.10826 14.1237 7.52221 14.7097C6.93616 15.2958 6.1413 15.625 5.3125 15.625M17.5 11.25H12.1875C12.0217 11.25 11.8628 11.1842 11.7456 11.0669C11.6283 10.9497 11.5625 10.7908 11.5625 10.625V5.625C11.5625 5.45924 11.6283 5.30027 11.7456 5.18306C11.8628 5.06585 12.0217 5 12.1875 5H16.875C17.0408 5 17.1997 5.06585 17.3169 5.18306C17.4342 5.30027 17.5 5.45924 17.5 5.625V12.5C17.5 13.3288 17.1708 14.1237 16.5847 14.7097C15.9987 15.2958 15.2038 15.625 14.375 15.625",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconXmark = createIcon(
  "IconXmark",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M15.625 4.375L4.375 15.625M15.625 15.625L4.375 4.375",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "2"
    }
  )
);
var IconXmarkSmall = createIcon(
  "IconXmarkSmall",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M13.5 6.5L6.5 13.5M13.5 13.5L6.5 6.5",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconMore = createIcon(
  "IconMore",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("path", { d: "M10 11.25C10.6904 11.25 11.25 10.6904 11.25 10C11.25 9.30964 10.6904 8.75 10 8.75C9.30964 8.75 8.75 9.30964 8.75 10C8.75 10.6904 9.30964 11.25 10 11.25Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M4.6875 11.25C5.37786 11.25 5.9375 10.6904 5.9375 10C5.9375 9.30964 5.37786 8.75 4.6875 8.75C3.99714 8.75 3.4375 9.30964 3.4375 10C3.4375 10.6904 3.99714 11.25 4.6875 11.25Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M15.3125 11.25C16.0029 11.25 16.5625 10.6904 16.5625 10C16.5625 9.30964 16.0029 8.75 15.3125 8.75C14.6221 8.75 14.0625 9.30964 14.0625 10C14.0625 10.6904 14.6221 11.25 15.3125 11.25Z" })
  ] })
);
var IconReorder = createIcon(
  "IconReorder",
  (0, import_jsx_runtime2.jsx)("path", { d: "M8.875 15.3125C8.875 16.2445 8.11948 17 7.1875 17C6.25552 17 5.5 16.2445 5.5 15.3125C5.5 14.3805 6.25552 13.625 7.1875 13.625C8.11948 13.625 8.875 14.3805 8.875 15.3125ZM14.5 15.3125C14.5 16.2445 13.7445 17 12.8125 17C11.8805 17 11.125 16.2445 11.125 15.3125C11.125 14.3805 11.8805 13.625 12.8125 13.625C13.7445 13.625 14.5 14.3805 14.5 15.3125ZM8.875 10C8.875 10.932 8.11948 11.6875 7.1875 11.6875C6.25552 11.6875 5.5 10.932 5.5 10C5.5 9.06802 6.25552 8.3125 7.1875 8.3125C8.11948 8.3125 8.875 9.06802 8.875 10ZM14.5 10C14.5 10.932 13.7445 11.6875 12.8125 11.6875C11.8805 11.6875 11.125 10.932 11.125 10C11.125 9.06802 11.8805 8.3125 12.8125 8.3125C13.7445 8.3125 14.5 9.06802 14.5 10ZM8.875 4.6875C8.875 5.61948 8.11948 6.375 7.1875 6.375C6.25552 6.375 5.5 5.61948 5.5 4.6875C5.5 3.75552 6.25552 3 7.1875 3C8.11948 3 8.875 3.75552 8.875 4.6875ZM14.5 4.6875C14.5 5.61948 13.7445 6.375 12.8125 6.375C11.8805 6.375 11.125 5.61948 11.125 4.6875C11.125 3.75552 11.8805 3 12.8125 3C13.7445 3 14.5 3.75552 14.5 4.6875Z" })
);
var IconChecks = createIcon(
  "IconChecks",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M1.5 10.5724L4.98387 13.9936L13.1129 6.00977M10.371 13.9936L18.5 6.00977",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconDownload = createIcon(
  "IconDownload",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M2.375 11.25C2.375 10.8358 2.71079 10.5 3.125 10.5C3.53921 10.5 3.875 10.8358 3.875 11.25V15.5H16.125V11.25C16.125 10.8358 16.4608 10.5 16.875 10.5C17.2892 10.5 17.625 10.8358 17.625 11.25V16.25C17.625 16.6642 17.2892 17 16.875 17H3.125C2.71079 17 2.375 16.6642 2.375 16.25V11.25ZM9.25 2.5C9.25 2.08579 9.58579 1.75 10 1.75C10.4142 1.75 10.75 2.08579 10.75 2.5V9.43945L12.5947 7.59473C12.8876 7.30183 13.3624 7.30183 13.6553 7.59473C13.9482 7.88762 13.9482 8.36238 13.6553 8.65527L10.5303 11.7803C10.2374 12.0732 9.76262 12.0732 9.46973 11.7803L6.34473 8.65527C6.05183 8.36238 6.05183 7.88762 6.34473 7.59473C6.63762 7.30183 7.11238 7.30183 7.40527 7.59473L9.25 9.43945V2.5Z",
      fill: "currentColor"
    }
  )
);
var IconEdit = createIcon(
  "IconEdit",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M10.625 5.0001L15 9.3751M7.24141 16.8751H3.75C3.58424 16.8751 3.42527 16.8093 3.30806 16.692C3.19085 16.5748 3.125 16.4159 3.125 16.2501V12.7587C3.12508 12.5932 3.19082 12.4344 3.30781 12.3173L12.9422 2.68291C13.0594 2.56579 13.2183 2.5 13.384 2.5C13.5497 2.5 13.7086 2.56579 13.8258 2.68291L17.3172 6.17198C17.4343 6.28917 17.5001 6.44808 17.5001 6.61377C17.5001 6.77946 17.4343 6.93837 17.3172 7.05557L7.68281 16.6923C7.56569 16.8093 7.40695 16.875 7.24141 16.8751Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconEmoji = createIcon(
  "IconEmoji",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("path", { d: "M16.75 10C16.75 6.27208 13.7279 3.25 10 3.25C6.27208 3.25 3.25 6.27208 3.25 10C3.25 13.7279 6.27208 16.75 10 16.75C13.7279 16.75 16.75 13.7279 16.75 10ZM18.25 10C18.25 14.5563 14.5563 18.25 10 18.25C5.44365 18.25 1.75 14.5563 1.75 10C1.75 5.44365 5.44365 1.75 10 1.75C14.5563 1.75 18.25 5.44365 18.25 10Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M7.1875 9.375C7.70527 9.375 8.125 8.95527 8.125 8.4375C8.125 7.91973 7.70527 7.5 7.1875 7.5C6.66973 7.5 6.25 7.91973 6.25 8.4375C6.25 8.95527 6.66973 9.375 7.1875 9.375Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M12.8125 9.375C13.3303 9.375 13.75 8.95527 13.75 8.4375C13.75 7.91973 13.3303 7.5 12.8125 7.5C12.2947 7.5 11.875 7.91973 11.875 8.4375C11.875 8.95527 12.2947 9.375 12.8125 9.375Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M12.4756 11.499C12.683 11.1407 13.1425 11.0182 13.501 11.2256C13.8593 11.433 13.9818 11.8925 13.7744 12.251C13.0125 13.568 11.6947 14.5 10 14.5C8.30531 14.5 6.98748 13.568 6.22559 12.251C6.01825 11.8925 6.14067 11.433 6.49902 11.2256C6.85749 11.0182 7.31695 11.1407 7.52441 11.499C8.05942 12.424 8.91824 13 10 13C11.0818 13 11.9406 12.424 12.4756 11.499Z" })
  ] })
);
var IconEmojiAdd = createIcon(
  "IconEmojiAdd",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M1.75 10C1.75 5.44365 5.44365 1.75 10 1.75C10.4142 1.75 10.75 2.08579 10.75 2.5C10.75 2.91421 10.4142 3.25 10 3.25C6.27208 3.25 3.25 6.27208 3.25 10C3.25 13.7279 6.27208 16.75 10 16.75C13.7279 16.75 16.75 13.7279 16.75 10C16.75 9.58579 17.0858 9.25 17.5 9.25C17.9142 9.25 18.25 9.58579 18.25 10C18.25 14.5563 14.5563 18.25 10 18.25C5.44365 18.25 1.75 14.5563 1.75 10Z",
        fill: "currentColor"
      }
    ),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M7.1875 9.375C7.70527 9.375 8.125 8.95527 8.125 8.4375C8.125 7.91973 7.70527 7.5 7.1875 7.5C6.66973 7.5 6.25 7.91973 6.25 8.4375C6.25 8.95527 6.66973 9.375 7.1875 9.375Z",
        fill: "currentColor"
      }
    ),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M12.8125 9.375C13.3303 9.375 13.75 8.95527 13.75 8.4375C13.75 7.91973 13.3303 7.5 12.8125 7.5C12.2947 7.5 11.875 7.91973 11.875 8.4375C11.875 8.95527 12.2947 9.375 12.8125 9.375Z",
        fill: "currentColor"
      }
    ),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M12.4756 11.499C12.683 11.1407 13.1425 11.0182 13.501 11.2256C13.8593 11.433 13.9818 11.8925 13.7744 12.251C13.0125 13.568 11.6947 14.5 10 14.5C8.30531 14.5 6.98748 13.568 6.22559 12.251C6.01825 11.8925 6.14067 11.433 6.49902 11.2256C6.85749 11.0182 7.31695 11.1407 7.52441 11.499C8.05942 12.424 8.91824 13 10 13C11.0818 13 11.9406 12.424 12.4756 11.499Z",
        fill: "currentColor"
      }
    ),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M15.083 6.87524V4.91626H13.125C12.7108 4.91626 12.375 4.58047 12.375 4.16626C12.3752 3.7522 12.7109 3.41626 13.125 3.41626H15.083V1.45825C15.083 1.04415 15.4189 0.708427 15.833 0.708252C16.2472 0.708252 16.583 1.04404 16.583 1.45825V3.41626H18.542C18.9559 3.41644 19.2918 3.7523 19.292 4.16626C19.292 4.58036 18.9561 4.91608 18.542 4.91626H16.583V6.87524C16.5828 7.28931 16.2471 7.62524 15.833 7.62524C15.4191 7.62507 15.0832 7.2892 15.083 6.87524Z",
        fill: "currentColor"
      }
    )
  ] })
);
var IconExclamationMarkFill = createIcon(
  "IconExclamationMarkFill",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("path", { d: "M10 16.875C10.6904 16.875 11.25 16.3154 11.25 15.625C11.25 14.9346 10.6904 14.375 10 14.375C9.30964 14.375 8.75 14.9346 8.75 15.625C8.75 16.3154 9.30964 16.875 10 16.875Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M9 11.875V3.75C9 3.19772 9.44772 2.75 10 2.75C10.5523 2.75 11 3.19772 11 3.75V11.875C11 12.4273 10.5523 12.875 10 12.875C9.44772 12.875 9 12.4273 9 11.875Z" })
  ] })
);
var IconExclamationCircleFill = createIcon(
  "IconExclamationCircleFill",
  (0, import_jsx_runtime2.jsx)("path", { d: "M10 1.875C8.39303 1.875 6.82214 2.35152 5.486 3.24431C4.14985 4.1371 3.10844 5.40605 2.49348 6.8907C1.87852 8.37535 1.71762 10.009 2.03112 11.5851C2.34463 13.1612 3.11846 14.6089 4.25476 15.7452C5.39106 16.8815 6.8388 17.6554 8.41489 17.9689C9.99099 18.2824 11.6247 18.1215 13.1093 17.5065C14.594 16.8916 15.8629 15.8502 16.7557 14.514C17.6485 13.1779 18.125 11.607 18.125 10C18.1227 7.84581 17.266 5.78051 15.7427 4.25727C14.2195 2.73403 12.1542 1.87727 10 1.875ZM9.375 6.25C9.375 6.08424 9.44085 5.92527 9.55806 5.80806C9.67527 5.69085 9.83424 5.625 10 5.625C10.1658 5.625 10.3247 5.69085 10.4419 5.80806C10.5592 5.92527 10.625 6.08424 10.625 6.25V10.625C10.625 10.7908 10.5592 10.9497 10.4419 11.0669C10.3247 11.1842 10.1658 11.25 10 11.25C9.83424 11.25 9.67527 11.1842 9.55806 11.0669C9.44085 10.9497 9.375 10.7908 9.375 10.625V6.25ZM10 14.375C9.81458 14.375 9.63333 14.32 9.47916 14.217C9.32499 14.114 9.20482 13.9676 9.13387 13.7963C9.06291 13.625 9.04434 13.4365 9.08052 13.2546C9.11669 13.0727 9.20598 12.9057 9.33709 12.7746C9.4682 12.6435 9.63525 12.5542 9.81711 12.518C9.99896 12.4818 10.1875 12.5004 10.3588 12.5714C10.5301 12.6423 10.6765 12.7625 10.7795 12.9167C10.8825 13.0708 10.9375 13.2521 10.9375 13.4375C10.9375 13.6861 10.8387 13.9246 10.6629 14.1004C10.4871 14.2762 10.2486 14.375 10 14.375Z" })
);
var IconExclamationMark = createIcon(
  "IconExclamationMark",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("path", { d: "M16.75 10C16.75 6.27208 13.7279 3.25 10 3.25C6.27208 3.25 3.25 6.27208 3.25 10C3.25 13.7279 6.27208 16.75 10 16.75C13.7279 16.75 16.75 13.7279 16.75 10ZM18.25 10C18.25 14.5563 14.5563 18.25 10 18.25C5.44365 18.25 1.75 14.5563 1.75 10C1.75 5.44365 5.44365 1.75 10 1.75C14.5563 1.75 18.25 5.44365 18.25 10Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M9.25 10.625V6.25C9.25 5.83579 9.58579 5.5 10 5.5C10.4142 5.5 10.75 5.83579 10.75 6.25V10.625C10.75 11.0392 10.4142 11.375 10 11.375C9.58579 11.375 9.25 11.0392 9.25 10.625Z" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M10 14.375C10.5178 14.375 10.9375 13.9553 10.9375 13.4375C10.9375 12.9197 10.5178 12.5 10 12.5C9.48223 12.5 9.0625 12.9197 9.0625 13.4375C9.0625 13.9553 9.48223 14.375 10 14.375Z" })
  ] })
);
var IconExclamationTriangleFill = createIcon(
  "IconExclamationTriangleFill",
  (0, import_jsx_runtime2.jsx)("path", { d: "M18.4999 14.6946L11.6678 2.82974C11.4971 2.53906 11.2534 2.29803 10.9608 2.13057C10.6682 1.9631 10.337 1.875 9.99986 1.875C9.66275 1.875 9.33149 1.9631 9.03892 2.13057C8.74635 2.29803 8.50262 2.53906 8.33189 2.82974L1.49986 14.6946C1.33559 14.9757 1.24902 15.2955 1.24902 15.6211C1.24902 15.9468 1.33559 16.2665 1.49986 16.5477C1.6684 16.8401 1.91171 17.0825 2.20483 17.2498C2.49795 17.4172 2.83032 17.5036 3.16783 17.5001H16.8319C17.1691 17.5033 17.5012 17.4168 17.794 17.2494C18.0868 17.0821 18.3299 16.8399 18.4983 16.5477C18.6628 16.2667 18.7496 15.947 18.7499 15.6214C18.7502 15.2957 18.6639 14.9759 18.4999 14.6946ZM9.37486 8.12505C9.37486 7.95929 9.44071 7.80032 9.55792 7.68311C9.67513 7.5659 9.8341 7.50005 9.99986 7.50005C10.1656 7.50005 10.3246 7.5659 10.4418 7.68311C10.559 7.80032 10.6249 7.95929 10.6249 8.12505V11.2501C10.6249 11.4158 10.559 11.5748 10.4418 11.692C10.3246 11.8092 10.1656 11.8751 9.99986 11.8751C9.8341 11.8751 9.67513 11.8092 9.55792 11.692C9.44071 11.5748 9.37486 11.4158 9.37486 11.2501V8.12505ZM9.99986 15.0001C9.81444 15.0001 9.63319 14.9451 9.47901 14.8421C9.32484 14.739 9.20468 14.5926 9.13372 14.4213C9.06277 14.25 9.0442 14.0615 9.08038 13.8797C9.11655 13.6978 9.20584 13.5308 9.33695 13.3996C9.46806 13.2685 9.63511 13.1792 9.81696 13.1431C9.99882 13.1069 10.1873 13.1255 10.3586 13.1964C10.5299 13.2674 10.6764 13.3875 10.7794 13.5417C10.8824 13.6959 10.9374 13.8771 10.9374 14.0626C10.9374 14.3112 10.8386 14.5496 10.6628 14.7255C10.487 14.9013 10.2485 15.0001 9.99986 15.0001Z" })
);
var IconEyeFill = createIcon(
  "IconEyeFill",
  (0, import_jsx_runtime2.jsx)("path", { d: "M19.3211 9.74688C19.2937 9.68516 18.632 8.21719 17.1609 6.74609C15.2008 4.78594 12.725 3.75 9.99999 3.75C7.27499 3.75 4.79921 4.78594 2.83905 6.74609C1.36796 8.21719 0.703118 9.6875 0.678899 9.74688C0.643362 9.82681 0.625 9.91331 0.625 10.0008C0.625 10.0883 0.643362 10.1748 0.678899 10.2547C0.706243 10.3164 1.36796 11.7836 2.83905 13.2547C4.79921 15.2141 7.27499 16.25 9.99999 16.25C12.725 16.25 15.2008 15.2141 17.1609 13.2547C18.632 11.7836 19.2937 10.3164 19.3211 10.2547C19.3566 10.1748 19.375 10.0883 19.375 10.0008C19.375 9.91331 19.3566 9.82681 19.3211 9.74688ZM9.99999 13.125C9.38193 13.125 8.77774 12.9417 8.26384 12.5983C7.74993 12.255 7.34939 11.7669 7.11287 11.1959C6.87634 10.6249 6.81446 9.99653 6.93504 9.39034C7.05562 8.78415 7.35324 8.22733 7.79028 7.79029C8.22732 7.35325 8.78414 7.05562 9.39033 6.93505C9.99652 6.81447 10.6249 6.87635 11.1959 7.11288C11.7669 7.3494 12.255 7.74994 12.5983 8.26384C12.9417 8.77775 13.125 9.38193 13.125 10C13.125 10.8288 12.7958 11.6237 12.2097 12.2097C11.6236 12.7958 10.8288 13.125 9.99999 13.125Z" })
);
var IconUnsupportedAttachment = createIcon(
  "IconUnsupportedAttachment",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M4.5 16.75H15.5V7.625H11.875C11.4608 7.625 11.125 7.28921 11.125 6.875V3.25H4.5V16.75ZM11.3447 9.46973C11.6376 9.17683 12.1124 9.17683 12.4053 9.46973C12.6982 9.76262 12.6982 10.2374 12.4053 10.5303L11.0605 11.875L12.4053 13.2197C12.6982 13.5126 12.6982 13.9874 12.4053 14.2803C12.1124 14.5732 11.6376 14.5732 11.3447 14.2803L10 12.9355L8.65527 14.2803C8.36238 14.5732 7.88762 14.5732 7.59473 14.2803C7.30183 13.9874 7.30183 13.5126 7.59473 13.2197L8.93945 11.875L7.59473 10.5303C7.30183 10.2374 7.30183 9.76262 7.59473 9.46973C7.88762 9.17683 8.36238 9.17683 8.65527 9.46973L10 10.8145L11.3447 9.46973ZM12.625 6.125H14.4395L12.625 4.31055V6.125ZM17 16.875C17 17.2397 16.8555 17.5898 16.5977 17.8477C16.3398 18.1055 15.9897 18.25 15.625 18.25H4.375C4.01033 18.25 3.66021 18.1055 3.40234 17.8477C3.14448 17.5898 3 17.2397 3 16.875V3.125C3 2.76033 3.14448 2.41021 3.40234 2.15234L3.50391 2.06152C3.74856 1.86111 4.05597 1.75 4.375 1.75H11.875C12.0739 1.75 12.2646 1.82907 12.4053 1.96973L16.7803 6.34473C16.9209 6.48538 17 6.67609 17 6.875V16.875Z",
      fill: "currentColor"
    }
  )
);
var IconUpload = createIcon(
  "IconUpload",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M11.875 2.5H4.375C4.20924 2.5 4.05027 2.56585 3.93306 2.68306C3.81585 2.80027 3.75 2.95924 3.75 3.125V16.875C3.75 17.0408 3.81585 17.1997 3.93306 17.3169C4.05027 17.4342 4.20924 17.5 4.375 17.5H15.625C15.7908 17.5 15.9497 17.4342 16.0669 17.3169C16.1842 17.1997 16.25 17.0408 16.25 16.875V6.875M11.875 2.5L16.25 6.875M11.875 2.5V6.875H16.25M8.125 11.25L10 9.375M10 9.375L11.875 11.25M10 9.375V14.375",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconFile = createIcon(
  "IconFile",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M11.875 2.5H4.375C4.20924 2.5 4.05027 2.56585 3.93306 2.68306C3.81585 2.80027 3.75 2.95924 3.75 3.125V16.875C3.75 17.0408 3.81585 17.1997 3.93306 17.3169C4.05027 17.4342 4.20924 17.5 4.375 17.5H15.625C15.7908 17.5 15.9497 17.4342 16.0669 17.3169C16.1842 17.1997 16.25 17.0408 16.25 16.875V6.875M11.875 2.5L16.25 6.875M11.875 2.5V6.875H16.25",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconFlag = createIcon(
  "IconFlag",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M3.75 17.5001V4.3751C8.75 0.0446299 12.5 8.70557 17.5 4.3751V13.7501C12.5 18.0806 8.75 9.41963 3.75 13.7501",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconImage = createIcon(
  "IconImage",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M4.42891 16.875L12.9953 8.30781C13.0534 8.2497 13.1223 8.2036 13.1982 8.17215C13.274 8.1407 13.3554 8.12451 13.4375 8.12451C13.5196 8.12451 13.601 8.1407 13.6768 8.17215C13.7527 8.2036 13.8216 8.2497 13.8797 8.30781L16.875 11.3039M3.75 3.125H16.25C16.5952 3.125 16.875 3.40482 16.875 3.75V16.25C16.875 16.5952 16.5952 16.875 16.25 16.875H3.75C3.40482 16.875 3.125 16.5952 3.125 16.25V3.75C3.125 3.40482 3.40482 3.125 3.75 3.125ZM8.75 7.5C8.75 8.19036 8.19036 8.75 7.5 8.75C6.80964 8.75 6.25 8.19036 6.25 7.5C6.25 6.80964 6.80964 6.25 7.5 6.25C8.19036 6.25 8.75 6.80964 8.75 7.5Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconSearch = createIcon(
  "IconSearch",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M13.1695 13.1695L17.5 17.5M15 8.75C15 12.2018 12.2018 15 8.75 15C5.29822 15 2.5 12.2018 2.5 8.75C2.5 5.29822 5.29822 2.5 8.75 2.5C12.2018 2.5 15 5.29822 15 8.75Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconLocation = createIcon(
  "IconLocation",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M10 10.625C11.3807 10.625 12.5 9.50571 12.5 8.125C12.5 6.74429 11.3807 5.625 10 5.625C8.61929 5.625 7.5 6.74429 7.5 8.125C7.5 9.50571 8.61929 10.625 10 10.625Z",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    ),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M16.25 8.125C16.25 13.75 10 18.125 10 18.125C10 18.125 3.75 13.75 3.75 8.125C3.75 6.4674 4.40848 4.87769 5.58058 3.70558C6.75269 2.53348 8.3424 1.875 10 1.875C11.6576 1.875 13.2473 2.53348 14.4194 3.70558C15.5915 4.87769 16.25 6.4674 16.25 8.125Z",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    )
  ] })
);
var IconVoice = createIcon(
  "IconVoice",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M10 15.625V18.75M10 15.625C11.4918 15.625 12.9226 15.0324 13.9775 13.9775C15.0324 12.9226 15.625 11.4918 15.625 10M10 15.625C8.50816 15.625 7.07742 15.0324 6.02252 13.9775C4.96763 12.9226 4.375 11.4918 4.375 10M10 1.875C11.7259 1.875 13.125 3.27411 13.125 5V10C13.125 11.7259 11.7259 13.125 10 13.125C8.27411 13.125 6.875 11.7259 6.875 10V5C6.875 3.27411 8.27411 1.875 10 1.875Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconMicrophoneSolid = createIcon(
  "IconMicrophoneSolid",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M6.25 10V5C6.25 4.00544 6.64509 3.05161 7.34835 2.34835C8.05161 1.64509 9.00544 1.25 10 1.25C10.9946 1.25 11.9484 1.64509 12.6517 2.34835C13.3549 3.05161 13.75 4.00544 13.75 5V10C13.75 10.9946 13.3549 11.9484 12.6517 12.6517C11.9484 13.3549 10.9946 13.75 10 13.75C9.00544 13.75 8.05161 13.3549 7.34835 12.6517C6.64509 11.9484 6.25 10.9946 6.25 10ZM16.25 10C16.25 9.83424 16.1842 9.67527 16.0669 9.55806C15.9497 9.44085 15.7908 9.375 15.625 9.375C15.4592 9.375 15.3003 9.44085 15.1831 9.55806C15.0658 9.67527 15 9.83424 15 10C15 11.3261 14.4732 12.5979 13.5355 13.5355C12.5979 14.4732 11.3261 15 10 15C8.67392 15 7.40215 14.4732 6.46447 13.5355C5.52678 12.5979 5 11.3261 5 10C5 9.83424 4.93415 9.67527 4.81694 9.55806C4.69973 9.44085 4.54076 9.375 4.375 9.375C4.20924 9.375 4.05027 9.44085 3.93306 9.55806C3.81585 9.67527 3.75 9.83424 3.75 10C3.7519 11.5489 4.32806 13.042 5.36707 14.1907C6.40607 15.3393 7.83409 16.062 9.375 16.2188V18.75C9.375 18.9158 9.44085 19.0747 9.55806 19.1919C9.67527 19.3092 9.83424 19.375 10 19.375C10.1658 19.375 10.3247 19.3092 10.4419 19.1919C10.5592 19.0747 10.625 18.9158 10.625 18.75V16.2188C12.1659 16.062 13.5939 15.3393 14.6329 14.1907C15.6719 13.042 16.2481 11.5489 16.25 10Z",
      fill: "currentColor"
    }
  )
);
var IconMute = createIcon(
  "IconMute",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M15.625 8.125V11.875M18.125 6.875V13.125M4.375 3.125L16.875 16.875M9.38672 4.92109L12.5 2.5V8.34609M12.5 12.0625V17.5L6.875 13.125H3.125C2.95924 13.125 2.80027 13.0592 2.68306 12.9419C2.56585 12.8247 2.5 12.6658 2.5 12.5V7.5C2.5 7.33424 2.56585 7.17527 2.68306 7.05806C2.80027 6.94085 2.95924 6.875 3.125 6.875H7.78437",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconSend = createIcon(
  "IconSend",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M11.2498 10H6.24982M6.24982 10L3.7881 2.71328C3.74537 2.59349 3.7402 2.4635 3.7733 2.34069C3.80639 2.21788 3.87616 2.10808 3.9733 2.02597C4.07044 1.94386 4.19032 1.89335 4.31693 1.88117C4.44353 1.86899 4.57084 1.89573 4.68185 1.95781L17.8069 9.4461C17.9045 9.50015 17.986 9.57937 18.0427 9.67555C18.0994 9.77172 18.1293 9.88133 18.1293 9.99297C18.1293 10.1046 18.0994 10.2142 18.0427 10.3104C17.986 10.4066 17.9045 10.4858 17.8069 10.5398L4.68185 18.0469C4.57051 18.1096 4.4426 18.1367 4.31537 18.1245C4.18815 18.1123 4.06772 18.0614 3.97032 17.9787C3.87292 17.8959 3.80323 17.7853 3.77065 17.6617C3.73807 17.5381 3.74416 17.4075 3.7881 17.2875L6.24982 10Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconAttachment = createIcon(
  "IconAttachment",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M12.4999 6.24998L5.9913 12.8664C5.76258 13.102 5.63576 13.4181 5.63823 13.7465C5.6407 14.0748 5.77227 14.389 6.00451 14.6211C6.23674 14.8533 6.55099 14.9847 6.87934 14.987C7.20769 14.9894 7.52376 14.8624 7.75927 14.6336L15.5179 6.76795C15.9868 6.29905 16.2502 5.6631 16.2502 4.99998C16.2502 4.33686 15.9868 3.70091 15.5179 3.23201C15.049 2.76312 14.413 2.49969 13.7499 2.49969C13.0868 2.49969 12.4508 2.76312 11.9819 3.23201L4.22333 11.0984C3.52953 11.8036 3.14249 12.7544 3.14652 13.7437C3.15054 14.7329 3.54531 15.6805 4.24483 16.38C4.94435 17.0796 5.89195 17.4743 6.88121 17.4784C7.87048 17.4824 8.82126 17.0953 9.52645 16.4015L15.9374 9.99998",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconPauseFill = createIcon(
  "IconPauseFill",
  (0, import_jsx_runtime2.jsx)("path", { d: "M16.875 3.75V16.25C16.875 16.5815 16.7433 16.8995 16.5089 17.1339C16.2745 17.3683 15.9565 17.5 15.625 17.5H12.5C12.1685 17.5 11.8505 17.3683 11.6161 17.1339C11.3817 16.8995 11.25 16.5815 11.25 16.25V3.75C11.25 3.41848 11.3817 3.10054 11.6161 2.86612C11.8505 2.6317 12.1685 2.5 12.5 2.5H15.625C15.9565 2.5 16.2745 2.6317 16.5089 2.86612C16.7433 3.10054 16.875 3.41848 16.875 3.75ZM7.5 2.5H4.375C4.04348 2.5 3.72554 2.6317 3.49112 2.86612C3.2567 3.10054 3.125 3.41848 3.125 3.75V16.25C3.125 16.5815 3.2567 16.8995 3.49112 17.1339C3.72554 17.3683 4.04348 17.5 4.375 17.5H7.5C7.83152 17.5 8.14946 17.3683 8.38388 17.1339C8.6183 16.8995 8.75 16.5815 8.75 16.25V3.75C8.75 3.41848 8.6183 3.10054 8.38388 2.86612C8.14946 2.6317 7.83152 2.5 7.5 2.5Z" })
);
var IconUser = createIcon(
  "IconUser",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M10 12.5C12.7614 12.5 15 10.2614 15 7.5C15 4.73858 12.7614 2.5 10 2.5C7.23858 2.5 5 4.73858 5 7.5C5 10.2614 7.23858 12.5 10 12.5ZM10 12.5C6.76172 12.5 4.01328 14.2602 2.5 16.875M10 12.5C13.2383 12.5 15.9867 14.2602 17.5 16.875",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconUserAdd = createIcon(
  "IconUserAdd",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("g", { clipPath: "url(#clip0_14246_498359)", children: (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M15.625 10.625H19.375M17.5 8.75V12.5M8.4375 12.5C11.0263 12.5 13.125 10.4013 13.125 7.8125C13.125 5.22367 11.0263 3.125 8.4375 3.125C5.84867 3.125 3.75 5.22367 3.75 7.8125C3.75 10.4013 5.84867 12.5 8.4375 12.5ZM8.4375 12.5C5.74688 12.5 3.48047 13.7148 1.875 15.625M8.4375 12.5C11.1281 12.5 13.3945 13.7148 15 15.625",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    ) }),
    (0, import_jsx_runtime2.jsx)("defs", { children: (0, import_jsx_runtime2.jsx)("clipPath", { id: "clip0_14246_498359", children: (0, import_jsx_runtime2.jsx)("rect", { fill: "white", height: "20", width: "20" }) }) })
  ] })
);
var IconUserCheck = createIcon(
  "IconUserCheck",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("g", { clipPath: "url(#clip0_14236_425131)", children: (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M8.4375 12.5C11.0263 12.5 13.125 10.4013 13.125 7.8125C13.125 5.22367 11.0263 3.125 8.4375 3.125C5.84867 3.125 3.75 5.22367 3.75 7.8125C3.75 10.4013 5.84867 12.5 8.4375 12.5ZM8.4375 12.5C5.74688 12.5 3.48047 13.7148 1.875 15.625M8.4375 12.5C11.1281 12.5 13.3945 13.7148 15 15.625M15.625 11.25L16.875 12.5L19.375 10",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    ) }),
    (0, import_jsx_runtime2.jsx)("defs", { children: (0, import_jsx_runtime2.jsx)("clipPath", { id: "clip0_14236_425131", children: (0, import_jsx_runtime2.jsx)("rect", { fill: "white", height: "20", width: "20" }) }) })
  ] })
);
var IconUserRemove = createIcon(
  "IconUserRemove",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("g", { clipPath: "url(#clip0_14246_434209)", children: (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M15.625 10.625H19.375M8.4375 12.5C11.0263 12.5 13.125 10.4013 13.125 7.8125C13.125 5.22367 11.0263 3.125 8.4375 3.125C5.84867 3.125 3.75 5.22367 3.75 7.8125C3.75 10.4013 5.84867 12.5 8.4375 12.5ZM8.4375 12.5C5.74688 12.5 3.48047 13.7148 1.875 15.625M8.4375 12.5C11.1281 12.5 13.3945 13.7148 15 15.625",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    ) }),
    (0, import_jsx_runtime2.jsx)("defs", { children: (0, import_jsx_runtime2.jsx)("clipPath", { id: "clip0_14246_434209", children: (0, import_jsx_runtime2.jsx)("rect", { fill: "white", height: "20", width: "20" }) }) })
  ] })
);
var IconPin = createIcon(
  "IconPin",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.52271 12.4773L3.75006 16.25M17.9422 7.68279C18.0594 7.56559 18.1252 7.40668 18.1252 7.24099C18.1252 7.0753 18.0594 6.9164 17.9422 6.7992L13.2032 2.05779C13.086 1.94067 12.9271 1.87488 12.7614 1.87488C12.5957 1.87488 12.4368 1.94067 12.3196 2.05779L7.84303 6.54685C7.84303 6.54685 5.67506 5.46326 3.35943 7.33201C3.29077 7.38697 3.23447 7.45581 3.19424 7.53402C3.154 7.61224 3.13072 7.69806 3.12593 7.78589C3.12114 7.87371 3.13493 7.96156 3.16642 8.04369C3.19791 8.12581 3.24637 8.20037 3.30865 8.26248L11.7383 16.6914C11.8015 16.7541 11.8773 16.8025 11.9606 16.8336C12.044 16.8646 12.133 16.8775 12.2218 16.8713C12.3105 16.8652 12.3969 16.8402 12.4752 16.798C12.5535 16.7558 12.6219 16.6973 12.6758 16.6265C13.3313 15.7547 14.361 13.9633 13.4657 12.1734L17.9422 7.68279Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconPlayFill = createIcon(
  "IconPlayFill",
  (0, import_jsx_runtime2.jsx)("path", { d: "M18.75 10C18.7505 10.2122 18.6961 10.4209 18.5921 10.6059C18.488 10.7908 18.3379 10.9457 18.1562 11.0555L6.9 17.9414C6.71022 18.0576 6.49287 18.1211 6.27037 18.1252C6.04788 18.1293 5.82832 18.0739 5.63438 17.9648C5.44227 17.8574 5.28225 17.7008 5.17075 17.511C5.05926 17.3213 5.00032 17.1052 5 16.8852V3.11484C5.00032 2.89475 5.05926 2.67872 5.17075 2.48896C5.28225 2.2992 5.44227 2.14257 5.63438 2.03516C5.82832 1.92605 6.04788 1.87071 6.27037 1.87483C6.49287 1.87895 6.71022 1.94239 6.9 2.05859L18.1562 8.94453C18.3379 9.05428 18.488 9.20916 18.5921 9.39411C18.6961 9.57906 18.7505 9.78779 18.75 10Z" })
);
var IconPlus = createIcon(
  "IconPlus",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M3.125 10H16.875M10 3.125V16.875",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "2"
    }
  )
);
var IconPlusSmall = createIcon(
  "IconPlusSmall",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M3.125 10H16.875M10 3.125V16.875",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconCommand = createIcon(
  "IconCommand",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M6.25 7.5L9.375 10L6.25 12.5M10.625 12.5H13.75M3.125 3.75H16.875C17.2202 3.75 17.5 4.02982 17.5 4.375V15.625C17.5 15.9702 17.2202 16.25 16.875 16.25H3.125C2.77982 16.25 2.5 15.9702 2.5 15.625V4.375C2.5 4.02982 2.77982 3.75 3.125 3.75Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconCopy = createIcon(
  "IconCopy",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M13.125 13.125H16.875V3.125H6.875V6.875M3.125 6.875H13.125V16.875H3.125V6.875Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconBolt = createIcon(
  "IconBolt",
  (0, import_jsx_runtime2.jsx)("path", { d: "M16.7071 9.80157L7.95711 19.1766C7.86438 19.2755 7.74198 19.3416 7.60839 19.3649C7.47479 19.3882 7.33724 19.3674 7.2165 19.3057C7.09576 19.244 6.99837 19.1446 6.93904 19.0227C6.8797 18.9007 6.86164 18.7628 6.88757 18.6297L8.03289 12.9008L3.53054 11.2102C3.43385 11.174 3.34762 11.1144 3.27956 11.0368C3.2115 10.9592 3.16373 10.8659 3.14052 10.7653C3.1173 10.6647 3.11937 10.5599 3.14653 10.4603C3.17369 10.3607 3.2251 10.2694 3.29617 10.1945L12.0462 0.819538C12.1389 0.720581 12.2613 0.654468 12.3949 0.631176C12.5285 0.607884 12.666 0.628675 12.7868 0.690414C12.9075 0.752152 13.0049 0.851488 13.0642 0.97343C13.1236 1.09537 13.1416 1.2333 13.1157 1.36641L11.9673 7.10157L16.4696 8.78985C16.5656 8.82626 16.6511 8.88576 16.7187 8.96307C16.7862 9.04039 16.8336 9.13315 16.8568 9.23316C16.88 9.33317 16.8781 9.43734 16.8515 9.53648C16.8248 9.63562 16.7742 9.72666 16.704 9.80157H16.7071Z" })
);
var IconTranslate = createIcon(
  "IconTranslate",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M18.75 16.875L14.375 8.125L10 16.875M11.25 14.375H17.5M7.5 2.5V4.375M2.5 4.375H12.5M10 4.375C10 6.36412 9.20982 8.27178 7.8033 9.6783C6.39678 11.0848 4.48912 11.875 2.5 11.875M5.42734 6.875C5.94442 8.33751 6.90226 9.60371 8.16893 10.4992C9.4356 11.3947 10.9488 11.8753 12.5 11.875",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconDelete = createIcon(
  "IconDelete",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M16.875 4.375H3.125M8.125 8.125V13.125M11.875 8.125V13.125M15.625 4.375V16.25C15.625 16.4158 15.5592 16.5747 15.4419 16.6919C15.3247 16.8092 15.1658 16.875 15 16.875H5C4.83424 16.875 4.67527 16.8092 4.55806 16.6919C4.44085 16.5747 4.375 16.4158 4.375 16.25V4.375M13.125 4.375V3.125C13.125 2.79348 12.9933 2.47554 12.7589 2.24112C12.5245 2.0067 12.2065 1.875 11.875 1.875H8.125C7.79348 1.875 7.47554 2.0067 7.24112 2.24112C7.0067 2.47554 6.875 2.79348 6.875 3.125V4.375",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconTrophy = createIcon(
  "IconTrophy",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.5 17.5H12.5M10 14.375V17.5M4.53125 10H3.75C3.08696 10 2.45107 9.73661 1.98223 9.26777C1.51339 8.79893 1.25 8.16304 1.25 7.5V6.25C1.25 6.08424 1.31585 5.92527 1.43306 5.80806C1.55027 5.69085 1.70924 5.625 1.875 5.625H4.375M15.4688 10H16.25C16.913 10 17.5489 9.73661 18.0178 9.26777C18.4866 8.79893 18.75 8.16304 18.75 7.5V6.25C18.75 6.08424 18.6842 5.92527 18.5669 5.80806C18.4497 5.69085 18.2908 5.625 18.125 5.625H15.625M4.375 3.75H15.625V8.67969C15.625 11.7812 13.1445 14.3516 10.043 14.375C9.30068 14.3807 8.5646 14.2394 7.87718 13.9592C7.18975 13.6791 6.56458 13.2656 6.03769 12.7427C5.5108 12.2198 5.09262 11.5978 4.80725 10.9126C4.52188 10.2273 4.37498 9.49231 4.375 8.75V3.75Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconUnpin = createIcon(
  "IconUnpin",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M7.52271 12.4773L3.75006 16.25M3.75006 3.12498L16.2501 16.875M14.4532 11.1828L17.9415 7.68279C18.0586 7.56559 18.1244 7.40668 18.1244 7.24099C18.1244 7.0753 18.0586 6.9164 17.9415 6.7992L13.2032 2.05779C13.086 1.94067 12.9271 1.87488 12.7614 1.87488C12.5957 1.87488 12.4368 1.94067 12.3196 2.05779L9.09615 5.28904M6.58756 6.24998C5.74537 6.18435 4.57271 6.3531 3.35943 7.33201C3.29077 7.38697 3.23447 7.45581 3.19424 7.53402C3.154 7.61224 3.13072 7.69806 3.12593 7.78589C3.12114 7.87371 3.13493 7.96156 3.16642 8.04369C3.19791 8.12581 3.24637 8.20037 3.30865 8.26248L11.7383 16.6914C11.8015 16.7541 11.8773 16.8025 11.9606 16.8336C12.044 16.8646 12.133 16.8775 12.2218 16.8713C12.3105 16.8652 12.3969 16.8402 12.4752 16.798C12.5535 16.7558 12.6219 16.6973 12.6758 16.6265C13.0829 16.0851 13.6344 15.1898 13.779 14.1594",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconVideo = createIcon(
  "IconVideo",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("g", { clipPath: "url(#clip0_14111_491476)", children: (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M15.625 8.75L19.375 6.25V13.75L15.625 11.25M2.5 5H15C15.3452 5 15.625 5.27982 15.625 5.625V14.375C15.625 14.7202 15.3452 15 15 15H2.5C2.15482 15 1.875 14.7202 1.875 14.375V5.625C1.875 5.27982 2.15482 5 2.5 5Z",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        strokeWidth: "1.5"
      }
    ) }),
    (0, import_jsx_runtime2.jsx)("defs", { children: (0, import_jsx_runtime2.jsx)("clipPath", { id: "clip0_14111_491476", children: (0, import_jsx_runtime2.jsx)("rect", { fill: "white", height: "20", width: "20" }) }) })
  ] }),
  { "data-rtl-mirror": "" }
);
var IconVideoFill = createIcon(
  "IconVideoFill",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("g", { clipPath: "url(#clip0_14064_467281)", children: (0, import_jsx_runtime2.jsx)("path", { d: "M15 5.625V14.375C15 14.7065 14.8683 15.0245 14.6339 15.2589C14.3995 15.4933 14.0815 15.625 13.75 15.625H2.5C2.16848 15.625 1.85054 15.4933 1.61612 15.2589C1.3817 15.0245 1.25 14.7065 1.25 14.375V5.625C1.25 5.29348 1.3817 4.97554 1.61612 4.74112C1.85054 4.5067 2.16848 4.375 2.5 4.375H13.75C14.0815 4.375 14.3995 4.5067 14.6339 4.74112C14.8683 4.97554 15 5.29348 15 5.625ZM19.5312 5.64453C19.4431 5.62295 19.3513 5.62029 19.2621 5.63672C19.1728 5.65315 19.088 5.68829 19.0133 5.73984L16.3891 7.48906C16.3463 7.51762 16.3112 7.55631 16.2869 7.6017C16.2626 7.64708 16.25 7.69776 16.25 7.74922V12.2508C16.25 12.3022 16.2626 12.3529 16.2869 12.3983C16.3112 12.4437 16.3463 12.4824 16.3891 12.5109L19.0281 14.2703C19.1269 14.3362 19.2424 14.3726 19.3611 14.3752C19.4798 14.3779 19.5968 14.3466 19.6984 14.2852C19.7924 14.2254 19.8695 14.1425 19.9223 14.0444C19.9751 13.9464 20.0019 13.8364 20 13.725V6.25C20.0001 6.11139 19.9541 5.97668 19.8692 5.86708C19.7843 5.75747 19.6655 5.67918 19.5312 5.64453Z" }) }),
    (0, import_jsx_runtime2.jsx)("defs", { children: (0, import_jsx_runtime2.jsx)("clipPath", { id: "clip0_14064_467281", children: (0, import_jsx_runtime2.jsx)("rect", { fill: "white", height: "20", width: "20" }) }) })
  ] }),
  { "data-rtl-mirror": "" }
);
var IconAudio = createIcon(
  "IconAudio",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M15.625 8.125V11.875M18.125 6.875V13.125M6.875 13.125H3.125C2.95924 13.125 2.80027 13.0592 2.68306 12.9419C2.56585 12.8247 2.5 12.6658 2.5 12.5V7.5C2.5 7.33424 2.56585 7.17527 2.68306 7.05806C2.80027 6.94085 2.95924 6.875 3.125 6.875H6.875L12.5 2.5V17.5L6.875 13.125Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  { "data-rtl-mirror": "" }
);
var IconArchive = createIcon(
  "IconArchive",
  (0, import_jsx_runtime2.jsx)(
    "path",
    {
      d: "M16.875 7.5V15C16.875 15.1658 16.8092 15.3247 16.6919 15.4419C16.5747 15.5592 16.4158 15.625 16.25 15.625H3.75C3.58424 15.625 3.42527 15.5592 3.30806 15.4419C3.19085 15.3247 3.125 15.1658 3.125 15V7.5M8.125 10.625H11.875M2.5 4.375H17.5C17.8452 4.375 18.125 4.65482 18.125 5V6.875C18.125 7.22018 17.8452 7.5 17.5 7.5H2.5C2.15482 7.5 1.875 7.22018 1.875 6.875V5C1.875 4.65482 2.15482 4.375 2.5 4.375Z",
      fill: "none",
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  )
);
var IconLoading = createIcon(
  "IconLoading",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M17.5 10C17.5 14.1422 14.1422 17.5 10 17.5C5.85787 17.5 2.5 14.1422 2.5 10C2.5 5.85787 5.85787 2.5 10 2.5C14.1422 2.5 17.5 5.85787 17.5 10Z",
        fill: "none",
        stroke: "currentColor",
        strokeOpacity: "0.3",
        strokeWidth: "2"
      }
    ),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M17.4544 10.8334C17.0701 14.3097 14.3098 17.07 10.8335 17.4543",
        fill: "none",
        stroke: "currentColor",
        strokeLinecap: "round",
        strokeWidth: "2"
      }
    )
  ] })
);
var IconGiphy = createIcon(
  "IconGiphy",
  (0, import_jsx_runtime2.jsxs)(import_jsx_runtime2.Fragment, { children: [
    (0, import_jsx_runtime2.jsx)("rect", { fill: "black", height: "20", rx: "10", width: "20" }),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        clipRule: "evenodd",
        d: "M6.5997 5.50024H13.4008V14.4999H6.59912L6.5997 5.50024Z",
        fill: "black",
        fillRule: "evenodd"
      }
    ),
    (0, import_jsx_runtime2.jsx)("path", { d: "M5.24023 5.1665H6.59998V14.8335H5.24023V5.1665Z", fill: "#04FF8E" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M13.4004 7.8335H14.7601V14.8335H13.4004V7.8335Z", fill: "#8E2EFF" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M5.24023 14.4998H14.7602V15.8333H5.24023V14.4998Z", fill: "#00C5FF" }),
    (0, import_jsx_runtime2.jsx)("path", { d: "M5.24023 4.16675H10.6804V5.50025H5.24023V4.16675Z", fill: "#FFF152" }),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        d: "M13.4003 6.83316V5.50025H12.0399V4.16675H10.6802V8.16666H14.76V6.83316",
        fill: "#FF5B5B"
      }
    ),
    (0, import_jsx_runtime2.jsx)("path", { d: "M13.4004 9.5V8.1665H14.7601", fill: "#551C99" }),
    (0, import_jsx_runtime2.jsx)(
      "path",
      {
        clipRule: "evenodd",
        d: "M10.6802 4.16675V5.50025H9.31982",
        fill: "#999131",
        fillRule: "evenodd"
      }
    )
  ] })
);
var DEFAULT_PLAYBACK_RATES = [1, 1.5, 2];
var isSeekable = (audioElement) => !(audioElement.duration === Infinity || isNaN(audioElement.duration));
var defaultRegisterAudioPlayerError = ({
  error: error2
} = {}) => {
  if (!error2) return;
  console.error("[AUDIO PLAYER]", error2);
};
var elementIsPlaying = (audioElement) => audioElement && !(audioElement.paused || audioElement.ended);
var AudioPlayer = class {
  constructor({
    durationSeconds,
    fileSize,
    id: id3,
    mimeType,
    playbackRates: customPlaybackRates,
    plugins,
    pool,
    src,
    title,
    waveformData
  }) {
    this._plugins = /* @__PURE__ */ new Map();
    this.playTimeout = void 0;
    this.unsubscribeEventListeners = null;
    this._disposed = false;
    this._metadataProbe = null;
    this._restoringPosition = false;
    this._removalTimeout = void 0;
    this.setDurationSeconds = (durationSeconds2) => {
      this._data.durationSeconds = durationSeconds2;
      this.state.partialNext({ durationSeconds: durationSeconds2 });
    };
    this.setPlaybackStartSafetyTimeout = () => {
      clearTimeout(this.playTimeout);
      this.playTimeout = setTimeout(() => {
        if (!this.elementRef) return;
        try {
          this.elementRef.pause();
          this.state.partialNext({ isPlaying: false });
        } catch (e) {
          this.registerError({ errCode: "failed-to-start" });
        }
      }, 2e3);
    };
    this.updateDurationFromElement = (element4) => {
      const duration2 = element4.duration;
      if (typeof duration2 !== "number" || isNaN(duration2) || !isFinite(duration2) || duration2 <= 0) {
        return;
      }
      this.setDurationSeconds(duration2);
    };
    this.clearMetadataProbe = () => {
      const probe = this._metadataProbe;
      this._metadataProbe = null;
      this._metadataProbePromise = void 0;
      if (!probe) return;
      try {
        probe.pause();
      } catch {
      }
      probe.removeAttribute("src");
      try {
        probe.load();
      } catch {
      }
    };
    this.preloadMetadata = () => {
      if (this._disposed || this.durationSeconds != null || !this.src || this._metadataProbePromise || typeof document === "undefined") {
        return;
      }
      const probe = document.createElement("audio");
      probe.preload = "metadata";
      this._metadataProbe = probe;
      this._metadataProbePromise = new Promise((resolve) => {
        const cleanup2 = () => {
          probe.removeEventListener("loadedmetadata", handleLoadedMetadata);
          probe.removeEventListener("error", handleError);
          if (this._metadataProbe === probe) {
            this.clearMetadataProbe();
          } else {
            this._metadataProbePromise = void 0;
          }
          resolve();
        };
        const handleLoadedMetadata = () => {
          this.updateDurationFromElement(probe);
          cleanup2();
        };
        const handleError = () => {
          cleanup2();
        };
        probe.addEventListener("loadedmetadata", handleLoadedMetadata, { once: true });
        probe.addEventListener("error", handleError, { once: true });
        probe.src = this.src;
        try {
          probe.load();
        } catch {
          cleanup2();
        }
      });
    };
    this.clearPlaybackStartSafetyTimeout = () => {
      if (!this.elementRef) return;
      clearTimeout(this.playTimeout);
      this.playTimeout = void 0;
    };
    this.clearPendingLoadedMeta = () => {
      const pending = this._pendingLoadedMeta;
      if (pending?.element && pending.onLoaded) {
        pending.element.removeEventListener("loadedmetadata", pending.onLoaded);
      }
      this._pendingLoadedMeta = void 0;
    };
    this.restoreSavedPosition = (elementRef) => {
      const saved = this.secondsElapsed;
      if (!saved || saved <= 0) return;
      const apply = () => {
        const duration2 = elementRef.duration;
        const clamped = typeof duration2 === "number" && !isNaN(duration2) && isFinite(duration2) ? Math.min(saved, duration2) : saved;
        try {
          if (elementRef.currentTime === clamped) return;
          elementRef.currentTime = clamped;
          this.setSecondsElapsed(clamped);
        } catch {
        }
      };
      if (elementRef.readyState < 1) {
        this.clearPendingLoadedMeta();
        this._restoringPosition = true;
        const onLoaded = () => {
          if (this._pendingLoadedMeta?.onLoaded !== onLoaded) return;
          this._pendingLoadedMeta = void 0;
          if (this.elementRef !== elementRef) {
            this._restoringPosition = false;
            return;
          }
          apply();
          this._restoringPosition = false;
        };
        elementRef.addEventListener("loadedmetadata", onLoaded, { once: true });
        this._pendingLoadedMeta = { element: elementRef, onLoaded };
      } else {
        this._restoringPosition = true;
        apply();
        this._restoringPosition = false;
      }
    };
    this.elementIsReady = () => {
      if (this._elementIsReadyPromise) return this._elementIsReadyPromise;
      this._elementIsReadyPromise = new Promise((resolve) => {
        if (!this.elementRef) return resolve(false);
        const element4 = this.elementRef;
        const handleLoaded = () => {
          element4.removeEventListener("loadedmetadata", handleLoaded);
          resolve(element4.readyState > 0);
        };
        element4.addEventListener("loadedmetadata", handleLoaded);
      });
      return this._elementIsReadyPromise;
    };
    this.setRef = (elementRef) => {
      if (elementIsPlaying(this.elementRef)) {
        this.releaseElement({ resetState: false });
      }
      this.clearPendingLoadedMeta();
      this.clearMetadataProbe();
      this._restoringPosition = false;
      this._elementIsReadyPromise = void 0;
      this.state.partialNext({ elementRef });
      if (elementRef) {
        this.registerSubscriptions();
      }
    };
    this.setSecondsElapsed = (secondsElapsed) => {
      const duration2 = this.elementRef?.duration ?? this.durationSeconds;
      this.state.partialNext({
        progressPercent: duration2 && secondsElapsed ? secondsElapsed / duration2 * 100 : 0,
        secondsElapsed
      });
    };
    this.canPlayMimeType = (mimeType2) => {
      if (!mimeType2) return false;
      if (this.elementRef) return !!this.elementRef.canPlayType(mimeType2);
      return !!new Audio().canPlayType(mimeType2);
    };
    this.play = async (params) => {
      if (this._disposed) return;
      const elementRef = this.ensureElementRef();
      if (elementIsPlaying(this.elementRef)) {
        if (this.isPlaying) return;
        this.state.partialNext({ isPlaying: true });
        return;
      }
      const { currentPlaybackRate, playbackRates: playbackRates2 } = {
        currentPlaybackRate: this.currentPlaybackRate,
        playbackRates: this.playbackRates,
        ...params
      };
      if (!this.canPlayRecord) {
        this.registerError({ errCode: "not-playable" });
        return;
      }
      this.restoreSavedPosition(elementRef);
      elementRef.playbackRate = currentPlaybackRate ?? this.currentPlaybackRate;
      this.setPlaybackStartSafetyTimeout();
      try {
        await elementRef.play();
        this.state.partialNext({
          currentPlaybackRate,
          isPlaying: true,
          playbackRates: playbackRates2
        });
        this._pool.setActiveAudioPlayer(this);
      } catch (e) {
        this.registerError({ error: e });
        this.state.partialNext({ isPlaying: false });
      } finally {
        this.clearPlaybackStartSafetyTimeout();
      }
    };
    this.pause = () => {
      if (!elementIsPlaying(this.elementRef)) return;
      this.clearPlaybackStartSafetyTimeout();
      this.elementRef.pause();
      this.state.partialNext({ isPlaying: false });
    };
    this.stop = () => {
      this.pause();
      this.state.partialNext({ isPlaying: false });
      this.setSecondsElapsed(0);
      if (this.elementRef) this.elementRef.currentTime = 0;
    };
    this.togglePlay = async () => this.isPlaying ? this.pause() : await this.play();
    this.increasePlaybackRate = () => {
      let currentPlaybackRateIndex = this.state.getLatestValue().playbackRates.findIndex((rate) => rate === this.currentPlaybackRate);
      if (currentPlaybackRateIndex === -1) {
        currentPlaybackRateIndex = 0;
      }
      const nextIndex = currentPlaybackRateIndex === this.playbackRates.length - 1 ? 0 : currentPlaybackRateIndex + 1;
      const currentPlaybackRate = this.playbackRates[nextIndex];
      this.state.partialNext({ currentPlaybackRate });
      if (this.elementRef) {
        this.elementRef.playbackRate = currentPlaybackRate;
      }
    };
    this.seek = (0, import_lodash.default)(async ({ clientX, currentTarget }) => {
      let element4 = this.elementRef;
      if (!this.elementRef) {
        element4 = this.ensureElementRef();
        const isReady = await this.elementIsReady();
        if (!isReady) return;
      }
      if (!currentTarget || !element4) return;
      if (!isSeekable(element4)) {
        this.registerError({ errCode: "seek-not-supported" });
        return;
      }
      const { width, x: x2 } = currentTarget.getBoundingClientRect();
      const ratio = (clientX - x2) / width;
      if (ratio > 1 || ratio < 0) return;
      const currentTime = ratio * element4.duration;
      this.setSecondsElapsed(currentTime);
      element4.currentTime = currentTime;
    }, 16);
    this.registerError = (params) => {
      defaultRegisterAudioPlayerError(params);
      this.plugins.forEach(({ onError }) => onError?.({ player: this, ...params }));
    };
    this.requestRemoval = () => {
      this._disposed = true;
      this.cancelScheduledRemoval();
      this.clearPendingLoadedMeta();
      this.clearMetadataProbe();
      this._restoringPosition = false;
      this.releaseElement({ resetState: true });
      this.unsubscribeEventListeners?.();
      this.unsubscribeEventListeners = null;
      this.plugins.forEach(({ onRemove }) => onRemove?.({ player: this }));
      this._pool.deregister(this.id);
    };
    this.cancelScheduledRemoval = () => {
      clearTimeout(this._removalTimeout);
      this._removalTimeout = void 0;
    };
    this.scheduleRemoval = (ms = 0) => {
      this.cancelScheduledRemoval();
      this._removalTimeout = setTimeout(() => {
        if (this.disposed) return;
        this.requestRemoval();
      }, ms);
    };
    this.releaseElementForHandoff = () => {
      if (!this.elementRef) return;
      this.releaseElement({ resetState: false });
      this.unsubscribeEventListeners?.();
      this.unsubscribeEventListeners = null;
    };
    this.registerSubscriptions = () => {
      this.unsubscribeEventListeners?.();
      const audioElement = this.elementRef;
      if (!audioElement) return;
      const handleEnded = () => {
        if (audioElement) {
          this.updateDurationFromElement(audioElement);
        }
        this.stop();
      };
      const handleError = (e) => {
        const audio = e.currentTarget;
        const state = { isPlaying: false };
        if (!audio?.error?.code) {
          this.state.partialNext(state);
          return;
        }
        if (audio.error.code === 4) {
          state.canPlayRecord = false;
          this.state.partialNext(state);
        }
        const errorMsg = [
          void 0,
          "MEDIA_ERR_ABORTED: fetch aborted by user",
          "MEDIA_ERR_NETWORK: network failed while fetching",
          "MEDIA_ERR_DECODE: audio fetched but couldn’t decode",
          "MEDIA_ERR_SRC_NOT_SUPPORTED: source not supported"
        ][audio?.error?.code];
        if (!errorMsg) return;
        defaultRegisterAudioPlayerError({ error: new Error(errorMsg + ` (${audio.src})`) });
      };
      const handleTimeupdate = () => {
        const t2 = audioElement?.currentTime ?? 0;
        if (this._restoringPosition && t2 === 0) return;
        if (!this.isPlaying && t2 === 0 && this.secondsElapsed > 0) return;
        this.setSecondsElapsed(t2);
      };
      const handleLoadedMetadata = () => {
        if (audioElement) {
          this.updateDurationFromElement(audioElement);
        }
      };
      audioElement.addEventListener("ended", handleEnded);
      audioElement.addEventListener("error", handleError);
      audioElement.addEventListener("loadedmetadata", handleLoadedMetadata);
      audioElement.addEventListener("timeupdate", handleTimeupdate);
      this.unsubscribeEventListeners = () => {
        audioElement.pause();
        audioElement.removeEventListener("ended", handleEnded);
        audioElement.removeEventListener("error", handleError);
        audioElement.removeEventListener("loadedmetadata", handleLoadedMetadata);
        audioElement.removeEventListener("timeupdate", handleTimeupdate);
      };
    };
    this._data = {
      durationSeconds,
      fileSize,
      id: id3,
      mimeType,
      src,
      title,
      waveformData
    };
    this._pool = pool;
    this.setPlugins(() => plugins ?? []);
    const playbackRates = customPlaybackRates?.length ? customPlaybackRates : DEFAULT_PLAYBACK_RATES;
    const canPlayRecord = mimeType ? !!new Audio().canPlayType(mimeType) : true;
    this.state = new StateStore({
      canPlayRecord,
      currentPlaybackRate: playbackRates[0],
      durationSeconds,
      elementRef: null,
      isPlaying: false,
      playbackError: null,
      playbackRates,
      progressPercent: 0,
      secondsElapsed: 0
    });
    this.plugins.forEach((p2) => p2.onInit?.({ player: this }));
    this.preloadMetadata();
  }
  get plugins() {
    return Array.from(this._plugins.values());
  }
  get canPlayRecord() {
    return this.state.getLatestValue().canPlayRecord;
  }
  get elementRef() {
    return this.state.getLatestValue().elementRef;
  }
  get isPlaying() {
    return this.state.getLatestValue().isPlaying;
  }
  get currentPlaybackRate() {
    return this.state.getLatestValue().currentPlaybackRate;
  }
  get playbackRates() {
    return this.state.getLatestValue().playbackRates;
  }
  get durationSeconds() {
    return this.state.getLatestValue().durationSeconds;
  }
  get fileSize() {
    return this._data.fileSize;
  }
  get id() {
    return this._data.id;
  }
  get src() {
    return this._data.src;
  }
  get mimeType() {
    return this._data.mimeType;
  }
  get title() {
    return this._data.title;
  }
  get waveformData() {
    return this._data.waveformData;
  }
  get secondsElapsed() {
    return this.state.getLatestValue().secondsElapsed;
  }
  get progressPercent() {
    return this.state.getLatestValue().progressPercent;
  }
  get disposed() {
    return this._disposed;
  }
  ensureElementRef() {
    if (this._disposed) {
      throw new Error("AudioPlayer is disposed");
    }
    if (!this.elementRef) {
      const el = this._pool.acquireElement({
        ownerId: this.id,
        src: this.src
      });
      this.setRef(el);
    }
    return this.elementRef;
  }
  setDescriptor(descriptor) {
    const previousSrc = this.src;
    this._data = { ...this._data, ...descriptor };
    if (descriptor.src !== previousSrc && this.elementRef) {
      this.elementRef.src = descriptor.src;
    }
    if (descriptor.src && descriptor.src !== previousSrc) {
      this.clearMetadataProbe();
      if (descriptor.durationSeconds == null) {
        this.setDurationSeconds(void 0);
        this.preloadMetadata();
      } else {
        this.setDurationSeconds(descriptor.durationSeconds);
      }
      return;
    }
    if (descriptor.durationSeconds != null) {
      this.setDurationSeconds(descriptor.durationSeconds);
    }
  }
  releaseElement({ resetState }) {
    this.clearPendingLoadedMeta();
    this.clearMetadataProbe();
    this._restoringPosition = false;
    if (resetState) {
      this.stop();
    } else {
      this.state.partialNext({ isPlaying: false });
      if (this.elementRef) {
        try {
          this.elementRef.pause();
        } catch {
        }
      }
    }
    if (this.elementRef) {
      this._pool.releaseElement(this.id);
      this.setRef(null);
    }
  }
  setPlugins(setter) {
    this._plugins = setter(this.plugins).reduce((acc, plugin) => {
      if (plugin.id) {
        acc.set(plugin.id, plugin);
      }
      return acc;
    }, /* @__PURE__ */ new Map());
  }
};
var AudioPlayerPool = class {
  constructor(config) {
    this.state = new StateStore({
      activeAudioPlayer: null
    });
    this.pool = /* @__PURE__ */ new Map();
    this.audios = /* @__PURE__ */ new Map();
    this.sharedAudio = null;
    this.sharedOwnerId = null;
    this.getOrAdd = (params) => {
      const { playbackRates, plugins, ...descriptor } = params;
      let player = this.pool.get(params.id);
      if (player) {
        if (!player.disposed) {
          player.setDescriptor(descriptor);
          return player;
        }
        this.deregister(params.id);
      }
      player = new AudioPlayer({
        playbackRates,
        plugins,
        ...descriptor,
        pool: this
      });
      this.pool.set(params.id, player);
      return player;
    };
    this.acquireElement = ({ ownerId, src }) => {
      if (!this.allowConcurrentPlayback) {
        if (!this.sharedAudio) {
          this.sharedAudio = new Audio();
        }
        if (this.sharedOwnerId && this.sharedOwnerId !== ownerId) {
          const previous3 = this.pool.get(this.sharedOwnerId);
          previous3?.pause();
          previous3?.releaseElementForHandoff();
        }
        this.sharedOwnerId = ownerId;
        if (this.sharedAudio.src !== src) {
          this.sharedAudio.src = src;
        }
        return this.sharedAudio;
      }
      let audio = this.audios.get(ownerId);
      if (!audio) {
        audio = new Audio();
        this.audios.set(ownerId, audio);
      }
      if (audio.src !== src) {
        audio.src = src;
      }
      return audio;
    };
    this.releaseElement = (ownerId) => {
      if (!this.allowConcurrentPlayback) {
        if (this.sharedOwnerId !== ownerId) return;
        const el2 = this.sharedAudio;
        if (el2) {
          try {
            el2.pause();
          } catch {
          }
          el2.removeAttribute("src");
          el2.load();
        }
        this.sharedOwnerId = null;
        return;
      }
      const el = this.audios.get(ownerId);
      if (!el) return;
      try {
        el.pause();
      } catch {
      }
      el.removeAttribute("src");
      el.load();
      this.audios.delete(ownerId);
    };
    this.setActiveAudioPlayer = (activeAudioPlayer) => {
      if (this.allowConcurrentPlayback) return;
      this.state.partialNext({ activeAudioPlayer });
    };
    this.remove = (id3) => {
      const player = this.pool.get(id3);
      if (!player) return;
      player.requestRemoval();
    };
    this.clear = () => {
      this.players.forEach((player) => {
        this.remove(player.id);
      });
    };
    this.registerSubscriptions = () => {
      this.players.forEach((p2) => {
        if (p2.elementRef) {
          p2.registerSubscriptions();
        }
      });
    };
    this.allowConcurrentPlayback = !!config?.allowConcurrentPlayback;
  }
  get players() {
    return Array.from(this.pool.values());
  }
  get activeAudioPlayer() {
    return this.state.getLatestValue().activeAudioPlayer;
  }
  /** Removes the AudioPlayer instance from the pool of players */
  deregister(id3) {
    if (this.pool.has(id3)) {
      this.pool.delete(id3);
    }
    if (this.activeAudioPlayer?.id === id3) {
      this.setActiveAudioPlayer(null);
    }
  }
};
var SEEK_NOT_SUPPORTED_NOTIFICATION_DEBOUNCE_INTERVAL_MS = 1e3;
var audioPlayerNotificationsPluginFactory = ({
  addNotification,
  panel = "channel",
  t: t2
}) => {
  const errors = {
    "failed-to-start": new Error(t2("Failed to play the recording")),
    "not-playable": new Error(
      t2("Recording format is not supported and cannot be reproduced")
    ),
    "seek-not-supported": new Error(t2("Cannot seek in the recording"))
  };
  let lastSeekNotSupportedNotificationAt;
  return {
    id: "AudioPlayerNotificationsPlugin",
    onError: ({ errCode, error: e }) => {
      if (errCode === "seek-not-supported") {
        const now = Date.now();
        if (typeof lastSeekNotSupportedNotificationAt === "number" && now - lastSeekNotSupportedNotificationAt < SEEK_NOT_SUPPORTED_NOTIFICATION_DEBOUNCE_INTERVAL_MS) {
          return;
        }
        lastSeekNotSupportedNotificationAt = now;
      }
      const error2 = (errCode && errors[errCode]) ?? e ?? new Error(t2("Error reproducing the recording"));
      addNotification({
        emitter: "AudioPlayer",
        error: error2,
        message: error2.message,
        severity: "error",
        targetPanels: [panel],
        type: "browser:audio:playback:error"
      });
    }
  };
};
var NOTIFICATION_TARGET_PANELS = [
  "channel",
  "thread",
  "channel-list",
  "thread-list"
];
var isNotificationTargetPanel = (value) => typeof value === "string" && NOTIFICATION_TARGET_PANELS.includes(value);
var getNotificationTargetPanel = (notification) => {
  const targetTag = notification.tags?.find((tag) => tag.startsWith("target:"));
  if (targetTag) {
    const candidate = targetTag.slice("target:".length);
    if (isNotificationTargetPanel(candidate)) return candidate;
  }
  const panel = notification.origin.context?.panel;
  return isNotificationTargetPanel(panel) ? panel : void 0;
};
var getNotificationTargetPanels = (notification) => {
  const targetPanels = (notification.tags ?? []).filter((tag) => tag.startsWith("target:")).map((tag) => tag.slice("target:".length)).filter(
    (value) => isNotificationTargetPanel(value)
  );
  if (targetPanels.length > 0) {
    return Array.from(new Set(targetPanels));
  }
  const panel = notification.origin.context?.panel;
  return isNotificationTargetPanel(panel) ? [panel] : [];
};
var getNotificationTargetTag = (panel) => `target:${panel}`;
var addNotificationTargetTag = (panel, tags) => {
  if (!panel) return tags ?? [];
  return Array.from(/* @__PURE__ */ new Set([getNotificationTargetTag(panel), ...tags ?? []]));
};
var isNotificationForPanel = (notification, panel, options) => {
  const explicitTargetPanels = getNotificationTargetPanels(notification);
  if (explicitTargetPanels.length > 0) {
    return explicitTargetPanels.includes(panel);
  }
  const resolvedPanel = options?.fallbackPanel ?? "channel";
  return resolvedPanel === panel;
};
var variantToClass = {
  danger: "str-chat__button--destructive",
  primary: "str-chat__button--primary",
  secondary: "str-chat__button--secondary"
};
var appearanceToClass = {
  ghost: "str-chat__button--ghost",
  outline: "str-chat__button--outline",
  solid: "str-chat__button--solid"
};
var sizeToClass = {
  lg: "str-chat__button--size-lg",
  md: "str-chat__button--size-md",
  sm: "str-chat__button--size-sm",
  xs: "str-chat__button--size-xs"
};
var Button = (0, import_react4.forwardRef)(function Button2({ appearance, children, circular, className, inverseTheme, size: size22, variant, ...props }, ref) {
  return (0, import_jsx_runtime2.jsx)(
    "button",
    {
      ref,
      type: "button",
      ...props,
      className: clsx_default(
        "str-chat__button",
        variant != null && variantToClass[variant],
        appearance != null && appearanceToClass[appearance],
        circular && "str-chat__button--circular",
        inverseTheme && "str-chat__theme-inverse",
        size22 != null && sizeToClass[size22],
        className
      ),
      children: (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__button__content", children })
    }
  );
});
var UnMemoizedEmptyStateIndicator = (props) => {
  const { listType, messageText } = props;
  const { t: t2 } = useTranslationContext("EmptyStateIndicator");
  if (listType === "thread") return null;
  if (listType === "channel") {
    const text8 = t2("No conversations yet");
    return (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__channel-list-empty", children: [
      (0, import_jsx_runtime2.jsx)(IconMessageBubbles, {}),
      (0, import_jsx_runtime2.jsx)("p", { role: "listitem", children: text8 })
    ] });
  }
  if (listType === "message") {
    const text8 = t2(messageText || "Send a message to start the conversation");
    return (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__empty-channel", children: [
      (0, import_jsx_runtime2.jsx)(IconMessageBubble, {}),
      (0, import_jsx_runtime2.jsx)("p", { className: "str-chat__empty-channel-text", role: "listitem", children: text8 })
    ] });
  }
  return (0, import_jsx_runtime2.jsx)("p", { children: t2("No items exist") });
};
var EmptyStateIndicator = import_react4.default.memo(
  UnMemoizedEmptyStateIndicator
);
var makeChannelReducer = () => (state, action) => {
  switch (action.type) {
    case "closeThread": {
      return {
        ...state,
        thread: null,
        threadLoadingMore: false,
        threadMessages: []
      };
    }
    case "copyMessagesFromChannel": {
      const { channel, parentId } = action;
      return {
        ...state,
        messages: [...channel.state.messages],
        pinnedMessages: [...channel.state.pinnedMessages],
        // copying messages from channel happens with new message - this resets the suppressAutoscroll
        suppressAutoscroll: false,
        threadMessages: parentId ? { ...channel.state.threads }[parentId] || [] : state.threadMessages
      };
    }
    case "copyStateFromChannelOnEvent": {
      const { channel } = action;
      return {
        ...state,
        members: { ...channel.state.members },
        messages: [...channel.state.messages],
        pinnedMessages: [...channel.state.pinnedMessages],
        read: { ...channel.state.read },
        watcherCount: channel.state.watcher_count,
        watchers: { ...channel.state.watchers }
      };
    }
    case "initStateFromChannel": {
      const { channel, hasMore } = action;
      return {
        ...state,
        hasMore,
        loading: false,
        members: { ...channel.state.members },
        messages: [...channel.state.messages],
        pinnedMessages: [...channel.state.pinnedMessages],
        read: { ...channel.state.read },
        watcherCount: channel.state.watcher_count,
        watchers: { ...channel.state.watchers }
      };
    }
    case "jumpToLatestMessageFinished": {
      const { hasMore, hasMoreNewer, messages } = action;
      return {
        ...state,
        hasMore,
        hasMoreNewer,
        highlightedMessageId: void 0,
        loading: false,
        messages,
        suppressAutoscroll: false
      };
    }
    case "jumpToMessageFinished": {
      return {
        ...state,
        hasMore: action.channel.state.messagePagination.hasPrev,
        hasMoreNewer: action.channel.state.messagePagination.hasNext,
        highlightedMessageId: action.highlightedMessageId,
        loadingMore: false,
        loadingMoreForJumpToChannelMessage: false,
        messages: action.channel.state.messages,
        suppressAutoscroll: false
      };
    }
    case "clearHighlightedMessage": {
      return {
        ...state,
        highlightedMessageId: void 0
      };
    }
    case "loadMoreFinished": {
      const { hasMore, messages } = action;
      return {
        ...state,
        hasMore,
        loadingMore: false,
        messages,
        suppressAutoscroll: false
      };
    }
    case "loadMoreNewerFinished": {
      const { hasMoreNewer, messages } = action;
      return {
        ...state,
        hasMoreNewer,
        loadingMoreNewer: false,
        messages
      };
    }
    case "loadMoreThreadFinished": {
      const { threadHasMore, threadMessages } = action;
      return {
        ...state,
        threadHasMore,
        threadLoadingMore: false,
        threadMessages
      };
    }
    case "openThread": {
      const { channel, message } = action;
      return {
        ...state,
        thread: message,
        threadHasMore: true,
        threadMessages: message.id ? { ...channel.state.threads }[message.id] || [] : [],
        threadSuppressAutoscroll: false
      };
    }
    case "setError": {
      const { error: error2 } = action;
      return { ...state, error: error2 };
    }
    case "setLoadingMore": {
      const { loadingMore } = action;
      return { ...state, loadingMore, suppressAutoscroll: loadingMore };
    }
    case "setLoadingMoreForJumpToChannelMessage": {
      const { loadingMoreForJumpToChannelMessage } = action;
      return {
        ...state,
        loadingMoreForJumpToChannelMessage,
        suppressAutoscroll: loadingMoreForJumpToChannelMessage
      };
    }
    case "setLoadingMoreNewer": {
      const { loadingMoreNewer } = action;
      return { ...state, loadingMoreNewer };
    }
    case "setThread": {
      const { message } = action;
      return { ...state, thread: message };
    }
    case "setTyping": {
      const { channel } = action;
      return {
        ...state,
        typing: { ...channel.state.typing }
      };
    }
    case "startLoadingThread": {
      return {
        ...state,
        threadLoadingMore: true,
        threadSuppressAutoscroll: true
      };
    }
    case "updateThreadOnEvent": {
      const { channel, message } = action;
      if (!state.thread) return state;
      return {
        ...state,
        thread: message?.id === state.thread.id ? channel.state.formatMessage(message) : state.thread,
        threadMessages: state.thread?.id ? { ...channel.state.threads }[state.thread.id] || [] : []
      };
    }
    default:
      return state;
  }
};
var initialState = {
  error: null,
  hasMore: true,
  hasMoreNewer: false,
  loading: true,
  loadingMore: false,
  loadingMoreForJumpToChannelMessage: false,
  members: {},
  messages: [],
  pinnedMessages: [],
  read: {},
  suppressAutoscroll: false,
  thread: null,
  threadHasMore: true,
  threadLoadingMore: false,
  threadMessages: [],
  threadSuppressAutoscroll: false,
  typing: {},
  watcherCount: 0,
  watchers: {}
};
var useCreateChannelStateContext = (value) => {
  const {
    channel,
    channelCapabilitiesArray = [],
    channelConfig,
    channelUnreadUiState,
    error: error2,
    giphyVersion,
    hasMore,
    hasMoreNewer,
    highlightedMessageId,
    imageAttachmentSizeHandler,
    loading,
    loadingMore,
    loadingMoreForJumpToChannelMessage,
    members,
    messages = [],
    mutes,
    notifications,
    pinnedMessages,
    read = {},
    shouldGenerateVideoThumbnail,
    skipMessageDataMemoization,
    suppressAutoscroll,
    thread,
    threadHasMore,
    threadLoadingMore,
    threadMessages = [],
    videoAttachmentSizeHandler,
    watcher_count,
    watcherCount,
    watchers
  } = value;
  const channelId = channel.cid;
  const lastRead = channel.initialized && channel.lastRead()?.getTime();
  const membersLength = Object.keys(members || []).length;
  const notificationsLength = notifications.length;
  const readUsers = Object.values(read);
  const readUsersLength = readUsers.length;
  const readUsersLastReadDateStrings = [];
  for (const { last_read } of readUsers) {
    if (!lastRead) continue;
    readUsersLastReadDateStrings.push(last_read?.toISOString());
  }
  const readUsersLastReads = readUsersLastReadDateStrings.join();
  const threadMessagesLength = threadMessages?.length;
  const channelCapabilities = {};
  channelCapabilitiesArray.forEach((capability) => {
    channelCapabilities[capability] = true;
  });
  const memoizedMessageData = skipMessageDataMemoization ? messages : messages.map(
    ({
      deleted_at,
      latest_reactions,
      pinned,
      reply_count,
      status,
      type,
      updated_at,
      user
    }) => `${type}${deleted_at}${latest_reactions ? latest_reactions.map(({ type: type2 }) => type2).join() : ""}${pinned}${reply_count}${status}${updated_at && (isDayOrMoment(updated_at) || isDate(updated_at)) ? updated_at.toISOString() : updated_at || ""}${user?.updated_at}`
  ).join();
  const memoizedThreadMessageData = threadMessages.map(
    ({ deleted_at, latest_reactions, pinned, status, updated_at, user }) => `${deleted_at}${latest_reactions ? latest_reactions.map(({ type }) => type).join() : ""}${pinned}${status}${updated_at && (isDayOrMoment(updated_at) || isDate(updated_at)) ? updated_at.toISOString() : updated_at || ""}${user?.updated_at}`
  ).join();
  const channelStateContext = (0, import_react4.useMemo)(
    () => ({
      channel,
      channelCapabilities,
      channelConfig,
      channelUnreadUiState,
      error: error2,
      giphyVersion,
      hasMore,
      hasMoreNewer,
      highlightedMessageId,
      imageAttachmentSizeHandler,
      loading,
      loadingMore,
      loadingMoreForJumpToChannelMessage,
      members,
      messages,
      mutes,
      notifications,
      pinnedMessages,
      read,
      shouldGenerateVideoThumbnail,
      suppressAutoscroll,
      thread,
      threadHasMore,
      threadLoadingMore,
      threadMessages,
      videoAttachmentSizeHandler,
      watcher_count,
      watcherCount,
      watchers
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      channel.data?.name,
      // otherwise ChannelHeader will not be updated
      channelId,
      channelUnreadUiState,
      error2,
      hasMore,
      hasMoreNewer,
      highlightedMessageId,
      lastRead,
      loading,
      loadingMore,
      loadingMoreForJumpToChannelMessage,
      membersLength,
      memoizedMessageData,
      memoizedThreadMessageData,
      notificationsLength,
      readUsersLength,
      readUsersLastReads,
      shouldGenerateVideoThumbnail,
      skipMessageDataMemoization,
      suppressAutoscroll,
      thread,
      threadHasMore,
      threadLoadingMore,
      threadMessagesLength,
      watcherCount
    ]
  );
  return channelStateContext;
};
var useCreateTypingContext = (value) => {
  const { typing } = value;
  const typingValue = Object.keys(typing || {}).join();
  const typingContext = (0, import_react4.useMemo)(
    () => ({
      typing
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [typingValue]
  );
  return typingContext;
};
var useEditMessageHandler = (doUpdateMessageRequest) => {
  const { channel, client } = useChatContext("useEditMessageHandler");
  return (updatedMessage, options) => {
    if (doUpdateMessageRequest && channel) {
      return Promise.resolve(
        doUpdateMessageRequest(channel.cid, updatedMessage, options)
      );
    }
    return client.updateMessage(updatedMessage, void 0, options);
  };
};
var useIsMounted = () => {
  const isMounted = (0, import_react4.useRef)(false);
  (0, import_react4.useEffect)(() => {
    isMounted.current = true;
    return () => {
      isMounted.current = false;
    };
  }, []);
  return isMounted;
};
var useMentionsHandlers = (onMentionsHover, onMentionsClick) => (0, import_react4.useCallback)(
  (event, mentioned_users) => {
    if (!onMentionsHover && !onMentionsClick || !(event.target instanceof HTMLElement)) {
      return;
    }
    const target = event.target;
    const textContent = target.innerHTML.replace("*", "");
    if (textContent[0] === "@") {
      const userName = textContent.replace("@", "");
      const user = mentioned_users?.find(
        ({ id: id3, name: name2 }) => name2 === userName || id3 === userName
      );
      if (onMentionsHover && typeof onMentionsHover === "function" && event.type === "mouseover") {
        onMentionsHover(event, user);
      }
      if (onMentionsClick && event.type === "click" && typeof onMentionsClick === "function") {
        onMentionsClick(event, user);
      }
    }
  },
  [onMentionsClick, onMentionsHover]
);
var LoadingMessage = ({
  bubbleSize,
  metadataSize,
  outgoing = false
}) => (0, import_jsx_runtime2.jsxs)(
  "div",
  {
    className: `str-chat__loading-channel-message ${outgoing ? "str-chat__loading-channel-message--outgoing" : "str-chat__loading-channel-message--incoming"}`,
    children: [
      !outgoing ? (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__loading-channel-message-avatar" }) : null,
      (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__loading-channel-message-content", children: [
        (0, import_jsx_runtime2.jsx)(
          "div",
          {
            className: `str-chat__loading-channel-message-bubble str-chat__loading-channel-message-bubble--${bubbleSize}`
          }
        ),
        (0, import_jsx_runtime2.jsx)(
          "div",
          {
            className: `str-chat__loading-channel-message-metadata str-chat__loading-channel-message-metadata--${metadataSize}`
          }
        )
      ] })
    ]
  }
);
var LoadingMessageInput = () => (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__message-composer-container str-chat__message-composer-container--loading", children: (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__message-composer", children: [
  (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__loading-channel-message-input-button" }),
  (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__loading-channel-message-input-pill" })
] }) });
var LoadingChannelHeader = () => (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__channel-header str-chat__channel-header--loading", children: [
  (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__channel-header__data str-chat__channel-header__data--loading", children: (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__loading-channel-header-name" }) }),
  (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__loading-channel-header-avatar" })
] });
var LoadingChannel = () => (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__loading-channel", children: [
  (0, import_jsx_runtime2.jsx)(LoadingChannelHeader, {}),
  (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__message-list str-chat__message-list--loading", children: (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__message-list-scroll", children: (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__loading-channel-message-list", children: [
    (0, import_jsx_runtime2.jsx)(LoadingMessage, { bubbleSize: "lg", metadataSize: "md" }),
    (0, import_jsx_runtime2.jsx)(LoadingMessage, { bubbleSize: "md", metadataSize: "sm", outgoing: true }),
    (0, import_jsx_runtime2.jsx)(LoadingMessage, { bubbleSize: "lg", metadataSize: "md" })
  ] }) }) }),
  (0, import_jsx_runtime2.jsx)(LoadingMessageInput, {})
] });
var UnMemoizedLoadingErrorIndicator = ({ error: error2 }) => {
  const { t: t2 } = useTranslationContext("LoadingErrorIndicator");
  if (!error2) return null;
  return (0, import_jsx_runtime2.jsx)("div", { children: t2("Error: {{ errorMessage }}", { errorMessage: error2.message }) });
};
var LoadingErrorIndicator = import_react4.default.memo(
  UnMemoizedLoadingErrorIndicator,
  (prevProps, nextProps) => prevProps.error?.message === nextProps.error?.message
);
var CHANNEL_CONTAINER_ID = "str-chat__channel";
var DEFAULT_NEXT_CHANNEL_PAGE_SIZE = 25;
var DEFAULT_JUMP_TO_PAGE_SIZE = 25;
var DEFAULT_THREAD_PAGE_SIZE = 25;
var DEFAULT_LOAD_PAGE_SCROLL_THRESHOLD = 250;
var DEFAULT_HIGHLIGHT_DURATION = 500;
var validateAndGetMessage = (func, args) => {
  if (!func || typeof func !== "function") return null;
  if (!Array.isArray(args)) {
    args = [args];
  }
  const returnValue = func(...args);
  if (typeof returnValue !== "string") return null;
  return returnValue;
};
var isUserMuted = (message, mutes) => {
  if (!mutes || !message) return false;
  const userMuted = mutes.filter((el) => el.target.id === message.user?.id);
  return !!userMuted.length;
};
var OPTIONAL_MESSAGE_ACTIONS = {
  deleteForMe: "deleteForMe"
};
var MESSAGE_ACTIONS = {
  delete: "delete",
  download: "download",
  edit: "edit",
  flag: "flag",
  markUnread: "markUnread",
  mute: "mute",
  pin: "pin",
  quote: "quote",
  react: "react",
  remindMe: "remindMe",
  reply: "reply",
  saveForLater: "saveForLater"
};
var getMessageActions = (actions, {
  canDelete,
  canEdit,
  canFlag,
  canMarkUnread,
  canMute,
  canPin,
  canQuote,
  canReact,
  canReply
}, channelConfig) => {
  const messageActionsAfterPermission = [];
  let messageActions = [];
  if (actions && typeof actions === "boolean") {
    messageActions = Object.keys(MESSAGE_ACTIONS);
  } else if (actions && Array.isArray(actions) && actions.length > 0) {
    messageActions = [...actions];
  } else {
    return [];
  }
  if (canDelete && messageActions.indexOf(MESSAGE_ACTIONS.delete) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.delete);
  }
  if (messageActions.indexOf(MESSAGE_ACTIONS.download) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.download);
  }
  if (canDelete && messageActions.indexOf(OPTIONAL_MESSAGE_ACTIONS.deleteForMe) > -1) {
    messageActionsAfterPermission.push(OPTIONAL_MESSAGE_ACTIONS.deleteForMe);
  }
  if (canEdit && messageActions.indexOf(MESSAGE_ACTIONS.edit) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.edit);
  }
  if (canFlag && messageActions.indexOf(MESSAGE_ACTIONS.flag) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.flag);
  }
  if (canMarkUnread && messageActions.indexOf(MESSAGE_ACTIONS.markUnread) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.markUnread);
  }
  if (canMute && messageActions.indexOf(MESSAGE_ACTIONS.mute) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.mute);
  }
  if (canPin && messageActions.indexOf(MESSAGE_ACTIONS.pin) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.pin);
  }
  if (canQuote && messageActions.indexOf(MESSAGE_ACTIONS.quote) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.quote);
  }
  if (canReact && messageActions.indexOf(MESSAGE_ACTIONS.react) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.react);
  }
  if (channelConfig?.["user_message_reminders"] && messageActions.indexOf(MESSAGE_ACTIONS.remindMe) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.remindMe);
  }
  if (canReply && messageActions.indexOf(MESSAGE_ACTIONS.reply) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.reply);
  }
  if (channelConfig?.["user_message_reminders"] && messageActions.indexOf(MESSAGE_ACTIONS.saveForLater) > -1) {
    messageActionsAfterPermission.push(MESSAGE_ACTIONS.saveForLater);
  }
  return messageActionsAfterPermission;
};
var ACTIONS_NOT_WORKING_IN_THREAD = [
  MESSAGE_ACTIONS.pin,
  MESSAGE_ACTIONS.reply,
  MESSAGE_ACTIONS.markUnread
];
function areMessagesEqual(prevMessage, nextMessage) {
  const areBaseMessagesEqual = (prevMessage2, nextMessage2) => prevMessage2.deleted_at === nextMessage2.deleted_at && prevMessage2.latest_reactions?.length === nextMessage2.latest_reactions?.length && prevMessage2.own_reactions?.length === nextMessage2.own_reactions?.length && prevMessage2.pinned === nextMessage2.pinned && prevMessage2.reply_count === nextMessage2.reply_count && prevMessage2.show_in_channel === nextMessage2.show_in_channel && prevMessage2.status === nextMessage2.status && prevMessage2.text === nextMessage2.text && prevMessage2.type === nextMessage2.type && prevMessage2.updated_at === nextMessage2.updated_at && prevMessage2.user?.updated_at === nextMessage2.user?.updated_at;
  return areBaseMessagesEqual(prevMessage, nextMessage) && Boolean(prevMessage.quoted_message) === Boolean(nextMessage.quoted_message) && (!prevMessage.quoted_message && !nextMessage.quoted_message || areBaseMessagesEqual(
    prevMessage.quoted_message,
    nextMessage.quoted_message
  ));
}
var areMessagePropsEqual = (prevProps, nextProps) => {
  const { message: prevMessage, Message: prevMessageUI } = prevProps;
  const { message: nextMessage, Message: nextMessageUI } = nextProps;
  if (prevMessageUI !== nextMessageUI) return false;
  if (nextProps.showDetailedReactions !== prevProps.showDetailedReactions) {
    return false;
  }
  if (nextProps.closeReactionSelectorOnClick !== prevProps.closeReactionSelectorOnClick) {
    return false;
  }
  const messagesAreEqual = areMessagesEqual(prevMessage, nextMessage);
  if (!messagesAreEqual) return false;
  const deepEqualProps = (0, import_react_fast_compare.default)(nextProps.messageActions, prevProps.messageActions) && (0, import_react_fast_compare.default)(nextProps.readBy, prevProps.readBy) && (0, import_react_fast_compare.default)(nextProps.deliveredTo, prevProps.deliveredTo) && (0, import_react_fast_compare.default)(nextProps.highlighted, prevProps.highlighted) && (0, import_react_fast_compare.default)(nextProps.groupStyles, prevProps.groupStyles) && // last 3 messages can have different group styles
  (0, import_react_fast_compare.default)(nextProps.mutes, prevProps.mutes) && (0, import_react_fast_compare.default)(nextProps.lastReceivedId, prevProps.lastReceivedId);
  if (!deepEqualProps) return false;
  return prevProps.messageListRect === nextProps.messageListRect;
};
var areMessageUIPropsEqual = (prevProps, nextProps) => {
  const { lastReceivedId: prevLastReceivedId, message: prevMessage } = prevProps;
  const { lastReceivedId: nextLastReceivedId, message: nextMessage } = nextProps;
  if (prevProps.highlighted !== nextProps.highlighted) return false;
  if (prevProps.threadList !== nextProps.threadList) return false;
  if (prevProps.endOfGroup !== nextProps.endOfGroup) return false;
  if (prevProps.mutes?.length !== nextProps.mutes?.length) return false;
  if (prevProps.readBy?.length !== nextProps.readBy?.length) return false;
  if (prevProps.deliveredTo?.length !== nextProps.deliveredTo?.length) return false;
  if (prevProps.groupStyles !== nextProps.groupStyles) return false;
  if (prevProps.showDetailedReactions !== nextProps.showDetailedReactions) {
    return false;
  }
  if ((prevMessage.id === prevLastReceivedId || prevMessage.id === nextLastReceivedId) && prevLastReceivedId !== nextLastReceivedId) {
    return false;
  }
  return areMessagesEqual(prevMessage, nextMessage);
};
var messageHasReactions = (message) => Object.values(message?.reaction_groups ?? {}).some(({ count: count2 }) => count2 > 0);
var messageHasQuotedMessage = (message) => !!message?.quoted_message;
var messageHasAttachments = (message) => !!message?.attachments && !!message.attachments.length;
var messageHasSingleAttachment = (message) => message?.attachments?.length === 1;
var messageHasGiphyAttachment = (message) => !!message?.attachments?.some((att) => att.type === "giphy");
var getImages = (message) => {
  if (!message?.attachments) {
    return [];
  }
  return message.attachments.filter((item) => item.type === "image");
};
var getNonImageAttachments = (message) => {
  if (!message?.attachments) {
    return [];
  }
  return message.attachments.filter((item) => item.type !== "image");
};
var mapToUserNameOrId = (user) => user.name || user.id;
var getReadByTooltipText = (users, t2, client, tooltipUserNameMapper) => {
  let outStr = "";
  if (!t2) {
    throw new Error(
      "getReadByTooltipText was called, but translation function is not available"
    );
  }
  if (!tooltipUserNameMapper) {
    throw new Error(
      "getReadByTooltipText was called, but tooltipUserNameMapper function is not available"
    );
  }
  const otherUsers = users.filter((item) => item && client?.user && item.id !== client.user.id).map(tooltipUserNameMapper);
  const slicedArr = otherUsers.slice(0, 5);
  const restLength = otherUsers.length - slicedArr.length;
  if (slicedArr.length === 1) {
    outStr = `${slicedArr[0]} `;
  } else if (slicedArr.length === 2) {
    outStr = t2("{{ firstUser }} and {{ secondUser }}", {
      firstUser: slicedArr[0],
      secondUser: slicedArr[1]
    });
  } else if (slicedArr.length > 2) {
    if (restLength === 0) {
      const lastUser = slicedArr.splice(slicedArr.length - 1, 1);
      outStr = t2("{{ commaSeparatedUsers }}, and {{ lastUser }}", {
        commaSeparatedUsers: slicedArr.join(", "),
        lastUser
      });
    } else {
      outStr = t2("{{ commaSeparatedUsers }} and {{ moreCount }} more", {
        commaSeparatedUsers: slicedArr.join(", "),
        moreCount: restLength
      });
    }
  }
  return outStr;
};
var countEmojis = (text8) => {
  const matches2 = text8?.match((0, import_emoji_regex.default)());
  return matches2 ? matches2.length : 0;
};
var messageTextHasEmojisOnly = (message) => {
  if (!message.text) return false;
  const noEmojis = message.text.replace((0, import_emoji_regex.default)(), "");
  const noSpace = noEmojis.replace(/[\s\n]/gm, "");
  return !noSpace;
};
var isMessageErrorRetryable = (message) => message.status === "failed" && message.error?.status !== 403;
var isNetworkSendFailure = (message) => message.status === "failed" && message.error?.status === 0;
var isMessageBounced = (message) => message.type === "error" && (message.moderation_details?.action === "MESSAGE_RESPONSE_ACTION_BOUNCE" || message.moderation?.action === "bounce");
var isMessageBlocked = (message) => message.shadowed || message.type === "error" && (message.moderation_details?.action === "MESSAGE_RESPONSE_ACTION_REMOVE" || message.moderation?.action === "remove");
var isMessageDeleted = (message) => Boolean(message.deleted_at || message.type === "deleted" || message.deleted_for_me);
var isMessageEdited = (message) => !!message.message_text_updated_at;
var hasResizeObserver = typeof window !== "undefined" && "ResizeObserver" in window;
function autoMiddlewareFor(p2) {
  if (!String(p2).startsWith("auto")) return null;
  const alignment = p2 === "auto-start" ? "start" : p2 === "auto-end" ? "end" : void 0;
  return autoPlacement3({ alignment });
}
function toOffsetMw(opt) {
  if (opt == null) return null;
  if (Array.isArray(opt)) {
    const [crossAxis, mainAxis] = opt;
    return offset3({ crossAxis, mainAxis });
  }
  if (typeof opt === "number") return offset3(opt);
  return offset3(opt);
}
function usePopoverPosition({
  allowFlip = true,
  allowShift = true,
  autoUpdateOptions,
  fitAvailableSpace = false,
  freeze = false,
  offset: offset22,
  placement = "bottom-start",
  shiftOptions
}) {
  const autoMw = autoMiddlewareFor(placement);
  const offsetMiddleware = toOffsetMw(offset22);
  const isSidePlacement = placement.startsWith("left") || placement.startsWith("right");
  const mergedShiftOptions = shiftOptions ? { padding: 8, ...shiftOptions } : { padding: 8 };
  const middleware = [
    // offset first (mirrors common Popper setups)
    ...offsetMiddleware ? [offsetMiddleware] : [],
    // choose between autoPlacement (Popper's "auto*") OR flip()
    // only allow flip when not explicitly 'left*' or 'right*'
    ...autoMw ? [autoMw] : allowFlip && !isSidePlacement ? [flip3()] : [],
    // viewport collision adjustments
    ...allowShift ? [shift3(mergedShiftOptions)] : [],
    // optional size constraining
    // eslint-disable-next-line @typescript-eslint/no-empty-function
    ...fitAvailableSpace ? [size3({ apply: () => {
    } })] : []
  ];
  const seedPlacement = String(placement).startsWith("auto") ? "bottom" : placement;
  return useFloating2({
    middleware,
    placement: seedPlacement,
    strategy: "fixed",
    whileElementsMounted: freeze ? void 0 : (reference, floating, update) => autoUpdate(reference, floating, update, {
      ancestorResize: true,
      ancestorScroll: true,
      animationFrame: false,
      elementResize: hasResizeObserver,
      ...autoUpdateOptions
    })
  });
}
var LegacyThreadContext = import_react4.default.createContext({ legacyThread: void 0 });
var useLegacyThreadContext = () => (0, import_react4.useContext)(LegacyThreadContext);
var cooldownTimerStateSelector = (state) => ({
  isCooldownActive: !!state.cooldownRemaining
});
var useIsCooldownActive = () => {
  const { channel } = useChannelStateContext();
  return useStateStore(channel.cooldownTimer.state, cooldownTimerStateSelector).isCooldownActive;
};
var queueCache = new FixedSizeQueueCache(64);
var useMessageComposerController = () => {
  const { client } = useChatContext();
  const { channel } = useChannelStateContext();
  const { legacyThread: parentMessage } = useLegacyThreadContext();
  const threadInstance = useThreadContext();
  const cachedParentMessage = (0, import_react4.useMemo)(() => {
    if (!parentMessage) return void 0;
    return parentMessage;
  }, [parentMessage?.id]);
  const messageComposer = (0, import_react4.useMemo)(() => {
    if (threadInstance) {
      return threadInstance.messageComposer;
    } else if (cachedParentMessage) {
      const compositionContext = {
        ...cachedParentMessage,
        legacyThreadId: cachedParentMessage.id
      };
      const tag = MessageComposer.constructTag(compositionContext);
      const cachedComposer = queueCache.get(tag);
      if (cachedComposer) return cachedComposer;
      return new MessageComposer({
        client,
        compositionContext
      });
    } else {
      return channel.messageComposer;
    }
  }, [cachedParentMessage, channel, client, threadInstance]);
  if (["legacy_thread", "message"].includes(
    messageComposer.contextType
  ) && !queueCache.peek(messageComposer.tag)) {
    queueCache.add(messageComposer.tag, messageComposer);
  }
  (0, import_react4.useEffect)(() => {
    const unsubscribe = messageComposer.registerSubscriptions();
    return () => {
      unsubscribe();
    };
  }, [messageComposer]);
  return messageComposer;
};
var CUSTOM_MESSAGE_TYPE = {
  date: "message.date",
  intro: "channel.intro"
};
var processMessages = (params) => {
  const { messages, reviewProcessedMessage, setGiphyPreviewMessage, ...context } = params;
  const {
    enableDateSeparator,
    hideDeletedMessages,
    hideNewMessageSeparator,
    lastRead,
    userId
  } = context;
  let unread = false;
  let ephemeralMessagePresent = false;
  let lastDateSeparator;
  const newMessages = [];
  for (let i2 = 0; i2 < messages.length; i2 += 1) {
    const message = messages[i2];
    if (hideDeletedMessages && message.type === "deleted") {
      continue;
    }
    if (setGiphyPreviewMessage && message.type === "ephemeral" && message.command === "giphy") {
      ephemeralMessagePresent = true;
      setGiphyPreviewMessage(message);
      continue;
    }
    const changes = [];
    const messageDate = message.created_at && isDate(message.created_at) && message.created_at.toDateString() || "";
    const previousMessage = messages[i2 - 1];
    let prevMessageDate = messageDate;
    if (enableDateSeparator && previousMessage?.created_at && isDate(previousMessage.created_at)) {
      prevMessageDate = previousMessage.created_at.toDateString();
    }
    if (!unread && !hideNewMessageSeparator) {
      unread = lastRead && message.created_at && new Date(lastRead) < message.created_at || false;
      if (enableDateSeparator && unread && message.user?.id !== userId) {
        changes.push({
          customType: CUSTOM_MESSAGE_TYPE.date,
          date: message.created_at,
          id: makeDateMessageId(message.created_at),
          unread
        });
      }
    }
    if (enableDateSeparator && (i2 === 0 || // always put date separator before the first message
    messageDate !== prevMessageDate || // add date separator btw. 2 messages created on different date
    // if hiding deleted messages replace the previous deleted message(s) with A separator if the last rendered message was created on different date
    hideDeletedMessages && previousMessage?.type === "deleted" && lastDateSeparator !== messageDate) && !isDateSeparatorMessage(changes[changes.length - 1])) {
      lastDateSeparator = messageDate;
      changes.push(
        {
          customType: CUSTOM_MESSAGE_TYPE.date,
          date: message.created_at,
          id: makeDateMessageId(message.created_at)
        },
        message
      );
    } else {
      changes.push(message);
    }
    newMessages.push(
      ...reviewProcessedMessage?.({
        changes,
        context,
        index: i2,
        messages,
        processedMessages: newMessages
      }) || changes
    );
  }
  if (setGiphyPreviewMessage && !ephemeralMessagePresent) {
    setGiphyPreviewMessage(void 0);
  }
  return newMessages;
};
var makeIntroMessage = () => ({
  customType: CUSTOM_MESSAGE_TYPE.intro,
  id: nanoid()
});
var makeDateMessageId = (date) => {
  let idSuffix;
  try {
    idSuffix = !date ? nanoid() : date instanceof Date ? date.toISOString() : date;
  } catch (e) {
    idSuffix = nanoid();
  }
  return `${CUSTOM_MESSAGE_TYPE.date}-${idSuffix}`;
};
var getLastReceived = (messages) => {
  for (let i2 = messages.length - 1; i2 > 0; i2 -= 1) {
    if (messages[i2].status === "received") {
      return messages[i2].id;
    }
  }
  return null;
};
var insertIntro = (messages, headerPosition) => {
  const newMessages = messages;
  const intro = makeIntroMessage();
  if (!headerPosition) {
    newMessages.unshift(intro);
    return newMessages;
  }
  if (!newMessages.length) {
    newMessages.unshift(intro);
    return newMessages;
  }
  for (let i2 = 0; i2 < messages.length; i2 += 1) {
    const messageTime = isDate(messages[i2].created_at) ? messages[i2].created_at.getTime() : null;
    const nextMessageTime = isDate(messages[i2 + 1].created_at) ? messages[i2 + 1].created_at.getTime() : null;
    if (messageTime && messageTime < headerPosition) {
      if (nextMessageTime && nextMessageTime < headerPosition) {
        if (messages[i2 + 1] && isDateSeparatorMessage(messages[i2 + 1])) continue;
        if (!nextMessageTime) {
          newMessages.push(intro);
          return newMessages;
        }
      } else {
        newMessages.splice(i2 + 1, 0, intro);
        return newMessages;
      }
    }
  }
  return newMessages;
};
var getGroupStyles = (message, previousMessage, nextMessage, noGroupByUser, maxTimeBetweenGroupedMessages) => {
  if (isDateSeparatorMessage(message) || isIntroMessage(message)) return "";
  if (noGroupByUser || message.attachments?.length !== 0) return "single";
  const isTopMessage = !previousMessage || isIntroMessage(previousMessage) || isDateSeparatorMessage(previousMessage) || previousMessage.type === "system" || previousMessage.type === "error" || previousMessage.attachments?.length !== 0 || message.user?.id !== previousMessage.user?.id || message.reaction_groups && Object.keys(message.reaction_groups).length > 0 || isMessageEdited(previousMessage) || maxTimeBetweenGroupedMessages !== void 0 && previousMessage.created_at && message.created_at && new Date(message.created_at).getTime() - new Date(previousMessage.created_at).getTime() > maxTimeBetweenGroupedMessages;
  const isBottomMessage = !nextMessage || isIntroMessage(nextMessage) || isDateSeparatorMessage(nextMessage) || nextMessage.type === "system" || nextMessage.type === "error" || nextMessage.attachments?.length !== 0 || message.user?.id !== nextMessage.user?.id || nextMessage.reaction_groups && Object.keys(nextMessage.reaction_groups).length > 0 || isMessageEdited(message) || maxTimeBetweenGroupedMessages !== void 0 && nextMessage.created_at && message.created_at && new Date(nextMessage.created_at).getTime() - new Date(message.created_at).getTime() > maxTimeBetweenGroupedMessages;
  if (!isTopMessage && !isBottomMessage) {
    if (message.type === "error") return "single";
    return "middle";
  }
  if (isBottomMessage) {
    if (isTopMessage || message.type === "error") return "single";
    return "bottom";
  }
  if (isTopMessage) return "top";
  return "";
};
var hasMoreMessagesProbably = (returnedCountMessages, limit) => returnedCountMessages >= limit;
function isIntroMessage(message) {
  return message.customType === CUSTOM_MESSAGE_TYPE.intro;
}
function isDateSeparatorMessage(message) {
  return message !== null && typeof message === "object" && message.customType === CUSTOM_MESSAGE_TYPE.date && isDate(message.date);
}
function isLocalMessage(message) {
  return !isDateSeparatorMessage(message) && !isIntroMessage(message);
}
var getIsFirstUnreadMessage = ({
  firstUnreadMessageId,
  isFirstMessage,
  lastReadDate,
  lastReadMessageId,
  message,
  previousMessage,
  unreadMessageCount = 0
}) => {
  if (message.parent_id) return false;
  const createdAtTimestamp = message.created_at && new Date(message.created_at).getTime();
  const lastReadTimestamp = lastReadDate?.getTime();
  const messageIsUnread = !!createdAtTimestamp && !!lastReadTimestamp && createdAtTimestamp > lastReadTimestamp;
  const previousMessageIsLastRead = !!lastReadMessageId && lastReadMessageId === previousMessage?.id;
  return firstUnreadMessageId === message.id || !!unreadMessageCount && messageIsUnread && (isFirstMessage || previousMessageIsLastRead);
};
var useImageFlagEmojisOnWindowsClass = () => {
  const { useImageFlagEmojisOnWindows } = useChatContext("Channel");
  return useImageFlagEmojisOnWindows && navigator.userAgent.match(/Win/) ? "str-chat--windows-flags" : "";
};
var getChatContainerClass = (customClass) => customClass ?? "str-chat__container";
var useChannelContainerClasses = ({
  customClasses
}) => {
  const windowsEmojiClass = useImageFlagEmojisOnWindowsClass();
  return {
    channelClass: customClasses?.channel ?? "str-chat__channel",
    chatClass: customClasses?.chat ?? "str-chat",
    chatContainerClass: getChatContainerClass(customClasses?.chatContainer),
    windowsEmojiClass
  };
};
var findInMsgSetById = (targetId, msgSet) => {
  for (let i2 = msgSet.length - 1; i2 >= 0; i2--) {
    const item = msgSet[i2];
    if (item.id === targetId) {
      return {
        index: i2,
        target: item
      };
    }
  }
  return {
    index: -1
  };
};
var findInMsgSetByDate = (targetDate, msgSet, exact = false) => {
  const targetTimestamp = targetDate.getTime();
  let left = 0;
  let middle = 0;
  let right = msgSet.length - 1;
  while (left <= right) {
    middle = Math.floor((right + left) / 2);
    const middleTimestamp = new Date(
      msgSet[middle].created_at
    ).getTime();
    const middleLeftTimestamp = msgSet[middle - 1]?.created_at && new Date(msgSet[middle - 1].created_at).getTime();
    const middleRightTimestamp = msgSet[middle + 1]?.created_at && new Date(msgSet[middle + 1].created_at).getTime();
    if (middleTimestamp === targetTimestamp || middleLeftTimestamp && middleRightTimestamp && middleLeftTimestamp < targetTimestamp && targetTimestamp < middleRightTimestamp) {
      return { index: middle, target: msgSet[middle] };
    }
    if (middleTimestamp < targetTimestamp) left = middle + 1;
    else right = middle - 1;
  }
  if (!exact || new Date(msgSet[left].created_at).getTime() === targetTimestamp) {
    return { index: left, target: msgSet[left] };
  }
  return { index: -1 };
};
var adaptMessageSendErrorToErrorFromResponse = (error2) => {
  if (error2 instanceof ErrorFromResponse) {
    return error2;
  }
  const fallbackMessage = error2 instanceof Error ? error2.message : "Message send failed";
  let message = fallbackMessage;
  let status = 0;
  let code4 = null;
  if (typeof error2 === "object" && error2 !== null) {
    const maybeAxiosError = error2;
    if (maybeAxiosError.name === "AxiosError" && maybeAxiosError.code === "ERR_NETWORK") {
      message = typeof maybeAxiosError.message === "string" ? maybeAxiosError.message : "Network Error";
      status = maybeAxiosError.response?.status ?? 0;
      return new ErrorFromResponse(message, {
        code: null,
        response: maybeAxiosError.response ?? {
          // Compatibility shim: this is an intentionally incomplete AxiosResponse-like object.
          data: {
            duration: "",
            message,
            more_info: "",
            StatusCode: status
          },
          status
        },
        status
      });
    }
    try {
      const stringError = JSON.stringify(error2);
      const parsedError = stringError ? JSON.parse(stringError) : {};
      if (typeof parsedError.message === "string") {
        message = parsedError.message;
      }
      if (typeof parsedError.status === "number") {
        status = parsedError.status;
      }
      if (typeof parsedError.code === "number") {
        code4 = parsedError.code;
      }
    } catch {
    }
  }
  return new ErrorFromResponse(message, {
    code: code4,
    response: {
      // Compatibility shim: this is an intentionally incomplete AxiosResponse-like object.
      data: {
        duration: "",
        message,
        more_info: "",
        StatusCode: status
      },
      status
    },
    status
  });
};
var WATCH_QUERY_IN_PROGRESS_FOR_CHANNEL = {};
var getChannel = async ({
  channel,
  client,
  id: id3,
  members,
  options,
  type
}) => {
  if (!channel && !type) {
    throw new Error("Channel or channel type have to be provided to query a channel.");
  }
  const theChannel = channel || client.channel(type, id3, { members });
  const originalCid = theChannel?.id ? theChannel.cid : members && members.length ? generateChannelTempCid(theChannel.type, members) : void 0;
  if (!originalCid) {
    throw new Error(
      "Channel ID or channel members array have to be provided to query a channel."
    );
  }
  const queryPromise = WATCH_QUERY_IN_PROGRESS_FOR_CHANNEL[originalCid];
  if (queryPromise) {
    await queryPromise;
  } else {
    try {
      WATCH_QUERY_IN_PROGRESS_FOR_CHANNEL[originalCid] = theChannel.watch(options);
      await WATCH_QUERY_IN_PROGRESS_FOR_CHANNEL[originalCid];
    } finally {
      delete WATCH_QUERY_IN_PROGRESS_FOR_CHANNEL[originalCid];
    }
  }
  return theChannel;
};
var generateChannelTempCid = (channelType, members) => {
  if (!members) return;
  const membersStr = [...members].sort().join(",");
  return `${channelType}:!members-${membersStr}`;
};
var getImageAttachmentConfiguration = (attachment, element4) => {
  let newUrl = void 0;
  const urlToTest = attachment.image_url || attachment.thumb_url || "";
  if (test(urlToTest, "url")) {
    const url = new URL(urlToTest);
    const resizeDimensions = getSizingRestrictions(url, element4);
    if (resizeDimensions) {
      resizeDimensions.height *= 2;
      resizeDimensions.width *= 2;
      addResizingParamsToUrl(resizeDimensions, url);
    }
    newUrl = url.href;
  }
  return {
    url: newUrl || ""
  };
};
var getVideoAttachmentConfiguration = (attachment, element4, shouldGenerateVideoThumbnail) => {
  let thumbUrl = void 0;
  if (attachment.thumb_url && shouldGenerateVideoThumbnail && test(attachment.thumb_url, "url")) {
    const url = new URL(attachment.thumb_url);
    const resizeDimensions = getSizingRestrictions(url, element4);
    if (resizeDimensions) {
      resizeDimensions.height *= 2;
      resizeDimensions.width *= 2;
      addResizingParamsToUrl(resizeDimensions, url);
    }
    thumbUrl = url.href;
  }
  return {
    thumbUrl,
    url: attachment.asset_url || ""
  };
};
var getSizingRestrictions = (url, htmlElement) => {
  const urlParams = url.searchParams;
  const originalHeight = Number(urlParams.get("oh")) || 1;
  const originalWidth = Number(urlParams.get("ow")) || 1;
  const cssSizeRestriction = getCSSSizeRestrictions(htmlElement);
  let resizeDimensions;
  if ((cssSizeRestriction.maxHeight || cssSizeRestriction.height) && cssSizeRestriction.maxWidth) {
    resizeDimensions = getResizeDimensions(
      originalHeight,
      originalWidth,
      /* eslint-disable-next-line @typescript-eslint/no-non-null-assertion */
      cssSizeRestriction.maxHeight || cssSizeRestriction.height,
      cssSizeRestriction.maxWidth
    );
  } else {
    resizeDimensions = void 0;
  }
  return resizeDimensions;
};
var getResizeDimensions = (originalHeight, originalWidth, maxHeight, maxWidth) => ({
  height: Math.round(Math.max(maxHeight, maxWidth / originalWidth * originalHeight)),
  width: Math.round(Math.max(maxHeight, maxWidth / originalHeight * originalWidth))
});
var getCSSSizeRestrictions = (htmlElement) => {
  const computedStylesheet = getComputedStyle(htmlElement);
  const height = getValueRepresentationOfCSSProperty(
    computedStylesheet.getPropertyValue("height")
  );
  const maxHeight = getValueRepresentationOfCSSProperty(
    computedStylesheet.getPropertyValue("max-height")
  );
  const maxWidth = getValueRepresentationOfCSSProperty(
    computedStylesheet.getPropertyValue("max-width")
  );
  if (!((height || maxHeight) && maxWidth)) {
    console.warn(
      `Invalid value set for height/max-height and/or max-width for HTML element, this can cause scrolling issues inside the message list, more info https://getstream.io/chat/docs/sdk/react/message-components/attachment/#image-and-video-sizing`
    );
  }
  return { height, maxHeight, maxWidth };
};
var getValueRepresentationOfCSSProperty = (property) => {
  if (!property.endsWith("px")) {
    return void 0;
  }
  const number2 = parseFloat(property);
  return isNaN(number2) ? void 0 : number2;
};
var addResizingParamsToUrl = (resizeDimensions, url) => {
  url.searchParams.set("h", resizeDimensions.height.toString());
  url.searchParams.set("w", resizeDimensions.width.toString());
};
var searchControllerStateSelector = (nextValue) => ({
  focusedMessage: nextValue.focusedMessage
});
var useSearchFocusedMessage = () => {
  const { searchController } = useChatContext("Channel");
  const { focusedMessage } = useStateStore(
    searchController._internalState,
    searchControllerStateSelector
  );
  return focusedMessage;
};
var ChannelContainer = ({
  children,
  className: additionalClassName,
  ...props
}) => {
  const { customClasses, theme } = useChatContext("Channel");
  const { channelClass, chatClass } = useChannelContainerClasses({
    customClasses
  });
  const className = clsx_default(chatClass, theme, channelClass, additionalClassName);
  return (0, import_jsx_runtime2.jsx)("div", { id: CHANNEL_CONTAINER_ID, ...props, className, children });
};
var UnMemoizedChannel = (props) => {
  const { channel: propsChannel, EmptyPlaceholder: EmptyPlaceholder2 } = props;
  const {
    EmptyStateIndicator: EmptyStateIndicator$1 = EmptyStateIndicator,
    LoadingErrorIndicator: LoadingErrorIndicator2,
    LoadingIndicator: LoadingIndicator2 = LoadingChannel
  } = useComponentContext();
  const { channel: contextChannel, channelsQueryState } = useChatContext("Channel");
  const channel = propsChannel || contextChannel;
  const emptyPlaceholder = "EmptyPlaceholder" in props ? EmptyPlaceholder2 : EmptyStateIndicator$1 && (0, import_jsx_runtime2.jsx)(EmptyStateIndicator$1, { listType: "message" });
  if (channelsQueryState.queryInProgress === "reload" && LoadingIndicator2) {
    return (0, import_jsx_runtime2.jsx)(ChannelContainer, { children: (0, import_jsx_runtime2.jsx)(LoadingIndicator2, {}) });
  }
  if (channelsQueryState.error && LoadingErrorIndicator2) {
    return (0, import_jsx_runtime2.jsx)(ChannelContainer, { children: (0, import_jsx_runtime2.jsx)(LoadingErrorIndicator2, { error: channelsQueryState.error }) });
  }
  if (channelsQueryState.error) {
    return (0, import_jsx_runtime2.jsx)(ChannelContainer, {});
  }
  if (!channel?.cid) {
    return (0, import_jsx_runtime2.jsx)(ChannelContainer, { children: emptyPlaceholder });
  }
  return (0, import_react4.createElement)(ChannelInner, { ...props, channel, key: channel.cid });
};
var ChannelInner = (props) => {
  const {
    activeUnreadHandler,
    allowConcurrentAudioPlayback,
    channel,
    channelQueryOptions,
    children,
    doDeleteMessageRequest,
    doMarkReadRequest,
    doSendMessageRequest,
    doUpdateMessageRequest,
    initializeOnMount = true,
    markReadOnMount = true,
    onMentionsClick,
    onMentionsHover,
    skipMessageDataMemoization
  } = props;
  const {
    LoadingErrorIndicator: LoadingErrorIndicator$1 = LoadingErrorIndicator,
    LoadingIndicator: LoadingIndicator2 = LoadingChannel
  } = useComponentContext();
  const { client, customClasses, latestMessageDatesByChannels, mutes, searchController } = useChatContext("Channel");
  const { addNotification } = useNotificationApi();
  const { t: t2 } = useTranslationContext("Channel");
  const chatContainerClass = getChatContainerClass(customClasses?.chatContainer);
  const windowsEmojiClass = useImageFlagEmojisOnWindowsClass();
  const thread = useThreadContext();
  const [channelConfig, setChannelConfig] = (0, import_react4.useState)(channel.getConfig());
  const [channelUnreadUiState, _setChannelUnreadUiState] = (0, import_react4.useState)();
  const channelReducer = (0, import_react4.useMemo)(() => makeChannelReducer(), []);
  const [state, dispatch] = (0, import_react4.useReducer)(
    channelReducer,
    // channel.initialized === false if client.channel().query() was not called, e.g. ChannelList is not used
    // => Channel will call channel.watch() in useLayoutEffect => state.loading is used to signal the watch() call state
    {
      ...initialState,
      hasMore: channel.state.messagePagination.hasPrev,
      loading: !channel.initialized,
      messages: channel.state.messages
    }
  );
  const jumpToMessageFromSearch = useSearchFocusedMessage();
  const isMounted = useIsMounted();
  const originalTitle = (0, import_react4.useRef)("");
  const lastRead = (0, import_react4.useRef)(void 0);
  const online = (0, import_react4.useRef)(true);
  const clearHighlightedMessageTimeoutId = (0, import_react4.useRef)(
    null
  );
  const channelCapabilitiesArray = channel.data?.own_capabilities;
  const throttledCopyStateFromChannel = (0, import_lodash.default)(
    () => dispatch({ channel, type: "copyStateFromChannelOnEvent" }),
    500,
    {
      leading: true,
      trailing: true
    }
  );
  const setChannelUnreadUiState = (0, import_react4.useMemo)(
    () => (0, import_lodash.default)(_setChannelUnreadUiState, 200, {
      leading: true,
      trailing: false
    }),
    []
  );
  const markRead = (0, import_react4.useMemo)(
    () => (0, import_lodash.default)(
      async (options) => {
        const { updateChannelUiUnreadState = true } = options ?? {};
        if (channel.disconnected || !channelConfig?.read_events) {
          return;
        }
        lastRead.current = /* @__PURE__ */ new Date();
        try {
          if (doMarkReadRequest) {
            doMarkReadRequest(
              channel,
              updateChannelUiUnreadState ? setChannelUnreadUiState : void 0
            );
          } else {
            const markReadResponse = await channel.markRead();
            if (updateChannelUiUnreadState && markReadResponse?.event) {
              _setChannelUnreadUiState({
                last_read: lastRead.current,
                last_read_message_id: markReadResponse.event.last_read_message_id,
                unread_messages: 0
              });
            }
          }
          if (activeUnreadHandler) {
            activeUnreadHandler(0, originalTitle.current);
          } else if (originalTitle.current) {
            document.title = originalTitle.current;
          }
        } catch (e) {
          console.error(t2("Failed to mark channel as read"));
        }
      },
      500,
      { leading: true, trailing: false }
    ),
    [
      activeUnreadHandler,
      channel,
      channelConfig,
      doMarkReadRequest,
      setChannelUnreadUiState,
      t2
    ]
  );
  const handleEvent = async (event) => {
    if (event.message) {
      dispatch({
        channel,
        message: event.message,
        type: "updateThreadOnEvent"
      });
    }
    if (event.type === "user.messages.deleted" && event.cid && event.cid !== channel.cid)
      return;
    if (event.type === "user.watching.start" || event.type === "user.watching.stop")
      return;
    if (event.type === "typing.start" || event.type === "typing.stop") {
      return dispatch({ channel, type: "setTyping" });
    }
    if (event.type === "connection.changed" && typeof event.online === "boolean") {
      online.current = event.online;
    }
    if (event.type === "message.new") {
      const mainChannelUpdated = !event.message?.parent_id || event.message?.show_in_channel;
      if (mainChannelUpdated) {
        if (document.hidden && channelConfig?.read_events && !channel.muteStatus().muted) {
          const unread = channel.countUnread(lastRead.current);
          if (activeUnreadHandler) {
            activeUnreadHandler(unread, originalTitle.current);
          } else {
            document.title = `(${unread}) ${originalTitle.current}`;
          }
        }
      }
      if (event.message?.user?.id === client.userID && event?.message?.created_at && event?.message?.cid) {
        const messageDate = new Date(event.message.created_at);
        const cid = event.message.cid;
        if (!latestMessageDatesByChannels[cid] || latestMessageDatesByChannels[cid].getTime() < messageDate.getTime()) {
          latestMessageDatesByChannels[cid] = messageDate;
        }
      }
    }
    if (event.type === "user.deleted") {
      const oldestID = channel.state?.messages?.[0]?.id;
      await channel.query({
        messages: { id_lt: oldestID, limit: DEFAULT_NEXT_CHANNEL_PAGE_SIZE },
        watchers: { limit: DEFAULT_NEXT_CHANNEL_PAGE_SIZE }
      });
    }
    if (event.type === "notification.mark_unread")
      _setChannelUnreadUiState((prev) => {
        if (!(event.last_read_at && event.user)) return prev;
        return {
          first_unread_message_id: event.first_unread_message_id,
          last_read: new Date(event.last_read_at),
          last_read_message_id: event.last_read_message_id,
          unread_messages: event.unread_messages ?? 0
        };
      });
    if (event.type === "channel.truncated" && event.cid === channel.cid) {
      _setChannelUnreadUiState(void 0);
    }
    throttledCopyStateFromChannel();
  };
  (0, import_react4.useLayoutEffect)(() => {
    let errored = false;
    let done = false;
    (async () => {
      if (!channel.initialized && initializeOnMount) {
        try {
          const members = [];
          if (!channel.id && channel.data?.members) {
            for (const member of channel.data.members) {
              let userId;
              if (typeof member === "string") {
                userId = member;
              } else if (typeof member === "object") {
                const { user, user_id } = member;
                userId = user_id || user?.id;
              }
              if (userId) {
                members.push(userId);
              }
            }
          }
          await getChannel({ channel, client, members, options: channelQueryOptions });
          const config = channel.getConfig();
          setChannelConfig(config);
        } catch (e) {
          dispatch({ error: e, type: "setError" });
          errored = true;
        }
      }
      done = true;
      originalTitle.current = document.title;
      if (!errored) {
        dispatch({
          channel,
          hasMore: channel.state.messagePagination.hasPrev,
          type: "initStateFromChannel"
        });
        if (client.user?.id && channel.state.read[client.user.id]) {
          const { user, ...ownReadState } = channel.state.read[client.user.id];
          _setChannelUnreadUiState(ownReadState);
        }
        if (channel.countUnread() > 0 && markReadOnMount)
          markRead({ updateChannelUiUnreadState: false });
        client.on("connection.changed", handleEvent);
        client.on("connection.recovered", handleEvent);
        client.on("user.updated", handleEvent);
        client.on("user.deleted", handleEvent);
        client.on("user.messages.deleted", handleEvent);
        channel.on(handleEvent);
      }
    })();
    return () => {
      if (errored || !done) return;
      channel?.off(handleEvent);
      client.off("connection.changed", handleEvent);
      client.off("connection.recovered", handleEvent);
      client.off("user.deleted", handleEvent);
    };
  }, [
    channel.cid,
    channelQueryOptions,
    doMarkReadRequest,
    channelConfig?.read_events,
    initializeOnMount
  ]);
  (0, import_react4.useEffect)(() => {
    if (!state.thread) return;
    const message = state.messages?.find((m2) => m2.id === state.thread?.id);
    if (message) dispatch({ message, type: "setThread" });
  }, [state.messages, state.thread]);
  const handleHighlightedMessageChange = (0, import_react4.useCallback)(
    ({
      highlightDuration,
      highlightedMessageId
    }) => {
      dispatch({
        channel,
        highlightedMessageId,
        type: "jumpToMessageFinished"
      });
      if (clearHighlightedMessageTimeoutId.current) {
        clearTimeout(clearHighlightedMessageTimeoutId.current);
      }
      clearHighlightedMessageTimeoutId.current = setTimeout(() => {
        if (searchController._internalState.getLatestValue().focusedMessage) {
          searchController._internalState.partialNext({ focusedMessage: void 0 });
        }
        clearHighlightedMessageTimeoutId.current = null;
        dispatch({ type: "clearHighlightedMessage" });
      }, highlightDuration ?? DEFAULT_HIGHLIGHT_DURATION);
    },
    [channel, searchController]
  );
  (0, import_react4.useEffect)(() => {
    if (!jumpToMessageFromSearch?.id) return;
    handleHighlightedMessageChange({ highlightedMessageId: jumpToMessageFromSearch.id });
  }, [jumpToMessageFromSearch, handleHighlightedMessageChange]);
  const notifyJumpToFirstUnreadError = (0, import_react4.useCallback)(() => {
    addNotification({
      context: { feature: "jumpToFirstUnread" },
      emitter: "Channel",
      message: t2("Failed to jump to the first unread message"),
      severity: "error",
      targetPanels: ["channel"],
      type: "channel:jumpToFirstUnread:failed"
    });
  }, [addNotification, t2]);
  const loadMoreFinished = (0, import_react4.useCallback)(
    (0, import_lodash2.default)(
      (hasMore, messages) => {
        if (!isMounted.current) return;
        dispatch({ hasMore, messages, type: "loadMoreFinished" });
      },
      2e3,
      { leading: true, trailing: true }
    ),
    []
  );
  const finishLoadMore = (0, import_react4.useCallback)(
    (hasMore, messages) => {
      if (!isMounted.current) return;
      dispatch({ hasMore, messages, type: "loadMoreFinished" });
    },
    [isMounted]
  );
  const loadMore = async (limit = DEFAULT_NEXT_CHANNEL_PAGE_SIZE) => {
    if (!online.current || !window.navigator.onLine || !channel.state.messagePagination.hasPrev)
      return 0;
    const oldestMessage = state?.messages?.[0];
    if (state.loadingMore || state.loadingMoreNewer || oldestMessage?.status !== "received") {
      return 0;
    }
    dispatch({ loadingMore: true, type: "setLoadingMore" });
    const oldestID = oldestMessage?.id;
    const perPage = limit;
    let queryResponse;
    try {
      queryResponse = await channel.query({
        messages: { id_lt: oldestID, limit: perPage },
        watchers: { limit: perPage }
      });
    } catch (e) {
      console.warn("message pagination request failed with error", e);
      dispatch({ loadingMore: false, type: "setLoadingMore" });
      return 0;
    }
    loadMoreFinished.cancel();
    finishLoadMore(channel.state.messagePagination.hasPrev, channel.state.messages);
    return queryResponse.messages.length;
  };
  const loadMoreNewer = async (limit = DEFAULT_NEXT_CHANNEL_PAGE_SIZE) => {
    if (!online.current || !window.navigator.onLine || !channel.state.messagePagination.hasNext)
      return 0;
    const newestMessage = state?.messages?.[state?.messages?.length - 1];
    if (state.loadingMore || state.loadingMoreNewer) return 0;
    dispatch({ loadingMoreNewer: true, type: "setLoadingMoreNewer" });
    const newestId = newestMessage?.id;
    const perPage = limit;
    let queryResponse;
    try {
      queryResponse = await channel.query({
        messages: { id_gt: newestId, limit: perPage },
        watchers: { limit: perPage }
      });
    } catch (e) {
      console.warn("message pagination request failed with error", e);
      dispatch({ loadingMoreNewer: false, type: "setLoadingMoreNewer" });
      return 0;
    }
    dispatch({
      hasMoreNewer: channel.state.messagePagination.hasNext,
      messages: channel.state.messages,
      type: "loadMoreNewerFinished"
    });
    return queryResponse.messages.length;
  };
  const jumpToMessage = (0, import_react4.useCallback)(
    async (messageId, messageLimit = DEFAULT_JUMP_TO_PAGE_SIZE, highlightDuration = DEFAULT_HIGHLIGHT_DURATION) => {
      dispatch({
        loadingMoreForJumpToChannelMessage: true,
        type: "setLoadingMoreForJumpToChannelMessage"
      });
      loadMoreFinished.cancel();
      try {
        await channel.state.loadMessageIntoState(messageId, void 0, messageLimit);
        handleHighlightedMessageChange({
          highlightDuration,
          highlightedMessageId: messageId
        });
      } catch (error2) {
        dispatch({
          loadingMoreForJumpToChannelMessage: false,
          type: "setLoadingMoreForJumpToChannelMessage"
        });
        throw error2;
      }
    },
    [channel, handleHighlightedMessageChange, loadMoreFinished]
  );
  const jumpToLatestMessage = (0, import_react4.useCallback)(async () => {
    loadMoreFinished.cancel();
    await channel.state.loadMessageIntoState("latest");
    dispatch({
      hasMore: channel.state.messagePagination.hasPrev,
      hasMoreNewer: channel.state.messagePagination.hasNext,
      messages: channel.state.messages,
      type: "jumpToLatestMessageFinished"
    });
  }, [channel, loadMoreFinished]);
  const jumpToFirstUnreadMessage = (0, import_react4.useCallback)(
    async (queryMessageLimit = DEFAULT_JUMP_TO_PAGE_SIZE, highlightDuration = DEFAULT_HIGHLIGHT_DURATION) => {
      if (!channelUnreadUiState?.unread_messages) return;
      let lastReadMessageId = channelUnreadUiState?.last_read_message_id;
      let firstUnreadMessageId = channelUnreadUiState?.first_unread_message_id;
      let isInCurrentMessageSet = false;
      if (firstUnreadMessageId) {
        const result = findInMsgSetById(firstUnreadMessageId, channel.state.messages);
        isInCurrentMessageSet = result.index !== -1;
      } else if (lastReadMessageId) {
        const result = findInMsgSetById(lastReadMessageId, channel.state.messages);
        isInCurrentMessageSet = !!result.target;
        firstUnreadMessageId = result.index > -1 ? channel.state.messages[result.index + 1]?.id : void 0;
      } else {
        const lastReadTimestamp = channelUnreadUiState.last_read.getTime();
        const { index: lastReadMessageIndex, target: lastReadMessage } = findInMsgSetByDate(
          channelUnreadUiState.last_read,
          channel.state.messages,
          true
        );
        if (lastReadMessage) {
          firstUnreadMessageId = channel.state.messages[lastReadMessageIndex + 1]?.id;
          isInCurrentMessageSet = !!firstUnreadMessageId;
          lastReadMessageId = lastReadMessage.id;
        } else {
          dispatch({ loadingMore: true, type: "setLoadingMore" });
          let messages;
          try {
            messages = (await channel.query(
              {
                messages: {
                  created_at_around: channelUnreadUiState.last_read.toISOString(),
                  limit: queryMessageLimit
                }
              },
              "new"
            )).messages;
          } catch (e) {
            notifyJumpToFirstUnreadError();
            finishLoadMore(
              channel.state.messagePagination.hasPrev,
              channel.state.messages
            );
            return;
          }
          const firstMessageWithCreationDate = messages.find((msg) => msg.created_at);
          if (!firstMessageWithCreationDate) {
            notifyJumpToFirstUnreadError();
            finishLoadMore(
              channel.state.messagePagination.hasPrev,
              channel.state.messages
            );
            return;
          }
          const firstMessageTimestamp = new Date(
            firstMessageWithCreationDate.created_at
          ).getTime();
          if (lastReadTimestamp < firstMessageTimestamp) {
            firstUnreadMessageId = firstMessageWithCreationDate.id;
          } else {
            const result = findInMsgSetByDate(channelUnreadUiState.last_read, messages);
            lastReadMessageId = result.target?.id;
          }
          finishLoadMore(
            channel.state.messagePagination.hasPrev,
            channel.state.messages
          );
        }
      }
      if (!firstUnreadMessageId && !lastReadMessageId) {
        notifyJumpToFirstUnreadError();
        return;
      }
      if (!isInCurrentMessageSet) {
        dispatch({ loadingMore: true, type: "setLoadingMore" });
        try {
          const targetId = firstUnreadMessageId ?? lastReadMessageId;
          await channel.state.loadMessageIntoState(
            targetId,
            void 0,
            queryMessageLimit
          );
          const indexOfTarget = channel.state.messages.findIndex(
            (message) => message.id === targetId
          );
          finishLoadMore(
            channel.state.messagePagination.hasPrev,
            channel.state.messages
          );
          firstUnreadMessageId = firstUnreadMessageId ?? channel.state.messages[indexOfTarget + 1]?.id;
        } catch (e) {
          notifyJumpToFirstUnreadError();
          finishLoadMore(
            channel.state.messagePagination.hasPrev,
            channel.state.messages
          );
          return;
        }
      }
      if (!firstUnreadMessageId) {
        notifyJumpToFirstUnreadError();
        return;
      }
      if (!channelUnreadUiState.first_unread_message_id)
        _setChannelUnreadUiState({
          ...channelUnreadUiState,
          first_unread_message_id: firstUnreadMessageId,
          last_read_message_id: lastReadMessageId
        });
      handleHighlightedMessageChange({
        highlightDuration,
        highlightedMessageId: firstUnreadMessageId
      });
    },
    [
      channel,
      finishLoadMore,
      handleHighlightedMessageChange,
      notifyJumpToFirstUnreadError,
      channelUnreadUiState
    ]
  );
  const deleteMessage = (0, import_react4.useCallback)(
    async (message, options) => {
      if (!message?.id) {
        throw new Error("Cannot delete a message - missing message ID.");
      }
      let deletedMessage;
      if (doDeleteMessageRequest) {
        deletedMessage = await doDeleteMessageRequest(message, options);
      } else {
        const result = await client.deleteMessage(message.id, options);
        deletedMessage = result.message;
      }
      return deletedMessage;
    },
    [client, doDeleteMessageRequest]
  );
  const updateMessage = (updatedMessage) => {
    channel.state.addMessageSorted(updatedMessage, true);
    dispatch({
      channel,
      parentId: state.thread && updatedMessage.parent_id,
      type: "copyMessagesFromChannel"
    });
  };
  const doSendMessage = async ({
    localMessage,
    message,
    options
  }) => {
    try {
      let messageResponse;
      if (doSendMessageRequest) {
        messageResponse = await doSendMessageRequest(channel, message, options);
      } else {
        messageResponse = await channel.sendMessage(message, options);
      }
      let existingMessage = void 0;
      for (let i2 = channel.state.messages.length - 1; i2 >= 0; i2--) {
        const msg = channel.state.messages[i2];
        if (msg.id && msg.id === message.id) {
          existingMessage = msg;
          break;
        }
      }
      const responseTimestamp = new Date(
        messageResponse?.message?.updated_at || 0
      ).getTime();
      const existingMessageTimestamp = existingMessage?.updated_at?.getTime() || 0;
      const responseIsTheNewest = responseTimestamp > existingMessageTimestamp;
      if (messageResponse?.message && (responseIsTheNewest || existingMessage?.status === "sending")) {
        updateMessage({
          ...messageResponse.message,
          status: "received"
        });
      }
    } catch (error2) {
      const parsedError = adaptMessageSendErrorToErrorFromResponse(error2);
      if (parsedError.code === 4 && error2 instanceof Error && error2.message.includes("already exists")) {
        updateMessage({
          ...localMessage,
          status: "received"
        });
      } else {
        updateMessage({
          ...localMessage,
          error: parsedError,
          status: "failed"
        });
        thread?.upsertReplyLocally({
          message: {
            ...localMessage,
            error: parsedError,
            status: "failed"
          }
        });
      }
    }
  };
  const sendMessage = async ({
    localMessage,
    message,
    options
  }) => {
    channel.state.filterErrorMessages();
    thread?.upsertReplyLocally({
      message: localMessage
    });
    updateMessage(localMessage);
    await doSendMessage({ localMessage, message, options });
  };
  const retrySendMessage = async (localMessage) => {
    const type = localMessage.type === "error" ? "regular" : localMessage.type;
    updateMessage({
      ...localMessage,
      error: void 0,
      status: "sending",
      type
    });
    await doSendMessage({
      localMessage,
      message: localMessageToNewMessagePayload({ ...localMessage, type })
    });
  };
  const removeMessage = (message) => {
    channel.state.removeMessage(message);
    dispatch({
      channel,
      parentId: state.thread && message.parent_id,
      type: "copyMessagesFromChannel"
    });
  };
  const openThread = (message, event) => {
    event?.preventDefault();
    dispatch({ channel, message, type: "openThread" });
  };
  const closeThread = (event) => {
    event?.preventDefault();
    dispatch({ type: "closeThread" });
  };
  const loadMoreThreadFinished = (0, import_react4.useCallback)(
    (0, import_lodash2.default)(
      (threadHasMore, threadMessages) => {
        dispatch({
          threadHasMore,
          threadMessages,
          type: "loadMoreThreadFinished"
        });
      },
      2e3,
      { leading: true, trailing: true }
    ),
    []
  );
  const loadMoreThread = async (limit = DEFAULT_THREAD_PAGE_SIZE) => {
    if (state.threadLoadingMore || !state.thread || !state.threadHasMore) return;
    dispatch({ type: "startLoadingThread" });
    const parentId = state.thread.id;
    if (!parentId) {
      return dispatch({ type: "closeThread" });
    }
    const oldMessages = channel.state.threads[parentId] || [];
    const oldestMessageId = oldMessages[0]?.id;
    try {
      const queryResponse = await channel.getReplies(parentId, {
        id_lt: oldestMessageId,
        limit
      });
      const threadHasMoreMessages = hasMoreMessagesProbably(
        queryResponse.messages.length,
        limit
      );
      const newThreadMessages = channel.state.threads[parentId] || [];
      loadMoreThreadFinished(threadHasMoreMessages, newThreadMessages);
    } catch (e) {
      loadMoreThreadFinished(false, oldMessages);
    }
  };
  const onMentionsHoverOrClick = useMentionsHandlers(onMentionsHover, onMentionsClick);
  const editMessage = useEditMessageHandler(doUpdateMessageRequest);
  const { typing, ...restState } = state;
  const channelStateContextValue = useCreateChannelStateContext({
    ...restState,
    channel,
    channelCapabilitiesArray,
    channelConfig,
    channelUnreadUiState,
    giphyVersion: props.giphyVersion || "fixed_height",
    imageAttachmentSizeHandler: props.imageAttachmentSizeHandler || getImageAttachmentConfiguration,
    mutes,
    notifications: [],
    shouldGenerateVideoThumbnail: props.shouldGenerateVideoThumbnail || true,
    videoAttachmentSizeHandler: props.videoAttachmentSizeHandler || getVideoAttachmentConfiguration,
    watcher_count: state.watcherCount
  });
  const channelActionContextValue = (0, import_react4.useMemo)(
    () => ({
      closeThread,
      deleteMessage,
      dispatch,
      editMessage,
      jumpToFirstUnreadMessage,
      jumpToLatestMessage,
      jumpToMessage,
      loadMore,
      loadMoreNewer,
      loadMoreThread,
      markRead,
      onMentionsClick: onMentionsHoverOrClick,
      onMentionsHover: onMentionsHoverOrClick,
      openThread,
      removeMessage,
      retrySendMessage,
      sendMessage,
      setChannelUnreadUiState,
      skipMessageDataMemoization,
      updateMessage
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      channel.cid,
      deleteMessage,
      loadMore,
      loadMoreNewer,
      markRead,
      jumpToFirstUnreadMessage,
      jumpToMessage,
      jumpToLatestMessage,
      setChannelUnreadUiState
    ]
  );
  const typingContextValue = useCreateTypingContext({
    typing
  });
  if (state.error) {
    return (0, import_jsx_runtime2.jsx)(ChannelContainer, { children: (0, import_jsx_runtime2.jsx)(LoadingErrorIndicator$1, { error: state.error }) });
  }
  if (state.loading) {
    return (0, import_jsx_runtime2.jsx)(ChannelContainer, { children: (0, import_jsx_runtime2.jsx)(LoadingIndicator2, {}) });
  }
  if (!channel.watch) {
    return (0, import_jsx_runtime2.jsx)(ChannelContainer, { children: (0, import_jsx_runtime2.jsx)("div", { children: t2("Channel Missing") }) });
  }
  return (0, import_jsx_runtime2.jsx)(ChannelContainer, { className: windowsEmojiClass, children: (0, import_jsx_runtime2.jsx)(ChannelStateProvider, { value: channelStateContextValue, children: (0, import_jsx_runtime2.jsx)(ChannelActionProvider, { value: channelActionContextValue, children: (0, import_jsx_runtime2.jsx)(TypingProvider, { value: typingContextValue, children: (0, import_jsx_runtime2.jsx)(WithAudioPlayback, { allowConcurrentPlayback: allowConcurrentAudioPlayback, children: (0, import_jsx_runtime2.jsx)("div", { className: clsx_default(chatContainerClass), children }) }) }) }) }) });
};
var Channel = import_react4.default.memo(UnMemoizedChannel);
var ThreadContext = (0, import_react4.createContext)(void 0);
var useThreadContext = () => (0, import_react4.useContext)(ThreadContext);
var ThreadProvider = ({
  children,
  thread
}) => (0, import_jsx_runtime2.jsx)(ThreadContext.Provider, { value: thread, children: (0, import_jsx_runtime2.jsx)(Channel, { channel: thread?.channel, children }) });
var UnreadCountBadge = ({
  children,
  count: count2,
  position: position3
}) => (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__unread-count-badge-container", children: [
  children,
  count2 > 0 && (0, import_jsx_runtime2.jsx)(
    "div",
    {
      className: clsx_default(
        "str-chat__unread-count-badge",
        position3 && `str-chat__unread-count-badge--${position3}`
      ),
      children: count2
    }
  )
] });
var ChatViewContext = (0, import_react4.createContext)(void 0);
var useChatViewContext = () => {
  const value = (0, import_react4.useContext)(ChatViewContext);
  if (!value) {
    console.warn(
      "The useChatViewContext hook was called outside of the ChatView provider."
    );
    return {};
  }
  return value;
};
var ChatView = ({ children }) => {
  const [activeChatView, setActiveChatView] = (0, import_react4.useState)("channels");
  const { theme } = useChatContext();
  const value = (0, import_react4.useMemo)(() => ({ activeChatView, setActiveChatView }), [activeChatView]);
  return (0, import_jsx_runtime2.jsx)(ChatViewContext.Provider, { value, children: (0, import_jsx_runtime2.jsx)("div", { className: clsx_default("str-chat", theme, "str-chat__chat-view"), children }) });
};
var ChannelsView = ({ children }) => {
  const { activeChatView } = useChatViewContext();
  if (activeChatView !== "channels") return null;
  return (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__chat-view__channels", children });
};
var ThreadsViewContext = (0, import_react4.createContext)({
  activeThread: void 0,
  setActiveThread: () => void 0
});
var useThreadsViewContext = () => (0, import_react4.useContext)(ThreadsViewContext);
var ThreadsView = ({ children }) => {
  const { activeChatView } = useChatViewContext();
  const [activeThread, setActiveThread] = (0, import_react4.useState)(void 0);
  const value = (0, import_react4.useMemo)(() => ({ activeThread, setActiveThread }), [activeThread]);
  if (activeChatView !== "threads") return null;
  return (0, import_jsx_runtime2.jsx)(ThreadsViewContext.Provider, { value, children: (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__chat-view__threads", children }) });
};
var useActiveThread = ({ activeThread }) => {
  (0, import_react4.useEffect)(() => {
    if (!activeThread) return;
    const handleVisibilityChange = () => {
      if (document.visibilityState === "visible" && document.hasFocus()) {
        activeThread.activate();
      }
      if (document.visibilityState === "hidden" || !document.hasFocus()) {
        activeThread.deactivate();
      }
    };
    handleVisibilityChange();
    window.addEventListener("focus", handleVisibilityChange);
    window.addEventListener("blur", handleVisibilityChange);
    return () => {
      activeThread.deactivate();
      window.addEventListener("blur", handleVisibilityChange);
      window.removeEventListener("focus", handleVisibilityChange);
    };
  }, [activeThread]);
};
var ThreadAdapter = ({ children }) => {
  const { client } = useChatContext("ThreadAdapter");
  const { EmptyStateIndicator: EmptyStateIndicator$1 = EmptyStateIndicator } = useComponentContext();
  const { activeThread } = useThreadsViewContext();
  const { t: t2 } = useTranslationContext("ThreadAdapter");
  const { isLoading, ready } = useStateStore(
    client.threads.state,
    threadAdapterSelector
  ) ?? {
    isLoading: false,
    ready: false
  };
  useActiveThread({ activeThread });
  if (!activeThread && ready && !isLoading && EmptyStateIndicator$1) {
    return (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__thread-container str-chat__thread", children: (0, import_jsx_runtime2.jsx)(
      EmptyStateIndicator$1,
      {
        listType: "message",
        messageText: t2("Select a thread to continue the conversation")
      }
    ) });
  }
  return (0, import_jsx_runtime2.jsx)(ThreadProvider, { thread: activeThread, children });
};
var ChatViewSelectorButton = ({
  ActiveIcon,
  children,
  className,
  Icon,
  iconOnly = true,
  isActive,
  text: text8,
  ...props
}) => {
  const SelectorIcon = isActive && ActiveIcon ? ActiveIcon : Icon;
  const shouldShowTooltip = !!text8 && iconOnly;
  return (0, import_jsx_runtime2.jsxs)("div", { className: "str-chat__chat-view__selector-button-container", children: [
    (0, import_jsx_runtime2.jsxs)(
      Button,
      {
        appearance: "ghost",
        "aria-label": props["aria-label"] ?? (shouldShowTooltip ? text8 : void 0),
        className: clsx_default("str-chat__chat-view__selector-button", className),
        role: "tab",
        variant: "secondary",
        ...props,
        children: [
          children ?? (SelectorIcon && (0, import_jsx_runtime2.jsx)(SelectorIcon, {})),
          !iconOnly && text8 && (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__chat-view__selector-button-text", children: text8 })
        ]
      }
    ),
    shouldShowTooltip && (0, import_jsx_runtime2.jsx)(
      "div",
      {
        "aria-hidden": "true",
        className: "str-chat__chat-view__selector-button-tooltip str-chat__tooltip",
        children: text8
      }
    )
  ] });
};
var threadAdapterSelector = ({ pagination, ready }) => ({
  isLoading: pagination.isLoading,
  ready
});
var unreadThreadCountSelector = ({ unreadThreadCount }) => ({
  unreadThreadCount
});
var ChatViewChannelsSelectorButton = ({
  iconOnly = true
}) => {
  const { activeChatView, setActiveChatView } = useChatViewContext();
  const { t: t2 } = useTranslationContext();
  const isActive = activeChatView === "channels";
  return (0, import_jsx_runtime2.jsx)(
    ChatViewSelectorButton,
    {
      ActiveIcon: IconMessageBubbleFill,
      "aria-selected": isActive,
      Icon: IconMessageBubble,
      iconOnly,
      isActive,
      onPointerDown: () => setActiveChatView("channels"),
      text: t2("Channels")
    }
  );
};
var ChatViewThreadsSelectorButton = ({
  iconOnly = true
}) => {
  const { client } = useChatContext();
  const { unreadThreadCount } = useStateStore(
    client.threads.state,
    unreadThreadCountSelector
  ) ?? {
    unreadThreadCount: 0
  };
  const { activeChatView, setActiveChatView } = useChatViewContext();
  const { t: t2 } = useTranslationContext();
  const isActive = activeChatView === "threads";
  return (0, import_jsx_runtime2.jsx)(
    ChatViewSelectorButton,
    {
      ActiveIcon: IconThreadFill,
      "aria-selected": isActive,
      Icon: IconThread,
      iconOnly,
      isActive,
      onPointerDown: () => setActiveChatView("threads"),
      text: t2("Threads"),
      children: (0, import_jsx_runtime2.jsx)(UnreadCountBadge, { count: unreadThreadCount, position: "top-right", children: isActive ? (0, import_jsx_runtime2.jsx)(IconThreadFill, {}) : (0, import_jsx_runtime2.jsx)(IconThread, {}) })
    }
  );
};
var defaultChatViewSelectorItemSet = [
  {
    Component: ChatViewChannelsSelectorButton,
    type: "channels"
  },
  {
    Component: ChatViewThreadsSelectorButton,
    type: "threads"
  }
];
var ChatViewSelector = ({
  iconOnly = true,
  itemSet = defaultChatViewSelectorItemSet
}) => (0, import_jsx_runtime2.jsx)("div", { className: "str-chat__chat-view__selector", children: itemSet.map(({ Component: Component2, type }) => (0, import_jsx_runtime2.jsx)(Component2, { iconOnly }, type)) });
ChatView.Channels = ChannelsView;
ChatView.Threads = ThreadsView;
ChatView.ThreadAdapter = ThreadAdapter;
ChatView.Selector = ChatViewSelector;
var useNotificationTarget = () => {
  const chatViewContext = (0, import_react4.useContext)(ChatViewContext);
  const { channels } = useChannelListContext();
  const { channel } = useChannelStateContext();
  const threadInstance = useThreadContext();
  const { legacyThread } = useLegacyThreadContext();
  if (threadInstance || legacyThread) return "thread";
  if (channel) return "channel";
  if (chatViewContext?.activeChatView === "threads") return "thread-list";
  if (channels) return "channel-list";
  return void 0;
};
var SYSTEM_NOTIFICATION_TAG = "system";
var hasSystemNotificationTag = (notification) => notification.tags?.includes(SYSTEM_NOTIFICATION_TAG) ?? false;
var getTargetTags = (targetPanels, inferredPanel, tags) => {
  if (targetPanels) {
    return Array.from(
      /* @__PURE__ */ new Set([...targetPanels.map(getNotificationTargetTag), ...tags ?? []])
    );
  }
  return addNotificationTargetTag(inferredPanel, tags);
};
var getTypeFromIncident = ({
  incident,
  severity,
  type
}) => {
  if (type) return type;
  if (!incident) return void 0;
  const status = incident.status ?? (severity === "error" ? "failed" : severity === "success" ? "success" : severity);
  return [incident.domain, incident.entity, incident.operation, status].filter((segment) => !!segment).join(":");
};
var useNotificationApi = () => {
  const { client } = useChatContext();
  const inferredPanel = useNotificationTarget();
  const addNotification = (0, import_react4.useCallback)(
    ({
      actions,
      context,
      duration: duration2,
      emitter,
      error: error2,
      incident,
      message,
      severity,
      tags,
      targetPanels,
      type
    }) => {
      const notificationTags = getTargetTags(targetPanels, inferredPanel, tags);
      const resolvedType = getTypeFromIncident({ incident, severity, type });
      const origin = context ? { context, emitter } : { emitter };
      const options = {
        ...actions ? { actions } : {},
        ...typeof duration2 === "number" ? { duration: duration2 } : {},
        ...error2 ? { originalError: error2 } : {},
        ...notificationTags.length > 0 ? { tags: notificationTags } : {},
        ...severity ? { severity } : {},
        ...resolvedType ? { type: resolvedType } : {}
      };
      client.notifications.add({
        message,
        options,
        origin
      });
    },
    [client, inferredPanel]
  );
  const addSystemNotification = (0, import_react4.useCallback)(
    ({
      actions,
      context,
      duration: duration2,
      emitter,
      error: error2,
      incident,
      message,
      severity,
      tags,
      type
    }) => {
      const notificationTags = Array.from(
        /* @__PURE__ */ new Set([SYSTEM_NOTIFICATION_TAG, ...tags ?? []])
      );
      const resolvedType = getTypeFromIncident({ incident, severity, type });
      const origin = context ? { context, emitter } : { emitter };
      const options = {
        ...actions ? { actions } : {},
        ...typeof duration2 === "number" ? { duration: duration2 } : {},
        ...error2 ? { originalError: error2 } : {},
        ...notificationTags.length > 0 ? { tags: notificationTags } : {},
        ...severity ? { severity } : {},
        ...resolvedType ? { type: resolvedType } : {}
      };
      return client.notifications.add({
        message,
        options,
        origin
      });
    },
    [client]
  );
  const removeNotification = (0, import_react4.useCallback)(
    (id3) => {
      client.notifications.remove(id3);
    },
    [client]
  );
  const startNotificationTimeout = (0, import_react4.useCallback)(
    (id3) => {
      client.notifications.startTimeout(id3);
    },
    [client]
  );
  return {
    addNotification,
    addSystemNotification,
    removeNotification,
    startNotificationTimeout
  };
};
var AudioPlayerContext = import_react4.default.createContext({
  audioPlayers: null
});
var WithAudioPlayback = ({
  allowConcurrentPlayback,
  children
}) => {
  const [audioPlayers] = (0, import_react4.useState)(() => new AudioPlayerPool({ allowConcurrentPlayback }));
  (0, import_react4.useEffect)(
    () => () => {
      audioPlayers.clear();
    },
    [audioPlayers]
  );
  return (0, import_jsx_runtime2.jsx)(AudioPlayerContext.Provider, { value: { audioPlayers }, children });
};
var makeAudioPlayerId = ({ requester, src }) => `${requester ?? "requester-unknown"}:${src}`;
var useAudioPlayer = ({
  durationSeconds,
  fileSize,
  mimeType,
  playbackRates,
  plugins,
  requester = "",
  src,
  title,
  waveformData
}) => {
  const { addNotification } = useNotificationApi();
  const panel = useNotificationTarget();
  const { t: t2 } = useTranslationContext();
  const { audioPlayers } = (0, import_react4.useContext)(AudioPlayerContext);
  const audioPlayer = src && audioPlayers ? audioPlayers.getOrAdd({
    durationSeconds,
    fileSize,
    id: makeAudioPlayerId({ requester, src }),
    mimeType,
    playbackRates,
    plugins,
    src,
    title,
    waveformData
  }) : void 0;
  (0, import_react4.useEffect)(() => {
    if (!audioPlayer) return;
    const notificationsPlugin = audioPlayerNotificationsPluginFactory({
      addNotification,
      panel,
      t: t2
    });
    audioPlayer.setPlugins((currentPlugins) => [
      ...currentPlugins.filter((plugin) => plugin.id !== notificationsPlugin.id),
      notificationsPlugin
    ]);
  }, [addNotification, audioPlayer, panel, t2]);
  return audioPlayer;
};
var activeAudioPlayerSelector = ({ activeAudioPlayer }) => ({
  activeAudioPlayer
});
var useActiveAudioPlayer = () => {
  const { audioPlayers } = (0, import_react4.useContext)(AudioPlayerContext);
  const { activeAudioPlayer } = useStateStore(audioPlayers?.state, activeAudioPlayerSelector) ?? {};
  return activeAudioPlayer;
};

// node_modules/stream-chat-react/dist/es/index.mjs
var import_sanitize_url = __toESM(require_dist(), 1);
var import_lodash3 = __toESM(require_lodash(), 1);
var import_react_player = __toESM(require_lib(), 1);

// node_modules/react-aria/dist/private/utils/domHelpers.mjs
var $d447af545b77c9f1$export$b204af158042fbac = (el) => {
  return el?.ownerDocument ?? document;
};
var $d447af545b77c9f1$export$f21a1ffae260145a = (el) => {
  if (el && "window" in el && el.window === el) return el;
  const doc = $d447af545b77c9f1$export$b204af158042fbac(el);
  return doc.defaultView || window;
};
function $d447af545b77c9f1$var$isNode(value) {
  return value !== null && typeof value === "object" && "nodeType" in value && typeof value.nodeType === "number";
}
function $d447af545b77c9f1$export$af51f0f06c0f328a(node2) {
  return $d447af545b77c9f1$var$isNode(node2) && node2.nodeType === Node.DOCUMENT_FRAGMENT_NODE && "host" in node2;
}

// node_modules/react-stately/dist/private/flags/flags.mjs
var $6a20a7989e6c817a$var$_shadowDOM = false;
function $6a20a7989e6c817a$export$98658e8c59125e6a() {
  return $6a20a7989e6c817a$var$_shadowDOM;
}

// node_modules/react-aria/dist/private/utils/shadowdom/DOMFunctions.mjs
function $23f2114a1b82827e$export$4282f70798064fe0(node2, otherNode) {
  if (!(0, $6a20a7989e6c817a$export$98658e8c59125e6a)()) return otherNode && node2 ? node2.contains(otherNode) : false;
  if (!node2 || !otherNode) return false;
  let currentNode = otherNode;
  while (currentNode !== null) {
    if (currentNode === node2) return true;
    if (currentNode.tagName === "SLOT" && currentNode.assignedSlot)
      currentNode = currentNode.assignedSlot.parentNode;
    else if ((0, $d447af545b77c9f1$export$af51f0f06c0f328a)(currentNode))
      currentNode = currentNode.host;
    else currentNode = currentNode.parentNode;
  }
  return false;
}
var $23f2114a1b82827e$export$cd4e5573fbe2b576 = (doc = document) => {
  if (!(0, $6a20a7989e6c817a$export$98658e8c59125e6a)()) return doc.activeElement;
  let activeElement2 = doc.activeElement;
  while (activeElement2 && "shadowRoot" in activeElement2 && activeElement2.shadowRoot?.activeElement) activeElement2 = activeElement2.shadowRoot.activeElement;
  return activeElement2;
};
function $23f2114a1b82827e$export$e58f029f0fbfdb29(event) {
  if ((0, $6a20a7989e6c817a$export$98658e8c59125e6a)() && event.target instanceof Element && event.target.shadowRoot) {
    if ("composedPath" in event) return event.composedPath()[0] ?? null;
    else if ("composedPath" in event.nativeEvent) return event.nativeEvent.composedPath()[0] ?? null;
  }
  return event.target;
}

// node_modules/react-aria/dist/private/utils/shadowdom/ShadowTreeWalker.mjs
var $654b97e09f2a30c1$export$63eb3ababa9c55c4 = class {
  constructor(doc, root4, whatToShow, filter2) {
    this._walkerStack = [];
    this._currentSetFor = /* @__PURE__ */ new Set();
    this._acceptNode = (node2) => {
      if (node2.nodeType === Node.ELEMENT_NODE) {
        const shadowRoot2 = node2.shadowRoot;
        if (shadowRoot2) {
          const walker = this._doc.createTreeWalker(shadowRoot2, this.whatToShow, {
            acceptNode: this._acceptNode
          });
          this._walkerStack.unshift(walker);
          return NodeFilter.FILTER_ACCEPT;
        } else {
          if (typeof this.filter === "function") return this.filter(node2);
          else if (this.filter?.acceptNode) return this.filter.acceptNode(node2);
          else if (this.filter === null) return NodeFilter.FILTER_ACCEPT;
        }
      }
      return NodeFilter.FILTER_SKIP;
    };
    this._doc = doc;
    this.root = root4;
    this.filter = filter2 ?? null;
    this.whatToShow = whatToShow ?? NodeFilter.SHOW_ALL;
    this._currentNode = root4;
    this._walkerStack.unshift(doc.createTreeWalker(root4, whatToShow, this._acceptNode));
    const shadowRoot = root4.shadowRoot;
    if (shadowRoot) {
      const walker = this._doc.createTreeWalker(shadowRoot, this.whatToShow, {
        acceptNode: this._acceptNode
      });
      this._walkerStack.unshift(walker);
    }
  }
  get currentNode() {
    return this._currentNode;
  }
  set currentNode(node2) {
    if (!(0, $23f2114a1b82827e$export$4282f70798064fe0)(this.root, node2)) throw new Error("Cannot set currentNode to a node that is not contained by the root node.");
    const walkers = [];
    let curNode = node2;
    let currentWalkerCurrentNode = node2;
    this._currentNode = node2;
    while (curNode && curNode !== this.root) if (curNode.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
      const shadowRoot = curNode;
      const walker2 = this._doc.createTreeWalker(shadowRoot, this.whatToShow, {
        acceptNode: this._acceptNode
      });
      walkers.push(walker2);
      walker2.currentNode = currentWalkerCurrentNode;
      this._currentSetFor.add(walker2);
      curNode = currentWalkerCurrentNode = shadowRoot.host;
    } else curNode = curNode.parentNode;
    const walker = this._doc.createTreeWalker(this.root, this.whatToShow, {
      acceptNode: this._acceptNode
    });
    walkers.push(walker);
    walker.currentNode = currentWalkerCurrentNode;
    this._currentSetFor.add(walker);
    this._walkerStack = walkers;
  }
  get doc() {
    return this._doc;
  }
  firstChild() {
    let currentNode = this.currentNode;
    let newNode = this.nextNode();
    if (!(0, $23f2114a1b82827e$export$4282f70798064fe0)(currentNode, newNode)) {
      this.currentNode = currentNode;
      return null;
    }
    if (newNode) this.currentNode = newNode;
    return newNode;
  }
  lastChild() {
    let walker = this._walkerStack[0];
    let newNode = walker.lastChild();
    if (newNode) this.currentNode = newNode;
    return newNode;
  }
  nextNode() {
    const nextNode = this._walkerStack[0].nextNode();
    if (nextNode) {
      const shadowRoot = nextNode.shadowRoot;
      if (shadowRoot) {
        let nodeResult;
        if (typeof this.filter === "function") nodeResult = this.filter(nextNode);
        else if (this.filter?.acceptNode) nodeResult = this.filter.acceptNode(nextNode);
        if (nodeResult === NodeFilter.FILTER_ACCEPT) {
          this.currentNode = nextNode;
          return nextNode;
        }
        let newNode = this.nextNode();
        if (newNode) this.currentNode = newNode;
        return newNode;
      }
      if (nextNode) this.currentNode = nextNode;
      return nextNode;
    } else {
      if (this._walkerStack.length > 1) {
        this._walkerStack.shift();
        let newNode = this.nextNode();
        if (newNode) this.currentNode = newNode;
        return newNode;
      } else return null;
    }
  }
  previousNode() {
    const currentWalker = this._walkerStack[0];
    if (currentWalker.currentNode === currentWalker.root) {
      if (this._currentSetFor.has(currentWalker)) {
        this._currentSetFor.delete(currentWalker);
        if (this._walkerStack.length > 1) {
          this._walkerStack.shift();
          let newNode = this.previousNode();
          if (newNode) this.currentNode = newNode;
          return newNode;
        } else return null;
      }
      return null;
    }
    const previousNode = currentWalker.previousNode();
    if (previousNode) {
      const shadowRoot = previousNode.shadowRoot;
      if (shadowRoot) {
        let nodeResult;
        if (typeof this.filter === "function") nodeResult = this.filter(previousNode);
        else if (this.filter?.acceptNode) nodeResult = this.filter.acceptNode(previousNode);
        if (nodeResult === NodeFilter.FILTER_ACCEPT) {
          if (previousNode) this.currentNode = previousNode;
          return previousNode;
        }
        let newNode = this.lastChild();
        if (newNode) this.currentNode = newNode;
        return newNode;
      }
      if (previousNode) this.currentNode = previousNode;
      return previousNode;
    } else {
      if (this._walkerStack.length > 1) {
        this._walkerStack.shift();
        let newNode = this.previousNode();
        if (newNode) this.currentNode = newNode;
        return newNode;
      } else return null;
    }
  }
  /**
   * @deprecated
   */
  nextSibling() {
    return null;
  }
  /**
   * @deprecated
   */
  previousSibling() {
    return null;
  }
  /**
   * @deprecated
   */
  parentNode() {
    return null;
  }
};
function $654b97e09f2a30c1$export$4d0f8be8b12a7ef6(doc, root4, whatToShow, filter2) {
  if ((0, $6a20a7989e6c817a$export$98658e8c59125e6a)()) return new $654b97e09f2a30c1$export$63eb3ababa9c55c4(doc, root4, whatToShow, filter2);
  return doc.createTreeWalker(root4, whatToShow, filter2);
}

// node_modules/react-aria/dist/private/utils/focusWithoutScrolling.mjs
function $1969ac565cfec8d0$export$de79e2c695e052f3(element4) {
  if ($1969ac565cfec8d0$var$supportsPreventScroll()) element4.focus({
    preventScroll: true
  });
  else {
    let scrollableElements = $1969ac565cfec8d0$var$getScrollableElements(element4);
    element4.focus();
    $1969ac565cfec8d0$var$restoreScrollPosition(scrollableElements);
  }
}
var $1969ac565cfec8d0$var$supportsPreventScrollCached = null;
function $1969ac565cfec8d0$var$supportsPreventScroll() {
  if ($1969ac565cfec8d0$var$supportsPreventScrollCached == null) {
    $1969ac565cfec8d0$var$supportsPreventScrollCached = false;
    try {
      let focusElem = document.createElement("div");
      focusElem.focus({
        get preventScroll() {
          $1969ac565cfec8d0$var$supportsPreventScrollCached = true;
          return true;
        }
      });
    } catch {
    }
  }
  return $1969ac565cfec8d0$var$supportsPreventScrollCached;
}
function $1969ac565cfec8d0$var$getScrollableElements(element4) {
  let parent = element4.parentNode;
  let scrollableElements = [];
  let rootScrollingElement = document.scrollingElement || document.documentElement;
  while (parent instanceof HTMLElement && parent !== rootScrollingElement) {
    if (parent.offsetHeight < parent.scrollHeight || parent.offsetWidth < parent.scrollWidth) scrollableElements.push({
      element: parent,
      scrollTop: parent.scrollTop,
      scrollLeft: parent.scrollLeft
    });
    parent = parent.parentNode;
  }
  if (rootScrollingElement instanceof HTMLElement) scrollableElements.push({
    element: rootScrollingElement,
    scrollTop: rootScrollingElement.scrollTop,
    scrollLeft: rootScrollingElement.scrollLeft
  });
  return scrollableElements;
}
function $1969ac565cfec8d0$var$restoreScrollPosition(scrollableElements) {
  for (let { element: element4, scrollTop, scrollLeft } of scrollableElements) {
    element4.scrollTop = scrollTop;
    element4.scrollLeft = scrollLeft;
  }
}

// node_modules/react-aria/dist/private/utils/isElementVisible.mjs
var $ae77152785188400$var$supportsCheckVisibility = typeof Element !== "undefined" && "checkVisibility" in Element.prototype;
function $ae77152785188400$var$isStyleVisible(element4) {
  const windowObject = (0, $d447af545b77c9f1$export$f21a1ffae260145a)(element4);
  if (!(element4 instanceof windowObject.HTMLElement) && !(element4 instanceof windowObject.SVGElement)) return false;
  let { display, visibility } = element4.style;
  let isVisible = display !== "none" && visibility !== "hidden" && visibility !== "collapse";
  if (isVisible) {
    const { getComputedStyle: getComputedStyle3 } = element4.ownerDocument.defaultView;
    let { display: computedDisplay, visibility: computedVisibility } = getComputedStyle3(element4);
    isVisible = computedDisplay !== "none" && computedVisibility !== "hidden" && computedVisibility !== "collapse";
  }
  return isVisible;
}
function $ae77152785188400$var$isAttributeVisible(element4, childElement) {
  return !element4.hasAttribute("hidden") && // Ignore HiddenSelect when tree walking.
  !element4.hasAttribute("data-react-aria-prevent-focus") && (element4.nodeName === "DETAILS" && childElement && childElement.nodeName !== "SUMMARY" ? element4.hasAttribute("open") : true);
}
function $ae77152785188400$export$e989c0fffaa6b27a(element4, childElement) {
  if ($ae77152785188400$var$supportsCheckVisibility) return element4.checkVisibility({
    visibilityProperty: true
  }) && !element4.closest("[data-react-aria-prevent-focus]");
  return element4.nodeName !== "#comment" && $ae77152785188400$var$isStyleVisible(element4) && $ae77152785188400$var$isAttributeVisible(element4, childElement) && (!element4.parentElement || $ae77152785188400$export$e989c0fffaa6b27a(element4.parentElement, element4));
}

// node_modules/react-aria/dist/private/utils/isFocusable.mjs
var $3b8b240c1bf84ab9$var$focusableElements = [
  "input:not([disabled]):not([type=hidden])",
  "select:not([disabled])",
  "textarea:not([disabled])",
  "button:not([disabled])",
  "a[href]",
  "area[href]",
  "summary",
  "iframe",
  "object",
  "embed",
  "audio[controls]",
  "video[controls]",
  '[contenteditable]:not([contenteditable^="false"])',
  "permission"
];
var $3b8b240c1bf84ab9$var$FOCUSABLE_ELEMENT_SELECTOR = $3b8b240c1bf84ab9$var$focusableElements.join(":not([hidden]),") + ",[tabindex]:not([disabled]):not([hidden])";
$3b8b240c1bf84ab9$var$focusableElements.push('[tabindex]:not([tabindex="-1"]):not([disabled])');
var $3b8b240c1bf84ab9$var$TABBABLE_ELEMENT_SELECTOR = $3b8b240c1bf84ab9$var$focusableElements.join(':not([hidden]):not([tabindex="-1"]),');
function $3b8b240c1bf84ab9$export$4c063cf1350e6fed(element4, options) {
  return element4.matches($3b8b240c1bf84ab9$var$FOCUSABLE_ELEMENT_SELECTOR) && !$3b8b240c1bf84ab9$var$isInert(element4) && (options?.skipVisibilityCheck || (0, $ae77152785188400$export$e989c0fffaa6b27a)(element4));
}
function $3b8b240c1bf84ab9$export$bebd5a1431fec25d(element4) {
  return element4.matches($3b8b240c1bf84ab9$var$TABBABLE_ELEMENT_SELECTOR) && (0, $ae77152785188400$export$e989c0fffaa6b27a)(element4) && !$3b8b240c1bf84ab9$var$isInert(element4);
}
function $3b8b240c1bf84ab9$var$isInert(element4) {
  let node2 = element4;
  while (node2 != null) {
    if (node2 instanceof node2.ownerDocument.defaultView.HTMLElement && node2.inert) return true;
    node2 = node2.parentElement;
  }
  return false;
}

// node_modules/react-aria/dist/private/utils/useLayoutEffect.mjs
var import_react6 = __toESM(require_react(), 1);
var $c4867b2f328c2698$export$e5c5a5f917a5871c = typeof document !== "undefined" ? (0, import_react6.default).useLayoutEffect : () => {
};

// node_modules/react-aria/dist/private/interactions/utils.mjs
var import_react7 = __toESM(require_react(), 1);
function $a92dc41f639950be$export$525bc4921d56d4a(nativeEvent) {
  let event = nativeEvent;
  event.nativeEvent = nativeEvent;
  event.isDefaultPrevented = () => event.defaultPrevented;
  event.isPropagationStopped = () => event.cancelBubble;
  event.persist = () => {
  };
  return event;
}
function $a92dc41f639950be$export$715c682d09d639cc(onBlur) {
  let stateRef = (0, import_react7.useRef)({
    isFocused: false,
    observer: null
  });
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    const state = stateRef.current;
    return () => {
      if (state.observer) {
        state.observer.disconnect();
        state.observer = null;
      }
    };
  }, []);
  return (0, import_react7.useCallback)((e) => {
    let eventTarget = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
    if (eventTarget instanceof HTMLButtonElement || eventTarget instanceof HTMLInputElement || eventTarget instanceof HTMLTextAreaElement || eventTarget instanceof HTMLSelectElement) {
      stateRef.current.isFocused = true;
      let target = eventTarget;
      let onBlurHandler = (e2) => {
        stateRef.current.isFocused = false;
        if (target.disabled) {
          let event = $a92dc41f639950be$export$525bc4921d56d4a(e2);
          onBlur?.(event);
        }
        if (stateRef.current.observer) {
          stateRef.current.observer.disconnect();
          stateRef.current.observer = null;
        }
      };
      target.addEventListener("focusout", onBlurHandler, {
        once: true
      });
      stateRef.current.observer = new MutationObserver(() => {
        if (stateRef.current.isFocused && target.disabled) {
          stateRef.current.observer?.disconnect();
          let relatedTargetEl = target === (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)() ? null : (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)();
          target.dispatchEvent(new FocusEvent("blur", {
            relatedTarget: relatedTargetEl
          }));
          target.dispatchEvent(new FocusEvent("focusout", {
            bubbles: true,
            relatedTarget: relatedTargetEl
          }));
        }
      });
      stateRef.current.observer.observe(target, {
        attributes: true,
        attributeFilter: [
          "disabled"
        ]
      });
    }
  }, [
    onBlur
  ]);
}
var $a92dc41f639950be$export$fda7da73ab5d4c48 = false;

// node_modules/react-aria/dist/private/utils/platform.mjs
function $2add3ce32c6007eb$var$testUserAgent(re3) {
  if (typeof window === "undefined" || window.navigator == null) return false;
  let brands = window.navigator["userAgentData"]?.brands;
  return Array.isArray(brands) && brands.some((brand) => re3.test(brand.brand)) || re3.test(window.navigator.userAgent);
}
function $2add3ce32c6007eb$var$testPlatform(re3) {
  return typeof window !== "undefined" && window.navigator != null ? re3.test(window.navigator["userAgentData"]?.platform || window.navigator.platform) : false;
}
function $2add3ce32c6007eb$var$cached(fn) {
  if (false) return fn;
  let res = null;
  return () => {
    if (res == null) res = fn();
    return res;
  };
}
var $2add3ce32c6007eb$export$9ac100e40613ea10 = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$var$testPlatform(/^Mac/i);
});
var $2add3ce32c6007eb$export$186c6964ca17d99 = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$var$testPlatform(/^iPhone/i);
});
var $2add3ce32c6007eb$export$7bef049ce92e4224 = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$var$testPlatform(/^iPad/i) || // iPadOS 13 lies and says it's a Mac, but we can distinguish by detecting touch support.
  $2add3ce32c6007eb$export$9ac100e40613ea10() && navigator.maxTouchPoints > 1;
});
var $2add3ce32c6007eb$export$fedb369cb70207f1 = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$export$186c6964ca17d99() || $2add3ce32c6007eb$export$7bef049ce92e4224();
});
var $2add3ce32c6007eb$export$e1865c3bedcd822b = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$export$9ac100e40613ea10() || $2add3ce32c6007eb$export$fedb369cb70207f1();
});
var $2add3ce32c6007eb$export$78551043582a6a98 = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$var$testUserAgent(/AppleWebKit/i) && !$2add3ce32c6007eb$export$6446a186d09e379e();
});
var $2add3ce32c6007eb$export$6446a186d09e379e = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$var$testUserAgent(/Chrome/i);
});
var $2add3ce32c6007eb$export$a11b0059900ceec8 = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$var$testUserAgent(/Android/i);
});
var $2add3ce32c6007eb$export$b7d78993b74f766d = $2add3ce32c6007eb$var$cached(function() {
  return $2add3ce32c6007eb$var$testUserAgent(/Firefox/i);
});

// node_modules/react-aria/dist/private/utils/isVirtualEvent.mjs
function $b5c62b033c25b96d$export$60278871457622de(event) {
  if (event.pointerType === "" && event.isTrusted) return true;
  if ((0, $2add3ce32c6007eb$export$a11b0059900ceec8)() && event.pointerType) return event.type === "click" && event.buttons === 1;
  return event.detail === 0 && !event.pointerType;
}

// node_modules/react-aria/dist/private/utils/openLink.mjs
var import_react8 = __toESM(require_react(), 1);
var $caaf0dd3060ed57c$var$RouterContext = (0, import_react8.createContext)({
  isNative: true,
  open: $caaf0dd3060ed57c$var$openSyntheticLink,
  useHref: (href) => href
});
function $caaf0dd3060ed57c$export$95185d699e05d4d7(target, modifiers, setOpening = true) {
  let { metaKey, ctrlKey, altKey, shiftKey } = modifiers;
  if ((0, $2add3ce32c6007eb$export$b7d78993b74f766d)() && window.event?.type?.startsWith("key") && target.target === "_blank") {
    if ((0, $2add3ce32c6007eb$export$9ac100e40613ea10)()) metaKey = true;
    else ctrlKey = true;
  }
  let event = (0, $2add3ce32c6007eb$export$78551043582a6a98)() && (0, $2add3ce32c6007eb$export$9ac100e40613ea10)() && !(0, $2add3ce32c6007eb$export$7bef049ce92e4224)() && true ? new KeyboardEvent("keydown", {
    keyIdentifier: "Enter",
    metaKey,
    ctrlKey,
    altKey,
    shiftKey
  }) : new MouseEvent("click", {
    metaKey,
    ctrlKey,
    altKey,
    shiftKey,
    detail: 1,
    bubbles: true,
    cancelable: true
  });
  $caaf0dd3060ed57c$export$95185d699e05d4d7.isOpening = setOpening;
  (0, $1969ac565cfec8d0$export$de79e2c695e052f3)(target);
  target.dispatchEvent(event);
  $caaf0dd3060ed57c$export$95185d699e05d4d7.isOpening = false;
}
$caaf0dd3060ed57c$export$95185d699e05d4d7.isOpening = false;
function $caaf0dd3060ed57c$var$getSyntheticLink(target, open) {
  if (target instanceof HTMLAnchorElement) open(target);
  else if (target.hasAttribute("data-href")) {
    let link3 = document.createElement("a");
    link3.href = target.getAttribute("data-href");
    if (target.hasAttribute("data-target")) link3.target = target.getAttribute("data-target");
    if (target.hasAttribute("data-rel")) link3.rel = target.getAttribute("data-rel");
    if (target.hasAttribute("data-download")) link3.download = target.getAttribute("data-download");
    if (target.hasAttribute("data-ping")) link3.ping = target.getAttribute("data-ping");
    if (target.hasAttribute("data-referrer-policy")) link3.referrerPolicy = target.getAttribute("data-referrer-policy");
    target.appendChild(link3);
    open(link3);
    target.removeChild(link3);
  }
}
function $caaf0dd3060ed57c$var$openSyntheticLink(target, modifiers) {
  $caaf0dd3060ed57c$var$getSyntheticLink(target, (link3) => $caaf0dd3060ed57c$export$95185d699e05d4d7(link3, modifiers));
}

// node_modules/react-aria/dist/private/ssr/SSRProvider.mjs
var import_react9 = __toESM(require_react(), 1);
var $c7eafbbe1ea5834e$var$defaultContext = {
  prefix: String(Math.round(Math.random() * 1e10)),
  current: 0
};
var $c7eafbbe1ea5834e$var$SSRContext = (0, import_react9.default).createContext($c7eafbbe1ea5834e$var$defaultContext);
var $c7eafbbe1ea5834e$var$IsSSRContext = (0, import_react9.default).createContext(false);
var $c7eafbbe1ea5834e$var$canUseDOM = Boolean(typeof window !== "undefined" && window.document && window.document.createElement);
var $c7eafbbe1ea5834e$var$componentIds = /* @__PURE__ */ new WeakMap();
function $c7eafbbe1ea5834e$var$useCounter(isDisabled = false) {
  let ctx = (0, import_react9.useContext)($c7eafbbe1ea5834e$var$SSRContext);
  let ref = (0, import_react9.useRef)(null);
  if (ref.current === null && !isDisabled) {
    let currentOwner = (0, import_react9.default).__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED?.ReactCurrentOwner?.current;
    if (currentOwner) {
      let prevComponentValue = $c7eafbbe1ea5834e$var$componentIds.get(currentOwner);
      if (prevComponentValue == null)
        $c7eafbbe1ea5834e$var$componentIds.set(currentOwner, {
          id: ctx.current,
          state: currentOwner.memoizedState
        });
      else if (currentOwner.memoizedState !== prevComponentValue.state) {
        ctx.current = prevComponentValue.id;
        $c7eafbbe1ea5834e$var$componentIds.delete(currentOwner);
      }
    }
    ref.current = ++ctx.current;
  }
  return ref.current;
}
function $c7eafbbe1ea5834e$var$useLegacySSRSafeId(defaultId) {
  let ctx = (0, import_react9.useContext)($c7eafbbe1ea5834e$var$SSRContext);
  if (ctx === $c7eafbbe1ea5834e$var$defaultContext && !$c7eafbbe1ea5834e$var$canUseDOM && true) console.warn("When server rendering, you must wrap your application in an <SSRProvider> to ensure consistent ids are generated between the client and server.");
  let counter = $c7eafbbe1ea5834e$var$useCounter(!!defaultId);
  let prefix = ctx === $c7eafbbe1ea5834e$var$defaultContext && false ? "react-aria" : `react-aria${ctx.prefix}`;
  return defaultId || `${prefix}-${counter}`;
}
function $c7eafbbe1ea5834e$var$useModernSSRSafeId(defaultId) {
  let id3 = (0, import_react9.default).useId();
  let [didSSR] = (0, import_react9.useState)($c7eafbbe1ea5834e$export$535bd6ca7f90a273());
  let prefix = didSSR || false ? "react-aria" : `react-aria${$c7eafbbe1ea5834e$var$defaultContext.prefix}`;
  return defaultId || `${prefix}-${id3}`;
}
var $c7eafbbe1ea5834e$export$619500959fc48b26 = typeof (0, import_react9.default)["useId"] === "function" ? $c7eafbbe1ea5834e$var$useModernSSRSafeId : $c7eafbbe1ea5834e$var$useLegacySSRSafeId;
function $c7eafbbe1ea5834e$var$getSnapshot() {
  return false;
}
function $c7eafbbe1ea5834e$var$getServerSnapshot() {
  return true;
}
function $c7eafbbe1ea5834e$var$subscribe(onStoreChange) {
  return () => {
  };
}
function $c7eafbbe1ea5834e$export$535bd6ca7f90a273() {
  if (typeof (0, import_react9.default)["useSyncExternalStore"] === "function") return (0, import_react9.default)["useSyncExternalStore"]($c7eafbbe1ea5834e$var$subscribe, $c7eafbbe1ea5834e$var$getSnapshot, $c7eafbbe1ea5834e$var$getServerSnapshot);
  return (0, import_react9.useContext)($c7eafbbe1ea5834e$var$IsSSRContext);
}

// node_modules/react-aria/dist/private/interactions/useFocusVisible.mjs
var import_react10 = __toESM(require_react(), 1);
var $8f5a2122b0992be3$var$currentModality = null;
var $8f5a2122b0992be3$var$currentPointerType = "keyboard";
var $8f5a2122b0992be3$export$901e90a13c50a14e = /* @__PURE__ */ new Set();
var $8f5a2122b0992be3$export$d90243b58daecda7 = /* @__PURE__ */ new Map();
var $8f5a2122b0992be3$var$hasEventBeforeFocus = false;
var $8f5a2122b0992be3$var$hasBlurredWindowRecently = false;
function $8f5a2122b0992be3$var$triggerChangeHandlers(modality, e) {
  for (let handler of $8f5a2122b0992be3$export$901e90a13c50a14e) handler(modality, e);
}
function $8f5a2122b0992be3$var$isValidKey(e) {
  return !(e.metaKey || !(0, $2add3ce32c6007eb$export$9ac100e40613ea10)() && e.altKey || e.ctrlKey || e.key === "Control" || e.key === "Shift" || e.key === "Meta");
}
function $8f5a2122b0992be3$var$handleKeyboardEvent(e) {
  $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
  if (!(0, $caaf0dd3060ed57c$export$95185d699e05d4d7).isOpening && $8f5a2122b0992be3$var$isValidKey(e)) {
    $8f5a2122b0992be3$var$currentModality = "keyboard";
    $8f5a2122b0992be3$var$currentPointerType = "keyboard";
    $8f5a2122b0992be3$var$triggerChangeHandlers("keyboard", e);
  }
}
function $8f5a2122b0992be3$var$handlePointerEvent(e) {
  $8f5a2122b0992be3$var$currentModality = "pointer";
  $8f5a2122b0992be3$var$currentPointerType = "pointerType" in e ? e.pointerType : "mouse";
  if (e.type === "mousedown" || e.type === "pointerdown") {
    $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
    $8f5a2122b0992be3$var$triggerChangeHandlers("pointer", e);
  }
}
function $8f5a2122b0992be3$var$handleClickEvent(e) {
  if (!(0, $caaf0dd3060ed57c$export$95185d699e05d4d7).isOpening && (0, $b5c62b033c25b96d$export$60278871457622de)(e)) {
    $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
    $8f5a2122b0992be3$var$currentModality = "virtual";
    $8f5a2122b0992be3$var$currentPointerType = "virtual";
  }
}
function $8f5a2122b0992be3$var$handleFocusEvent(e) {
  let ownerWindow = (0, $d447af545b77c9f1$export$f21a1ffae260145a)((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e));
  let ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e));
  if ((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e) === ownerWindow || (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e) === ownerDocument || (0, $a92dc41f639950be$export$fda7da73ab5d4c48) || !e.isTrusted) return;
  if (!$8f5a2122b0992be3$var$hasEventBeforeFocus && !$8f5a2122b0992be3$var$hasBlurredWindowRecently) {
    $8f5a2122b0992be3$var$currentModality = "virtual";
    $8f5a2122b0992be3$var$currentPointerType = "virtual";
    $8f5a2122b0992be3$var$triggerChangeHandlers("virtual", e);
  }
  $8f5a2122b0992be3$var$hasEventBeforeFocus = false;
  $8f5a2122b0992be3$var$hasBlurredWindowRecently = false;
}
function $8f5a2122b0992be3$var$handleWindowBlur() {
  if (0, $a92dc41f639950be$export$fda7da73ab5d4c48) return;
  $8f5a2122b0992be3$var$hasEventBeforeFocus = false;
  $8f5a2122b0992be3$var$hasBlurredWindowRecently = true;
}
function $8f5a2122b0992be3$var$setupGlobalFocusEvents(element4) {
  if (typeof window === "undefined" || typeof document === "undefined") return;
  const windowObject = (0, $d447af545b77c9f1$export$f21a1ffae260145a)(element4);
  const documentObject = (0, $d447af545b77c9f1$export$b204af158042fbac)(element4);
  if ($8f5a2122b0992be3$export$d90243b58daecda7.get(windowObject)) return;
  let focus = windowObject.HTMLElement.prototype.focus;
  windowObject.HTMLElement.prototype.focus = function() {
    $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
    focus.apply(this, arguments);
  };
  documentObject.addEventListener("keydown", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
  documentObject.addEventListener("keyup", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
  documentObject.addEventListener("click", $8f5a2122b0992be3$var$handleClickEvent, true);
  windowObject.addEventListener("focus", $8f5a2122b0992be3$var$handleFocusEvent, true);
  windowObject.addEventListener("blur", $8f5a2122b0992be3$var$handleWindowBlur, false);
  if (typeof PointerEvent !== "undefined") {
    documentObject.addEventListener("pointerdown", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.addEventListener("pointermove", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.addEventListener("pointerup", $8f5a2122b0992be3$var$handlePointerEvent, true);
  } else if (false) {
    documentObject.addEventListener("mousedown", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.addEventListener("mousemove", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.addEventListener("mouseup", $8f5a2122b0992be3$var$handlePointerEvent, true);
  }
  windowObject.addEventListener("beforeunload", () => {
    $8f5a2122b0992be3$var$tearDownWindowFocusTracking(element4);
  }, {
    once: true
  });
  $8f5a2122b0992be3$export$d90243b58daecda7.set(windowObject, {
    focus
  });
}
var $8f5a2122b0992be3$var$tearDownWindowFocusTracking = (element4, loadListener) => {
  const windowObject = (0, $d447af545b77c9f1$export$f21a1ffae260145a)(element4);
  const documentObject = (0, $d447af545b77c9f1$export$b204af158042fbac)(element4);
  if (loadListener) documentObject.removeEventListener("DOMContentLoaded", loadListener);
  if (!$8f5a2122b0992be3$export$d90243b58daecda7.has(windowObject)) return;
  windowObject.HTMLElement.prototype.focus = $8f5a2122b0992be3$export$d90243b58daecda7.get(windowObject).focus;
  documentObject.removeEventListener("keydown", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
  documentObject.removeEventListener("keyup", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
  documentObject.removeEventListener("click", $8f5a2122b0992be3$var$handleClickEvent, true);
  windowObject.removeEventListener("focus", $8f5a2122b0992be3$var$handleFocusEvent, true);
  windowObject.removeEventListener("blur", $8f5a2122b0992be3$var$handleWindowBlur, false);
  if (typeof PointerEvent !== "undefined") {
    documentObject.removeEventListener("pointerdown", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.removeEventListener("pointermove", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.removeEventListener("pointerup", $8f5a2122b0992be3$var$handlePointerEvent, true);
  } else if (false) {
    documentObject.removeEventListener("mousedown", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.removeEventListener("mousemove", $8f5a2122b0992be3$var$handlePointerEvent, true);
    documentObject.removeEventListener("mouseup", $8f5a2122b0992be3$var$handlePointerEvent, true);
  }
  $8f5a2122b0992be3$export$d90243b58daecda7.delete(windowObject);
};
function $8f5a2122b0992be3$export$2f1888112f558a7d(element4) {
  const documentObject = (0, $d447af545b77c9f1$export$b204af158042fbac)(element4);
  let loadListener;
  if (documentObject.readyState !== "loading") $8f5a2122b0992be3$var$setupGlobalFocusEvents(element4);
  else {
    loadListener = () => {
      $8f5a2122b0992be3$var$setupGlobalFocusEvents(element4);
    };
    documentObject.addEventListener("DOMContentLoaded", loadListener);
  }
  return () => $8f5a2122b0992be3$var$tearDownWindowFocusTracking(element4, loadListener);
}
if (typeof document !== "undefined") $8f5a2122b0992be3$export$2f1888112f558a7d();
function $8f5a2122b0992be3$export$630ff653c5ada6a9() {
  return $8f5a2122b0992be3$var$currentModality;
}

// node_modules/react-aria/dist/private/utils/runAfterTransition.mjs
var $081cb5757e08788e$var$transitionsByElement = /* @__PURE__ */ new Map();
var $081cb5757e08788e$var$transitionCallbacks = /* @__PURE__ */ new Set();
function $081cb5757e08788e$var$setupGlobalEvents() {
  if (typeof window === "undefined") return;
  function isTransitionEvent(event) {
    return "propertyName" in event;
  }
  let onTransitionStart = (e) => {
    let eventTarget = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
    if (!isTransitionEvent(e) || !eventTarget) return;
    let transitions = $081cb5757e08788e$var$transitionsByElement.get(eventTarget);
    if (!transitions) {
      transitions = /* @__PURE__ */ new Set();
      $081cb5757e08788e$var$transitionsByElement.set(eventTarget, transitions);
      eventTarget.addEventListener("transitioncancel", onTransitionEnd, {
        once: true
      });
    }
    transitions.add(e.propertyName);
  };
  let onTransitionEnd = (e) => {
    let eventTarget = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
    if (!isTransitionEvent(e) || !eventTarget) return;
    let properties = $081cb5757e08788e$var$transitionsByElement.get(eventTarget);
    if (!properties) return;
    properties.delete(e.propertyName);
    if (properties.size === 0) {
      eventTarget.removeEventListener("transitioncancel", onTransitionEnd);
      $081cb5757e08788e$var$transitionsByElement.delete(eventTarget);
    }
    if ($081cb5757e08788e$var$transitionsByElement.size === 0) {
      for (let cb of $081cb5757e08788e$var$transitionCallbacks) cb();
      $081cb5757e08788e$var$transitionCallbacks.clear();
    }
  };
  document.body.addEventListener("transitionrun", onTransitionStart);
  document.body.addEventListener("transitionend", onTransitionEnd);
}
if (typeof document !== "undefined") {
  if (document.readyState !== "loading") $081cb5757e08788e$var$setupGlobalEvents();
  else document.addEventListener("DOMContentLoaded", $081cb5757e08788e$var$setupGlobalEvents);
}
function $081cb5757e08788e$var$cleanupDetachedElements() {
  for (const [eventTarget] of $081cb5757e08788e$var$transitionsByElement)
    if ("isConnected" in eventTarget && !eventTarget.isConnected) $081cb5757e08788e$var$transitionsByElement.delete(eventTarget);
}
function $081cb5757e08788e$export$24490316f764c430(fn) {
  requestAnimationFrame(() => {
    $081cb5757e08788e$var$cleanupDetachedElements();
    if ($081cb5757e08788e$var$transitionsByElement.size === 0) fn();
    else $081cb5757e08788e$var$transitionCallbacks.add(fn);
  });
}

// node_modules/react-aria/dist/private/interactions/focusSafely.mjs
function $f192c2f16961cbe0$export$80f3e147d781571c(element4) {
  if (!element4.isConnected) return;
  const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(element4);
  if ((0, $8f5a2122b0992be3$export$630ff653c5ada6a9)() === "virtual") {
    let lastFocusedElement = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument);
    (0, $081cb5757e08788e$export$24490316f764c430)(() => {
      const activeElement2 = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument);
      if ((activeElement2 === lastFocusedElement || activeElement2 === ownerDocument.body) && element4.isConnected) (0, $1969ac565cfec8d0$export$de79e2c695e052f3)(element4);
    });
  } else (0, $1969ac565cfec8d0$export$de79e2c695e052f3)(element4);
}

// node_modules/react-aria/dist/private/focus/FocusScope.mjs
var import_react11 = __toESM(require_react(), 1);
var $535772f9d2c1f38d$var$FocusContext = (0, import_react11.default).createContext(null);
var $535772f9d2c1f38d$var$RESTORE_FOCUS_EVENT = "react-aria-focus-scope-restore";
var $535772f9d2c1f38d$var$activeScope = null;
function $535772f9d2c1f38d$export$20e40289641fbbb6(props) {
  let { children, contain, restoreFocus, autoFocus } = props;
  let startRef = (0, import_react11.useRef)(null);
  let endRef = (0, import_react11.useRef)(null);
  let scopeRef = (0, import_react11.useRef)([]);
  let { parentNode } = (0, import_react11.useContext)($535772f9d2c1f38d$var$FocusContext) || {};
  let node2 = (0, import_react11.useMemo)(() => new $535772f9d2c1f38d$var$TreeNode({
    scopeRef
  }), [
    scopeRef
  ]);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    let parent = parentNode || $535772f9d2c1f38d$export$d06fae2ee68b101e.root;
    if ($535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(parent.scopeRef) && $535772f9d2c1f38d$var$activeScope && !$535772f9d2c1f38d$var$isAncestorScope($535772f9d2c1f38d$var$activeScope, parent.scopeRef)) {
      let activeNode = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode($535772f9d2c1f38d$var$activeScope);
      if (activeNode) parent = activeNode;
    }
    parent.addChild(node2);
    $535772f9d2c1f38d$export$d06fae2ee68b101e.addNode(node2);
  }, [
    node2,
    parentNode
  ]);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    let node3 = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scopeRef);
    if (node3) node3.contain = !!contain;
  }, [
    contain
  ]);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    let node3 = startRef.current?.nextSibling;
    let nodes = [];
    let stopPropagation = (e) => e.stopPropagation();
    while (node3 && node3 !== endRef.current) {
      nodes.push(node3);
      node3.addEventListener($535772f9d2c1f38d$var$RESTORE_FOCUS_EVENT, stopPropagation);
      node3 = node3.nextSibling;
    }
    scopeRef.current = nodes;
    return () => {
      for (let node4 of nodes) node4.removeEventListener($535772f9d2c1f38d$var$RESTORE_FOCUS_EVENT, stopPropagation);
    };
  }, [
    children
  ]);
  $535772f9d2c1f38d$var$useActiveScopeTracker(scopeRef, restoreFocus, contain);
  $535772f9d2c1f38d$var$useFocusContainment(scopeRef, contain);
  $535772f9d2c1f38d$var$useRestoreFocus(scopeRef, restoreFocus, contain);
  $535772f9d2c1f38d$var$useAutoFocus(scopeRef, autoFocus);
  (0, import_react11.useEffect)(() => {
    const activeElement2 = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)((0, $d447af545b77c9f1$export$b204af158042fbac)(scopeRef.current ? scopeRef.current[0] : void 0));
    let scope = null;
    if ($535772f9d2c1f38d$var$isElementInScope(activeElement2, scopeRef.current)) {
      for (let node3 of $535772f9d2c1f38d$export$d06fae2ee68b101e.traverse()) if (node3.scopeRef && $535772f9d2c1f38d$var$isElementInScope(activeElement2, node3.scopeRef.current)) scope = node3;
      if (scope === $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scopeRef)) $535772f9d2c1f38d$var$activeScope = scope.scopeRef;
    }
  }, [
    scopeRef
  ]);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    return () => {
      let parentScope = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scopeRef)?.parent?.scopeRef ?? null;
      if ((scopeRef === $535772f9d2c1f38d$var$activeScope || $535772f9d2c1f38d$var$isAncestorScope(scopeRef, $535772f9d2c1f38d$var$activeScope)) && (!parentScope || $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(parentScope))) $535772f9d2c1f38d$var$activeScope = parentScope;
      $535772f9d2c1f38d$export$d06fae2ee68b101e.removeTreeNode(scopeRef);
    };
  }, [
    scopeRef
  ]);
  let focusManager = (0, import_react11.useMemo)(() => $535772f9d2c1f38d$var$createFocusManagerForScope(scopeRef), []);
  let value = (0, import_react11.useMemo)(() => ({
    focusManager,
    parentNode: node2
  }), [
    node2,
    focusManager
  ]);
  return (0, import_react11.default).createElement($535772f9d2c1f38d$var$FocusContext.Provider, {
    value
  }, (0, import_react11.default).createElement("span", {
    "data-focus-scope-start": true,
    hidden: true,
    ref: startRef
  }), children, (0, import_react11.default).createElement("span", {
    "data-focus-scope-end": true,
    hidden: true,
    ref: endRef
  }));
}
function $535772f9d2c1f38d$var$createFocusManagerForScope(scopeRef) {
  return {
    focusNext(opts = {}) {
      let scope = scopeRef.current;
      let { from, tabbable: tabbable2, wrap: wrap3, accept } = opts;
      let node2 = from || (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)((0, $d447af545b77c9f1$export$b204af158042fbac)(scope[0] ?? void 0));
      let sentinel = scope[0].previousElementSibling;
      let scopeRoot = $535772f9d2c1f38d$var$getScopeRoot(scope);
      let walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(scopeRoot, {
        tabbable: tabbable2,
        accept
      }, scope);
      walker.currentNode = $535772f9d2c1f38d$var$isElementInScope(node2, scope) ? node2 : sentinel;
      let nextNode = walker.nextNode();
      if (!nextNode && wrap3) {
        walker.currentNode = sentinel;
        nextNode = walker.nextNode();
      }
      if (nextNode) $535772f9d2c1f38d$var$focusElement(nextNode, true);
      return nextNode;
    },
    focusPrevious(opts = {}) {
      let scope = scopeRef.current;
      let { from, tabbable: tabbable2, wrap: wrap3, accept } = opts;
      let node2 = from || (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)((0, $d447af545b77c9f1$export$b204af158042fbac)(scope[0] ?? void 0));
      let sentinel = scope[scope.length - 1].nextElementSibling;
      let scopeRoot = $535772f9d2c1f38d$var$getScopeRoot(scope);
      let walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(scopeRoot, {
        tabbable: tabbable2,
        accept
      }, scope);
      walker.currentNode = $535772f9d2c1f38d$var$isElementInScope(node2, scope) ? node2 : sentinel;
      let previousNode = walker.previousNode();
      if (!previousNode && wrap3) {
        walker.currentNode = sentinel;
        previousNode = walker.previousNode();
      }
      if (previousNode) $535772f9d2c1f38d$var$focusElement(previousNode, true);
      return previousNode;
    },
    focusFirst(opts = {}) {
      let scope = scopeRef.current;
      let { tabbable: tabbable2, accept } = opts;
      let scopeRoot = $535772f9d2c1f38d$var$getScopeRoot(scope);
      let walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(scopeRoot, {
        tabbable: tabbable2,
        accept
      }, scope);
      walker.currentNode = scope[0].previousElementSibling;
      let nextNode = walker.nextNode();
      if (nextNode) $535772f9d2c1f38d$var$focusElement(nextNode, true);
      return nextNode;
    },
    focusLast(opts = {}) {
      let scope = scopeRef.current;
      let { tabbable: tabbable2, accept } = opts;
      let scopeRoot = $535772f9d2c1f38d$var$getScopeRoot(scope);
      let walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(scopeRoot, {
        tabbable: tabbable2,
        accept
      }, scope);
      walker.currentNode = scope[scope.length - 1].nextElementSibling;
      let previousNode = walker.previousNode();
      if (previousNode) $535772f9d2c1f38d$var$focusElement(previousNode, true);
      return previousNode;
    }
  };
}
function $535772f9d2c1f38d$var$getScopeRoot(scope) {
  return scope[0].parentElement;
}
function $535772f9d2c1f38d$var$shouldContainFocus(scopeRef) {
  let scope = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode($535772f9d2c1f38d$var$activeScope);
  while (scope && scope.scopeRef !== scopeRef) {
    if (scope.contain) return false;
    scope = scope.parent;
  }
  return true;
}
function $535772f9d2c1f38d$var$getRadiosInGroup(element4) {
  if (!element4.form)
    return Array.from((0, $d447af545b77c9f1$export$b204af158042fbac)(element4).querySelectorAll(`input[type="radio"][name="${CSS.escape(element4.name)}"]`)).filter((radio) => !radio.form);
  const radioList = element4.form.elements.namedItem(element4.name);
  let ownerWindow = (0, $d447af545b77c9f1$export$f21a1ffae260145a)(element4);
  if (radioList instanceof ownerWindow.RadioNodeList) return Array.from(radioList).filter((el) => el instanceof ownerWindow.HTMLInputElement);
  if (radioList instanceof ownerWindow.HTMLInputElement) return [
    radioList
  ];
  return [];
}
function $535772f9d2c1f38d$var$isTabbableRadio(element4) {
  if (element4.checked) return true;
  const radios = $535772f9d2c1f38d$var$getRadiosInGroup(element4);
  return radios.length > 0 && !radios.some((radio) => radio.checked);
}
function $535772f9d2c1f38d$var$useFocusContainment(scopeRef, contain) {
  let focusedNode = (0, import_react11.useRef)(void 0);
  let raf = (0, import_react11.useRef)(void 0);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    let scope = scopeRef.current;
    if (!contain) {
      if (raf.current) {
        cancelAnimationFrame(raf.current);
        raf.current = void 0;
      }
      return;
    }
    const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(scope ? scope[0] : void 0);
    let onKeyDown = (e) => {
      if (e.key !== "Tab" || e.altKey || e.ctrlKey || e.metaKey || !$535772f9d2c1f38d$var$shouldContainFocus(scopeRef) || e.isComposing) return;
      let focusedElement = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument);
      let scope2 = scopeRef.current;
      if (!scope2 || !$535772f9d2c1f38d$var$isElementInScope(focusedElement, scope2)) return;
      let scopeRoot = $535772f9d2c1f38d$var$getScopeRoot(scope2);
      let walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(scopeRoot, {
        tabbable: true
      }, scope2);
      if (!focusedElement) return;
      walker.currentNode = focusedElement;
      let nextElement = e.shiftKey ? walker.previousNode() : walker.nextNode();
      if (!nextElement) {
        walker.currentNode = e.shiftKey ? scope2[scope2.length - 1].nextElementSibling : scope2[0].previousElementSibling;
        nextElement = e.shiftKey ? walker.previousNode() : walker.nextNode();
      }
      e.preventDefault();
      if (nextElement) {
        $535772f9d2c1f38d$var$focusElement(nextElement, true);
        if (nextElement instanceof (0, $d447af545b77c9f1$export$f21a1ffae260145a)(nextElement).HTMLInputElement) nextElement.select();
      }
    };
    let onFocus = (e) => {
      if ((!$535772f9d2c1f38d$var$activeScope || $535772f9d2c1f38d$var$isAncestorScope($535772f9d2c1f38d$var$activeScope, scopeRef)) && $535772f9d2c1f38d$var$isElementInScope((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e), scopeRef.current)) {
        $535772f9d2c1f38d$var$activeScope = scopeRef;
        focusedNode.current = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
      } else if ($535772f9d2c1f38d$var$shouldContainFocus(scopeRef) && !$535772f9d2c1f38d$var$isElementInChildScope((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e), scopeRef)) {
        if (focusedNode.current) focusedNode.current.focus();
        else if ($535772f9d2c1f38d$var$activeScope && $535772f9d2c1f38d$var$activeScope.current) $535772f9d2c1f38d$var$focusFirstInScope($535772f9d2c1f38d$var$activeScope.current);
      } else if ($535772f9d2c1f38d$var$shouldContainFocus(scopeRef)) focusedNode.current = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
    };
    let onBlur = (e) => {
      if (raf.current) cancelAnimationFrame(raf.current);
      raf.current = requestAnimationFrame(() => {
        let modality = (0, $8f5a2122b0992be3$export$630ff653c5ada6a9)();
        let shouldSkipFocusRestore = (modality === "virtual" || modality === null) && (0, $2add3ce32c6007eb$export$a11b0059900ceec8)() && (0, $2add3ce32c6007eb$export$6446a186d09e379e)();
        let activeElement2 = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument);
        if (!shouldSkipFocusRestore && activeElement2 && $535772f9d2c1f38d$var$shouldContainFocus(scopeRef) && !$535772f9d2c1f38d$var$isElementInChildScope(activeElement2, scopeRef)) {
          $535772f9d2c1f38d$var$activeScope = scopeRef;
          let target = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
          if (target && target.isConnected) {
            focusedNode.current = target;
            focusedNode.current?.focus();
          } else if ($535772f9d2c1f38d$var$activeScope.current) $535772f9d2c1f38d$var$focusFirstInScope($535772f9d2c1f38d$var$activeScope.current);
        }
      });
    };
    ownerDocument.addEventListener("keydown", onKeyDown, false);
    ownerDocument.addEventListener("focusin", onFocus, false);
    scope?.forEach((element4) => element4.addEventListener("focusin", onFocus, false));
    scope?.forEach((element4) => element4.addEventListener("focusout", onBlur, false));
    return () => {
      ownerDocument.removeEventListener("keydown", onKeyDown, false);
      ownerDocument.removeEventListener("focusin", onFocus, false);
      scope?.forEach((element4) => element4.removeEventListener("focusin", onFocus, false));
      scope?.forEach((element4) => element4.removeEventListener("focusout", onBlur, false));
    };
  }, [
    scopeRef,
    contain
  ]);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    return () => {
      if (raf.current) cancelAnimationFrame(raf.current);
    };
  }, [
    raf
  ]);
}
function $535772f9d2c1f38d$var$isElementInAnyScope(element4) {
  return $535772f9d2c1f38d$var$isElementInChildScope(element4);
}
function $535772f9d2c1f38d$var$isElementInScope(element4, scope) {
  if (!element4) return false;
  if (!scope) return false;
  return scope.some((node2) => (0, $23f2114a1b82827e$export$4282f70798064fe0)(node2, element4));
}
function $535772f9d2c1f38d$var$isElementInChildScope(element4, scope = null) {
  if (element4 instanceof Element && element4.closest("[data-react-aria-top-layer]")) return true;
  for (let { scopeRef: s2 } of $535772f9d2c1f38d$export$d06fae2ee68b101e.traverse($535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scope))) {
    if (s2 && $535772f9d2c1f38d$var$isElementInScope(element4, s2.current)) return true;
  }
  return false;
}
function $535772f9d2c1f38d$var$isAncestorScope(ancestor, scope) {
  let parent = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scope)?.parent;
  while (parent) {
    if (parent.scopeRef === ancestor) return true;
    parent = parent.parent;
  }
  return false;
}
function $535772f9d2c1f38d$var$focusElement(element4, scroll = false) {
  if (element4 != null && !scroll) try {
    (0, $f192c2f16961cbe0$export$80f3e147d781571c)(element4);
  } catch {
  }
  else if (element4 != null) try {
    element4.focus();
  } catch {
  }
}
function $535772f9d2c1f38d$var$getFirstInScope(scope, tabbable2 = true) {
  let sentinel = scope[0].previousElementSibling;
  let scopeRoot = $535772f9d2c1f38d$var$getScopeRoot(scope);
  let walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(scopeRoot, {
    tabbable: tabbable2
  }, scope);
  walker.currentNode = sentinel;
  let nextNode = walker.nextNode();
  if (tabbable2 && !nextNode) {
    scopeRoot = $535772f9d2c1f38d$var$getScopeRoot(scope);
    walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(scopeRoot, {
      tabbable: false
    }, scope);
    walker.currentNode = sentinel;
    nextNode = walker.nextNode();
  }
  return nextNode;
}
function $535772f9d2c1f38d$var$focusFirstInScope(scope, tabbable2 = true) {
  $535772f9d2c1f38d$var$focusElement($535772f9d2c1f38d$var$getFirstInScope(scope, tabbable2));
}
function $535772f9d2c1f38d$var$useAutoFocus(scopeRef, autoFocus) {
  const autoFocusRef = (0, import_react11.default).useRef(autoFocus);
  (0, import_react11.useEffect)(() => {
    if (autoFocusRef.current) {
      $535772f9d2c1f38d$var$activeScope = scopeRef;
      const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(scopeRef.current ? scopeRef.current[0] : void 0);
      if (!$535772f9d2c1f38d$var$isElementInScope((0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument), $535772f9d2c1f38d$var$activeScope.current) && scopeRef.current) $535772f9d2c1f38d$var$focusFirstInScope(scopeRef.current);
    }
    autoFocusRef.current = false;
  }, [
    scopeRef
  ]);
}
function $535772f9d2c1f38d$var$useActiveScopeTracker(scopeRef, restore, contain) {
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    if (restore || contain) return;
    let scope = scopeRef.current;
    const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(scope ? scope[0] : void 0);
    let onFocus = (e) => {
      let target = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
      if ($535772f9d2c1f38d$var$isElementInScope(target, scopeRef.current)) $535772f9d2c1f38d$var$activeScope = scopeRef;
      else if (!$535772f9d2c1f38d$var$isElementInAnyScope(target)) $535772f9d2c1f38d$var$activeScope = null;
    };
    ownerDocument.addEventListener("focusin", onFocus, false);
    scope?.forEach((element4) => element4.addEventListener("focusin", onFocus, false));
    return () => {
      ownerDocument.removeEventListener("focusin", onFocus, false);
      scope?.forEach((element4) => element4.removeEventListener("focusin", onFocus, false));
    };
  }, [
    scopeRef,
    restore,
    contain
  ]);
}
function $535772f9d2c1f38d$var$shouldRestoreFocus(scopeRef) {
  let scope = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode($535772f9d2c1f38d$var$activeScope);
  while (scope && scope.scopeRef !== scopeRef) {
    if (scope.nodeToRestore) return false;
    scope = scope.parent;
  }
  return scope?.scopeRef === scopeRef;
}
function $535772f9d2c1f38d$var$useRestoreFocus(scopeRef, restoreFocus, contain) {
  const nodeToRestoreRef = (0, import_react11.useRef)(typeof document !== "undefined" ? (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)((0, $d447af545b77c9f1$export$b204af158042fbac)(scopeRef.current ? scopeRef.current[0] : void 0)) : null);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    let scope = scopeRef.current;
    const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(scope ? scope[0] : void 0);
    if (!restoreFocus || contain) return;
    let onFocus = () => {
      if ((!$535772f9d2c1f38d$var$activeScope || $535772f9d2c1f38d$var$isAncestorScope($535772f9d2c1f38d$var$activeScope, scopeRef)) && $535772f9d2c1f38d$var$isElementInScope((0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument), scopeRef.current)) $535772f9d2c1f38d$var$activeScope = scopeRef;
    };
    ownerDocument.addEventListener("focusin", onFocus, false);
    scope?.forEach((element4) => element4.addEventListener("focusin", onFocus, false));
    return () => {
      ownerDocument.removeEventListener("focusin", onFocus, false);
      scope?.forEach((element4) => element4.removeEventListener("focusin", onFocus, false));
    };
  }, [
    scopeRef,
    contain
  ]);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(scopeRef.current ? scopeRef.current[0] : void 0);
    if (!restoreFocus) return;
    let onKeyDown = (e) => {
      if (e.key !== "Tab" || e.altKey || e.ctrlKey || e.metaKey || !$535772f9d2c1f38d$var$shouldContainFocus(scopeRef) || e.isComposing) return;
      let focusedElement = ownerDocument.activeElement;
      if (!$535772f9d2c1f38d$var$isElementInChildScope(focusedElement, scopeRef) || !$535772f9d2c1f38d$var$shouldRestoreFocus(scopeRef)) return;
      let treeNode = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scopeRef);
      if (!treeNode) return;
      let nodeToRestore = treeNode.nodeToRestore;
      let walker = $535772f9d2c1f38d$export$2d6ec8fc375ceafa(ownerDocument.body, {
        tabbable: true
      });
      walker.currentNode = focusedElement;
      let nextElement = e.shiftKey ? walker.previousNode() : walker.nextNode();
      if (!nodeToRestore || !nodeToRestore.isConnected || nodeToRestore === ownerDocument.body) {
        nodeToRestore = void 0;
        treeNode.nodeToRestore = void 0;
      }
      if ((!nextElement || !$535772f9d2c1f38d$var$isElementInChildScope(nextElement, scopeRef)) && nodeToRestore) {
        walker.currentNode = nodeToRestore;
        do
          nextElement = e.shiftKey ? walker.previousNode() : walker.nextNode();
        while ($535772f9d2c1f38d$var$isElementInChildScope(nextElement, scopeRef));
        e.preventDefault();
        e.stopPropagation();
        if (nextElement) $535772f9d2c1f38d$var$focusElement(nextElement, true);
        else if (!$535772f9d2c1f38d$var$isElementInAnyScope(nodeToRestore)) focusedElement.blur();
        else $535772f9d2c1f38d$var$focusElement(nodeToRestore, true);
      }
    };
    if (!contain) ownerDocument.addEventListener("keydown", onKeyDown, true);
    return () => {
      if (!contain) ownerDocument.removeEventListener("keydown", onKeyDown, true);
    };
  }, [
    scopeRef,
    restoreFocus,
    contain
  ]);
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(scopeRef.current ? scopeRef.current[0] : void 0);
    if (!restoreFocus) return;
    let treeNode = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scopeRef);
    if (!treeNode) return;
    treeNode.nodeToRestore = nodeToRestoreRef.current ?? void 0;
    return () => {
      let treeNode2 = $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(scopeRef);
      if (!treeNode2) return;
      let nodeToRestore = treeNode2.nodeToRestore;
      let activeElement2 = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument);
      if (restoreFocus && nodeToRestore && (activeElement2 && $535772f9d2c1f38d$var$isElementInChildScope(activeElement2, scopeRef) || activeElement2 === ownerDocument.body && $535772f9d2c1f38d$var$shouldRestoreFocus(scopeRef))) {
        let clonedTree = $535772f9d2c1f38d$export$d06fae2ee68b101e.clone();
        requestAnimationFrame(() => {
          if (ownerDocument.activeElement === ownerDocument.body) {
            let treeNode3 = clonedTree.getTreeNode(scopeRef);
            while (treeNode3) {
              if (treeNode3.nodeToRestore && treeNode3.nodeToRestore.isConnected) {
                $535772f9d2c1f38d$var$restoreFocusToElement(treeNode3.nodeToRestore);
                return;
              }
              treeNode3 = treeNode3.parent;
            }
            treeNode3 = clonedTree.getTreeNode(scopeRef);
            while (treeNode3) {
              if (treeNode3.scopeRef && treeNode3.scopeRef.current && $535772f9d2c1f38d$export$d06fae2ee68b101e.getTreeNode(treeNode3.scopeRef)) {
                let node2 = $535772f9d2c1f38d$var$getFirstInScope(treeNode3.scopeRef.current, true);
                $535772f9d2c1f38d$var$restoreFocusToElement(node2);
                return;
              }
              treeNode3 = treeNode3.parent;
            }
          }
        });
      }
    };
  }, [
    scopeRef,
    restoreFocus
  ]);
}
function $535772f9d2c1f38d$var$restoreFocusToElement(node2) {
  if (node2.dispatchEvent(new CustomEvent($535772f9d2c1f38d$var$RESTORE_FOCUS_EVENT, {
    bubbles: true,
    cancelable: true
  }))) $535772f9d2c1f38d$var$focusElement(node2);
}
function $535772f9d2c1f38d$export$2d6ec8fc375ceafa(root4, opts, scope) {
  let filter2 = opts?.tabbable ? (0, $3b8b240c1bf84ab9$export$bebd5a1431fec25d) : (0, $3b8b240c1bf84ab9$export$4c063cf1350e6fed);
  let rootElement = root4?.nodeType === Node.ELEMENT_NODE ? root4 : null;
  let doc = (0, $d447af545b77c9f1$export$b204af158042fbac)(rootElement);
  let walker = (0, $654b97e09f2a30c1$export$4d0f8be8b12a7ef6)(doc, root4 || doc, NodeFilter.SHOW_ELEMENT, {
    acceptNode(node2) {
      if ((0, $23f2114a1b82827e$export$4282f70798064fe0)(opts?.from, node2)) return NodeFilter.FILTER_REJECT;
      if (opts?.tabbable && node2.tagName === "INPUT" && node2.getAttribute("type") === "radio") {
        if (!$535772f9d2c1f38d$var$isTabbableRadio(node2)) return NodeFilter.FILTER_REJECT;
        if (walker.currentNode.tagName === "INPUT" && walker.currentNode.type === "radio" && walker.currentNode.name === node2.name) return NodeFilter.FILTER_REJECT;
      }
      if (filter2(node2) && (!scope || $535772f9d2c1f38d$var$isElementInScope(node2, scope)) && (!opts?.accept || opts.accept(node2))) return NodeFilter.FILTER_ACCEPT;
      return NodeFilter.FILTER_SKIP;
    }
  });
  if (opts?.from) walker.currentNode = opts.from;
  return walker;
}
var $535772f9d2c1f38d$var$Tree = class _$535772f9d2c1f38d$var$Tree {
  constructor() {
    this.fastMap = /* @__PURE__ */ new Map();
    this.root = new $535772f9d2c1f38d$var$TreeNode({
      scopeRef: null
    });
    this.fastMap.set(null, this.root);
  }
  get size() {
    return this.fastMap.size;
  }
  getTreeNode(data) {
    return this.fastMap.get(data);
  }
  addTreeNode(scopeRef, parent, nodeToRestore) {
    let parentNode = this.fastMap.get(parent ?? null);
    if (!parentNode) return;
    let node2 = new $535772f9d2c1f38d$var$TreeNode({
      scopeRef
    });
    parentNode.addChild(node2);
    node2.parent = parentNode;
    this.fastMap.set(scopeRef, node2);
    if (nodeToRestore) node2.nodeToRestore = nodeToRestore;
  }
  addNode(node2) {
    this.fastMap.set(node2.scopeRef, node2);
  }
  removeTreeNode(scopeRef) {
    if (scopeRef === null) return;
    let node2 = this.fastMap.get(scopeRef);
    if (!node2) return;
    let parentNode = node2.parent;
    for (let current of this.traverse()) if (current !== node2 && node2.nodeToRestore && current.nodeToRestore && node2.scopeRef && node2.scopeRef.current && $535772f9d2c1f38d$var$isElementInScope(current.nodeToRestore, node2.scopeRef.current)) current.nodeToRestore = node2.nodeToRestore;
    let children = node2.children;
    if (parentNode) {
      parentNode.removeChild(node2);
      if (children.size > 0) children.forEach((child) => parentNode && parentNode.addChild(child));
    }
    this.fastMap.delete(node2.scopeRef);
  }
  // Pre Order Depth First
  *traverse(node2 = this.root) {
    if (node2.scopeRef != null) yield node2;
    if (node2.children.size > 0) for (let child of node2.children) yield* this.traverse(child);
  }
  clone() {
    let newTree = new _$535772f9d2c1f38d$var$Tree();
    for (let node2 of this.traverse()) newTree.addTreeNode(node2.scopeRef, node2.parent?.scopeRef ?? null, node2.nodeToRestore);
    return newTree;
  }
};
var $535772f9d2c1f38d$var$TreeNode = class {
  constructor(props) {
    this.children = /* @__PURE__ */ new Set();
    this.contain = false;
    this.scopeRef = props.scopeRef;
  }
  addChild(node2) {
    this.children.add(node2);
    node2.parent = this;
  }
  removeChild(node2) {
    this.children.delete(node2);
    node2.parent = void 0;
  }
};
var $535772f9d2c1f38d$export$d06fae2ee68b101e = new $535772f9d2c1f38d$var$Tree();

// node_modules/react-aria/dist/private/utils/chain.mjs
function $a4e76a5424781910$export$e08e3b67e392101e(...callbacks) {
  return (...args) => {
    for (let callback of callbacks) if (typeof callback === "function") callback(...args);
  };
}

// node_modules/react-aria/dist/private/utils/useValueEffect.mjs
var import_react12 = __toESM(require_react(), 1);

// node_modules/react-aria/dist/private/utils/useId.mjs
var import_react13 = __toESM(require_react(), 1);
var $390e54f620492c70$var$canUseDOM = Boolean(typeof window !== "undefined" && window.document && window.document.createElement);
var $390e54f620492c70$export$d41a04c74483c6ef = /* @__PURE__ */ new Map();
var $390e54f620492c70$var$registry;
if (typeof FinalizationRegistry !== "undefined") $390e54f620492c70$var$registry = new FinalizationRegistry((heldValue) => {
  $390e54f620492c70$export$d41a04c74483c6ef.delete(heldValue);
});
function $390e54f620492c70$export$cd8c9cb68f842629(idA, idB) {
  if (idA === idB) return idA;
  let setIdsA = $390e54f620492c70$export$d41a04c74483c6ef.get(idA);
  if (setIdsA) {
    setIdsA.forEach((ref) => ref.current = idB);
    return idB;
  }
  let setIdsB = $390e54f620492c70$export$d41a04c74483c6ef.get(idB);
  if (setIdsB) {
    setIdsB.forEach((ref) => ref.current = idA);
    return idA;
  }
  return idB;
}

// node_modules/react-aria/dist/private/utils/mergeRefs.mjs
function $4064df0d6f9620e1$export$c9058316764c140e(...refs) {
  if (refs.length === 1 && refs[0]) return refs[0];
  return (value) => {
    let hasCleanup = false;
    const cleanups = refs.map((ref) => {
      const cleanup2 = $4064df0d6f9620e1$var$setRef(ref, value);
      hasCleanup ||= typeof cleanup2 == "function";
      return cleanup2;
    });
    if (hasCleanup) return () => {
      cleanups.forEach((cleanup2, i2) => {
        if (typeof cleanup2 === "function") cleanup2();
        else $4064df0d6f9620e1$var$setRef(refs[i2], null);
      });
    };
  };
}
function $4064df0d6f9620e1$var$setRef(ref, value) {
  if (typeof ref === "function") return ref(value);
  else if (ref != null) ref.current = value;
}

// node_modules/react-aria/dist/private/utils/mergeProps.mjs
function $bbaa08b3cd72f041$export$9d1611c77c2fe928(...args) {
  let result = {
    ...args[0]
  };
  for (let i2 = 1; i2 < args.length; i2++) {
    let props = args[i2];
    for (let key in props) {
      let a2 = result[key];
      let b2 = props[key];
      if (typeof a2 === "function" && typeof b2 === "function" && // This is a lot faster than a regex.
      key[0] === "o" && key[1] === "n" && key.charCodeAt(2) >= /* 'A' */
      65 && key.charCodeAt(2) <= /* 'Z' */
      90) result[key] = (0, $a4e76a5424781910$export$e08e3b67e392101e)(a2, b2);
      else if ((key === "className" || key === "UNSAFE_className") && typeof a2 === "string" && typeof b2 === "string") result[key] = (0, clsx_default)(a2, b2);
      else if (key === "id" && a2 && b2) result.id = (0, $390e54f620492c70$export$cd8c9cb68f842629)(a2, b2);
      else if (key === "ref" && a2 && b2) result.ref = (0, $4064df0d6f9620e1$export$c9058316764c140e)(a2, b2);
      else result[key] = b2 !== void 0 ? b2 : a2;
    }
  }
  return result;
}

// node_modules/react-aria/dist/private/interactions/useFocus.mjs
var import_react14 = __toESM(require_react(), 1);
function $1e74c67db218ce67$export$f8168d8dd8fd66e6(props) {
  let { isDisabled, onFocus: onFocusProp, onBlur: onBlurProp, onFocusChange } = props;
  const onBlur = (0, import_react14.useCallback)((e) => {
    if ((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e) === e.currentTarget) {
      if (onBlurProp) onBlurProp(e);
      if (onFocusChange) onFocusChange(false);
      return true;
    }
  }, [
    onBlurProp,
    onFocusChange
  ]);
  const onSyntheticFocus = (0, $a92dc41f639950be$export$715c682d09d639cc)(onBlur);
  const onFocus = (0, import_react14.useCallback)((e) => {
    let eventTarget = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e);
    const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(eventTarget);
    const activeElement2 = ownerDocument ? (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument) : (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)();
    if (eventTarget === e.currentTarget && eventTarget === activeElement2) {
      if (onFocusProp) onFocusProp(e);
      if (onFocusChange) onFocusChange(true);
      onSyntheticFocus(e);
    }
  }, [
    onFocusChange,
    onFocusProp,
    onSyntheticFocus
  ]);
  return {
    focusProps: {
      onFocus: !isDisabled && (onFocusProp || onFocusChange || onBlurProp) ? onFocus : void 0,
      onBlur: !isDisabled && (onBlurProp || onFocusChange) ? onBlur : void 0
    }
  };
}

// node_modules/react-aria/dist/private/utils/useGlobalListeners.mjs
var import_react15 = __toESM(require_react(), 1);

// node_modules/react-aria/dist/private/interactions/useFocusWithin.mjs
var import_react16 = __toESM(require_react(), 1);

// node_modules/react-aria/dist/private/focus/useFocusRing.mjs
var import_react17 = __toESM(require_react(), 1);

// node_modules/react-aria/dist/private/focus/FocusRing.mjs
var import_react18 = __toESM(require_react(), 1);

// node_modules/react-aria/dist/private/focus/useHasTabbableChild.mjs
var import_react19 = __toESM(require_react(), 1);

// node_modules/react-aria/dist/private/interactions/createEventHandler.mjs
function $8dba16319206abb6$export$48d1ea6320830260(handler) {
  if (!handler) return void 0;
  let shouldStopPropagation = true;
  return (e) => {
    let event = {
      ...e,
      preventDefault() {
        e.preventDefault();
      },
      isDefaultPrevented() {
        return e.isDefaultPrevented();
      },
      stopPropagation() {
        if (shouldStopPropagation && true) console.error("stopPropagation is now the default behavior for events in React Spectrum. You can use continuePropagation() to revert this behavior.");
        else shouldStopPropagation = true;
      },
      continuePropagation() {
        shouldStopPropagation = false;
      },
      isPropagationStopped() {
        return shouldStopPropagation;
      }
    };
    handler(event);
    if (shouldStopPropagation) e.stopPropagation();
  };
}

// node_modules/react-aria/dist/private/interactions/useKeyboard.mjs
function $8296dad1a4c5e0dc$export$8f71654801c2f7cd(props) {
  return {
    keyboardProps: props.isDisabled ? {} : {
      onKeyDown: (0, $8dba16319206abb6$export$48d1ea6320830260)(props.onKeyDown),
      onKeyUp: (0, $8dba16319206abb6$export$48d1ea6320830260)(props.onKeyUp)
    }
  };
}

// node_modules/react-aria/dist/private/utils/useObjectRef.mjs
var import_react20 = __toESM(require_react(), 1);
function $03e8ab2d84d7657a$export$4338b53315abf666(ref) {
  const objRef = (0, import_react20.useRef)(null);
  const cleanupRef = (0, import_react20.useRef)(void 0);
  const refEffect = (0, import_react20.useCallback)((instance2) => {
    if (typeof ref === "function") {
      const refCallback = ref;
      const refCleanup = refCallback(instance2);
      return () => {
        if (typeof refCleanup === "function") refCleanup();
        else refCallback(null);
      };
    } else if (ref) {
      ref.current = instance2;
      return () => {
        ref.current = null;
      };
    }
  }, [
    ref
  ]);
  return (0, import_react20.useMemo)(() => ({
    get current() {
      return objRef.current;
    },
    set current(value) {
      objRef.current = value;
      if (cleanupRef.current) {
        cleanupRef.current();
        cleanupRef.current = void 0;
      }
      if (value != null) cleanupRef.current = refEffect(value);
    }
  }), [
    refEffect
  ]);
}

// node_modules/react-aria/dist/private/utils/useSyncRef.mjs
function $b7115c395c64f7b5$export$4debdb1a3f0fa79e(context, ref) {
  (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
    if (context && context.ref && ref) {
      context.ref.current = ref.current;
      return () => {
        if (context.ref) context.ref.current = null;
      };
    }
  });
}

// node_modules/react-aria/dist/private/interactions/useFocusable.mjs
var import_react21 = __toESM(require_react(), 1);
var $d1116acdf220c2da$export$f9762fab77588ecb = (0, import_react21.default).createContext(null);
function $d1116acdf220c2da$var$useFocusableContext(ref) {
  let context = (0, import_react21.useContext)($d1116acdf220c2da$export$f9762fab77588ecb) || {};
  (0, $b7115c395c64f7b5$export$4debdb1a3f0fa79e)(context, ref);
  let { ref: _2, ...otherProps } = context;
  return otherProps;
}
var $d1116acdf220c2da$export$13f3202a3e5ddd5 = (0, import_react21.default).forwardRef(function FocusableProvider(props, ref) {
  let { children, ...otherProps } = props;
  let objRef = (0, $03e8ab2d84d7657a$export$4338b53315abf666)(ref);
  let context = {
    ...otherProps,
    ref: objRef
  };
  return (0, import_react21.default).createElement($d1116acdf220c2da$export$f9762fab77588ecb.Provider, {
    value: context
  }, children);
});
function $d1116acdf220c2da$export$4c014de7c8940b4c(props, domRef) {
  let { focusProps } = (0, $1e74c67db218ce67$export$f8168d8dd8fd66e6)(props);
  let { keyboardProps } = (0, $8296dad1a4c5e0dc$export$8f71654801c2f7cd)(props);
  let interactions = (0, $bbaa08b3cd72f041$export$9d1611c77c2fe928)(focusProps, keyboardProps);
  let domProps = $d1116acdf220c2da$var$useFocusableContext(domRef);
  let interactionProps = props.isDisabled ? {} : domProps;
  let autoFocusRef = (0, import_react21.useRef)(props.autoFocus);
  (0, import_react21.useEffect)(() => {
    if (autoFocusRef.current && domRef.current) (0, $f192c2f16961cbe0$export$80f3e147d781571c)(domRef.current);
    autoFocusRef.current = false;
  }, [
    domRef
  ]);
  let tabIndex = props.excludeFromTabOrder ? -1 : 0;
  if (props.isDisabled) tabIndex = void 0;
  return {
    focusableProps: (0, $bbaa08b3cd72f041$export$9d1611c77c2fe928)({
      ...interactions,
      tabIndex
    }, interactionProps)
  };
}
var $d1116acdf220c2da$export$35a3bebf7ef2d934 = (0, import_react21.forwardRef)(({ children, ...props }, ref) => {
  ref = (0, $03e8ab2d84d7657a$export$4338b53315abf666)(ref);
  let { focusableProps } = $d1116acdf220c2da$export$4c014de7c8940b4c(props, ref);
  let child = (0, import_react21.default).Children.only(children);
  (0, import_react21.useEffect)(() => {
    if (false) return;
    let el = ref.current;
    if (!el || !(el instanceof (0, $d447af545b77c9f1$export$f21a1ffae260145a)(el).Element)) {
      console.error("<Focusable> child must forward its ref to a DOM element.");
      return;
    }
    if (!props.isDisabled && !(0, $3b8b240c1bf84ab9$export$4c063cf1350e6fed)(el)) {
      console.warn("<Focusable> child must be focusable. Please ensure the tabIndex prop is passed through.");
      return;
    }
    if (el.localName !== "button" && el.localName !== "input" && el.localName !== "select" && el.localName !== "textarea" && el.localName !== "a" && el.localName !== "area" && el.localName !== "summary" && el.localName !== "img" && el.localName !== "svg") {
      let role = el.getAttribute("role");
      if (!role) console.warn("<Focusable> child must have an interactive ARIA role.");
      else if (
        // https://w3c.github.io/aria/#widget_roles
        role !== "application" && role !== "button" && role !== "checkbox" && role !== "combobox" && role !== "gridcell" && role !== "link" && role !== "menuitem" && role !== "menuitemcheckbox" && role !== "menuitemradio" && role !== "option" && role !== "radio" && role !== "searchbox" && role !== "separator" && role !== "slider" && role !== "spinbutton" && role !== "switch" && role !== "tab" && role !== "tabpanel" && role !== "textbox" && role !== "treeitem" && // aria-describedby is also announced on these roles
        role !== "img" && role !== "meter" && role !== "progressbar"
      ) console.warn(`<Focusable> child must have an interactive ARIA role. Got "${role}".`);
    }
  }, [
    ref,
    props.isDisabled
  ]);
  let childRef = parseInt((0, import_react21.default).version, 10) < 19 ? child.ref : child.props.ref;
  return (0, import_react21.default).cloneElement(child, {
    ...(0, $bbaa08b3cd72f041$export$9d1611c77c2fe928)(focusableProps, child.props),
    // @ts-ignore
    ref: (0, $4064df0d6f9620e1$export$c9058316764c140e)(childRef, ref)
  });
});

// node_modules/stream-chat-react/dist/es/index.mjs
var import_react_dom4 = __toESM(require_react_dom(), 1);

// node_modules/devlop/lib/development.js
var AssertionError = class extends Error {
  name = (
    /** @type {const} */
    "Assertion"
  );
  code = (
    /** @type {const} */
    "ERR_ASSERTION"
  );
  /**
   * Create an assertion error.
   *
   * @param {string} message
   *   Message explaining error.
   * @param {unknown} actual
   *   Value.
   * @param {unknown} expected
   *   Baseline.
   * @param {string} operator
   *   Name of equality operation.
   * @param {boolean} generated
   *   Whether `message` is a custom message or not
   * @returns
   *   Instance.
   */
  // eslint-disable-next-line max-params
  constructor(message, actual, expected, operator, generated) {
    super(message);
    if (Error.captureStackTrace) {
      Error.captureStackTrace(this, this.constructor);
    }
    this.actual = actual;
    this.expected = expected;
    this.generated = generated;
    this.operator = operator;
  }
};
function ok(value, message) {
  assert(
    Boolean(value),
    false,
    true,
    "ok",
    "Expected value to be truthy",
    message
  );
}
function unreachable(message) {
  assert(false, false, true, "ok", "Unreachable", message);
}
function assert(bool, actual, expected, operator, defaultMessage, userMessage) {
  if (!bool) {
    throw userMessage instanceof Error ? userMessage : new AssertionError(
      userMessage || defaultMessage,
      actual,
      expected,
      operator,
      !userMessage
    );
  }
}

// node_modules/comma-separated-tokens/index.js
function stringify(values2, options) {
  const settings = options || {};
  const input = values2[values2.length - 1] === "" ? [...values2, ""] : values2;
  return input.join(
    (settings.padRight ? " " : "") + "," + (settings.padLeft === false ? "" : " ")
  ).trim();
}

// node_modules/estree-util-is-identifier-name/lib/index.js
var nameRe = /^[$_\p{ID_Start}][$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
var nameReJsx = /^[$_\p{ID_Start}][-$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
var emptyOptions = {};
function name(name2, options) {
  const settings = options || emptyOptions;
  const re3 = settings.jsx ? nameReJsx : nameRe;
  return re3.test(name2);
}

// node_modules/hast-util-whitespace/lib/index.js
var re = /[ \t\n\f\r]/g;
function whitespace(thing) {
  return typeof thing === "object" ? thing.type === "text" ? empty(thing.value) : false : empty(thing);
}
function empty(value) {
  return value.replace(re, "") === "";
}

// node_modules/property-information/lib/util/schema.js
var Schema = class {
  /**
   * @param {SchemaType['property']} property
   *   Property.
   * @param {SchemaType['normal']} normal
   *   Normal.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Schema.
   */
  constructor(property, normal, space2) {
    this.normal = normal;
    this.property = property;
    if (space2) {
      this.space = space2;
    }
  }
};
Schema.prototype.normal = {};
Schema.prototype.property = {};
Schema.prototype.space = void 0;

// node_modules/property-information/lib/util/merge.js
function merge(definitions, space2) {
  const property = {};
  const normal = {};
  for (const definition3 of definitions) {
    Object.assign(property, definition3.property);
    Object.assign(normal, definition3.normal);
  }
  return new Schema(property, normal, space2);
}

// node_modules/property-information/lib/normalize.js
function normalize(value) {
  return value.toLowerCase();
}

// node_modules/property-information/lib/util/info.js
var Info = class {
  /**
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @returns
   *   Info.
   */
  constructor(property, attribute) {
    this.attribute = attribute;
    this.property = property;
  }
};
Info.prototype.attribute = "";
Info.prototype.booleanish = false;
Info.prototype.boolean = false;
Info.prototype.commaOrSpaceSeparated = false;
Info.prototype.commaSeparated = false;
Info.prototype.defined = false;
Info.prototype.mustUseProperty = false;
Info.prototype.number = false;
Info.prototype.overloadedBoolean = false;
Info.prototype.property = "";
Info.prototype.spaceSeparated = false;
Info.prototype.space = void 0;

// node_modules/property-information/lib/util/types.js
var types_exports = {};
__export(types_exports, {
  boolean: () => boolean,
  booleanish: () => booleanish,
  commaOrSpaceSeparated: () => commaOrSpaceSeparated,
  commaSeparated: () => commaSeparated,
  number: () => number,
  overloadedBoolean: () => overloadedBoolean,
  spaceSeparated: () => spaceSeparated
});
var powers = 0;
var boolean = increment();
var booleanish = increment();
var overloadedBoolean = increment();
var number = increment();
var spaceSeparated = increment();
var commaSeparated = increment();
var commaOrSpaceSeparated = increment();
function increment() {
  return 2 ** ++powers;
}

// node_modules/property-information/lib/util/defined-info.js
var checks = (
  /** @type {ReadonlyArray<keyof typeof types>} */
  Object.keys(types_exports)
);
var DefinedInfo = class extends Info {
  /**
   * @constructor
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @param {number | null | undefined} [mask]
   *   Mask.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Info.
   */
  constructor(property, attribute, mask, space2) {
    let index6 = -1;
    super(property, attribute);
    mark(this, "space", space2);
    if (typeof mask === "number") {
      while (++index6 < checks.length) {
        const check = checks[index6];
        mark(this, checks[index6], (mask & types_exports[check]) === types_exports[check]);
      }
    }
  }
};
DefinedInfo.prototype.defined = true;
function mark(values2, key, value) {
  if (value) {
    values2[key] = value;
  }
}

// node_modules/property-information/lib/util/create.js
function create(definition3) {
  const properties = {};
  const normals = {};
  for (const [property, value] of Object.entries(definition3.properties)) {
    const info = new DefinedInfo(
      property,
      definition3.transform(definition3.attributes || {}, property),
      value,
      definition3.space
    );
    if (definition3.mustUseProperty && definition3.mustUseProperty.includes(property)) {
      info.mustUseProperty = true;
    }
    properties[property] = info;
    normals[normalize(property)] = property;
    normals[normalize(info.attribute)] = property;
  }
  return new Schema(properties, normals, definition3.space);
}

// node_modules/property-information/lib/aria.js
var aria = create({
  properties: {
    ariaActiveDescendant: null,
    ariaAtomic: booleanish,
    ariaAutoComplete: null,
    ariaBusy: booleanish,
    ariaChecked: booleanish,
    ariaColCount: number,
    ariaColIndex: number,
    ariaColSpan: number,
    ariaControls: spaceSeparated,
    ariaCurrent: null,
    ariaDescribedBy: spaceSeparated,
    ariaDetails: null,
    ariaDisabled: booleanish,
    ariaDropEffect: spaceSeparated,
    ariaErrorMessage: null,
    ariaExpanded: booleanish,
    ariaFlowTo: spaceSeparated,
    ariaGrabbed: booleanish,
    ariaHasPopup: null,
    ariaHidden: booleanish,
    ariaInvalid: null,
    ariaKeyShortcuts: null,
    ariaLabel: null,
    ariaLabelledBy: spaceSeparated,
    ariaLevel: number,
    ariaLive: null,
    ariaModal: booleanish,
    ariaMultiLine: booleanish,
    ariaMultiSelectable: booleanish,
    ariaOrientation: null,
    ariaOwns: spaceSeparated,
    ariaPlaceholder: null,
    ariaPosInSet: number,
    ariaPressed: booleanish,
    ariaReadOnly: booleanish,
    ariaRelevant: null,
    ariaRequired: booleanish,
    ariaRoleDescription: spaceSeparated,
    ariaRowCount: number,
    ariaRowIndex: number,
    ariaRowSpan: number,
    ariaSelected: booleanish,
    ariaSetSize: number,
    ariaSort: null,
    ariaValueMax: number,
    ariaValueMin: number,
    ariaValueNow: number,
    ariaValueText: null,
    role: null
  },
  transform(_2, property) {
    return property === "role" ? property : "aria-" + property.slice(4).toLowerCase();
  }
});

// node_modules/property-information/lib/util/case-sensitive-transform.js
function caseSensitiveTransform(attributes, attribute) {
  return attribute in attributes ? attributes[attribute] : attribute;
}

// node_modules/property-information/lib/util/case-insensitive-transform.js
function caseInsensitiveTransform(attributes, property) {
  return caseSensitiveTransform(attributes, property.toLowerCase());
}

// node_modules/property-information/lib/html.js
var html = create({
  attributes: {
    acceptcharset: "accept-charset",
    classname: "class",
    htmlfor: "for",
    httpequiv: "http-equiv"
  },
  mustUseProperty: ["checked", "multiple", "muted", "selected"],
  properties: {
    // Standard Properties.
    abbr: null,
    accept: commaSeparated,
    acceptCharset: spaceSeparated,
    accessKey: spaceSeparated,
    action: null,
    allow: null,
    allowFullScreen: boolean,
    allowPaymentRequest: boolean,
    allowUserMedia: boolean,
    alt: null,
    as: null,
    async: boolean,
    autoCapitalize: null,
    autoComplete: spaceSeparated,
    autoFocus: boolean,
    autoPlay: boolean,
    blocking: spaceSeparated,
    capture: null,
    charSet: null,
    checked: boolean,
    cite: null,
    className: spaceSeparated,
    cols: number,
    colSpan: null,
    content: null,
    contentEditable: booleanish,
    controls: boolean,
    controlsList: spaceSeparated,
    coords: number | commaSeparated,
    crossOrigin: null,
    data: null,
    dateTime: null,
    decoding: null,
    default: boolean,
    defer: boolean,
    dir: null,
    dirName: null,
    disabled: boolean,
    download: overloadedBoolean,
    draggable: booleanish,
    encType: null,
    enterKeyHint: null,
    fetchPriority: null,
    form: null,
    formAction: null,
    formEncType: null,
    formMethod: null,
    formNoValidate: boolean,
    formTarget: null,
    headers: spaceSeparated,
    height: number,
    hidden: overloadedBoolean,
    high: number,
    href: null,
    hrefLang: null,
    htmlFor: spaceSeparated,
    httpEquiv: spaceSeparated,
    id: null,
    imageSizes: null,
    imageSrcSet: null,
    inert: boolean,
    inputMode: null,
    integrity: null,
    is: null,
    isMap: boolean,
    itemId: null,
    itemProp: spaceSeparated,
    itemRef: spaceSeparated,
    itemScope: boolean,
    itemType: spaceSeparated,
    kind: null,
    label: null,
    lang: null,
    language: null,
    list: null,
    loading: null,
    loop: boolean,
    low: number,
    manifest: null,
    max: null,
    maxLength: number,
    media: null,
    method: null,
    min: null,
    minLength: number,
    multiple: boolean,
    muted: boolean,
    name: null,
    nonce: null,
    noModule: boolean,
    noValidate: boolean,
    onAbort: null,
    onAfterPrint: null,
    onAuxClick: null,
    onBeforeMatch: null,
    onBeforePrint: null,
    onBeforeToggle: null,
    onBeforeUnload: null,
    onBlur: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onContextLost: null,
    onContextMenu: null,
    onContextRestored: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFormData: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLanguageChange: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadEnd: null,
    onLoadStart: null,
    onMessage: null,
    onMessageError: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRejectionHandled: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onScrollEnd: null,
    onSecurityPolicyViolation: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onSlotChange: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnhandledRejection: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onWheel: null,
    open: boolean,
    optimum: number,
    pattern: null,
    ping: spaceSeparated,
    placeholder: null,
    playsInline: boolean,
    popover: null,
    popoverTarget: null,
    popoverTargetAction: null,
    poster: null,
    preload: null,
    readOnly: boolean,
    referrerPolicy: null,
    rel: spaceSeparated,
    required: boolean,
    reversed: boolean,
    rows: number,
    rowSpan: number,
    sandbox: spaceSeparated,
    scope: null,
    scoped: boolean,
    seamless: boolean,
    selected: boolean,
    shadowRootClonable: boolean,
    shadowRootDelegatesFocus: boolean,
    shadowRootMode: null,
    shape: null,
    size: number,
    sizes: null,
    slot: null,
    span: number,
    spellCheck: booleanish,
    src: null,
    srcDoc: null,
    srcLang: null,
    srcSet: null,
    start: number,
    step: null,
    style: null,
    tabIndex: number,
    target: null,
    title: null,
    translate: null,
    type: null,
    typeMustMatch: boolean,
    useMap: null,
    value: booleanish,
    width: number,
    wrap: null,
    writingSuggestions: null,
    // Legacy.
    // See: https://html.spec.whatwg.org/#other-elements,-attributes-and-apis
    align: null,
    // Several. Use CSS `text-align` instead,
    aLink: null,
    // `<body>`. Use CSS `a:active {color}` instead
    archive: spaceSeparated,
    // `<object>`. List of URIs to archives
    axis: null,
    // `<td>` and `<th>`. Use `scope` on `<th>`
    background: null,
    // `<body>`. Use CSS `background-image` instead
    bgColor: null,
    // `<body>` and table elements. Use CSS `background-color` instead
    border: number,
    // `<table>`. Use CSS `border-width` instead,
    borderColor: null,
    // `<table>`. Use CSS `border-color` instead,
    bottomMargin: number,
    // `<body>`
    cellPadding: null,
    // `<table>`
    cellSpacing: null,
    // `<table>`
    char: null,
    // Several table elements. When `align=char`, sets the character to align on
    charOff: null,
    // Several table elements. When `char`, offsets the alignment
    classId: null,
    // `<object>`
    clear: null,
    // `<br>`. Use CSS `clear` instead
    code: null,
    // `<object>`
    codeBase: null,
    // `<object>`
    codeType: null,
    // `<object>`
    color: null,
    // `<font>` and `<hr>`. Use CSS instead
    compact: boolean,
    // Lists. Use CSS to reduce space between items instead
    declare: boolean,
    // `<object>`
    event: null,
    // `<script>`
    face: null,
    // `<font>`. Use CSS instead
    frame: null,
    // `<table>`
    frameBorder: null,
    // `<iframe>`. Use CSS `border` instead
    hSpace: number,
    // `<img>` and `<object>`
    leftMargin: number,
    // `<body>`
    link: null,
    // `<body>`. Use CSS `a:link {color: *}` instead
    longDesc: null,
    // `<frame>`, `<iframe>`, and `<img>`. Use an `<a>`
    lowSrc: null,
    // `<img>`. Use a `<picture>`
    marginHeight: number,
    // `<body>`
    marginWidth: number,
    // `<body>`
    noResize: boolean,
    // `<frame>`
    noHref: boolean,
    // `<area>`. Use no href instead of an explicit `nohref`
    noShade: boolean,
    // `<hr>`. Use background-color and height instead of borders
    noWrap: boolean,
    // `<td>` and `<th>`
    object: null,
    // `<applet>`
    profile: null,
    // `<head>`
    prompt: null,
    // `<isindex>`
    rev: null,
    // `<link>`
    rightMargin: number,
    // `<body>`
    rules: null,
    // `<table>`
    scheme: null,
    // `<meta>`
    scrolling: booleanish,
    // `<frame>`. Use overflow in the child context
    standby: null,
    // `<object>`
    summary: null,
    // `<table>`
    text: null,
    // `<body>`. Use CSS `color` instead
    topMargin: number,
    // `<body>`
    valueType: null,
    // `<param>`
    version: null,
    // `<html>`. Use a doctype.
    vAlign: null,
    // Several. Use CSS `vertical-align` instead
    vLink: null,
    // `<body>`. Use CSS `a:visited {color}` instead
    vSpace: number,
    // `<img>` and `<object>`
    // Non-standard Properties.
    allowTransparency: null,
    autoCorrect: null,
    autoSave: null,
    disablePictureInPicture: boolean,
    disableRemotePlayback: boolean,
    prefix: null,
    property: null,
    results: number,
    security: null,
    unselectable: null
  },
  space: "html",
  transform: caseInsensitiveTransform
});

// node_modules/property-information/lib/svg.js
var svg = create({
  attributes: {
    accentHeight: "accent-height",
    alignmentBaseline: "alignment-baseline",
    arabicForm: "arabic-form",
    baselineShift: "baseline-shift",
    capHeight: "cap-height",
    className: "class",
    clipPath: "clip-path",
    clipRule: "clip-rule",
    colorInterpolation: "color-interpolation",
    colorInterpolationFilters: "color-interpolation-filters",
    colorProfile: "color-profile",
    colorRendering: "color-rendering",
    crossOrigin: "crossorigin",
    dataType: "datatype",
    dominantBaseline: "dominant-baseline",
    enableBackground: "enable-background",
    fillOpacity: "fill-opacity",
    fillRule: "fill-rule",
    floodColor: "flood-color",
    floodOpacity: "flood-opacity",
    fontFamily: "font-family",
    fontSize: "font-size",
    fontSizeAdjust: "font-size-adjust",
    fontStretch: "font-stretch",
    fontStyle: "font-style",
    fontVariant: "font-variant",
    fontWeight: "font-weight",
    glyphName: "glyph-name",
    glyphOrientationHorizontal: "glyph-orientation-horizontal",
    glyphOrientationVertical: "glyph-orientation-vertical",
    hrefLang: "hreflang",
    horizAdvX: "horiz-adv-x",
    horizOriginX: "horiz-origin-x",
    horizOriginY: "horiz-origin-y",
    imageRendering: "image-rendering",
    letterSpacing: "letter-spacing",
    lightingColor: "lighting-color",
    markerEnd: "marker-end",
    markerMid: "marker-mid",
    markerStart: "marker-start",
    navDown: "nav-down",
    navDownLeft: "nav-down-left",
    navDownRight: "nav-down-right",
    navLeft: "nav-left",
    navNext: "nav-next",
    navPrev: "nav-prev",
    navRight: "nav-right",
    navUp: "nav-up",
    navUpLeft: "nav-up-left",
    navUpRight: "nav-up-right",
    onAbort: "onabort",
    onActivate: "onactivate",
    onAfterPrint: "onafterprint",
    onBeforePrint: "onbeforeprint",
    onBegin: "onbegin",
    onCancel: "oncancel",
    onCanPlay: "oncanplay",
    onCanPlayThrough: "oncanplaythrough",
    onChange: "onchange",
    onClick: "onclick",
    onClose: "onclose",
    onCopy: "oncopy",
    onCueChange: "oncuechange",
    onCut: "oncut",
    onDblClick: "ondblclick",
    onDrag: "ondrag",
    onDragEnd: "ondragend",
    onDragEnter: "ondragenter",
    onDragExit: "ondragexit",
    onDragLeave: "ondragleave",
    onDragOver: "ondragover",
    onDragStart: "ondragstart",
    onDrop: "ondrop",
    onDurationChange: "ondurationchange",
    onEmptied: "onemptied",
    onEnd: "onend",
    onEnded: "onended",
    onError: "onerror",
    onFocus: "onfocus",
    onFocusIn: "onfocusin",
    onFocusOut: "onfocusout",
    onHashChange: "onhashchange",
    onInput: "oninput",
    onInvalid: "oninvalid",
    onKeyDown: "onkeydown",
    onKeyPress: "onkeypress",
    onKeyUp: "onkeyup",
    onLoad: "onload",
    onLoadedData: "onloadeddata",
    onLoadedMetadata: "onloadedmetadata",
    onLoadStart: "onloadstart",
    onMessage: "onmessage",
    onMouseDown: "onmousedown",
    onMouseEnter: "onmouseenter",
    onMouseLeave: "onmouseleave",
    onMouseMove: "onmousemove",
    onMouseOut: "onmouseout",
    onMouseOver: "onmouseover",
    onMouseUp: "onmouseup",
    onMouseWheel: "onmousewheel",
    onOffline: "onoffline",
    onOnline: "ononline",
    onPageHide: "onpagehide",
    onPageShow: "onpageshow",
    onPaste: "onpaste",
    onPause: "onpause",
    onPlay: "onplay",
    onPlaying: "onplaying",
    onPopState: "onpopstate",
    onProgress: "onprogress",
    onRateChange: "onratechange",
    onRepeat: "onrepeat",
    onReset: "onreset",
    onResize: "onresize",
    onScroll: "onscroll",
    onSeeked: "onseeked",
    onSeeking: "onseeking",
    onSelect: "onselect",
    onShow: "onshow",
    onStalled: "onstalled",
    onStorage: "onstorage",
    onSubmit: "onsubmit",
    onSuspend: "onsuspend",
    onTimeUpdate: "ontimeupdate",
    onToggle: "ontoggle",
    onUnload: "onunload",
    onVolumeChange: "onvolumechange",
    onWaiting: "onwaiting",
    onZoom: "onzoom",
    overlinePosition: "overline-position",
    overlineThickness: "overline-thickness",
    paintOrder: "paint-order",
    panose1: "panose-1",
    pointerEvents: "pointer-events",
    referrerPolicy: "referrerpolicy",
    renderingIntent: "rendering-intent",
    shapeRendering: "shape-rendering",
    stopColor: "stop-color",
    stopOpacity: "stop-opacity",
    strikethroughPosition: "strikethrough-position",
    strikethroughThickness: "strikethrough-thickness",
    strokeDashArray: "stroke-dasharray",
    strokeDashOffset: "stroke-dashoffset",
    strokeLineCap: "stroke-linecap",
    strokeLineJoin: "stroke-linejoin",
    strokeMiterLimit: "stroke-miterlimit",
    strokeOpacity: "stroke-opacity",
    strokeWidth: "stroke-width",
    tabIndex: "tabindex",
    textAnchor: "text-anchor",
    textDecoration: "text-decoration",
    textRendering: "text-rendering",
    transformOrigin: "transform-origin",
    typeOf: "typeof",
    underlinePosition: "underline-position",
    underlineThickness: "underline-thickness",
    unicodeBidi: "unicode-bidi",
    unicodeRange: "unicode-range",
    unitsPerEm: "units-per-em",
    vAlphabetic: "v-alphabetic",
    vHanging: "v-hanging",
    vIdeographic: "v-ideographic",
    vMathematical: "v-mathematical",
    vectorEffect: "vector-effect",
    vertAdvY: "vert-adv-y",
    vertOriginX: "vert-origin-x",
    vertOriginY: "vert-origin-y",
    wordSpacing: "word-spacing",
    writingMode: "writing-mode",
    xHeight: "x-height",
    // These were camelcased in Tiny. Now lowercased in SVG 2
    playbackOrder: "playbackorder",
    timelineBegin: "timelinebegin"
  },
  properties: {
    about: commaOrSpaceSeparated,
    accentHeight: number,
    accumulate: null,
    additive: null,
    alignmentBaseline: null,
    alphabetic: number,
    amplitude: number,
    arabicForm: null,
    ascent: number,
    attributeName: null,
    attributeType: null,
    azimuth: number,
    bandwidth: null,
    baselineShift: null,
    baseFrequency: null,
    baseProfile: null,
    bbox: null,
    begin: null,
    bias: number,
    by: null,
    calcMode: null,
    capHeight: number,
    className: spaceSeparated,
    clip: null,
    clipPath: null,
    clipPathUnits: null,
    clipRule: null,
    color: null,
    colorInterpolation: null,
    colorInterpolationFilters: null,
    colorProfile: null,
    colorRendering: null,
    content: null,
    contentScriptType: null,
    contentStyleType: null,
    crossOrigin: null,
    cursor: null,
    cx: null,
    cy: null,
    d: null,
    dataType: null,
    defaultAction: null,
    descent: number,
    diffuseConstant: number,
    direction: null,
    display: null,
    dur: null,
    divisor: number,
    dominantBaseline: null,
    download: boolean,
    dx: null,
    dy: null,
    edgeMode: null,
    editable: null,
    elevation: number,
    enableBackground: null,
    end: null,
    event: null,
    exponent: number,
    externalResourcesRequired: null,
    fill: null,
    fillOpacity: number,
    fillRule: null,
    filter: null,
    filterRes: null,
    filterUnits: null,
    floodColor: null,
    floodOpacity: null,
    focusable: null,
    focusHighlight: null,
    fontFamily: null,
    fontSize: null,
    fontSizeAdjust: null,
    fontStretch: null,
    fontStyle: null,
    fontVariant: null,
    fontWeight: null,
    format: null,
    fr: null,
    from: null,
    fx: null,
    fy: null,
    g1: commaSeparated,
    g2: commaSeparated,
    glyphName: commaSeparated,
    glyphOrientationHorizontal: null,
    glyphOrientationVertical: null,
    glyphRef: null,
    gradientTransform: null,
    gradientUnits: null,
    handler: null,
    hanging: number,
    hatchContentUnits: null,
    hatchUnits: null,
    height: null,
    href: null,
    hrefLang: null,
    horizAdvX: number,
    horizOriginX: number,
    horizOriginY: number,
    id: null,
    ideographic: number,
    imageRendering: null,
    initialVisibility: null,
    in: null,
    in2: null,
    intercept: number,
    k: number,
    k1: number,
    k2: number,
    k3: number,
    k4: number,
    kernelMatrix: commaOrSpaceSeparated,
    kernelUnitLength: null,
    keyPoints: null,
    // SEMI_COLON_SEPARATED
    keySplines: null,
    // SEMI_COLON_SEPARATED
    keyTimes: null,
    // SEMI_COLON_SEPARATED
    kerning: null,
    lang: null,
    lengthAdjust: null,
    letterSpacing: null,
    lightingColor: null,
    limitingConeAngle: number,
    local: null,
    markerEnd: null,
    markerMid: null,
    markerStart: null,
    markerHeight: null,
    markerUnits: null,
    markerWidth: null,
    mask: null,
    maskContentUnits: null,
    maskUnits: null,
    mathematical: null,
    max: null,
    media: null,
    mediaCharacterEncoding: null,
    mediaContentEncodings: null,
    mediaSize: number,
    mediaTime: null,
    method: null,
    min: null,
    mode: null,
    name: null,
    navDown: null,
    navDownLeft: null,
    navDownRight: null,
    navLeft: null,
    navNext: null,
    navPrev: null,
    navRight: null,
    navUp: null,
    navUpLeft: null,
    navUpRight: null,
    numOctaves: null,
    observer: null,
    offset: null,
    onAbort: null,
    onActivate: null,
    onAfterPrint: null,
    onBeforePrint: null,
    onBegin: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnd: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFocusIn: null,
    onFocusOut: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadStart: null,
    onMessage: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onMouseWheel: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRepeat: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onShow: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onZoom: null,
    opacity: null,
    operator: null,
    order: null,
    orient: null,
    orientation: null,
    origin: null,
    overflow: null,
    overlay: null,
    overlinePosition: number,
    overlineThickness: number,
    paintOrder: null,
    panose1: null,
    path: null,
    pathLength: number,
    patternContentUnits: null,
    patternTransform: null,
    patternUnits: null,
    phase: null,
    ping: spaceSeparated,
    pitch: null,
    playbackOrder: null,
    pointerEvents: null,
    points: null,
    pointsAtX: number,
    pointsAtY: number,
    pointsAtZ: number,
    preserveAlpha: null,
    preserveAspectRatio: null,
    primitiveUnits: null,
    propagate: null,
    property: commaOrSpaceSeparated,
    r: null,
    radius: null,
    referrerPolicy: null,
    refX: null,
    refY: null,
    rel: commaOrSpaceSeparated,
    rev: commaOrSpaceSeparated,
    renderingIntent: null,
    repeatCount: null,
    repeatDur: null,
    requiredExtensions: commaOrSpaceSeparated,
    requiredFeatures: commaOrSpaceSeparated,
    requiredFonts: commaOrSpaceSeparated,
    requiredFormats: commaOrSpaceSeparated,
    resource: null,
    restart: null,
    result: null,
    rotate: null,
    rx: null,
    ry: null,
    scale: null,
    seed: null,
    shapeRendering: null,
    side: null,
    slope: null,
    snapshotTime: null,
    specularConstant: number,
    specularExponent: number,
    spreadMethod: null,
    spacing: null,
    startOffset: null,
    stdDeviation: null,
    stemh: null,
    stemv: null,
    stitchTiles: null,
    stopColor: null,
    stopOpacity: null,
    strikethroughPosition: number,
    strikethroughThickness: number,
    string: null,
    stroke: null,
    strokeDashArray: commaOrSpaceSeparated,
    strokeDashOffset: null,
    strokeLineCap: null,
    strokeLineJoin: null,
    strokeMiterLimit: number,
    strokeOpacity: number,
    strokeWidth: null,
    style: null,
    surfaceScale: number,
    syncBehavior: null,
    syncBehaviorDefault: null,
    syncMaster: null,
    syncTolerance: null,
    syncToleranceDefault: null,
    systemLanguage: commaOrSpaceSeparated,
    tabIndex: number,
    tableValues: null,
    target: null,
    targetX: number,
    targetY: number,
    textAnchor: null,
    textDecoration: null,
    textRendering: null,
    textLength: null,
    timelineBegin: null,
    title: null,
    transformBehavior: null,
    type: null,
    typeOf: commaOrSpaceSeparated,
    to: null,
    transform: null,
    transformOrigin: null,
    u1: null,
    u2: null,
    underlinePosition: number,
    underlineThickness: number,
    unicode: null,
    unicodeBidi: null,
    unicodeRange: null,
    unitsPerEm: number,
    values: null,
    vAlphabetic: number,
    vMathematical: number,
    vectorEffect: null,
    vHanging: number,
    vIdeographic: number,
    version: null,
    vertAdvY: number,
    vertOriginX: number,
    vertOriginY: number,
    viewBox: null,
    viewTarget: null,
    visibility: null,
    width: null,
    widths: null,
    wordSpacing: null,
    writingMode: null,
    x: null,
    x1: null,
    x2: null,
    xChannelSelector: null,
    xHeight: number,
    y: null,
    y1: null,
    y2: null,
    yChannelSelector: null,
    z: null,
    zoomAndPan: null
  },
  space: "svg",
  transform: caseSensitiveTransform
});

// node_modules/property-information/lib/xlink.js
var xlink = create({
  properties: {
    xLinkActuate: null,
    xLinkArcRole: null,
    xLinkHref: null,
    xLinkRole: null,
    xLinkShow: null,
    xLinkTitle: null,
    xLinkType: null
  },
  space: "xlink",
  transform(_2, property) {
    return "xlink:" + property.slice(5).toLowerCase();
  }
});

// node_modules/property-information/lib/xmlns.js
var xmlns = create({
  attributes: { xmlnsxlink: "xmlns:xlink" },
  properties: { xmlnsXLink: null, xmlns: null },
  space: "xmlns",
  transform: caseInsensitiveTransform
});

// node_modules/property-information/lib/xml.js
var xml = create({
  properties: { xmlBase: null, xmlLang: null, xmlSpace: null },
  space: "xml",
  transform(_2, property) {
    return "xml:" + property.slice(3).toLowerCase();
  }
});

// node_modules/property-information/lib/hast-to-react.js
var hastToReact = {
  classId: "classID",
  dataType: "datatype",
  itemId: "itemID",
  strokeDashArray: "strokeDasharray",
  strokeDashOffset: "strokeDashoffset",
  strokeLineCap: "strokeLinecap",
  strokeLineJoin: "strokeLinejoin",
  strokeMiterLimit: "strokeMiterlimit",
  typeOf: "typeof",
  xLinkActuate: "xlinkActuate",
  xLinkArcRole: "xlinkArcrole",
  xLinkHref: "xlinkHref",
  xLinkRole: "xlinkRole",
  xLinkShow: "xlinkShow",
  xLinkTitle: "xlinkTitle",
  xLinkType: "xlinkType",
  xmlnsXLink: "xmlnsXlink"
};

// node_modules/property-information/lib/find.js
var cap = /[A-Z]/g;
var dash = /-[a-z]/g;
var valid = /^data[-\w.:]+$/i;
function find2(schema, value) {
  const normal = normalize(value);
  let property = value;
  let Type = Info;
  if (normal in schema.normal) {
    return schema.property[schema.normal[normal]];
  }
  if (normal.length > 4 && normal.slice(0, 4) === "data" && valid.test(value)) {
    if (value.charAt(4) === "-") {
      const rest = value.slice(5).replace(dash, camelcase);
      property = "data" + rest.charAt(0).toUpperCase() + rest.slice(1);
    } else {
      const rest = value.slice(4);
      if (!dash.test(rest)) {
        let dashes = rest.replace(cap, kebab);
        if (dashes.charAt(0) !== "-") {
          dashes = "-" + dashes;
        }
        value = "data" + dashes;
      }
    }
    Type = DefinedInfo;
  }
  return new Type(property, value);
}
function kebab($0) {
  return "-" + $0.toLowerCase();
}
function camelcase($0) {
  return $0.charAt(1).toUpperCase();
}

// node_modules/property-information/index.js
var html2 = merge([aria, html, xlink, xmlns, xml], "html");
var svg2 = merge([aria, svg, xlink, xmlns, xml], "svg");

// node_modules/space-separated-tokens/index.js
function stringify2(values2) {
  return values2.join(" ").trim();
}

// node_modules/hast-util-to-jsx-runtime/lib/index.js
var import_style_to_js = __toESM(require_cjs4(), 1);

// node_modules/unist-util-position/lib/index.js
var pointEnd = point("end");
var pointStart = point("start");
function point(type) {
  return point4;
  function point4(node2) {
    const point5 = node2 && node2.position && node2.position[type] || {};
    if (typeof point5.line === "number" && point5.line > 0 && typeof point5.column === "number" && point5.column > 0) {
      return {
        line: point5.line,
        column: point5.column,
        offset: typeof point5.offset === "number" && point5.offset > -1 ? point5.offset : void 0
      };
    }
  }
}
function position(node2) {
  const start2 = pointStart(node2);
  const end = pointEnd(node2);
  if (start2 && end) {
    return { start: start2, end };
  }
}

// node_modules/unist-util-stringify-position/lib/index.js
function stringifyPosition(value) {
  if (!value || typeof value !== "object") {
    return "";
  }
  if ("position" in value || "type" in value) {
    return position2(value.position);
  }
  if ("start" in value || "end" in value) {
    return position2(value);
  }
  if ("line" in value || "column" in value) {
    return point2(value);
  }
  return "";
}
function point2(point4) {
  return index3(point4 && point4.line) + ":" + index3(point4 && point4.column);
}
function position2(pos) {
  return point2(pos && pos.start) + "-" + point2(pos && pos.end);
}
function index3(value) {
  return value && typeof value === "number" ? value : 1;
}

// node_modules/vfile-message/lib/index.js
var VFileMessage = class extends Error {
  /**
   * Create a message for `reason`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {Options | null | undefined} [options]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | Options | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns
   *   Instance of `VFileMessage`.
   */
  // eslint-disable-next-line complexity
  constructor(causeOrReason, optionsOrParentOrPlace, origin) {
    super();
    if (typeof optionsOrParentOrPlace === "string") {
      origin = optionsOrParentOrPlace;
      optionsOrParentOrPlace = void 0;
    }
    let reason = "";
    let options = {};
    let legacyCause = false;
    if (optionsOrParentOrPlace) {
      if ("line" in optionsOrParentOrPlace && "column" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("start" in optionsOrParentOrPlace && "end" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("type" in optionsOrParentOrPlace) {
        options = {
          ancestors: [optionsOrParentOrPlace],
          place: optionsOrParentOrPlace.position
        };
      } else {
        options = { ...optionsOrParentOrPlace };
      }
    }
    if (typeof causeOrReason === "string") {
      reason = causeOrReason;
    } else if (!options.cause && causeOrReason) {
      legacyCause = true;
      reason = causeOrReason.message;
      options.cause = causeOrReason;
    }
    if (!options.ruleId && !options.source && typeof origin === "string") {
      const index6 = origin.indexOf(":");
      if (index6 === -1) {
        options.ruleId = origin;
      } else {
        options.source = origin.slice(0, index6);
        options.ruleId = origin.slice(index6 + 1);
      }
    }
    if (!options.place && options.ancestors && options.ancestors) {
      const parent = options.ancestors[options.ancestors.length - 1];
      if (parent) {
        options.place = parent.position;
      }
    }
    const start2 = options.place && "start" in options.place ? options.place.start : options.place;
    this.ancestors = options.ancestors || void 0;
    this.cause = options.cause || void 0;
    this.column = start2 ? start2.column : void 0;
    this.fatal = void 0;
    this.file = "";
    this.message = reason;
    this.line = start2 ? start2.line : void 0;
    this.name = stringifyPosition(options.place) || "1:1";
    this.place = options.place || void 0;
    this.reason = this.message;
    this.ruleId = options.ruleId || void 0;
    this.source = options.source || void 0;
    this.stack = legacyCause && options.cause && typeof options.cause.stack === "string" ? options.cause.stack : "";
    this.actual = void 0;
    this.expected = void 0;
    this.note = void 0;
    this.url = void 0;
  }
};
VFileMessage.prototype.file = "";
VFileMessage.prototype.name = "";
VFileMessage.prototype.reason = "";
VFileMessage.prototype.message = "";
VFileMessage.prototype.stack = "";
VFileMessage.prototype.column = void 0;
VFileMessage.prototype.line = void 0;
VFileMessage.prototype.ancestors = void 0;
VFileMessage.prototype.cause = void 0;
VFileMessage.prototype.fatal = void 0;
VFileMessage.prototype.place = void 0;
VFileMessage.prototype.ruleId = void 0;
VFileMessage.prototype.source = void 0;

// node_modules/hast-util-to-jsx-runtime/lib/index.js
var own = {}.hasOwnProperty;
var emptyMap = /* @__PURE__ */ new Map();
var cap2 = /[A-Z]/g;
var tableElements = /* @__PURE__ */ new Set(["table", "tbody", "thead", "tfoot", "tr"]);
var tableCellElement = /* @__PURE__ */ new Set(["td", "th"]);
var docs = "https://github.com/syntax-tree/hast-util-to-jsx-runtime";
function toJsxRuntime(tree, options) {
  if (!options || options.Fragment === void 0) {
    throw new TypeError("Expected `Fragment` in options");
  }
  const filePath = options.filePath || void 0;
  let create2;
  if (options.development) {
    if (typeof options.jsxDEV !== "function") {
      throw new TypeError(
        "Expected `jsxDEV` in options when `development: true`"
      );
    }
    create2 = developmentCreate(filePath, options.jsxDEV);
  } else {
    if (typeof options.jsx !== "function") {
      throw new TypeError("Expected `jsx` in production options");
    }
    if (typeof options.jsxs !== "function") {
      throw new TypeError("Expected `jsxs` in production options");
    }
    create2 = productionCreate(filePath, options.jsx, options.jsxs);
  }
  const state = {
    Fragment: options.Fragment,
    ancestors: [],
    components: options.components || {},
    create: create2,
    elementAttributeNameCase: options.elementAttributeNameCase || "react",
    evaluater: options.createEvaluater ? options.createEvaluater() : void 0,
    filePath,
    ignoreInvalidStyle: options.ignoreInvalidStyle || false,
    passKeys: options.passKeys !== false,
    passNode: options.passNode || false,
    schema: options.space === "svg" ? svg2 : html2,
    stylePropertyNameCase: options.stylePropertyNameCase || "dom",
    tableCellAlignToStyle: options.tableCellAlignToStyle !== false
  };
  const result = one(state, tree, void 0);
  if (result && typeof result !== "string") {
    return result;
  }
  return state.create(
    tree,
    state.Fragment,
    { children: result || void 0 },
    void 0
  );
}
function one(state, node2, key) {
  if (node2.type === "element") {
    return element(state, node2, key);
  }
  if (node2.type === "mdxFlowExpression" || node2.type === "mdxTextExpression") {
    return mdxExpression(state, node2);
  }
  if (node2.type === "mdxJsxFlowElement" || node2.type === "mdxJsxTextElement") {
    return mdxJsxElement(state, node2, key);
  }
  if (node2.type === "mdxjsEsm") {
    return mdxEsm(state, node2);
  }
  if (node2.type === "root") {
    return root(state, node2, key);
  }
  if (node2.type === "text") {
    return text(state, node2);
  }
}
function element(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.tagName.toLowerCase() === "svg" && parentSchema.space === "html") {
    schema = svg2;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = findComponentFromName(state, node2.tagName, false);
  const props = createElementProps(state, node2);
  let children = createChildren(state, node2);
  if (tableElements.has(node2.tagName)) {
    children = children.filter(function(child) {
      return typeof child === "string" ? !whitespace(child) : true;
    });
  }
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function mdxExpression(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    const program = node2.data.estree;
    const expression = program.body[0];
    ok(expression.type === "ExpressionStatement");
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateExpression(expression.expression)
    );
  }
  crashEstree(state, node2.position);
}
function mdxEsm(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateProgram(node2.data.estree)
    );
  }
  crashEstree(state, node2.position);
}
function mdxJsxElement(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.name === "svg" && parentSchema.space === "html") {
    schema = svg2;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = node2.name === null ? state.Fragment : findComponentFromName(state, node2.name, true);
  const props = createJsxElementProps(state, node2);
  const children = createChildren(state, node2);
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function root(state, node2, key) {
  const props = {};
  addChildren(props, createChildren(state, node2));
  return state.create(node2, state.Fragment, props, key);
}
function text(_2, node2) {
  return node2.value;
}
function addNode(state, props, type, node2) {
  if (typeof type !== "string" && type !== state.Fragment && state.passNode) {
    props.node = node2;
  }
}
function addChildren(props, children) {
  if (children.length > 0) {
    const value = children.length > 1 ? children : children[0];
    if (value) {
      props.children = value;
    }
  }
}
function productionCreate(_2, jsx5, jsxs5) {
  return create2;
  function create2(_3, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const fn = isStaticChildren ? jsxs5 : jsx5;
    return key ? fn(type, props, key) : fn(type, props);
  }
}
function developmentCreate(filePath, jsxDEV) {
  return create2;
  function create2(node2, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const point4 = pointStart(node2);
    return jsxDEV(
      type,
      props,
      key,
      isStaticChildren,
      {
        columnNumber: point4 ? point4.column - 1 : void 0,
        fileName: filePath,
        lineNumber: point4 ? point4.line : void 0
      },
      void 0
    );
  }
}
function createElementProps(state, node2) {
  const props = {};
  let alignValue;
  let prop;
  for (prop in node2.properties) {
    if (prop !== "children" && own.call(node2.properties, prop)) {
      const result = createProperty(state, prop, node2.properties[prop]);
      if (result) {
        const [key, value] = result;
        if (state.tableCellAlignToStyle && key === "align" && typeof value === "string" && tableCellElement.has(node2.tagName)) {
          alignValue = value;
        } else {
          props[key] = value;
        }
      }
    }
  }
  if (alignValue) {
    const style = (
      /** @type {Style} */
      props.style || (props.style = {})
    );
    style[state.stylePropertyNameCase === "css" ? "text-align" : "textAlign"] = alignValue;
  }
  return props;
}
function createJsxElementProps(state, node2) {
  const props = {};
  for (const attribute of node2.attributes) {
    if (attribute.type === "mdxJsxExpressionAttribute") {
      if (attribute.data && attribute.data.estree && state.evaluater) {
        const program = attribute.data.estree;
        const expression = program.body[0];
        ok(expression.type === "ExpressionStatement");
        const objectExpression = expression.expression;
        ok(objectExpression.type === "ObjectExpression");
        const property = objectExpression.properties[0];
        ok(property.type === "SpreadElement");
        Object.assign(
          props,
          state.evaluater.evaluateExpression(property.argument)
        );
      } else {
        crashEstree(state, node2.position);
      }
    } else {
      const name2 = attribute.name;
      let value;
      if (attribute.value && typeof attribute.value === "object") {
        if (attribute.value.data && attribute.value.data.estree && state.evaluater) {
          const program = attribute.value.data.estree;
          const expression = program.body[0];
          ok(expression.type === "ExpressionStatement");
          value = state.evaluater.evaluateExpression(expression.expression);
        } else {
          crashEstree(state, node2.position);
        }
      } else {
        value = attribute.value === null ? true : attribute.value;
      }
      props[name2] = /** @type {Props[keyof Props]} */
      value;
    }
  }
  return props;
}
function createChildren(state, node2) {
  const children = [];
  let index6 = -1;
  const countsByName = state.passKeys ? /* @__PURE__ */ new Map() : emptyMap;
  while (++index6 < node2.children.length) {
    const child = node2.children[index6];
    let key;
    if (state.passKeys) {
      const name2 = child.type === "element" ? child.tagName : child.type === "mdxJsxFlowElement" || child.type === "mdxJsxTextElement" ? child.name : void 0;
      if (name2) {
        const count2 = countsByName.get(name2) || 0;
        key = name2 + "-" + count2;
        countsByName.set(name2, count2 + 1);
      }
    }
    const result = one(state, child, key);
    if (result !== void 0) children.push(result);
  }
  return children;
}
function createProperty(state, prop, value) {
  const info = find2(state.schema, prop);
  if (value === null || value === void 0 || typeof value === "number" && Number.isNaN(value)) {
    return;
  }
  if (Array.isArray(value)) {
    value = info.commaSeparated ? stringify(value) : stringify2(value);
  }
  if (info.property === "style") {
    let styleObject = typeof value === "object" ? value : parseStyle(state, String(value));
    if (state.stylePropertyNameCase === "css") {
      styleObject = transformStylesToCssCasing(styleObject);
    }
    return ["style", styleObject];
  }
  return [
    state.elementAttributeNameCase === "react" && info.space ? hastToReact[info.property] || info.property : info.attribute,
    value
  ];
}
function parseStyle(state, value) {
  try {
    return (0, import_style_to_js.default)(value, { reactCompat: true });
  } catch (error2) {
    if (state.ignoreInvalidStyle) {
      return {};
    }
    const cause = (
      /** @type {Error} */
      error2
    );
    const message = new VFileMessage("Cannot parse `style` attribute", {
      ancestors: state.ancestors,
      cause,
      ruleId: "style",
      source: "hast-util-to-jsx-runtime"
    });
    message.file = state.filePath || void 0;
    message.url = docs + "#cannot-parse-style-attribute";
    throw message;
  }
}
function findComponentFromName(state, name2, allowExpression) {
  let result;
  if (!allowExpression) {
    result = { type: "Literal", value: name2 };
  } else if (name2.includes(".")) {
    const identifiers = name2.split(".");
    let index6 = -1;
    let node2;
    while (++index6 < identifiers.length) {
      const prop = name(identifiers[index6]) ? { type: "Identifier", name: identifiers[index6] } : { type: "Literal", value: identifiers[index6] };
      node2 = node2 ? {
        type: "MemberExpression",
        object: node2,
        property: prop,
        computed: Boolean(index6 && prop.type === "Literal"),
        optional: false
      } : prop;
    }
    ok(node2, "always a result");
    result = node2;
  } else {
    result = name(name2) && !/^[a-z]/.test(name2) ? { type: "Identifier", name: name2 } : { type: "Literal", value: name2 };
  }
  if (result.type === "Literal") {
    const name3 = (
      /** @type {string | number} */
      result.value
    );
    return own.call(state.components, name3) ? state.components[name3] : name3;
  }
  if (state.evaluater) {
    return state.evaluater.evaluateExpression(result);
  }
  crashEstree(state);
}
function crashEstree(state, place) {
  const message = new VFileMessage(
    "Cannot handle MDX estrees without `createEvaluater`",
    {
      ancestors: state.ancestors,
      place,
      ruleId: "mdx-estree",
      source: "hast-util-to-jsx-runtime"
    }
  );
  message.file = state.filePath || void 0;
  message.url = docs + "#cannot-handle-mdx-estrees-without-createevaluater";
  throw message;
}
function transformStylesToCssCasing(domCasing) {
  const cssCasing = {};
  let from;
  for (from in domCasing) {
    if (own.call(domCasing, from)) {
      cssCasing[transformStyleToCssCasing(from)] = domCasing[from];
    }
  }
  return cssCasing;
}
function transformStyleToCssCasing(from) {
  let to = from.replace(cap2, toDash);
  if (to.slice(0, 3) === "ms-") to = "-" + to;
  return to;
}
function toDash($0) {
  return "-" + $0.toLowerCase();
}

// node_modules/html-url-attributes/lib/index.js
var urlAttributes = {
  action: ["form"],
  cite: ["blockquote", "del", "ins", "q"],
  data: ["object"],
  formAction: ["button", "input"],
  href: ["a", "area", "base", "link"],
  icon: ["menuitem"],
  itemId: null,
  manifest: ["html"],
  ping: ["a", "area"],
  poster: ["video"],
  src: [
    "audio",
    "embed",
    "iframe",
    "img",
    "input",
    "script",
    "source",
    "track",
    "video"
  ]
};

// node_modules/react-markdown/lib/index.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_react22 = __toESM(require_react(), 1);

// node_modules/mdast-util-to-string/lib/index.js
var emptyOptions2 = {};
function toString(value, options) {
  const settings = options || emptyOptions2;
  const includeImageAlt = typeof settings.includeImageAlt === "boolean" ? settings.includeImageAlt : true;
  const includeHtml = typeof settings.includeHtml === "boolean" ? settings.includeHtml : true;
  return one2(value, includeImageAlt, includeHtml);
}
function one2(value, includeImageAlt, includeHtml) {
  if (node(value)) {
    if ("value" in value) {
      return value.type === "html" && !includeHtml ? "" : value.value;
    }
    if (includeImageAlt && "alt" in value && value.alt) {
      return value.alt;
    }
    if ("children" in value) {
      return all(value.children, includeImageAlt, includeHtml);
    }
  }
  if (Array.isArray(value)) {
    return all(value, includeImageAlt, includeHtml);
  }
  return "";
}
function all(values2, includeImageAlt, includeHtml) {
  const result = [];
  let index6 = -1;
  while (++index6 < values2.length) {
    result[index6] = one2(values2[index6], includeImageAlt, includeHtml);
  }
  return result.join("");
}
function node(value) {
  return Boolean(value && typeof value === "object");
}

// node_modules/decode-named-character-reference/index.dom.js
var element2 = document.createElement("i");
function decodeNamedCharacterReference(value) {
  const characterReference2 = "&" + value + ";";
  element2.innerHTML = characterReference2;
  const character = element2.textContent;
  if (character.charCodeAt(character.length - 1) === 59 && value !== "semi") {
    return false;
  }
  return character === characterReference2 ? false : character;
}

// node_modules/micromark-util-symbol/lib/codes.js
var codes = (
  /** @type {const} */
  {
    carriageReturn: -5,
    lineFeed: -4,
    carriageReturnLineFeed: -3,
    horizontalTab: -2,
    virtualSpace: -1,
    eof: null,
    nul: 0,
    soh: 1,
    stx: 2,
    etx: 3,
    eot: 4,
    enq: 5,
    ack: 6,
    bel: 7,
    bs: 8,
    ht: 9,
    // `\t`
    lf: 10,
    // `\n`
    vt: 11,
    // `\v`
    ff: 12,
    // `\f`
    cr: 13,
    // `\r`
    so: 14,
    si: 15,
    dle: 16,
    dc1: 17,
    dc2: 18,
    dc3: 19,
    dc4: 20,
    nak: 21,
    syn: 22,
    etb: 23,
    can: 24,
    em: 25,
    sub: 26,
    esc: 27,
    fs: 28,
    gs: 29,
    rs: 30,
    us: 31,
    space: 32,
    exclamationMark: 33,
    // `!`
    quotationMark: 34,
    // `"`
    numberSign: 35,
    // `#`
    dollarSign: 36,
    // `$`
    percentSign: 37,
    // `%`
    ampersand: 38,
    // `&`
    apostrophe: 39,
    // `'`
    leftParenthesis: 40,
    // `(`
    rightParenthesis: 41,
    // `)`
    asterisk: 42,
    // `*`
    plusSign: 43,
    // `+`
    comma: 44,
    // `,`
    dash: 45,
    // `-`
    dot: 46,
    // `.`
    slash: 47,
    // `/`
    digit0: 48,
    // `0`
    digit1: 49,
    // `1`
    digit2: 50,
    // `2`
    digit3: 51,
    // `3`
    digit4: 52,
    // `4`
    digit5: 53,
    // `5`
    digit6: 54,
    // `6`
    digit7: 55,
    // `7`
    digit8: 56,
    // `8`
    digit9: 57,
    // `9`
    colon: 58,
    // `:`
    semicolon: 59,
    // `;`
    lessThan: 60,
    // `<`
    equalsTo: 61,
    // `=`
    greaterThan: 62,
    // `>`
    questionMark: 63,
    // `?`
    atSign: 64,
    // `@`
    uppercaseA: 65,
    // `A`
    uppercaseB: 66,
    // `B`
    uppercaseC: 67,
    // `C`
    uppercaseD: 68,
    // `D`
    uppercaseE: 69,
    // `E`
    uppercaseF: 70,
    // `F`
    uppercaseG: 71,
    // `G`
    uppercaseH: 72,
    // `H`
    uppercaseI: 73,
    // `I`
    uppercaseJ: 74,
    // `J`
    uppercaseK: 75,
    // `K`
    uppercaseL: 76,
    // `L`
    uppercaseM: 77,
    // `M`
    uppercaseN: 78,
    // `N`
    uppercaseO: 79,
    // `O`
    uppercaseP: 80,
    // `P`
    uppercaseQ: 81,
    // `Q`
    uppercaseR: 82,
    // `R`
    uppercaseS: 83,
    // `S`
    uppercaseT: 84,
    // `T`
    uppercaseU: 85,
    // `U`
    uppercaseV: 86,
    // `V`
    uppercaseW: 87,
    // `W`
    uppercaseX: 88,
    // `X`
    uppercaseY: 89,
    // `Y`
    uppercaseZ: 90,
    // `Z`
    leftSquareBracket: 91,
    // `[`
    backslash: 92,
    // `\`
    rightSquareBracket: 93,
    // `]`
    caret: 94,
    // `^`
    underscore: 95,
    // `_`
    graveAccent: 96,
    // `` ` ``
    lowercaseA: 97,
    // `a`
    lowercaseB: 98,
    // `b`
    lowercaseC: 99,
    // `c`
    lowercaseD: 100,
    // `d`
    lowercaseE: 101,
    // `e`
    lowercaseF: 102,
    // `f`
    lowercaseG: 103,
    // `g`
    lowercaseH: 104,
    // `h`
    lowercaseI: 105,
    // `i`
    lowercaseJ: 106,
    // `j`
    lowercaseK: 107,
    // `k`
    lowercaseL: 108,
    // `l`
    lowercaseM: 109,
    // `m`
    lowercaseN: 110,
    // `n`
    lowercaseO: 111,
    // `o`
    lowercaseP: 112,
    // `p`
    lowercaseQ: 113,
    // `q`
    lowercaseR: 114,
    // `r`
    lowercaseS: 115,
    // `s`
    lowercaseT: 116,
    // `t`
    lowercaseU: 117,
    // `u`
    lowercaseV: 118,
    // `v`
    lowercaseW: 119,
    // `w`
    lowercaseX: 120,
    // `x`
    lowercaseY: 121,
    // `y`
    lowercaseZ: 122,
    // `z`
    leftCurlyBrace: 123,
    // `{`
    verticalBar: 124,
    // `|`
    rightCurlyBrace: 125,
    // `}`
    tilde: 126,
    // `~`
    del: 127,
    // Unicode Specials block.
    byteOrderMarker: 65279,
    // Unicode Specials block.
    replacementCharacter: 65533
    // `�`
  }
);

// node_modules/micromark-util-symbol/lib/constants.js
var constants = (
  /** @type {const} */
  {
    attentionSideAfter: 2,
    // Symbol to mark an attention sequence as after content: `a*`
    attentionSideBefore: 1,
    // Symbol to mark an attention sequence as before content: `*a`
    atxHeadingOpeningFenceSizeMax: 6,
    // 6 number signs is fine, 7 isn’t.
    autolinkDomainSizeMax: 63,
    // 63 characters is fine, 64 is too many.
    autolinkSchemeSizeMax: 32,
    // 32 characters is fine, 33 is too many.
    cdataOpeningString: "CDATA[",
    // And preceded by `<![`.
    characterGroupPunctuation: 2,
    // Symbol used to indicate a character is punctuation
    characterGroupWhitespace: 1,
    // Symbol used to indicate a character is whitespace
    characterReferenceDecimalSizeMax: 7,
    // `&#9999999;`.
    characterReferenceHexadecimalSizeMax: 6,
    // `&#xff9999;`.
    characterReferenceNamedSizeMax: 31,
    // `&CounterClockwiseContourIntegral;`.
    codeFencedSequenceSizeMin: 3,
    // At least 3 ticks or tildes are needed.
    contentTypeContent: "content",
    contentTypeDocument: "document",
    contentTypeFlow: "flow",
    contentTypeString: "string",
    contentTypeText: "text",
    hardBreakPrefixSizeMin: 2,
    // At least 2 trailing spaces are needed.
    htmlBasic: 6,
    // Symbol for `<div`
    htmlCdata: 5,
    // Symbol for `<![CDATA[]]>`
    htmlComment: 2,
    // Symbol for `<!---->`
    htmlComplete: 7,
    // Symbol for `<x>`
    htmlDeclaration: 4,
    // Symbol for `<!doctype>`
    htmlInstruction: 3,
    // Symbol for `<?php?>`
    htmlRawSizeMax: 8,
    // Length of `textarea`.
    htmlRaw: 1,
    // Symbol for `<script>`
    linkResourceDestinationBalanceMax: 32,
    // See: <https://spec.commonmark.org/0.30/#link-destination>, <https://github.com/remarkjs/react-markdown/issues/658#issuecomment-984345577>
    linkReferenceSizeMax: 999,
    // See: <https://spec.commonmark.org/0.30/#link-label>
    listItemValueSizeMax: 10,
    // See: <https://spec.commonmark.org/0.30/#ordered-list-marker>
    numericBaseDecimal: 10,
    numericBaseHexadecimal: 16,
    tabSize: 4,
    // Tabs have a hard-coded size of 4, per CommonMark.
    thematicBreakMarkerCountMin: 3,
    // At least 3 asterisks, dashes, or underscores are needed.
    v8MaxSafeChunkSize: 1e4
    // V8 (and potentially others) have problems injecting giant arrays into other arrays, hence we operate in chunks.
  }
);

// node_modules/micromark-util-symbol/lib/types.js
var types = (
  /** @type {const} */
  {
    // Generic type for data, such as in a title, a destination, etc.
    data: "data",
    // Generic type for syntactic whitespace (tabs, virtual spaces, spaces).
    // Such as, between a fenced code fence and an info string.
    whitespace: "whitespace",
    // Generic type for line endings (line feed, carriage return, carriage return +
    // line feed).
    lineEnding: "lineEnding",
    // A line ending, but ending a blank line.
    lineEndingBlank: "lineEndingBlank",
    // Generic type for whitespace (tabs, virtual spaces, spaces) at the start of a
    // line.
    linePrefix: "linePrefix",
    // Generic type for whitespace (tabs, virtual spaces, spaces) at the end of a
    // line.
    lineSuffix: "lineSuffix",
    // Whole ATX heading:
    //
    // ```markdown
    // #
    // ## Alpha
    // ### Bravo ###
    // ```
    //
    // Includes `atxHeadingSequence`, `whitespace`, `atxHeadingText`.
    atxHeading: "atxHeading",
    // Sequence of number signs in an ATX heading (`###`).
    atxHeadingSequence: "atxHeadingSequence",
    // Content in an ATX heading (`alpha`).
    // Includes text.
    atxHeadingText: "atxHeadingText",
    // Whole autolink (`<https://example.com>` or `<admin@example.com>`)
    // Includes `autolinkMarker` and `autolinkProtocol` or `autolinkEmail`.
    autolink: "autolink",
    // Email autolink w/o markers (`admin@example.com`)
    autolinkEmail: "autolinkEmail",
    // Marker around an `autolinkProtocol` or `autolinkEmail` (`<` or `>`).
    autolinkMarker: "autolinkMarker",
    // Protocol autolink w/o markers (`https://example.com`)
    autolinkProtocol: "autolinkProtocol",
    // A whole character escape (`\-`).
    // Includes `escapeMarker` and `characterEscapeValue`.
    characterEscape: "characterEscape",
    // The escaped character (`-`).
    characterEscapeValue: "characterEscapeValue",
    // A whole character reference (`&amp;`, `&#8800;`, or `&#x1D306;`).
    // Includes `characterReferenceMarker`, an optional
    // `characterReferenceMarkerNumeric`, in which case an optional
    // `characterReferenceMarkerHexadecimal`, and a `characterReferenceValue`.
    characterReference: "characterReference",
    // The start or end marker (`&` or `;`).
    characterReferenceMarker: "characterReferenceMarker",
    // Mark reference as numeric (`#`).
    characterReferenceMarkerNumeric: "characterReferenceMarkerNumeric",
    // Mark reference as numeric (`x` or `X`).
    characterReferenceMarkerHexadecimal: "characterReferenceMarkerHexadecimal",
    // Value of character reference w/o markers (`amp`, `8800`, or `1D306`).
    characterReferenceValue: "characterReferenceValue",
    // Whole fenced code:
    //
    // ````markdown
    // ```js
    // alert(1)
    // ```
    // ````
    codeFenced: "codeFenced",
    // A fenced code fence, including whitespace, sequence, info, and meta
    // (` ```js `).
    codeFencedFence: "codeFencedFence",
    // Sequence of grave accent or tilde characters (` ``` `) in a fence.
    codeFencedFenceSequence: "codeFencedFenceSequence",
    // Info word (`js`) in a fence.
    // Includes string.
    codeFencedFenceInfo: "codeFencedFenceInfo",
    // Meta words (`highlight="1"`) in a fence.
    // Includes string.
    codeFencedFenceMeta: "codeFencedFenceMeta",
    // A line of code.
    codeFlowValue: "codeFlowValue",
    // Whole indented code:
    //
    // ```markdown
    //     alert(1)
    // ```
    //
    // Includes `lineEnding`, `linePrefix`, and `codeFlowValue`.
    codeIndented: "codeIndented",
    // A text code (``` `alpha` ```).
    // Includes `codeTextSequence`, `codeTextData`, `lineEnding`, and can include
    // `codeTextPadding`.
    codeText: "codeText",
    codeTextData: "codeTextData",
    // A space or line ending right after or before a tick.
    codeTextPadding: "codeTextPadding",
    // A text code fence (` `` `).
    codeTextSequence: "codeTextSequence",
    // Whole content:
    //
    // ```markdown
    // [a]: b
    // c
    // =
    // d
    // ```
    //
    // Includes `paragraph` and `definition`.
    content: "content",
    // Whole definition:
    //
    // ```markdown
    // [micromark]: https://github.com/micromark/micromark
    // ```
    //
    // Includes `definitionLabel`, `definitionMarker`, `whitespace`,
    // `definitionDestination`, and optionally `lineEnding` and `definitionTitle`.
    definition: "definition",
    // Destination of a definition (`https://github.com/micromark/micromark` or
    // `<https://github.com/micromark/micromark>`).
    // Includes `definitionDestinationLiteral` or `definitionDestinationRaw`.
    definitionDestination: "definitionDestination",
    // Enclosed destination of a definition
    // (`<https://github.com/micromark/micromark>`).
    // Includes `definitionDestinationLiteralMarker` and optionally
    // `definitionDestinationString`.
    definitionDestinationLiteral: "definitionDestinationLiteral",
    // Markers of an enclosed definition destination (`<` or `>`).
    definitionDestinationLiteralMarker: "definitionDestinationLiteralMarker",
    // Unenclosed destination of a definition
    // (`https://github.com/micromark/micromark`).
    // Includes `definitionDestinationString`.
    definitionDestinationRaw: "definitionDestinationRaw",
    // Text in an destination (`https://github.com/micromark/micromark`).
    // Includes string.
    definitionDestinationString: "definitionDestinationString",
    // Label of a definition (`[micromark]`).
    // Includes `definitionLabelMarker` and `definitionLabelString`.
    definitionLabel: "definitionLabel",
    // Markers of a definition label (`[` or `]`).
    definitionLabelMarker: "definitionLabelMarker",
    // Value of a definition label (`micromark`).
    // Includes string.
    definitionLabelString: "definitionLabelString",
    // Marker between a label and a destination (`:`).
    definitionMarker: "definitionMarker",
    // Title of a definition (`"x"`, `'y'`, or `(z)`).
    // Includes `definitionTitleMarker` and optionally `definitionTitleString`.
    definitionTitle: "definitionTitle",
    // Marker around a title of a definition (`"`, `'`, `(`, or `)`).
    definitionTitleMarker: "definitionTitleMarker",
    // Data without markers in a title (`z`).
    // Includes string.
    definitionTitleString: "definitionTitleString",
    // Emphasis (`*alpha*`).
    // Includes `emphasisSequence` and `emphasisText`.
    emphasis: "emphasis",
    // Sequence of emphasis markers (`*` or `_`).
    emphasisSequence: "emphasisSequence",
    // Emphasis text (`alpha`).
    // Includes text.
    emphasisText: "emphasisText",
    // The character escape marker (`\`).
    escapeMarker: "escapeMarker",
    // A hard break created with a backslash (`\\n`).
    // Note: does not include the line ending.
    hardBreakEscape: "hardBreakEscape",
    // A hard break created with trailing spaces (`  \n`).
    // Does not include the line ending.
    hardBreakTrailing: "hardBreakTrailing",
    // Flow HTML:
    //
    // ```markdown
    // <div
    // ```
    //
    // Inlcudes `lineEnding`, `htmlFlowData`.
    htmlFlow: "htmlFlow",
    htmlFlowData: "htmlFlowData",
    // HTML in text (the tag in `a <i> b`).
    // Includes `lineEnding`, `htmlTextData`.
    htmlText: "htmlText",
    htmlTextData: "htmlTextData",
    // Whole image (`![alpha](bravo)`, `![alpha][bravo]`, `![alpha][]`, or
    // `![alpha]`).
    // Includes `label` and an optional `resource` or `reference`.
    image: "image",
    // Whole link label (`[*alpha*]`).
    // Includes `labelLink` or `labelImage`, `labelText`, and `labelEnd`.
    label: "label",
    // Text in an label (`*alpha*`).
    // Includes text.
    labelText: "labelText",
    // Start a link label (`[`).
    // Includes a `labelMarker`.
    labelLink: "labelLink",
    // Start an image label (`![`).
    // Includes `labelImageMarker` and `labelMarker`.
    labelImage: "labelImage",
    // Marker of a label (`[` or `]`).
    labelMarker: "labelMarker",
    // Marker to start an image (`!`).
    labelImageMarker: "labelImageMarker",
    // End a label (`]`).
    // Includes `labelMarker`.
    labelEnd: "labelEnd",
    // Whole link (`[alpha](bravo)`, `[alpha][bravo]`, `[alpha][]`, or `[alpha]`).
    // Includes `label` and an optional `resource` or `reference`.
    link: "link",
    // Whole paragraph:
    //
    // ```markdown
    // alpha
    // bravo.
    // ```
    //
    // Includes text.
    paragraph: "paragraph",
    // A reference (`[alpha]` or `[]`).
    // Includes `referenceMarker` and an optional `referenceString`.
    reference: "reference",
    // A reference marker (`[` or `]`).
    referenceMarker: "referenceMarker",
    // Reference text (`alpha`).
    // Includes string.
    referenceString: "referenceString",
    // A resource (`(https://example.com "alpha")`).
    // Includes `resourceMarker`, an optional `resourceDestination` with an optional
    // `whitespace` and `resourceTitle`.
    resource: "resource",
    // A resource destination (`https://example.com`).
    // Includes `resourceDestinationLiteral` or `resourceDestinationRaw`.
    resourceDestination: "resourceDestination",
    // A literal resource destination (`<https://example.com>`).
    // Includes `resourceDestinationLiteralMarker` and optionally
    // `resourceDestinationString`.
    resourceDestinationLiteral: "resourceDestinationLiteral",
    // A resource destination marker (`<` or `>`).
    resourceDestinationLiteralMarker: "resourceDestinationLiteralMarker",
    // A raw resource destination (`https://example.com`).
    // Includes `resourceDestinationString`.
    resourceDestinationRaw: "resourceDestinationRaw",
    // Resource destination text (`https://example.com`).
    // Includes string.
    resourceDestinationString: "resourceDestinationString",
    // A resource marker (`(` or `)`).
    resourceMarker: "resourceMarker",
    // A resource title (`"alpha"`, `'alpha'`, or `(alpha)`).
    // Includes `resourceTitleMarker` and optionally `resourceTitleString`.
    resourceTitle: "resourceTitle",
    // A resource title marker (`"`, `'`, `(`, or `)`).
    resourceTitleMarker: "resourceTitleMarker",
    // Resource destination title (`alpha`).
    // Includes string.
    resourceTitleString: "resourceTitleString",
    // Whole setext heading:
    //
    // ```markdown
    // alpha
    // bravo
    // =====
    // ```
    //
    // Includes `setextHeadingText`, `lineEnding`, `linePrefix`, and
    // `setextHeadingLine`.
    setextHeading: "setextHeading",
    // Content in a setext heading (`alpha\nbravo`).
    // Includes text.
    setextHeadingText: "setextHeadingText",
    // Underline in a setext heading, including whitespace suffix (`==`).
    // Includes `setextHeadingLineSequence`.
    setextHeadingLine: "setextHeadingLine",
    // Sequence of equals or dash characters in underline in a setext heading (`-`).
    setextHeadingLineSequence: "setextHeadingLineSequence",
    // Strong (`**alpha**`).
    // Includes `strongSequence` and `strongText`.
    strong: "strong",
    // Sequence of strong markers (`**` or `__`).
    strongSequence: "strongSequence",
    // Strong text (`alpha`).
    // Includes text.
    strongText: "strongText",
    // Whole thematic break:
    //
    // ```markdown
    // * * *
    // ```
    //
    // Includes `thematicBreakSequence` and `whitespace`.
    thematicBreak: "thematicBreak",
    // A sequence of one or more thematic break markers (`***`).
    thematicBreakSequence: "thematicBreakSequence",
    // Whole block quote:
    //
    // ```markdown
    // > a
    // >
    // > b
    // ```
    //
    // Includes `blockQuotePrefix` and flow.
    blockQuote: "blockQuote",
    // The `>` or `> ` of a block quote.
    blockQuotePrefix: "blockQuotePrefix",
    // The `>` of a block quote prefix.
    blockQuoteMarker: "blockQuoteMarker",
    // The optional ` ` of a block quote prefix.
    blockQuotePrefixWhitespace: "blockQuotePrefixWhitespace",
    // Whole ordered list:
    //
    // ```markdown
    // 1. a
    //    b
    // ```
    //
    // Includes `listItemPrefix`, flow, and optionally  `listItemIndent` on further
    // lines.
    listOrdered: "listOrdered",
    // Whole unordered list:
    //
    // ```markdown
    // - a
    //   b
    // ```
    //
    // Includes `listItemPrefix`, flow, and optionally  `listItemIndent` on further
    // lines.
    listUnordered: "listUnordered",
    // The indent of further list item lines.
    listItemIndent: "listItemIndent",
    // A marker, as in, `*`, `+`, `-`, `.`, or `)`.
    listItemMarker: "listItemMarker",
    // The thing that starts a list item, such as `1. `.
    // Includes `listItemValue` if ordered, `listItemMarker`, and
    // `listItemPrefixWhitespace` (unless followed by a line ending).
    listItemPrefix: "listItemPrefix",
    // The whitespace after a marker.
    listItemPrefixWhitespace: "listItemPrefixWhitespace",
    // The numerical value of an ordered item.
    listItemValue: "listItemValue",
    // Internal types used for subtokenizers, compiled away
    chunkDocument: "chunkDocument",
    chunkContent: "chunkContent",
    chunkFlow: "chunkFlow",
    chunkText: "chunkText",
    chunkString: "chunkString"
  }
);

// node_modules/micromark-util-symbol/lib/values.js
var values = (
  /** @type {const} */
  {
    ht: "	",
    lf: "\n",
    cr: "\r",
    space: " ",
    exclamationMark: "!",
    quotationMark: '"',
    numberSign: "#",
    dollarSign: "$",
    percentSign: "%",
    ampersand: "&",
    apostrophe: "'",
    leftParenthesis: "(",
    rightParenthesis: ")",
    asterisk: "*",
    plusSign: "+",
    comma: ",",
    dash: "-",
    dot: ".",
    slash: "/",
    digit0: "0",
    digit1: "1",
    digit2: "2",
    digit3: "3",
    digit4: "4",
    digit5: "5",
    digit6: "6",
    digit7: "7",
    digit8: "8",
    digit9: "9",
    colon: ":",
    semicolon: ";",
    lessThan: "<",
    equalsTo: "=",
    greaterThan: ">",
    questionMark: "?",
    atSign: "@",
    uppercaseA: "A",
    uppercaseB: "B",
    uppercaseC: "C",
    uppercaseD: "D",
    uppercaseE: "E",
    uppercaseF: "F",
    uppercaseG: "G",
    uppercaseH: "H",
    uppercaseI: "I",
    uppercaseJ: "J",
    uppercaseK: "K",
    uppercaseL: "L",
    uppercaseM: "M",
    uppercaseN: "N",
    uppercaseO: "O",
    uppercaseP: "P",
    uppercaseQ: "Q",
    uppercaseR: "R",
    uppercaseS: "S",
    uppercaseT: "T",
    uppercaseU: "U",
    uppercaseV: "V",
    uppercaseW: "W",
    uppercaseX: "X",
    uppercaseY: "Y",
    uppercaseZ: "Z",
    leftSquareBracket: "[",
    backslash: "\\",
    rightSquareBracket: "]",
    caret: "^",
    underscore: "_",
    graveAccent: "`",
    lowercaseA: "a",
    lowercaseB: "b",
    lowercaseC: "c",
    lowercaseD: "d",
    lowercaseE: "e",
    lowercaseF: "f",
    lowercaseG: "g",
    lowercaseH: "h",
    lowercaseI: "i",
    lowercaseJ: "j",
    lowercaseK: "k",
    lowercaseL: "l",
    lowercaseM: "m",
    lowercaseN: "n",
    lowercaseO: "o",
    lowercaseP: "p",
    lowercaseQ: "q",
    lowercaseR: "r",
    lowercaseS: "s",
    lowercaseT: "t",
    lowercaseU: "u",
    lowercaseV: "v",
    lowercaseW: "w",
    lowercaseX: "x",
    lowercaseY: "y",
    lowercaseZ: "z",
    leftCurlyBrace: "{",
    verticalBar: "|",
    rightCurlyBrace: "}",
    tilde: "~",
    replacementCharacter: "�"
  }
);

// node_modules/micromark-util-chunked/dev/index.js
function splice(list4, start2, remove, items) {
  const end = list4.length;
  let chunkStart = 0;
  let parameters;
  if (start2 < 0) {
    start2 = -start2 > end ? 0 : end + start2;
  } else {
    start2 = start2 > end ? end : start2;
  }
  remove = remove > 0 ? remove : 0;
  if (items.length < constants.v8MaxSafeChunkSize) {
    parameters = Array.from(items);
    parameters.unshift(start2, remove);
    list4.splice(...parameters);
  } else {
    if (remove) list4.splice(start2, remove);
    while (chunkStart < items.length) {
      parameters = items.slice(
        chunkStart,
        chunkStart + constants.v8MaxSafeChunkSize
      );
      parameters.unshift(start2, 0);
      list4.splice(...parameters);
      chunkStart += constants.v8MaxSafeChunkSize;
      start2 += constants.v8MaxSafeChunkSize;
    }
  }
}
function push(list4, items) {
  if (list4.length > 0) {
    splice(list4, list4.length, 0, items);
    return list4;
  }
  return items;
}

// node_modules/micromark-util-combine-extensions/index.js
var hasOwnProperty = {}.hasOwnProperty;
function combineExtensions(extensions) {
  const all2 = {};
  let index6 = -1;
  while (++index6 < extensions.length) {
    syntaxExtension(all2, extensions[index6]);
  }
  return all2;
}
function syntaxExtension(all2, extension2) {
  let hook;
  for (hook in extension2) {
    const maybe = hasOwnProperty.call(all2, hook) ? all2[hook] : void 0;
    const left = maybe || (all2[hook] = {});
    const right = extension2[hook];
    let code4;
    if (right) {
      for (code4 in right) {
        if (!hasOwnProperty.call(left, code4)) left[code4] = [];
        const value = right[code4];
        constructs(
          // @ts-expect-error Looks like a list.
          left[code4],
          Array.isArray(value) ? value : value ? [value] : []
        );
      }
    }
  }
}
function constructs(existing, list4) {
  let index6 = -1;
  const before = [];
  while (++index6 < list4.length) {
    ;
    (list4[index6].add === "after" ? existing : before).push(list4[index6]);
  }
  splice(existing, 0, 0, before);
}

// node_modules/micromark-util-decode-numeric-character-reference/dev/index.js
function decodeNumericCharacterReference(value, base) {
  const code4 = Number.parseInt(value, base);
  if (
    // C0 except for HT, LF, FF, CR, space.
    code4 < codes.ht || code4 === codes.vt || code4 > codes.cr && code4 < codes.space || // Control character (DEL) of C0, and C1 controls.
    code4 > codes.tilde && code4 < 160 || // Lone high surrogates and low surrogates.
    code4 > 55295 && code4 < 57344 || // Noncharacters.
    code4 > 64975 && code4 < 65008 || /* eslint-disable no-bitwise */
    (code4 & 65535) === 65535 || (code4 & 65535) === 65534 || /* eslint-enable no-bitwise */
    // Out of range
    code4 > 1114111
  ) {
    return values.replacementCharacter;
  }
  return String.fromCodePoint(code4);
}

// node_modules/micromark-util-normalize-identifier/dev/index.js
function normalizeIdentifier(value) {
  return value.replace(/[\t\n\r ]+/g, values.space).replace(/^ | $/g, "").toLowerCase().toUpperCase();
}

// node_modules/micromark-util-character/dev/index.js
var asciiAlpha = regexCheck(/[A-Za-z]/);
var asciiAlphanumeric = regexCheck(/[\dA-Za-z]/);
var asciiAtext = regexCheck(/[#-'*+\--9=?A-Z^-~]/);
function asciiControl(code4) {
  return (
    // Special whitespace codes (which have negative values), C0 and Control
    // character DEL
    code4 !== null && (code4 < codes.space || code4 === codes.del)
  );
}
var asciiDigit = regexCheck(/\d/);
var asciiHexDigit = regexCheck(/[\dA-Fa-f]/);
var asciiPunctuation = regexCheck(/[!-/:-@[-`{-~]/);
function markdownLineEnding(code4) {
  return code4 !== null && code4 < codes.horizontalTab;
}
function markdownLineEndingOrSpace(code4) {
  return code4 !== null && (code4 < codes.nul || code4 === codes.space);
}
function markdownSpace(code4) {
  return code4 === codes.horizontalTab || code4 === codes.virtualSpace || code4 === codes.space;
}
var unicodePunctuation = regexCheck(new RegExp("\\p{P}|\\p{S}", "u"));
var unicodeWhitespace = regexCheck(/\s/);
function regexCheck(regex) {
  return check;
  function check(code4) {
    return code4 !== null && code4 > -1 && regex.test(String.fromCharCode(code4));
  }
}

// node_modules/micromark-util-sanitize-uri/dev/index.js
function normalizeUri(value) {
  const result = [];
  let index6 = -1;
  let start2 = 0;
  let skip2 = 0;
  while (++index6 < value.length) {
    const code4 = value.charCodeAt(index6);
    let replace2 = "";
    if (code4 === codes.percentSign && asciiAlphanumeric(value.charCodeAt(index6 + 1)) && asciiAlphanumeric(value.charCodeAt(index6 + 2))) {
      skip2 = 2;
    } else if (code4 < 128) {
      if (!/[!#$&-;=?-Z_a-z~]/.test(String.fromCharCode(code4))) {
        replace2 = String.fromCharCode(code4);
      }
    } else if (code4 > 55295 && code4 < 57344) {
      const next = value.charCodeAt(index6 + 1);
      if (code4 < 56320 && next > 56319 && next < 57344) {
        replace2 = String.fromCharCode(code4, next);
        skip2 = 1;
      } else {
        replace2 = values.replacementCharacter;
      }
    } else {
      replace2 = String.fromCharCode(code4);
    }
    if (replace2) {
      result.push(value.slice(start2, index6), encodeURIComponent(replace2));
      start2 = index6 + skip2 + 1;
      replace2 = "";
    }
    if (skip2) {
      index6 += skip2;
      skip2 = 0;
    }
  }
  return result.join("") + value.slice(start2);
}

// node_modules/micromark/dev/lib/compile.js
var hasOwnProperty2 = {}.hasOwnProperty;

// node_modules/micromark-factory-space/dev/index.js
function factorySpace(effects, ok3, type, max2) {
  const limit = max2 ? max2 - 1 : Number.POSITIVE_INFINITY;
  let size4 = 0;
  return start2;
  function start2(code4) {
    if (markdownSpace(code4)) {
      effects.enter(type);
      return prefix(code4);
    }
    return ok3(code4);
  }
  function prefix(code4) {
    if (markdownSpace(code4) && size4++ < limit) {
      effects.consume(code4);
      return prefix;
    }
    effects.exit(type);
    return ok3(code4);
  }
}

// node_modules/micromark/dev/lib/initialize/content.js
var content = { tokenize: initializeContent };
function initializeContent(effects) {
  const contentStart = effects.attempt(
    this.parser.constructs.contentInitial,
    afterContentStartConstruct,
    paragraphInitial
  );
  let previous3;
  return contentStart;
  function afterContentStartConstruct(code4) {
    ok(
      code4 === codes.eof || markdownLineEnding(code4),
      "expected eol or eof"
    );
    if (code4 === codes.eof) {
      effects.consume(code4);
      return;
    }
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return factorySpace(effects, contentStart, types.linePrefix);
  }
  function paragraphInitial(code4) {
    ok(
      code4 !== codes.eof && !markdownLineEnding(code4),
      "expected anything other than a line ending or EOF"
    );
    effects.enter(types.paragraph);
    return lineStart(code4);
  }
  function lineStart(code4) {
    const token = effects.enter(types.chunkText, {
      contentType: constants.contentTypeText,
      previous: previous3
    });
    if (previous3) {
      previous3.next = token;
    }
    previous3 = token;
    return data(code4);
  }
  function data(code4) {
    if (code4 === codes.eof) {
      effects.exit(types.chunkText);
      effects.exit(types.paragraph);
      effects.consume(code4);
      return;
    }
    if (markdownLineEnding(code4)) {
      effects.consume(code4);
      effects.exit(types.chunkText);
      return lineStart;
    }
    effects.consume(code4);
    return data;
  }
}

// node_modules/micromark/dev/lib/initialize/document.js
var document2 = { tokenize: initializeDocument };
var containerConstruct = { tokenize: tokenizeContainer };
function initializeDocument(effects) {
  const self2 = this;
  const stack = [];
  let continued = 0;
  let childFlow;
  let childToken;
  let lineStartOffset;
  return start2;
  function start2(code4) {
    if (continued < stack.length) {
      const item = stack[continued];
      self2.containerState = item[1];
      ok(
        item[0].continuation,
        "expected `continuation` to be defined on container construct"
      );
      return effects.attempt(
        item[0].continuation,
        documentContinue,
        checkNewContainers
      )(code4);
    }
    return checkNewContainers(code4);
  }
  function documentContinue(code4) {
    ok(
      self2.containerState,
      "expected `containerState` to be defined after continuation"
    );
    continued++;
    if (self2.containerState._closeFlow) {
      self2.containerState._closeFlow = void 0;
      if (childFlow) {
        closeFlow();
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let point4;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === types.chunkFlow) {
          point4 = self2.events[indexBeforeFlow][1].end;
          break;
        }
      }
      ok(point4, "could not find previous flow chunk");
      exitContainers(continued);
      let index6 = indexBeforeExits;
      while (index6 < self2.events.length) {
        self2.events[index6][1].end = { ...point4 };
        index6++;
      }
      splice(
        self2.events,
        indexBeforeFlow + 1,
        0,
        self2.events.slice(indexBeforeExits)
      );
      self2.events.length = index6;
      return checkNewContainers(code4);
    }
    return start2(code4);
  }
  function checkNewContainers(code4) {
    if (continued === stack.length) {
      if (!childFlow) {
        return documentContinued(code4);
      }
      if (childFlow.currentConstruct && childFlow.currentConstruct.concrete) {
        return flowStart(code4);
      }
      self2.interrupt = Boolean(
        childFlow.currentConstruct && !childFlow._gfmTableDynamicInterruptHack
      );
    }
    self2.containerState = {};
    return effects.check(
      containerConstruct,
      thereIsANewContainer,
      thereIsNoNewContainer
    )(code4);
  }
  function thereIsANewContainer(code4) {
    if (childFlow) closeFlow();
    exitContainers(continued);
    return documentContinued(code4);
  }
  function thereIsNoNewContainer(code4) {
    self2.parser.lazy[self2.now().line] = continued !== stack.length;
    lineStartOffset = self2.now().offset;
    return flowStart(code4);
  }
  function documentContinued(code4) {
    self2.containerState = {};
    return effects.attempt(
      containerConstruct,
      containerContinue,
      flowStart
    )(code4);
  }
  function containerContinue(code4) {
    ok(
      self2.currentConstruct,
      "expected `currentConstruct` to be defined on tokenizer"
    );
    ok(
      self2.containerState,
      "expected `containerState` to be defined on tokenizer"
    );
    continued++;
    stack.push([self2.currentConstruct, self2.containerState]);
    return documentContinued(code4);
  }
  function flowStart(code4) {
    if (code4 === codes.eof) {
      if (childFlow) closeFlow();
      exitContainers(0);
      effects.consume(code4);
      return;
    }
    childFlow = childFlow || self2.parser.flow(self2.now());
    effects.enter(types.chunkFlow, {
      _tokenizer: childFlow,
      contentType: constants.contentTypeFlow,
      previous: childToken
    });
    return flowContinue(code4);
  }
  function flowContinue(code4) {
    if (code4 === codes.eof) {
      writeToChild(effects.exit(types.chunkFlow), true);
      exitContainers(0);
      effects.consume(code4);
      return;
    }
    if (markdownLineEnding(code4)) {
      effects.consume(code4);
      writeToChild(effects.exit(types.chunkFlow));
      continued = 0;
      self2.interrupt = void 0;
      return start2;
    }
    effects.consume(code4);
    return flowContinue;
  }
  function writeToChild(token, endOfFile) {
    ok(childFlow, "expected `childFlow` to be defined when continuing");
    const stream2 = self2.sliceStream(token);
    if (endOfFile) stream2.push(null);
    token.previous = childToken;
    if (childToken) childToken.next = token;
    childToken = token;
    childFlow.defineSkip(token.start);
    childFlow.write(stream2);
    if (self2.parser.lazy[token.start.line]) {
      let index6 = childFlow.events.length;
      while (index6--) {
        if (
          // The token starts before the line ending…
          childFlow.events[index6][1].start.offset < lineStartOffset && // …and either is not ended yet…
          (!childFlow.events[index6][1].end || // …or ends after it.
          childFlow.events[index6][1].end.offset > lineStartOffset)
        ) {
          return;
        }
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let seen;
      let point4;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === types.chunkFlow) {
          if (seen) {
            point4 = self2.events[indexBeforeFlow][1].end;
            break;
          }
          seen = true;
        }
      }
      ok(point4, "could not find previous flow chunk");
      exitContainers(continued);
      index6 = indexBeforeExits;
      while (index6 < self2.events.length) {
        self2.events[index6][1].end = { ...point4 };
        index6++;
      }
      splice(
        self2.events,
        indexBeforeFlow + 1,
        0,
        self2.events.slice(indexBeforeExits)
      );
      self2.events.length = index6;
    }
  }
  function exitContainers(size4) {
    let index6 = stack.length;
    while (index6-- > size4) {
      const entry = stack[index6];
      self2.containerState = entry[1];
      ok(
        entry[0].exit,
        "expected `exit` to be defined on container construct"
      );
      entry[0].exit.call(self2, effects);
    }
    stack.length = size4;
  }
  function closeFlow() {
    ok(
      self2.containerState,
      "expected `containerState` to be defined when closing flow"
    );
    ok(childFlow, "expected `childFlow` to be defined when closing it");
    childFlow.write([codes.eof]);
    childToken = void 0;
    childFlow = void 0;
    self2.containerState._closeFlow = void 0;
  }
}
function tokenizeContainer(effects, ok3, nok) {
  ok(
    this.parser.constructs.disable.null,
    "expected `disable.null` to be populated"
  );
  return factorySpace(
    effects,
    effects.attempt(this.parser.constructs.document, ok3, nok),
    types.linePrefix,
    this.parser.constructs.disable.null.includes("codeIndented") ? void 0 : constants.tabSize
  );
}

// node_modules/micromark-util-classify-character/dev/index.js
function classifyCharacter(code4) {
  if (code4 === codes.eof || markdownLineEndingOrSpace(code4) || unicodeWhitespace(code4)) {
    return constants.characterGroupWhitespace;
  }
  if (unicodePunctuation(code4)) {
    return constants.characterGroupPunctuation;
  }
}

// node_modules/micromark-util-resolve-all/index.js
function resolveAll(constructs2, events, context) {
  const called = [];
  let index6 = -1;
  while (++index6 < constructs2.length) {
    const resolve = constructs2[index6].resolveAll;
    if (resolve && !called.includes(resolve)) {
      events = resolve(events, context);
      called.push(resolve);
    }
  }
  return events;
}

// node_modules/micromark-core-commonmark/dev/lib/attention.js
var attention = {
  name: "attention",
  resolveAll: resolveAllAttention,
  tokenize: tokenizeAttention
};
function resolveAllAttention(events, context) {
  let index6 = -1;
  let open;
  let group;
  let text8;
  let openingSequence;
  let closingSequence;
  let use2;
  let nextEvents;
  let offset4;
  while (++index6 < events.length) {
    if (events[index6][0] === "enter" && events[index6][1].type === "attentionSequence" && events[index6][1]._close) {
      open = index6;
      while (open--) {
        if (events[open][0] === "exit" && events[open][1].type === "attentionSequence" && events[open][1]._open && // If the markers are the same:
        context.sliceSerialize(events[open][1]).charCodeAt(0) === context.sliceSerialize(events[index6][1]).charCodeAt(0)) {
          if ((events[open][1]._close || events[index6][1]._open) && (events[index6][1].end.offset - events[index6][1].start.offset) % 3 && !((events[open][1].end.offset - events[open][1].start.offset + events[index6][1].end.offset - events[index6][1].start.offset) % 3)) {
            continue;
          }
          use2 = events[open][1].end.offset - events[open][1].start.offset > 1 && events[index6][1].end.offset - events[index6][1].start.offset > 1 ? 2 : 1;
          const start2 = { ...events[open][1].end };
          const end = { ...events[index6][1].start };
          movePoint(start2, -use2);
          movePoint(end, use2);
          openingSequence = {
            type: use2 > 1 ? types.strongSequence : types.emphasisSequence,
            start: start2,
            end: { ...events[open][1].end }
          };
          closingSequence = {
            type: use2 > 1 ? types.strongSequence : types.emphasisSequence,
            start: { ...events[index6][1].start },
            end
          };
          text8 = {
            type: use2 > 1 ? types.strongText : types.emphasisText,
            start: { ...events[open][1].end },
            end: { ...events[index6][1].start }
          };
          group = {
            type: use2 > 1 ? types.strong : types.emphasis,
            start: { ...openingSequence.start },
            end: { ...closingSequence.end }
          };
          events[open][1].end = { ...openingSequence.start };
          events[index6][1].start = { ...closingSequence.end };
          nextEvents = [];
          if (events[open][1].end.offset - events[open][1].start.offset) {
            nextEvents = push(nextEvents, [
              ["enter", events[open][1], context],
              ["exit", events[open][1], context]
            ]);
          }
          nextEvents = push(nextEvents, [
            ["enter", group, context],
            ["enter", openingSequence, context],
            ["exit", openingSequence, context],
            ["enter", text8, context]
          ]);
          ok(
            context.parser.constructs.insideSpan.null,
            "expected `insideSpan` to be populated"
          );
          nextEvents = push(
            nextEvents,
            resolveAll(
              context.parser.constructs.insideSpan.null,
              events.slice(open + 1, index6),
              context
            )
          );
          nextEvents = push(nextEvents, [
            ["exit", text8, context],
            ["enter", closingSequence, context],
            ["exit", closingSequence, context],
            ["exit", group, context]
          ]);
          if (events[index6][1].end.offset - events[index6][1].start.offset) {
            offset4 = 2;
            nextEvents = push(nextEvents, [
              ["enter", events[index6][1], context],
              ["exit", events[index6][1], context]
            ]);
          } else {
            offset4 = 0;
          }
          splice(events, open - 1, index6 - open + 3, nextEvents);
          index6 = open + nextEvents.length - offset4 - 2;
          break;
        }
      }
    }
  }
  index6 = -1;
  while (++index6 < events.length) {
    if (events[index6][1].type === "attentionSequence") {
      events[index6][1].type = "data";
    }
  }
  return events;
}
function tokenizeAttention(effects, ok3) {
  const attentionMarkers2 = this.parser.constructs.attentionMarkers.null;
  const previous3 = this.previous;
  const before = classifyCharacter(previous3);
  let marker;
  return start2;
  function start2(code4) {
    ok(
      code4 === codes.asterisk || code4 === codes.underscore,
      "expected asterisk or underscore"
    );
    marker = code4;
    effects.enter("attentionSequence");
    return inside(code4);
  }
  function inside(code4) {
    if (code4 === marker) {
      effects.consume(code4);
      return inside;
    }
    const token = effects.exit("attentionSequence");
    const after = classifyCharacter(code4);
    ok(attentionMarkers2, "expected `attentionMarkers` to be populated");
    const open = !after || after === constants.characterGroupPunctuation && before || attentionMarkers2.includes(code4);
    const close = !before || before === constants.characterGroupPunctuation && after || attentionMarkers2.includes(previous3);
    token._open = Boolean(
      marker === codes.asterisk ? open : open && (before || !close)
    );
    token._close = Boolean(
      marker === codes.asterisk ? close : close && (after || !open)
    );
    return ok3(code4);
  }
}
function movePoint(point4, offset4) {
  point4.column += offset4;
  point4.offset += offset4;
  point4._bufferIndex += offset4;
}

// node_modules/micromark-core-commonmark/dev/lib/autolink.js
var autolink = { name: "autolink", tokenize: tokenizeAutolink };
function tokenizeAutolink(effects, ok3, nok) {
  let size4 = 0;
  return start2;
  function start2(code4) {
    ok(code4 === codes.lessThan, "expected `<`");
    effects.enter(types.autolink);
    effects.enter(types.autolinkMarker);
    effects.consume(code4);
    effects.exit(types.autolinkMarker);
    effects.enter(types.autolinkProtocol);
    return open;
  }
  function open(code4) {
    if (asciiAlpha(code4)) {
      effects.consume(code4);
      return schemeOrEmailAtext;
    }
    if (code4 === codes.atSign) {
      return nok(code4);
    }
    return emailAtext(code4);
  }
  function schemeOrEmailAtext(code4) {
    if (code4 === codes.plusSign || code4 === codes.dash || code4 === codes.dot || asciiAlphanumeric(code4)) {
      size4 = 1;
      return schemeInsideOrEmailAtext(code4);
    }
    return emailAtext(code4);
  }
  function schemeInsideOrEmailAtext(code4) {
    if (code4 === codes.colon) {
      effects.consume(code4);
      size4 = 0;
      return urlInside;
    }
    if ((code4 === codes.plusSign || code4 === codes.dash || code4 === codes.dot || asciiAlphanumeric(code4)) && size4++ < constants.autolinkSchemeSizeMax) {
      effects.consume(code4);
      return schemeInsideOrEmailAtext;
    }
    size4 = 0;
    return emailAtext(code4);
  }
  function urlInside(code4) {
    if (code4 === codes.greaterThan) {
      effects.exit(types.autolinkProtocol);
      effects.enter(types.autolinkMarker);
      effects.consume(code4);
      effects.exit(types.autolinkMarker);
      effects.exit(types.autolink);
      return ok3;
    }
    if (code4 === codes.eof || code4 === codes.space || code4 === codes.lessThan || asciiControl(code4)) {
      return nok(code4);
    }
    effects.consume(code4);
    return urlInside;
  }
  function emailAtext(code4) {
    if (code4 === codes.atSign) {
      effects.consume(code4);
      return emailAtSignOrDot;
    }
    if (asciiAtext(code4)) {
      effects.consume(code4);
      return emailAtext;
    }
    return nok(code4);
  }
  function emailAtSignOrDot(code4) {
    return asciiAlphanumeric(code4) ? emailLabel(code4) : nok(code4);
  }
  function emailLabel(code4) {
    if (code4 === codes.dot) {
      effects.consume(code4);
      size4 = 0;
      return emailAtSignOrDot;
    }
    if (code4 === codes.greaterThan) {
      effects.exit(types.autolinkProtocol).type = types.autolinkEmail;
      effects.enter(types.autolinkMarker);
      effects.consume(code4);
      effects.exit(types.autolinkMarker);
      effects.exit(types.autolink);
      return ok3;
    }
    return emailValue(code4);
  }
  function emailValue(code4) {
    if ((code4 === codes.dash || asciiAlphanumeric(code4)) && size4++ < constants.autolinkDomainSizeMax) {
      const next = code4 === codes.dash ? emailValue : emailLabel;
      effects.consume(code4);
      return next;
    }
    return nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/blank-line.js
var blankLine = { partial: true, tokenize: tokenizeBlankLine };
function tokenizeBlankLine(effects, ok3, nok) {
  return start2;
  function start2(code4) {
    return markdownSpace(code4) ? factorySpace(effects, after, types.linePrefix)(code4) : after(code4);
  }
  function after(code4) {
    return code4 === codes.eof || markdownLineEnding(code4) ? ok3(code4) : nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/block-quote.js
var blockQuote = {
  continuation: { tokenize: tokenizeBlockQuoteContinuation },
  exit,
  name: "blockQuote",
  tokenize: tokenizeBlockQuoteStart
};
function tokenizeBlockQuoteStart(effects, ok3, nok) {
  const self2 = this;
  return start2;
  function start2(code4) {
    if (code4 === codes.greaterThan) {
      const state = self2.containerState;
      ok(state, "expected `containerState` to be defined in container");
      if (!state.open) {
        effects.enter(types.blockQuote, { _container: true });
        state.open = true;
      }
      effects.enter(types.blockQuotePrefix);
      effects.enter(types.blockQuoteMarker);
      effects.consume(code4);
      effects.exit(types.blockQuoteMarker);
      return after;
    }
    return nok(code4);
  }
  function after(code4) {
    if (markdownSpace(code4)) {
      effects.enter(types.blockQuotePrefixWhitespace);
      effects.consume(code4);
      effects.exit(types.blockQuotePrefixWhitespace);
      effects.exit(types.blockQuotePrefix);
      return ok3;
    }
    effects.exit(types.blockQuotePrefix);
    return ok3(code4);
  }
}
function tokenizeBlockQuoteContinuation(effects, ok3, nok) {
  const self2 = this;
  return contStart;
  function contStart(code4) {
    if (markdownSpace(code4)) {
      ok(
        self2.parser.constructs.disable.null,
        "expected `disable.null` to be populated"
      );
      return factorySpace(
        effects,
        contBefore,
        types.linePrefix,
        self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : constants.tabSize
      )(code4);
    }
    return contBefore(code4);
  }
  function contBefore(code4) {
    return effects.attempt(blockQuote, ok3, nok)(code4);
  }
}
function exit(effects) {
  effects.exit(types.blockQuote);
}

// node_modules/micromark-core-commonmark/dev/lib/character-escape.js
var characterEscape = {
  name: "characterEscape",
  tokenize: tokenizeCharacterEscape
};
function tokenizeCharacterEscape(effects, ok3, nok) {
  return start2;
  function start2(code4) {
    ok(code4 === codes.backslash, "expected `\\`");
    effects.enter(types.characterEscape);
    effects.enter(types.escapeMarker);
    effects.consume(code4);
    effects.exit(types.escapeMarker);
    return inside;
  }
  function inside(code4) {
    if (asciiPunctuation(code4)) {
      effects.enter(types.characterEscapeValue);
      effects.consume(code4);
      effects.exit(types.characterEscapeValue);
      effects.exit(types.characterEscape);
      return ok3;
    }
    return nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/character-reference.js
var characterReference = {
  name: "characterReference",
  tokenize: tokenizeCharacterReference
};
function tokenizeCharacterReference(effects, ok3, nok) {
  const self2 = this;
  let size4 = 0;
  let max2;
  let test2;
  return start2;
  function start2(code4) {
    ok(code4 === codes.ampersand, "expected `&`");
    effects.enter(types.characterReference);
    effects.enter(types.characterReferenceMarker);
    effects.consume(code4);
    effects.exit(types.characterReferenceMarker);
    return open;
  }
  function open(code4) {
    if (code4 === codes.numberSign) {
      effects.enter(types.characterReferenceMarkerNumeric);
      effects.consume(code4);
      effects.exit(types.characterReferenceMarkerNumeric);
      return numeric;
    }
    effects.enter(types.characterReferenceValue);
    max2 = constants.characterReferenceNamedSizeMax;
    test2 = asciiAlphanumeric;
    return value(code4);
  }
  function numeric(code4) {
    if (code4 === codes.uppercaseX || code4 === codes.lowercaseX) {
      effects.enter(types.characterReferenceMarkerHexadecimal);
      effects.consume(code4);
      effects.exit(types.characterReferenceMarkerHexadecimal);
      effects.enter(types.characterReferenceValue);
      max2 = constants.characterReferenceHexadecimalSizeMax;
      test2 = asciiHexDigit;
      return value;
    }
    effects.enter(types.characterReferenceValue);
    max2 = constants.characterReferenceDecimalSizeMax;
    test2 = asciiDigit;
    return value(code4);
  }
  function value(code4) {
    if (code4 === codes.semicolon && size4) {
      const token = effects.exit(types.characterReferenceValue);
      if (test2 === asciiAlphanumeric && !decodeNamedCharacterReference(self2.sliceSerialize(token))) {
        return nok(code4);
      }
      effects.enter(types.characterReferenceMarker);
      effects.consume(code4);
      effects.exit(types.characterReferenceMarker);
      effects.exit(types.characterReference);
      return ok3;
    }
    if (test2(code4) && size4++ < max2) {
      effects.consume(code4);
      return value;
    }
    return nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/code-fenced.js
var nonLazyContinuation = {
  partial: true,
  tokenize: tokenizeNonLazyContinuation
};
var codeFenced = {
  concrete: true,
  name: "codeFenced",
  tokenize: tokenizeCodeFenced
};
function tokenizeCodeFenced(effects, ok3, nok) {
  const self2 = this;
  const closeStart = { partial: true, tokenize: tokenizeCloseStart };
  let initialPrefix = 0;
  let sizeOpen = 0;
  let marker;
  return start2;
  function start2(code4) {
    return beforeSequenceOpen(code4);
  }
  function beforeSequenceOpen(code4) {
    ok(
      code4 === codes.graveAccent || code4 === codes.tilde,
      "expected `` ` `` or `~`"
    );
    const tail = self2.events[self2.events.length - 1];
    initialPrefix = tail && tail[1].type === types.linePrefix ? tail[2].sliceSerialize(tail[1], true).length : 0;
    marker = code4;
    effects.enter(types.codeFenced);
    effects.enter(types.codeFencedFence);
    effects.enter(types.codeFencedFenceSequence);
    return sequenceOpen(code4);
  }
  function sequenceOpen(code4) {
    if (code4 === marker) {
      sizeOpen++;
      effects.consume(code4);
      return sequenceOpen;
    }
    if (sizeOpen < constants.codeFencedSequenceSizeMin) {
      return nok(code4);
    }
    effects.exit(types.codeFencedFenceSequence);
    return markdownSpace(code4) ? factorySpace(effects, infoBefore, types.whitespace)(code4) : infoBefore(code4);
  }
  function infoBefore(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.codeFencedFence);
      return self2.interrupt ? ok3(code4) : effects.check(nonLazyContinuation, atNonLazyBreak, after)(code4);
    }
    effects.enter(types.codeFencedFenceInfo);
    effects.enter(types.chunkString, { contentType: constants.contentTypeString });
    return info(code4);
  }
  function info(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.chunkString);
      effects.exit(types.codeFencedFenceInfo);
      return infoBefore(code4);
    }
    if (markdownSpace(code4)) {
      effects.exit(types.chunkString);
      effects.exit(types.codeFencedFenceInfo);
      return factorySpace(effects, metaBefore, types.whitespace)(code4);
    }
    if (code4 === codes.graveAccent && code4 === marker) {
      return nok(code4);
    }
    effects.consume(code4);
    return info;
  }
  function metaBefore(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      return infoBefore(code4);
    }
    effects.enter(types.codeFencedFenceMeta);
    effects.enter(types.chunkString, { contentType: constants.contentTypeString });
    return meta(code4);
  }
  function meta(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.chunkString);
      effects.exit(types.codeFencedFenceMeta);
      return infoBefore(code4);
    }
    if (code4 === codes.graveAccent && code4 === marker) {
      return nok(code4);
    }
    effects.consume(code4);
    return meta;
  }
  function atNonLazyBreak(code4) {
    ok(markdownLineEnding(code4), "expected eol");
    return effects.attempt(closeStart, after, contentBefore)(code4);
  }
  function contentBefore(code4) {
    ok(markdownLineEnding(code4), "expected eol");
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return contentStart;
  }
  function contentStart(code4) {
    return initialPrefix > 0 && markdownSpace(code4) ? factorySpace(
      effects,
      beforeContentChunk,
      types.linePrefix,
      initialPrefix + 1
    )(code4) : beforeContentChunk(code4);
  }
  function beforeContentChunk(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      return effects.check(nonLazyContinuation, atNonLazyBreak, after)(code4);
    }
    effects.enter(types.codeFlowValue);
    return contentChunk(code4);
  }
  function contentChunk(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.codeFlowValue);
      return beforeContentChunk(code4);
    }
    effects.consume(code4);
    return contentChunk;
  }
  function after(code4) {
    effects.exit(types.codeFenced);
    return ok3(code4);
  }
  function tokenizeCloseStart(effects2, ok4, nok2) {
    let size4 = 0;
    return startBefore;
    function startBefore(code4) {
      ok(markdownLineEnding(code4), "expected eol");
      effects2.enter(types.lineEnding);
      effects2.consume(code4);
      effects2.exit(types.lineEnding);
      return start3;
    }
    function start3(code4) {
      ok(
        self2.parser.constructs.disable.null,
        "expected `disable.null` to be populated"
      );
      effects2.enter(types.codeFencedFence);
      return markdownSpace(code4) ? factorySpace(
        effects2,
        beforeSequenceClose,
        types.linePrefix,
        self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : constants.tabSize
      )(code4) : beforeSequenceClose(code4);
    }
    function beforeSequenceClose(code4) {
      if (code4 === marker) {
        effects2.enter(types.codeFencedFenceSequence);
        return sequenceClose(code4);
      }
      return nok2(code4);
    }
    function sequenceClose(code4) {
      if (code4 === marker) {
        size4++;
        effects2.consume(code4);
        return sequenceClose;
      }
      if (size4 >= sizeOpen) {
        effects2.exit(types.codeFencedFenceSequence);
        return markdownSpace(code4) ? factorySpace(effects2, sequenceCloseAfter, types.whitespace)(code4) : sequenceCloseAfter(code4);
      }
      return nok2(code4);
    }
    function sequenceCloseAfter(code4) {
      if (code4 === codes.eof || markdownLineEnding(code4)) {
        effects2.exit(types.codeFencedFence);
        return ok4(code4);
      }
      return nok2(code4);
    }
  }
}
function tokenizeNonLazyContinuation(effects, ok3, nok) {
  const self2 = this;
  return start2;
  function start2(code4) {
    if (code4 === codes.eof) {
      return nok(code4);
    }
    ok(markdownLineEnding(code4), "expected eol");
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return lineStart;
  }
  function lineStart(code4) {
    return self2.parser.lazy[self2.now().line] ? nok(code4) : ok3(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/code-indented.js
var codeIndented = {
  name: "codeIndented",
  tokenize: tokenizeCodeIndented
};
var furtherStart = { partial: true, tokenize: tokenizeFurtherStart };
function tokenizeCodeIndented(effects, ok3, nok) {
  const self2 = this;
  return start2;
  function start2(code4) {
    ok(markdownSpace(code4));
    effects.enter(types.codeIndented);
    return factorySpace(
      effects,
      afterPrefix,
      types.linePrefix,
      constants.tabSize + 1
    )(code4);
  }
  function afterPrefix(code4) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === types.linePrefix && tail[2].sliceSerialize(tail[1], true).length >= constants.tabSize ? atBreak(code4) : nok(code4);
  }
  function atBreak(code4) {
    if (code4 === codes.eof) {
      return after(code4);
    }
    if (markdownLineEnding(code4)) {
      return effects.attempt(furtherStart, atBreak, after)(code4);
    }
    effects.enter(types.codeFlowValue);
    return inside(code4);
  }
  function inside(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.codeFlowValue);
      return atBreak(code4);
    }
    effects.consume(code4);
    return inside;
  }
  function after(code4) {
    effects.exit(types.codeIndented);
    return ok3(code4);
  }
}
function tokenizeFurtherStart(effects, ok3, nok) {
  const self2 = this;
  return furtherStart2;
  function furtherStart2(code4) {
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code4);
    }
    if (markdownLineEnding(code4)) {
      effects.enter(types.lineEnding);
      effects.consume(code4);
      effects.exit(types.lineEnding);
      return furtherStart2;
    }
    return factorySpace(
      effects,
      afterPrefix,
      types.linePrefix,
      constants.tabSize + 1
    )(code4);
  }
  function afterPrefix(code4) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === types.linePrefix && tail[2].sliceSerialize(tail[1], true).length >= constants.tabSize ? ok3(code4) : markdownLineEnding(code4) ? furtherStart2(code4) : nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/code-text.js
var codeText = {
  name: "codeText",
  previous,
  resolve: resolveCodeText,
  tokenize: tokenizeCodeText
};
function resolveCodeText(events) {
  let tailExitIndex = events.length - 4;
  let headEnterIndex = 3;
  let index6;
  let enter;
  if ((events[headEnterIndex][1].type === types.lineEnding || events[headEnterIndex][1].type === "space") && (events[tailExitIndex][1].type === types.lineEnding || events[tailExitIndex][1].type === "space")) {
    index6 = headEnterIndex;
    while (++index6 < tailExitIndex) {
      if (events[index6][1].type === types.codeTextData) {
        events[headEnterIndex][1].type = types.codeTextPadding;
        events[tailExitIndex][1].type = types.codeTextPadding;
        headEnterIndex += 2;
        tailExitIndex -= 2;
        break;
      }
    }
  }
  index6 = headEnterIndex - 1;
  tailExitIndex++;
  while (++index6 <= tailExitIndex) {
    if (enter === void 0) {
      if (index6 !== tailExitIndex && events[index6][1].type !== types.lineEnding) {
        enter = index6;
      }
    } else if (index6 === tailExitIndex || events[index6][1].type === types.lineEnding) {
      events[enter][1].type = types.codeTextData;
      if (index6 !== enter + 2) {
        events[enter][1].end = events[index6 - 1][1].end;
        events.splice(enter + 2, index6 - enter - 2);
        tailExitIndex -= index6 - enter - 2;
        index6 = enter + 2;
      }
      enter = void 0;
    }
  }
  return events;
}
function previous(code4) {
  return code4 !== codes.graveAccent || this.events[this.events.length - 1][1].type === types.characterEscape;
}
function tokenizeCodeText(effects, ok3, nok) {
  const self2 = this;
  let sizeOpen = 0;
  let size4;
  let token;
  return start2;
  function start2(code4) {
    ok(code4 === codes.graveAccent, "expected `` ` ``");
    ok(previous.call(self2, self2.previous), "expected correct previous");
    effects.enter(types.codeText);
    effects.enter(types.codeTextSequence);
    return sequenceOpen(code4);
  }
  function sequenceOpen(code4) {
    if (code4 === codes.graveAccent) {
      effects.consume(code4);
      sizeOpen++;
      return sequenceOpen;
    }
    effects.exit(types.codeTextSequence);
    return between(code4);
  }
  function between(code4) {
    if (code4 === codes.eof) {
      return nok(code4);
    }
    if (code4 === codes.space) {
      effects.enter("space");
      effects.consume(code4);
      effects.exit("space");
      return between;
    }
    if (code4 === codes.graveAccent) {
      token = effects.enter(types.codeTextSequence);
      size4 = 0;
      return sequenceClose(code4);
    }
    if (markdownLineEnding(code4)) {
      effects.enter(types.lineEnding);
      effects.consume(code4);
      effects.exit(types.lineEnding);
      return between;
    }
    effects.enter(types.codeTextData);
    return data(code4);
  }
  function data(code4) {
    if (code4 === codes.eof || code4 === codes.space || code4 === codes.graveAccent || markdownLineEnding(code4)) {
      effects.exit(types.codeTextData);
      return between(code4);
    }
    effects.consume(code4);
    return data;
  }
  function sequenceClose(code4) {
    if (code4 === codes.graveAccent) {
      effects.consume(code4);
      size4++;
      return sequenceClose;
    }
    if (size4 === sizeOpen) {
      effects.exit(types.codeTextSequence);
      effects.exit(types.codeText);
      return ok3(code4);
    }
    token.type = types.codeTextData;
    return data(code4);
  }
}

// node_modules/micromark-util-subtokenize/dev/lib/splice-buffer.js
var SpliceBuffer = class {
  /**
   * @param {ReadonlyArray<T> | null | undefined} [initial]
   *   Initial items (optional).
   * @returns
   *   Splice buffer.
   */
  constructor(initial) {
    this.left = initial ? [...initial] : [];
    this.right = [];
  }
  /**
   * Array access;
   * does not move the cursor.
   *
   * @param {number} index
   *   Index.
   * @return {T}
   *   Item.
   */
  get(index6) {
    if (index6 < 0 || index6 >= this.left.length + this.right.length) {
      throw new RangeError(
        "Cannot access index `" + index6 + "` in a splice buffer of size `" + (this.left.length + this.right.length) + "`"
      );
    }
    if (index6 < this.left.length) return this.left[index6];
    return this.right[this.right.length - index6 + this.left.length - 1];
  }
  /**
   * The length of the splice buffer, one greater than the largest index in the
   * array.
   */
  get length() {
    return this.left.length + this.right.length;
  }
  /**
   * Remove and return `list[0]`;
   * moves the cursor to `0`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  shift() {
    this.setCursor(0);
    return this.right.pop();
  }
  /**
   * Slice the buffer to get an array;
   * does not move the cursor.
   *
   * @param {number} start
   *   Start.
   * @param {number | null | undefined} [end]
   *   End (optional).
   * @returns {Array<T>}
   *   Array of items.
   */
  slice(start2, end) {
    const stop = end === null || end === void 0 ? Number.POSITIVE_INFINITY : end;
    if (stop < this.left.length) {
      return this.left.slice(start2, stop);
    }
    if (start2 > this.left.length) {
      return this.right.slice(
        this.right.length - stop + this.left.length,
        this.right.length - start2 + this.left.length
      ).reverse();
    }
    return this.left.slice(start2).concat(
      this.right.slice(this.right.length - stop + this.left.length).reverse()
    );
  }
  /**
   * Mimics the behavior of Array.prototype.splice() except for the change of
   * interface necessary to avoid segfaults when patching in very large arrays.
   *
   * This operation moves cursor is moved to `start` and results in the cursor
   * placed after any inserted items.
   *
   * @param {number} start
   *   Start;
   *   zero-based index at which to start changing the array;
   *   negative numbers count backwards from the end of the array and values
   *   that are out-of bounds are clamped to the appropriate end of the array.
   * @param {number | null | undefined} [deleteCount=0]
   *   Delete count (default: `0`);
   *   maximum number of elements to delete, starting from start.
   * @param {Array<T> | null | undefined} [items=[]]
   *   Items to include in place of the deleted items (default: `[]`).
   * @return {Array<T>}
   *   Any removed items.
   */
  splice(start2, deleteCount, items) {
    const count2 = deleteCount || 0;
    this.setCursor(Math.trunc(start2));
    const removed = this.right.splice(
      this.right.length - count2,
      Number.POSITIVE_INFINITY
    );
    if (items) chunkedPush(this.left, items);
    return removed.reverse();
  }
  /**
   * Remove and return the highest-numbered item in the array, so
   * `list[list.length - 1]`;
   * Moves the cursor to `length`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  pop() {
    this.setCursor(Number.POSITIVE_INFINITY);
    return this.left.pop();
  }
  /**
   * Inserts a single item to the high-numbered side of the array;
   * moves the cursor to `length`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  push(item) {
    this.setCursor(Number.POSITIVE_INFINITY);
    this.left.push(item);
  }
  /**
   * Inserts many items to the high-numbered side of the array.
   * Moves the cursor to `length`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  pushMany(items) {
    this.setCursor(Number.POSITIVE_INFINITY);
    chunkedPush(this.left, items);
  }
  /**
   * Inserts a single item to the low-numbered side of the array;
   * Moves the cursor to `0`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  unshift(item) {
    this.setCursor(0);
    this.right.push(item);
  }
  /**
   * Inserts many items to the low-numbered side of the array;
   * moves the cursor to `0`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  unshiftMany(items) {
    this.setCursor(0);
    chunkedPush(this.right, items.reverse());
  }
  /**
   * Move the cursor to a specific position in the array. Requires
   * time proportional to the distance moved.
   *
   * If `n < 0`, the cursor will end up at the beginning.
   * If `n > length`, the cursor will end up at the end.
   *
   * @param {number} n
   *   Position.
   * @return {undefined}
   *   Nothing.
   */
  setCursor(n2) {
    if (n2 === this.left.length || n2 > this.left.length && this.right.length === 0 || n2 < 0 && this.left.length === 0)
      return;
    if (n2 < this.left.length) {
      const removed = this.left.splice(n2, Number.POSITIVE_INFINITY);
      chunkedPush(this.right, removed.reverse());
    } else {
      const removed = this.right.splice(
        this.left.length + this.right.length - n2,
        Number.POSITIVE_INFINITY
      );
      chunkedPush(this.left, removed.reverse());
    }
  }
};
function chunkedPush(list4, right) {
  let chunkStart = 0;
  if (right.length < constants.v8MaxSafeChunkSize) {
    list4.push(...right);
  } else {
    while (chunkStart < right.length) {
      list4.push(
        ...right.slice(chunkStart, chunkStart + constants.v8MaxSafeChunkSize)
      );
      chunkStart += constants.v8MaxSafeChunkSize;
    }
  }
}

// node_modules/micromark-util-subtokenize/dev/index.js
function subtokenize(eventsArray) {
  const jumps = {};
  let index6 = -1;
  let event;
  let lineIndex;
  let otherIndex;
  let otherEvent;
  let parameters;
  let subevents;
  let more;
  const events = new SpliceBuffer(eventsArray);
  while (++index6 < events.length) {
    while (index6 in jumps) {
      index6 = jumps[index6];
    }
    event = events.get(index6);
    if (index6 && event[1].type === types.chunkFlow && events.get(index6 - 1)[1].type === types.listItemPrefix) {
      ok(event[1]._tokenizer, "expected `_tokenizer` on subtokens");
      subevents = event[1]._tokenizer.events;
      otherIndex = 0;
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === types.lineEndingBlank) {
        otherIndex += 2;
      }
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === types.content) {
        while (++otherIndex < subevents.length) {
          if (subevents[otherIndex][1].type === types.content) {
            break;
          }
          if (subevents[otherIndex][1].type === types.chunkText) {
            subevents[otherIndex][1]._isInFirstContentOfListItem = true;
            otherIndex++;
          }
        }
      }
    }
    if (event[0] === "enter") {
      if (event[1].contentType) {
        Object.assign(jumps, subcontent(events, index6));
        index6 = jumps[index6];
        more = true;
      }
    } else if (event[1]._container) {
      otherIndex = index6;
      lineIndex = void 0;
      while (otherIndex--) {
        otherEvent = events.get(otherIndex);
        if (otherEvent[1].type === types.lineEnding || otherEvent[1].type === types.lineEndingBlank) {
          if (otherEvent[0] === "enter") {
            if (lineIndex) {
              events.get(lineIndex)[1].type = types.lineEndingBlank;
            }
            otherEvent[1].type = types.lineEnding;
            lineIndex = otherIndex;
          }
        } else if (otherEvent[1].type === types.linePrefix || otherEvent[1].type === types.listItemIndent) {
        } else {
          break;
        }
      }
      if (lineIndex) {
        event[1].end = { ...events.get(lineIndex)[1].start };
        parameters = events.slice(lineIndex, index6);
        parameters.unshift(event);
        events.splice(lineIndex, index6 - lineIndex + 1, parameters);
      }
    }
  }
  splice(eventsArray, 0, Number.POSITIVE_INFINITY, events.slice(0));
  return !more;
}
function subcontent(events, eventIndex) {
  const token = events.get(eventIndex)[1];
  const context = events.get(eventIndex)[2];
  let startPosition = eventIndex - 1;
  const startPositions = [];
  ok(token.contentType, "expected `contentType` on subtokens");
  let tokenizer = token._tokenizer;
  if (!tokenizer) {
    tokenizer = context.parser[token.contentType](token.start);
    if (token._contentTypeTextTrailing) {
      tokenizer._contentTypeTextTrailing = true;
    }
  }
  const childEvents = tokenizer.events;
  const jumps = [];
  const gaps = {};
  let stream2;
  let previous3;
  let index6 = -1;
  let current = token;
  let adjust = 0;
  let start2 = 0;
  const breaks = [start2];
  while (current) {
    while (events.get(++startPosition)[1] !== current) {
    }
    ok(
      !previous3 || current.previous === previous3,
      "expected previous to match"
    );
    ok(!previous3 || previous3.next === current, "expected next to match");
    startPositions.push(startPosition);
    if (!current._tokenizer) {
      stream2 = context.sliceStream(current);
      if (!current.next) {
        stream2.push(codes.eof);
      }
      if (previous3) {
        tokenizer.defineSkip(current.start);
      }
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = true;
      }
      tokenizer.write(stream2);
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = void 0;
      }
    }
    previous3 = current;
    current = current.next;
  }
  current = token;
  while (++index6 < childEvents.length) {
    if (
      // Find a void token that includes a break.
      childEvents[index6][0] === "exit" && childEvents[index6 - 1][0] === "enter" && childEvents[index6][1].type === childEvents[index6 - 1][1].type && childEvents[index6][1].start.line !== childEvents[index6][1].end.line
    ) {
      ok(current, "expected a current token");
      start2 = index6 + 1;
      breaks.push(start2);
      current._tokenizer = void 0;
      current.previous = void 0;
      current = current.next;
    }
  }
  tokenizer.events = [];
  if (current) {
    current._tokenizer = void 0;
    current.previous = void 0;
    ok(!current.next, "expected no next token");
  } else {
    breaks.pop();
  }
  index6 = breaks.length;
  while (index6--) {
    const slice = childEvents.slice(breaks[index6], breaks[index6 + 1]);
    const start3 = startPositions.pop();
    ok(start3 !== void 0, "expected a start position when splicing");
    jumps.push([start3, start3 + slice.length - 1]);
    events.splice(start3, 2, slice);
  }
  jumps.reverse();
  index6 = -1;
  while (++index6 < jumps.length) {
    gaps[adjust + jumps[index6][0]] = adjust + jumps[index6][1];
    adjust += jumps[index6][1] - jumps[index6][0] - 1;
  }
  return gaps;
}

// node_modules/micromark-core-commonmark/dev/lib/content.js
var content2 = { resolve: resolveContent, tokenize: tokenizeContent };
var continuationConstruct = { partial: true, tokenize: tokenizeContinuation };
function resolveContent(events) {
  subtokenize(events);
  return events;
}
function tokenizeContent(effects, ok3) {
  let previous3;
  return chunkStart;
  function chunkStart(code4) {
    ok(
      code4 !== codes.eof && !markdownLineEnding(code4),
      "expected no eof or eol"
    );
    effects.enter(types.content);
    previous3 = effects.enter(types.chunkContent, {
      contentType: constants.contentTypeContent
    });
    return chunkInside(code4);
  }
  function chunkInside(code4) {
    if (code4 === codes.eof) {
      return contentEnd(code4);
    }
    if (markdownLineEnding(code4)) {
      return effects.check(
        continuationConstruct,
        contentContinue,
        contentEnd
      )(code4);
    }
    effects.consume(code4);
    return chunkInside;
  }
  function contentEnd(code4) {
    effects.exit(types.chunkContent);
    effects.exit(types.content);
    return ok3(code4);
  }
  function contentContinue(code4) {
    ok(markdownLineEnding(code4), "expected eol");
    effects.consume(code4);
    effects.exit(types.chunkContent);
    ok(previous3, "expected previous token");
    previous3.next = effects.enter(types.chunkContent, {
      contentType: constants.contentTypeContent,
      previous: previous3
    });
    previous3 = previous3.next;
    return chunkInside;
  }
}
function tokenizeContinuation(effects, ok3, nok) {
  const self2 = this;
  return startLookahead;
  function startLookahead(code4) {
    ok(markdownLineEnding(code4), "expected a line ending");
    effects.exit(types.chunkContent);
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return factorySpace(effects, prefixed, types.linePrefix);
  }
  function prefixed(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      return nok(code4);
    }
    ok(
      self2.parser.constructs.disable.null,
      "expected `disable.null` to be populated"
    );
    const tail = self2.events[self2.events.length - 1];
    if (!self2.parser.constructs.disable.null.includes("codeIndented") && tail && tail[1].type === types.linePrefix && tail[2].sliceSerialize(tail[1], true).length >= constants.tabSize) {
      return ok3(code4);
    }
    return effects.interrupt(self2.parser.constructs.flow, nok, ok3)(code4);
  }
}

// node_modules/micromark-factory-destination/dev/index.js
function factoryDestination(effects, ok3, nok, type, literalType, literalMarkerType, rawType, stringType, max2) {
  const limit = max2 || Number.POSITIVE_INFINITY;
  let balance = 0;
  return start2;
  function start2(code4) {
    if (code4 === codes.lessThan) {
      effects.enter(type);
      effects.enter(literalType);
      effects.enter(literalMarkerType);
      effects.consume(code4);
      effects.exit(literalMarkerType);
      return enclosedBefore;
    }
    if (code4 === codes.eof || code4 === codes.space || code4 === codes.rightParenthesis || asciiControl(code4)) {
      return nok(code4);
    }
    effects.enter(type);
    effects.enter(rawType);
    effects.enter(stringType);
    effects.enter(types.chunkString, { contentType: constants.contentTypeString });
    return raw(code4);
  }
  function enclosedBefore(code4) {
    if (code4 === codes.greaterThan) {
      effects.enter(literalMarkerType);
      effects.consume(code4);
      effects.exit(literalMarkerType);
      effects.exit(literalType);
      effects.exit(type);
      return ok3;
    }
    effects.enter(stringType);
    effects.enter(types.chunkString, { contentType: constants.contentTypeString });
    return enclosed(code4);
  }
  function enclosed(code4) {
    if (code4 === codes.greaterThan) {
      effects.exit(types.chunkString);
      effects.exit(stringType);
      return enclosedBefore(code4);
    }
    if (code4 === codes.eof || code4 === codes.lessThan || markdownLineEnding(code4)) {
      return nok(code4);
    }
    effects.consume(code4);
    return code4 === codes.backslash ? enclosedEscape : enclosed;
  }
  function enclosedEscape(code4) {
    if (code4 === codes.lessThan || code4 === codes.greaterThan || code4 === codes.backslash) {
      effects.consume(code4);
      return enclosed;
    }
    return enclosed(code4);
  }
  function raw(code4) {
    if (!balance && (code4 === codes.eof || code4 === codes.rightParenthesis || markdownLineEndingOrSpace(code4))) {
      effects.exit(types.chunkString);
      effects.exit(stringType);
      effects.exit(rawType);
      effects.exit(type);
      return ok3(code4);
    }
    if (balance < limit && code4 === codes.leftParenthesis) {
      effects.consume(code4);
      balance++;
      return raw;
    }
    if (code4 === codes.rightParenthesis) {
      effects.consume(code4);
      balance--;
      return raw;
    }
    if (code4 === codes.eof || code4 === codes.space || code4 === codes.leftParenthesis || asciiControl(code4)) {
      return nok(code4);
    }
    effects.consume(code4);
    return code4 === codes.backslash ? rawEscape : raw;
  }
  function rawEscape(code4) {
    if (code4 === codes.leftParenthesis || code4 === codes.rightParenthesis || code4 === codes.backslash) {
      effects.consume(code4);
      return raw;
    }
    return raw(code4);
  }
}

// node_modules/micromark-factory-label/dev/index.js
function factoryLabel(effects, ok3, nok, type, markerType, stringType) {
  const self2 = this;
  let size4 = 0;
  let seen;
  return start2;
  function start2(code4) {
    ok(code4 === codes.leftSquareBracket, "expected `[`");
    effects.enter(type);
    effects.enter(markerType);
    effects.consume(code4);
    effects.exit(markerType);
    effects.enter(stringType);
    return atBreak;
  }
  function atBreak(code4) {
    if (size4 > constants.linkReferenceSizeMax || code4 === codes.eof || code4 === codes.leftSquareBracket || code4 === codes.rightSquareBracket && !seen || // To do: remove in the future once we’ve switched from
    // `micromark-extension-footnote` to `micromark-extension-gfm-footnote`,
    // which doesn’t need this.
    // Hidden footnotes hook.
    /* c8 ignore next 3 */
    code4 === codes.caret && !size4 && "_hiddenFootnoteSupport" in self2.parser.constructs) {
      return nok(code4);
    }
    if (code4 === codes.rightSquareBracket) {
      effects.exit(stringType);
      effects.enter(markerType);
      effects.consume(code4);
      effects.exit(markerType);
      effects.exit(type);
      return ok3;
    }
    if (markdownLineEnding(code4)) {
      effects.enter(types.lineEnding);
      effects.consume(code4);
      effects.exit(types.lineEnding);
      return atBreak;
    }
    effects.enter(types.chunkString, { contentType: constants.contentTypeString });
    return labelInside(code4);
  }
  function labelInside(code4) {
    if (code4 === codes.eof || code4 === codes.leftSquareBracket || code4 === codes.rightSquareBracket || markdownLineEnding(code4) || size4++ > constants.linkReferenceSizeMax) {
      effects.exit(types.chunkString);
      return atBreak(code4);
    }
    effects.consume(code4);
    if (!seen) seen = !markdownSpace(code4);
    return code4 === codes.backslash ? labelEscape : labelInside;
  }
  function labelEscape(code4) {
    if (code4 === codes.leftSquareBracket || code4 === codes.backslash || code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      size4++;
      return labelInside;
    }
    return labelInside(code4);
  }
}

// node_modules/micromark-factory-title/dev/index.js
function factoryTitle(effects, ok3, nok, type, markerType, stringType) {
  let marker;
  return start2;
  function start2(code4) {
    if (code4 === codes.quotationMark || code4 === codes.apostrophe || code4 === codes.leftParenthesis) {
      effects.enter(type);
      effects.enter(markerType);
      effects.consume(code4);
      effects.exit(markerType);
      marker = code4 === codes.leftParenthesis ? codes.rightParenthesis : code4;
      return begin;
    }
    return nok(code4);
  }
  function begin(code4) {
    if (code4 === marker) {
      effects.enter(markerType);
      effects.consume(code4);
      effects.exit(markerType);
      effects.exit(type);
      return ok3;
    }
    effects.enter(stringType);
    return atBreak(code4);
  }
  function atBreak(code4) {
    if (code4 === marker) {
      effects.exit(stringType);
      return begin(marker);
    }
    if (code4 === codes.eof) {
      return nok(code4);
    }
    if (markdownLineEnding(code4)) {
      effects.enter(types.lineEnding);
      effects.consume(code4);
      effects.exit(types.lineEnding);
      return factorySpace(effects, atBreak, types.linePrefix);
    }
    effects.enter(types.chunkString, { contentType: constants.contentTypeString });
    return inside(code4);
  }
  function inside(code4) {
    if (code4 === marker || code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.chunkString);
      return atBreak(code4);
    }
    effects.consume(code4);
    return code4 === codes.backslash ? escape2 : inside;
  }
  function escape2(code4) {
    if (code4 === marker || code4 === codes.backslash) {
      effects.consume(code4);
      return inside;
    }
    return inside(code4);
  }
}

// node_modules/micromark-factory-whitespace/dev/index.js
function factoryWhitespace(effects, ok3) {
  let seen;
  return start2;
  function start2(code4) {
    if (markdownLineEnding(code4)) {
      effects.enter(types.lineEnding);
      effects.consume(code4);
      effects.exit(types.lineEnding);
      seen = true;
      return start2;
    }
    if (markdownSpace(code4)) {
      return factorySpace(
        effects,
        start2,
        seen ? types.linePrefix : types.lineSuffix
      )(code4);
    }
    return ok3(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/definition.js
var definition = { name: "definition", tokenize: tokenizeDefinition };
var titleBefore = { partial: true, tokenize: tokenizeTitleBefore };
function tokenizeDefinition(effects, ok3, nok) {
  const self2 = this;
  let identifier;
  return start2;
  function start2(code4) {
    effects.enter(types.definition);
    return before(code4);
  }
  function before(code4) {
    ok(code4 === codes.leftSquareBracket, "expected `[`");
    return factoryLabel.call(
      self2,
      effects,
      labelAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      types.definitionLabel,
      types.definitionLabelMarker,
      types.definitionLabelString
    )(code4);
  }
  function labelAfter(code4) {
    identifier = normalizeIdentifier(
      self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1)
    );
    if (code4 === codes.colon) {
      effects.enter(types.definitionMarker);
      effects.consume(code4);
      effects.exit(types.definitionMarker);
      return markerAfter;
    }
    return nok(code4);
  }
  function markerAfter(code4) {
    return markdownLineEndingOrSpace(code4) ? factoryWhitespace(effects, destinationBefore)(code4) : destinationBefore(code4);
  }
  function destinationBefore(code4) {
    return factoryDestination(
      effects,
      destinationAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      types.definitionDestination,
      types.definitionDestinationLiteral,
      types.definitionDestinationLiteralMarker,
      types.definitionDestinationRaw,
      types.definitionDestinationString
    )(code4);
  }
  function destinationAfter(code4) {
    return effects.attempt(titleBefore, after, after)(code4);
  }
  function after(code4) {
    return markdownSpace(code4) ? factorySpace(effects, afterWhitespace, types.whitespace)(code4) : afterWhitespace(code4);
  }
  function afterWhitespace(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.definition);
      self2.parser.defined.push(identifier);
      return ok3(code4);
    }
    return nok(code4);
  }
}
function tokenizeTitleBefore(effects, ok3, nok) {
  return titleBefore2;
  function titleBefore2(code4) {
    return markdownLineEndingOrSpace(code4) ? factoryWhitespace(effects, beforeMarker)(code4) : nok(code4);
  }
  function beforeMarker(code4) {
    return factoryTitle(
      effects,
      titleAfter,
      nok,
      types.definitionTitle,
      types.definitionTitleMarker,
      types.definitionTitleString
    )(code4);
  }
  function titleAfter(code4) {
    return markdownSpace(code4) ? factorySpace(
      effects,
      titleAfterOptionalWhitespace,
      types.whitespace
    )(code4) : titleAfterOptionalWhitespace(code4);
  }
  function titleAfterOptionalWhitespace(code4) {
    return code4 === codes.eof || markdownLineEnding(code4) ? ok3(code4) : nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/hard-break-escape.js
var hardBreakEscape = {
  name: "hardBreakEscape",
  tokenize: tokenizeHardBreakEscape
};
function tokenizeHardBreakEscape(effects, ok3, nok) {
  return start2;
  function start2(code4) {
    ok(code4 === codes.backslash, "expected `\\`");
    effects.enter(types.hardBreakEscape);
    effects.consume(code4);
    return after;
  }
  function after(code4) {
    if (markdownLineEnding(code4)) {
      effects.exit(types.hardBreakEscape);
      return ok3(code4);
    }
    return nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/heading-atx.js
var headingAtx = {
  name: "headingAtx",
  resolve: resolveHeadingAtx,
  tokenize: tokenizeHeadingAtx
};
function resolveHeadingAtx(events, context) {
  let contentEnd = events.length - 2;
  let contentStart = 3;
  let content3;
  let text8;
  if (events[contentStart][1].type === types.whitespace) {
    contentStart += 2;
  }
  if (contentEnd - 2 > contentStart && events[contentEnd][1].type === types.whitespace) {
    contentEnd -= 2;
  }
  if (events[contentEnd][1].type === types.atxHeadingSequence && (contentStart === contentEnd - 1 || contentEnd - 4 > contentStart && events[contentEnd - 2][1].type === types.whitespace)) {
    contentEnd -= contentStart + 1 === contentEnd ? 2 : 4;
  }
  if (contentEnd > contentStart) {
    content3 = {
      type: types.atxHeadingText,
      start: events[contentStart][1].start,
      end: events[contentEnd][1].end
    };
    text8 = {
      type: types.chunkText,
      start: events[contentStart][1].start,
      end: events[contentEnd][1].end,
      contentType: constants.contentTypeText
    };
    splice(events, contentStart, contentEnd - contentStart + 1, [
      ["enter", content3, context],
      ["enter", text8, context],
      ["exit", text8, context],
      ["exit", content3, context]
    ]);
  }
  return events;
}
function tokenizeHeadingAtx(effects, ok3, nok) {
  let size4 = 0;
  return start2;
  function start2(code4) {
    effects.enter(types.atxHeading);
    return before(code4);
  }
  function before(code4) {
    ok(code4 === codes.numberSign, "expected `#`");
    effects.enter(types.atxHeadingSequence);
    return sequenceOpen(code4);
  }
  function sequenceOpen(code4) {
    if (code4 === codes.numberSign && size4++ < constants.atxHeadingOpeningFenceSizeMax) {
      effects.consume(code4);
      return sequenceOpen;
    }
    if (code4 === codes.eof || markdownLineEndingOrSpace(code4)) {
      effects.exit(types.atxHeadingSequence);
      return atBreak(code4);
    }
    return nok(code4);
  }
  function atBreak(code4) {
    if (code4 === codes.numberSign) {
      effects.enter(types.atxHeadingSequence);
      return sequenceFurther(code4);
    }
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.atxHeading);
      return ok3(code4);
    }
    if (markdownSpace(code4)) {
      return factorySpace(effects, atBreak, types.whitespace)(code4);
    }
    effects.enter(types.atxHeadingText);
    return data(code4);
  }
  function sequenceFurther(code4) {
    if (code4 === codes.numberSign) {
      effects.consume(code4);
      return sequenceFurther;
    }
    effects.exit(types.atxHeadingSequence);
    return atBreak(code4);
  }
  function data(code4) {
    if (code4 === codes.eof || code4 === codes.numberSign || markdownLineEndingOrSpace(code4)) {
      effects.exit(types.atxHeadingText);
      return atBreak(code4);
    }
    effects.consume(code4);
    return data;
  }
}

// node_modules/micromark-util-html-tag-name/index.js
var htmlBlockNames = [
  "address",
  "article",
  "aside",
  "base",
  "basefont",
  "blockquote",
  "body",
  "caption",
  "center",
  "col",
  "colgroup",
  "dd",
  "details",
  "dialog",
  "dir",
  "div",
  "dl",
  "dt",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "frame",
  "frameset",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hr",
  "html",
  "iframe",
  "legend",
  "li",
  "link",
  "main",
  "menu",
  "menuitem",
  "nav",
  "noframes",
  "ol",
  "optgroup",
  "option",
  "p",
  "param",
  "search",
  "section",
  "summary",
  "table",
  "tbody",
  "td",
  "tfoot",
  "th",
  "thead",
  "title",
  "tr",
  "track",
  "ul"
];
var htmlRawNames = ["pre", "script", "style", "textarea"];

// node_modules/micromark-core-commonmark/dev/lib/html-flow.js
var htmlFlow = {
  concrete: true,
  name: "htmlFlow",
  resolveTo: resolveToHtmlFlow,
  tokenize: tokenizeHtmlFlow
};
var blankLineBefore = { partial: true, tokenize: tokenizeBlankLineBefore };
var nonLazyContinuationStart = {
  partial: true,
  tokenize: tokenizeNonLazyContinuationStart
};
function resolveToHtmlFlow(events) {
  let index6 = events.length;
  while (index6--) {
    if (events[index6][0] === "enter" && events[index6][1].type === types.htmlFlow) {
      break;
    }
  }
  if (index6 > 1 && events[index6 - 2][1].type === types.linePrefix) {
    events[index6][1].start = events[index6 - 2][1].start;
    events[index6 + 1][1].start = events[index6 - 2][1].start;
    events.splice(index6 - 2, 2);
  }
  return events;
}
function tokenizeHtmlFlow(effects, ok3, nok) {
  const self2 = this;
  let marker;
  let closingTag;
  let buffer;
  let index6;
  let markerB;
  return start2;
  function start2(code4) {
    return before(code4);
  }
  function before(code4) {
    ok(code4 === codes.lessThan, "expected `<`");
    effects.enter(types.htmlFlow);
    effects.enter(types.htmlFlowData);
    effects.consume(code4);
    return open;
  }
  function open(code4) {
    if (code4 === codes.exclamationMark) {
      effects.consume(code4);
      return declarationOpen;
    }
    if (code4 === codes.slash) {
      effects.consume(code4);
      closingTag = true;
      return tagCloseStart;
    }
    if (code4 === codes.questionMark) {
      effects.consume(code4);
      marker = constants.htmlInstruction;
      return self2.interrupt ? ok3 : continuationDeclarationInside;
    }
    if (asciiAlpha(code4)) {
      ok(code4 !== null);
      effects.consume(code4);
      buffer = String.fromCharCode(code4);
      return tagName;
    }
    return nok(code4);
  }
  function declarationOpen(code4) {
    if (code4 === codes.dash) {
      effects.consume(code4);
      marker = constants.htmlComment;
      return commentOpenInside;
    }
    if (code4 === codes.leftSquareBracket) {
      effects.consume(code4);
      marker = constants.htmlCdata;
      index6 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code4)) {
      effects.consume(code4);
      marker = constants.htmlDeclaration;
      return self2.interrupt ? ok3 : continuationDeclarationInside;
    }
    return nok(code4);
  }
  function commentOpenInside(code4) {
    if (code4 === codes.dash) {
      effects.consume(code4);
      return self2.interrupt ? ok3 : continuationDeclarationInside;
    }
    return nok(code4);
  }
  function cdataOpenInside(code4) {
    const value = constants.cdataOpeningString;
    if (code4 === value.charCodeAt(index6++)) {
      effects.consume(code4);
      if (index6 === value.length) {
        return self2.interrupt ? ok3 : continuation;
      }
      return cdataOpenInside;
    }
    return nok(code4);
  }
  function tagCloseStart(code4) {
    if (asciiAlpha(code4)) {
      ok(code4 !== null);
      effects.consume(code4);
      buffer = String.fromCharCode(code4);
      return tagName;
    }
    return nok(code4);
  }
  function tagName(code4) {
    if (code4 === codes.eof || code4 === codes.slash || code4 === codes.greaterThan || markdownLineEndingOrSpace(code4)) {
      const slash = code4 === codes.slash;
      const name2 = buffer.toLowerCase();
      if (!slash && !closingTag && htmlRawNames.includes(name2)) {
        marker = constants.htmlRaw;
        return self2.interrupt ? ok3(code4) : continuation(code4);
      }
      if (htmlBlockNames.includes(buffer.toLowerCase())) {
        marker = constants.htmlBasic;
        if (slash) {
          effects.consume(code4);
          return basicSelfClosing;
        }
        return self2.interrupt ? ok3(code4) : continuation(code4);
      }
      marker = constants.htmlComplete;
      return self2.interrupt && !self2.parser.lazy[self2.now().line] ? nok(code4) : closingTag ? completeClosingTagAfter(code4) : completeAttributeNameBefore(code4);
    }
    if (code4 === codes.dash || asciiAlphanumeric(code4)) {
      effects.consume(code4);
      buffer += String.fromCharCode(code4);
      return tagName;
    }
    return nok(code4);
  }
  function basicSelfClosing(code4) {
    if (code4 === codes.greaterThan) {
      effects.consume(code4);
      return self2.interrupt ? ok3 : continuation;
    }
    return nok(code4);
  }
  function completeClosingTagAfter(code4) {
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return completeClosingTagAfter;
    }
    return completeEnd(code4);
  }
  function completeAttributeNameBefore(code4) {
    if (code4 === codes.slash) {
      effects.consume(code4);
      return completeEnd;
    }
    if (code4 === codes.colon || code4 === codes.underscore || asciiAlpha(code4)) {
      effects.consume(code4);
      return completeAttributeName;
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return completeAttributeNameBefore;
    }
    return completeEnd(code4);
  }
  function completeAttributeName(code4) {
    if (code4 === codes.dash || code4 === codes.dot || code4 === codes.colon || code4 === codes.underscore || asciiAlphanumeric(code4)) {
      effects.consume(code4);
      return completeAttributeName;
    }
    return completeAttributeNameAfter(code4);
  }
  function completeAttributeNameAfter(code4) {
    if (code4 === codes.equalsTo) {
      effects.consume(code4);
      return completeAttributeValueBefore;
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return completeAttributeNameAfter;
    }
    return completeAttributeNameBefore(code4);
  }
  function completeAttributeValueBefore(code4) {
    if (code4 === codes.eof || code4 === codes.lessThan || code4 === codes.equalsTo || code4 === codes.greaterThan || code4 === codes.graveAccent) {
      return nok(code4);
    }
    if (code4 === codes.quotationMark || code4 === codes.apostrophe) {
      effects.consume(code4);
      markerB = code4;
      return completeAttributeValueQuoted;
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return completeAttributeValueBefore;
    }
    return completeAttributeValueUnquoted(code4);
  }
  function completeAttributeValueQuoted(code4) {
    if (code4 === markerB) {
      effects.consume(code4);
      markerB = null;
      return completeAttributeValueQuotedAfter;
    }
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      return nok(code4);
    }
    effects.consume(code4);
    return completeAttributeValueQuoted;
  }
  function completeAttributeValueUnquoted(code4) {
    if (code4 === codes.eof || code4 === codes.quotationMark || code4 === codes.apostrophe || code4 === codes.slash || code4 === codes.lessThan || code4 === codes.equalsTo || code4 === codes.greaterThan || code4 === codes.graveAccent || markdownLineEndingOrSpace(code4)) {
      return completeAttributeNameAfter(code4);
    }
    effects.consume(code4);
    return completeAttributeValueUnquoted;
  }
  function completeAttributeValueQuotedAfter(code4) {
    if (code4 === codes.slash || code4 === codes.greaterThan || markdownSpace(code4)) {
      return completeAttributeNameBefore(code4);
    }
    return nok(code4);
  }
  function completeEnd(code4) {
    if (code4 === codes.greaterThan) {
      effects.consume(code4);
      return completeAfter;
    }
    return nok(code4);
  }
  function completeAfter(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      return continuation(code4);
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return completeAfter;
    }
    return nok(code4);
  }
  function continuation(code4) {
    if (code4 === codes.dash && marker === constants.htmlComment) {
      effects.consume(code4);
      return continuationCommentInside;
    }
    if (code4 === codes.lessThan && marker === constants.htmlRaw) {
      effects.consume(code4);
      return continuationRawTagOpen;
    }
    if (code4 === codes.greaterThan && marker === constants.htmlDeclaration) {
      effects.consume(code4);
      return continuationClose;
    }
    if (code4 === codes.questionMark && marker === constants.htmlInstruction) {
      effects.consume(code4);
      return continuationDeclarationInside;
    }
    if (code4 === codes.rightSquareBracket && marker === constants.htmlCdata) {
      effects.consume(code4);
      return continuationCdataInside;
    }
    if (markdownLineEnding(code4) && (marker === constants.htmlBasic || marker === constants.htmlComplete)) {
      effects.exit(types.htmlFlowData);
      return effects.check(
        blankLineBefore,
        continuationAfter,
        continuationStart
      )(code4);
    }
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.htmlFlowData);
      return continuationStart(code4);
    }
    effects.consume(code4);
    return continuation;
  }
  function continuationStart(code4) {
    return effects.check(
      nonLazyContinuationStart,
      continuationStartNonLazy,
      continuationAfter
    )(code4);
  }
  function continuationStartNonLazy(code4) {
    ok(markdownLineEnding(code4));
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return continuationBefore;
  }
  function continuationBefore(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      return continuationStart(code4);
    }
    effects.enter(types.htmlFlowData);
    return continuation(code4);
  }
  function continuationCommentInside(code4) {
    if (code4 === codes.dash) {
      effects.consume(code4);
      return continuationDeclarationInside;
    }
    return continuation(code4);
  }
  function continuationRawTagOpen(code4) {
    if (code4 === codes.slash) {
      effects.consume(code4);
      buffer = "";
      return continuationRawEndTag;
    }
    return continuation(code4);
  }
  function continuationRawEndTag(code4) {
    if (code4 === codes.greaterThan) {
      const name2 = buffer.toLowerCase();
      if (htmlRawNames.includes(name2)) {
        effects.consume(code4);
        return continuationClose;
      }
      return continuation(code4);
    }
    if (asciiAlpha(code4) && buffer.length < constants.htmlRawSizeMax) {
      ok(code4 !== null);
      effects.consume(code4);
      buffer += String.fromCharCode(code4);
      return continuationRawEndTag;
    }
    return continuation(code4);
  }
  function continuationCdataInside(code4) {
    if (code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      return continuationDeclarationInside;
    }
    return continuation(code4);
  }
  function continuationDeclarationInside(code4) {
    if (code4 === codes.greaterThan) {
      effects.consume(code4);
      return continuationClose;
    }
    if (code4 === codes.dash && marker === constants.htmlComment) {
      effects.consume(code4);
      return continuationDeclarationInside;
    }
    return continuation(code4);
  }
  function continuationClose(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.htmlFlowData);
      return continuationAfter(code4);
    }
    effects.consume(code4);
    return continuationClose;
  }
  function continuationAfter(code4) {
    effects.exit(types.htmlFlow);
    return ok3(code4);
  }
}
function tokenizeNonLazyContinuationStart(effects, ok3, nok) {
  const self2 = this;
  return start2;
  function start2(code4) {
    if (markdownLineEnding(code4)) {
      effects.enter(types.lineEnding);
      effects.consume(code4);
      effects.exit(types.lineEnding);
      return after;
    }
    return nok(code4);
  }
  function after(code4) {
    return self2.parser.lazy[self2.now().line] ? nok(code4) : ok3(code4);
  }
}
function tokenizeBlankLineBefore(effects, ok3, nok) {
  return start2;
  function start2(code4) {
    ok(markdownLineEnding(code4), "expected a line ending");
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return effects.attempt(blankLine, ok3, nok);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/html-text.js
var htmlText = { name: "htmlText", tokenize: tokenizeHtmlText };
function tokenizeHtmlText(effects, ok3, nok) {
  const self2 = this;
  let marker;
  let index6;
  let returnState;
  return start2;
  function start2(code4) {
    ok(code4 === codes.lessThan, "expected `<`");
    effects.enter(types.htmlText);
    effects.enter(types.htmlTextData);
    effects.consume(code4);
    return open;
  }
  function open(code4) {
    if (code4 === codes.exclamationMark) {
      effects.consume(code4);
      return declarationOpen;
    }
    if (code4 === codes.slash) {
      effects.consume(code4);
      return tagCloseStart;
    }
    if (code4 === codes.questionMark) {
      effects.consume(code4);
      return instruction;
    }
    if (asciiAlpha(code4)) {
      effects.consume(code4);
      return tagOpen;
    }
    return nok(code4);
  }
  function declarationOpen(code4) {
    if (code4 === codes.dash) {
      effects.consume(code4);
      return commentOpenInside;
    }
    if (code4 === codes.leftSquareBracket) {
      effects.consume(code4);
      index6 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code4)) {
      effects.consume(code4);
      return declaration;
    }
    return nok(code4);
  }
  function commentOpenInside(code4) {
    if (code4 === codes.dash) {
      effects.consume(code4);
      return commentEnd;
    }
    return nok(code4);
  }
  function comment(code4) {
    if (code4 === codes.eof) {
      return nok(code4);
    }
    if (code4 === codes.dash) {
      effects.consume(code4);
      return commentClose;
    }
    if (markdownLineEnding(code4)) {
      returnState = comment;
      return lineEndingBefore(code4);
    }
    effects.consume(code4);
    return comment;
  }
  function commentClose(code4) {
    if (code4 === codes.dash) {
      effects.consume(code4);
      return commentEnd;
    }
    return comment(code4);
  }
  function commentEnd(code4) {
    return code4 === codes.greaterThan ? end(code4) : code4 === codes.dash ? commentClose(code4) : comment(code4);
  }
  function cdataOpenInside(code4) {
    const value = constants.cdataOpeningString;
    if (code4 === value.charCodeAt(index6++)) {
      effects.consume(code4);
      return index6 === value.length ? cdata : cdataOpenInside;
    }
    return nok(code4);
  }
  function cdata(code4) {
    if (code4 === codes.eof) {
      return nok(code4);
    }
    if (code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      return cdataClose;
    }
    if (markdownLineEnding(code4)) {
      returnState = cdata;
      return lineEndingBefore(code4);
    }
    effects.consume(code4);
    return cdata;
  }
  function cdataClose(code4) {
    if (code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      return cdataEnd;
    }
    return cdata(code4);
  }
  function cdataEnd(code4) {
    if (code4 === codes.greaterThan) {
      return end(code4);
    }
    if (code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      return cdataEnd;
    }
    return cdata(code4);
  }
  function declaration(code4) {
    if (code4 === codes.eof || code4 === codes.greaterThan) {
      return end(code4);
    }
    if (markdownLineEnding(code4)) {
      returnState = declaration;
      return lineEndingBefore(code4);
    }
    effects.consume(code4);
    return declaration;
  }
  function instruction(code4) {
    if (code4 === codes.eof) {
      return nok(code4);
    }
    if (code4 === codes.questionMark) {
      effects.consume(code4);
      return instructionClose;
    }
    if (markdownLineEnding(code4)) {
      returnState = instruction;
      return lineEndingBefore(code4);
    }
    effects.consume(code4);
    return instruction;
  }
  function instructionClose(code4) {
    return code4 === codes.greaterThan ? end(code4) : instruction(code4);
  }
  function tagCloseStart(code4) {
    if (asciiAlpha(code4)) {
      effects.consume(code4);
      return tagClose;
    }
    return nok(code4);
  }
  function tagClose(code4) {
    if (code4 === codes.dash || asciiAlphanumeric(code4)) {
      effects.consume(code4);
      return tagClose;
    }
    return tagCloseBetween(code4);
  }
  function tagCloseBetween(code4) {
    if (markdownLineEnding(code4)) {
      returnState = tagCloseBetween;
      return lineEndingBefore(code4);
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return tagCloseBetween;
    }
    return end(code4);
  }
  function tagOpen(code4) {
    if (code4 === codes.dash || asciiAlphanumeric(code4)) {
      effects.consume(code4);
      return tagOpen;
    }
    if (code4 === codes.slash || code4 === codes.greaterThan || markdownLineEndingOrSpace(code4)) {
      return tagOpenBetween(code4);
    }
    return nok(code4);
  }
  function tagOpenBetween(code4) {
    if (code4 === codes.slash) {
      effects.consume(code4);
      return end;
    }
    if (code4 === codes.colon || code4 === codes.underscore || asciiAlpha(code4)) {
      effects.consume(code4);
      return tagOpenAttributeName;
    }
    if (markdownLineEnding(code4)) {
      returnState = tagOpenBetween;
      return lineEndingBefore(code4);
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return tagOpenBetween;
    }
    return end(code4);
  }
  function tagOpenAttributeName(code4) {
    if (code4 === codes.dash || code4 === codes.dot || code4 === codes.colon || code4 === codes.underscore || asciiAlphanumeric(code4)) {
      effects.consume(code4);
      return tagOpenAttributeName;
    }
    return tagOpenAttributeNameAfter(code4);
  }
  function tagOpenAttributeNameAfter(code4) {
    if (code4 === codes.equalsTo) {
      effects.consume(code4);
      return tagOpenAttributeValueBefore;
    }
    if (markdownLineEnding(code4)) {
      returnState = tagOpenAttributeNameAfter;
      return lineEndingBefore(code4);
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return tagOpenAttributeNameAfter;
    }
    return tagOpenBetween(code4);
  }
  function tagOpenAttributeValueBefore(code4) {
    if (code4 === codes.eof || code4 === codes.lessThan || code4 === codes.equalsTo || code4 === codes.greaterThan || code4 === codes.graveAccent) {
      return nok(code4);
    }
    if (code4 === codes.quotationMark || code4 === codes.apostrophe) {
      effects.consume(code4);
      marker = code4;
      return tagOpenAttributeValueQuoted;
    }
    if (markdownLineEnding(code4)) {
      returnState = tagOpenAttributeValueBefore;
      return lineEndingBefore(code4);
    }
    if (markdownSpace(code4)) {
      effects.consume(code4);
      return tagOpenAttributeValueBefore;
    }
    effects.consume(code4);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuoted(code4) {
    if (code4 === marker) {
      effects.consume(code4);
      marker = void 0;
      return tagOpenAttributeValueQuotedAfter;
    }
    if (code4 === codes.eof) {
      return nok(code4);
    }
    if (markdownLineEnding(code4)) {
      returnState = tagOpenAttributeValueQuoted;
      return lineEndingBefore(code4);
    }
    effects.consume(code4);
    return tagOpenAttributeValueQuoted;
  }
  function tagOpenAttributeValueUnquoted(code4) {
    if (code4 === codes.eof || code4 === codes.quotationMark || code4 === codes.apostrophe || code4 === codes.lessThan || code4 === codes.equalsTo || code4 === codes.graveAccent) {
      return nok(code4);
    }
    if (code4 === codes.slash || code4 === codes.greaterThan || markdownLineEndingOrSpace(code4)) {
      return tagOpenBetween(code4);
    }
    effects.consume(code4);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuotedAfter(code4) {
    if (code4 === codes.slash || code4 === codes.greaterThan || markdownLineEndingOrSpace(code4)) {
      return tagOpenBetween(code4);
    }
    return nok(code4);
  }
  function end(code4) {
    if (code4 === codes.greaterThan) {
      effects.consume(code4);
      effects.exit(types.htmlTextData);
      effects.exit(types.htmlText);
      return ok3;
    }
    return nok(code4);
  }
  function lineEndingBefore(code4) {
    ok(returnState, "expected return state");
    ok(markdownLineEnding(code4), "expected eol");
    effects.exit(types.htmlTextData);
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return lineEndingAfter;
  }
  function lineEndingAfter(code4) {
    ok(
      self2.parser.constructs.disable.null,
      "expected `disable.null` to be populated"
    );
    return markdownSpace(code4) ? factorySpace(
      effects,
      lineEndingAfterPrefix,
      types.linePrefix,
      self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : constants.tabSize
    )(code4) : lineEndingAfterPrefix(code4);
  }
  function lineEndingAfterPrefix(code4) {
    effects.enter(types.htmlTextData);
    return returnState(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/label-end.js
var labelEnd = {
  name: "labelEnd",
  resolveAll: resolveAllLabelEnd,
  resolveTo: resolveToLabelEnd,
  tokenize: tokenizeLabelEnd
};
var resourceConstruct = { tokenize: tokenizeResource };
var referenceFullConstruct = { tokenize: tokenizeReferenceFull };
var referenceCollapsedConstruct = { tokenize: tokenizeReferenceCollapsed };
function resolveAllLabelEnd(events) {
  let index6 = -1;
  const newEvents = [];
  while (++index6 < events.length) {
    const token = events[index6][1];
    newEvents.push(events[index6]);
    if (token.type === types.labelImage || token.type === types.labelLink || token.type === types.labelEnd) {
      const offset4 = token.type === types.labelImage ? 4 : 2;
      token.type = types.data;
      index6 += offset4;
    }
  }
  if (events.length !== newEvents.length) {
    splice(events, 0, events.length, newEvents);
  }
  return events;
}
function resolveToLabelEnd(events, context) {
  let index6 = events.length;
  let offset4 = 0;
  let token;
  let open;
  let close;
  let media;
  while (index6--) {
    token = events[index6][1];
    if (open) {
      if (token.type === types.link || token.type === types.labelLink && token._inactive) {
        break;
      }
      if (events[index6][0] === "enter" && token.type === types.labelLink) {
        token._inactive = true;
      }
    } else if (close) {
      if (events[index6][0] === "enter" && (token.type === types.labelImage || token.type === types.labelLink) && !token._balanced) {
        open = index6;
        if (token.type !== types.labelLink) {
          offset4 = 2;
          break;
        }
      }
    } else if (token.type === types.labelEnd) {
      close = index6;
    }
  }
  ok(open !== void 0, "`open` is supposed to be found");
  ok(close !== void 0, "`close` is supposed to be found");
  const group = {
    type: events[open][1].type === types.labelLink ? types.link : types.image,
    start: { ...events[open][1].start },
    end: { ...events[events.length - 1][1].end }
  };
  const label = {
    type: types.label,
    start: { ...events[open][1].start },
    end: { ...events[close][1].end }
  };
  const text8 = {
    type: types.labelText,
    start: { ...events[open + offset4 + 2][1].end },
    end: { ...events[close - 2][1].start }
  };
  media = [
    ["enter", group, context],
    ["enter", label, context]
  ];
  media = push(media, events.slice(open + 1, open + offset4 + 3));
  media = push(media, [["enter", text8, context]]);
  ok(
    context.parser.constructs.insideSpan.null,
    "expected `insideSpan.null` to be populated"
  );
  media = push(
    media,
    resolveAll(
      context.parser.constructs.insideSpan.null,
      events.slice(open + offset4 + 4, close - 3),
      context
    )
  );
  media = push(media, [
    ["exit", text8, context],
    events[close - 2],
    events[close - 1],
    ["exit", label, context]
  ]);
  media = push(media, events.slice(close + 1));
  media = push(media, [["exit", group, context]]);
  splice(events, open, events.length, media);
  return events;
}
function tokenizeLabelEnd(effects, ok3, nok) {
  const self2 = this;
  let index6 = self2.events.length;
  let labelStart;
  let defined;
  while (index6--) {
    if ((self2.events[index6][1].type === types.labelImage || self2.events[index6][1].type === types.labelLink) && !self2.events[index6][1]._balanced) {
      labelStart = self2.events[index6][1];
      break;
    }
  }
  return start2;
  function start2(code4) {
    ok(code4 === codes.rightSquareBracket, "expected `]`");
    if (!labelStart) {
      return nok(code4);
    }
    if (labelStart._inactive) {
      return labelEndNok(code4);
    }
    defined = self2.parser.defined.includes(
      normalizeIdentifier(
        self2.sliceSerialize({ start: labelStart.end, end: self2.now() })
      )
    );
    effects.enter(types.labelEnd);
    effects.enter(types.labelMarker);
    effects.consume(code4);
    effects.exit(types.labelMarker);
    effects.exit(types.labelEnd);
    return after;
  }
  function after(code4) {
    if (code4 === codes.leftParenthesis) {
      return effects.attempt(
        resourceConstruct,
        labelEndOk,
        defined ? labelEndOk : labelEndNok
      )(code4);
    }
    if (code4 === codes.leftSquareBracket) {
      return effects.attempt(
        referenceFullConstruct,
        labelEndOk,
        defined ? referenceNotFull : labelEndNok
      )(code4);
    }
    return defined ? labelEndOk(code4) : labelEndNok(code4);
  }
  function referenceNotFull(code4) {
    return effects.attempt(
      referenceCollapsedConstruct,
      labelEndOk,
      labelEndNok
    )(code4);
  }
  function labelEndOk(code4) {
    return ok3(code4);
  }
  function labelEndNok(code4) {
    labelStart._balanced = true;
    return nok(code4);
  }
}
function tokenizeResource(effects, ok3, nok) {
  return resourceStart;
  function resourceStart(code4) {
    ok(code4 === codes.leftParenthesis, "expected left paren");
    effects.enter(types.resource);
    effects.enter(types.resourceMarker);
    effects.consume(code4);
    effects.exit(types.resourceMarker);
    return resourceBefore;
  }
  function resourceBefore(code4) {
    return markdownLineEndingOrSpace(code4) ? factoryWhitespace(effects, resourceOpen)(code4) : resourceOpen(code4);
  }
  function resourceOpen(code4) {
    if (code4 === codes.rightParenthesis) {
      return resourceEnd(code4);
    }
    return factoryDestination(
      effects,
      resourceDestinationAfter,
      resourceDestinationMissing,
      types.resourceDestination,
      types.resourceDestinationLiteral,
      types.resourceDestinationLiteralMarker,
      types.resourceDestinationRaw,
      types.resourceDestinationString,
      constants.linkResourceDestinationBalanceMax
    )(code4);
  }
  function resourceDestinationAfter(code4) {
    return markdownLineEndingOrSpace(code4) ? factoryWhitespace(effects, resourceBetween)(code4) : resourceEnd(code4);
  }
  function resourceDestinationMissing(code4) {
    return nok(code4);
  }
  function resourceBetween(code4) {
    if (code4 === codes.quotationMark || code4 === codes.apostrophe || code4 === codes.leftParenthesis) {
      return factoryTitle(
        effects,
        resourceTitleAfter,
        nok,
        types.resourceTitle,
        types.resourceTitleMarker,
        types.resourceTitleString
      )(code4);
    }
    return resourceEnd(code4);
  }
  function resourceTitleAfter(code4) {
    return markdownLineEndingOrSpace(code4) ? factoryWhitespace(effects, resourceEnd)(code4) : resourceEnd(code4);
  }
  function resourceEnd(code4) {
    if (code4 === codes.rightParenthesis) {
      effects.enter(types.resourceMarker);
      effects.consume(code4);
      effects.exit(types.resourceMarker);
      effects.exit(types.resource);
      return ok3;
    }
    return nok(code4);
  }
}
function tokenizeReferenceFull(effects, ok3, nok) {
  const self2 = this;
  return referenceFull;
  function referenceFull(code4) {
    ok(code4 === codes.leftSquareBracket, "expected left bracket");
    return factoryLabel.call(
      self2,
      effects,
      referenceFullAfter,
      referenceFullMissing,
      types.reference,
      types.referenceMarker,
      types.referenceString
    )(code4);
  }
  function referenceFullAfter(code4) {
    return self2.parser.defined.includes(
      normalizeIdentifier(
        self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1)
      )
    ) ? ok3(code4) : nok(code4);
  }
  function referenceFullMissing(code4) {
    return nok(code4);
  }
}
function tokenizeReferenceCollapsed(effects, ok3, nok) {
  return referenceCollapsedStart;
  function referenceCollapsedStart(code4) {
    ok(code4 === codes.leftSquareBracket, "expected left bracket");
    effects.enter(types.reference);
    effects.enter(types.referenceMarker);
    effects.consume(code4);
    effects.exit(types.referenceMarker);
    return referenceCollapsedOpen;
  }
  function referenceCollapsedOpen(code4) {
    if (code4 === codes.rightSquareBracket) {
      effects.enter(types.referenceMarker);
      effects.consume(code4);
      effects.exit(types.referenceMarker);
      effects.exit(types.reference);
      return ok3;
    }
    return nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/label-start-image.js
var labelStartImage = {
  name: "labelStartImage",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartImage
};
function tokenizeLabelStartImage(effects, ok3, nok) {
  const self2 = this;
  return start2;
  function start2(code4) {
    ok(code4 === codes.exclamationMark, "expected `!`");
    effects.enter(types.labelImage);
    effects.enter(types.labelImageMarker);
    effects.consume(code4);
    effects.exit(types.labelImageMarker);
    return open;
  }
  function open(code4) {
    if (code4 === codes.leftSquareBracket) {
      effects.enter(types.labelMarker);
      effects.consume(code4);
      effects.exit(types.labelMarker);
      effects.exit(types.labelImage);
      return after;
    }
    return nok(code4);
  }
  function after(code4) {
    return code4 === codes.caret && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code4) : ok3(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/label-start-link.js
var labelStartLink = {
  name: "labelStartLink",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartLink
};
function tokenizeLabelStartLink(effects, ok3, nok) {
  const self2 = this;
  return start2;
  function start2(code4) {
    ok(code4 === codes.leftSquareBracket, "expected `[`");
    effects.enter(types.labelLink);
    effects.enter(types.labelMarker);
    effects.consume(code4);
    effects.exit(types.labelMarker);
    effects.exit(types.labelLink);
    return after;
  }
  function after(code4) {
    return code4 === codes.caret && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code4) : ok3(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/line-ending.js
var lineEnding = { name: "lineEnding", tokenize: tokenizeLineEnding };
function tokenizeLineEnding(effects, ok3) {
  return start2;
  function start2(code4) {
    ok(markdownLineEnding(code4), "expected eol");
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    return factorySpace(effects, ok3, types.linePrefix);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/thematic-break.js
var thematicBreak = {
  name: "thematicBreak",
  tokenize: tokenizeThematicBreak
};
function tokenizeThematicBreak(effects, ok3, nok) {
  let size4 = 0;
  let marker;
  return start2;
  function start2(code4) {
    effects.enter(types.thematicBreak);
    return before(code4);
  }
  function before(code4) {
    ok(
      code4 === codes.asterisk || code4 === codes.dash || code4 === codes.underscore,
      "expected `*`, `-`, or `_`"
    );
    marker = code4;
    return atBreak(code4);
  }
  function atBreak(code4) {
    if (code4 === marker) {
      effects.enter(types.thematicBreakSequence);
      return sequence(code4);
    }
    if (size4 >= constants.thematicBreakMarkerCountMin && (code4 === codes.eof || markdownLineEnding(code4))) {
      effects.exit(types.thematicBreak);
      return ok3(code4);
    }
    return nok(code4);
  }
  function sequence(code4) {
    if (code4 === marker) {
      effects.consume(code4);
      size4++;
      return sequence;
    }
    effects.exit(types.thematicBreakSequence);
    return markdownSpace(code4) ? factorySpace(effects, atBreak, types.whitespace)(code4) : atBreak(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/list.js
var list = {
  continuation: { tokenize: tokenizeListContinuation },
  exit: tokenizeListEnd,
  name: "list",
  tokenize: tokenizeListStart
};
var listItemPrefixWhitespaceConstruct = {
  partial: true,
  tokenize: tokenizeListItemPrefixWhitespace
};
var indentConstruct = { partial: true, tokenize: tokenizeIndent };
function tokenizeListStart(effects, ok3, nok) {
  const self2 = this;
  const tail = self2.events[self2.events.length - 1];
  let initialSize = tail && tail[1].type === types.linePrefix ? tail[2].sliceSerialize(tail[1], true).length : 0;
  let size4 = 0;
  return start2;
  function start2(code4) {
    ok(self2.containerState, "expected state");
    const kind = self2.containerState.type || (code4 === codes.asterisk || code4 === codes.plusSign || code4 === codes.dash ? types.listUnordered : types.listOrdered);
    if (kind === types.listUnordered ? !self2.containerState.marker || code4 === self2.containerState.marker : asciiDigit(code4)) {
      if (!self2.containerState.type) {
        self2.containerState.type = kind;
        effects.enter(kind, { _container: true });
      }
      if (kind === types.listUnordered) {
        effects.enter(types.listItemPrefix);
        return code4 === codes.asterisk || code4 === codes.dash ? effects.check(thematicBreak, nok, atMarker)(code4) : atMarker(code4);
      }
      if (!self2.interrupt || code4 === codes.digit1) {
        effects.enter(types.listItemPrefix);
        effects.enter(types.listItemValue);
        return inside(code4);
      }
    }
    return nok(code4);
  }
  function inside(code4) {
    ok(self2.containerState, "expected state");
    if (asciiDigit(code4) && ++size4 < constants.listItemValueSizeMax) {
      effects.consume(code4);
      return inside;
    }
    if ((!self2.interrupt || size4 < 2) && (self2.containerState.marker ? code4 === self2.containerState.marker : code4 === codes.rightParenthesis || code4 === codes.dot)) {
      effects.exit(types.listItemValue);
      return atMarker(code4);
    }
    return nok(code4);
  }
  function atMarker(code4) {
    ok(self2.containerState, "expected state");
    ok(code4 !== codes.eof, "eof (`null`) is not a marker");
    effects.enter(types.listItemMarker);
    effects.consume(code4);
    effects.exit(types.listItemMarker);
    self2.containerState.marker = self2.containerState.marker || code4;
    return effects.check(
      blankLine,
      // Can’t be empty when interrupting.
      self2.interrupt ? nok : onBlank,
      effects.attempt(
        listItemPrefixWhitespaceConstruct,
        endOfPrefix,
        otherPrefix
      )
    );
  }
  function onBlank(code4) {
    ok(self2.containerState, "expected state");
    self2.containerState.initialBlankLine = true;
    initialSize++;
    return endOfPrefix(code4);
  }
  function otherPrefix(code4) {
    if (markdownSpace(code4)) {
      effects.enter(types.listItemPrefixWhitespace);
      effects.consume(code4);
      effects.exit(types.listItemPrefixWhitespace);
      return endOfPrefix;
    }
    return nok(code4);
  }
  function endOfPrefix(code4) {
    ok(self2.containerState, "expected state");
    self2.containerState.size = initialSize + self2.sliceSerialize(effects.exit(types.listItemPrefix), true).length;
    return ok3(code4);
  }
}
function tokenizeListContinuation(effects, ok3, nok) {
  const self2 = this;
  ok(self2.containerState, "expected state");
  self2.containerState._closeFlow = void 0;
  return effects.check(blankLine, onBlank, notBlank);
  function onBlank(code4) {
    ok(self2.containerState, "expected state");
    ok(typeof self2.containerState.size === "number", "expected size");
    self2.containerState.furtherBlankLines = self2.containerState.furtherBlankLines || self2.containerState.initialBlankLine;
    return factorySpace(
      effects,
      ok3,
      types.listItemIndent,
      self2.containerState.size + 1
    )(code4);
  }
  function notBlank(code4) {
    ok(self2.containerState, "expected state");
    if (self2.containerState.furtherBlankLines || !markdownSpace(code4)) {
      self2.containerState.furtherBlankLines = void 0;
      self2.containerState.initialBlankLine = void 0;
      return notInCurrentItem(code4);
    }
    self2.containerState.furtherBlankLines = void 0;
    self2.containerState.initialBlankLine = void 0;
    return effects.attempt(indentConstruct, ok3, notInCurrentItem)(code4);
  }
  function notInCurrentItem(code4) {
    ok(self2.containerState, "expected state");
    self2.containerState._closeFlow = true;
    self2.interrupt = void 0;
    ok(
      self2.parser.constructs.disable.null,
      "expected `disable.null` to be populated"
    );
    return factorySpace(
      effects,
      effects.attempt(list, ok3, nok),
      types.linePrefix,
      self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : constants.tabSize
    )(code4);
  }
}
function tokenizeIndent(effects, ok3, nok) {
  const self2 = this;
  ok(self2.containerState, "expected state");
  ok(typeof self2.containerState.size === "number", "expected size");
  return factorySpace(
    effects,
    afterPrefix,
    types.listItemIndent,
    self2.containerState.size + 1
  );
  function afterPrefix(code4) {
    ok(self2.containerState, "expected state");
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === types.listItemIndent && tail[2].sliceSerialize(tail[1], true).length === self2.containerState.size ? ok3(code4) : nok(code4);
  }
}
function tokenizeListEnd(effects) {
  ok(this.containerState, "expected state");
  ok(typeof this.containerState.type === "string", "expected type");
  effects.exit(this.containerState.type);
}
function tokenizeListItemPrefixWhitespace(effects, ok3, nok) {
  const self2 = this;
  ok(
    self2.parser.constructs.disable.null,
    "expected `disable.null` to be populated"
  );
  return factorySpace(
    effects,
    afterPrefix,
    types.listItemPrefixWhitespace,
    self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : constants.tabSize + 1
  );
  function afterPrefix(code4) {
    const tail = self2.events[self2.events.length - 1];
    return !markdownSpace(code4) && tail && tail[1].type === types.listItemPrefixWhitespace ? ok3(code4) : nok(code4);
  }
}

// node_modules/micromark-core-commonmark/dev/lib/setext-underline.js
var setextUnderline = {
  name: "setextUnderline",
  resolveTo: resolveToSetextUnderline,
  tokenize: tokenizeSetextUnderline
};
function resolveToSetextUnderline(events, context) {
  let index6 = events.length;
  let content3;
  let text8;
  let definition3;
  while (index6--) {
    if (events[index6][0] === "enter") {
      if (events[index6][1].type === types.content) {
        content3 = index6;
        break;
      }
      if (events[index6][1].type === types.paragraph) {
        text8 = index6;
      }
    } else {
      if (events[index6][1].type === types.content) {
        events.splice(index6, 1);
      }
      if (!definition3 && events[index6][1].type === types.definition) {
        definition3 = index6;
      }
    }
  }
  ok(text8 !== void 0, "expected a `text` index to be found");
  ok(content3 !== void 0, "expected a `text` index to be found");
  ok(events[content3][2] === context, "enter context should be same");
  ok(
    events[events.length - 1][2] === context,
    "enter context should be same"
  );
  const heading3 = {
    type: types.setextHeading,
    start: { ...events[content3][1].start },
    end: { ...events[events.length - 1][1].end }
  };
  events[text8][1].type = types.setextHeadingText;
  if (definition3) {
    events.splice(text8, 0, ["enter", heading3, context]);
    events.splice(definition3 + 1, 0, ["exit", events[content3][1], context]);
    events[content3][1].end = { ...events[definition3][1].end };
  } else {
    events[content3][1] = heading3;
  }
  events.push(["exit", heading3, context]);
  return events;
}
function tokenizeSetextUnderline(effects, ok3, nok) {
  const self2 = this;
  let marker;
  return start2;
  function start2(code4) {
    let index6 = self2.events.length;
    let paragraph3;
    ok(
      code4 === codes.dash || code4 === codes.equalsTo,
      "expected `=` or `-`"
    );
    while (index6--) {
      if (self2.events[index6][1].type !== types.lineEnding && self2.events[index6][1].type !== types.linePrefix && self2.events[index6][1].type !== types.content) {
        paragraph3 = self2.events[index6][1].type === types.paragraph;
        break;
      }
    }
    if (!self2.parser.lazy[self2.now().line] && (self2.interrupt || paragraph3)) {
      effects.enter(types.setextHeadingLine);
      marker = code4;
      return before(code4);
    }
    return nok(code4);
  }
  function before(code4) {
    effects.enter(types.setextHeadingLineSequence);
    return inside(code4);
  }
  function inside(code4) {
    if (code4 === marker) {
      effects.consume(code4);
      return inside;
    }
    effects.exit(types.setextHeadingLineSequence);
    return markdownSpace(code4) ? factorySpace(effects, after, types.lineSuffix)(code4) : after(code4);
  }
  function after(code4) {
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit(types.setextHeadingLine);
      return ok3(code4);
    }
    return nok(code4);
  }
}

// node_modules/micromark/dev/lib/initialize/flow.js
var flow = { tokenize: initializeFlow };
function initializeFlow(effects) {
  const self2 = this;
  const initial = effects.attempt(
    // Try to parse a blank line.
    blankLine,
    atBlankEnding,
    // Try to parse initial flow (essentially, only code).
    effects.attempt(
      this.parser.constructs.flowInitial,
      afterConstruct,
      factorySpace(
        effects,
        effects.attempt(
          this.parser.constructs.flow,
          afterConstruct,
          effects.attempt(content2, afterConstruct)
        ),
        types.linePrefix
      )
    )
  );
  return initial;
  function atBlankEnding(code4) {
    ok(
      code4 === codes.eof || markdownLineEnding(code4),
      "expected eol or eof"
    );
    if (code4 === codes.eof) {
      effects.consume(code4);
      return;
    }
    effects.enter(types.lineEndingBlank);
    effects.consume(code4);
    effects.exit(types.lineEndingBlank);
    self2.currentConstruct = void 0;
    return initial;
  }
  function afterConstruct(code4) {
    ok(
      code4 === codes.eof || markdownLineEnding(code4),
      "expected eol or eof"
    );
    if (code4 === codes.eof) {
      effects.consume(code4);
      return;
    }
    effects.enter(types.lineEnding);
    effects.consume(code4);
    effects.exit(types.lineEnding);
    self2.currentConstruct = void 0;
    return initial;
  }
}

// node_modules/micromark/dev/lib/initialize/text.js
var resolver = { resolveAll: createResolver() };
var string = initializeFactory("string");
var text2 = initializeFactory("text");
function initializeFactory(field) {
  return {
    resolveAll: createResolver(
      field === "text" ? resolveAllLineSuffixes : void 0
    ),
    tokenize: initializeText
  };
  function initializeText(effects) {
    const self2 = this;
    const constructs2 = this.parser.constructs[field];
    const text8 = effects.attempt(constructs2, start2, notText);
    return start2;
    function start2(code4) {
      return atBreak(code4) ? text8(code4) : notText(code4);
    }
    function notText(code4) {
      if (code4 === codes.eof) {
        effects.consume(code4);
        return;
      }
      effects.enter(types.data);
      effects.consume(code4);
      return data;
    }
    function data(code4) {
      if (atBreak(code4)) {
        effects.exit(types.data);
        return text8(code4);
      }
      effects.consume(code4);
      return data;
    }
    function atBreak(code4) {
      if (code4 === codes.eof) {
        return true;
      }
      const list4 = constructs2[code4];
      let index6 = -1;
      if (list4) {
        ok(Array.isArray(list4), "expected `disable.null` to be populated");
        while (++index6 < list4.length) {
          const item = list4[index6];
          if (!item.previous || item.previous.call(self2, self2.previous)) {
            return true;
          }
        }
      }
      return false;
    }
  }
}
function createResolver(extraResolver) {
  return resolveAllText;
  function resolveAllText(events, context) {
    let index6 = -1;
    let enter;
    while (++index6 <= events.length) {
      if (enter === void 0) {
        if (events[index6] && events[index6][1].type === types.data) {
          enter = index6;
          index6++;
        }
      } else if (!events[index6] || events[index6][1].type !== types.data) {
        if (index6 !== enter + 2) {
          events[enter][1].end = events[index6 - 1][1].end;
          events.splice(enter + 2, index6 - enter - 2);
          index6 = enter + 2;
        }
        enter = void 0;
      }
    }
    return extraResolver ? extraResolver(events, context) : events;
  }
}
function resolveAllLineSuffixes(events, context) {
  let eventIndex = 0;
  while (++eventIndex <= events.length) {
    if ((eventIndex === events.length || events[eventIndex][1].type === types.lineEnding) && events[eventIndex - 1][1].type === types.data) {
      const data = events[eventIndex - 1][1];
      const chunks = context.sliceStream(data);
      let index6 = chunks.length;
      let bufferIndex = -1;
      let size4 = 0;
      let tabs;
      while (index6--) {
        const chunk = chunks[index6];
        if (typeof chunk === "string") {
          bufferIndex = chunk.length;
          while (chunk.charCodeAt(bufferIndex - 1) === codes.space) {
            size4++;
            bufferIndex--;
          }
          if (bufferIndex) break;
          bufferIndex = -1;
        } else if (chunk === codes.horizontalTab) {
          tabs = true;
          size4++;
        } else if (chunk === codes.virtualSpace) {
        } else {
          index6++;
          break;
        }
      }
      if (context._contentTypeTextTrailing && eventIndex === events.length) {
        size4 = 0;
      }
      if (size4) {
        const token = {
          type: eventIndex === events.length || tabs || size4 < constants.hardBreakPrefixSizeMin ? types.lineSuffix : types.hardBreakTrailing,
          start: {
            _bufferIndex: index6 ? bufferIndex : data.start._bufferIndex + bufferIndex,
            _index: data.start._index + index6,
            line: data.end.line,
            column: data.end.column - size4,
            offset: data.end.offset - size4
          },
          end: { ...data.end }
        };
        data.end = { ...token.start };
        if (data.start.offset === data.end.offset) {
          Object.assign(data, token);
        } else {
          events.splice(
            eventIndex,
            0,
            ["enter", token, context],
            ["exit", token, context]
          );
          eventIndex += 2;
        }
      }
      eventIndex++;
    }
  }
  return events;
}

// node_modules/micromark/dev/lib/constructs.js
var constructs_exports = {};
__export(constructs_exports, {
  attentionMarkers: () => attentionMarkers,
  contentInitial: () => contentInitial,
  disable: () => disable,
  document: () => document3,
  flow: () => flow2,
  flowInitial: () => flowInitial,
  insideSpan: () => insideSpan,
  string: () => string2,
  text: () => text3
});
var document3 = {
  [codes.asterisk]: list,
  [codes.plusSign]: list,
  [codes.dash]: list,
  [codes.digit0]: list,
  [codes.digit1]: list,
  [codes.digit2]: list,
  [codes.digit3]: list,
  [codes.digit4]: list,
  [codes.digit5]: list,
  [codes.digit6]: list,
  [codes.digit7]: list,
  [codes.digit8]: list,
  [codes.digit9]: list,
  [codes.greaterThan]: blockQuote
};
var contentInitial = {
  [codes.leftSquareBracket]: definition
};
var flowInitial = {
  [codes.horizontalTab]: codeIndented,
  [codes.virtualSpace]: codeIndented,
  [codes.space]: codeIndented
};
var flow2 = {
  [codes.numberSign]: headingAtx,
  [codes.asterisk]: thematicBreak,
  [codes.dash]: [setextUnderline, thematicBreak],
  [codes.lessThan]: htmlFlow,
  [codes.equalsTo]: setextUnderline,
  [codes.underscore]: thematicBreak,
  [codes.graveAccent]: codeFenced,
  [codes.tilde]: codeFenced
};
var string2 = {
  [codes.ampersand]: characterReference,
  [codes.backslash]: characterEscape
};
var text3 = {
  [codes.carriageReturn]: lineEnding,
  [codes.lineFeed]: lineEnding,
  [codes.carriageReturnLineFeed]: lineEnding,
  [codes.exclamationMark]: labelStartImage,
  [codes.ampersand]: characterReference,
  [codes.asterisk]: attention,
  [codes.lessThan]: [autolink, htmlText],
  [codes.leftSquareBracket]: labelStartLink,
  [codes.backslash]: [hardBreakEscape, characterEscape],
  [codes.rightSquareBracket]: labelEnd,
  [codes.underscore]: attention,
  [codes.graveAccent]: codeText
};
var insideSpan = { null: [attention, resolver] };
var attentionMarkers = { null: [codes.asterisk, codes.underscore] };
var disable = { null: [] };

// node_modules/micromark/dev/lib/create-tokenizer.js
var import_debug = __toESM(require_browser(), 1);
var debug = (0, import_debug.default)("micromark");
function createTokenizer(parser, initialize, from) {
  let point4 = {
    _bufferIndex: -1,
    _index: 0,
    line: from && from.line || 1,
    column: from && from.column || 1,
    offset: from && from.offset || 0
  };
  const columnStart = {};
  const resolveAllConstructs = [];
  let chunks = [];
  let stack = [];
  let consumed = true;
  const effects = {
    attempt: constructFactory(onsuccessfulconstruct),
    check: constructFactory(onsuccessfulcheck),
    consume,
    enter,
    exit: exit3,
    interrupt: constructFactory(onsuccessfulcheck, { interrupt: true })
  };
  const context = {
    code: codes.eof,
    containerState: {},
    defineSkip,
    events: [],
    now,
    parser,
    previous: codes.eof,
    sliceSerialize,
    sliceStream,
    write
  };
  let state = initialize.tokenize.call(context, effects);
  let expectedCode;
  if (initialize.resolveAll) {
    resolveAllConstructs.push(initialize);
  }
  return context;
  function write(slice) {
    chunks = push(chunks, slice);
    main();
    if (chunks[chunks.length - 1] !== codes.eof) {
      return [];
    }
    addResult(initialize, 0);
    context.events = resolveAll(resolveAllConstructs, context.events, context);
    return context.events;
  }
  function sliceSerialize(token, expandTabs) {
    return serializeChunks(sliceStream(token), expandTabs);
  }
  function sliceStream(token) {
    return sliceChunks(chunks, token);
  }
  function now() {
    const { _bufferIndex, _index, line, column, offset: offset4 } = point4;
    return { _bufferIndex, _index, line, column, offset: offset4 };
  }
  function defineSkip(value) {
    columnStart[value.line] = value.column;
    accountForPotentialSkip();
    debug("position: define skip: `%j`", point4);
  }
  function main() {
    let chunkIndex;
    while (point4._index < chunks.length) {
      const chunk = chunks[point4._index];
      if (typeof chunk === "string") {
        chunkIndex = point4._index;
        if (point4._bufferIndex < 0) {
          point4._bufferIndex = 0;
        }
        while (point4._index === chunkIndex && point4._bufferIndex < chunk.length) {
          go(chunk.charCodeAt(point4._bufferIndex));
        }
      } else {
        go(chunk);
      }
    }
  }
  function go(code4) {
    ok(consumed === true, "expected character to be consumed");
    consumed = void 0;
    debug("main: passing `%s` to %s", code4, state && state.name);
    expectedCode = code4;
    ok(typeof state === "function", "expected state");
    state = state(code4);
  }
  function consume(code4) {
    ok(code4 === expectedCode, "expected given code to equal expected code");
    debug("consume: `%s`", code4);
    ok(
      consumed === void 0,
      "expected code to not have been consumed: this might be because `return x(code)` instead of `return x` was used"
    );
    ok(
      code4 === null ? context.events.length === 0 || context.events[context.events.length - 1][0] === "exit" : context.events[context.events.length - 1][0] === "enter",
      "expected last token to be open"
    );
    if (markdownLineEnding(code4)) {
      point4.line++;
      point4.column = 1;
      point4.offset += code4 === codes.carriageReturnLineFeed ? 2 : 1;
      accountForPotentialSkip();
      debug("position: after eol: `%j`", point4);
    } else if (code4 !== codes.virtualSpace) {
      point4.column++;
      point4.offset++;
    }
    if (point4._bufferIndex < 0) {
      point4._index++;
    } else {
      point4._bufferIndex++;
      if (point4._bufferIndex === // Points w/ non-negative `_bufferIndex` reference
      // strings.
      /** @type {string} */
      chunks[point4._index].length) {
        point4._bufferIndex = -1;
        point4._index++;
      }
    }
    context.previous = code4;
    consumed = true;
  }
  function enter(type, fields) {
    const token = fields || {};
    token.type = type;
    token.start = now();
    ok(typeof type === "string", "expected string type");
    ok(type.length > 0, "expected non-empty string");
    debug("enter: `%s`", type);
    context.events.push(["enter", token, context]);
    stack.push(token);
    return token;
  }
  function exit3(type) {
    ok(typeof type === "string", "expected string type");
    ok(type.length > 0, "expected non-empty string");
    const token = stack.pop();
    ok(token, "cannot close w/o open tokens");
    token.end = now();
    ok(type === token.type, "expected exit token to match current token");
    ok(
      !(token.start._index === token.end._index && token.start._bufferIndex === token.end._bufferIndex),
      "expected non-empty token (`" + type + "`)"
    );
    debug("exit: `%s`", token.type);
    context.events.push(["exit", token, context]);
    return token;
  }
  function onsuccessfulconstruct(construct, info) {
    addResult(construct, info.from);
  }
  function onsuccessfulcheck(_2, info) {
    info.restore();
  }
  function constructFactory(onreturn, fields) {
    return hook;
    function hook(constructs2, returnState, bogusState) {
      let listOfConstructs;
      let constructIndex;
      let currentConstruct;
      let info;
      return Array.isArray(constructs2) ? (
        /* c8 ignore next 1 */
        handleListOfConstructs(constructs2)
      ) : "tokenize" in constructs2 ? (
        // Looks like a construct.
        handleListOfConstructs([
          /** @type {Construct} */
          constructs2
        ])
      ) : handleMapOfConstructs(constructs2);
      function handleMapOfConstructs(map4) {
        return start2;
        function start2(code4) {
          const left = code4 !== null && map4[code4];
          const all2 = code4 !== null && map4.null;
          const list4 = [
            // To do: add more extension tests.
            /* c8 ignore next 2 */
            ...Array.isArray(left) ? left : left ? [left] : [],
            ...Array.isArray(all2) ? all2 : all2 ? [all2] : []
          ];
          return handleListOfConstructs(list4)(code4);
        }
      }
      function handleListOfConstructs(list4) {
        listOfConstructs = list4;
        constructIndex = 0;
        if (list4.length === 0) {
          ok(bogusState, "expected `bogusState` to be given");
          return bogusState;
        }
        return handleConstruct(list4[constructIndex]);
      }
      function handleConstruct(construct) {
        return start2;
        function start2(code4) {
          info = store();
          currentConstruct = construct;
          if (!construct.partial) {
            context.currentConstruct = construct;
          }
          ok(
            context.parser.constructs.disable.null,
            "expected `disable.null` to be populated"
          );
          if (construct.name && context.parser.constructs.disable.null.includes(construct.name)) {
            return nok(code4);
          }
          return construct.tokenize.call(
            // If we do have fields, create an object w/ `context` as its
            // prototype.
            // This allows a “live binding”, which is needed for `interrupt`.
            fields ? Object.assign(Object.create(context), fields) : context,
            effects,
            ok3,
            nok
          )(code4);
        }
      }
      function ok3(code4) {
        ok(code4 === expectedCode, "expected code");
        consumed = true;
        onreturn(currentConstruct, info);
        return returnState;
      }
      function nok(code4) {
        ok(code4 === expectedCode, "expected code");
        consumed = true;
        info.restore();
        if (++constructIndex < listOfConstructs.length) {
          return handleConstruct(listOfConstructs[constructIndex]);
        }
        return bogusState;
      }
    }
  }
  function addResult(construct, from2) {
    if (construct.resolveAll && !resolveAllConstructs.includes(construct)) {
      resolveAllConstructs.push(construct);
    }
    if (construct.resolve) {
      splice(
        context.events,
        from2,
        context.events.length - from2,
        construct.resolve(context.events.slice(from2), context)
      );
    }
    if (construct.resolveTo) {
      context.events = construct.resolveTo(context.events, context);
    }
    ok(
      construct.partial || context.events.length === 0 || context.events[context.events.length - 1][0] === "exit",
      "expected last token to end"
    );
  }
  function store() {
    const startPoint = now();
    const startPrevious = context.previous;
    const startCurrentConstruct = context.currentConstruct;
    const startEventsIndex = context.events.length;
    const startStack = Array.from(stack);
    return { from: startEventsIndex, restore };
    function restore() {
      point4 = startPoint;
      context.previous = startPrevious;
      context.currentConstruct = startCurrentConstruct;
      context.events.length = startEventsIndex;
      stack = startStack;
      accountForPotentialSkip();
      debug("position: restore: `%j`", point4);
    }
  }
  function accountForPotentialSkip() {
    if (point4.line in columnStart && point4.column < 2) {
      point4.column = columnStart[point4.line];
      point4.offset += columnStart[point4.line] - 1;
    }
  }
}
function sliceChunks(chunks, token) {
  const startIndex = token.start._index;
  const startBufferIndex = token.start._bufferIndex;
  const endIndex = token.end._index;
  const endBufferIndex = token.end._bufferIndex;
  let view;
  if (startIndex === endIndex) {
    ok(endBufferIndex > -1, "expected non-negative end buffer index");
    ok(startBufferIndex > -1, "expected non-negative start buffer index");
    view = [chunks[startIndex].slice(startBufferIndex, endBufferIndex)];
  } else {
    view = chunks.slice(startIndex, endIndex);
    if (startBufferIndex > -1) {
      const head = view[0];
      if (typeof head === "string") {
        view[0] = head.slice(startBufferIndex);
      } else {
        ok(startBufferIndex === 0, "expected `startBufferIndex` to be `0`");
        view.shift();
      }
    }
    if (endBufferIndex > 0) {
      view.push(chunks[endIndex].slice(0, endBufferIndex));
    }
  }
  return view;
}
function serializeChunks(chunks, expandTabs) {
  let index6 = -1;
  const result = [];
  let atTab;
  while (++index6 < chunks.length) {
    const chunk = chunks[index6];
    let value;
    if (typeof chunk === "string") {
      value = chunk;
    } else
      switch (chunk) {
        case codes.carriageReturn: {
          value = values.cr;
          break;
        }
        case codes.lineFeed: {
          value = values.lf;
          break;
        }
        case codes.carriageReturnLineFeed: {
          value = values.cr + values.lf;
          break;
        }
        case codes.horizontalTab: {
          value = expandTabs ? values.space : values.ht;
          break;
        }
        case codes.virtualSpace: {
          if (!expandTabs && atTab) continue;
          value = values.space;
          break;
        }
        default: {
          ok(typeof chunk === "number", "expected number");
          value = String.fromCharCode(chunk);
        }
      }
    atTab = chunk === codes.horizontalTab;
    result.push(value);
  }
  return result.join("");
}

// node_modules/micromark/dev/lib/parse.js
function parse(options) {
  const settings = options || {};
  const constructs2 = (
    /** @type {FullNormalizedExtension} */
    combineExtensions([constructs_exports, ...settings.extensions || []])
  );
  const parser = {
    constructs: constructs2,
    content: create2(content),
    defined: [],
    document: create2(document2),
    flow: create2(flow),
    lazy: {},
    string: create2(string),
    text: create2(text2)
  };
  return parser;
  function create2(initial) {
    return creator;
    function creator(from) {
      return createTokenizer(parser, initial, from);
    }
  }
}

// node_modules/micromark/dev/lib/postprocess.js
function postprocess(events) {
  while (!subtokenize(events)) {
  }
  return events;
}

// node_modules/micromark/dev/lib/preprocess.js
var search = /[\0\t\n\r]/g;
function preprocess() {
  let column = 1;
  let buffer = "";
  let start2 = true;
  let atCarriageReturn;
  return preprocessor;
  function preprocessor(value, encoding, end) {
    const chunks = [];
    let match;
    let next;
    let startPosition;
    let endPosition;
    let code4;
    value = buffer + (typeof value === "string" ? value.toString() : new TextDecoder(encoding || void 0).decode(value));
    startPosition = 0;
    buffer = "";
    if (start2) {
      if (value.charCodeAt(0) === codes.byteOrderMarker) {
        startPosition++;
      }
      start2 = void 0;
    }
    while (startPosition < value.length) {
      search.lastIndex = startPosition;
      match = search.exec(value);
      endPosition = match && match.index !== void 0 ? match.index : value.length;
      code4 = value.charCodeAt(endPosition);
      if (!match) {
        buffer = value.slice(startPosition);
        break;
      }
      if (code4 === codes.lf && startPosition === endPosition && atCarriageReturn) {
        chunks.push(codes.carriageReturnLineFeed);
        atCarriageReturn = void 0;
      } else {
        if (atCarriageReturn) {
          chunks.push(codes.carriageReturn);
          atCarriageReturn = void 0;
        }
        if (startPosition < endPosition) {
          chunks.push(value.slice(startPosition, endPosition));
          column += endPosition - startPosition;
        }
        switch (code4) {
          case codes.nul: {
            chunks.push(codes.replacementCharacter);
            column++;
            break;
          }
          case codes.ht: {
            next = Math.ceil(column / constants.tabSize) * constants.tabSize;
            chunks.push(codes.horizontalTab);
            while (column++ < next) chunks.push(codes.virtualSpace);
            break;
          }
          case codes.lf: {
            chunks.push(codes.lineFeed);
            column = 1;
            break;
          }
          default: {
            atCarriageReturn = true;
            column = 1;
          }
        }
      }
      startPosition = endPosition + 1;
    }
    if (end) {
      if (atCarriageReturn) chunks.push(codes.carriageReturn);
      if (buffer) chunks.push(buffer);
      chunks.push(codes.eof);
    }
    return chunks;
  }
}

// node_modules/micromark-util-decode-string/dev/index.js
var characterEscapeOrReference = /\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;
function decodeString(value) {
  return value.replace(characterEscapeOrReference, decode);
}
function decode($0, $1, $2) {
  if ($1) {
    return $1;
  }
  const head = $2.charCodeAt(0);
  if (head === codes.numberSign) {
    const head2 = $2.charCodeAt(1);
    const hex = head2 === codes.lowercaseX || head2 === codes.uppercaseX;
    return decodeNumericCharacterReference(
      $2.slice(hex ? 2 : 1),
      hex ? constants.numericBaseHexadecimal : constants.numericBaseDecimal
    );
  }
  return decodeNamedCharacterReference($2) || $0;
}

// node_modules/mdast-util-from-markdown/dev/lib/index.js
var own2 = {}.hasOwnProperty;
function fromMarkdown(value, encoding, options) {
  if (encoding && typeof encoding === "object") {
    options = encoding;
    encoding = void 0;
  }
  return compiler(options)(
    postprocess(
      parse(options).document().write(preprocess()(value, encoding, true))
    )
  );
}
function compiler(options) {
  const config = {
    transforms: [],
    canContainEols: ["emphasis", "fragment", "heading", "paragraph", "strong"],
    enter: {
      autolink: opener(link3),
      autolinkProtocol: onenterdata,
      autolinkEmail: onenterdata,
      atxHeading: opener(heading3),
      blockQuote: opener(blockQuote2),
      characterEscape: onenterdata,
      characterReference: onenterdata,
      codeFenced: opener(codeFlow),
      codeFencedFenceInfo: buffer,
      codeFencedFenceMeta: buffer,
      codeIndented: opener(codeFlow, buffer),
      codeText: opener(codeText2, buffer),
      codeTextData: onenterdata,
      data: onenterdata,
      codeFlowValue: onenterdata,
      definition: opener(definition3),
      definitionDestinationString: buffer,
      definitionLabelString: buffer,
      definitionTitleString: buffer,
      emphasis: opener(emphasis3),
      hardBreakEscape: opener(hardBreak3),
      hardBreakTrailing: opener(hardBreak3),
      htmlFlow: opener(html5, buffer),
      htmlFlowData: onenterdata,
      htmlText: opener(html5, buffer),
      htmlTextData: onenterdata,
      image: opener(image3),
      label: buffer,
      link: opener(link3),
      listItem: opener(listItem3),
      listItemValue: onenterlistitemvalue,
      listOrdered: opener(list4, onenterlistordered),
      listUnordered: opener(list4),
      paragraph: opener(paragraph3),
      reference: onenterreference,
      referenceString: buffer,
      resourceDestinationString: buffer,
      resourceTitleString: buffer,
      setextHeading: opener(heading3),
      strong: opener(strong3),
      thematicBreak: opener(thematicBreak4)
    },
    exit: {
      atxHeading: closer(),
      atxHeadingSequence: onexitatxheadingsequence,
      autolink: closer(),
      autolinkEmail: onexitautolinkemail,
      autolinkProtocol: onexitautolinkprotocol,
      blockQuote: closer(),
      characterEscapeValue: onexitdata,
      characterReferenceMarkerHexadecimal: onexitcharacterreferencemarker,
      characterReferenceMarkerNumeric: onexitcharacterreferencemarker,
      characterReferenceValue: onexitcharacterreferencevalue,
      characterReference: onexitcharacterreference,
      codeFenced: closer(onexitcodefenced),
      codeFencedFence: onexitcodefencedfence,
      codeFencedFenceInfo: onexitcodefencedfenceinfo,
      codeFencedFenceMeta: onexitcodefencedfencemeta,
      codeFlowValue: onexitdata,
      codeIndented: closer(onexitcodeindented),
      codeText: closer(onexitcodetext),
      codeTextData: onexitdata,
      data: onexitdata,
      definition: closer(),
      definitionDestinationString: onexitdefinitiondestinationstring,
      definitionLabelString: onexitdefinitionlabelstring,
      definitionTitleString: onexitdefinitiontitlestring,
      emphasis: closer(),
      hardBreakEscape: closer(onexithardbreak),
      hardBreakTrailing: closer(onexithardbreak),
      htmlFlow: closer(onexithtmlflow),
      htmlFlowData: onexitdata,
      htmlText: closer(onexithtmltext),
      htmlTextData: onexitdata,
      image: closer(onexitimage),
      label: onexitlabel,
      labelText: onexitlabeltext,
      lineEnding: onexitlineending,
      link: closer(onexitlink),
      listItem: closer(),
      listOrdered: closer(),
      listUnordered: closer(),
      paragraph: closer(),
      referenceString: onexitreferencestring,
      resourceDestinationString: onexitresourcedestinationstring,
      resourceTitleString: onexitresourcetitlestring,
      resource: onexitresource,
      setextHeading: closer(onexitsetextheading),
      setextHeadingLineSequence: onexitsetextheadinglinesequence,
      setextHeadingText: onexitsetextheadingtext,
      strong: closer(),
      thematicBreak: closer()
    }
  };
  configure(config, (options || {}).mdastExtensions || []);
  const data = {};
  return compile2;
  function compile2(events) {
    let tree = { type: "root", children: [] };
    const context = {
      stack: [tree],
      tokenStack: [],
      config,
      enter,
      exit: exit3,
      buffer,
      resume,
      data
    };
    const listStack = [];
    let index6 = -1;
    while (++index6 < events.length) {
      if (events[index6][1].type === types.listOrdered || events[index6][1].type === types.listUnordered) {
        if (events[index6][0] === "enter") {
          listStack.push(index6);
        } else {
          const tail = listStack.pop();
          ok(typeof tail === "number", "expected list to be open");
          index6 = prepareList(events, tail, index6);
        }
      }
    }
    index6 = -1;
    while (++index6 < events.length) {
      const handler = config[events[index6][0]];
      if (own2.call(handler, events[index6][1].type)) {
        handler[events[index6][1].type].call(
          Object.assign(
            { sliceSerialize: events[index6][2].sliceSerialize },
            context
          ),
          events[index6][1]
        );
      }
    }
    if (context.tokenStack.length > 0) {
      const tail = context.tokenStack[context.tokenStack.length - 1];
      const handler = tail[1] || defaultOnError;
      handler.call(context, void 0, tail[0]);
    }
    tree.position = {
      start: point3(
        events.length > 0 ? events[0][1].start : { line: 1, column: 1, offset: 0 }
      ),
      end: point3(
        events.length > 0 ? events[events.length - 2][1].end : { line: 1, column: 1, offset: 0 }
      )
    };
    index6 = -1;
    while (++index6 < config.transforms.length) {
      tree = config.transforms[index6](tree) || tree;
    }
    return tree;
  }
  function prepareList(events, start2, length) {
    let index6 = start2 - 1;
    let containerBalance = -1;
    let listSpread = false;
    let listItem4;
    let lineIndex;
    let firstBlankLineIndex;
    let atMarker;
    while (++index6 <= length) {
      const event = events[index6];
      switch (event[1].type) {
        case types.listUnordered:
        case types.listOrdered:
        case types.blockQuote: {
          if (event[0] === "enter") {
            containerBalance++;
          } else {
            containerBalance--;
          }
          atMarker = void 0;
          break;
        }
        case types.lineEndingBlank: {
          if (event[0] === "enter") {
            if (listItem4 && !atMarker && !containerBalance && !firstBlankLineIndex) {
              firstBlankLineIndex = index6;
            }
            atMarker = void 0;
          }
          break;
        }
        case types.linePrefix:
        case types.listItemValue:
        case types.listItemMarker:
        case types.listItemPrefix:
        case types.listItemPrefixWhitespace: {
          break;
        }
        default: {
          atMarker = void 0;
        }
      }
      if (!containerBalance && event[0] === "enter" && event[1].type === types.listItemPrefix || containerBalance === -1 && event[0] === "exit" && (event[1].type === types.listUnordered || event[1].type === types.listOrdered)) {
        if (listItem4) {
          let tailIndex = index6;
          lineIndex = void 0;
          while (tailIndex--) {
            const tailEvent = events[tailIndex];
            if (tailEvent[1].type === types.lineEnding || tailEvent[1].type === types.lineEndingBlank) {
              if (tailEvent[0] === "exit") continue;
              if (lineIndex) {
                events[lineIndex][1].type = types.lineEndingBlank;
                listSpread = true;
              }
              tailEvent[1].type = types.lineEnding;
              lineIndex = tailIndex;
            } else if (tailEvent[1].type === types.linePrefix || tailEvent[1].type === types.blockQuotePrefix || tailEvent[1].type === types.blockQuotePrefixWhitespace || tailEvent[1].type === types.blockQuoteMarker || tailEvent[1].type === types.listItemIndent) {
            } else {
              break;
            }
          }
          if (firstBlankLineIndex && (!lineIndex || firstBlankLineIndex < lineIndex)) {
            listItem4._spread = true;
          }
          listItem4.end = Object.assign(
            {},
            lineIndex ? events[lineIndex][1].start : event[1].end
          );
          events.splice(lineIndex || index6, 0, ["exit", listItem4, event[2]]);
          index6++;
          length++;
        }
        if (event[1].type === types.listItemPrefix) {
          const item = {
            type: "listItem",
            _spread: false,
            start: Object.assign({}, event[1].start),
            // @ts-expect-error: we’ll add `end` in a second.
            end: void 0
          };
          listItem4 = item;
          events.splice(index6, 0, ["enter", item, event[2]]);
          index6++;
          length++;
          firstBlankLineIndex = void 0;
          atMarker = true;
        }
      }
    }
    events[start2][1]._spread = listSpread;
    return length;
  }
  function opener(create2, and) {
    return open;
    function open(token) {
      enter.call(this, create2(token), token);
      if (and) and.call(this, token);
    }
  }
  function buffer() {
    this.stack.push({ type: "fragment", children: [] });
  }
  function enter(node2, token, errorHandler) {
    const parent = this.stack[this.stack.length - 1];
    ok(parent, "expected `parent`");
    ok("children" in parent, "expected `parent`");
    const siblings = parent.children;
    siblings.push(node2);
    this.stack.push(node2);
    this.tokenStack.push([token, errorHandler || void 0]);
    node2.position = {
      start: point3(token.start),
      // @ts-expect-error: `end` will be patched later.
      end: void 0
    };
  }
  function closer(and) {
    return close;
    function close(token) {
      if (and) and.call(this, token);
      exit3.call(this, token);
    }
  }
  function exit3(token, onExitError) {
    const node2 = this.stack.pop();
    ok(node2, "expected `node`");
    const open = this.tokenStack.pop();
    if (!open) {
      throw new Error(
        "Cannot close `" + token.type + "` (" + stringifyPosition({ start: token.start, end: token.end }) + "): it’s not open"
      );
    } else if (open[0].type !== token.type) {
      if (onExitError) {
        onExitError.call(this, token, open[0]);
      } else {
        const handler = open[1] || defaultOnError;
        handler.call(this, token, open[0]);
      }
    }
    ok(node2.type !== "fragment", "unexpected fragment `exit`ed");
    ok(node2.position, "expected `position` to be defined");
    node2.position.end = point3(token.end);
  }
  function resume() {
    return toString(this.stack.pop());
  }
  function onenterlistordered() {
    this.data.expectingFirstListItemValue = true;
  }
  function onenterlistitemvalue(token) {
    if (this.data.expectingFirstListItemValue) {
      const ancestor = this.stack[this.stack.length - 2];
      ok(ancestor, "expected nodes on stack");
      ok(ancestor.type === "list", "expected list on stack");
      ancestor.start = Number.parseInt(
        this.sliceSerialize(token),
        constants.numericBaseDecimal
      );
      this.data.expectingFirstListItemValue = void 0;
    }
  }
  function onexitcodefencedfenceinfo() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "code", "expected code on stack");
    node2.lang = data2;
  }
  function onexitcodefencedfencemeta() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "code", "expected code on stack");
    node2.meta = data2;
  }
  function onexitcodefencedfence() {
    if (this.data.flowCodeInside) return;
    this.buffer();
    this.data.flowCodeInside = true;
  }
  function onexitcodefenced() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "code", "expected code on stack");
    node2.value = data2.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g, "");
    this.data.flowCodeInside = void 0;
  }
  function onexitcodeindented() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "code", "expected code on stack");
    node2.value = data2.replace(/(\r?\n|\r)$/g, "");
  }
  function onexitdefinitionlabelstring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "definition", "expected definition on stack");
    node2.label = label;
    node2.identifier = normalizeIdentifier(
      this.sliceSerialize(token)
    ).toLowerCase();
  }
  function onexitdefinitiontitlestring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "definition", "expected definition on stack");
    node2.title = data2;
  }
  function onexitdefinitiondestinationstring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "definition", "expected definition on stack");
    node2.url = data2;
  }
  function onexitatxheadingsequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "heading", "expected heading on stack");
    if (!node2.depth) {
      const depth = this.sliceSerialize(token).length;
      ok(
        depth === 1 || depth === 2 || depth === 3 || depth === 4 || depth === 5 || depth === 6,
        "expected `depth` between `1` and `6`"
      );
      node2.depth = depth;
    }
  }
  function onexitsetextheadingtext() {
    this.data.setextHeadingSlurpLineEnding = true;
  }
  function onexitsetextheadinglinesequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "heading", "expected heading on stack");
    node2.depth = this.sliceSerialize(token).codePointAt(0) === codes.equalsTo ? 1 : 2;
  }
  function onexitsetextheading() {
    this.data.setextHeadingSlurpLineEnding = void 0;
  }
  function onenterdata(token) {
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok("children" in node2, "expected parent on stack");
    const siblings = node2.children;
    let tail = siblings[siblings.length - 1];
    if (!tail || tail.type !== "text") {
      tail = text8();
      tail.position = {
        start: point3(token.start),
        // @ts-expect-error: we’ll add `end` later.
        end: void 0
      };
      siblings.push(tail);
    }
    this.stack.push(tail);
  }
  function onexitdata(token) {
    const tail = this.stack.pop();
    ok(tail, "expected a `node` to be on the stack");
    ok("value" in tail, "expected a `literal` to be on the stack");
    ok(tail.position, "expected `node` to have an open position");
    tail.value += this.sliceSerialize(token);
    tail.position.end = point3(token.end);
  }
  function onexitlineending(token) {
    const context = this.stack[this.stack.length - 1];
    ok(context, "expected `node`");
    if (this.data.atHardBreak) {
      ok("children" in context, "expected `parent`");
      const tail = context.children[context.children.length - 1];
      ok(tail.position, "expected tail to have a starting position");
      tail.position.end = point3(token.end);
      this.data.atHardBreak = void 0;
      return;
    }
    if (!this.data.setextHeadingSlurpLineEnding && config.canContainEols.includes(context.type)) {
      onenterdata.call(this, token);
      onexitdata.call(this, token);
    }
  }
  function onexithardbreak() {
    this.data.atHardBreak = true;
  }
  function onexithtmlflow() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "html", "expected html on stack");
    node2.value = data2;
  }
  function onexithtmltext() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "html", "expected html on stack");
    node2.value = data2;
  }
  function onexitcodetext() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "inlineCode", "expected inline code on stack");
    node2.value = data2;
  }
  function onexitlink() {
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "link", "expected link on stack");
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitimage() {
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "image", "expected image on stack");
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitlabeltext(token) {
    const string3 = this.sliceSerialize(token);
    const ancestor = this.stack[this.stack.length - 2];
    ok(ancestor, "expected ancestor on stack");
    ok(
      ancestor.type === "image" || ancestor.type === "link",
      "expected image or link on stack"
    );
    ancestor.label = decodeString(string3);
    ancestor.identifier = normalizeIdentifier(string3).toLowerCase();
  }
  function onexitlabel() {
    const fragment = this.stack[this.stack.length - 1];
    ok(fragment, "expected node on stack");
    ok(fragment.type === "fragment", "expected fragment on stack");
    const value = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(
      node2.type === "image" || node2.type === "link",
      "expected image or link on stack"
    );
    this.data.inReference = true;
    if (node2.type === "link") {
      const children = fragment.children;
      node2.children = children;
    } else {
      node2.alt = value;
    }
  }
  function onexitresourcedestinationstring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(
      node2.type === "image" || node2.type === "link",
      "expected image or link on stack"
    );
    node2.url = data2;
  }
  function onexitresourcetitlestring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(
      node2.type === "image" || node2.type === "link",
      "expected image or link on stack"
    );
    node2.title = data2;
  }
  function onexitresource() {
    this.data.inReference = void 0;
  }
  function onenterreference() {
    this.data.referenceType = "collapsed";
  }
  function onexitreferencestring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(
      node2.type === "image" || node2.type === "link",
      "expected image reference or link reference on stack"
    );
    node2.label = label;
    node2.identifier = normalizeIdentifier(
      this.sliceSerialize(token)
    ).toLowerCase();
    this.data.referenceType = "full";
  }
  function onexitcharacterreferencemarker(token) {
    ok(
      token.type === "characterReferenceMarkerNumeric" || token.type === "characterReferenceMarkerHexadecimal"
    );
    this.data.characterReferenceType = token.type;
  }
  function onexitcharacterreferencevalue(token) {
    const data2 = this.sliceSerialize(token);
    const type = this.data.characterReferenceType;
    let value;
    if (type) {
      value = decodeNumericCharacterReference(
        data2,
        type === types.characterReferenceMarkerNumeric ? constants.numericBaseDecimal : constants.numericBaseHexadecimal
      );
      this.data.characterReferenceType = void 0;
    } else {
      const result = decodeNamedCharacterReference(data2);
      ok(result !== false, "expected reference to decode");
      value = result;
    }
    const tail = this.stack[this.stack.length - 1];
    ok(tail, "expected `node`");
    ok("value" in tail, "expected `node.value`");
    tail.value += value;
  }
  function onexitcharacterreference(token) {
    const tail = this.stack.pop();
    ok(tail, "expected `node`");
    ok(tail.position, "expected `node.position`");
    tail.position.end = point3(token.end);
  }
  function onexitautolinkprotocol(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "link", "expected link on stack");
    node2.url = this.sliceSerialize(token);
  }
  function onexitautolinkemail(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    ok(node2, "expected node on stack");
    ok(node2.type === "link", "expected link on stack");
    node2.url = "mailto:" + this.sliceSerialize(token);
  }
  function blockQuote2() {
    return { type: "blockquote", children: [] };
  }
  function codeFlow() {
    return { type: "code", lang: null, meta: null, value: "" };
  }
  function codeText2() {
    return { type: "inlineCode", value: "" };
  }
  function definition3() {
    return {
      type: "definition",
      identifier: "",
      label: null,
      title: null,
      url: ""
    };
  }
  function emphasis3() {
    return { type: "emphasis", children: [] };
  }
  function heading3() {
    return {
      type: "heading",
      // @ts-expect-error `depth` will be set later.
      depth: 0,
      children: []
    };
  }
  function hardBreak3() {
    return { type: "break" };
  }
  function html5() {
    return { type: "html", value: "" };
  }
  function image3() {
    return { type: "image", title: null, url: "", alt: null };
  }
  function link3() {
    return { type: "link", title: null, url: "", children: [] };
  }
  function list4(token) {
    return {
      type: "list",
      ordered: token.type === "listOrdered",
      start: null,
      spread: token._spread,
      children: []
    };
  }
  function listItem3(token) {
    return {
      type: "listItem",
      spread: token._spread,
      checked: null,
      children: []
    };
  }
  function paragraph3() {
    return { type: "paragraph", children: [] };
  }
  function strong3() {
    return { type: "strong", children: [] };
  }
  function text8() {
    return { type: "text", value: "" };
  }
  function thematicBreak4() {
    return { type: "thematicBreak" };
  }
}
function point3(d2) {
  return { line: d2.line, column: d2.column, offset: d2.offset };
}
function configure(combined, extensions) {
  let index6 = -1;
  while (++index6 < extensions.length) {
    const value = extensions[index6];
    if (Array.isArray(value)) {
      configure(combined, value);
    } else {
      extension(combined, value);
    }
  }
}
function extension(combined, extension2) {
  let key;
  for (key in extension2) {
    if (own2.call(extension2, key)) {
      switch (key) {
        case "canContainEols": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "transforms": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "enter":
        case "exit": {
          const right = extension2[key];
          if (right) {
            Object.assign(combined[key], right);
          }
          break;
        }
      }
    }
  }
}
function defaultOnError(left, right) {
  if (left) {
    throw new Error(
      "Cannot close `" + left.type + "` (" + stringifyPosition({ start: left.start, end: left.end }) + "): a different token (`" + right.type + "`, " + stringifyPosition({ start: right.start, end: right.end }) + ") is open"
    );
  } else {
    throw new Error(
      "Cannot close document, a token (`" + right.type + "`, " + stringifyPosition({ start: right.start, end: right.end }) + ") is still open"
    );
  }
}

// node_modules/remark-parse/lib/index.js
function remarkParse(options) {
  const self2 = this;
  self2.parser = parser;
  function parser(doc) {
    return fromMarkdown(doc, {
      ...self2.data("settings"),
      ...options,
      // Note: these options are not in the readme.
      // The goal is for them to be set by plugins on `data` instead of being
      // passed by users.
      extensions: self2.data("micromarkExtensions") || [],
      mdastExtensions: self2.data("fromMarkdownExtensions") || []
    });
  }
}

// node_modules/mdast-util-to-hast/lib/handlers/blockquote.js
function blockquote(state, node2) {
  const result = {
    type: "element",
    tagName: "blockquote",
    properties: {},
    children: state.wrap(state.all(node2), true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/break.js
function hardBreak(state, node2) {
  const result = { type: "element", tagName: "br", properties: {}, children: [] };
  state.patch(node2, result);
  return [state.applyData(node2, result), { type: "text", value: "\n" }];
}

// node_modules/mdast-util-to-hast/lib/handlers/code.js
function code(state, node2) {
  const value = node2.value ? node2.value + "\n" : "";
  const properties = {};
  const language = node2.lang ? node2.lang.split(/\s+/) : [];
  if (language.length > 0) {
    properties.className = ["language-" + language[0]];
  }
  let result = {
    type: "element",
    tagName: "code",
    properties,
    children: [{ type: "text", value }]
  };
  if (node2.meta) {
    result.data = { meta: node2.meta };
  }
  state.patch(node2, result);
  result = state.applyData(node2, result);
  result = { type: "element", tagName: "pre", properties: {}, children: [result] };
  state.patch(node2, result);
  return result;
}

// node_modules/mdast-util-to-hast/lib/handlers/delete.js
function strikethrough(state, node2) {
  const result = {
    type: "element",
    tagName: "del",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/emphasis.js
function emphasis(state, node2) {
  const result = {
    type: "element",
    tagName: "em",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/footnote-reference.js
function footnoteReference(state, node2) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const id3 = String(node2.identifier).toUpperCase();
  const safeId = normalizeUri(id3.toLowerCase());
  const index6 = state.footnoteOrder.indexOf(id3);
  let counter;
  let reuseCounter = state.footnoteCounts.get(id3);
  if (reuseCounter === void 0) {
    reuseCounter = 0;
    state.footnoteOrder.push(id3);
    counter = state.footnoteOrder.length;
  } else {
    counter = index6 + 1;
  }
  reuseCounter += 1;
  state.footnoteCounts.set(id3, reuseCounter);
  const link3 = {
    type: "element",
    tagName: "a",
    properties: {
      href: "#" + clobberPrefix + "fn-" + safeId,
      id: clobberPrefix + "fnref-" + safeId + (reuseCounter > 1 ? "-" + reuseCounter : ""),
      dataFootnoteRef: true,
      ariaDescribedBy: ["footnote-label"]
    },
    children: [{ type: "text", value: String(counter) }]
  };
  state.patch(node2, link3);
  const sup = {
    type: "element",
    tagName: "sup",
    properties: {},
    children: [link3]
  };
  state.patch(node2, sup);
  return state.applyData(node2, sup);
}

// node_modules/mdast-util-to-hast/lib/handlers/heading.js
function heading(state, node2) {
  const result = {
    type: "element",
    tagName: "h" + node2.depth,
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/html.js
function html3(state, node2) {
  if (state.options.allowDangerousHtml) {
    const result = { type: "raw", value: node2.value };
    state.patch(node2, result);
    return state.applyData(node2, result);
  }
  return void 0;
}

// node_modules/mdast-util-to-hast/lib/revert.js
function revert(state, node2) {
  const subtype = node2.referenceType;
  let suffix = "]";
  if (subtype === "collapsed") {
    suffix += "[]";
  } else if (subtype === "full") {
    suffix += "[" + (node2.label || node2.identifier) + "]";
  }
  if (node2.type === "imageReference") {
    return [{ type: "text", value: "![" + node2.alt + suffix }];
  }
  const contents = state.all(node2);
  const head = contents[0];
  if (head && head.type === "text") {
    head.value = "[" + head.value;
  } else {
    contents.unshift({ type: "text", value: "[" });
  }
  const tail = contents[contents.length - 1];
  if (tail && tail.type === "text") {
    tail.value += suffix;
  } else {
    contents.push({ type: "text", value: suffix });
  }
  return contents;
}

// node_modules/mdast-util-to-hast/lib/handlers/image-reference.js
function imageReference(state, node2) {
  const id3 = String(node2.identifier).toUpperCase();
  const definition3 = state.definitionById.get(id3);
  if (!definition3) {
    return revert(state, node2);
  }
  const properties = { src: normalizeUri(definition3.url || ""), alt: node2.alt };
  if (definition3.title !== null && definition3.title !== void 0) {
    properties.title = definition3.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/image.js
function image(state, node2) {
  const properties = { src: normalizeUri(node2.url) };
  if (node2.alt !== null && node2.alt !== void 0) {
    properties.alt = node2.alt;
  }
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/inline-code.js
function inlineCode(state, node2) {
  const text8 = { type: "text", value: node2.value.replace(/\r?\n|\r/g, " ") };
  state.patch(node2, text8);
  const result = {
    type: "element",
    tagName: "code",
    properties: {},
    children: [text8]
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/link-reference.js
function linkReference(state, node2) {
  const id3 = String(node2.identifier).toUpperCase();
  const definition3 = state.definitionById.get(id3);
  if (!definition3) {
    return revert(state, node2);
  }
  const properties = { href: normalizeUri(definition3.url || "") };
  if (definition3.title !== null && definition3.title !== void 0) {
    properties.title = definition3.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/link.js
function link(state, node2) {
  const properties = { href: normalizeUri(node2.url) };
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/list-item.js
function listItem(state, node2, parent) {
  const results = state.all(node2);
  const loose = parent ? listLoose(parent) : listItemLoose(node2);
  const properties = {};
  const children = [];
  if (typeof node2.checked === "boolean") {
    const head = results[0];
    let paragraph3;
    if (head && head.type === "element" && head.tagName === "p") {
      paragraph3 = head;
    } else {
      paragraph3 = { type: "element", tagName: "p", properties: {}, children: [] };
      results.unshift(paragraph3);
    }
    if (paragraph3.children.length > 0) {
      paragraph3.children.unshift({ type: "text", value: " " });
    }
    paragraph3.children.unshift({
      type: "element",
      tagName: "input",
      properties: { type: "checkbox", checked: node2.checked, disabled: true },
      children: []
    });
    properties.className = ["task-list-item"];
  }
  let index6 = -1;
  while (++index6 < results.length) {
    const child = results[index6];
    if (loose || index6 !== 0 || child.type !== "element" || child.tagName !== "p") {
      children.push({ type: "text", value: "\n" });
    }
    if (child.type === "element" && child.tagName === "p" && !loose) {
      children.push(...child.children);
    } else {
      children.push(child);
    }
  }
  const tail = results[results.length - 1];
  if (tail && (loose || tail.type !== "element" || tail.tagName !== "p")) {
    children.push({ type: "text", value: "\n" });
  }
  const result = { type: "element", tagName: "li", properties, children };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function listLoose(node2) {
  let loose = false;
  if (node2.type === "list") {
    loose = node2.spread || false;
    const children = node2.children;
    let index6 = -1;
    while (!loose && ++index6 < children.length) {
      loose = listItemLoose(children[index6]);
    }
  }
  return loose;
}
function listItemLoose(node2) {
  const spread = node2.spread;
  return spread === null || spread === void 0 ? node2.children.length > 1 : spread;
}

// node_modules/mdast-util-to-hast/lib/handlers/list.js
function list2(state, node2) {
  const properties = {};
  const results = state.all(node2);
  let index6 = -1;
  if (typeof node2.start === "number" && node2.start !== 1) {
    properties.start = node2.start;
  }
  while (++index6 < results.length) {
    const child = results[index6];
    if (child.type === "element" && child.tagName === "li" && child.properties && Array.isArray(child.properties.className) && child.properties.className.includes("task-list-item")) {
      properties.className = ["contains-task-list"];
      break;
    }
  }
  const result = {
    type: "element",
    tagName: node2.ordered ? "ol" : "ul",
    properties,
    children: state.wrap(results, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/paragraph.js
function paragraph(state, node2) {
  const result = {
    type: "element",
    tagName: "p",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/root.js
function root2(state, node2) {
  const result = { type: "root", children: state.wrap(state.all(node2)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/strong.js
function strong(state, node2) {
  const result = {
    type: "element",
    tagName: "strong",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/table.js
function table(state, node2) {
  const rows = state.all(node2);
  const firstRow = rows.shift();
  const tableContent = [];
  if (firstRow) {
    const head = {
      type: "element",
      tagName: "thead",
      properties: {},
      children: state.wrap([firstRow], true)
    };
    state.patch(node2.children[0], head);
    tableContent.push(head);
  }
  if (rows.length > 0) {
    const body = {
      type: "element",
      tagName: "tbody",
      properties: {},
      children: state.wrap(rows, true)
    };
    const start2 = pointStart(node2.children[1]);
    const end = pointEnd(node2.children[node2.children.length - 1]);
    if (start2 && end) body.position = { start: start2, end };
    tableContent.push(body);
  }
  const result = {
    type: "element",
    tagName: "table",
    properties: {},
    children: state.wrap(tableContent, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/table-row.js
function tableRow(state, node2, parent) {
  const siblings = parent ? parent.children : void 0;
  const rowIndex = siblings ? siblings.indexOf(node2) : 1;
  const tagName = rowIndex === 0 ? "th" : "td";
  const align = parent && parent.type === "table" ? parent.align : void 0;
  const length = align ? align.length : node2.children.length;
  let cellIndex = -1;
  const cells = [];
  while (++cellIndex < length) {
    const cell = node2.children[cellIndex];
    const properties = {};
    const alignValue = align ? align[cellIndex] : void 0;
    if (alignValue) {
      properties.align = alignValue;
    }
    let result2 = { type: "element", tagName, properties, children: [] };
    if (cell) {
      result2.children = state.all(cell);
      state.patch(cell, result2);
      result2 = state.applyData(cell, result2);
    }
    cells.push(result2);
  }
  const result = {
    type: "element",
    tagName: "tr",
    properties: {},
    children: state.wrap(cells, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/table-cell.js
function tableCell(state, node2) {
  const result = {
    type: "element",
    tagName: "td",
    // Assume body cell.
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/trim-lines/index.js
var tab = 9;
var space = 32;
function trimLines(value) {
  const source = String(value);
  const search2 = /\r?\n|\r/g;
  let match = search2.exec(source);
  let last = 0;
  const lines = [];
  while (match) {
    lines.push(
      trimLine(source.slice(last, match.index), last > 0, true),
      match[0]
    );
    last = match.index + match[0].length;
    match = search2.exec(source);
  }
  lines.push(trimLine(source.slice(last), last > 0, false));
  return lines.join("");
}
function trimLine(value, start2, end) {
  let startIndex = 0;
  let endIndex = value.length;
  if (start2) {
    let code4 = value.codePointAt(startIndex);
    while (code4 === tab || code4 === space) {
      startIndex++;
      code4 = value.codePointAt(startIndex);
    }
  }
  if (end) {
    let code4 = value.codePointAt(endIndex - 1);
    while (code4 === tab || code4 === space) {
      endIndex--;
      code4 = value.codePointAt(endIndex - 1);
    }
  }
  return endIndex > startIndex ? value.slice(startIndex, endIndex) : "";
}

// node_modules/mdast-util-to-hast/lib/handlers/text.js
function text4(state, node2) {
  const result = { type: "text", value: trimLines(String(node2.value)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/thematic-break.js
function thematicBreak2(state, node2) {
  const result = {
    type: "element",
    tagName: "hr",
    properties: {},
    children: []
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}

// node_modules/mdast-util-to-hast/lib/handlers/index.js
var handlers = {
  blockquote,
  break: hardBreak,
  code,
  delete: strikethrough,
  emphasis,
  footnoteReference,
  heading,
  html: html3,
  imageReference,
  image,
  inlineCode,
  linkReference,
  link,
  listItem,
  list: list2,
  paragraph,
  // @ts-expect-error: root is different, but hard to type.
  root: root2,
  strong,
  table,
  tableCell,
  tableRow,
  text: text4,
  thematicBreak: thematicBreak2,
  toml: ignore,
  yaml: ignore,
  definition: ignore,
  footnoteDefinition: ignore
};
function ignore() {
  return void 0;
}

// node_modules/@ungap/structured-clone/esm/types.js
var VOID = -1;
var PRIMITIVE = 0;
var ARRAY = 1;
var OBJECT = 2;
var DATE = 3;
var REGEXP = 4;
var MAP = 5;
var SET = 6;
var ERROR = 7;
var BIGINT = 8;

// node_modules/@ungap/structured-clone/esm/deserialize.js
var env = typeof self === "object" ? self : globalThis;
var deserializer = ($2, _2) => {
  const as = (out, index6) => {
    $2.set(index6, out);
    return out;
  };
  const unpair = (index6) => {
    if ($2.has(index6))
      return $2.get(index6);
    const [type, value] = _2[index6];
    switch (type) {
      case PRIMITIVE:
      case VOID:
        return as(value, index6);
      case ARRAY: {
        const arr = as([], index6);
        for (const index7 of value)
          arr.push(unpair(index7));
        return arr;
      }
      case OBJECT: {
        const object = as({}, index6);
        for (const [key, index7] of value)
          object[unpair(key)] = unpair(index7);
        return object;
      }
      case DATE:
        return as(new Date(value), index6);
      case REGEXP: {
        const { source, flags } = value;
        return as(new RegExp(source, flags), index6);
      }
      case MAP: {
        const map4 = as(/* @__PURE__ */ new Map(), index6);
        for (const [key, index7] of value)
          map4.set(unpair(key), unpair(index7));
        return map4;
      }
      case SET: {
        const set = as(/* @__PURE__ */ new Set(), index6);
        for (const index7 of value)
          set.add(unpair(index7));
        return set;
      }
      case ERROR: {
        const { name: name2, message } = value;
        return as(new env[name2](message), index6);
      }
      case BIGINT:
        return as(BigInt(value), index6);
      case "BigInt":
        return as(Object(BigInt(value)), index6);
      case "ArrayBuffer":
        return as(new Uint8Array(value).buffer, value);
      case "DataView": {
        const { buffer } = new Uint8Array(value);
        return as(new DataView(buffer), value);
      }
    }
    return as(new env[type](value), index6);
  };
  return unpair;
};
var deserialize = (serialized) => deserializer(/* @__PURE__ */ new Map(), serialized)(0);

// node_modules/@ungap/structured-clone/esm/serialize.js
var EMPTY = "";
var { toString: toString2 } = {};
var { keys } = Object;
var typeOf = (value) => {
  const type = typeof value;
  if (type !== "object" || !value)
    return [PRIMITIVE, type];
  const asString = toString2.call(value).slice(8, -1);
  switch (asString) {
    case "Array":
      return [ARRAY, EMPTY];
    case "Object":
      return [OBJECT, EMPTY];
    case "Date":
      return [DATE, EMPTY];
    case "RegExp":
      return [REGEXP, EMPTY];
    case "Map":
      return [MAP, EMPTY];
    case "Set":
      return [SET, EMPTY];
    case "DataView":
      return [ARRAY, asString];
  }
  if (asString.includes("Array"))
    return [ARRAY, asString];
  if (asString.includes("Error"))
    return [ERROR, asString];
  return [OBJECT, asString];
};
var shouldSkip = ([TYPE, type]) => TYPE === PRIMITIVE && (type === "function" || type === "symbol");
var serializer = (strict, json, $2, _2) => {
  const as = (out, value) => {
    const index6 = _2.push(out) - 1;
    $2.set(value, index6);
    return index6;
  };
  const pair = (value) => {
    if ($2.has(value))
      return $2.get(value);
    let [TYPE, type] = typeOf(value);
    switch (TYPE) {
      case PRIMITIVE: {
        let entry = value;
        switch (type) {
          case "bigint":
            TYPE = BIGINT;
            entry = value.toString();
            break;
          case "function":
          case "symbol":
            if (strict)
              throw new TypeError("unable to serialize " + type);
            entry = null;
            break;
          case "undefined":
            return as([VOID], value);
        }
        return as([TYPE, entry], value);
      }
      case ARRAY: {
        if (type) {
          let spread = value;
          if (type === "DataView") {
            spread = new Uint8Array(value.buffer);
          } else if (type === "ArrayBuffer") {
            spread = new Uint8Array(value);
          }
          return as([type, [...spread]], value);
        }
        const arr = [];
        const index6 = as([TYPE, arr], value);
        for (const entry of value)
          arr.push(pair(entry));
        return index6;
      }
      case OBJECT: {
        if (type) {
          switch (type) {
            case "BigInt":
              return as([type, value.toString()], value);
            case "Boolean":
            case "Number":
            case "String":
              return as([type, value.valueOf()], value);
          }
        }
        if (json && "toJSON" in value)
          return pair(value.toJSON());
        const entries = [];
        const index6 = as([TYPE, entries], value);
        for (const key of keys(value)) {
          if (strict || !shouldSkip(typeOf(value[key])))
            entries.push([pair(key), pair(value[key])]);
        }
        return index6;
      }
      case DATE:
        return as([TYPE, value.toISOString()], value);
      case REGEXP: {
        const { source, flags } = value;
        return as([TYPE, { source, flags }], value);
      }
      case MAP: {
        const entries = [];
        const index6 = as([TYPE, entries], value);
        for (const [key, entry] of value) {
          if (strict || !(shouldSkip(typeOf(key)) || shouldSkip(typeOf(entry))))
            entries.push([pair(key), pair(entry)]);
        }
        return index6;
      }
      case SET: {
        const entries = [];
        const index6 = as([TYPE, entries], value);
        for (const entry of value) {
          if (strict || !shouldSkip(typeOf(entry)))
            entries.push(pair(entry));
        }
        return index6;
      }
    }
    const { message } = value;
    return as([TYPE, { name: type, message }], value);
  };
  return pair;
};
var serialize = (value, { json, lossy } = {}) => {
  const _2 = [];
  return serializer(!(json || lossy), !!json, /* @__PURE__ */ new Map(), _2)(value), _2;
};

// node_modules/@ungap/structured-clone/esm/index.js
var esm_default = typeof structuredClone === "function" ? (
  /* c8 ignore start */
  (any, options) => options && ("json" in options || "lossy" in options) ? deserialize(serialize(any, options)) : structuredClone(any)
) : (any, options) => deserialize(serialize(any, options));

// node_modules/mdast-util-to-hast/lib/footer.js
function defaultFootnoteBackContent(_2, rereferenceIndex) {
  const result = [{ type: "text", value: "↩" }];
  if (rereferenceIndex > 1) {
    result.push({
      type: "element",
      tagName: "sup",
      properties: {},
      children: [{ type: "text", value: String(rereferenceIndex) }]
    });
  }
  return result;
}
function defaultFootnoteBackLabel(referenceIndex, rereferenceIndex) {
  return "Back to reference " + (referenceIndex + 1) + (rereferenceIndex > 1 ? "-" + rereferenceIndex : "");
}
function footer(state) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const footnoteBackContent = state.options.footnoteBackContent || defaultFootnoteBackContent;
  const footnoteBackLabel = state.options.footnoteBackLabel || defaultFootnoteBackLabel;
  const footnoteLabel = state.options.footnoteLabel || "Footnotes";
  const footnoteLabelTagName = state.options.footnoteLabelTagName || "h2";
  const footnoteLabelProperties = state.options.footnoteLabelProperties || {
    className: ["sr-only"]
  };
  const listItems = [];
  let referenceIndex = -1;
  while (++referenceIndex < state.footnoteOrder.length) {
    const definition3 = state.footnoteById.get(
      state.footnoteOrder[referenceIndex]
    );
    if (!definition3) {
      continue;
    }
    const content3 = state.all(definition3);
    const id3 = String(definition3.identifier).toUpperCase();
    const safeId = normalizeUri(id3.toLowerCase());
    let rereferenceIndex = 0;
    const backReferences = [];
    const counts = state.footnoteCounts.get(id3);
    while (counts !== void 0 && ++rereferenceIndex <= counts) {
      if (backReferences.length > 0) {
        backReferences.push({ type: "text", value: " " });
      }
      let children = typeof footnoteBackContent === "string" ? footnoteBackContent : footnoteBackContent(referenceIndex, rereferenceIndex);
      if (typeof children === "string") {
        children = { type: "text", value: children };
      }
      backReferences.push({
        type: "element",
        tagName: "a",
        properties: {
          href: "#" + clobberPrefix + "fnref-" + safeId + (rereferenceIndex > 1 ? "-" + rereferenceIndex : ""),
          dataFootnoteBackref: "",
          ariaLabel: typeof footnoteBackLabel === "string" ? footnoteBackLabel : footnoteBackLabel(referenceIndex, rereferenceIndex),
          className: ["data-footnote-backref"]
        },
        children: Array.isArray(children) ? children : [children]
      });
    }
    const tail = content3[content3.length - 1];
    if (tail && tail.type === "element" && tail.tagName === "p") {
      const tailTail = tail.children[tail.children.length - 1];
      if (tailTail && tailTail.type === "text") {
        tailTail.value += " ";
      } else {
        tail.children.push({ type: "text", value: " " });
      }
      tail.children.push(...backReferences);
    } else {
      content3.push(...backReferences);
    }
    const listItem3 = {
      type: "element",
      tagName: "li",
      properties: { id: clobberPrefix + "fn-" + safeId },
      children: state.wrap(content3, true)
    };
    state.patch(definition3, listItem3);
    listItems.push(listItem3);
  }
  if (listItems.length === 0) {
    return;
  }
  return {
    type: "element",
    tagName: "section",
    properties: { dataFootnotes: true, className: ["footnotes"] },
    children: [
      {
        type: "element",
        tagName: footnoteLabelTagName,
        properties: {
          ...esm_default(footnoteLabelProperties),
          id: "footnote-label"
        },
        children: [{ type: "text", value: footnoteLabel }]
      },
      { type: "text", value: "\n" },
      {
        type: "element",
        tagName: "ol",
        properties: {},
        children: state.wrap(listItems, true)
      },
      { type: "text", value: "\n" }
    ]
  };
}

// node_modules/unist-util-is/lib/index.js
var convert = (
  // Note: overloads in JSDoc can’t yet use different `@template`s.
  /**
   * @type {(
   *   (<Condition extends string>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & {type: Condition}) &
   *   (<Condition extends Props>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & Condition) &
   *   (<Condition extends TestFunction>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & Predicate<Condition, Node>) &
   *   ((test?: null | undefined) => (node?: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node) &
   *   ((test?: Test) => Check)
   * )}
   */
  /**
   * @param {Test} [test]
   * @returns {Check}
   */
  (function(test2) {
    if (test2 === null || test2 === void 0) {
      return ok2;
    }
    if (typeof test2 === "function") {
      return castFactory(test2);
    }
    if (typeof test2 === "object") {
      return Array.isArray(test2) ? anyFactory(test2) : (
        // Cast because `ReadonlyArray` goes into the above but `isArray`
        // narrows to `Array`.
        propertiesFactory(
          /** @type {Props} */
          test2
        )
      );
    }
    if (typeof test2 === "string") {
      return typeFactory(test2);
    }
    throw new Error("Expected function, string, or object as test");
  })
);
function anyFactory(tests) {
  const checks2 = [];
  let index6 = -1;
  while (++index6 < tests.length) {
    checks2[index6] = convert(tests[index6]);
  }
  return castFactory(any);
  function any(...parameters) {
    let index7 = -1;
    while (++index7 < checks2.length) {
      if (checks2[index7].apply(this, parameters)) return true;
    }
    return false;
  }
}
function propertiesFactory(check) {
  const checkAsRecord = (
    /** @type {Record<string, unknown>} */
    check
  );
  return castFactory(all2);
  function all2(node2) {
    const nodeAsRecord = (
      /** @type {Record<string, unknown>} */
      /** @type {unknown} */
      node2
    );
    let key;
    for (key in check) {
      if (nodeAsRecord[key] !== checkAsRecord[key]) return false;
    }
    return true;
  }
}
function typeFactory(check) {
  return castFactory(type);
  function type(node2) {
    return node2 && node2.type === check;
  }
}
function castFactory(testFunction) {
  return check;
  function check(value, index6, parent) {
    return Boolean(
      looksLikeANode(value) && testFunction.call(
        this,
        value,
        typeof index6 === "number" ? index6 : void 0,
        parent || void 0
      )
    );
  }
}
function ok2() {
  return true;
}
function looksLikeANode(value) {
  return value !== null && typeof value === "object" && "type" in value;
}

// node_modules/unist-util-visit-parents/lib/color.js
function color(d2) {
  return d2;
}

// node_modules/unist-util-visit-parents/lib/index.js
var empty2 = [];
var CONTINUE = true;
var EXIT = false;
var SKIP = "skip";
function visitParents(tree, test2, visitor2, reverse) {
  let check;
  if (typeof test2 === "function" && typeof visitor2 !== "function") {
    reverse = visitor2;
    visitor2 = test2;
  } else {
    check = test2;
  }
  const is2 = convert(check);
  const step = reverse ? -1 : 1;
  factory(tree, void 0, [])();
  function factory(node2, index6, parents) {
    const value = (
      /** @type {Record<string, unknown>} */
      node2 && typeof node2 === "object" ? node2 : {}
    );
    if (typeof value.type === "string") {
      const name2 = (
        // `hast`
        typeof value.tagName === "string" ? value.tagName : (
          // `xast`
          typeof value.name === "string" ? value.name : void 0
        )
      );
      Object.defineProperty(visit2, "name", {
        value: "node (" + color(node2.type + (name2 ? "<" + name2 + ">" : "")) + ")"
      });
    }
    return visit2;
    function visit2() {
      let result = empty2;
      let subresult;
      let offset4;
      let grandparents;
      if (!test2 || is2(node2, index6, parents[parents.length - 1] || void 0)) {
        result = toResult(visitor2(node2, parents));
        if (result[0] === EXIT) {
          return result;
        }
      }
      if ("children" in node2 && node2.children) {
        const nodeAsParent = (
          /** @type {UnistParent} */
          node2
        );
        if (nodeAsParent.children && result[0] !== SKIP) {
          offset4 = (reverse ? nodeAsParent.children.length : -1) + step;
          grandparents = parents.concat(nodeAsParent);
          while (offset4 > -1 && offset4 < nodeAsParent.children.length) {
            const child = nodeAsParent.children[offset4];
            subresult = factory(child, offset4, grandparents)();
            if (subresult[0] === EXIT) {
              return subresult;
            }
            offset4 = typeof subresult[1] === "number" ? subresult[1] : offset4 + step;
          }
        }
      }
      return result;
    }
  }
}
function toResult(value) {
  if (Array.isArray(value)) {
    return value;
  }
  if (typeof value === "number") {
    return [CONTINUE, value];
  }
  return value === null || value === void 0 ? empty2 : [value];
}

// node_modules/unist-util-visit/lib/index.js
function visit(tree, testOrVisitor, visitorOrReverse, maybeReverse) {
  let reverse;
  let test2;
  let visitor2;
  if (typeof testOrVisitor === "function" && typeof visitorOrReverse !== "function") {
    test2 = void 0;
    visitor2 = testOrVisitor;
    reverse = visitorOrReverse;
  } else {
    test2 = testOrVisitor;
    visitor2 = visitorOrReverse;
    reverse = maybeReverse;
  }
  visitParents(tree, test2, overload, reverse);
  function overload(node2, parents) {
    const parent = parents[parents.length - 1];
    const index6 = parent ? parent.children.indexOf(node2) : void 0;
    return visitor2(node2, index6, parent);
  }
}

// node_modules/mdast-util-to-hast/lib/state.js
var own3 = {}.hasOwnProperty;
var emptyOptions3 = {};
function createState(tree, options) {
  const settings = options || emptyOptions3;
  const definitionById = /* @__PURE__ */ new Map();
  const footnoteById = /* @__PURE__ */ new Map();
  const footnoteCounts = /* @__PURE__ */ new Map();
  const handlers2 = { ...handlers, ...settings.handlers };
  const state = {
    all: all2,
    applyData,
    definitionById,
    footnoteById,
    footnoteCounts,
    footnoteOrder: [],
    handlers: handlers2,
    one: one3,
    options: settings,
    patch,
    wrap
  };
  visit(tree, function(node2) {
    if (node2.type === "definition" || node2.type === "footnoteDefinition") {
      const map4 = node2.type === "definition" ? definitionById : footnoteById;
      const id3 = String(node2.identifier).toUpperCase();
      if (!map4.has(id3)) {
        map4.set(id3, node2);
      }
    }
  });
  return state;
  function one3(node2, parent) {
    const type = node2.type;
    const handle2 = state.handlers[type];
    if (own3.call(state.handlers, type) && handle2) {
      return handle2(state, node2, parent);
    }
    if (state.options.passThrough && state.options.passThrough.includes(type)) {
      if ("children" in node2) {
        const { children, ...shallow } = node2;
        const result = esm_default(shallow);
        result.children = state.all(node2);
        return result;
      }
      return esm_default(node2);
    }
    const unknown = state.options.unknownHandler || defaultUnknownHandler;
    return unknown(state, node2, parent);
  }
  function all2(parent) {
    const values2 = [];
    if ("children" in parent) {
      const nodes = parent.children;
      let index6 = -1;
      while (++index6 < nodes.length) {
        const result = state.one(nodes[index6], parent);
        if (result) {
          if (index6 && nodes[index6 - 1].type === "break") {
            if (!Array.isArray(result) && result.type === "text") {
              result.value = trimMarkdownSpaceStart(result.value);
            }
            if (!Array.isArray(result) && result.type === "element") {
              const head = result.children[0];
              if (head && head.type === "text") {
                head.value = trimMarkdownSpaceStart(head.value);
              }
            }
          }
          if (Array.isArray(result)) {
            values2.push(...result);
          } else {
            values2.push(result);
          }
        }
      }
    }
    return values2;
  }
}
function patch(from, to) {
  if (from.position) to.position = position(from);
}
function applyData(from, to) {
  let result = to;
  if (from && from.data) {
    const hName = from.data.hName;
    const hChildren = from.data.hChildren;
    const hProperties = from.data.hProperties;
    if (typeof hName === "string") {
      if (result.type === "element") {
        result.tagName = hName;
      } else {
        const children = "children" in result ? result.children : [result];
        result = { type: "element", tagName: hName, properties: {}, children };
      }
    }
    if (result.type === "element" && hProperties) {
      Object.assign(result.properties, esm_default(hProperties));
    }
    if ("children" in result && result.children && hChildren !== null && hChildren !== void 0) {
      result.children = hChildren;
    }
  }
  return result;
}
function defaultUnknownHandler(state, node2) {
  const data = node2.data || {};
  const result = "value" in node2 && !(own3.call(data, "hProperties") || own3.call(data, "hChildren")) ? { type: "text", value: node2.value } : {
    type: "element",
    tagName: "div",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function wrap(nodes, loose) {
  const result = [];
  let index6 = -1;
  if (loose) {
    result.push({ type: "text", value: "\n" });
  }
  while (++index6 < nodes.length) {
    if (index6) result.push({ type: "text", value: "\n" });
    result.push(nodes[index6]);
  }
  if (loose && nodes.length > 0) {
    result.push({ type: "text", value: "\n" });
  }
  return result;
}
function trimMarkdownSpaceStart(value) {
  let index6 = 0;
  let code4 = value.charCodeAt(index6);
  while (code4 === 9 || code4 === 32) {
    index6++;
    code4 = value.charCodeAt(index6);
  }
  return value.slice(index6);
}

// node_modules/mdast-util-to-hast/lib/index.js
function toHast(tree, options) {
  const state = createState(tree, options);
  const node2 = state.one(tree, void 0);
  const foot = footer(state);
  const result = Array.isArray(node2) ? { type: "root", children: node2 } : node2 || { type: "root", children: [] };
  if (foot) {
    ok("children" in result);
    result.children.push({ type: "text", value: "\n" }, foot);
  }
  return result;
}

// node_modules/remark-rehype/lib/index.js
function remarkRehype(destination, options) {
  if (destination && "run" in destination) {
    return async function(tree, file) {
      const hastTree = (
        /** @type {HastRoot} */
        toHast(tree, { file, ...options })
      );
      await destination.run(hastTree, file);
    };
  }
  return function(tree, file) {
    return (
      /** @type {HastRoot} */
      toHast(tree, { file, ...destination || options })
    );
  };
}

// node_modules/bail/index.js
function bail(error2) {
  if (error2) {
    throw error2;
  }
}

// node_modules/unified/lib/index.js
var import_extend = __toESM(require_extend(), 1);

// node_modules/is-plain-obj/index.js
function isPlainObject(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const prototype = Object.getPrototypeOf(value);
  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in value) && !(Symbol.iterator in value);
}

// node_modules/trough/lib/index.js
function trough() {
  const fns = [];
  const pipeline = { run, use: use2 };
  return pipeline;
  function run(...values2) {
    let middlewareIndex = -1;
    const callback = values2.pop();
    if (typeof callback !== "function") {
      throw new TypeError("Expected function as last argument, not " + callback);
    }
    next(null, ...values2);
    function next(error2, ...output) {
      const fn = fns[++middlewareIndex];
      let index6 = -1;
      if (error2) {
        callback(error2);
        return;
      }
      while (++index6 < values2.length) {
        if (output[index6] === null || output[index6] === void 0) {
          output[index6] = values2[index6];
        }
      }
      values2 = output;
      if (fn) {
        wrap2(fn, next)(...output);
      } else {
        callback(null, ...output);
      }
    }
  }
  function use2(middelware) {
    if (typeof middelware !== "function") {
      throw new TypeError(
        "Expected `middelware` to be a function, not " + middelware
      );
    }
    fns.push(middelware);
    return pipeline;
  }
}
function wrap2(middleware, callback) {
  let called;
  return wrapped;
  function wrapped(...parameters) {
    const fnExpectsCallback = middleware.length > parameters.length;
    let result;
    if (fnExpectsCallback) {
      parameters.push(done);
    }
    try {
      result = middleware.apply(this, parameters);
    } catch (error2) {
      const exception = (
        /** @type {Error} */
        error2
      );
      if (fnExpectsCallback && called) {
        throw exception;
      }
      return done(exception);
    }
    if (!fnExpectsCallback) {
      if (result && result.then && typeof result.then === "function") {
        result.then(then, done);
      } else if (result instanceof Error) {
        done(result);
      } else {
        then(result);
      }
    }
  }
  function done(error2, ...output) {
    if (!called) {
      called = true;
      callback(error2, ...output);
    }
  }
  function then(value) {
    done(null, value);
  }
}

// node_modules/vfile/lib/minpath.browser.js
var minpath = { basename, dirname, extname, join, sep: "/" };
function basename(path2, extname2) {
  if (extname2 !== void 0 && typeof extname2 !== "string") {
    throw new TypeError('"ext" argument must be a string');
  }
  assertPath(path2);
  let start2 = 0;
  let end = -1;
  let index6 = path2.length;
  let seenNonSlash;
  if (extname2 === void 0 || extname2.length === 0 || extname2.length > path2.length) {
    while (index6--) {
      if (path2.codePointAt(index6) === 47) {
        if (seenNonSlash) {
          start2 = index6 + 1;
          break;
        }
      } else if (end < 0) {
        seenNonSlash = true;
        end = index6 + 1;
      }
    }
    return end < 0 ? "" : path2.slice(start2, end);
  }
  if (extname2 === path2) {
    return "";
  }
  let firstNonSlashEnd = -1;
  let extnameIndex = extname2.length - 1;
  while (index6--) {
    if (path2.codePointAt(index6) === 47) {
      if (seenNonSlash) {
        start2 = index6 + 1;
        break;
      }
    } else {
      if (firstNonSlashEnd < 0) {
        seenNonSlash = true;
        firstNonSlashEnd = index6 + 1;
      }
      if (extnameIndex > -1) {
        if (path2.codePointAt(index6) === extname2.codePointAt(extnameIndex--)) {
          if (extnameIndex < 0) {
            end = index6;
          }
        } else {
          extnameIndex = -1;
          end = firstNonSlashEnd;
        }
      }
    }
  }
  if (start2 === end) {
    end = firstNonSlashEnd;
  } else if (end < 0) {
    end = path2.length;
  }
  return path2.slice(start2, end);
}
function dirname(path2) {
  assertPath(path2);
  if (path2.length === 0) {
    return ".";
  }
  let end = -1;
  let index6 = path2.length;
  let unmatchedSlash;
  while (--index6) {
    if (path2.codePointAt(index6) === 47) {
      if (unmatchedSlash) {
        end = index6;
        break;
      }
    } else if (!unmatchedSlash) {
      unmatchedSlash = true;
    }
  }
  return end < 0 ? path2.codePointAt(0) === 47 ? "/" : "." : end === 1 && path2.codePointAt(0) === 47 ? "//" : path2.slice(0, end);
}
function extname(path2) {
  assertPath(path2);
  let index6 = path2.length;
  let end = -1;
  let startPart = 0;
  let startDot = -1;
  let preDotState = 0;
  let unmatchedSlash;
  while (index6--) {
    const code4 = path2.codePointAt(index6);
    if (code4 === 47) {
      if (unmatchedSlash) {
        startPart = index6 + 1;
        break;
      }
      continue;
    }
    if (end < 0) {
      unmatchedSlash = true;
      end = index6 + 1;
    }
    if (code4 === 46) {
      if (startDot < 0) {
        startDot = index6;
      } else if (preDotState !== 1) {
        preDotState = 1;
      }
    } else if (startDot > -1) {
      preDotState = -1;
    }
  }
  if (startDot < 0 || end < 0 || // We saw a non-dot character immediately before the dot.
  preDotState === 0 || // The (right-most) trimmed path component is exactly `..`.
  preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
    return "";
  }
  return path2.slice(startDot, end);
}
function join(...segments) {
  let index6 = -1;
  let joined;
  while (++index6 < segments.length) {
    assertPath(segments[index6]);
    if (segments[index6]) {
      joined = joined === void 0 ? segments[index6] : joined + "/" + segments[index6];
    }
  }
  return joined === void 0 ? "." : normalize2(joined);
}
function normalize2(path2) {
  assertPath(path2);
  const absolute = path2.codePointAt(0) === 47;
  let value = normalizeString(path2, !absolute);
  if (value.length === 0 && !absolute) {
    value = ".";
  }
  if (value.length > 0 && path2.codePointAt(path2.length - 1) === 47) {
    value += "/";
  }
  return absolute ? "/" + value : value;
}
function normalizeString(path2, allowAboveRoot) {
  let result = "";
  let lastSegmentLength = 0;
  let lastSlash = -1;
  let dots = 0;
  let index6 = -1;
  let code4;
  let lastSlashIndex;
  while (++index6 <= path2.length) {
    if (index6 < path2.length) {
      code4 = path2.codePointAt(index6);
    } else if (code4 === 47) {
      break;
    } else {
      code4 = 47;
    }
    if (code4 === 47) {
      if (lastSlash === index6 - 1 || dots === 1) {
      } else if (lastSlash !== index6 - 1 && dots === 2) {
        if (result.length < 2 || lastSegmentLength !== 2 || result.codePointAt(result.length - 1) !== 46 || result.codePointAt(result.length - 2) !== 46) {
          if (result.length > 2) {
            lastSlashIndex = result.lastIndexOf("/");
            if (lastSlashIndex !== result.length - 1) {
              if (lastSlashIndex < 0) {
                result = "";
                lastSegmentLength = 0;
              } else {
                result = result.slice(0, lastSlashIndex);
                lastSegmentLength = result.length - 1 - result.lastIndexOf("/");
              }
              lastSlash = index6;
              dots = 0;
              continue;
            }
          } else if (result.length > 0) {
            result = "";
            lastSegmentLength = 0;
            lastSlash = index6;
            dots = 0;
            continue;
          }
        }
        if (allowAboveRoot) {
          result = result.length > 0 ? result + "/.." : "..";
          lastSegmentLength = 2;
        }
      } else {
        if (result.length > 0) {
          result += "/" + path2.slice(lastSlash + 1, index6);
        } else {
          result = path2.slice(lastSlash + 1, index6);
        }
        lastSegmentLength = index6 - lastSlash - 1;
      }
      lastSlash = index6;
      dots = 0;
    } else if (code4 === 46 && dots > -1) {
      dots++;
    } else {
      dots = -1;
    }
  }
  return result;
}
function assertPath(path2) {
  if (typeof path2 !== "string") {
    throw new TypeError(
      "Path must be a string. Received " + JSON.stringify(path2)
    );
  }
}

// node_modules/vfile/lib/minproc.browser.js
var minproc = { cwd };
function cwd() {
  return "/";
}

// node_modules/vfile/lib/minurl.shared.js
function isUrl(fileUrlOrPath) {
  return Boolean(
    fileUrlOrPath !== null && typeof fileUrlOrPath === "object" && "href" in fileUrlOrPath && fileUrlOrPath.href && "protocol" in fileUrlOrPath && fileUrlOrPath.protocol && // @ts-expect-error: indexing is fine.
    fileUrlOrPath.auth === void 0
  );
}

// node_modules/vfile/lib/minurl.browser.js
function urlToPath(path2) {
  if (typeof path2 === "string") {
    path2 = new URL(path2);
  } else if (!isUrl(path2)) {
    const error2 = new TypeError(
      'The "path" argument must be of type string or an instance of URL. Received `' + path2 + "`"
    );
    error2.code = "ERR_INVALID_ARG_TYPE";
    throw error2;
  }
  if (path2.protocol !== "file:") {
    const error2 = new TypeError("The URL must be of scheme file");
    error2.code = "ERR_INVALID_URL_SCHEME";
    throw error2;
  }
  return getPathFromURLPosix(path2);
}
function getPathFromURLPosix(url) {
  if (url.hostname !== "") {
    const error2 = new TypeError(
      'File URL host must be "localhost" or empty on darwin'
    );
    error2.code = "ERR_INVALID_FILE_URL_HOST";
    throw error2;
  }
  const pathname = url.pathname;
  let index6 = -1;
  while (++index6 < pathname.length) {
    if (pathname.codePointAt(index6) === 37 && pathname.codePointAt(index6 + 1) === 50) {
      const third = pathname.codePointAt(index6 + 2);
      if (third === 70 || third === 102) {
        const error2 = new TypeError(
          "File URL path must not include encoded / characters"
        );
        error2.code = "ERR_INVALID_FILE_URL_PATH";
        throw error2;
      }
    }
  }
  return decodeURIComponent(pathname);
}

// node_modules/vfile/lib/index.js
var order = (
  /** @type {const} */
  [
    "history",
    "path",
    "basename",
    "stem",
    "extname",
    "dirname"
  ]
);
var VFile = class {
  /**
   * Create a new virtual file.
   *
   * `options` is treated as:
   *
   * *   `string` or `Uint8Array` — `{value: options}`
   * *   `URL` — `{path: options}`
   * *   `VFile` — shallow copies its data over to the new file
   * *   `object` — all fields are shallow copied over to the new file
   *
   * Path related fields are set in the following order (least specific to
   * most specific): `history`, `path`, `basename`, `stem`, `extname`,
   * `dirname`.
   *
   * You cannot set `dirname` or `extname` without setting either `history`,
   * `path`, `basename`, or `stem` too.
   *
   * @param {Compatible | null | undefined} [value]
   *   File value.
   * @returns
   *   New instance.
   */
  constructor(value) {
    let options;
    if (!value) {
      options = {};
    } else if (isUrl(value)) {
      options = { path: value };
    } else if (typeof value === "string" || isUint8Array(value)) {
      options = { value };
    } else {
      options = value;
    }
    this.cwd = "cwd" in options ? "" : minproc.cwd();
    this.data = {};
    this.history = [];
    this.messages = [];
    this.value;
    this.map;
    this.result;
    this.stored;
    let index6 = -1;
    while (++index6 < order.length) {
      const field2 = order[index6];
      if (field2 in options && options[field2] !== void 0 && options[field2] !== null) {
        this[field2] = field2 === "history" ? [...options[field2]] : options[field2];
      }
    }
    let field;
    for (field in options) {
      if (!order.includes(field)) {
        this[field] = options[field];
      }
    }
  }
  /**
   * Get the basename (including extname) (example: `'index.min.js'`).
   *
   * @returns {string | undefined}
   *   Basename.
   */
  get basename() {
    return typeof this.path === "string" ? minpath.basename(this.path) : void 0;
  }
  /**
   * Set basename (including extname) (`'index.min.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} basename
   *   Basename.
   * @returns {undefined}
   *   Nothing.
   */
  set basename(basename2) {
    assertNonEmpty(basename2, "basename");
    assertPart(basename2, "basename");
    this.path = minpath.join(this.dirname || "", basename2);
  }
  /**
   * Get the parent path (example: `'~'`).
   *
   * @returns {string | undefined}
   *   Dirname.
   */
  get dirname() {
    return typeof this.path === "string" ? minpath.dirname(this.path) : void 0;
  }
  /**
   * Set the parent path (example: `'~'`).
   *
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} dirname
   *   Dirname.
   * @returns {undefined}
   *   Nothing.
   */
  set dirname(dirname2) {
    assertPath2(this.basename, "dirname");
    this.path = minpath.join(dirname2 || "", this.basename);
  }
  /**
   * Get the extname (including dot) (example: `'.js'`).
   *
   * @returns {string | undefined}
   *   Extname.
   */
  get extname() {
    return typeof this.path === "string" ? minpath.extname(this.path) : void 0;
  }
  /**
   * Set the extname (including dot) (example: `'.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} extname
   *   Extname.
   * @returns {undefined}
   *   Nothing.
   */
  set extname(extname2) {
    assertPart(extname2, "extname");
    assertPath2(this.dirname, "extname");
    if (extname2) {
      if (extname2.codePointAt(0) !== 46) {
        throw new Error("`extname` must start with `.`");
      }
      if (extname2.includes(".", 1)) {
        throw new Error("`extname` cannot contain multiple dots");
      }
    }
    this.path = minpath.join(this.dirname, this.stem + (extname2 || ""));
  }
  /**
   * Get the full path (example: `'~/index.min.js'`).
   *
   * @returns {string}
   *   Path.
   */
  get path() {
    return this.history[this.history.length - 1];
  }
  /**
   * Set the full path (example: `'~/index.min.js'`).
   *
   * Cannot be nullified.
   * You can set a file URL (a `URL` object with a `file:` protocol) which will
   * be turned into a path with `url.fileURLToPath`.
   *
   * @param {URL | string} path
   *   Path.
   * @returns {undefined}
   *   Nothing.
   */
  set path(path2) {
    if (isUrl(path2)) {
      path2 = urlToPath(path2);
    }
    assertNonEmpty(path2, "path");
    if (this.path !== path2) {
      this.history.push(path2);
    }
  }
  /**
   * Get the stem (basename w/o extname) (example: `'index.min'`).
   *
   * @returns {string | undefined}
   *   Stem.
   */
  get stem() {
    return typeof this.path === "string" ? minpath.basename(this.path, this.extname) : void 0;
  }
  /**
   * Set the stem (basename w/o extname) (example: `'index.min'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} stem
   *   Stem.
   * @returns {undefined}
   *   Nothing.
   */
  set stem(stem) {
    assertNonEmpty(stem, "stem");
    assertPart(stem, "stem");
    this.path = minpath.join(this.dirname || "", stem + (this.extname || ""));
  }
  // Normal prototypal methods.
  /**
   * Create a fatal message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `true` (error; file not usable)
   * and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {never}
   *   Never.
   * @throws {VFileMessage}
   *   Message.
   */
  fail(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = true;
    throw message;
  }
  /**
   * Create an info message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `undefined` (info; change
   * likely not needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  info(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = void 0;
    return message;
  }
  /**
   * Create a message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `false` (warning; change may be
   * needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  message(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = new VFileMessage(
      // @ts-expect-error: the overloads are fine.
      causeOrReason,
      optionsOrParentOrPlace,
      origin
    );
    if (this.path) {
      message.name = this.path + ":" + message.name;
      message.file = this.path;
    }
    message.fatal = false;
    this.messages.push(message);
    return message;
  }
  /**
   * Serialize the file.
   *
   * > **Note**: which encodings are supported depends on the engine.
   * > For info on Node.js, see:
   * > <https://nodejs.org/api/util.html#whatwg-supported-encodings>.
   *
   * @param {string | null | undefined} [encoding='utf8']
   *   Character encoding to understand `value` as when it’s a `Uint8Array`
   *   (default: `'utf-8'`).
   * @returns {string}
   *   Serialized file.
   */
  toString(encoding) {
    if (this.value === void 0) {
      return "";
    }
    if (typeof this.value === "string") {
      return this.value;
    }
    const decoder = new TextDecoder(encoding || void 0);
    return decoder.decode(this.value);
  }
};
function assertPart(part, name2) {
  if (part && part.includes(minpath.sep)) {
    throw new Error(
      "`" + name2 + "` cannot be a path: did not expect `" + minpath.sep + "`"
    );
  }
}
function assertNonEmpty(part, name2) {
  if (!part) {
    throw new Error("`" + name2 + "` cannot be empty");
  }
}
function assertPath2(path2, name2) {
  if (!path2) {
    throw new Error("Setting `" + name2 + "` requires `path` to be set too");
  }
}
function isUint8Array(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}

// node_modules/unified/lib/callable-instance.js
var CallableInstance = (
  /**
   * @type {new <Parameters extends Array<unknown>, Result>(property: string | symbol) => (...parameters: Parameters) => Result}
   */
  /** @type {unknown} */
  /**
   * @this {Function}
   * @param {string | symbol} property
   * @returns {(...parameters: Array<unknown>) => unknown}
   */
  (function(property) {
    const self2 = this;
    const constr = self2.constructor;
    const proto = (
      /** @type {Record<string | symbol, Function>} */
      // Prototypes do exist.
      // type-coverage:ignore-next-line
      constr.prototype
    );
    const value = proto[property];
    const apply = function() {
      return value.apply(apply, arguments);
    };
    Object.setPrototypeOf(apply, proto);
    return apply;
  })
);

// node_modules/unified/lib/index.js
var own4 = {}.hasOwnProperty;
var Processor = class _Processor extends CallableInstance {
  /**
   * Create a processor.
   */
  constructor() {
    super("copy");
    this.Compiler = void 0;
    this.Parser = void 0;
    this.attachers = [];
    this.compiler = void 0;
    this.freezeIndex = -1;
    this.frozen = void 0;
    this.namespace = {};
    this.parser = void 0;
    this.transformers = trough();
  }
  /**
   * Copy a processor.
   *
   * @deprecated
   *   This is a private internal method and should not be used.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   New *unfrozen* processor ({@linkcode Processor}) that is
   *   configured to work the same as its ancestor.
   *   When the descendant processor is configured in the future it does not
   *   affect the ancestral processor.
   */
  copy() {
    const destination = (
      /** @type {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>} */
      new _Processor()
    );
    let index6 = -1;
    while (++index6 < this.attachers.length) {
      const attacher = this.attachers[index6];
      destination.use(...attacher);
    }
    destination.data((0, import_extend.default)(true, {}, this.namespace));
    return destination;
  }
  /**
   * Configure the processor with info available to all plugins.
   * Information is stored in an object.
   *
   * Typically, options can be given to a specific plugin, but sometimes it
   * makes sense to have information shared with several plugins.
   * For example, a list of HTML elements that are self-closing, which is
   * needed during all phases.
   *
   * > **Note**: setting information cannot occur on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * > **Note**: to register custom data in TypeScript, augment the
   * > {@linkcode Data} interface.
   *
   * @example
   *   This example show how to get and set info:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   const processor = unified().data('alpha', 'bravo')
   *
   *   processor.data('alpha') // => 'bravo'
   *
   *   processor.data() // => {alpha: 'bravo'}
   *
   *   processor.data({charlie: 'delta'})
   *
   *   processor.data() // => {charlie: 'delta'}
   *   ```
   *
   * @template {keyof Data} Key
   *
   * @overload
   * @returns {Data}
   *
   * @overload
   * @param {Data} dataset
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Key} key
   * @returns {Data[Key]}
   *
   * @overload
   * @param {Key} key
   * @param {Data[Key]} value
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @param {Data | Key} [key]
   *   Key to get or set, or entire dataset to set, or nothing to get the
   *   entire dataset (optional).
   * @param {Data[Key]} [value]
   *   Value to set (optional).
   * @returns {unknown}
   *   The current processor when setting, the value at `key` when getting, or
   *   the entire dataset when getting without key.
   */
  data(key, value) {
    if (typeof key === "string") {
      if (arguments.length === 2) {
        assertUnfrozen("data", this.frozen);
        this.namespace[key] = value;
        return this;
      }
      return own4.call(this.namespace, key) && this.namespace[key] || void 0;
    }
    if (key) {
      assertUnfrozen("data", this.frozen);
      this.namespace = key;
      return this;
    }
    return this.namespace;
  }
  /**
   * Freeze a processor.
   *
   * Frozen processors are meant to be extended and not to be configured
   * directly.
   *
   * When a processor is frozen it cannot be unfrozen.
   * New processors working the same way can be created by calling the
   * processor.
   *
   * It’s possible to freeze processors explicitly by calling `.freeze()`.
   * Processors freeze automatically when `.parse()`, `.run()`, `.runSync()`,
   * `.stringify()`, `.process()`, or `.processSync()` are called.
   *
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   The current processor.
   */
  freeze() {
    if (this.frozen) {
      return this;
    }
    const self2 = (
      /** @type {Processor} */
      /** @type {unknown} */
      this
    );
    while (++this.freezeIndex < this.attachers.length) {
      const [attacher, ...options] = this.attachers[this.freezeIndex];
      if (options[0] === false) {
        continue;
      }
      if (options[0] === true) {
        options[0] = void 0;
      }
      const transformer = attacher.call(self2, ...options);
      if (typeof transformer === "function") {
        this.transformers.use(transformer);
      }
    }
    this.frozen = true;
    this.freezeIndex = Number.POSITIVE_INFINITY;
    return this;
  }
  /**
   * Parse text to a syntax tree.
   *
   * > **Note**: `parse` freezes the processor if not already *frozen*.
   *
   * > **Note**: `parse` performs the parse phase, not the run phase or other
   * > phases.
   *
   * @param {Compatible | undefined} [file]
   *   file to parse (optional); typically `string` or `VFile`; any value
   *   accepted as `x` in `new VFile(x)`.
   * @returns {ParseTree extends undefined ? Node : ParseTree}
   *   Syntax tree representing `file`.
   */
  parse(file) {
    this.freeze();
    const realFile = vfile(file);
    const parser = this.parser || this.Parser;
    assertParser("parse", parser);
    return parser(String(realFile), realFile);
  }
  /**
   * Process the given file as configured on the processor.
   *
   * > **Note**: `process` freezes the processor if not already *frozen*.
   *
   * > **Note**: `process` performs the parse, run, and stringify phases.
   *
   * @overload
   * @param {Compatible | undefined} file
   * @param {ProcessCallback<VFileWithOutput<CompileResult>>} done
   * @returns {undefined}
   *
   * @overload
   * @param {Compatible | undefined} [file]
   * @returns {Promise<VFileWithOutput<CompileResult>>}
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`]; any value accepted as
   *   `x` in `new VFile(x)`.
   * @param {ProcessCallback<VFileWithOutput<CompileResult>> | undefined} [done]
   *   Callback (optional).
   * @returns {Promise<VFile> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise a promise, rejected with a fatal error or resolved with the
   *   processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  process(file, done) {
    const self2 = this;
    this.freeze();
    assertParser("process", this.parser || this.Parser);
    assertCompiler("process", this.compiler || this.Compiler);
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      const realFile = vfile(file);
      const parseTree = (
        /** @type {HeadTree extends undefined ? Node : HeadTree} */
        /** @type {unknown} */
        self2.parse(realFile)
      );
      self2.run(parseTree, realFile, function(error2, tree, file2) {
        if (error2 || !tree || !file2) {
          return realDone(error2);
        }
        const compileTree = (
          /** @type {CompileTree extends undefined ? Node : CompileTree} */
          /** @type {unknown} */
          tree
        );
        const compileResult = self2.stringify(compileTree, file2);
        if (looksLikeAValue(compileResult)) {
          file2.value = compileResult;
        } else {
          file2.result = compileResult;
        }
        realDone(
          error2,
          /** @type {VFileWithOutput<CompileResult>} */
          file2
        );
      });
      function realDone(error2, file2) {
        if (error2 || !file2) {
          reject(error2);
        } else if (resolve) {
          resolve(file2);
        } else {
          ok(done, "`done` is defined if `resolve` is not");
          done(void 0, file2);
        }
      }
    }
  }
  /**
   * Process the given file as configured on the processor.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `processSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `processSync` performs the parse, run, and stringify phases.
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`; any value accepted as
   *   `x` in `new VFile(x)`.
   * @returns {VFileWithOutput<CompileResult>}
   *   The processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  processSync(file) {
    let complete = false;
    let result;
    this.freeze();
    assertParser("processSync", this.parser || this.Parser);
    assertCompiler("processSync", this.compiler || this.Compiler);
    this.process(file, realDone);
    assertDone("processSync", "process", complete);
    ok(result, "we either bailed on an error or have a tree");
    return result;
    function realDone(error2, file2) {
      complete = true;
      bail(error2);
      result = file2;
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * > **Note**: `run` freezes the processor if not already *frozen*.
   *
   * > **Note**: `run` performs the run phase, not other phases.
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} file
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} [file]
   * @returns {Promise<TailTree extends undefined ? Node : TailTree>}
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {(
   *   RunCallback<TailTree extends undefined ? Node : TailTree> |
   *   Compatible
   * )} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} [done]
   *   Callback (optional).
   * @returns {Promise<TailTree extends undefined ? Node : TailTree> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise, a promise rejected with a fatal error or resolved with the
   *   transformed tree.
   */
  run(tree, file, done) {
    assertNode(tree);
    this.freeze();
    const transformers = this.transformers;
    if (!done && typeof file === "function") {
      done = file;
      file = void 0;
    }
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      ok(
        typeof file !== "function",
        "`file` can’t be a `done` anymore, we checked"
      );
      const realFile = vfile(file);
      transformers.run(tree, realFile, realDone);
      function realDone(error2, outputTree, file2) {
        const resultingTree = (
          /** @type {TailTree extends undefined ? Node : TailTree} */
          outputTree || tree
        );
        if (error2) {
          reject(error2);
        } else if (resolve) {
          resolve(resultingTree);
        } else {
          ok(done, "`done` is defined if `resolve` is not");
          done(void 0, resultingTree, file2);
        }
      }
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `runSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `runSync` performs the run phase, not other phases.
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {TailTree extends undefined ? Node : TailTree}
   *   Transformed tree.
   */
  runSync(tree, file) {
    let complete = false;
    let result;
    this.run(tree, file, realDone);
    assertDone("runSync", "run", complete);
    ok(result, "we either bailed on an error or have a tree");
    return result;
    function realDone(error2, tree2) {
      bail(error2);
      result = tree2;
      complete = true;
    }
  }
  /**
   * Compile a syntax tree.
   *
   * > **Note**: `stringify` freezes the processor if not already *frozen*.
   *
   * > **Note**: `stringify` performs the stringify phase, not the run phase
   * > or other phases.
   *
   * @param {CompileTree extends undefined ? Node : CompileTree} tree
   *   Tree to compile.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {CompileResult extends undefined ? Value : CompileResult}
   *   Textual representation of the tree (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most compilers
   *   > return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  stringify(tree, file) {
    this.freeze();
    const realFile = vfile(file);
    const compiler2 = this.compiler || this.Compiler;
    assertCompiler("stringify", compiler2);
    assertNode(tree);
    return compiler2(tree, realFile);
  }
  /**
   * Configure the processor to use a plugin, a list of usable values, or a
   * preset.
   *
   * If the processor is already using a plugin, the previous plugin
   * configuration is changed based on the options that are passed in.
   * In other words, the plugin is not added a second time.
   *
   * > **Note**: `use` cannot be called on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * @example
   *   There are many ways to pass plugins to `.use()`.
   *   This example gives an overview:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   unified()
   *     // Plugin with options:
   *     .use(pluginA, {x: true, y: true})
   *     // Passing the same plugin again merges configuration (to `{x: true, y: false, z: true}`):
   *     .use(pluginA, {y: false, z: true})
   *     // Plugins:
   *     .use([pluginB, pluginC])
   *     // Two plugins, the second with options:
   *     .use([pluginD, [pluginE, {}]])
   *     // Preset with plugins and settings:
   *     .use({plugins: [pluginF, [pluginG, {}]], settings: {position: false}})
   *     // Settings only:
   *     .use({settings: {position: false}})
   *   ```
   *
   * @template {Array<unknown>} [Parameters=[]]
   * @template {Node | string | undefined} [Input=undefined]
   * @template [Output=Input]
   *
   * @overload
   * @param {Preset | null | undefined} [preset]
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {PluggableList} list
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Plugin<Parameters, Input, Output>} plugin
   * @param {...(Parameters | [boolean])} parameters
   * @returns {UsePlugin<ParseTree, HeadTree, TailTree, CompileTree, CompileResult, Input, Output>}
   *
   * @param {PluggableList | Plugin | Preset | null | undefined} value
   *   Usable value.
   * @param {...unknown} parameters
   *   Parameters, when a plugin is given as a usable value.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   Current processor.
   */
  use(value, ...parameters) {
    const attachers = this.attachers;
    const namespace = this.namespace;
    assertUnfrozen("use", this.frozen);
    if (value === null || value === void 0) {
    } else if (typeof value === "function") {
      addPlugin(value, parameters);
    } else if (typeof value === "object") {
      if (Array.isArray(value)) {
        addList(value);
      } else {
        addPreset(value);
      }
    } else {
      throw new TypeError("Expected usable value, not `" + value + "`");
    }
    return this;
    function add(value2) {
      if (typeof value2 === "function") {
        addPlugin(value2, []);
      } else if (typeof value2 === "object") {
        if (Array.isArray(value2)) {
          const [plugin, ...parameters2] = (
            /** @type {PluginTuple<Array<unknown>>} */
            value2
          );
          addPlugin(plugin, parameters2);
        } else {
          addPreset(value2);
        }
      } else {
        throw new TypeError("Expected usable value, not `" + value2 + "`");
      }
    }
    function addPreset(result) {
      if (!("plugins" in result) && !("settings" in result)) {
        throw new Error(
          "Expected usable value but received an empty preset, which is probably a mistake: presets typically come with `plugins` and sometimes with `settings`, but this has neither"
        );
      }
      addList(result.plugins);
      if (result.settings) {
        namespace.settings = (0, import_extend.default)(true, namespace.settings, result.settings);
      }
    }
    function addList(plugins) {
      let index6 = -1;
      if (plugins === null || plugins === void 0) {
      } else if (Array.isArray(plugins)) {
        while (++index6 < plugins.length) {
          const thing = plugins[index6];
          add(thing);
        }
      } else {
        throw new TypeError("Expected a list of plugins, not `" + plugins + "`");
      }
    }
    function addPlugin(plugin, parameters2) {
      let index6 = -1;
      let entryIndex = -1;
      while (++index6 < attachers.length) {
        if (attachers[index6][0] === plugin) {
          entryIndex = index6;
          break;
        }
      }
      if (entryIndex === -1) {
        attachers.push([plugin, ...parameters2]);
      } else if (parameters2.length > 0) {
        let [primary, ...rest] = parameters2;
        const currentPrimary = attachers[entryIndex][1];
        if (isPlainObject(currentPrimary) && isPlainObject(primary)) {
          primary = (0, import_extend.default)(true, currentPrimary, primary);
        }
        attachers[entryIndex] = [plugin, primary, ...rest];
      }
    }
  }
};
var unified = new Processor().freeze();
function assertParser(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `parser`");
  }
}
function assertCompiler(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `compiler`");
  }
}
function assertUnfrozen(name2, frozen) {
  if (frozen) {
    throw new Error(
      "Cannot call `" + name2 + "` on a frozen processor.\nCreate a new processor first, by calling it: use `processor()` instead of `processor`."
    );
  }
}
function assertNode(node2) {
  if (!isPlainObject(node2) || typeof node2.type !== "string") {
    throw new TypeError("Expected node, got `" + node2 + "`");
  }
}
function assertDone(name2, asyncName, complete) {
  if (!complete) {
    throw new Error(
      "`" + name2 + "` finished async. Use `" + asyncName + "` instead"
    );
  }
}
function vfile(value) {
  return looksLikeAVFile(value) ? value : new VFile(value);
}
function looksLikeAVFile(value) {
  return Boolean(
    value && typeof value === "object" && "message" in value && "messages" in value
  );
}
function looksLikeAValue(value) {
  return typeof value === "string" || isUint8Array2(value);
}
function isUint8Array2(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}

// node_modules/react-markdown/lib/index.js
var changelog = "https://github.com/remarkjs/react-markdown/blob/main/changelog.md";
var emptyPlugins = [];
var emptyRemarkRehypeOptions = { allowDangerousHtml: true };
var safeProtocol = /^(https?|ircs?|mailto|xmpp)$/i;
var deprecations = [
  { from: "astPlugins", id: "remove-buggy-html-in-markdown-parser" },
  { from: "allowDangerousHtml", id: "remove-buggy-html-in-markdown-parser" },
  {
    from: "allowNode",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowElement"
  },
  {
    from: "allowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowedElements"
  },
  {
    from: "disallowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "disallowedElements"
  },
  { from: "escapeHtml", id: "remove-buggy-html-in-markdown-parser" },
  { from: "includeElementIndex", id: "#remove-includeelementindex" },
  {
    from: "includeNodeIndex",
    id: "change-includenodeindex-to-includeelementindex"
  },
  { from: "linkTarget", id: "remove-linktarget" },
  { from: "plugins", id: "change-plugins-to-remarkplugins", to: "remarkPlugins" },
  { from: "rawSourcePos", id: "#remove-rawsourcepos" },
  { from: "renderers", id: "change-renderers-to-components", to: "components" },
  { from: "source", id: "change-source-to-children", to: "children" },
  { from: "sourcePos", id: "#remove-sourcepos" },
  { from: "transformImageUri", id: "#add-urltransform", to: "urlTransform" },
  { from: "transformLinkUri", id: "#add-urltransform", to: "urlTransform" }
];
function Markdown(options) {
  const processor = createProcessor(options);
  const file = createFile(options);
  return post(processor.runSync(processor.parse(file), file), options);
}
function createProcessor(options) {
  const rehypePlugins = options.rehypePlugins || emptyPlugins;
  const remarkPlugins2 = options.remarkPlugins || emptyPlugins;
  const remarkRehypeOptions = options.remarkRehypeOptions ? { ...options.remarkRehypeOptions, ...emptyRemarkRehypeOptions } : emptyRemarkRehypeOptions;
  const processor = unified().use(remarkParse).use(remarkPlugins2).use(remarkRehype, remarkRehypeOptions).use(rehypePlugins);
  return processor;
}
function createFile(options) {
  const children = options.children || "";
  const file = new VFile();
  if (typeof children === "string") {
    file.value = children;
  } else {
    unreachable(
      "Unexpected value `" + children + "` for `children` prop, expected `string`"
    );
  }
  return file;
}
function post(tree, options) {
  const allowedElements = options.allowedElements;
  const allowElement = options.allowElement;
  const components = options.components;
  const disallowedElements = options.disallowedElements;
  const skipHtml = options.skipHtml;
  const unwrapDisallowed = options.unwrapDisallowed;
  const urlTransform2 = options.urlTransform || defaultUrlTransform;
  for (const deprecation of deprecations) {
    if (Object.hasOwn(options, deprecation.from)) {
      unreachable(
        "Unexpected `" + deprecation.from + "` prop, " + (deprecation.to ? "use `" + deprecation.to + "` instead" : "remove it") + " (see <" + changelog + "#" + deprecation.id + "> for more info)"
      );
    }
  }
  if (allowedElements && disallowedElements) {
    unreachable(
      "Unexpected combined `allowedElements` and `disallowedElements`, expected one or the other"
    );
  }
  if (options.className) {
    tree = {
      type: "element",
      tagName: "div",
      properties: { className: options.className },
      // Assume no doctypes.
      children: (
        /** @type {Array<ElementContent>} */
        tree.type === "root" ? tree.children : [tree]
      )
    };
  }
  visit(tree, transform2);
  return toJsxRuntime(tree, {
    Fragment: import_jsx_runtime3.Fragment,
    // @ts-expect-error
    // React components are allowed to return numbers,
    // but not according to the types in hast-util-to-jsx-runtime
    components,
    ignoreInvalidStyle: true,
    jsx: import_jsx_runtime3.jsx,
    jsxs: import_jsx_runtime3.jsxs,
    passKeys: true,
    passNode: true
  });
  function transform2(node2, index6, parent) {
    if (node2.type === "raw" && parent && typeof index6 === "number") {
      if (skipHtml) {
        parent.children.splice(index6, 1);
      } else {
        parent.children[index6] = { type: "text", value: node2.value };
      }
      return index6;
    }
    if (node2.type === "element") {
      let key;
      for (key in urlAttributes) {
        if (Object.hasOwn(urlAttributes, key) && Object.hasOwn(node2.properties, key)) {
          const value = node2.properties[key];
          const test2 = urlAttributes[key];
          if (test2 === null || test2.includes(node2.tagName)) {
            node2.properties[key] = urlTransform2(String(value || ""), key, node2);
          }
        }
      }
    }
    if (node2.type === "element") {
      let remove = allowedElements ? !allowedElements.includes(node2.tagName) : disallowedElements ? disallowedElements.includes(node2.tagName) : false;
      if (!remove && allowElement && typeof index6 === "number") {
        remove = !allowElement(node2, index6, parent);
      }
      if (remove && parent && typeof index6 === "number") {
        if (unwrapDisallowed && node2.children) {
          parent.children.splice(index6, 1, ...node2.children);
        } else {
          parent.children.splice(index6, 1);
        }
        return index6;
      }
    }
  }
}
function defaultUrlTransform(value) {
  const colon = value.indexOf(":");
  const questionMark = value.indexOf("?");
  const numberSign = value.indexOf("#");
  const slash = value.indexOf("/");
  if (
    // If there is no protocol, it’s relative.
    colon === -1 || // If the first colon is after a `?`, `#`, or `/`, it’s not a protocol.
    slash !== -1 && colon > slash || questionMark !== -1 && colon > questionMark || numberSign !== -1 && colon > numberSign || // It is a protocol, it should be allowed.
    safeProtocol.test(value.slice(0, colon))
  ) {
    return value;
  }
  return "";
}

// node_modules/ccount/index.js
function ccount(value, character) {
  const source = String(value);
  if (typeof character !== "string") {
    throw new TypeError("Expected character");
  }
  let count2 = 0;
  let index6 = source.indexOf(character);
  while (index6 !== -1) {
    count2++;
    index6 = source.indexOf(character, index6 + character.length);
  }
  return count2;
}

// node_modules/mdast-util-find-and-replace/node_modules/escape-string-regexp/index.js
function escapeStringRegexp(string3) {
  if (typeof string3 !== "string") {
    throw new TypeError("Expected a string");
  }
  return string3.replace(/[|\\{}()[\]^$+*?.]/g, "\\$&").replace(/-/g, "\\x2d");
}

// node_modules/mdast-util-find-and-replace/lib/index.js
function findAndReplace(tree, list4, options) {
  const settings = options || {};
  const ignored = convert(settings.ignore || []);
  const pairs = toPairs(list4);
  let pairIndex = -1;
  while (++pairIndex < pairs.length) {
    visitParents(tree, "text", visitor2);
  }
  function visitor2(node2, parents) {
    let index6 = -1;
    let grandparent;
    while (++index6 < parents.length) {
      const parent = parents[index6];
      const siblings = grandparent ? grandparent.children : void 0;
      if (ignored(
        parent,
        siblings ? siblings.indexOf(parent) : void 0,
        grandparent
      )) {
        return;
      }
      grandparent = parent;
    }
    if (grandparent) {
      return handler(node2, parents);
    }
  }
  function handler(node2, parents) {
    const parent = parents[parents.length - 1];
    const find3 = pairs[pairIndex][0];
    const replace2 = pairs[pairIndex][1];
    let start2 = 0;
    const siblings = parent.children;
    const index6 = siblings.indexOf(node2);
    let change = false;
    let nodes = [];
    find3.lastIndex = 0;
    let match = find3.exec(node2.value);
    while (match) {
      const position3 = match.index;
      const matchObject = {
        index: match.index,
        input: match.input,
        stack: [...parents, node2]
      };
      let value = replace2(...match, matchObject);
      if (typeof value === "string") {
        value = value.length > 0 ? { type: "text", value } : void 0;
      }
      if (value === false) {
        find3.lastIndex = position3 + 1;
      } else {
        if (start2 !== position3) {
          nodes.push({
            type: "text",
            value: node2.value.slice(start2, position3)
          });
        }
        if (Array.isArray(value)) {
          nodes.push(...value);
        } else if (value) {
          nodes.push(value);
        }
        start2 = position3 + match[0].length;
        change = true;
      }
      if (!find3.global) {
        break;
      }
      match = find3.exec(node2.value);
    }
    if (change) {
      if (start2 < node2.value.length) {
        nodes.push({ type: "text", value: node2.value.slice(start2) });
      }
      parent.children.splice(index6, 1, ...nodes);
    } else {
      nodes = [node2];
    }
    return index6 + nodes.length;
  }
}
function toPairs(tupleOrList) {
  const result = [];
  if (!Array.isArray(tupleOrList)) {
    throw new TypeError("Expected find and replace tuple or list of tuples");
  }
  const list4 = !tupleOrList[0] || Array.isArray(tupleOrList[0]) ? tupleOrList : [tupleOrList];
  let index6 = -1;
  while (++index6 < list4.length) {
    const tuple = list4[index6];
    result.push([toExpression(tuple[0]), toFunction(tuple[1])]);
  }
  return result;
}
function toExpression(find3) {
  return typeof find3 === "string" ? new RegExp(escapeStringRegexp(find3), "g") : find3;
}
function toFunction(replace2) {
  return typeof replace2 === "function" ? replace2 : function() {
    return replace2;
  };
}

// node_modules/mdast-util-gfm-autolink-literal/lib/index.js
var inConstruct = "phrasing";
var notInConstruct = ["autolink", "link", "image", "label"];
function gfmAutolinkLiteralFromMarkdown() {
  return {
    transforms: [transformGfmAutolinkLiterals],
    enter: {
      literalAutolink: enterLiteralAutolink,
      literalAutolinkEmail: enterLiteralAutolinkValue,
      literalAutolinkHttp: enterLiteralAutolinkValue,
      literalAutolinkWww: enterLiteralAutolinkValue
    },
    exit: {
      literalAutolink: exitLiteralAutolink,
      literalAutolinkEmail: exitLiteralAutolinkEmail,
      literalAutolinkHttp: exitLiteralAutolinkHttp,
      literalAutolinkWww: exitLiteralAutolinkWww
    }
  };
}
function gfmAutolinkLiteralToMarkdown() {
  return {
    unsafe: [
      {
        character: "@",
        before: "[+\\-.\\w]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ".",
        before: "[Ww]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ":",
        before: "[ps]",
        after: "\\/",
        inConstruct,
        notInConstruct
      }
    ]
  };
}
function enterLiteralAutolink(token) {
  this.enter({ type: "link", title: null, url: "", children: [] }, token);
}
function enterLiteralAutolinkValue(token) {
  this.config.enter.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkHttp(token) {
  this.config.exit.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkWww(token) {
  this.config.exit.data.call(this, token);
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "link");
  node2.url = "http://" + this.sliceSerialize(token);
}
function exitLiteralAutolinkEmail(token) {
  this.config.exit.autolinkEmail.call(this, token);
}
function exitLiteralAutolink(token) {
  this.exit(token);
}
function transformGfmAutolinkLiterals(tree) {
  findAndReplace(
    tree,
    [
      [/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi, findUrl],
      [new RegExp("(?<=^|\\s|\\p{P}|\\p{S})([-.\\w+]+)@([-\\w]+(?:\\.[-\\w]+)+)", "gu"), findEmail]
    ],
    { ignore: ["link", "linkReference"] }
  );
}
function findUrl(_2, protocol, domain2, path2, match) {
  let prefix = "";
  if (!previous2(match)) {
    return false;
  }
  if (/^w/i.test(protocol)) {
    domain2 = protocol + domain2;
    protocol = "";
    prefix = "http://";
  }
  if (!isCorrectDomain(domain2)) {
    return false;
  }
  const parts = splitUrl(domain2 + path2);
  if (!parts[0]) return false;
  const result = {
    type: "link",
    title: null,
    url: prefix + protocol + parts[0],
    children: [{ type: "text", value: protocol + parts[0] }]
  };
  if (parts[1]) {
    return [result, { type: "text", value: parts[1] }];
  }
  return result;
}
function findEmail(_2, atext, label, match) {
  if (
    // Not an expected previous character.
    !previous2(match, true) || // Label ends in not allowed character.
    /[-\d_]$/.test(label)
  ) {
    return false;
  }
  return {
    type: "link",
    title: null,
    url: "mailto:" + atext + "@" + label,
    children: [{ type: "text", value: atext + "@" + label }]
  };
}
function isCorrectDomain(domain2) {
  const parts = domain2.split(".");
  if (parts.length < 2 || parts[parts.length - 1] && (/_/.test(parts[parts.length - 1]) || !/[a-zA-Z\d]/.test(parts[parts.length - 1])) || parts[parts.length - 2] && (/_/.test(parts[parts.length - 2]) || !/[a-zA-Z\d]/.test(parts[parts.length - 2]))) {
    return false;
  }
  return true;
}
function splitUrl(url) {
  const trailExec = /[!"&'),.:;<>?\]}]+$/.exec(url);
  if (!trailExec) {
    return [url, void 0];
  }
  url = url.slice(0, trailExec.index);
  let trail2 = trailExec[0];
  let closingParenIndex = trail2.indexOf(")");
  const openingParens = ccount(url, "(");
  let closingParens = ccount(url, ")");
  while (closingParenIndex !== -1 && openingParens > closingParens) {
    url += trail2.slice(0, closingParenIndex + 1);
    trail2 = trail2.slice(closingParenIndex + 1);
    closingParenIndex = trail2.indexOf(")");
    closingParens++;
  }
  return [url, trail2];
}
function previous2(match, email) {
  const code4 = match.input.charCodeAt(match.index - 1);
  return (match.index === 0 || unicodeWhitespace(code4) || unicodePunctuation(code4)) && // If it’s an email, the previous character should not be a slash.
  (!email || code4 !== 47);
}

// node_modules/mdast-util-gfm-footnote/lib/index.js
footnoteReference2.peek = footnoteReferencePeek;
function enterFootnoteCallString() {
  this.buffer();
}
function enterFootnoteCall(token) {
  this.enter({ type: "footnoteReference", identifier: "", label: "" }, token);
}
function enterFootnoteDefinitionLabelString() {
  this.buffer();
}
function enterFootnoteDefinition(token) {
  this.enter(
    { type: "footnoteDefinition", identifier: "", label: "", children: [] },
    token
  );
}
function exitFootnoteCallString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "footnoteReference");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteCall(token) {
  this.exit(token);
}
function exitFootnoteDefinitionLabelString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "footnoteDefinition");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteDefinition(token) {
  this.exit(token);
}
function footnoteReferencePeek() {
  return "[";
}
function footnoteReference2(node2, _2, state, info) {
  const tracker = state.createTracker(info);
  let value = tracker.move("[^");
  const exit3 = state.enter("footnoteReference");
  const subexit = state.enter("reference");
  value += tracker.move(
    state.safe(state.associationId(node2), { after: "]", before: value })
  );
  subexit();
  exit3();
  value += tracker.move("]");
  return value;
}
function gfmFootnoteFromMarkdown() {
  return {
    enter: {
      gfmFootnoteCallString: enterFootnoteCallString,
      gfmFootnoteCall: enterFootnoteCall,
      gfmFootnoteDefinitionLabelString: enterFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: enterFootnoteDefinition
    },
    exit: {
      gfmFootnoteCallString: exitFootnoteCallString,
      gfmFootnoteCall: exitFootnoteCall,
      gfmFootnoteDefinitionLabelString: exitFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: exitFootnoteDefinition
    }
  };
}
function gfmFootnoteToMarkdown(options) {
  let firstLineBlank = false;
  if (options && options.firstLineBlank) {
    firstLineBlank = true;
  }
  return {
    handlers: { footnoteDefinition, footnoteReference: footnoteReference2 },
    // This is on by default already.
    unsafe: [{ character: "[", inConstruct: ["label", "phrasing", "reference"] }]
  };
  function footnoteDefinition(node2, _2, state, info) {
    const tracker = state.createTracker(info);
    let value = tracker.move("[^");
    const exit3 = state.enter("footnoteDefinition");
    const subexit = state.enter("label");
    value += tracker.move(
      state.safe(state.associationId(node2), { before: value, after: "]" })
    );
    subexit();
    value += tracker.move("]:");
    if (node2.children && node2.children.length > 0) {
      tracker.shift(4);
      value += tracker.move(
        (firstLineBlank ? "\n" : " ") + state.indentLines(
          state.containerFlow(node2, tracker.current()),
          firstLineBlank ? mapAll : mapExceptFirst
        )
      );
    }
    exit3();
    return value;
  }
}
function mapExceptFirst(line, index6, blank) {
  return index6 === 0 ? line : mapAll(line, index6, blank);
}
function mapAll(line, index6, blank) {
  return (blank ? "" : "    ") + line;
}

// node_modules/mdast-util-gfm-strikethrough/lib/index.js
var constructsWithoutStrikethrough = [
  "autolink",
  "destinationLiteral",
  "destinationRaw",
  "reference",
  "titleQuote",
  "titleApostrophe"
];
handleDelete.peek = peekDelete;
function gfmStrikethroughFromMarkdown() {
  return {
    canContainEols: ["delete"],
    enter: { strikethrough: enterStrikethrough },
    exit: { strikethrough: exitStrikethrough }
  };
}
function gfmStrikethroughToMarkdown() {
  return {
    unsafe: [
      {
        character: "~",
        inConstruct: "phrasing",
        notInConstruct: constructsWithoutStrikethrough
      }
    ],
    handlers: { delete: handleDelete }
  };
}
function enterStrikethrough(token) {
  this.enter({ type: "delete", children: [] }, token);
}
function exitStrikethrough(token) {
  this.exit(token);
}
function handleDelete(node2, _2, state, info) {
  const tracker = state.createTracker(info);
  const exit3 = state.enter("strikethrough");
  let value = tracker.move("~~");
  value += state.containerPhrasing(node2, {
    ...tracker.current(),
    before: value,
    after: "~"
  });
  value += tracker.move("~~");
  exit3();
  return value;
}
function peekDelete() {
  return "~";
}

// node_modules/markdown-table/index.js
function defaultStringLength(value) {
  return value.length;
}
function markdownTable(table2, options) {
  const settings = options || {};
  const align = (settings.align || []).concat();
  const stringLength = settings.stringLength || defaultStringLength;
  const alignments2 = [];
  const cellMatrix = [];
  const sizeMatrix = [];
  const longestCellByColumn = [];
  let mostCellsPerRow = 0;
  let rowIndex = -1;
  while (++rowIndex < table2.length) {
    const row2 = [];
    const sizes2 = [];
    let columnIndex2 = -1;
    if (table2[rowIndex].length > mostCellsPerRow) {
      mostCellsPerRow = table2[rowIndex].length;
    }
    while (++columnIndex2 < table2[rowIndex].length) {
      const cell = serialize2(table2[rowIndex][columnIndex2]);
      if (settings.alignDelimiters !== false) {
        const size4 = stringLength(cell);
        sizes2[columnIndex2] = size4;
        if (longestCellByColumn[columnIndex2] === void 0 || size4 > longestCellByColumn[columnIndex2]) {
          longestCellByColumn[columnIndex2] = size4;
        }
      }
      row2.push(cell);
    }
    cellMatrix[rowIndex] = row2;
    sizeMatrix[rowIndex] = sizes2;
  }
  let columnIndex = -1;
  if (typeof align === "object" && "length" in align) {
    while (++columnIndex < mostCellsPerRow) {
      alignments2[columnIndex] = toAlignment(align[columnIndex]);
    }
  } else {
    const code4 = toAlignment(align);
    while (++columnIndex < mostCellsPerRow) {
      alignments2[columnIndex] = code4;
    }
  }
  columnIndex = -1;
  const row = [];
  const sizes = [];
  while (++columnIndex < mostCellsPerRow) {
    const code4 = alignments2[columnIndex];
    let before = "";
    let after = "";
    if (code4 === 99) {
      before = ":";
      after = ":";
    } else if (code4 === 108) {
      before = ":";
    } else if (code4 === 114) {
      after = ":";
    }
    let size4 = settings.alignDelimiters === false ? 1 : Math.max(
      1,
      longestCellByColumn[columnIndex] - before.length - after.length
    );
    const cell = before + "-".repeat(size4) + after;
    if (settings.alignDelimiters !== false) {
      size4 = before.length + size4 + after.length;
      if (size4 > longestCellByColumn[columnIndex]) {
        longestCellByColumn[columnIndex] = size4;
      }
      sizes[columnIndex] = size4;
    }
    row[columnIndex] = cell;
  }
  cellMatrix.splice(1, 0, row);
  sizeMatrix.splice(1, 0, sizes);
  rowIndex = -1;
  const lines = [];
  while (++rowIndex < cellMatrix.length) {
    const row2 = cellMatrix[rowIndex];
    const sizes2 = sizeMatrix[rowIndex];
    columnIndex = -1;
    const line = [];
    while (++columnIndex < mostCellsPerRow) {
      const cell = row2[columnIndex] || "";
      let before = "";
      let after = "";
      if (settings.alignDelimiters !== false) {
        const size4 = longestCellByColumn[columnIndex] - (sizes2[columnIndex] || 0);
        const code4 = alignments2[columnIndex];
        if (code4 === 114) {
          before = " ".repeat(size4);
        } else if (code4 === 99) {
          if (size4 % 2) {
            before = " ".repeat(size4 / 2 + 0.5);
            after = " ".repeat(size4 / 2 - 0.5);
          } else {
            before = " ".repeat(size4 / 2);
            after = before;
          }
        } else {
          after = " ".repeat(size4);
        }
      }
      if (settings.delimiterStart !== false && !columnIndex) {
        line.push("|");
      }
      if (settings.padding !== false && // Don’t add the opening space if we’re not aligning and the cell is
      // empty: there will be a closing space.
      !(settings.alignDelimiters === false && cell === "") && (settings.delimiterStart !== false || columnIndex)) {
        line.push(" ");
      }
      if (settings.alignDelimiters !== false) {
        line.push(before);
      }
      line.push(cell);
      if (settings.alignDelimiters !== false) {
        line.push(after);
      }
      if (settings.padding !== false) {
        line.push(" ");
      }
      if (settings.delimiterEnd !== false || columnIndex !== mostCellsPerRow - 1) {
        line.push("|");
      }
    }
    lines.push(
      settings.delimiterEnd === false ? line.join("").replace(/ +$/, "") : line.join("")
    );
  }
  return lines.join("\n");
}
function serialize2(value) {
  return value === null || value === void 0 ? "" : String(value);
}
function toAlignment(value) {
  const code4 = typeof value === "string" ? value.codePointAt(0) : 0;
  return code4 === 67 || code4 === 99 ? 99 : code4 === 76 || code4 === 108 ? 108 : code4 === 82 || code4 === 114 ? 114 : 0;
}

// node_modules/zwitch/index.js
var own5 = {}.hasOwnProperty;

// node_modules/mdast-util-to-markdown/lib/configure.js
var own6 = {}.hasOwnProperty;

// node_modules/mdast-util-to-markdown/lib/handle/blockquote.js
function blockquote2(node2, _2, state, info) {
  const exit3 = state.enter("blockquote");
  const tracker = state.createTracker(info);
  tracker.move("> ");
  tracker.shift(2);
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map
  );
  exit3();
  return value;
}
function map(line, _2, blank) {
  return ">" + (blank ? "" : " ") + line;
}

// node_modules/mdast-util-to-markdown/lib/util/pattern-in-scope.js
function patternInScope(stack, pattern) {
  return listInScope(stack, pattern.inConstruct, true) && !listInScope(stack, pattern.notInConstruct, false);
}
function listInScope(stack, list4, none) {
  if (typeof list4 === "string") {
    list4 = [list4];
  }
  if (!list4 || list4.length === 0) {
    return none;
  }
  let index6 = -1;
  while (++index6 < list4.length) {
    if (stack.includes(list4[index6])) {
      return true;
    }
  }
  return false;
}

// node_modules/mdast-util-to-markdown/lib/handle/break.js
function hardBreak2(_2, _1, state, info) {
  let index6 = -1;
  while (++index6 < state.unsafe.length) {
    if (state.unsafe[index6].character === "\n" && patternInScope(state.stack, state.unsafe[index6])) {
      return /[ \t]/.test(info.before) ? "" : " ";
    }
  }
  return "\\\n";
}

// node_modules/longest-streak/index.js
function longestStreak(value, substring) {
  const source = String(value);
  let index6 = source.indexOf(substring);
  let expected = index6;
  let count2 = 0;
  let max2 = 0;
  if (typeof substring !== "string") {
    throw new TypeError("Expected substring");
  }
  while (index6 !== -1) {
    if (index6 === expected) {
      if (++count2 > max2) {
        max2 = count2;
      }
    } else {
      count2 = 1;
    }
    expected = index6 + substring.length;
    index6 = source.indexOf(substring, expected);
  }
  return max2;
}

// node_modules/mdast-util-to-markdown/lib/util/format-code-as-indented.js
function formatCodeAsIndented(node2, state) {
  return Boolean(
    state.options.fences === false && node2.value && // If there’s no info…
    !node2.lang && // And there’s a non-whitespace character…
    /[^ \r\n]/.test(node2.value) && // And the value doesn’t start or end in a blank…
    !/^[\t ]*(?:[\r\n]|$)|(?:^|[\r\n])[\t ]*$/.test(node2.value)
  );
}

// node_modules/mdast-util-to-markdown/lib/util/check-fence.js
function checkFence(state) {
  const marker = state.options.fence || "`";
  if (marker !== "`" && marker !== "~") {
    throw new Error(
      "Cannot serialize code with `" + marker + "` for `options.fence`, expected `` ` `` or `~`"
    );
  }
  return marker;
}

// node_modules/mdast-util-to-markdown/lib/handle/code.js
function code2(node2, _2, state, info) {
  const marker = checkFence(state);
  const raw = node2.value || "";
  const suffix = marker === "`" ? "GraveAccent" : "Tilde";
  if (formatCodeAsIndented(node2, state)) {
    const exit4 = state.enter("codeIndented");
    const value2 = state.indentLines(raw, map2);
    exit4();
    return value2;
  }
  const tracker = state.createTracker(info);
  const sequence = marker.repeat(Math.max(longestStreak(raw, marker) + 1, 3));
  const exit3 = state.enter("codeFenced");
  let value = tracker.move(sequence);
  if (node2.lang) {
    const subexit = state.enter(`codeFencedLang${suffix}`);
    value += tracker.move(
      state.safe(node2.lang, {
        before: value,
        after: " ",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  if (node2.lang && node2.meta) {
    const subexit = state.enter(`codeFencedMeta${suffix}`);
    value += tracker.move(" ");
    value += tracker.move(
      state.safe(node2.meta, {
        before: value,
        after: "\n",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  value += tracker.move("\n");
  if (raw) {
    value += tracker.move(raw + "\n");
  }
  value += tracker.move(sequence);
  exit3();
  return value;
}
function map2(line, _2, blank) {
  return (blank ? "" : "    ") + line;
}

// node_modules/mdast-util-to-markdown/lib/util/check-quote.js
function checkQuote(state) {
  const marker = state.options.quote || '"';
  if (marker !== '"' && marker !== "'") {
    throw new Error(
      "Cannot serialize title with `" + marker + "` for `options.quote`, expected `\"`, or `'`"
    );
  }
  return marker;
}

// node_modules/mdast-util-to-markdown/lib/handle/definition.js
function definition2(node2, _2, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit3 = state.enter("definition");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  value += tracker.move(
    state.safe(state.associationId(node2), {
      before: value,
      after: "]",
      ...tracker.current()
    })
  );
  value += tracker.move("]: ");
  subexit();
  if (
    // If there’s no url, or…
    !node2.url || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : "\n",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  exit3();
  return value;
}

// node_modules/mdast-util-to-markdown/lib/util/check-emphasis.js
function checkEmphasis(state) {
  const marker = state.options.emphasis || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize emphasis with `" + marker + "` for `options.emphasis`, expected `*`, or `_`"
    );
  }
  return marker;
}

// node_modules/mdast-util-to-markdown/lib/util/encode-character-reference.js
function encodeCharacterReference(code4) {
  return "&#x" + code4.toString(16).toUpperCase() + ";";
}

// node_modules/mdast-util-to-markdown/lib/util/encode-info.js
function encodeInfo(outside, inside, marker) {
  const outsideKind = classifyCharacter(outside);
  const insideKind = classifyCharacter(inside);
  if (outsideKind === void 0) {
    return insideKind === void 0 ? (
      // Letter inside:
      // we have to encode *both* letters for `_` as it is looser.
      // it already forms for `*` (and GFMs `~`).
      marker === "_" ? { inside: true, outside: true } : { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (letter, whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: encode outer (letter)
      { inside: false, outside: true }
    );
  }
  if (outsideKind === 1) {
    return insideKind === void 0 ? (
      // Letter inside: already forms.
      { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: already forms.
      { inside: false, outside: false }
    );
  }
  return insideKind === void 0 ? (
    // Letter inside: already forms.
    { inside: false, outside: false }
  ) : insideKind === 1 ? (
    // Whitespace inside: encode inner (whitespace).
    { inside: true, outside: false }
  ) : (
    // Punctuation inside: already forms.
    { inside: false, outside: false }
  );
}

// node_modules/mdast-util-to-markdown/lib/handle/emphasis.js
emphasis2.peek = emphasisPeek;
function emphasis2(node2, _2, state, info) {
  const marker = checkEmphasis(state);
  const exit3 = state.enter("emphasis");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker);
  exit3();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function emphasisPeek(_2, _1, state) {
  return state.options.emphasis || "*";
}

// node_modules/mdast-util-to-markdown/lib/util/format-heading-as-setext.js
function formatHeadingAsSetext(node2, state) {
  let literalWithBreak = false;
  visit(node2, function(node3) {
    if ("value" in node3 && /\r?\n|\r/.test(node3.value) || node3.type === "break") {
      literalWithBreak = true;
      return EXIT;
    }
  });
  return Boolean(
    (!node2.depth || node2.depth < 3) && toString(node2) && (state.options.setext || literalWithBreak)
  );
}

// node_modules/mdast-util-to-markdown/lib/handle/heading.js
function heading2(node2, _2, state, info) {
  const rank = Math.max(Math.min(6, node2.depth || 1), 1);
  const tracker = state.createTracker(info);
  if (formatHeadingAsSetext(node2, state)) {
    const exit4 = state.enter("headingSetext");
    const subexit2 = state.enter("phrasing");
    const value2 = state.containerPhrasing(node2, {
      ...tracker.current(),
      before: "\n",
      after: "\n"
    });
    subexit2();
    exit4();
    return value2 + "\n" + (rank === 1 ? "=" : "-").repeat(
      // The whole size…
      value2.length - // Minus the position of the character after the last EOL (or
      // 0 if there is none)…
      (Math.max(value2.lastIndexOf("\r"), value2.lastIndexOf("\n")) + 1)
    );
  }
  const sequence = "#".repeat(rank);
  const exit3 = state.enter("headingAtx");
  const subexit = state.enter("phrasing");
  tracker.move(sequence + " ");
  let value = state.containerPhrasing(node2, {
    before: "# ",
    after: "\n",
    ...tracker.current()
  });
  if (/^[\t ]/.test(value)) {
    value = encodeCharacterReference(value.charCodeAt(0)) + value.slice(1);
  }
  value = value ? sequence + " " + value : sequence;
  if (state.options.closeAtx) {
    value += " " + sequence;
  }
  subexit();
  exit3();
  return value;
}

// node_modules/mdast-util-to-markdown/lib/handle/html.js
html4.peek = htmlPeek;
function html4(node2) {
  return node2.value || "";
}
function htmlPeek() {
  return "<";
}

// node_modules/mdast-util-to-markdown/lib/handle/image.js
image2.peek = imagePeek;
function image2(node2, _2, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit3 = state.enter("image");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  value += tracker.move(
    state.safe(node2.alt, { before: value, after: "]", ...tracker.current() })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit3();
  return value;
}
function imagePeek() {
  return "!";
}

// node_modules/mdast-util-to-markdown/lib/handle/image-reference.js
imageReference2.peek = imageReferencePeek;
function imageReference2(node2, _2, state, info) {
  const type = node2.referenceType;
  const exit3 = state.enter("imageReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  const alt = state.safe(node2.alt, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(alt + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit3();
  if (type === "full" || !alt || alt !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function imageReferencePeek() {
  return "!";
}

// node_modules/mdast-util-to-markdown/lib/handle/inline-code.js
inlineCode2.peek = inlineCodePeek;
function inlineCode2(node2, _2, state) {
  let value = node2.value || "";
  let sequence = "`";
  let index6 = -1;
  while (new RegExp("(^|[^`])" + sequence + "([^`]|$)").test(value)) {
    sequence += "`";
  }
  if (/[^ \r\n]/.test(value) && (/^[ \r\n]/.test(value) && /[ \r\n]$/.test(value) || /^`|`$/.test(value))) {
    value = " " + value + " ";
  }
  while (++index6 < state.unsafe.length) {
    const pattern = state.unsafe[index6];
    const expression = state.compilePattern(pattern);
    let match;
    if (!pattern.atBreak) continue;
    while (match = expression.exec(value)) {
      let position3 = match.index;
      if (value.charCodeAt(position3) === 10 && value.charCodeAt(position3 - 1) === 13) {
        position3--;
      }
      value = value.slice(0, position3) + " " + value.slice(match.index + 1);
    }
  }
  return sequence + value + sequence;
}
function inlineCodePeek() {
  return "`";
}

// node_modules/mdast-util-to-markdown/lib/util/format-link-as-autolink.js
function formatLinkAsAutolink(node2, state) {
  const raw = toString(node2);
  return Boolean(
    !state.options.resourceLink && // If there’s a url…
    node2.url && // And there’s a no title…
    !node2.title && // And the content of `node` is a single text node…
    node2.children && node2.children.length === 1 && node2.children[0].type === "text" && // And if the url is the same as the content…
    (raw === node2.url || "mailto:" + raw === node2.url) && // And that starts w/ a protocol…
    /^[a-z][a-z+.-]+:/i.test(node2.url) && // And that doesn’t contain ASCII control codes (character escapes and
    // references don’t work), space, or angle brackets…
    !/[\0- <>\u007F]/.test(node2.url)
  );
}

// node_modules/mdast-util-to-markdown/lib/handle/link.js
link2.peek = linkPeek;
function link2(node2, _2, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const tracker = state.createTracker(info);
  let exit3;
  let subexit;
  if (formatLinkAsAutolink(node2, state)) {
    const stack = state.stack;
    state.stack = [];
    exit3 = state.enter("autolink");
    let value2 = tracker.move("<");
    value2 += tracker.move(
      state.containerPhrasing(node2, {
        before: value2,
        after: ">",
        ...tracker.current()
      })
    );
    value2 += tracker.move(">");
    exit3();
    state.stack = stack;
    return value2;
  }
  exit3 = state.enter("link");
  subexit = state.enter("label");
  let value = tracker.move("[");
  value += tracker.move(
    state.containerPhrasing(node2, {
      before: value,
      after: "](",
      ...tracker.current()
    })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit3();
  return value;
}
function linkPeek(node2, _2, state) {
  return formatLinkAsAutolink(node2, state) ? "<" : "[";
}

// node_modules/mdast-util-to-markdown/lib/handle/link-reference.js
linkReference2.peek = linkReferencePeek;
function linkReference2(node2, _2, state, info) {
  const type = node2.referenceType;
  const exit3 = state.enter("linkReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  const text8 = state.containerPhrasing(node2, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(text8 + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit3();
  if (type === "full" || !text8 || text8 !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function linkReferencePeek() {
  return "[";
}

// node_modules/mdast-util-to-markdown/lib/util/check-bullet.js
function checkBullet(state) {
  const marker = state.options.bullet || "*";
  if (marker !== "*" && marker !== "+" && marker !== "-") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bullet`, expected `*`, `+`, or `-`"
    );
  }
  return marker;
}

// node_modules/mdast-util-to-markdown/lib/util/check-bullet-other.js
function checkBulletOther(state) {
  const bullet = checkBullet(state);
  const bulletOther = state.options.bulletOther;
  if (!bulletOther) {
    return bullet === "*" ? "-" : "*";
  }
  if (bulletOther !== "*" && bulletOther !== "+" && bulletOther !== "-") {
    throw new Error(
      "Cannot serialize items with `" + bulletOther + "` for `options.bulletOther`, expected `*`, `+`, or `-`"
    );
  }
  if (bulletOther === bullet) {
    throw new Error(
      "Expected `bullet` (`" + bullet + "`) and `bulletOther` (`" + bulletOther + "`) to be different"
    );
  }
  return bulletOther;
}

// node_modules/mdast-util-to-markdown/lib/util/check-bullet-ordered.js
function checkBulletOrdered(state) {
  const marker = state.options.bulletOrdered || ".";
  if (marker !== "." && marker !== ")") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bulletOrdered`, expected `.` or `)`"
    );
  }
  return marker;
}

// node_modules/mdast-util-to-markdown/lib/util/check-rule.js
function checkRule(state) {
  const marker = state.options.rule || "*";
  if (marker !== "*" && marker !== "-" && marker !== "_") {
    throw new Error(
      "Cannot serialize rules with `" + marker + "` for `options.rule`, expected `*`, `-`, or `_`"
    );
  }
  return marker;
}

// node_modules/mdast-util-to-markdown/lib/handle/list.js
function list3(node2, parent, state, info) {
  const exit3 = state.enter("list");
  const bulletCurrent = state.bulletCurrent;
  let bullet = node2.ordered ? checkBulletOrdered(state) : checkBullet(state);
  const bulletOther = node2.ordered ? bullet === "." ? ")" : "." : checkBulletOther(state);
  let useDifferentMarker = parent && state.bulletLastUsed ? bullet === state.bulletLastUsed : false;
  if (!node2.ordered) {
    const firstListItem = node2.children ? node2.children[0] : void 0;
    if (
      // Bullet could be used as a thematic break marker:
      (bullet === "*" || bullet === "-") && // Empty first list item:
      firstListItem && (!firstListItem.children || !firstListItem.children[0]) && // Directly in two other list items:
      state.stack[state.stack.length - 1] === "list" && state.stack[state.stack.length - 2] === "listItem" && state.stack[state.stack.length - 3] === "list" && state.stack[state.stack.length - 4] === "listItem" && // That are each the first child.
      state.indexStack[state.indexStack.length - 1] === 0 && state.indexStack[state.indexStack.length - 2] === 0 && state.indexStack[state.indexStack.length - 3] === 0
    ) {
      useDifferentMarker = true;
    }
    if (checkRule(state) === bullet && firstListItem) {
      let index6 = -1;
      while (++index6 < node2.children.length) {
        const item = node2.children[index6];
        if (item && item.type === "listItem" && item.children && item.children[0] && item.children[0].type === "thematicBreak") {
          useDifferentMarker = true;
          break;
        }
      }
    }
  }
  if (useDifferentMarker) {
    bullet = bulletOther;
  }
  state.bulletCurrent = bullet;
  const value = state.containerFlow(node2, info);
  state.bulletLastUsed = bullet;
  state.bulletCurrent = bulletCurrent;
  exit3();
  return value;
}

// node_modules/mdast-util-to-markdown/lib/util/check-list-item-indent.js
function checkListItemIndent(state) {
  const style = state.options.listItemIndent || "one";
  if (style !== "tab" && style !== "one" && style !== "mixed") {
    throw new Error(
      "Cannot serialize items with `" + style + "` for `options.listItemIndent`, expected `tab`, `one`, or `mixed`"
    );
  }
  return style;
}

// node_modules/mdast-util-to-markdown/lib/handle/list-item.js
function listItem2(node2, parent, state, info) {
  const listItemIndent = checkListItemIndent(state);
  let bullet = state.bulletCurrent || checkBullet(state);
  if (parent && parent.type === "list" && parent.ordered) {
    bullet = (typeof parent.start === "number" && parent.start > -1 ? parent.start : 1) + (state.options.incrementListMarker === false ? 0 : parent.children.indexOf(node2)) + bullet;
  }
  let size4 = bullet.length + 1;
  if (listItemIndent === "tab" || listItemIndent === "mixed" && (parent && parent.type === "list" && parent.spread || node2.spread)) {
    size4 = Math.ceil(size4 / 4) * 4;
  }
  const tracker = state.createTracker(info);
  tracker.move(bullet + " ".repeat(size4 - bullet.length));
  tracker.shift(size4);
  const exit3 = state.enter("listItem");
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map4
  );
  exit3();
  return value;
  function map4(line, index6, blank) {
    if (index6) {
      return (blank ? "" : " ".repeat(size4)) + line;
    }
    return (blank ? bullet : bullet + " ".repeat(size4 - bullet.length)) + line;
  }
}

// node_modules/mdast-util-to-markdown/lib/handle/paragraph.js
function paragraph2(node2, _2, state, info) {
  const exit3 = state.enter("paragraph");
  const subexit = state.enter("phrasing");
  const value = state.containerPhrasing(node2, info);
  subexit();
  exit3();
  return value;
}

// node_modules/mdast-util-phrasing/lib/index.js
var phrasing = (
  /** @type {(node?: unknown) => node is Exclude<PhrasingContent, Html>} */
  convert([
    "break",
    "delete",
    "emphasis",
    // To do: next major: removed since footnotes were added to GFM.
    "footnote",
    "footnoteReference",
    "image",
    "imageReference",
    "inlineCode",
    // Enabled by `mdast-util-math`:
    "inlineMath",
    "link",
    "linkReference",
    // Enabled by `mdast-util-mdx`:
    "mdxJsxTextElement",
    // Enabled by `mdast-util-mdx`:
    "mdxTextExpression",
    "strong",
    "text",
    // Enabled by `mdast-util-directive`:
    "textDirective"
  ])
);

// node_modules/mdast-util-to-markdown/lib/handle/root.js
function root3(node2, _2, state, info) {
  const hasPhrasing = node2.children.some(function(d2) {
    return phrasing(d2);
  });
  const container = hasPhrasing ? state.containerPhrasing : state.containerFlow;
  return container.call(state, node2, info);
}

// node_modules/mdast-util-to-markdown/lib/util/check-strong.js
function checkStrong(state) {
  const marker = state.options.strong || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize strong with `" + marker + "` for `options.strong`, expected `*`, or `_`"
    );
  }
  return marker;
}

// node_modules/mdast-util-to-markdown/lib/handle/strong.js
strong2.peek = strongPeek;
function strong2(node2, _2, state, info) {
  const marker = checkStrong(state);
  const exit3 = state.enter("strong");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker + marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker + marker);
  exit3();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function strongPeek(_2, _1, state) {
  return state.options.strong || "*";
}

// node_modules/mdast-util-to-markdown/lib/handle/text.js
function text5(node2, _2, state, info) {
  return state.safe(node2.value, info);
}

// node_modules/mdast-util-to-markdown/lib/util/check-rule-repetition.js
function checkRuleRepetition(state) {
  const repetition = state.options.ruleRepetition || 3;
  if (repetition < 3) {
    throw new Error(
      "Cannot serialize rules with repetition `" + repetition + "` for `options.ruleRepetition`, expected `3` or more"
    );
  }
  return repetition;
}

// node_modules/mdast-util-to-markdown/lib/handle/thematic-break.js
function thematicBreak3(_2, _1, state) {
  const value = (checkRule(state) + (state.options.ruleSpaces ? " " : "")).repeat(checkRuleRepetition(state));
  return state.options.ruleSpaces ? value.slice(0, -1) : value;
}

// node_modules/mdast-util-to-markdown/lib/handle/index.js
var handle = {
  blockquote: blockquote2,
  break: hardBreak2,
  code: code2,
  definition: definition2,
  emphasis: emphasis2,
  hardBreak: hardBreak2,
  heading: heading2,
  html: html4,
  image: image2,
  imageReference: imageReference2,
  inlineCode: inlineCode2,
  link: link2,
  linkReference: linkReference2,
  list: list3,
  listItem: listItem2,
  paragraph: paragraph2,
  root: root3,
  strong: strong2,
  text: text5,
  thematicBreak: thematicBreak3
};

// node_modules/mdast-util-gfm-table/lib/index.js
function gfmTableFromMarkdown() {
  return {
    enter: {
      table: enterTable,
      tableData: enterCell,
      tableHeader: enterCell,
      tableRow: enterRow
    },
    exit: {
      codeText: exitCodeText,
      table: exitTable,
      tableData: exit2,
      tableHeader: exit2,
      tableRow: exit2
    }
  };
}
function enterTable(token) {
  const align = token._align;
  ok(align, "expected `_align` on table");
  this.enter(
    {
      type: "table",
      align: align.map(function(d2) {
        return d2 === "none" ? null : d2;
      }),
      children: []
    },
    token
  );
  this.data.inTable = true;
}
function exitTable(token) {
  this.exit(token);
  this.data.inTable = void 0;
}
function enterRow(token) {
  this.enter({ type: "tableRow", children: [] }, token);
}
function exit2(token) {
  this.exit(token);
}
function enterCell(token) {
  this.enter({ type: "tableCell", children: [] }, token);
}
function exitCodeText(token) {
  let value = this.resume();
  if (this.data.inTable) {
    value = value.replace(/\\([\\|])/g, replace);
  }
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "inlineCode");
  node2.value = value;
  this.exit(token);
}
function replace($0, $1) {
  return $1 === "|" ? $1 : $0;
}
function gfmTableToMarkdown(options) {
  const settings = options || {};
  const padding = settings.tableCellPadding;
  const alignDelimiters = settings.tablePipeAlign;
  const stringLength = settings.stringLength;
  const around = padding ? " " : "|";
  return {
    unsafe: [
      { character: "\r", inConstruct: "tableCell" },
      { character: "\n", inConstruct: "tableCell" },
      // A pipe, when followed by a tab or space (padding), or a dash or colon
      // (unpadded delimiter row), could result in a table.
      { atBreak: true, character: "|", after: "[	 :-]" },
      // A pipe in a cell must be encoded.
      { character: "|", inConstruct: "tableCell" },
      // A colon must be followed by a dash, in which case it could start a
      // delimiter row.
      { atBreak: true, character: ":", after: "-" },
      // A delimiter row can also start with a dash, when followed by more
      // dashes, a colon, or a pipe.
      // This is a stricter version than the built in check for lists, thematic
      // breaks, and setex heading underlines though:
      // <https://github.com/syntax-tree/mdast-util-to-markdown/blob/51a2038/lib/unsafe.js#L57>
      { atBreak: true, character: "-", after: "[:|-]" }
    ],
    handlers: {
      inlineCode: inlineCodeWithTable,
      table: handleTable,
      tableCell: handleTableCell,
      tableRow: handleTableRow
    }
  };
  function handleTable(node2, _2, state, info) {
    return serializeData(handleTableAsData(node2, state, info), node2.align);
  }
  function handleTableRow(node2, _2, state, info) {
    const row = handleTableRowAsData(node2, state, info);
    const value = serializeData([row]);
    return value.slice(0, value.indexOf("\n"));
  }
  function handleTableCell(node2, _2, state, info) {
    const exit3 = state.enter("tableCell");
    const subexit = state.enter("phrasing");
    const value = state.containerPhrasing(node2, {
      ...info,
      before: around,
      after: around
    });
    subexit();
    exit3();
    return value;
  }
  function serializeData(matrix, align) {
    return markdownTable(matrix, {
      align,
      // @ts-expect-error: `markdown-table` types should support `null`.
      alignDelimiters,
      // @ts-expect-error: `markdown-table` types should support `null`.
      padding,
      // @ts-expect-error: `markdown-table` types should support `null`.
      stringLength
    });
  }
  function handleTableAsData(node2, state, info) {
    const children = node2.children;
    let index6 = -1;
    const result = [];
    const subexit = state.enter("table");
    while (++index6 < children.length) {
      result[index6] = handleTableRowAsData(children[index6], state, info);
    }
    subexit();
    return result;
  }
  function handleTableRowAsData(node2, state, info) {
    const children = node2.children;
    let index6 = -1;
    const result = [];
    const subexit = state.enter("tableRow");
    while (++index6 < children.length) {
      result[index6] = handleTableCell(children[index6], node2, state, info);
    }
    subexit();
    return result;
  }
  function inlineCodeWithTable(node2, parent, state) {
    let value = handle.inlineCode(node2, parent, state);
    if (state.stack.includes("tableCell")) {
      value = value.replace(/\|/g, "\\$&");
    }
    return value;
  }
}

// node_modules/mdast-util-gfm-task-list-item/lib/index.js
function gfmTaskListItemFromMarkdown() {
  return {
    exit: {
      taskListCheckValueChecked: exitCheck,
      taskListCheckValueUnchecked: exitCheck,
      paragraph: exitParagraphWithTaskListItem
    }
  };
}
function gfmTaskListItemToMarkdown() {
  return {
    unsafe: [{ atBreak: true, character: "-", after: "[:|-]" }],
    handlers: { listItem: listItemWithTaskListItem }
  };
}
function exitCheck(token) {
  const node2 = this.stack[this.stack.length - 2];
  ok(node2.type === "listItem");
  node2.checked = token.type === "taskListCheckValueChecked";
}
function exitParagraphWithTaskListItem(token) {
  const parent = this.stack[this.stack.length - 2];
  if (parent && parent.type === "listItem" && typeof parent.checked === "boolean") {
    const node2 = this.stack[this.stack.length - 1];
    ok(node2.type === "paragraph");
    const head = node2.children[0];
    if (head && head.type === "text") {
      const siblings = parent.children;
      let index6 = -1;
      let firstParaghraph;
      while (++index6 < siblings.length) {
        const sibling = siblings[index6];
        if (sibling.type === "paragraph") {
          firstParaghraph = sibling;
          break;
        }
      }
      if (firstParaghraph === node2) {
        head.value = head.value.slice(1);
        if (head.value.length === 0) {
          node2.children.shift();
        } else if (node2.position && head.position && typeof head.position.start.offset === "number") {
          head.position.start.column++;
          head.position.start.offset++;
          node2.position.start = Object.assign({}, head.position.start);
        }
      }
    }
  }
  this.exit(token);
}
function listItemWithTaskListItem(node2, parent, state, info) {
  const head = node2.children[0];
  const checkable = typeof node2.checked === "boolean" && head && head.type === "paragraph";
  const checkbox = "[" + (node2.checked ? "x" : " ") + "] ";
  const tracker = state.createTracker(info);
  if (checkable) {
    tracker.move(checkbox);
  }
  let value = handle.listItem(node2, parent, state, {
    ...info,
    ...tracker.current()
  });
  if (checkable) {
    value = value.replace(/^(?:[*+-]|\d+\.)([\r\n]| {1,3})/, check);
  }
  return value;
  function check($0) {
    return $0 + checkbox;
  }
}

// node_modules/mdast-util-gfm/lib/index.js
function gfmFromMarkdown() {
  return [
    gfmAutolinkLiteralFromMarkdown(),
    gfmFootnoteFromMarkdown(),
    gfmStrikethroughFromMarkdown(),
    gfmTableFromMarkdown(),
    gfmTaskListItemFromMarkdown()
  ];
}
function gfmToMarkdown(options) {
  return {
    extensions: [
      gfmAutolinkLiteralToMarkdown(),
      gfmFootnoteToMarkdown(options),
      gfmStrikethroughToMarkdown(),
      gfmTableToMarkdown(options),
      gfmTaskListItemToMarkdown()
    ]
  };
}

// node_modules/micromark-extension-gfm-autolink-literal/dev/lib/syntax.js
var wwwPrefix = { tokenize: tokenizeWwwPrefix, partial: true };
var domain = { tokenize: tokenizeDomain, partial: true };
var path = { tokenize: tokenizePath, partial: true };
var trail = { tokenize: tokenizeTrail, partial: true };
var emailDomainDotTrail = {
  tokenize: tokenizeEmailDomainDotTrail,
  partial: true
};
var wwwAutolink = {
  name: "wwwAutolink",
  tokenize: tokenizeWwwAutolink,
  previous: previousWww
};
var protocolAutolink = {
  name: "protocolAutolink",
  tokenize: tokenizeProtocolAutolink,
  previous: previousProtocol
};
var emailAutolink = {
  name: "emailAutolink",
  tokenize: tokenizeEmailAutolink,
  previous: previousEmail
};
var text6 = {};
function gfmAutolinkLiteral() {
  return { text: text6 };
}
var code3 = codes.digit0;
while (code3 < codes.leftCurlyBrace) {
  text6[code3] = emailAutolink;
  code3++;
  if (code3 === codes.colon) code3 = codes.uppercaseA;
  else if (code3 === codes.leftSquareBracket) code3 = codes.lowercaseA;
}
text6[codes.plusSign] = emailAutolink;
text6[codes.dash] = emailAutolink;
text6[codes.dot] = emailAutolink;
text6[codes.underscore] = emailAutolink;
text6[codes.uppercaseH] = [emailAutolink, protocolAutolink];
text6[codes.lowercaseH] = [emailAutolink, protocolAutolink];
text6[codes.uppercaseW] = [emailAutolink, wwwAutolink];
text6[codes.lowercaseW] = [emailAutolink, wwwAutolink];
function tokenizeEmailAutolink(effects, ok3, nok) {
  const self2 = this;
  let dot;
  let data;
  return start2;
  function start2(code4) {
    if (!gfmAtext(code4) || !previousEmail.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code4);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkEmail");
    return atext(code4);
  }
  function atext(code4) {
    if (gfmAtext(code4)) {
      effects.consume(code4);
      return atext;
    }
    if (code4 === codes.atSign) {
      effects.consume(code4);
      return emailDomain;
    }
    return nok(code4);
  }
  function emailDomain(code4) {
    if (code4 === codes.dot) {
      return effects.check(
        emailDomainDotTrail,
        emailDomainAfter,
        emailDomainDot
      )(code4);
    }
    if (code4 === codes.dash || code4 === codes.underscore || asciiAlphanumeric(code4)) {
      data = true;
      effects.consume(code4);
      return emailDomain;
    }
    return emailDomainAfter(code4);
  }
  function emailDomainDot(code4) {
    effects.consume(code4);
    dot = true;
    return emailDomain;
  }
  function emailDomainAfter(code4) {
    if (data && dot && asciiAlpha(self2.previous)) {
      effects.exit("literalAutolinkEmail");
      effects.exit("literalAutolink");
      return ok3(code4);
    }
    return nok(code4);
  }
}
function tokenizeWwwAutolink(effects, ok3, nok) {
  const self2 = this;
  return wwwStart;
  function wwwStart(code4) {
    if (code4 !== codes.uppercaseW && code4 !== codes.lowercaseW || !previousWww.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code4);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkWww");
    return effects.check(
      wwwPrefix,
      effects.attempt(domain, effects.attempt(path, wwwAfter), nok),
      nok
    )(code4);
  }
  function wwwAfter(code4) {
    effects.exit("literalAutolinkWww");
    effects.exit("literalAutolink");
    return ok3(code4);
  }
}
function tokenizeProtocolAutolink(effects, ok3, nok) {
  const self2 = this;
  let buffer = "";
  let seen = false;
  return protocolStart;
  function protocolStart(code4) {
    if ((code4 === codes.uppercaseH || code4 === codes.lowercaseH) && previousProtocol.call(self2, self2.previous) && !previousUnbalanced(self2.events)) {
      effects.enter("literalAutolink");
      effects.enter("literalAutolinkHttp");
      buffer += String.fromCodePoint(code4);
      effects.consume(code4);
      return protocolPrefixInside;
    }
    return nok(code4);
  }
  function protocolPrefixInside(code4) {
    if (asciiAlpha(code4) && buffer.length < 5) {
      buffer += String.fromCodePoint(code4);
      effects.consume(code4);
      return protocolPrefixInside;
    }
    if (code4 === codes.colon) {
      const protocol = buffer.toLowerCase();
      if (protocol === "http" || protocol === "https") {
        effects.consume(code4);
        return protocolSlashesInside;
      }
    }
    return nok(code4);
  }
  function protocolSlashesInside(code4) {
    if (code4 === codes.slash) {
      effects.consume(code4);
      if (seen) {
        return afterProtocol;
      }
      seen = true;
      return protocolSlashesInside;
    }
    return nok(code4);
  }
  function afterProtocol(code4) {
    return code4 === codes.eof || asciiControl(code4) || markdownLineEndingOrSpace(code4) || unicodeWhitespace(code4) || unicodePunctuation(code4) ? nok(code4) : effects.attempt(domain, effects.attempt(path, protocolAfter), nok)(code4);
  }
  function protocolAfter(code4) {
    effects.exit("literalAutolinkHttp");
    effects.exit("literalAutolink");
    return ok3(code4);
  }
}
function tokenizeWwwPrefix(effects, ok3, nok) {
  let size4 = 0;
  return wwwPrefixInside;
  function wwwPrefixInside(code4) {
    if ((code4 === codes.uppercaseW || code4 === codes.lowercaseW) && size4 < 3) {
      size4++;
      effects.consume(code4);
      return wwwPrefixInside;
    }
    if (code4 === codes.dot && size4 === 3) {
      effects.consume(code4);
      return wwwPrefixAfter;
    }
    return nok(code4);
  }
  function wwwPrefixAfter(code4) {
    return code4 === codes.eof ? nok(code4) : ok3(code4);
  }
}
function tokenizeDomain(effects, ok3, nok) {
  let underscoreInLastSegment;
  let underscoreInLastLastSegment;
  let seen;
  return domainInside;
  function domainInside(code4) {
    if (code4 === codes.dot || code4 === codes.underscore) {
      return effects.check(trail, domainAfter, domainAtPunctuation)(code4);
    }
    if (code4 === codes.eof || markdownLineEndingOrSpace(code4) || unicodeWhitespace(code4) || code4 !== codes.dash && unicodePunctuation(code4)) {
      return domainAfter(code4);
    }
    seen = true;
    effects.consume(code4);
    return domainInside;
  }
  function domainAtPunctuation(code4) {
    if (code4 === codes.underscore) {
      underscoreInLastSegment = true;
    } else {
      underscoreInLastLastSegment = underscoreInLastSegment;
      underscoreInLastSegment = void 0;
    }
    effects.consume(code4);
    return domainInside;
  }
  function domainAfter(code4) {
    if (underscoreInLastLastSegment || underscoreInLastSegment || !seen) {
      return nok(code4);
    }
    return ok3(code4);
  }
}
function tokenizePath(effects, ok3) {
  let sizeOpen = 0;
  let sizeClose = 0;
  return pathInside;
  function pathInside(code4) {
    if (code4 === codes.leftParenthesis) {
      sizeOpen++;
      effects.consume(code4);
      return pathInside;
    }
    if (code4 === codes.rightParenthesis && sizeClose < sizeOpen) {
      return pathAtPunctuation(code4);
    }
    if (code4 === codes.exclamationMark || code4 === codes.quotationMark || code4 === codes.ampersand || code4 === codes.apostrophe || code4 === codes.rightParenthesis || code4 === codes.asterisk || code4 === codes.comma || code4 === codes.dot || code4 === codes.colon || code4 === codes.semicolon || code4 === codes.lessThan || code4 === codes.questionMark || code4 === codes.rightSquareBracket || code4 === codes.underscore || code4 === codes.tilde) {
      return effects.check(trail, ok3, pathAtPunctuation)(code4);
    }
    if (code4 === codes.eof || markdownLineEndingOrSpace(code4) || unicodeWhitespace(code4)) {
      return ok3(code4);
    }
    effects.consume(code4);
    return pathInside;
  }
  function pathAtPunctuation(code4) {
    if (code4 === codes.rightParenthesis) {
      sizeClose++;
    }
    effects.consume(code4);
    return pathInside;
  }
}
function tokenizeTrail(effects, ok3, nok) {
  return trail2;
  function trail2(code4) {
    if (code4 === codes.exclamationMark || code4 === codes.quotationMark || code4 === codes.apostrophe || code4 === codes.rightParenthesis || code4 === codes.asterisk || code4 === codes.comma || code4 === codes.dot || code4 === codes.colon || code4 === codes.semicolon || code4 === codes.questionMark || code4 === codes.underscore || code4 === codes.tilde) {
      effects.consume(code4);
      return trail2;
    }
    if (code4 === codes.ampersand) {
      effects.consume(code4);
      return trailCharacterReferenceStart;
    }
    if (code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      return trailBracketAfter;
    }
    if (
      // `<` is an end.
      code4 === codes.lessThan || // So is whitespace.
      code4 === codes.eof || markdownLineEndingOrSpace(code4) || unicodeWhitespace(code4)
    ) {
      return ok3(code4);
    }
    return nok(code4);
  }
  function trailBracketAfter(code4) {
    if (code4 === codes.eof || code4 === codes.leftParenthesis || code4 === codes.leftSquareBracket || markdownLineEndingOrSpace(code4) || unicodeWhitespace(code4)) {
      return ok3(code4);
    }
    return trail2(code4);
  }
  function trailCharacterReferenceStart(code4) {
    return asciiAlpha(code4) ? trailCharacterReferenceInside(code4) : nok(code4);
  }
  function trailCharacterReferenceInside(code4) {
    if (code4 === codes.semicolon) {
      effects.consume(code4);
      return trail2;
    }
    if (asciiAlpha(code4)) {
      effects.consume(code4);
      return trailCharacterReferenceInside;
    }
    return nok(code4);
  }
}
function tokenizeEmailDomainDotTrail(effects, ok3, nok) {
  return start2;
  function start2(code4) {
    effects.consume(code4);
    return after;
  }
  function after(code4) {
    return asciiAlphanumeric(code4) ? nok(code4) : ok3(code4);
  }
}
function previousWww(code4) {
  return code4 === codes.eof || code4 === codes.leftParenthesis || code4 === codes.asterisk || code4 === codes.underscore || code4 === codes.leftSquareBracket || code4 === codes.rightSquareBracket || code4 === codes.tilde || markdownLineEndingOrSpace(code4);
}
function previousProtocol(code4) {
  return !asciiAlpha(code4);
}
function previousEmail(code4) {
  return !(code4 === codes.slash || gfmAtext(code4));
}
function gfmAtext(code4) {
  return code4 === codes.plusSign || code4 === codes.dash || code4 === codes.dot || code4 === codes.underscore || asciiAlphanumeric(code4);
}
function previousUnbalanced(events) {
  let index6 = events.length;
  let result = false;
  while (index6--) {
    const token = events[index6][1];
    if ((token.type === "labelLink" || token.type === "labelImage") && !token._balanced) {
      result = true;
      break;
    }
    if (token._gfmAutolinkLiteralWalkedInto) {
      result = false;
      break;
    }
  }
  if (events.length > 0 && !result) {
    events[events.length - 1][1]._gfmAutolinkLiteralWalkedInto = true;
  }
  return result;
}

// node_modules/micromark-extension-gfm-footnote/dev/lib/syntax.js
var indent = { tokenize: tokenizeIndent2, partial: true };
function gfmFootnote() {
  return {
    document: {
      [codes.leftSquareBracket]: {
        name: "gfmFootnoteDefinition",
        tokenize: tokenizeDefinitionStart,
        continuation: { tokenize: tokenizeDefinitionContinuation },
        exit: gfmFootnoteDefinitionEnd
      }
    },
    text: {
      [codes.leftSquareBracket]: {
        name: "gfmFootnoteCall",
        tokenize: tokenizeGfmFootnoteCall
      },
      [codes.rightSquareBracket]: {
        name: "gfmPotentialFootnoteCall",
        add: "after",
        tokenize: tokenizePotentialGfmFootnoteCall,
        resolveTo: resolveToPotentialGfmFootnoteCall
      }
    }
  };
}
function tokenizePotentialGfmFootnoteCall(effects, ok3, nok) {
  const self2 = this;
  let index6 = self2.events.length;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let labelStart;
  while (index6--) {
    const token = self2.events[index6][1];
    if (token.type === types.labelImage) {
      labelStart = token;
      break;
    }
    if (token.type === "gfmFootnoteCall" || token.type === types.labelLink || token.type === types.label || token.type === types.image || token.type === types.link) {
      break;
    }
  }
  return start2;
  function start2(code4) {
    ok(code4 === codes.rightSquareBracket, "expected `]`");
    if (!labelStart || !labelStart._balanced) {
      return nok(code4);
    }
    const id3 = normalizeIdentifier(
      self2.sliceSerialize({ start: labelStart.end, end: self2.now() })
    );
    if (id3.codePointAt(0) !== codes.caret || !defined.includes(id3.slice(1))) {
      return nok(code4);
    }
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code4);
    effects.exit("gfmFootnoteCallLabelMarker");
    return ok3(code4);
  }
}
function resolveToPotentialGfmFootnoteCall(events, context) {
  let index6 = events.length;
  let labelStart;
  while (index6--) {
    if (events[index6][1].type === types.labelImage && events[index6][0] === "enter") {
      labelStart = events[index6][1];
      break;
    }
  }
  ok(labelStart, "expected `labelStart` to resolve");
  events[index6 + 1][1].type = types.data;
  events[index6 + 3][1].type = "gfmFootnoteCallLabelMarker";
  const call2 = {
    type: "gfmFootnoteCall",
    start: Object.assign({}, events[index6 + 3][1].start),
    end: Object.assign({}, events[events.length - 1][1].end)
  };
  const marker = {
    type: "gfmFootnoteCallMarker",
    start: Object.assign({}, events[index6 + 3][1].end),
    end: Object.assign({}, events[index6 + 3][1].end)
  };
  marker.end.column++;
  marker.end.offset++;
  marker.end._bufferIndex++;
  const string3 = {
    type: "gfmFootnoteCallString",
    start: Object.assign({}, marker.end),
    end: Object.assign({}, events[events.length - 1][1].start)
  };
  const chunk = {
    type: types.chunkString,
    contentType: "string",
    start: Object.assign({}, string3.start),
    end: Object.assign({}, string3.end)
  };
  const replacement = [
    // Take the `labelImageMarker` (now `data`, the `!`)
    events[index6 + 1],
    events[index6 + 2],
    ["enter", call2, context],
    // The `[`
    events[index6 + 3],
    events[index6 + 4],
    // The `^`.
    ["enter", marker, context],
    ["exit", marker, context],
    // Everything in between.
    ["enter", string3, context],
    ["enter", chunk, context],
    ["exit", chunk, context],
    ["exit", string3, context],
    // The ending (`]`, properly parsed and labelled).
    events[events.length - 2],
    events[events.length - 1],
    ["exit", call2, context]
  ];
  events.splice(index6, events.length - index6 + 1, ...replacement);
  return events;
}
function tokenizeGfmFootnoteCall(effects, ok3, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let size4 = 0;
  let data;
  return start2;
  function start2(code4) {
    ok(code4 === codes.leftSquareBracket, "expected `[`");
    effects.enter("gfmFootnoteCall");
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code4);
    effects.exit("gfmFootnoteCallLabelMarker");
    return callStart;
  }
  function callStart(code4) {
    if (code4 !== codes.caret) return nok(code4);
    effects.enter("gfmFootnoteCallMarker");
    effects.consume(code4);
    effects.exit("gfmFootnoteCallMarker");
    effects.enter("gfmFootnoteCallString");
    effects.enter("chunkString").contentType = "string";
    return callData;
  }
  function callData(code4) {
    if (
      // Too long.
      size4 > constants.linkReferenceSizeMax || // Closing brace with nothing.
      code4 === codes.rightSquareBracket && !data || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code4 === codes.eof || code4 === codes.leftSquareBracket || markdownLineEndingOrSpace(code4)
    ) {
      return nok(code4);
    }
    if (code4 === codes.rightSquareBracket) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteCallString");
      if (!defined.includes(normalizeIdentifier(self2.sliceSerialize(token)))) {
        return nok(code4);
      }
      effects.enter("gfmFootnoteCallLabelMarker");
      effects.consume(code4);
      effects.exit("gfmFootnoteCallLabelMarker");
      effects.exit("gfmFootnoteCall");
      return ok3;
    }
    if (!markdownLineEndingOrSpace(code4)) {
      data = true;
    }
    size4++;
    effects.consume(code4);
    return code4 === codes.backslash ? callEscape : callData;
  }
  function callEscape(code4) {
    if (code4 === codes.leftSquareBracket || code4 === codes.backslash || code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      size4++;
      return callData;
    }
    return callData(code4);
  }
}
function tokenizeDefinitionStart(effects, ok3, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let identifier;
  let size4 = 0;
  let data;
  return start2;
  function start2(code4) {
    ok(code4 === codes.leftSquareBracket, "expected `[`");
    effects.enter("gfmFootnoteDefinition")._container = true;
    effects.enter("gfmFootnoteDefinitionLabel");
    effects.enter("gfmFootnoteDefinitionLabelMarker");
    effects.consume(code4);
    effects.exit("gfmFootnoteDefinitionLabelMarker");
    return labelAtMarker;
  }
  function labelAtMarker(code4) {
    if (code4 === codes.caret) {
      effects.enter("gfmFootnoteDefinitionMarker");
      effects.consume(code4);
      effects.exit("gfmFootnoteDefinitionMarker");
      effects.enter("gfmFootnoteDefinitionLabelString");
      effects.enter("chunkString").contentType = "string";
      return labelInside;
    }
    return nok(code4);
  }
  function labelInside(code4) {
    if (
      // Too long.
      size4 > constants.linkReferenceSizeMax || // Closing brace with nothing.
      code4 === codes.rightSquareBracket && !data || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code4 === codes.eof || code4 === codes.leftSquareBracket || markdownLineEndingOrSpace(code4)
    ) {
      return nok(code4);
    }
    if (code4 === codes.rightSquareBracket) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteDefinitionLabelString");
      identifier = normalizeIdentifier(self2.sliceSerialize(token));
      effects.enter("gfmFootnoteDefinitionLabelMarker");
      effects.consume(code4);
      effects.exit("gfmFootnoteDefinitionLabelMarker");
      effects.exit("gfmFootnoteDefinitionLabel");
      return labelAfter;
    }
    if (!markdownLineEndingOrSpace(code4)) {
      data = true;
    }
    size4++;
    effects.consume(code4);
    return code4 === codes.backslash ? labelEscape : labelInside;
  }
  function labelEscape(code4) {
    if (code4 === codes.leftSquareBracket || code4 === codes.backslash || code4 === codes.rightSquareBracket) {
      effects.consume(code4);
      size4++;
      return labelInside;
    }
    return labelInside(code4);
  }
  function labelAfter(code4) {
    if (code4 === codes.colon) {
      effects.enter("definitionMarker");
      effects.consume(code4);
      effects.exit("definitionMarker");
      if (!defined.includes(identifier)) {
        defined.push(identifier);
      }
      return factorySpace(
        effects,
        whitespaceAfter,
        "gfmFootnoteDefinitionWhitespace"
      );
    }
    return nok(code4);
  }
  function whitespaceAfter(code4) {
    return ok3(code4);
  }
}
function tokenizeDefinitionContinuation(effects, ok3, nok) {
  return effects.check(blankLine, ok3, effects.attempt(indent, ok3, nok));
}
function gfmFootnoteDefinitionEnd(effects) {
  effects.exit("gfmFootnoteDefinition");
}
function tokenizeIndent2(effects, ok3, nok) {
  const self2 = this;
  return factorySpace(
    effects,
    afterPrefix,
    "gfmFootnoteDefinitionIndent",
    constants.tabSize + 1
  );
  function afterPrefix(code4) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "gfmFootnoteDefinitionIndent" && tail[2].sliceSerialize(tail[1], true).length === constants.tabSize ? ok3(code4) : nok(code4);
  }
}

// node_modules/micromark-extension-gfm-footnote/dev/lib/html.js
var own7 = {}.hasOwnProperty;

// node_modules/micromark-extension-gfm-strikethrough/dev/lib/syntax.js
function gfmStrikethrough(options) {
  const options_ = options || {};
  let single = options_.singleTilde;
  const tokenizer = {
    name: "strikethrough",
    tokenize: tokenizeStrikethrough,
    resolveAll: resolveAllStrikethrough
  };
  if (single === null || single === void 0) {
    single = true;
  }
  return {
    text: { [codes.tilde]: tokenizer },
    insideSpan: { null: [tokenizer] },
    attentionMarkers: { null: [codes.tilde] }
  };
  function resolveAllStrikethrough(events, context) {
    let index6 = -1;
    while (++index6 < events.length) {
      if (events[index6][0] === "enter" && events[index6][1].type === "strikethroughSequenceTemporary" && events[index6][1]._close) {
        let open = index6;
        while (open--) {
          if (events[open][0] === "exit" && events[open][1].type === "strikethroughSequenceTemporary" && events[open][1]._open && // If the sizes are the same:
          events[index6][1].end.offset - events[index6][1].start.offset === events[open][1].end.offset - events[open][1].start.offset) {
            events[index6][1].type = "strikethroughSequence";
            events[open][1].type = "strikethroughSequence";
            const strikethrough2 = {
              type: "strikethrough",
              start: Object.assign({}, events[open][1].start),
              end: Object.assign({}, events[index6][1].end)
            };
            const text8 = {
              type: "strikethroughText",
              start: Object.assign({}, events[open][1].end),
              end: Object.assign({}, events[index6][1].start)
            };
            const nextEvents = [
              ["enter", strikethrough2, context],
              ["enter", events[open][1], context],
              ["exit", events[open][1], context],
              ["enter", text8, context]
            ];
            const insideSpan2 = context.parser.constructs.insideSpan.null;
            if (insideSpan2) {
              splice(
                nextEvents,
                nextEvents.length,
                0,
                resolveAll(insideSpan2, events.slice(open + 1, index6), context)
              );
            }
            splice(nextEvents, nextEvents.length, 0, [
              ["exit", text8, context],
              ["enter", events[index6][1], context],
              ["exit", events[index6][1], context],
              ["exit", strikethrough2, context]
            ]);
            splice(events, open - 1, index6 - open + 3, nextEvents);
            index6 = open + nextEvents.length - 2;
            break;
          }
        }
      }
    }
    index6 = -1;
    while (++index6 < events.length) {
      if (events[index6][1].type === "strikethroughSequenceTemporary") {
        events[index6][1].type = types.data;
      }
    }
    return events;
  }
  function tokenizeStrikethrough(effects, ok3, nok) {
    const previous3 = this.previous;
    const events = this.events;
    let size4 = 0;
    return start2;
    function start2(code4) {
      ok(code4 === codes.tilde, "expected `~`");
      if (previous3 === codes.tilde && events[events.length - 1][1].type !== types.characterEscape) {
        return nok(code4);
      }
      effects.enter("strikethroughSequenceTemporary");
      return more(code4);
    }
    function more(code4) {
      const before = classifyCharacter(previous3);
      if (code4 === codes.tilde) {
        if (size4 > 1) return nok(code4);
        effects.consume(code4);
        size4++;
        return more;
      }
      if (size4 < 2 && !single) return nok(code4);
      const token = effects.exit("strikethroughSequenceTemporary");
      const after = classifyCharacter(code4);
      token._open = !after || after === constants.attentionSideAfter && Boolean(before);
      token._close = !before || before === constants.attentionSideAfter && Boolean(after);
      return ok3(code4);
    }
  }
}

// node_modules/micromark-extension-gfm-table/dev/lib/edit-map.js
var EditMap = class {
  /**
   * Create a new edit map.
   */
  constructor() {
    this.map = [];
  }
  /**
   * Create an edit: a remove and/or add at a certain place.
   *
   * @param {number} index
   * @param {number} remove
   * @param {Array<Event>} add
   * @returns {undefined}
   */
  add(index6, remove, add) {
    addImplementation(this, index6, remove, add);
  }
  // To do: add this when moving to `micromark`.
  // /**
  //  * Create an edit: but insert `add` before existing additions.
  //  *
  //  * @param {number} index
  //  * @param {number} remove
  //  * @param {Array<Event>} add
  //  * @returns {undefined}
  //  */
  // addBefore(index, remove, add) {
  //   addImplementation(this, index, remove, add, true)
  // }
  /**
   * Done, change the events.
   *
   * @param {Array<Event>} events
   * @returns {undefined}
   */
  consume(events) {
    this.map.sort(function(a2, b2) {
      return a2[0] - b2[0];
    });
    if (this.map.length === 0) {
      return;
    }
    let index6 = this.map.length;
    const vecs = [];
    while (index6 > 0) {
      index6 -= 1;
      vecs.push(
        events.slice(this.map[index6][0] + this.map[index6][1]),
        this.map[index6][2]
      );
      events.length = this.map[index6][0];
    }
    vecs.push(events.slice());
    events.length = 0;
    let slice = vecs.pop();
    while (slice) {
      for (const element4 of slice) {
        events.push(element4);
      }
      slice = vecs.pop();
    }
    this.map.length = 0;
  }
};
function addImplementation(editMap, at2, remove, add) {
  let index6 = 0;
  if (remove === 0 && add.length === 0) {
    return;
  }
  while (index6 < editMap.map.length) {
    if (editMap.map[index6][0] === at2) {
      editMap.map[index6][1] += remove;
      editMap.map[index6][2].push(...add);
      return;
    }
    index6 += 1;
  }
  editMap.map.push([at2, remove, add]);
}

// node_modules/micromark-extension-gfm-table/dev/lib/infer.js
function gfmTableAlign(events, index6) {
  ok(events[index6][1].type === "table", "expected table");
  let inDelimiterRow = false;
  const align = [];
  while (index6 < events.length) {
    const event = events[index6];
    if (inDelimiterRow) {
      if (event[0] === "enter") {
        if (event[1].type === "tableContent") {
          align.push(
            events[index6 + 1][1].type === "tableDelimiterMarker" ? "left" : "none"
          );
        }
      } else if (event[1].type === "tableContent") {
        if (events[index6 - 1][1].type === "tableDelimiterMarker") {
          const alignIndex = align.length - 1;
          align[alignIndex] = align[alignIndex] === "left" ? "center" : "right";
        }
      } else if (event[1].type === "tableDelimiterRow") {
        break;
      }
    } else if (event[0] === "enter" && event[1].type === "tableDelimiterRow") {
      inDelimiterRow = true;
    }
    index6 += 1;
  }
  return align;
}

// node_modules/micromark-extension-gfm-table/dev/lib/syntax.js
function gfmTable() {
  return {
    flow: {
      null: { name: "table", tokenize: tokenizeTable, resolveAll: resolveTable }
    }
  };
}
function tokenizeTable(effects, ok3, nok) {
  const self2 = this;
  let size4 = 0;
  let sizeB = 0;
  let seen;
  return start2;
  function start2(code4) {
    let index6 = self2.events.length - 1;
    while (index6 > -1) {
      const type = self2.events[index6][1].type;
      if (type === types.lineEnding || // Note: markdown-rs uses `whitespace` instead of `linePrefix`
      type === types.linePrefix)
        index6--;
      else break;
    }
    const tail = index6 > -1 ? self2.events[index6][1].type : null;
    const next = tail === "tableHead" || tail === "tableRow" ? bodyRowStart : headRowBefore;
    if (next === bodyRowStart && self2.parser.lazy[self2.now().line]) {
      return nok(code4);
    }
    return next(code4);
  }
  function headRowBefore(code4) {
    effects.enter("tableHead");
    effects.enter("tableRow");
    return headRowStart(code4);
  }
  function headRowStart(code4) {
    if (code4 === codes.verticalBar) {
      return headRowBreak(code4);
    }
    seen = true;
    sizeB += 1;
    return headRowBreak(code4);
  }
  function headRowBreak(code4) {
    if (code4 === codes.eof) {
      return nok(code4);
    }
    if (markdownLineEnding(code4)) {
      if (sizeB > 1) {
        sizeB = 0;
        self2.interrupt = true;
        effects.exit("tableRow");
        effects.enter(types.lineEnding);
        effects.consume(code4);
        effects.exit(types.lineEnding);
        return headDelimiterStart;
      }
      return nok(code4);
    }
    if (markdownSpace(code4)) {
      return factorySpace(effects, headRowBreak, types.whitespace)(code4);
    }
    sizeB += 1;
    if (seen) {
      seen = false;
      size4 += 1;
    }
    if (code4 === codes.verticalBar) {
      effects.enter("tableCellDivider");
      effects.consume(code4);
      effects.exit("tableCellDivider");
      seen = true;
      return headRowBreak;
    }
    effects.enter(types.data);
    return headRowData(code4);
  }
  function headRowData(code4) {
    if (code4 === codes.eof || code4 === codes.verticalBar || markdownLineEndingOrSpace(code4)) {
      effects.exit(types.data);
      return headRowBreak(code4);
    }
    effects.consume(code4);
    return code4 === codes.backslash ? headRowEscape : headRowData;
  }
  function headRowEscape(code4) {
    if (code4 === codes.backslash || code4 === codes.verticalBar) {
      effects.consume(code4);
      return headRowData;
    }
    return headRowData(code4);
  }
  function headDelimiterStart(code4) {
    self2.interrupt = false;
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code4);
    }
    effects.enter("tableDelimiterRow");
    seen = false;
    if (markdownSpace(code4)) {
      ok(self2.parser.constructs.disable.null, "expected `disabled.null`");
      return factorySpace(
        effects,
        headDelimiterBefore,
        types.linePrefix,
        self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : constants.tabSize
      )(code4);
    }
    return headDelimiterBefore(code4);
  }
  function headDelimiterBefore(code4) {
    if (code4 === codes.dash || code4 === codes.colon) {
      return headDelimiterValueBefore(code4);
    }
    if (code4 === codes.verticalBar) {
      seen = true;
      effects.enter("tableCellDivider");
      effects.consume(code4);
      effects.exit("tableCellDivider");
      return headDelimiterCellBefore;
    }
    return headDelimiterNok(code4);
  }
  function headDelimiterCellBefore(code4) {
    if (markdownSpace(code4)) {
      return factorySpace(
        effects,
        headDelimiterValueBefore,
        types.whitespace
      )(code4);
    }
    return headDelimiterValueBefore(code4);
  }
  function headDelimiterValueBefore(code4) {
    if (code4 === codes.colon) {
      sizeB += 1;
      seen = true;
      effects.enter("tableDelimiterMarker");
      effects.consume(code4);
      effects.exit("tableDelimiterMarker");
      return headDelimiterLeftAlignmentAfter;
    }
    if (code4 === codes.dash) {
      sizeB += 1;
      return headDelimiterLeftAlignmentAfter(code4);
    }
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      return headDelimiterCellAfter(code4);
    }
    return headDelimiterNok(code4);
  }
  function headDelimiterLeftAlignmentAfter(code4) {
    if (code4 === codes.dash) {
      effects.enter("tableDelimiterFiller");
      return headDelimiterFiller(code4);
    }
    return headDelimiterNok(code4);
  }
  function headDelimiterFiller(code4) {
    if (code4 === codes.dash) {
      effects.consume(code4);
      return headDelimiterFiller;
    }
    if (code4 === codes.colon) {
      seen = true;
      effects.exit("tableDelimiterFiller");
      effects.enter("tableDelimiterMarker");
      effects.consume(code4);
      effects.exit("tableDelimiterMarker");
      return headDelimiterRightAlignmentAfter;
    }
    effects.exit("tableDelimiterFiller");
    return headDelimiterRightAlignmentAfter(code4);
  }
  function headDelimiterRightAlignmentAfter(code4) {
    if (markdownSpace(code4)) {
      return factorySpace(
        effects,
        headDelimiterCellAfter,
        types.whitespace
      )(code4);
    }
    return headDelimiterCellAfter(code4);
  }
  function headDelimiterCellAfter(code4) {
    if (code4 === codes.verticalBar) {
      return headDelimiterBefore(code4);
    }
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      if (!seen || size4 !== sizeB) {
        return headDelimiterNok(code4);
      }
      effects.exit("tableDelimiterRow");
      effects.exit("tableHead");
      return ok3(code4);
    }
    return headDelimiterNok(code4);
  }
  function headDelimiterNok(code4) {
    return nok(code4);
  }
  function bodyRowStart(code4) {
    effects.enter("tableRow");
    return bodyRowBreak(code4);
  }
  function bodyRowBreak(code4) {
    if (code4 === codes.verticalBar) {
      effects.enter("tableCellDivider");
      effects.consume(code4);
      effects.exit("tableCellDivider");
      return bodyRowBreak;
    }
    if (code4 === codes.eof || markdownLineEnding(code4)) {
      effects.exit("tableRow");
      return ok3(code4);
    }
    if (markdownSpace(code4)) {
      return factorySpace(effects, bodyRowBreak, types.whitespace)(code4);
    }
    effects.enter(types.data);
    return bodyRowData(code4);
  }
  function bodyRowData(code4) {
    if (code4 === codes.eof || code4 === codes.verticalBar || markdownLineEndingOrSpace(code4)) {
      effects.exit(types.data);
      return bodyRowBreak(code4);
    }
    effects.consume(code4);
    return code4 === codes.backslash ? bodyRowEscape : bodyRowData;
  }
  function bodyRowEscape(code4) {
    if (code4 === codes.backslash || code4 === codes.verticalBar) {
      effects.consume(code4);
      return bodyRowData;
    }
    return bodyRowData(code4);
  }
}
function resolveTable(events, context) {
  let index6 = -1;
  let inFirstCellAwaitingPipe = true;
  let rowKind = 0;
  let lastCell = [0, 0, 0, 0];
  let cell = [0, 0, 0, 0];
  let afterHeadAwaitingFirstBodyRow = false;
  let lastTableEnd = 0;
  let currentTable;
  let currentBody;
  let currentCell;
  const map4 = new EditMap();
  while (++index6 < events.length) {
    const event = events[index6];
    const token = event[1];
    if (event[0] === "enter") {
      if (token.type === "tableHead") {
        afterHeadAwaitingFirstBodyRow = false;
        if (lastTableEnd !== 0) {
          ok(currentTable, "there should be a table opening");
          flushTableEnd(map4, context, lastTableEnd, currentTable, currentBody);
          currentBody = void 0;
          lastTableEnd = 0;
        }
        currentTable = {
          type: "table",
          start: Object.assign({}, token.start),
          // Note: correct end is set later.
          end: Object.assign({}, token.end)
        };
        map4.add(index6, 0, [["enter", currentTable, context]]);
      } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
        inFirstCellAwaitingPipe = true;
        currentCell = void 0;
        lastCell = [0, 0, 0, 0];
        cell = [0, index6 + 1, 0, 0];
        if (afterHeadAwaitingFirstBodyRow) {
          afterHeadAwaitingFirstBodyRow = false;
          currentBody = {
            type: "tableBody",
            start: Object.assign({}, token.start),
            // Note: correct end is set later.
            end: Object.assign({}, token.end)
          };
          map4.add(index6, 0, [["enter", currentBody, context]]);
        }
        rowKind = token.type === "tableDelimiterRow" ? 2 : currentBody ? 3 : 1;
      } else if (rowKind && (token.type === types.data || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
        inFirstCellAwaitingPipe = false;
        if (cell[2] === 0) {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(
              map4,
              context,
              lastCell,
              rowKind,
              void 0,
              currentCell
            );
            lastCell = [0, 0, 0, 0];
          }
          cell[2] = index6;
        }
      } else if (token.type === "tableCellDivider") {
        if (inFirstCellAwaitingPipe) {
          inFirstCellAwaitingPipe = false;
        } else {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(
              map4,
              context,
              lastCell,
              rowKind,
              void 0,
              currentCell
            );
          }
          lastCell = cell;
          cell = [lastCell[1], index6, 0, 0];
        }
      }
    } else if (token.type === "tableHead") {
      afterHeadAwaitingFirstBodyRow = true;
      lastTableEnd = index6;
    } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
      lastTableEnd = index6;
      if (lastCell[1] !== 0) {
        cell[0] = cell[1];
        currentCell = flushCell(
          map4,
          context,
          lastCell,
          rowKind,
          index6,
          currentCell
        );
      } else if (cell[1] !== 0) {
        currentCell = flushCell(map4, context, cell, rowKind, index6, currentCell);
      }
      rowKind = 0;
    } else if (rowKind && (token.type === types.data || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
      cell[3] = index6;
    }
  }
  if (lastTableEnd !== 0) {
    ok(currentTable, "expected table opening");
    flushTableEnd(map4, context, lastTableEnd, currentTable, currentBody);
  }
  map4.consume(context.events);
  index6 = -1;
  while (++index6 < context.events.length) {
    const event = context.events[index6];
    if (event[0] === "enter" && event[1].type === "table") {
      event[1]._align = gfmTableAlign(context.events, index6);
    }
  }
  return events;
}
function flushCell(map4, context, range, rowKind, rowEnd, previousCell) {
  const groupName = rowKind === 1 ? "tableHeader" : rowKind === 2 ? "tableDelimiter" : "tableData";
  const valueName = "tableContent";
  if (range[0] !== 0) {
    ok(previousCell, "expected previous cell enter");
    previousCell.end = Object.assign({}, getPoint(context.events, range[0]));
    map4.add(range[0], 0, [["exit", previousCell, context]]);
  }
  const now = getPoint(context.events, range[1]);
  previousCell = {
    type: groupName,
    start: Object.assign({}, now),
    // Note: correct end is set later.
    end: Object.assign({}, now)
  };
  map4.add(range[1], 0, [["enter", previousCell, context]]);
  if (range[2] !== 0) {
    const relatedStart = getPoint(context.events, range[2]);
    const relatedEnd = getPoint(context.events, range[3]);
    const valueToken = {
      type: valueName,
      start: Object.assign({}, relatedStart),
      end: Object.assign({}, relatedEnd)
    };
    map4.add(range[2], 0, [["enter", valueToken, context]]);
    ok(range[3] !== 0);
    if (rowKind !== 2) {
      const start2 = context.events[range[2]];
      const end = context.events[range[3]];
      start2[1].end = Object.assign({}, end[1].end);
      start2[1].type = types.chunkText;
      start2[1].contentType = constants.contentTypeText;
      if (range[3] > range[2] + 1) {
        const a2 = range[2] + 1;
        const b2 = range[3] - range[2] - 1;
        map4.add(a2, b2, []);
      }
    }
    map4.add(range[3] + 1, 0, [["exit", valueToken, context]]);
  }
  if (rowEnd !== void 0) {
    previousCell.end = Object.assign({}, getPoint(context.events, rowEnd));
    map4.add(rowEnd, 0, [["exit", previousCell, context]]);
    previousCell = void 0;
  }
  return previousCell;
}
function flushTableEnd(map4, context, index6, table2, tableBody) {
  const exits = [];
  const related = getPoint(context.events, index6);
  if (tableBody) {
    tableBody.end = Object.assign({}, related);
    exits.push(["exit", tableBody, context]);
  }
  table2.end = Object.assign({}, related);
  exits.push(["exit", table2, context]);
  map4.add(index6 + 1, 0, exits);
}
function getPoint(events, index6) {
  const event = events[index6];
  const side = event[0] === "enter" ? "start" : "end";
  return event[1][side];
}

// node_modules/micromark-extension-gfm-tagfilter/lib/index.js
var reFlow = /<(\/?)(iframe|noembed|noframes|plaintext|script|style|title|textarea|xmp)(?=[\t\n\f\r />])/gi;
var reText = new RegExp("^" + reFlow.source, "i");

// node_modules/micromark-extension-gfm-task-list-item/dev/lib/syntax.js
var tasklistCheck = { name: "tasklistCheck", tokenize: tokenizeTasklistCheck };
function gfmTaskListItem() {
  return {
    text: { [codes.leftSquareBracket]: tasklistCheck }
  };
}
function tokenizeTasklistCheck(effects, ok3, nok) {
  const self2 = this;
  return open;
  function open(code4) {
    ok(code4 === codes.leftSquareBracket, "expected `[`");
    if (
      // Exit if there’s stuff before.
      self2.previous !== codes.eof || // Exit if not in the first content that is the first child of a list
      // item.
      !self2._gfmTasklistFirstContentOfListItem
    ) {
      return nok(code4);
    }
    effects.enter("taskListCheck");
    effects.enter("taskListCheckMarker");
    effects.consume(code4);
    effects.exit("taskListCheckMarker");
    return inside;
  }
  function inside(code4) {
    if (markdownLineEndingOrSpace(code4)) {
      effects.enter("taskListCheckValueUnchecked");
      effects.consume(code4);
      effects.exit("taskListCheckValueUnchecked");
      return close;
    }
    if (code4 === codes.uppercaseX || code4 === codes.lowercaseX) {
      effects.enter("taskListCheckValueChecked");
      effects.consume(code4);
      effects.exit("taskListCheckValueChecked");
      return close;
    }
    return nok(code4);
  }
  function close(code4) {
    if (code4 === codes.rightSquareBracket) {
      effects.enter("taskListCheckMarker");
      effects.consume(code4);
      effects.exit("taskListCheckMarker");
      effects.exit("taskListCheck");
      return after;
    }
    return nok(code4);
  }
  function after(code4) {
    if (markdownLineEnding(code4)) {
      return ok3(code4);
    }
    if (markdownSpace(code4)) {
      return effects.check({ tokenize: spaceThenNonSpace }, ok3, nok)(code4);
    }
    return nok(code4);
  }
}
function spaceThenNonSpace(effects, ok3, nok) {
  return factorySpace(effects, after, types.whitespace);
  function after(code4) {
    return code4 === codes.eof ? nok(code4) : ok3(code4);
  }
}

// node_modules/micromark-extension-gfm/index.js
function gfm(options) {
  return combineExtensions([
    gfmAutolinkLiteral(),
    gfmFootnote(),
    gfmStrikethrough(options),
    gfmTable(),
    gfmTaskListItem()
  ]);
}

// node_modules/remark-gfm/lib/index.js
var emptyOptions4 = {};
function remarkGfm(options) {
  const self2 = (
    /** @type {Processor<Root>} */
    this
  );
  const settings = options || emptyOptions4;
  const data = self2.data();
  const micromarkExtensions = data.micromarkExtensions || (data.micromarkExtensions = []);
  const fromMarkdownExtensions = data.fromMarkdownExtensions || (data.fromMarkdownExtensions = []);
  const toMarkdownExtensions = data.toMarkdownExtensions || (data.toMarkdownExtensions = []);
  micromarkExtensions.push(gfm(settings));
  fromMarkdownExtensions.push(gfmFromMarkdown());
  toMarkdownExtensions.push(gfmToMarkdown(settings));
}

// node_modules/stream-chat-react/dist/es/index.mjs
var import_lodash4 = __toESM(require_lodash3(), 1);
var import_shim2 = __toESM(require_shim(), 1);
var import_lodash5 = __toESM(require_lodash2(), 1);

// node_modules/i18next/dist/esm/i18next.js
var isString = (obj) => typeof obj === "string";
var defer = () => {
  let res;
  let rej;
  const promise = new Promise((resolve, reject) => {
    res = resolve;
    rej = reject;
  });
  promise.resolve = res;
  promise.reject = rej;
  return promise;
};
var makeString = (object) => {
  if (object == null) return "";
  return "" + object;
};
var copy = (a2, s2, t2) => {
  a2.forEach((m2) => {
    if (s2[m2]) t2[m2] = s2[m2];
  });
};
var lastOfPathSeparatorRegExp = /###/g;
var cleanKey = (key) => key && key.indexOf("###") > -1 ? key.replace(lastOfPathSeparatorRegExp, ".") : key;
var canNotTraverseDeeper = (object) => !object || isString(object);
var getLastOfPath = (object, path2, Empty) => {
  const stack = !isString(path2) ? path2 : path2.split(".");
  let stackIndex = 0;
  while (stackIndex < stack.length - 1) {
    if (canNotTraverseDeeper(object)) return {};
    const key = cleanKey(stack[stackIndex]);
    if (!object[key] && Empty) object[key] = new Empty();
    if (Object.prototype.hasOwnProperty.call(object, key)) {
      object = object[key];
    } else {
      object = {};
    }
    ++stackIndex;
  }
  if (canNotTraverseDeeper(object)) return {};
  return {
    obj: object,
    k: cleanKey(stack[stackIndex])
  };
};
var setPath = (object, path2, newValue) => {
  const {
    obj,
    k: k2
  } = getLastOfPath(object, path2, Object);
  if (obj !== void 0 || path2.length === 1) {
    obj[k2] = newValue;
    return;
  }
  let e = path2[path2.length - 1];
  let p2 = path2.slice(0, path2.length - 1);
  let last = getLastOfPath(object, p2, Object);
  while (last.obj === void 0 && p2.length) {
    e = `${p2[p2.length - 1]}.${e}`;
    p2 = p2.slice(0, p2.length - 1);
    last = getLastOfPath(object, p2, Object);
    if (last?.obj && typeof last.obj[`${last.k}.${e}`] !== "undefined") {
      last.obj = void 0;
    }
  }
  last.obj[`${last.k}.${e}`] = newValue;
};
var pushPath = (object, path2, newValue, concat) => {
  const {
    obj,
    k: k2
  } = getLastOfPath(object, path2, Object);
  obj[k2] = obj[k2] || [];
  obj[k2].push(newValue);
};
var getPath = (object, path2) => {
  const {
    obj,
    k: k2
  } = getLastOfPath(object, path2);
  if (!obj) return void 0;
  if (!Object.prototype.hasOwnProperty.call(obj, k2)) return void 0;
  return obj[k2];
};
var getPathWithDefaults = (data, defaultData, key) => {
  const value = getPath(data, key);
  if (value !== void 0) {
    return value;
  }
  return getPath(defaultData, key);
};
var deepExtend = (target, source, overwrite) => {
  for (const prop in source) {
    if (prop !== "__proto__" && prop !== "constructor") {
      if (prop in target) {
        if (isString(target[prop]) || target[prop] instanceof String || isString(source[prop]) || source[prop] instanceof String) {
          if (overwrite) target[prop] = source[prop];
        } else {
          deepExtend(target[prop], source[prop], overwrite);
        }
      } else {
        target[prop] = source[prop];
      }
    }
  }
  return target;
};
var regexEscape = (str) => str.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
var _entityMap = {
  "&": "&amp;",
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;",
  "/": "&#x2F;"
};
var escape = (data) => {
  if (isString(data)) {
    return data.replace(/[&<>"'\/]/g, (s2) => _entityMap[s2]);
  }
  return data;
};
var RegExpCache = class {
  constructor(capacity) {
    this.capacity = capacity;
    this.regExpMap = /* @__PURE__ */ new Map();
    this.regExpQueue = [];
  }
  getRegExp(pattern) {
    const regExpFromCache = this.regExpMap.get(pattern);
    if (regExpFromCache !== void 0) {
      return regExpFromCache;
    }
    const regExpNew = new RegExp(pattern);
    if (this.regExpQueue.length === this.capacity) {
      this.regExpMap.delete(this.regExpQueue.shift());
    }
    this.regExpMap.set(pattern, regExpNew);
    this.regExpQueue.push(pattern);
    return regExpNew;
  }
};
var chars = [" ", ",", "?", "!", ";"];
var looksLikeObjectPathRegExpCache = new RegExpCache(20);
var looksLikeObjectPath = (key, nsSeparator, keySeparator) => {
  nsSeparator = nsSeparator || "";
  keySeparator = keySeparator || "";
  const possibleChars = chars.filter((c2) => nsSeparator.indexOf(c2) < 0 && keySeparator.indexOf(c2) < 0);
  if (possibleChars.length === 0) return true;
  const r3 = looksLikeObjectPathRegExpCache.getRegExp(`(${possibleChars.map((c2) => c2 === "?" ? "\\?" : c2).join("|")})`);
  let matched = !r3.test(key);
  if (!matched) {
    const ki = key.indexOf(keySeparator);
    if (ki > 0 && !r3.test(key.substring(0, ki))) {
      matched = true;
    }
  }
  return matched;
};
var deepFind = (obj, path2, keySeparator = ".") => {
  if (!obj) return void 0;
  if (obj[path2]) {
    if (!Object.prototype.hasOwnProperty.call(obj, path2)) return void 0;
    return obj[path2];
  }
  const tokens = path2.split(keySeparator);
  let current = obj;
  for (let i2 = 0; i2 < tokens.length; ) {
    if (!current || typeof current !== "object") {
      return void 0;
    }
    let next;
    let nextPath = "";
    for (let j2 = i2; j2 < tokens.length; ++j2) {
      if (j2 !== i2) {
        nextPath += keySeparator;
      }
      nextPath += tokens[j2];
      next = current[nextPath];
      if (next !== void 0) {
        if (["string", "number", "boolean"].indexOf(typeof next) > -1 && j2 < tokens.length - 1) {
          continue;
        }
        i2 += j2 - i2 + 1;
        break;
      }
    }
    current = next;
  }
  return current;
};
var getCleanedCode = (code4) => code4?.replace(/_/g, "-");
var consoleLogger = {
  type: "logger",
  log(args) {
    this.output("log", args);
  },
  warn(args) {
    this.output("warn", args);
  },
  error(args) {
    this.output("error", args);
  },
  output(type, args) {
    console?.[type]?.apply?.(console, args);
  }
};
var Logger = class _Logger {
  constructor(concreteLogger, options = {}) {
    this.init(concreteLogger, options);
  }
  init(concreteLogger, options = {}) {
    this.prefix = options.prefix || "i18next:";
    this.logger = concreteLogger || consoleLogger;
    this.options = options;
    this.debug = options.debug;
  }
  log(...args) {
    return this.forward(args, "log", "", true);
  }
  warn(...args) {
    return this.forward(args, "warn", "", true);
  }
  error(...args) {
    return this.forward(args, "error", "");
  }
  deprecate(...args) {
    return this.forward(args, "warn", "WARNING DEPRECATED: ", true);
  }
  forward(args, lvl, prefix, debugOnly) {
    if (debugOnly && !this.debug) return null;
    if (isString(args[0])) args[0] = `${prefix}${this.prefix} ${args[0]}`;
    return this.logger[lvl](args);
  }
  create(moduleName) {
    return new _Logger(this.logger, {
      ...{
        prefix: `${this.prefix}:${moduleName}:`
      },
      ...this.options
    });
  }
  clone(options) {
    options = options || this.options;
    options.prefix = options.prefix || this.prefix;
    return new _Logger(this.logger, options);
  }
};
var baseLogger = new Logger();
var EventEmitter = class {
  constructor() {
    this.observers = {};
  }
  on(events, listener) {
    events.split(" ").forEach((event) => {
      if (!this.observers[event]) this.observers[event] = /* @__PURE__ */ new Map();
      const numListeners = this.observers[event].get(listener) || 0;
      this.observers[event].set(listener, numListeners + 1);
    });
    return this;
  }
  off(event, listener) {
    if (!this.observers[event]) return;
    if (!listener) {
      delete this.observers[event];
      return;
    }
    this.observers[event].delete(listener);
  }
  emit(event, ...args) {
    if (this.observers[event]) {
      const cloned = Array.from(this.observers[event].entries());
      cloned.forEach(([observer, numTimesAdded]) => {
        for (let i2 = 0; i2 < numTimesAdded; i2++) {
          observer(...args);
        }
      });
    }
    if (this.observers["*"]) {
      const cloned = Array.from(this.observers["*"].entries());
      cloned.forEach(([observer, numTimesAdded]) => {
        for (let i2 = 0; i2 < numTimesAdded; i2++) {
          observer.apply(observer, [event, ...args]);
        }
      });
    }
  }
};
var ResourceStore = class extends EventEmitter {
  constructor(data, options = {
    ns: ["translation"],
    defaultNS: "translation"
  }) {
    super();
    this.data = data || {};
    this.options = options;
    if (this.options.keySeparator === void 0) {
      this.options.keySeparator = ".";
    }
    if (this.options.ignoreJSONStructure === void 0) {
      this.options.ignoreJSONStructure = true;
    }
  }
  addNamespaces(ns) {
    if (this.options.ns.indexOf(ns) < 0) {
      this.options.ns.push(ns);
    }
  }
  removeNamespaces(ns) {
    const index6 = this.options.ns.indexOf(ns);
    if (index6 > -1) {
      this.options.ns.splice(index6, 1);
    }
  }
  getResource(lng, ns, key, options = {}) {
    const keySeparator = options.keySeparator !== void 0 ? options.keySeparator : this.options.keySeparator;
    const ignoreJSONStructure = options.ignoreJSONStructure !== void 0 ? options.ignoreJSONStructure : this.options.ignoreJSONStructure;
    let path2;
    if (lng.indexOf(".") > -1) {
      path2 = lng.split(".");
    } else {
      path2 = [lng, ns];
      if (key) {
        if (Array.isArray(key)) {
          path2.push(...key);
        } else if (isString(key) && keySeparator) {
          path2.push(...key.split(keySeparator));
        } else {
          path2.push(key);
        }
      }
    }
    const result = getPath(this.data, path2);
    if (!result && !ns && !key && lng.indexOf(".") > -1) {
      lng = path2[0];
      ns = path2[1];
      key = path2.slice(2).join(".");
    }
    if (result || !ignoreJSONStructure || !isString(key)) return result;
    return deepFind(this.data?.[lng]?.[ns], key, keySeparator);
  }
  addResource(lng, ns, key, value, options = {
    silent: false
  }) {
    const keySeparator = options.keySeparator !== void 0 ? options.keySeparator : this.options.keySeparator;
    let path2 = [lng, ns];
    if (key) path2 = path2.concat(keySeparator ? key.split(keySeparator) : key);
    if (lng.indexOf(".") > -1) {
      path2 = lng.split(".");
      value = ns;
      ns = path2[1];
    }
    this.addNamespaces(ns);
    setPath(this.data, path2, value);
    if (!options.silent) this.emit("added", lng, ns, key, value);
  }
  addResources(lng, ns, resources, options = {
    silent: false
  }) {
    for (const m2 in resources) {
      if (isString(resources[m2]) || Array.isArray(resources[m2])) this.addResource(lng, ns, m2, resources[m2], {
        silent: true
      });
    }
    if (!options.silent) this.emit("added", lng, ns, resources);
  }
  addResourceBundle(lng, ns, resources, deep, overwrite, options = {
    silent: false,
    skipCopy: false
  }) {
    let path2 = [lng, ns];
    if (lng.indexOf(".") > -1) {
      path2 = lng.split(".");
      deep = resources;
      resources = ns;
      ns = path2[1];
    }
    this.addNamespaces(ns);
    let pack = getPath(this.data, path2) || {};
    if (!options.skipCopy) resources = JSON.parse(JSON.stringify(resources));
    if (deep) {
      deepExtend(pack, resources, overwrite);
    } else {
      pack = {
        ...pack,
        ...resources
      };
    }
    setPath(this.data, path2, pack);
    if (!options.silent) this.emit("added", lng, ns, resources);
  }
  removeResourceBundle(lng, ns) {
    if (this.hasResourceBundle(lng, ns)) {
      delete this.data[lng][ns];
    }
    this.removeNamespaces(ns);
    this.emit("removed", lng, ns);
  }
  hasResourceBundle(lng, ns) {
    return this.getResource(lng, ns) !== void 0;
  }
  getResourceBundle(lng, ns) {
    if (!ns) ns = this.options.defaultNS;
    return this.getResource(lng, ns);
  }
  getDataByLanguage(lng) {
    return this.data[lng];
  }
  hasLanguageSomeTranslations(lng) {
    const data = this.getDataByLanguage(lng);
    const n2 = data && Object.keys(data) || [];
    return !!n2.find((v2) => data[v2] && Object.keys(data[v2]).length > 0);
  }
  toJSON() {
    return this.data;
  }
};
var postProcessor = {
  processors: {},
  addPostProcessor(module) {
    this.processors[module.name] = module;
  },
  handle(processors, value, key, options, translator) {
    processors.forEach((processor) => {
      value = this.processors[processor]?.process(value, key, options, translator) ?? value;
    });
    return value;
  }
};
var PATH_KEY = /* @__PURE__ */ Symbol("i18next/PATH_KEY");
function createProxy() {
  const state = [];
  const handler = /* @__PURE__ */ Object.create(null);
  let proxy;
  handler.get = (target, key) => {
    proxy?.revoke?.();
    if (key === PATH_KEY) return state;
    state.push(key);
    proxy = Proxy.revocable(target, handler);
    return proxy.proxy;
  };
  return Proxy.revocable(/* @__PURE__ */ Object.create(null), handler).proxy;
}
function keysFromSelector(selector2, opts) {
  const {
    [PATH_KEY]: path2
  } = selector2(createProxy());
  const keySeparator = opts?.keySeparator ?? ".";
  const nsSeparator = opts?.nsSeparator ?? ":";
  if (path2.length > 1 && nsSeparator) {
    const ns = opts?.ns;
    const nsArray = Array.isArray(ns) ? ns : null;
    if (nsArray && nsArray.length > 1 && nsArray.slice(1).includes(path2[0])) {
      return `${path2[0]}${nsSeparator}${path2.slice(1).join(keySeparator)}`;
    }
  }
  return path2.join(keySeparator);
}
var checkedLoadedFor = {};
var shouldHandleAsObject = (res) => !isString(res) && typeof res !== "boolean" && typeof res !== "number";
var Translator = class _Translator extends EventEmitter {
  constructor(services, options = {}) {
    super();
    copy(["resourceStore", "languageUtils", "pluralResolver", "interpolator", "backendConnector", "i18nFormat", "utils"], services, this);
    this.options = options;
    if (this.options.keySeparator === void 0) {
      this.options.keySeparator = ".";
    }
    this.logger = baseLogger.create("translator");
  }
  changeLanguage(lng) {
    if (lng) this.language = lng;
  }
  exists(key, o2 = {
    interpolation: {}
  }) {
    const opt = {
      ...o2
    };
    if (key == null) return false;
    const resolved = this.resolve(key, opt);
    if (resolved?.res === void 0) return false;
    const isObject2 = shouldHandleAsObject(resolved.res);
    if (opt.returnObjects === false && isObject2) {
      return false;
    }
    return true;
  }
  extractFromKey(key, opt) {
    let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
    if (nsSeparator === void 0) nsSeparator = ":";
    const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
    let namespaces = opt.ns || this.options.defaultNS || [];
    const wouldCheckForNsInKey = nsSeparator && key.indexOf(nsSeparator) > -1;
    const seemsNaturalLanguage = !this.options.userDefinedKeySeparator && !opt.keySeparator && !this.options.userDefinedNsSeparator && !opt.nsSeparator && !looksLikeObjectPath(key, nsSeparator, keySeparator);
    if (wouldCheckForNsInKey && !seemsNaturalLanguage) {
      const m2 = key.match(this.interpolator.nestingRegexp);
      if (m2 && m2.length > 0) {
        return {
          key,
          namespaces: isString(namespaces) ? [namespaces] : namespaces
        };
      }
      const parts = key.split(nsSeparator);
      if (nsSeparator !== keySeparator || nsSeparator === keySeparator && this.options.ns.indexOf(parts[0]) > -1) namespaces = parts.shift();
      key = parts.join(keySeparator);
    }
    return {
      key,
      namespaces: isString(namespaces) ? [namespaces] : namespaces
    };
  }
  translate(keys3, o2, lastKey) {
    let opt = typeof o2 === "object" ? {
      ...o2
    } : o2;
    if (typeof opt !== "object" && this.options.overloadTranslationOptionHandler) {
      opt = this.options.overloadTranslationOptionHandler(arguments);
    }
    if (typeof opt === "object") opt = {
      ...opt
    };
    if (!opt) opt = {};
    if (keys3 == null) return "";
    if (typeof keys3 === "function") keys3 = keysFromSelector(keys3, {
      ...this.options,
      ...opt
    });
    if (!Array.isArray(keys3)) keys3 = [String(keys3)];
    keys3 = keys3.map((k2) => typeof k2 === "function" ? keysFromSelector(k2, {
      ...this.options,
      ...opt
    }) : String(k2));
    const returnDetails = opt.returnDetails !== void 0 ? opt.returnDetails : this.options.returnDetails;
    const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
    const {
      key,
      namespaces
    } = this.extractFromKey(keys3[keys3.length - 1], opt);
    const namespace = namespaces[namespaces.length - 1];
    let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
    if (nsSeparator === void 0) nsSeparator = ":";
    const lng = opt.lng || this.language;
    const appendNamespaceToCIMode = opt.appendNamespaceToCIMode || this.options.appendNamespaceToCIMode;
    if (lng?.toLowerCase() === "cimode") {
      if (appendNamespaceToCIMode) {
        if (returnDetails) {
          return {
            res: `${namespace}${nsSeparator}${key}`,
            usedKey: key,
            exactUsedKey: key,
            usedLng: lng,
            usedNS: namespace,
            usedParams: this.getUsedParamsDetails(opt)
          };
        }
        return `${namespace}${nsSeparator}${key}`;
      }
      if (returnDetails) {
        return {
          res: key,
          usedKey: key,
          exactUsedKey: key,
          usedLng: lng,
          usedNS: namespace,
          usedParams: this.getUsedParamsDetails(opt)
        };
      }
      return key;
    }
    const resolved = this.resolve(keys3, opt);
    let res = resolved?.res;
    const resUsedKey = resolved?.usedKey || key;
    const resExactUsedKey = resolved?.exactUsedKey || key;
    const noObject = ["[object Number]", "[object Function]", "[object RegExp]"];
    const joinArrays = opt.joinArrays !== void 0 ? opt.joinArrays : this.options.joinArrays;
    const handleAsObjectInI18nFormat = !this.i18nFormat || this.i18nFormat.handleAsObject;
    const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
    const hasDefaultValue = _Translator.hasDefaultValue(opt);
    const defaultValueSuffix = needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, opt) : "";
    const defaultValueSuffixOrdinalFallback = opt.ordinal && needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, {
      ordinal: false
    }) : "";
    const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
    const defaultValue = needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] || opt[`defaultValue${defaultValueSuffix}`] || opt[`defaultValue${defaultValueSuffixOrdinalFallback}`] || opt.defaultValue;
    let resForObjHndl = res;
    if (handleAsObjectInI18nFormat && !res && hasDefaultValue) {
      resForObjHndl = defaultValue;
    }
    const handleAsObject = shouldHandleAsObject(resForObjHndl);
    const resType = Object.prototype.toString.apply(resForObjHndl);
    if (handleAsObjectInI18nFormat && resForObjHndl && handleAsObject && noObject.indexOf(resType) < 0 && !(isString(joinArrays) && Array.isArray(resForObjHndl))) {
      if (!opt.returnObjects && !this.options.returnObjects) {
        if (!this.options.returnedObjectHandler) {
          this.logger.warn("accessing an object - but returnObjects options is not enabled!");
        }
        const r3 = this.options.returnedObjectHandler ? this.options.returnedObjectHandler(resUsedKey, resForObjHndl, {
          ...opt,
          ns: namespaces
        }) : `key '${key} (${this.language})' returned an object instead of string.`;
        if (returnDetails) {
          resolved.res = r3;
          resolved.usedParams = this.getUsedParamsDetails(opt);
          return resolved;
        }
        return r3;
      }
      if (keySeparator) {
        const resTypeIsArray = Array.isArray(resForObjHndl);
        const copy2 = resTypeIsArray ? [] : {};
        const newKeyToUse = resTypeIsArray ? resExactUsedKey : resUsedKey;
        for (const m2 in resForObjHndl) {
          if (Object.prototype.hasOwnProperty.call(resForObjHndl, m2)) {
            const deepKey = `${newKeyToUse}${keySeparator}${m2}`;
            if (hasDefaultValue && !res) {
              copy2[m2] = this.translate(deepKey, {
                ...opt,
                defaultValue: shouldHandleAsObject(defaultValue) ? defaultValue[m2] : void 0,
                ...{
                  joinArrays: false,
                  ns: namespaces
                }
              });
            } else {
              copy2[m2] = this.translate(deepKey, {
                ...opt,
                ...{
                  joinArrays: false,
                  ns: namespaces
                }
              });
            }
            if (copy2[m2] === deepKey) copy2[m2] = resForObjHndl[m2];
          }
        }
        res = copy2;
      }
    } else if (handleAsObjectInI18nFormat && isString(joinArrays) && Array.isArray(res)) {
      res = res.join(joinArrays);
      if (res) res = this.extendTranslation(res, keys3, opt, lastKey);
    } else {
      let usedDefault = false;
      let usedKey = false;
      if (!this.isValidLookup(res) && hasDefaultValue) {
        usedDefault = true;
        res = defaultValue;
      }
      if (!this.isValidLookup(res)) {
        usedKey = true;
        res = key;
      }
      const missingKeyNoValueFallbackToKey = opt.missingKeyNoValueFallbackToKey || this.options.missingKeyNoValueFallbackToKey;
      const resForMissing = missingKeyNoValueFallbackToKey && usedKey ? void 0 : res;
      const updateMissing = hasDefaultValue && defaultValue !== res && this.options.updateMissing;
      if (usedKey || usedDefault || updateMissing) {
        this.logger.log(updateMissing ? "updateKey" : "missingKey", lng, namespace, key, updateMissing ? defaultValue : res);
        if (keySeparator) {
          const fk = this.resolve(key, {
            ...opt,
            keySeparator: false
          });
          if (fk && fk.res) this.logger.warn("Seems the loaded translations were in flat JSON format instead of nested. Either set keySeparator: false on init or make sure your translations are published in nested format.");
        }
        let lngs = [];
        const fallbackLngs = this.languageUtils.getFallbackCodes(this.options.fallbackLng, opt.lng || this.language);
        if (this.options.saveMissingTo === "fallback" && fallbackLngs && fallbackLngs[0]) {
          for (let i2 = 0; i2 < fallbackLngs.length; i2++) {
            lngs.push(fallbackLngs[i2]);
          }
        } else if (this.options.saveMissingTo === "all") {
          lngs = this.languageUtils.toResolveHierarchy(opt.lng || this.language);
        } else {
          lngs.push(opt.lng || this.language);
        }
        const send = (l2, k2, specificDefaultValue) => {
          const defaultForMissing = hasDefaultValue && specificDefaultValue !== res ? specificDefaultValue : resForMissing;
          if (this.options.missingKeyHandler) {
            this.options.missingKeyHandler(l2, namespace, k2, defaultForMissing, updateMissing, opt);
          } else if (this.backendConnector?.saveMissing) {
            this.backendConnector.saveMissing(l2, namespace, k2, defaultForMissing, updateMissing, opt);
          }
          this.emit("missingKey", l2, namespace, k2, res);
        };
        if (this.options.saveMissing) {
          if (this.options.saveMissingPlurals && needsPluralHandling) {
            lngs.forEach((language) => {
              const suffixes = this.pluralResolver.getSuffixes(language, opt);
              if (needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] && suffixes.indexOf(`${this.options.pluralSeparator}zero`) < 0) {
                suffixes.push(`${this.options.pluralSeparator}zero`);
              }
              suffixes.forEach((suffix) => {
                send([language], key + suffix, opt[`defaultValue${suffix}`] || defaultValue);
              });
            });
          } else {
            send(lngs, key, defaultValue);
          }
        }
      }
      res = this.extendTranslation(res, keys3, opt, resolved, lastKey);
      if (usedKey && res === key && this.options.appendNamespaceToMissingKey) {
        res = `${namespace}${nsSeparator}${key}`;
      }
      if ((usedKey || usedDefault) && this.options.parseMissingKeyHandler) {
        res = this.options.parseMissingKeyHandler(this.options.appendNamespaceToMissingKey ? `${namespace}${nsSeparator}${key}` : key, usedDefault ? res : void 0, opt);
      }
    }
    if (returnDetails) {
      resolved.res = res;
      resolved.usedParams = this.getUsedParamsDetails(opt);
      return resolved;
    }
    return res;
  }
  extendTranslation(res, key, opt, resolved, lastKey) {
    if (this.i18nFormat?.parse) {
      res = this.i18nFormat.parse(res, {
        ...this.options.interpolation.defaultVariables,
        ...opt
      }, opt.lng || this.language || resolved.usedLng, resolved.usedNS, resolved.usedKey, {
        resolved
      });
    } else if (!opt.skipInterpolation) {
      if (opt.interpolation) this.interpolator.init({
        ...opt,
        ...{
          interpolation: {
            ...this.options.interpolation,
            ...opt.interpolation
          }
        }
      });
      const skipOnVariables = isString(res) && (opt?.interpolation?.skipOnVariables !== void 0 ? opt.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables);
      let nestBef;
      if (skipOnVariables) {
        const nb = res.match(this.interpolator.nestingRegexp);
        nestBef = nb && nb.length;
      }
      let data = opt.replace && !isString(opt.replace) ? opt.replace : opt;
      if (this.options.interpolation.defaultVariables) data = {
        ...this.options.interpolation.defaultVariables,
        ...data
      };
      res = this.interpolator.interpolate(res, data, opt.lng || this.language || resolved.usedLng, opt);
      if (skipOnVariables) {
        const na = res.match(this.interpolator.nestingRegexp);
        const nestAft = na && na.length;
        if (nestBef < nestAft) opt.nest = false;
      }
      if (!opt.lng && resolved && resolved.res) opt.lng = this.language || resolved.usedLng;
      if (opt.nest !== false) res = this.interpolator.nest(res, (...args) => {
        if (lastKey?.[0] === args[0] && !opt.context) {
          this.logger.warn(`It seems you are nesting recursively key: ${args[0]} in key: ${key[0]}`);
          return null;
        }
        return this.translate(...args, key);
      }, opt);
      if (opt.interpolation) this.interpolator.reset();
    }
    const postProcess = opt.postProcess || this.options.postProcess;
    const postProcessorNames = isString(postProcess) ? [postProcess] : postProcess;
    if (res != null && postProcessorNames?.length && opt.applyPostProcessor !== false) {
      res = postProcessor.handle(postProcessorNames, res, key, this.options && this.options.postProcessPassResolved ? {
        i18nResolved: {
          ...resolved,
          usedParams: this.getUsedParamsDetails(opt)
        },
        ...opt
      } : opt, this);
    }
    return res;
  }
  resolve(keys3, opt = {}) {
    let found;
    let usedKey;
    let exactUsedKey;
    let usedLng;
    let usedNS;
    if (isString(keys3)) keys3 = [keys3];
    if (Array.isArray(keys3)) keys3 = keys3.map((k2) => typeof k2 === "function" ? keysFromSelector(k2, {
      ...this.options,
      ...opt
    }) : k2);
    keys3.forEach((k2) => {
      if (this.isValidLookup(found)) return;
      const extracted = this.extractFromKey(k2, opt);
      const key = extracted.key;
      usedKey = key;
      let namespaces = extracted.namespaces;
      if (this.options.fallbackNS) namespaces = namespaces.concat(this.options.fallbackNS);
      const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
      const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
      const needsContextHandling = opt.context !== void 0 && (isString(opt.context) || typeof opt.context === "number") && opt.context !== "";
      const codes2 = opt.lngs ? opt.lngs : this.languageUtils.toResolveHierarchy(opt.lng || this.language, opt.fallbackLng);
      namespaces.forEach((ns) => {
        if (this.isValidLookup(found)) return;
        usedNS = ns;
        if (!checkedLoadedFor[`${codes2[0]}-${ns}`] && this.utils?.hasLoadedNamespace && !this.utils?.hasLoadedNamespace(usedNS)) {
          checkedLoadedFor[`${codes2[0]}-${ns}`] = true;
          this.logger.warn(`key "${usedKey}" for languages "${codes2.join(", ")}" won't get resolved as namespace "${usedNS}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
        }
        codes2.forEach((code4) => {
          if (this.isValidLookup(found)) return;
          usedLng = code4;
          const finalKeys = [key];
          if (this.i18nFormat?.addLookupKeys) {
            this.i18nFormat.addLookupKeys(finalKeys, key, code4, ns, opt);
          } else {
            let pluralSuffix;
            if (needsPluralHandling) pluralSuffix = this.pluralResolver.getSuffix(code4, opt.count, opt);
            const zeroSuffix = `${this.options.pluralSeparator}zero`;
            const ordinalPrefix = `${this.options.pluralSeparator}ordinal${this.options.pluralSeparator}`;
            if (needsPluralHandling) {
              if (opt.ordinal && pluralSuffix.indexOf(ordinalPrefix) === 0) {
                finalKeys.push(key + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
              }
              finalKeys.push(key + pluralSuffix);
              if (needsZeroSuffixLookup) {
                finalKeys.push(key + zeroSuffix);
              }
            }
            if (needsContextHandling) {
              const contextKey = `${key}${this.options.contextSeparator || "_"}${opt.context}`;
              finalKeys.push(contextKey);
              if (needsPluralHandling) {
                if (opt.ordinal && pluralSuffix.indexOf(ordinalPrefix) === 0) {
                  finalKeys.push(contextKey + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
                }
                finalKeys.push(contextKey + pluralSuffix);
                if (needsZeroSuffixLookup) {
                  finalKeys.push(contextKey + zeroSuffix);
                }
              }
            }
          }
          let possibleKey;
          while (possibleKey = finalKeys.pop()) {
            if (!this.isValidLookup(found)) {
              exactUsedKey = possibleKey;
              found = this.getResource(code4, ns, possibleKey, opt);
            }
          }
        });
      });
    });
    return {
      res: found,
      usedKey,
      exactUsedKey,
      usedLng,
      usedNS
    };
  }
  isValidLookup(res) {
    return res !== void 0 && !(!this.options.returnNull && res === null) && !(!this.options.returnEmptyString && res === "");
  }
  getResource(code4, ns, key, options = {}) {
    if (this.i18nFormat?.getResource) return this.i18nFormat.getResource(code4, ns, key, options);
    return this.resourceStore.getResource(code4, ns, key, options);
  }
  getUsedParamsDetails(options = {}) {
    const optionsKeys = ["defaultValue", "ordinal", "context", "replace", "lng", "lngs", "fallbackLng", "ns", "keySeparator", "nsSeparator", "returnObjects", "returnDetails", "joinArrays", "postProcess", "interpolation"];
    const useOptionsReplaceForData = options.replace && !isString(options.replace);
    let data = useOptionsReplaceForData ? options.replace : options;
    if (useOptionsReplaceForData && typeof options.count !== "undefined") {
      data.count = options.count;
    }
    if (this.options.interpolation.defaultVariables) {
      data = {
        ...this.options.interpolation.defaultVariables,
        ...data
      };
    }
    if (!useOptionsReplaceForData) {
      data = {
        ...data
      };
      for (const key of optionsKeys) {
        delete data[key];
      }
    }
    return data;
  }
  static hasDefaultValue(options) {
    const prefix = "defaultValue";
    for (const option in options) {
      if (Object.prototype.hasOwnProperty.call(options, option) && prefix === option.substring(0, prefix.length) && void 0 !== options[option]) {
        return true;
      }
    }
    return false;
  }
};
var LanguageUtil = class {
  constructor(options) {
    this.options = options;
    this.supportedLngs = this.options.supportedLngs || false;
    this.logger = baseLogger.create("languageUtils");
  }
  getScriptPartFromCode(code4) {
    code4 = getCleanedCode(code4);
    if (!code4 || code4.indexOf("-") < 0) return null;
    const p2 = code4.split("-");
    if (p2.length === 2) return null;
    p2.pop();
    if (p2[p2.length - 1].toLowerCase() === "x") return null;
    return this.formatLanguageCode(p2.join("-"));
  }
  getLanguagePartFromCode(code4) {
    code4 = getCleanedCode(code4);
    if (!code4 || code4.indexOf("-") < 0) return code4;
    const p2 = code4.split("-");
    return this.formatLanguageCode(p2[0]);
  }
  formatLanguageCode(code4) {
    if (isString(code4) && code4.indexOf("-") > -1) {
      let formattedCode;
      try {
        formattedCode = Intl.getCanonicalLocales(code4)[0];
      } catch (e) {
      }
      if (formattedCode && this.options.lowerCaseLng) {
        formattedCode = formattedCode.toLowerCase();
      }
      if (formattedCode) return formattedCode;
      if (this.options.lowerCaseLng) {
        return code4.toLowerCase();
      }
      return code4;
    }
    return this.options.cleanCode || this.options.lowerCaseLng ? code4.toLowerCase() : code4;
  }
  isSupportedCode(code4) {
    if (this.options.load === "languageOnly" || this.options.nonExplicitSupportedLngs) {
      code4 = this.getLanguagePartFromCode(code4);
    }
    return !this.supportedLngs || !this.supportedLngs.length || this.supportedLngs.indexOf(code4) > -1;
  }
  getBestMatchFromCodes(codes2) {
    if (!codes2) return null;
    let found;
    codes2.forEach((code4) => {
      if (found) return;
      const cleanedLng = this.formatLanguageCode(code4);
      if (!this.options.supportedLngs || this.isSupportedCode(cleanedLng)) found = cleanedLng;
    });
    if (!found && this.options.supportedLngs) {
      codes2.forEach((code4) => {
        if (found) return;
        const lngScOnly = this.getScriptPartFromCode(code4);
        if (this.isSupportedCode(lngScOnly)) return found = lngScOnly;
        const lngOnly = this.getLanguagePartFromCode(code4);
        if (this.isSupportedCode(lngOnly)) return found = lngOnly;
        found = this.options.supportedLngs.find((supportedLng) => {
          if (supportedLng === lngOnly) return supportedLng;
          if (supportedLng.indexOf("-") < 0 && lngOnly.indexOf("-") < 0) return;
          if (supportedLng.indexOf("-") > 0 && lngOnly.indexOf("-") < 0 && supportedLng.substring(0, supportedLng.indexOf("-")) === lngOnly) return supportedLng;
          if (supportedLng.indexOf(lngOnly) === 0 && lngOnly.length > 1) return supportedLng;
        });
      });
    }
    if (!found) found = this.getFallbackCodes(this.options.fallbackLng)[0];
    return found;
  }
  getFallbackCodes(fallbacks, code4) {
    if (!fallbacks) return [];
    if (typeof fallbacks === "function") fallbacks = fallbacks(code4);
    if (isString(fallbacks)) fallbacks = [fallbacks];
    if (Array.isArray(fallbacks)) return fallbacks;
    if (!code4) return fallbacks.default || [];
    let found = fallbacks[code4];
    if (!found) found = fallbacks[this.getScriptPartFromCode(code4)];
    if (!found) found = fallbacks[this.formatLanguageCode(code4)];
    if (!found) found = fallbacks[this.getLanguagePartFromCode(code4)];
    if (!found) found = fallbacks.default;
    return found || [];
  }
  toResolveHierarchy(code4, fallbackCode) {
    const fallbackCodes = this.getFallbackCodes((fallbackCode === false ? [] : fallbackCode) || this.options.fallbackLng || [], code4);
    const codes2 = [];
    const addCode = (c2) => {
      if (!c2) return;
      if (this.isSupportedCode(c2)) {
        codes2.push(c2);
      } else {
        this.logger.warn(`rejecting language code not found in supportedLngs: ${c2}`);
      }
    };
    if (isString(code4) && (code4.indexOf("-") > -1 || code4.indexOf("_") > -1)) {
      if (this.options.load !== "languageOnly") addCode(this.formatLanguageCode(code4));
      if (this.options.load !== "languageOnly" && this.options.load !== "currentOnly") addCode(this.getScriptPartFromCode(code4));
      if (this.options.load !== "currentOnly") addCode(this.getLanguagePartFromCode(code4));
    } else if (isString(code4)) {
      addCode(this.formatLanguageCode(code4));
    }
    fallbackCodes.forEach((fc) => {
      if (codes2.indexOf(fc) < 0) addCode(this.formatLanguageCode(fc));
    });
    return codes2;
  }
};
var suffixesOrder = {
  zero: 0,
  one: 1,
  two: 2,
  few: 3,
  many: 4,
  other: 5
};
var dummyRule = {
  select: (count2) => count2 === 1 ? "one" : "other",
  resolvedOptions: () => ({
    pluralCategories: ["one", "other"]
  })
};
var PluralResolver = class {
  constructor(languageUtils, options = {}) {
    this.languageUtils = languageUtils;
    this.options = options;
    this.logger = baseLogger.create("pluralResolver");
    this.pluralRulesCache = {};
  }
  clearCache() {
    this.pluralRulesCache = {};
  }
  getRule(code4, options = {}) {
    const cleanedCode = getCleanedCode(code4 === "dev" ? "en" : code4);
    const type = options.ordinal ? "ordinal" : "cardinal";
    const cacheKey = JSON.stringify({
      cleanedCode,
      type
    });
    if (cacheKey in this.pluralRulesCache) {
      return this.pluralRulesCache[cacheKey];
    }
    let rule;
    try {
      rule = new Intl.PluralRules(cleanedCode, {
        type
      });
    } catch (err) {
      if (typeof Intl === "undefined") {
        this.logger.error("No Intl support, please use an Intl polyfill!");
        return dummyRule;
      }
      if (!code4.match(/-|_/)) return dummyRule;
      const lngPart = this.languageUtils.getLanguagePartFromCode(code4);
      rule = this.getRule(lngPart, options);
    }
    this.pluralRulesCache[cacheKey] = rule;
    return rule;
  }
  needsPlural(code4, options = {}) {
    let rule = this.getRule(code4, options);
    if (!rule) rule = this.getRule("dev", options);
    return rule?.resolvedOptions().pluralCategories.length > 1;
  }
  getPluralFormsOfKey(code4, key, options = {}) {
    return this.getSuffixes(code4, options).map((suffix) => `${key}${suffix}`);
  }
  getSuffixes(code4, options = {}) {
    let rule = this.getRule(code4, options);
    if (!rule) rule = this.getRule("dev", options);
    if (!rule) return [];
    return rule.resolvedOptions().pluralCategories.sort((pluralCategory1, pluralCategory2) => suffixesOrder[pluralCategory1] - suffixesOrder[pluralCategory2]).map((pluralCategory) => `${this.options.prepend}${options.ordinal ? `ordinal${this.options.prepend}` : ""}${pluralCategory}`);
  }
  getSuffix(code4, count2, options = {}) {
    const rule = this.getRule(code4, options);
    if (rule) {
      return `${this.options.prepend}${options.ordinal ? `ordinal${this.options.prepend}` : ""}${rule.select(count2)}`;
    }
    this.logger.warn(`no plural rule found for: ${code4}`);
    return this.getSuffix("dev", count2, options);
  }
};
var deepFindWithDefaults = (data, defaultData, key, keySeparator = ".", ignoreJSONStructure = true) => {
  let path2 = getPathWithDefaults(data, defaultData, key);
  if (!path2 && ignoreJSONStructure && isString(key)) {
    path2 = deepFind(data, key, keySeparator);
    if (path2 === void 0) path2 = deepFind(defaultData, key, keySeparator);
  }
  return path2;
};
var regexSafe = (val) => val.replace(/\$/g, "$$$$");
var Interpolator = class {
  constructor(options = {}) {
    this.logger = baseLogger.create("interpolator");
    this.options = options;
    this.format = options?.interpolation?.format || ((value) => value);
    this.init(options);
  }
  init(options = {}) {
    if (!options.interpolation) options.interpolation = {
      escapeValue: true
    };
    const {
      escape: escape$1,
      escapeValue,
      useRawValueToEscape,
      prefix,
      prefixEscaped,
      suffix,
      suffixEscaped,
      formatSeparator,
      unescapeSuffix,
      unescapePrefix,
      nestingPrefix,
      nestingPrefixEscaped,
      nestingSuffix,
      nestingSuffixEscaped,
      nestingOptionsSeparator,
      maxReplaces,
      alwaysFormat
    } = options.interpolation;
    this.escape = escape$1 !== void 0 ? escape$1 : escape;
    this.escapeValue = escapeValue !== void 0 ? escapeValue : true;
    this.useRawValueToEscape = useRawValueToEscape !== void 0 ? useRawValueToEscape : false;
    this.prefix = prefix ? regexEscape(prefix) : prefixEscaped || "{{";
    this.suffix = suffix ? regexEscape(suffix) : suffixEscaped || "}}";
    this.formatSeparator = formatSeparator || ",";
    this.unescapePrefix = unescapeSuffix ? "" : unescapePrefix || "-";
    this.unescapeSuffix = this.unescapePrefix ? "" : unescapeSuffix || "";
    this.nestingPrefix = nestingPrefix ? regexEscape(nestingPrefix) : nestingPrefixEscaped || regexEscape("$t(");
    this.nestingSuffix = nestingSuffix ? regexEscape(nestingSuffix) : nestingSuffixEscaped || regexEscape(")");
    this.nestingOptionsSeparator = nestingOptionsSeparator || ",";
    this.maxReplaces = maxReplaces || 1e3;
    this.alwaysFormat = alwaysFormat !== void 0 ? alwaysFormat : false;
    this.resetRegExp();
  }
  reset() {
    if (this.options) this.init(this.options);
  }
  resetRegExp() {
    const getOrResetRegExp = (existingRegExp, pattern) => {
      if (existingRegExp?.source === pattern) {
        existingRegExp.lastIndex = 0;
        return existingRegExp;
      }
      return new RegExp(pattern, "g");
    };
    this.regexp = getOrResetRegExp(this.regexp, `${this.prefix}(.+?)${this.suffix}`);
    this.regexpUnescape = getOrResetRegExp(this.regexpUnescape, `${this.prefix}${this.unescapePrefix}(.+?)${this.unescapeSuffix}${this.suffix}`);
    this.nestingRegexp = getOrResetRegExp(this.nestingRegexp, `${this.nestingPrefix}((?:[^()"']+|"[^"]*"|'[^']*'|\\((?:[^()]|"[^"]*"|'[^']*')*\\))*?)${this.nestingSuffix}`);
  }
  interpolate(str, data, lng, options) {
    let match;
    let value;
    let replaces;
    const defaultData = this.options && this.options.interpolation && this.options.interpolation.defaultVariables || {};
    const handleFormat = (key) => {
      if (key.indexOf(this.formatSeparator) < 0) {
        const path2 = deepFindWithDefaults(data, defaultData, key, this.options.keySeparator, this.options.ignoreJSONStructure);
        return this.alwaysFormat ? this.format(path2, void 0, lng, {
          ...options,
          ...data,
          interpolationkey: key
        }) : path2;
      }
      const p2 = key.split(this.formatSeparator);
      const k2 = p2.shift().trim();
      const f2 = p2.join(this.formatSeparator).trim();
      return this.format(deepFindWithDefaults(data, defaultData, k2, this.options.keySeparator, this.options.ignoreJSONStructure), f2, lng, {
        ...options,
        ...data,
        interpolationkey: k2
      });
    };
    this.resetRegExp();
    const missingInterpolationHandler = options?.missingInterpolationHandler || this.options.missingInterpolationHandler;
    const skipOnVariables = options?.interpolation?.skipOnVariables !== void 0 ? options.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables;
    const todos = [{
      regex: this.regexpUnescape,
      safeValue: (val) => regexSafe(val)
    }, {
      regex: this.regexp,
      safeValue: (val) => this.escapeValue ? regexSafe(this.escape(val)) : regexSafe(val)
    }];
    todos.forEach((todo) => {
      replaces = 0;
      while (match = todo.regex.exec(str)) {
        const matchedVar = match[1].trim();
        value = handleFormat(matchedVar);
        if (value === void 0) {
          if (typeof missingInterpolationHandler === "function") {
            const temp = missingInterpolationHandler(str, match, options);
            value = isString(temp) ? temp : "";
          } else if (options && Object.prototype.hasOwnProperty.call(options, matchedVar)) {
            value = "";
          } else if (skipOnVariables) {
            value = match[0];
            continue;
          } else {
            this.logger.warn(`missed to pass in variable ${matchedVar} for interpolating ${str}`);
            value = "";
          }
        } else if (!isString(value) && !this.useRawValueToEscape) {
          value = makeString(value);
        }
        const safeValue = todo.safeValue(value);
        str = str.replace(match[0], safeValue);
        if (skipOnVariables) {
          todo.regex.lastIndex += value.length;
          todo.regex.lastIndex -= match[0].length;
        } else {
          todo.regex.lastIndex = 0;
        }
        replaces++;
        if (replaces >= this.maxReplaces) {
          break;
        }
      }
    });
    return str;
  }
  nest(str, fc, options = {}) {
    let match;
    let value;
    let clonedOptions;
    const handleHasOptions = (key, inheritedOptions) => {
      const sep = this.nestingOptionsSeparator;
      if (key.indexOf(sep) < 0) return key;
      const c2 = key.split(new RegExp(`${regexEscape(sep)}[ ]*{`));
      let optionsString = `{${c2[1]}`;
      key = c2[0];
      optionsString = this.interpolate(optionsString, clonedOptions);
      const matchedSingleQuotes = optionsString.match(/'/g);
      const matchedDoubleQuotes = optionsString.match(/"/g);
      if ((matchedSingleQuotes?.length ?? 0) % 2 === 0 && !matchedDoubleQuotes || (matchedDoubleQuotes?.length ?? 0) % 2 !== 0) {
        optionsString = optionsString.replace(/'/g, '"');
      }
      try {
        clonedOptions = JSON.parse(optionsString);
        if (inheritedOptions) clonedOptions = {
          ...inheritedOptions,
          ...clonedOptions
        };
      } catch (e) {
        this.logger.warn(`failed parsing options string in nesting for key ${key}`, e);
        return `${key}${sep}${optionsString}`;
      }
      if (clonedOptions.defaultValue && clonedOptions.defaultValue.indexOf(this.prefix) > -1) delete clonedOptions.defaultValue;
      return key;
    };
    while (match = this.nestingRegexp.exec(str)) {
      let formatters = [];
      clonedOptions = {
        ...options
      };
      clonedOptions = clonedOptions.replace && !isString(clonedOptions.replace) ? clonedOptions.replace : clonedOptions;
      clonedOptions.applyPostProcessor = false;
      delete clonedOptions.defaultValue;
      const keyEndIndex = /{.*}/.test(match[1]) ? match[1].lastIndexOf("}") + 1 : match[1].indexOf(this.formatSeparator);
      if (keyEndIndex !== -1) {
        formatters = match[1].slice(keyEndIndex).split(this.formatSeparator).map((elem) => elem.trim()).filter(Boolean);
        match[1] = match[1].slice(0, keyEndIndex);
      }
      value = fc(handleHasOptions.call(this, match[1].trim(), clonedOptions), clonedOptions);
      if (value && match[0] === str && !isString(value)) return value;
      if (!isString(value)) value = makeString(value);
      if (!value) {
        this.logger.warn(`missed to resolve ${match[1]} for nesting ${str}`);
        value = "";
      }
      if (formatters.length) {
        value = formatters.reduce((v2, f2) => this.format(v2, f2, options.lng, {
          ...options,
          interpolationkey: match[1].trim()
        }), value.trim());
      }
      str = str.replace(match[0], value);
      this.regexp.lastIndex = 0;
    }
    return str;
  }
};
var parseFormatStr = (formatStr) => {
  let formatName = formatStr.toLowerCase().trim();
  const formatOptions = {};
  if (formatStr.indexOf("(") > -1) {
    const p2 = formatStr.split("(");
    formatName = p2[0].toLowerCase().trim();
    const optStr = p2[1].substring(0, p2[1].length - 1);
    if (formatName === "currency" && optStr.indexOf(":") < 0) {
      if (!formatOptions.currency) formatOptions.currency = optStr.trim();
    } else if (formatName === "relativetime" && optStr.indexOf(":") < 0) {
      if (!formatOptions.range) formatOptions.range = optStr.trim();
    } else {
      const opts = optStr.split(";");
      opts.forEach((opt) => {
        if (opt) {
          const [key, ...rest] = opt.split(":");
          const val = rest.join(":").trim().replace(/^'+|'+$/g, "");
          const trimmedKey = key.trim();
          if (!formatOptions[trimmedKey]) formatOptions[trimmedKey] = val;
          if (val === "false") formatOptions[trimmedKey] = false;
          if (val === "true") formatOptions[trimmedKey] = true;
          if (!isNaN(val)) formatOptions[trimmedKey] = parseInt(val, 10);
        }
      });
    }
  }
  return {
    formatName,
    formatOptions
  };
};
var createCachedFormatter = (fn) => {
  const cache = {};
  return (v2, l2, o2) => {
    let optForCache = o2;
    if (o2 && o2.interpolationkey && o2.formatParams && o2.formatParams[o2.interpolationkey] && o2[o2.interpolationkey]) {
      optForCache = {
        ...optForCache,
        [o2.interpolationkey]: void 0
      };
    }
    const key = l2 + JSON.stringify(optForCache);
    let frm = cache[key];
    if (!frm) {
      frm = fn(getCleanedCode(l2), o2);
      cache[key] = frm;
    }
    return frm(v2);
  };
};
var createNonCachedFormatter = (fn) => (v2, l2, o2) => fn(getCleanedCode(l2), o2)(v2);
var Formatter = class {
  constructor(options = {}) {
    this.logger = baseLogger.create("formatter");
    this.options = options;
    this.init(options);
  }
  init(services, options = {
    interpolation: {}
  }) {
    this.formatSeparator = options.interpolation.formatSeparator || ",";
    const cf = options.cacheInBuiltFormats ? createCachedFormatter : createNonCachedFormatter;
    this.formats = {
      number: cf((lng, opt) => {
        const formatter = new Intl.NumberFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val);
      }),
      currency: cf((lng, opt) => {
        const formatter = new Intl.NumberFormat(lng, {
          ...opt,
          style: "currency"
        });
        return (val) => formatter.format(val);
      }),
      datetime: cf((lng, opt) => {
        const formatter = new Intl.DateTimeFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val);
      }),
      relativetime: cf((lng, opt) => {
        const formatter = new Intl.RelativeTimeFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val, opt.range || "day");
      }),
      list: cf((lng, opt) => {
        const formatter = new Intl.ListFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val);
      })
    };
  }
  add(name2, fc) {
    this.formats[name2.toLowerCase().trim()] = fc;
  }
  addCached(name2, fc) {
    this.formats[name2.toLowerCase().trim()] = createCachedFormatter(fc);
  }
  format(value, format, lng, options = {}) {
    const formats = format.split(this.formatSeparator);
    if (formats.length > 1 && formats[0].indexOf("(") > 1 && formats[0].indexOf(")") < 0 && formats.find((f2) => f2.indexOf(")") > -1)) {
      const lastIndex = formats.findIndex((f2) => f2.indexOf(")") > -1);
      formats[0] = [formats[0], ...formats.splice(1, lastIndex)].join(this.formatSeparator);
    }
    const result = formats.reduce((mem, f2) => {
      const {
        formatName,
        formatOptions
      } = parseFormatStr(f2);
      if (this.formats[formatName]) {
        let formatted = mem;
        try {
          const valOptions = options?.formatParams?.[options.interpolationkey] || {};
          const l2 = valOptions.locale || valOptions.lng || options.locale || options.lng || lng;
          formatted = this.formats[formatName](mem, l2, {
            ...formatOptions,
            ...options,
            ...valOptions
          });
        } catch (error2) {
          this.logger.warn(error2);
        }
        return formatted;
      } else {
        this.logger.warn(`there was no format function for ${formatName}`);
      }
      return mem;
    }, value);
    return result;
  }
};
var removePending = (q2, name2) => {
  if (q2.pending[name2] !== void 0) {
    delete q2.pending[name2];
    q2.pendingCount--;
  }
};
var Connector = class extends EventEmitter {
  constructor(backend, store, services, options = {}) {
    super();
    this.backend = backend;
    this.store = store;
    this.services = services;
    this.languageUtils = services.languageUtils;
    this.options = options;
    this.logger = baseLogger.create("backendConnector");
    this.waitingReads = [];
    this.maxParallelReads = options.maxParallelReads || 10;
    this.readingCalls = 0;
    this.maxRetries = options.maxRetries >= 0 ? options.maxRetries : 5;
    this.retryTimeout = options.retryTimeout >= 1 ? options.retryTimeout : 350;
    this.state = {};
    this.queue = [];
    this.backend?.init?.(services, options.backend, options);
  }
  queueLoad(languages, namespaces, options, callback) {
    const toLoad = {};
    const pending = {};
    const toLoadLanguages = {};
    const toLoadNamespaces = {};
    languages.forEach((lng) => {
      let hasAllNamespaces = true;
      namespaces.forEach((ns) => {
        const name2 = `${lng}|${ns}`;
        if (!options.reload && this.store.hasResourceBundle(lng, ns)) {
          this.state[name2] = 2;
        } else if (this.state[name2] < 0) ;
        else if (this.state[name2] === 1) {
          if (pending[name2] === void 0) pending[name2] = true;
        } else {
          this.state[name2] = 1;
          hasAllNamespaces = false;
          if (pending[name2] === void 0) pending[name2] = true;
          if (toLoad[name2] === void 0) toLoad[name2] = true;
          if (toLoadNamespaces[ns] === void 0) toLoadNamespaces[ns] = true;
        }
      });
      if (!hasAllNamespaces) toLoadLanguages[lng] = true;
    });
    if (Object.keys(toLoad).length || Object.keys(pending).length) {
      this.queue.push({
        pending,
        pendingCount: Object.keys(pending).length,
        loaded: {},
        errors: [],
        callback
      });
    }
    return {
      toLoad: Object.keys(toLoad),
      pending: Object.keys(pending),
      toLoadLanguages: Object.keys(toLoadLanguages),
      toLoadNamespaces: Object.keys(toLoadNamespaces)
    };
  }
  loaded(name2, err, data) {
    const s2 = name2.split("|");
    const lng = s2[0];
    const ns = s2[1];
    if (err) this.emit("failedLoading", lng, ns, err);
    if (!err && data) {
      this.store.addResourceBundle(lng, ns, data, void 0, void 0, {
        skipCopy: true
      });
    }
    this.state[name2] = err ? -1 : 2;
    if (err && data) this.state[name2] = 0;
    const loaded = {};
    this.queue.forEach((q2) => {
      pushPath(q2.loaded, [lng], ns);
      removePending(q2, name2);
      if (err) q2.errors.push(err);
      if (q2.pendingCount === 0 && !q2.done) {
        Object.keys(q2.loaded).forEach((l2) => {
          if (!loaded[l2]) loaded[l2] = {};
          const loadedKeys = q2.loaded[l2];
          if (loadedKeys.length) {
            loadedKeys.forEach((n2) => {
              if (loaded[l2][n2] === void 0) loaded[l2][n2] = true;
            });
          }
        });
        q2.done = true;
        if (q2.errors.length) {
          q2.callback(q2.errors);
        } else {
          q2.callback();
        }
      }
    });
    this.emit("loaded", loaded);
    this.queue = this.queue.filter((q2) => !q2.done);
  }
  read(lng, ns, fcName, tried = 0, wait = this.retryTimeout, callback) {
    if (!lng.length) return callback(null, {});
    if (this.readingCalls >= this.maxParallelReads) {
      this.waitingReads.push({
        lng,
        ns,
        fcName,
        tried,
        wait,
        callback
      });
      return;
    }
    this.readingCalls++;
    const resolver2 = (err, data) => {
      this.readingCalls--;
      if (this.waitingReads.length > 0) {
        const next = this.waitingReads.shift();
        this.read(next.lng, next.ns, next.fcName, next.tried, next.wait, next.callback);
      }
      if (err && data && tried < this.maxRetries) {
        setTimeout(() => {
          this.read.call(this, lng, ns, fcName, tried + 1, wait * 2, callback);
        }, wait);
        return;
      }
      callback(err, data);
    };
    const fc = this.backend[fcName].bind(this.backend);
    if (fc.length === 2) {
      try {
        const r3 = fc(lng, ns);
        if (r3 && typeof r3.then === "function") {
          r3.then((data) => resolver2(null, data)).catch(resolver2);
        } else {
          resolver2(null, r3);
        }
      } catch (err) {
        resolver2(err);
      }
      return;
    }
    return fc(lng, ns, resolver2);
  }
  prepareLoading(languages, namespaces, options = {}, callback) {
    if (!this.backend) {
      this.logger.warn("No backend was added via i18next.use. Will not load resources.");
      return callback && callback();
    }
    if (isString(languages)) languages = this.languageUtils.toResolveHierarchy(languages);
    if (isString(namespaces)) namespaces = [namespaces];
    const toLoad = this.queueLoad(languages, namespaces, options, callback);
    if (!toLoad.toLoad.length) {
      if (!toLoad.pending.length) callback();
      return null;
    }
    toLoad.toLoad.forEach((name2) => {
      this.loadOne(name2);
    });
  }
  load(languages, namespaces, callback) {
    this.prepareLoading(languages, namespaces, {}, callback);
  }
  reload(languages, namespaces, callback) {
    this.prepareLoading(languages, namespaces, {
      reload: true
    }, callback);
  }
  loadOne(name2, prefix = "") {
    const s2 = name2.split("|");
    const lng = s2[0];
    const ns = s2[1];
    this.read(lng, ns, "read", void 0, void 0, (err, data) => {
      if (err) this.logger.warn(`${prefix}loading namespace ${ns} for language ${lng} failed`, err);
      if (!err && data) this.logger.log(`${prefix}loaded namespace ${ns} for language ${lng}`, data);
      this.loaded(name2, err, data);
    });
  }
  saveMissing(languages, namespace, key, fallbackValue, isUpdate, options = {}, clb = () => {
  }) {
    if (this.services?.utils?.hasLoadedNamespace && !this.services?.utils?.hasLoadedNamespace(namespace)) {
      this.logger.warn(`did not save key "${key}" as the namespace "${namespace}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
      return;
    }
    if (key === void 0 || key === null || key === "") return;
    if (this.backend?.create) {
      const opts = {
        ...options,
        isUpdate
      };
      const fc = this.backend.create.bind(this.backend);
      if (fc.length < 6) {
        try {
          let r3;
          if (fc.length === 5) {
            r3 = fc(languages, namespace, key, fallbackValue, opts);
          } else {
            r3 = fc(languages, namespace, key, fallbackValue);
          }
          if (r3 && typeof r3.then === "function") {
            r3.then((data) => clb(null, data)).catch(clb);
          } else {
            clb(null, r3);
          }
        } catch (err) {
          clb(err);
        }
      } else {
        fc(languages, namespace, key, fallbackValue, clb, opts);
      }
    }
    if (!languages || !languages[0]) return;
    this.store.addResource(languages[0], namespace, key, fallbackValue);
  }
};
var get = () => ({
  debug: false,
  initAsync: true,
  ns: ["translation"],
  defaultNS: ["translation"],
  fallbackLng: ["dev"],
  fallbackNS: false,
  supportedLngs: false,
  nonExplicitSupportedLngs: false,
  load: "all",
  preload: false,
  simplifyPluralSuffix: true,
  keySeparator: ".",
  nsSeparator: ":",
  pluralSeparator: "_",
  contextSeparator: "_",
  partialBundledLanguages: false,
  saveMissing: false,
  updateMissing: false,
  saveMissingTo: "fallback",
  saveMissingPlurals: true,
  missingKeyHandler: false,
  missingInterpolationHandler: false,
  postProcess: false,
  postProcessPassResolved: false,
  returnNull: false,
  returnEmptyString: true,
  returnObjects: false,
  joinArrays: false,
  returnedObjectHandler: false,
  parseMissingKeyHandler: false,
  appendNamespaceToMissingKey: false,
  appendNamespaceToCIMode: false,
  overloadTranslationOptionHandler: (args) => {
    let ret = {};
    if (typeof args[1] === "object") ret = args[1];
    if (isString(args[1])) ret.defaultValue = args[1];
    if (isString(args[2])) ret.tDescription = args[2];
    if (typeof args[2] === "object" || typeof args[3] === "object") {
      const options = args[3] || args[2];
      Object.keys(options).forEach((key) => {
        ret[key] = options[key];
      });
    }
    return ret;
  },
  interpolation: {
    escapeValue: true,
    format: (value) => value,
    prefix: "{{",
    suffix: "}}",
    formatSeparator: ",",
    unescapePrefix: "-",
    nestingPrefix: "$t(",
    nestingSuffix: ")",
    nestingOptionsSeparator: ",",
    maxReplaces: 1e3,
    skipOnVariables: true
  },
  cacheInBuiltFormats: true
});
var transformOptions = (options) => {
  if (isString(options.ns)) options.ns = [options.ns];
  if (isString(options.fallbackLng)) options.fallbackLng = [options.fallbackLng];
  if (isString(options.fallbackNS)) options.fallbackNS = [options.fallbackNS];
  if (options.supportedLngs?.indexOf?.("cimode") < 0) {
    options.supportedLngs = options.supportedLngs.concat(["cimode"]);
  }
  if (typeof options.initImmediate === "boolean") options.initAsync = options.initImmediate;
  return options;
};
var noop6 = () => {
};
var bindMemberFunctions = (inst) => {
  const mems = Object.getOwnPropertyNames(Object.getPrototypeOf(inst));
  mems.forEach((mem) => {
    if (typeof inst[mem] === "function") {
      inst[mem] = inst[mem].bind(inst);
    }
  });
};
var SUPPORT_NOTICE_KEY = "__i18next_supportNoticeShown";
var getSupportNoticeShown = () => {
  if (typeof globalThis !== "undefined" && !!globalThis[SUPPORT_NOTICE_KEY]) return true;
  if (typeof process !== "undefined" && process.env && process.env.I18NEXT_NO_SUPPORT_NOTICE) return true;
  if (typeof process !== "undefined" && process.env && false) return true;
  return false;
};
var setSupportNoticeShown = () => {
  if (typeof globalThis !== "undefined") globalThis[SUPPORT_NOTICE_KEY] = true;
};
var usesLocize = (inst) => {
  if (inst?.modules?.backend?.name?.indexOf("Locize") > 0) return true;
  if (inst?.modules?.backend?.constructor?.name?.indexOf("Locize") > 0) return true;
  if (inst?.options?.backend?.backends) {
    if (inst.options.backend.backends.some((b2) => b2?.name?.indexOf("Locize") > 0 || b2?.constructor?.name?.indexOf("Locize") > 0)) return true;
  }
  if (inst?.options?.backend?.projectId) return true;
  if (inst?.options?.backend?.backendOptions) {
    if (inst.options.backend.backendOptions.some((b2) => b2?.projectId)) return true;
  }
  return false;
};
var I18n = class _I18n extends EventEmitter {
  constructor(options = {}, callback) {
    super();
    this.options = transformOptions(options);
    this.services = {};
    this.logger = baseLogger;
    this.modules = {
      external: []
    };
    bindMemberFunctions(this);
    if (callback && !this.isInitialized && !options.isClone) {
      if (!this.options.initAsync) {
        this.init(options, callback);
        return this;
      }
      setTimeout(() => {
        this.init(options, callback);
      }, 0);
    }
  }
  init(options = {}, callback) {
    this.isInitializing = true;
    if (typeof options === "function") {
      callback = options;
      options = {};
    }
    if (options.defaultNS == null && options.ns) {
      if (isString(options.ns)) {
        options.defaultNS = options.ns;
      } else if (options.ns.indexOf("translation") < 0) {
        options.defaultNS = options.ns[0];
      }
    }
    const defOpts = get();
    this.options = {
      ...defOpts,
      ...this.options,
      ...transformOptions(options)
    };
    this.options.interpolation = {
      ...defOpts.interpolation,
      ...this.options.interpolation
    };
    if (options.keySeparator !== void 0) {
      this.options.userDefinedKeySeparator = options.keySeparator;
    }
    if (options.nsSeparator !== void 0) {
      this.options.userDefinedNsSeparator = options.nsSeparator;
    }
    if (typeof this.options.overloadTranslationOptionHandler !== "function") {
      this.options.overloadTranslationOptionHandler = defOpts.overloadTranslationOptionHandler;
    }
    if (this.options.showSupportNotice !== false && !usesLocize(this) && !getSupportNoticeShown()) {
      if (typeof console !== "undefined" && typeof console.info !== "undefined") console.info("🌐 i18next is made possible by our own product, Locize — consider powering your project with managed localization (AI, CDN, integrations): https://locize.com 💙");
      setSupportNoticeShown();
    }
    const createClassOnDemand = (ClassOrObject) => {
      if (!ClassOrObject) return null;
      if (typeof ClassOrObject === "function") return new ClassOrObject();
      return ClassOrObject;
    };
    if (!this.options.isClone) {
      if (this.modules.logger) {
        baseLogger.init(createClassOnDemand(this.modules.logger), this.options);
      } else {
        baseLogger.init(null, this.options);
      }
      let formatter;
      if (this.modules.formatter) {
        formatter = this.modules.formatter;
      } else {
        formatter = Formatter;
      }
      const lu = new LanguageUtil(this.options);
      this.store = new ResourceStore(this.options.resources, this.options);
      const s2 = this.services;
      s2.logger = baseLogger;
      s2.resourceStore = this.store;
      s2.languageUtils = lu;
      s2.pluralResolver = new PluralResolver(lu, {
        prepend: this.options.pluralSeparator,
        simplifyPluralSuffix: this.options.simplifyPluralSuffix
      });
      const usingLegacyFormatFunction = this.options.interpolation.format && this.options.interpolation.format !== defOpts.interpolation.format;
      if (usingLegacyFormatFunction) {
        this.logger.deprecate(`init: you are still using the legacy format function, please use the new approach: https://www.i18next.com/translation-function/formatting`);
      }
      if (formatter && (!this.options.interpolation.format || this.options.interpolation.format === defOpts.interpolation.format)) {
        s2.formatter = createClassOnDemand(formatter);
        if (s2.formatter.init) s2.formatter.init(s2, this.options);
        this.options.interpolation.format = s2.formatter.format.bind(s2.formatter);
      }
      s2.interpolator = new Interpolator(this.options);
      s2.utils = {
        hasLoadedNamespace: this.hasLoadedNamespace.bind(this)
      };
      s2.backendConnector = new Connector(createClassOnDemand(this.modules.backend), s2.resourceStore, s2, this.options);
      s2.backendConnector.on("*", (event, ...args) => {
        this.emit(event, ...args);
      });
      if (this.modules.languageDetector) {
        s2.languageDetector = createClassOnDemand(this.modules.languageDetector);
        if (s2.languageDetector.init) s2.languageDetector.init(s2, this.options.detection, this.options);
      }
      if (this.modules.i18nFormat) {
        s2.i18nFormat = createClassOnDemand(this.modules.i18nFormat);
        if (s2.i18nFormat.init) s2.i18nFormat.init(this);
      }
      this.translator = new Translator(this.services, this.options);
      this.translator.on("*", (event, ...args) => {
        this.emit(event, ...args);
      });
      this.modules.external.forEach((m2) => {
        if (m2.init) m2.init(this);
      });
    }
    this.format = this.options.interpolation.format;
    if (!callback) callback = noop6;
    if (this.options.fallbackLng && !this.services.languageDetector && !this.options.lng) {
      const codes2 = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
      if (codes2.length > 0 && codes2[0] !== "dev") this.options.lng = codes2[0];
    }
    if (!this.services.languageDetector && !this.options.lng) {
      this.logger.warn("init: no languageDetector is used and no lng is defined");
    }
    const storeApi = ["getResource", "hasResourceBundle", "getResourceBundle", "getDataByLanguage"];
    storeApi.forEach((fcName) => {
      this[fcName] = (...args) => this.store[fcName](...args);
    });
    const storeApiChained = ["addResource", "addResources", "addResourceBundle", "removeResourceBundle"];
    storeApiChained.forEach((fcName) => {
      this[fcName] = (...args) => {
        this.store[fcName](...args);
        return this;
      };
    });
    const deferred = defer();
    const load = () => {
      const finish = (err, t2) => {
        this.isInitializing = false;
        if (this.isInitialized && !this.initializedStoreOnce) this.logger.warn("init: i18next is already initialized. You should call init just once!");
        this.isInitialized = true;
        if (!this.options.isClone) this.logger.log("initialized", this.options);
        this.emit("initialized", this.options);
        deferred.resolve(t2);
        callback(err, t2);
      };
      if (this.languages && !this.isInitialized) return finish(null, this.t.bind(this));
      this.changeLanguage(this.options.lng, finish);
    };
    if (this.options.resources || !this.options.initAsync) {
      load();
    } else {
      setTimeout(load, 0);
    }
    return deferred;
  }
  loadResources(language, callback = noop6) {
    let usedCallback = callback;
    const usedLng = isString(language) ? language : this.language;
    if (typeof language === "function") usedCallback = language;
    if (!this.options.resources || this.options.partialBundledLanguages) {
      if (usedLng?.toLowerCase() === "cimode" && (!this.options.preload || this.options.preload.length === 0)) return usedCallback();
      const toLoad = [];
      const append = (lng) => {
        if (!lng) return;
        if (lng === "cimode") return;
        const lngs = this.services.languageUtils.toResolveHierarchy(lng);
        lngs.forEach((l2) => {
          if (l2 === "cimode") return;
          if (toLoad.indexOf(l2) < 0) toLoad.push(l2);
        });
      };
      if (!usedLng) {
        const fallbacks = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
        fallbacks.forEach((l2) => append(l2));
      } else {
        append(usedLng);
      }
      this.options.preload?.forEach?.((l2) => append(l2));
      this.services.backendConnector.load(toLoad, this.options.ns, (e) => {
        if (!e && !this.resolvedLanguage && this.language) this.setResolvedLanguage(this.language);
        usedCallback(e);
      });
    } else {
      usedCallback(null);
    }
  }
  reloadResources(lngs, ns, callback) {
    const deferred = defer();
    if (typeof lngs === "function") {
      callback = lngs;
      lngs = void 0;
    }
    if (typeof ns === "function") {
      callback = ns;
      ns = void 0;
    }
    if (!lngs) lngs = this.languages;
    if (!ns) ns = this.options.ns;
    if (!callback) callback = noop6;
    this.services.backendConnector.reload(lngs, ns, (err) => {
      deferred.resolve();
      callback(err);
    });
    return deferred;
  }
  use(module) {
    if (!module) throw new Error("You are passing an undefined module! Please check the object you are passing to i18next.use()");
    if (!module.type) throw new Error("You are passing a wrong module! Please check the object you are passing to i18next.use()");
    if (module.type === "backend") {
      this.modules.backend = module;
    }
    if (module.type === "logger" || module.log && module.warn && module.error) {
      this.modules.logger = module;
    }
    if (module.type === "languageDetector") {
      this.modules.languageDetector = module;
    }
    if (module.type === "i18nFormat") {
      this.modules.i18nFormat = module;
    }
    if (module.type === "postProcessor") {
      postProcessor.addPostProcessor(module);
    }
    if (module.type === "formatter") {
      this.modules.formatter = module;
    }
    if (module.type === "3rdParty") {
      this.modules.external.push(module);
    }
    return this;
  }
  setResolvedLanguage(l2) {
    if (!l2 || !this.languages) return;
    if (["cimode", "dev"].indexOf(l2) > -1) return;
    for (let li = 0; li < this.languages.length; li++) {
      const lngInLngs = this.languages[li];
      if (["cimode", "dev"].indexOf(lngInLngs) > -1) continue;
      if (this.store.hasLanguageSomeTranslations(lngInLngs)) {
        this.resolvedLanguage = lngInLngs;
        break;
      }
    }
    if (!this.resolvedLanguage && this.languages.indexOf(l2) < 0 && this.store.hasLanguageSomeTranslations(l2)) {
      this.resolvedLanguage = l2;
      this.languages.unshift(l2);
    }
  }
  changeLanguage(lng, callback) {
    this.isLanguageChangingTo = lng;
    const deferred = defer();
    this.emit("languageChanging", lng);
    const setLngProps = (l2) => {
      this.language = l2;
      this.languages = this.services.languageUtils.toResolveHierarchy(l2);
      this.resolvedLanguage = void 0;
      this.setResolvedLanguage(l2);
    };
    const done = (err, l2) => {
      if (l2) {
        if (this.isLanguageChangingTo === lng) {
          setLngProps(l2);
          this.translator.changeLanguage(l2);
          this.isLanguageChangingTo = void 0;
          this.emit("languageChanged", l2);
          this.logger.log("languageChanged", l2);
        }
      } else {
        this.isLanguageChangingTo = void 0;
      }
      deferred.resolve((...args) => this.t(...args));
      if (callback) callback(err, (...args) => this.t(...args));
    };
    const setLng = (lngs) => {
      if (!lng && !lngs && this.services.languageDetector) lngs = [];
      const fl = isString(lngs) ? lngs : lngs && lngs[0];
      const l2 = this.store.hasLanguageSomeTranslations(fl) ? fl : this.services.languageUtils.getBestMatchFromCodes(isString(lngs) ? [lngs] : lngs);
      if (l2) {
        if (!this.language) {
          setLngProps(l2);
        }
        if (!this.translator.language) this.translator.changeLanguage(l2);
        this.services.languageDetector?.cacheUserLanguage?.(l2);
      }
      this.loadResources(l2, (err) => {
        done(err, l2);
      });
    };
    if (!lng && this.services.languageDetector && !this.services.languageDetector.async) {
      setLng(this.services.languageDetector.detect());
    } else if (!lng && this.services.languageDetector && this.services.languageDetector.async) {
      if (this.services.languageDetector.detect.length === 0) {
        this.services.languageDetector.detect().then(setLng);
      } else {
        this.services.languageDetector.detect(setLng);
      }
    } else {
      setLng(lng);
    }
    return deferred;
  }
  getFixedT(lng, ns, keyPrefix) {
    const fixedT = (key, opts, ...rest) => {
      let o2;
      if (typeof opts !== "object") {
        o2 = this.options.overloadTranslationOptionHandler([key, opts].concat(rest));
      } else {
        o2 = {
          ...opts
        };
      }
      o2.lng = o2.lng || fixedT.lng;
      o2.lngs = o2.lngs || fixedT.lngs;
      o2.ns = o2.ns || fixedT.ns;
      if (o2.keyPrefix !== "") o2.keyPrefix = o2.keyPrefix || keyPrefix || fixedT.keyPrefix;
      const selectorOpts = {
        ...this.options,
        ...o2
      };
      if (typeof o2.keyPrefix === "function") o2.keyPrefix = keysFromSelector(o2.keyPrefix, selectorOpts);
      const keySeparator = this.options.keySeparator || ".";
      let resultKey;
      if (o2.keyPrefix && Array.isArray(key)) {
        resultKey = key.map((k2) => {
          if (typeof k2 === "function") k2 = keysFromSelector(k2, selectorOpts);
          return `${o2.keyPrefix}${keySeparator}${k2}`;
        });
      } else {
        if (typeof key === "function") key = keysFromSelector(key, selectorOpts);
        resultKey = o2.keyPrefix ? `${o2.keyPrefix}${keySeparator}${key}` : key;
      }
      return this.t(resultKey, o2);
    };
    if (isString(lng)) {
      fixedT.lng = lng;
    } else {
      fixedT.lngs = lng;
    }
    fixedT.ns = ns;
    fixedT.keyPrefix = keyPrefix;
    return fixedT;
  }
  t(...args) {
    return this.translator?.translate(...args);
  }
  exists(...args) {
    return this.translator?.exists(...args);
  }
  setDefaultNamespace(ns) {
    this.options.defaultNS = ns;
  }
  hasLoadedNamespace(ns, options = {}) {
    if (!this.isInitialized) {
      this.logger.warn("hasLoadedNamespace: i18next was not initialized", this.languages);
      return false;
    }
    if (!this.languages || !this.languages.length) {
      this.logger.warn("hasLoadedNamespace: i18n.languages were undefined or empty", this.languages);
      return false;
    }
    const lng = options.lng || this.resolvedLanguage || this.languages[0];
    const fallbackLng = this.options ? this.options.fallbackLng : false;
    const lastLng = this.languages[this.languages.length - 1];
    if (lng.toLowerCase() === "cimode") return true;
    const loadNotPending = (l2, n2) => {
      const loadState = this.services.backendConnector.state[`${l2}|${n2}`];
      return loadState === -1 || loadState === 0 || loadState === 2;
    };
    if (options.precheck) {
      const preResult = options.precheck(this, loadNotPending);
      if (preResult !== void 0) return preResult;
    }
    if (this.hasResourceBundle(lng, ns)) return true;
    if (!this.services.backendConnector.backend || this.options.resources && !this.options.partialBundledLanguages) return true;
    if (loadNotPending(lng, ns) && (!fallbackLng || loadNotPending(lastLng, ns))) return true;
    return false;
  }
  loadNamespaces(ns, callback) {
    const deferred = defer();
    if (!this.options.ns) {
      if (callback) callback();
      return Promise.resolve();
    }
    if (isString(ns)) ns = [ns];
    ns.forEach((n2) => {
      if (this.options.ns.indexOf(n2) < 0) this.options.ns.push(n2);
    });
    this.loadResources((err) => {
      deferred.resolve();
      if (callback) callback(err);
    });
    return deferred;
  }
  loadLanguages(lngs, callback) {
    const deferred = defer();
    if (isString(lngs)) lngs = [lngs];
    const preloaded = this.options.preload || [];
    const newLngs = lngs.filter((lng) => preloaded.indexOf(lng) < 0 && this.services.languageUtils.isSupportedCode(lng));
    if (!newLngs.length) {
      if (callback) callback();
      return Promise.resolve();
    }
    this.options.preload = preloaded.concat(newLngs);
    this.loadResources((err) => {
      deferred.resolve();
      if (callback) callback(err);
    });
    return deferred;
  }
  dir(lng) {
    if (!lng) lng = this.resolvedLanguage || (this.languages?.length > 0 ? this.languages[0] : this.language);
    if (!lng) return "rtl";
    try {
      const l2 = new Intl.Locale(lng);
      if (l2 && l2.getTextInfo) {
        const ti = l2.getTextInfo();
        if (ti && ti.direction) return ti.direction;
      }
    } catch (e) {
    }
    const rtlLngs = ["ar", "shu", "sqr", "ssh", "xaa", "yhd", "yud", "aao", "abh", "abv", "acm", "acq", "acw", "acx", "acy", "adf", "ads", "aeb", "aec", "afb", "ajp", "apc", "apd", "arb", "arq", "ars", "ary", "arz", "auz", "avl", "ayh", "ayl", "ayn", "ayp", "bbz", "pga", "he", "iw", "ps", "pbt", "pbu", "pst", "prp", "prd", "ug", "ur", "ydd", "yds", "yih", "ji", "yi", "hbo", "men", "xmn", "fa", "jpr", "peo", "pes", "prs", "dv", "sam", "ckb"];
    const languageUtils = this.services?.languageUtils || new LanguageUtil(get());
    if (lng.toLowerCase().indexOf("-latn") > 1) return "ltr";
    return rtlLngs.indexOf(languageUtils.getLanguagePartFromCode(lng)) > -1 || lng.toLowerCase().indexOf("-arab") > 1 ? "rtl" : "ltr";
  }
  static createInstance(options = {}, callback) {
    const instance2 = new _I18n(options, callback);
    instance2.createInstance = _I18n.createInstance;
    return instance2;
  }
  cloneInstance(options = {}, callback = noop6) {
    const forkResourceStore = options.forkResourceStore;
    if (forkResourceStore) delete options.forkResourceStore;
    const mergedOptions = {
      ...this.options,
      ...options,
      ...{
        isClone: true
      }
    };
    const clone = new _I18n(mergedOptions);
    if (options.debug !== void 0 || options.prefix !== void 0) {
      clone.logger = clone.logger.clone(options);
    }
    const membersToCopy = ["store", "services", "language"];
    membersToCopy.forEach((m2) => {
      clone[m2] = this[m2];
    });
    clone.services = {
      ...this.services
    };
    clone.services.utils = {
      hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
    };
    if (forkResourceStore) {
      const clonedData = Object.keys(this.store.data).reduce((prev, l2) => {
        prev[l2] = {
          ...this.store.data[l2]
        };
        prev[l2] = Object.keys(prev[l2]).reduce((acc, n2) => {
          acc[n2] = {
            ...prev[l2][n2]
          };
          return acc;
        }, prev[l2]);
        return prev;
      }, {});
      clone.store = new ResourceStore(clonedData, mergedOptions);
      clone.services.resourceStore = clone.store;
    }
    if (options.interpolation) {
      const defOpts = get();
      const mergedInterpolation = {
        ...defOpts.interpolation,
        ...this.options.interpolation,
        ...options.interpolation
      };
      const mergedForInterpolator = {
        ...mergedOptions,
        interpolation: mergedInterpolation
      };
      clone.services.interpolator = new Interpolator(mergedForInterpolator);
    }
    clone.translator = new Translator(clone.services, mergedOptions);
    clone.translator.on("*", (event, ...args) => {
      clone.emit(event, ...args);
    });
    clone.init(mergedOptions, callback);
    clone.translator.options = mergedOptions;
    clone.translator.backendConnector.services.utils = {
      hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
    };
    return clone;
  }
  toJSON() {
    return {
      options: this.options,
      store: this.store,
      language: this.language,
      languages: this.languages,
      resolvedLanguage: this.resolvedLanguage
    };
  }
};
var instance = I18n.createInstance();
var createInstance = instance.createInstance;
var dir = instance.dir;
var init = instance.init;
var loadResources = instance.loadResources;
var reloadResources = instance.reloadResources;
var use = instance.use;
var changeLanguage = instance.changeLanguage;
var getFixedT = instance.getFixedT;
var t = instance.t;
var exists = instance.exists;
var setDefaultNamespace = instance.setDefaultNamespace;
var hasLoadedNamespace = instance.hasLoadedNamespace;
var loadNamespaces = instance.loadNamespaces;
var loadLanguages = instance.loadLanguages;

// node_modules/stream-chat-react/dist/es/index.mjs
var import_dayjs2 = __toESM(require_dayjs_min(), 1);
var import_fix_webm_duration = __toESM(require_fix_webm_duration(), 1);
var import_lodash6 = __toESM(require_lodash4(), 1);

// node_modules/hast-util-is-element/lib/index.js
var convertElement = (
  // Note: overloads in JSDoc can’t yet use different `@template`s.
  /**
   * @type {(
   *   (<Condition extends TestFunction>(test: Condition) => (element: unknown, index?: number | null | undefined, parent?: Parents | null | undefined, context?: unknown) => element is Element & Predicate<Condition, Element>) &
   *   (<Condition extends string>(test: Condition) => (element: unknown, index?: number | null | undefined, parent?: Parents | null | undefined, context?: unknown) => element is Element & {tagName: Condition}) &
   *   ((test?: null | undefined) => (element?: unknown, index?: number | null | undefined, parent?: Parents | null | undefined, context?: unknown) => element is Element) &
   *   ((test?: Test) => Check)
   * )}
   */
  /**
   * @param {Test | null | undefined} [test]
   * @returns {Check}
   */
  (function(test2) {
    if (test2 === null || test2 === void 0) {
      return element3;
    }
    if (typeof test2 === "string") {
      return tagNameFactory(test2);
    }
    if (typeof test2 === "object") {
      return anyFactory2(test2);
    }
    if (typeof test2 === "function") {
      return castFactory2(test2);
    }
    throw new Error("Expected function, string, or array as `test`");
  })
);
function anyFactory2(tests) {
  const checks2 = [];
  let index6 = -1;
  while (++index6 < tests.length) {
    checks2[index6] = convertElement(tests[index6]);
  }
  return castFactory2(any);
  function any(...parameters) {
    let index7 = -1;
    while (++index7 < checks2.length) {
      if (checks2[index7].apply(this, parameters)) return true;
    }
    return false;
  }
}
function tagNameFactory(check) {
  return castFactory2(tagName);
  function tagName(element4) {
    return element4.tagName === check;
  }
}
function castFactory2(testFunction) {
  return check;
  function check(value, index6, parent) {
    return Boolean(
      looksLikeAnElement(value) && testFunction.call(
        this,
        value,
        typeof index6 === "number" ? index6 : void 0,
        parent || void 0
      )
    );
  }
}
function element3(element4) {
  return Boolean(
    element4 && typeof element4 === "object" && "type" in element4 && element4.type === "element" && "tagName" in element4 && typeof element4.tagName === "string"
  );
}
function looksLikeAnElement(value) {
  return value !== null && typeof value === "object" && "type" in value && "tagName" in value;
}

// node_modules/hast-util-find-and-replace/node_modules/escape-string-regexp/index.js
function escapeStringRegexp2(string3) {
  if (typeof string3 !== "string") {
    throw new TypeError("Expected a string");
  }
  return string3.replace(/[|\\{}()[\]^$+*?.]/g, "\\$&").replace(/-/g, "\\x2d");
}

// node_modules/hast-util-find-and-replace/lib/index.js
var emptyOptions5 = {};
var defaultIgnore = ["math", "script", "style", "svg", "title"];
function findAndReplace2(tree, list4, options) {
  const settings = options || emptyOptions5;
  const ignored = convertElement(settings.ignore || defaultIgnore);
  const pairs = toPairs2(list4);
  let pairIndex = -1;
  while (++pairIndex < pairs.length) {
    visitParents(tree, "text", visitor2);
  }
  function visitor2(node2, parents) {
    let index6 = -1;
    let grandparent;
    while (++index6 < parents.length) {
      const parent = parents[index6];
      const siblings = grandparent ? grandparent.children : void 0;
      if (ignored(
        parent,
        siblings ? siblings.indexOf(parent) : void 0,
        grandparent
      )) {
        return;
      }
      grandparent = parent;
    }
    if (grandparent) {
      return handler(node2, parents);
    }
  }
  function handler(node2, parents) {
    const parent = parents[parents.length - 1];
    const find3 = pairs[pairIndex][0];
    const replace2 = pairs[pairIndex][1];
    let start2 = 0;
    const siblings = parent.children;
    const index6 = siblings.indexOf(node2);
    let change = false;
    let nodes = [];
    find3.lastIndex = 0;
    let match = find3.exec(node2.value);
    while (match) {
      const position3 = match.index;
      const matchObject = {
        index: match.index,
        input: match.input,
        stack: [...parents, node2]
      };
      let value = replace2(...match, matchObject);
      if (typeof value === "string") {
        value = value.length > 0 ? { type: "text", value } : void 0;
      }
      if (value === false) {
        find3.lastIndex = position3 + 1;
      } else {
        if (start2 !== position3) {
          nodes.push({ type: "text", value: node2.value.slice(start2, position3) });
        }
        if (Array.isArray(value)) {
          nodes.push(...value);
        } else if (value) {
          nodes.push(value);
        }
        start2 = position3 + match[0].length;
        change = true;
      }
      if (!find3.global) {
        break;
      }
      match = find3.exec(node2.value);
    }
    if (change) {
      if (start2 < node2.value.length) {
        nodes.push({ type: "text", value: node2.value.slice(start2) });
      }
      parent.children.splice(index6, 1, ...nodes);
    } else {
      nodes = [node2];
    }
    return index6 + nodes.length;
  }
}
function toPairs2(tupleOrList) {
  const result = [];
  if (!Array.isArray(tupleOrList)) {
    throw new TypeError("Expected find and replace tuple or list of tuples");
  }
  const list4 = !tupleOrList[0] || Array.isArray(tupleOrList[0]) ? tupleOrList : [tupleOrList];
  let index6 = -1;
  while (++index6 < list4.length) {
    const tuple = list4[index6];
    result.push([toExpression2(tuple[0]), toFunction2(tuple[1])]);
  }
  return result;
}
function toExpression2(find3) {
  return typeof find3 === "string" ? new RegExp(escapeStringRegexp2(find3), "g") : find3;
}
function toFunction2(replace2) {
  return typeof replace2 === "function" ? replace2 : function() {
    return replace2;
  };
}

// node_modules/unist-builder/lib/index.js
function u(type, props, value) {
  const node2 = { type: String(type) };
  if ((value === void 0 || value === null) && (typeof props === "string" || Array.isArray(props))) {
    value = props;
  } else {
    Object.assign(node2, props);
  }
  if (Array.isArray(value)) {
    node2.children = value;
  } else if (value !== void 0 && value !== null) {
    node2.value = String(value);
  }
  return node2;
}

// node_modules/stream-chat-react/dist/es/index.mjs
var import_emoji_regex2 = __toESM(require_emoji_regex(), 1);

// node_modules/react-dropzone/dist/es/index.js
var import_react23 = __toESM(require_react());
var import_prop_types = __toESM(require_prop_types());

// node_modules/tslib/tslib.es6.mjs
function __awaiter(thisArg, _arguments, P2, generator) {
  function adopt(value) {
    return value instanceof P2 ? value : new P2(function(resolve) {
      resolve(value);
    });
  }
  return new (P2 || (P2 = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}

// node_modules/file-selector/dist/es2015/file.js
var COMMON_MIME_TYPES = /* @__PURE__ */ new Map([
  // https://github.com/guzzle/psr7/blob/2d9260799e713f1c475d3c5fdc3d6561ff7441b2/src/MimeType.php
  ["1km", "application/vnd.1000minds.decision-model+xml"],
  ["3dml", "text/vnd.in3d.3dml"],
  ["3ds", "image/x-3ds"],
  ["3g2", "video/3gpp2"],
  ["3gp", "video/3gp"],
  ["3gpp", "video/3gpp"],
  ["3mf", "model/3mf"],
  ["7z", "application/x-7z-compressed"],
  ["7zip", "application/x-7z-compressed"],
  ["123", "application/vnd.lotus-1-2-3"],
  ["aab", "application/x-authorware-bin"],
  ["aac", "audio/x-acc"],
  ["aam", "application/x-authorware-map"],
  ["aas", "application/x-authorware-seg"],
  ["abw", "application/x-abiword"],
  ["ac", "application/vnd.nokia.n-gage.ac+xml"],
  ["ac3", "audio/ac3"],
  ["acc", "application/vnd.americandynamics.acc"],
  ["ace", "application/x-ace-compressed"],
  ["acu", "application/vnd.acucobol"],
  ["acutc", "application/vnd.acucorp"],
  ["adp", "audio/adpcm"],
  ["aep", "application/vnd.audiograph"],
  ["afm", "application/x-font-type1"],
  ["afp", "application/vnd.ibm.modcap"],
  ["ahead", "application/vnd.ahead.space"],
  ["ai", "application/pdf"],
  ["aif", "audio/x-aiff"],
  ["aifc", "audio/x-aiff"],
  ["aiff", "audio/x-aiff"],
  ["air", "application/vnd.adobe.air-application-installer-package+zip"],
  ["ait", "application/vnd.dvb.ait"],
  ["ami", "application/vnd.amiga.ami"],
  ["amr", "audio/amr"],
  ["apk", "application/vnd.android.package-archive"],
  ["apng", "image/apng"],
  ["appcache", "text/cache-manifest"],
  ["application", "application/x-ms-application"],
  ["apr", "application/vnd.lotus-approach"],
  ["arc", "application/x-freearc"],
  ["arj", "application/x-arj"],
  ["asc", "application/pgp-signature"],
  ["asf", "video/x-ms-asf"],
  ["asm", "text/x-asm"],
  ["aso", "application/vnd.accpac.simply.aso"],
  ["asx", "video/x-ms-asf"],
  ["atc", "application/vnd.acucorp"],
  ["atom", "application/atom+xml"],
  ["atomcat", "application/atomcat+xml"],
  ["atomdeleted", "application/atomdeleted+xml"],
  ["atomsvc", "application/atomsvc+xml"],
  ["atx", "application/vnd.antix.game-component"],
  ["au", "audio/x-au"],
  ["avi", "video/x-msvideo"],
  ["avif", "image/avif"],
  ["aw", "application/applixware"],
  ["azf", "application/vnd.airzip.filesecure.azf"],
  ["azs", "application/vnd.airzip.filesecure.azs"],
  ["azv", "image/vnd.airzip.accelerator.azv"],
  ["azw", "application/vnd.amazon.ebook"],
  ["b16", "image/vnd.pco.b16"],
  ["bat", "application/x-msdownload"],
  ["bcpio", "application/x-bcpio"],
  ["bdf", "application/x-font-bdf"],
  ["bdm", "application/vnd.syncml.dm+wbxml"],
  ["bdoc", "application/x-bdoc"],
  ["bed", "application/vnd.realvnc.bed"],
  ["bh2", "application/vnd.fujitsu.oasysprs"],
  ["bin", "application/octet-stream"],
  ["blb", "application/x-blorb"],
  ["blorb", "application/x-blorb"],
  ["bmi", "application/vnd.bmi"],
  ["bmml", "application/vnd.balsamiq.bmml+xml"],
  ["bmp", "image/bmp"],
  ["book", "application/vnd.framemaker"],
  ["box", "application/vnd.previewsystems.box"],
  ["boz", "application/x-bzip2"],
  ["bpk", "application/octet-stream"],
  ["bpmn", "application/octet-stream"],
  ["bsp", "model/vnd.valve.source.compiled-map"],
  ["btif", "image/prs.btif"],
  ["buffer", "application/octet-stream"],
  ["bz", "application/x-bzip"],
  ["bz2", "application/x-bzip2"],
  ["c", "text/x-c"],
  ["c4d", "application/vnd.clonk.c4group"],
  ["c4f", "application/vnd.clonk.c4group"],
  ["c4g", "application/vnd.clonk.c4group"],
  ["c4p", "application/vnd.clonk.c4group"],
  ["c4u", "application/vnd.clonk.c4group"],
  ["c11amc", "application/vnd.cluetrust.cartomobile-config"],
  ["c11amz", "application/vnd.cluetrust.cartomobile-config-pkg"],
  ["cab", "application/vnd.ms-cab-compressed"],
  ["caf", "audio/x-caf"],
  ["cap", "application/vnd.tcpdump.pcap"],
  ["car", "application/vnd.curl.car"],
  ["cat", "application/vnd.ms-pki.seccat"],
  ["cb7", "application/x-cbr"],
  ["cba", "application/x-cbr"],
  ["cbr", "application/x-cbr"],
  ["cbt", "application/x-cbr"],
  ["cbz", "application/x-cbr"],
  ["cc", "text/x-c"],
  ["cco", "application/x-cocoa"],
  ["cct", "application/x-director"],
  ["ccxml", "application/ccxml+xml"],
  ["cdbcmsg", "application/vnd.contact.cmsg"],
  ["cda", "application/x-cdf"],
  ["cdf", "application/x-netcdf"],
  ["cdfx", "application/cdfx+xml"],
  ["cdkey", "application/vnd.mediastation.cdkey"],
  ["cdmia", "application/cdmi-capability"],
  ["cdmic", "application/cdmi-container"],
  ["cdmid", "application/cdmi-domain"],
  ["cdmio", "application/cdmi-object"],
  ["cdmiq", "application/cdmi-queue"],
  ["cdr", "application/cdr"],
  ["cdx", "chemical/x-cdx"],
  ["cdxml", "application/vnd.chemdraw+xml"],
  ["cdy", "application/vnd.cinderella"],
  ["cer", "application/pkix-cert"],
  ["cfs", "application/x-cfs-compressed"],
  ["cgm", "image/cgm"],
  ["chat", "application/x-chat"],
  ["chm", "application/vnd.ms-htmlhelp"],
  ["chrt", "application/vnd.kde.kchart"],
  ["cif", "chemical/x-cif"],
  ["cii", "application/vnd.anser-web-certificate-issue-initiation"],
  ["cil", "application/vnd.ms-artgalry"],
  ["cjs", "application/node"],
  ["cla", "application/vnd.claymore"],
  ["class", "application/octet-stream"],
  ["clkk", "application/vnd.crick.clicker.keyboard"],
  ["clkp", "application/vnd.crick.clicker.palette"],
  ["clkt", "application/vnd.crick.clicker.template"],
  ["clkw", "application/vnd.crick.clicker.wordbank"],
  ["clkx", "application/vnd.crick.clicker"],
  ["clp", "application/x-msclip"],
  ["cmc", "application/vnd.cosmocaller"],
  ["cmdf", "chemical/x-cmdf"],
  ["cml", "chemical/x-cml"],
  ["cmp", "application/vnd.yellowriver-custom-menu"],
  ["cmx", "image/x-cmx"],
  ["cod", "application/vnd.rim.cod"],
  ["coffee", "text/coffeescript"],
  ["com", "application/x-msdownload"],
  ["conf", "text/plain"],
  ["cpio", "application/x-cpio"],
  ["cpp", "text/x-c"],
  ["cpt", "application/mac-compactpro"],
  ["crd", "application/x-mscardfile"],
  ["crl", "application/pkix-crl"],
  ["crt", "application/x-x509-ca-cert"],
  ["crx", "application/x-chrome-extension"],
  ["cryptonote", "application/vnd.rig.cryptonote"],
  ["csh", "application/x-csh"],
  ["csl", "application/vnd.citationstyles.style+xml"],
  ["csml", "chemical/x-csml"],
  ["csp", "application/vnd.commonspace"],
  ["csr", "application/octet-stream"],
  ["css", "text/css"],
  ["cst", "application/x-director"],
  ["csv", "text/csv"],
  ["cu", "application/cu-seeme"],
  ["curl", "text/vnd.curl"],
  ["cww", "application/prs.cww"],
  ["cxt", "application/x-director"],
  ["cxx", "text/x-c"],
  ["dae", "model/vnd.collada+xml"],
  ["daf", "application/vnd.mobius.daf"],
  ["dart", "application/vnd.dart"],
  ["dataless", "application/vnd.fdsn.seed"],
  ["davmount", "application/davmount+xml"],
  ["dbf", "application/vnd.dbf"],
  ["dbk", "application/docbook+xml"],
  ["dcr", "application/x-director"],
  ["dcurl", "text/vnd.curl.dcurl"],
  ["dd2", "application/vnd.oma.dd2+xml"],
  ["ddd", "application/vnd.fujixerox.ddd"],
  ["ddf", "application/vnd.syncml.dmddf+xml"],
  ["dds", "image/vnd.ms-dds"],
  ["deb", "application/x-debian-package"],
  ["def", "text/plain"],
  ["deploy", "application/octet-stream"],
  ["der", "application/x-x509-ca-cert"],
  ["dfac", "application/vnd.dreamfactory"],
  ["dgc", "application/x-dgc-compressed"],
  ["dic", "text/x-c"],
  ["dir", "application/x-director"],
  ["dis", "application/vnd.mobius.dis"],
  ["disposition-notification", "message/disposition-notification"],
  ["dist", "application/octet-stream"],
  ["distz", "application/octet-stream"],
  ["djv", "image/vnd.djvu"],
  ["djvu", "image/vnd.djvu"],
  ["dll", "application/octet-stream"],
  ["dmg", "application/x-apple-diskimage"],
  ["dmn", "application/octet-stream"],
  ["dmp", "application/vnd.tcpdump.pcap"],
  ["dms", "application/octet-stream"],
  ["dna", "application/vnd.dna"],
  ["doc", "application/msword"],
  ["docm", "application/vnd.ms-word.template.macroEnabled.12"],
  ["docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document"],
  ["dot", "application/msword"],
  ["dotm", "application/vnd.ms-word.template.macroEnabled.12"],
  ["dotx", "application/vnd.openxmlformats-officedocument.wordprocessingml.template"],
  ["dp", "application/vnd.osgi.dp"],
  ["dpg", "application/vnd.dpgraph"],
  ["dra", "audio/vnd.dra"],
  ["drle", "image/dicom-rle"],
  ["dsc", "text/prs.lines.tag"],
  ["dssc", "application/dssc+der"],
  ["dtb", "application/x-dtbook+xml"],
  ["dtd", "application/xml-dtd"],
  ["dts", "audio/vnd.dts"],
  ["dtshd", "audio/vnd.dts.hd"],
  ["dump", "application/octet-stream"],
  ["dvb", "video/vnd.dvb.file"],
  ["dvi", "application/x-dvi"],
  ["dwd", "application/atsc-dwd+xml"],
  ["dwf", "model/vnd.dwf"],
  ["dwg", "image/vnd.dwg"],
  ["dxf", "image/vnd.dxf"],
  ["dxp", "application/vnd.spotfire.dxp"],
  ["dxr", "application/x-director"],
  ["ear", "application/java-archive"],
  ["ecelp4800", "audio/vnd.nuera.ecelp4800"],
  ["ecelp7470", "audio/vnd.nuera.ecelp7470"],
  ["ecelp9600", "audio/vnd.nuera.ecelp9600"],
  ["ecma", "application/ecmascript"],
  ["edm", "application/vnd.novadigm.edm"],
  ["edx", "application/vnd.novadigm.edx"],
  ["efif", "application/vnd.picsel"],
  ["ei6", "application/vnd.pg.osasli"],
  ["elc", "application/octet-stream"],
  ["emf", "image/emf"],
  ["eml", "message/rfc822"],
  ["emma", "application/emma+xml"],
  ["emotionml", "application/emotionml+xml"],
  ["emz", "application/x-msmetafile"],
  ["eol", "audio/vnd.digital-winds"],
  ["eot", "application/vnd.ms-fontobject"],
  ["eps", "application/postscript"],
  ["epub", "application/epub+zip"],
  ["es", "application/ecmascript"],
  ["es3", "application/vnd.eszigno3+xml"],
  ["esa", "application/vnd.osgi.subsystem"],
  ["esf", "application/vnd.epson.esf"],
  ["et3", "application/vnd.eszigno3+xml"],
  ["etx", "text/x-setext"],
  ["eva", "application/x-eva"],
  ["evy", "application/x-envoy"],
  ["exe", "application/octet-stream"],
  ["exi", "application/exi"],
  ["exp", "application/express"],
  ["exr", "image/aces"],
  ["ext", "application/vnd.novadigm.ext"],
  ["ez", "application/andrew-inset"],
  ["ez2", "application/vnd.ezpix-album"],
  ["ez3", "application/vnd.ezpix-package"],
  ["f", "text/x-fortran"],
  ["f4v", "video/mp4"],
  ["f77", "text/x-fortran"],
  ["f90", "text/x-fortran"],
  ["fbs", "image/vnd.fastbidsheet"],
  ["fcdt", "application/vnd.adobe.formscentral.fcdt"],
  ["fcs", "application/vnd.isac.fcs"],
  ["fdf", "application/vnd.fdf"],
  ["fdt", "application/fdt+xml"],
  ["fe_launch", "application/vnd.denovo.fcselayout-link"],
  ["fg5", "application/vnd.fujitsu.oasysgp"],
  ["fgd", "application/x-director"],
  ["fh", "image/x-freehand"],
  ["fh4", "image/x-freehand"],
  ["fh5", "image/x-freehand"],
  ["fh7", "image/x-freehand"],
  ["fhc", "image/x-freehand"],
  ["fig", "application/x-xfig"],
  ["fits", "image/fits"],
  ["flac", "audio/x-flac"],
  ["fli", "video/x-fli"],
  ["flo", "application/vnd.micrografx.flo"],
  ["flv", "video/x-flv"],
  ["flw", "application/vnd.kde.kivio"],
  ["flx", "text/vnd.fmi.flexstor"],
  ["fly", "text/vnd.fly"],
  ["fm", "application/vnd.framemaker"],
  ["fnc", "application/vnd.frogans.fnc"],
  ["fo", "application/vnd.software602.filler.form+xml"],
  ["for", "text/x-fortran"],
  ["fpx", "image/vnd.fpx"],
  ["frame", "application/vnd.framemaker"],
  ["fsc", "application/vnd.fsc.weblaunch"],
  ["fst", "image/vnd.fst"],
  ["ftc", "application/vnd.fluxtime.clip"],
  ["fti", "application/vnd.anser-web-funds-transfer-initiation"],
  ["fvt", "video/vnd.fvt"],
  ["fxp", "application/vnd.adobe.fxp"],
  ["fxpl", "application/vnd.adobe.fxp"],
  ["fzs", "application/vnd.fuzzysheet"],
  ["g2w", "application/vnd.geoplan"],
  ["g3", "image/g3fax"],
  ["g3w", "application/vnd.geospace"],
  ["gac", "application/vnd.groove-account"],
  ["gam", "application/x-tads"],
  ["gbr", "application/rpki-ghostbusters"],
  ["gca", "application/x-gca-compressed"],
  ["gdl", "model/vnd.gdl"],
  ["gdoc", "application/vnd.google-apps.document"],
  ["geo", "application/vnd.dynageo"],
  ["geojson", "application/geo+json"],
  ["gex", "application/vnd.geometry-explorer"],
  ["ggb", "application/vnd.geogebra.file"],
  ["ggt", "application/vnd.geogebra.tool"],
  ["ghf", "application/vnd.groove-help"],
  ["gif", "image/gif"],
  ["gim", "application/vnd.groove-identity-message"],
  ["glb", "model/gltf-binary"],
  ["gltf", "model/gltf+json"],
  ["gml", "application/gml+xml"],
  ["gmx", "application/vnd.gmx"],
  ["gnumeric", "application/x-gnumeric"],
  ["gpg", "application/gpg-keys"],
  ["gph", "application/vnd.flographit"],
  ["gpx", "application/gpx+xml"],
  ["gqf", "application/vnd.grafeq"],
  ["gqs", "application/vnd.grafeq"],
  ["gram", "application/srgs"],
  ["gramps", "application/x-gramps-xml"],
  ["gre", "application/vnd.geometry-explorer"],
  ["grv", "application/vnd.groove-injector"],
  ["grxml", "application/srgs+xml"],
  ["gsf", "application/x-font-ghostscript"],
  ["gsheet", "application/vnd.google-apps.spreadsheet"],
  ["gslides", "application/vnd.google-apps.presentation"],
  ["gtar", "application/x-gtar"],
  ["gtm", "application/vnd.groove-tool-message"],
  ["gtw", "model/vnd.gtw"],
  ["gv", "text/vnd.graphviz"],
  ["gxf", "application/gxf"],
  ["gxt", "application/vnd.geonext"],
  ["gz", "application/gzip"],
  ["gzip", "application/gzip"],
  ["h", "text/x-c"],
  ["h261", "video/h261"],
  ["h263", "video/h263"],
  ["h264", "video/h264"],
  ["hal", "application/vnd.hal+xml"],
  ["hbci", "application/vnd.hbci"],
  ["hbs", "text/x-handlebars-template"],
  ["hdd", "application/x-virtualbox-hdd"],
  ["hdf", "application/x-hdf"],
  ["heic", "image/heic"],
  ["heics", "image/heic-sequence"],
  ["heif", "image/heif"],
  ["heifs", "image/heif-sequence"],
  ["hej2", "image/hej2k"],
  ["held", "application/atsc-held+xml"],
  ["hh", "text/x-c"],
  ["hjson", "application/hjson"],
  ["hlp", "application/winhlp"],
  ["hpgl", "application/vnd.hp-hpgl"],
  ["hpid", "application/vnd.hp-hpid"],
  ["hps", "application/vnd.hp-hps"],
  ["hqx", "application/mac-binhex40"],
  ["hsj2", "image/hsj2"],
  ["htc", "text/x-component"],
  ["htke", "application/vnd.kenameaapp"],
  ["htm", "text/html"],
  ["html", "text/html"],
  ["hvd", "application/vnd.yamaha.hv-dic"],
  ["hvp", "application/vnd.yamaha.hv-voice"],
  ["hvs", "application/vnd.yamaha.hv-script"],
  ["i2g", "application/vnd.intergeo"],
  ["icc", "application/vnd.iccprofile"],
  ["ice", "x-conference/x-cooltalk"],
  ["icm", "application/vnd.iccprofile"],
  ["ico", "image/x-icon"],
  ["ics", "text/calendar"],
  ["ief", "image/ief"],
  ["ifb", "text/calendar"],
  ["ifm", "application/vnd.shana.informed.formdata"],
  ["iges", "model/iges"],
  ["igl", "application/vnd.igloader"],
  ["igm", "application/vnd.insors.igm"],
  ["igs", "model/iges"],
  ["igx", "application/vnd.micrografx.igx"],
  ["iif", "application/vnd.shana.informed.interchange"],
  ["img", "application/octet-stream"],
  ["imp", "application/vnd.accpac.simply.imp"],
  ["ims", "application/vnd.ms-ims"],
  ["in", "text/plain"],
  ["ini", "text/plain"],
  ["ink", "application/inkml+xml"],
  ["inkml", "application/inkml+xml"],
  ["install", "application/x-install-instructions"],
  ["iota", "application/vnd.astraea-software.iota"],
  ["ipfix", "application/ipfix"],
  ["ipk", "application/vnd.shana.informed.package"],
  ["irm", "application/vnd.ibm.rights-management"],
  ["irp", "application/vnd.irepository.package+xml"],
  ["iso", "application/x-iso9660-image"],
  ["itp", "application/vnd.shana.informed.formtemplate"],
  ["its", "application/its+xml"],
  ["ivp", "application/vnd.immervision-ivp"],
  ["ivu", "application/vnd.immervision-ivu"],
  ["jad", "text/vnd.sun.j2me.app-descriptor"],
  ["jade", "text/jade"],
  ["jam", "application/vnd.jam"],
  ["jar", "application/java-archive"],
  ["jardiff", "application/x-java-archive-diff"],
  ["java", "text/x-java-source"],
  ["jhc", "image/jphc"],
  ["jisp", "application/vnd.jisp"],
  ["jls", "image/jls"],
  ["jlt", "application/vnd.hp-jlyt"],
  ["jng", "image/x-jng"],
  ["jnlp", "application/x-java-jnlp-file"],
  ["joda", "application/vnd.joost.joda-archive"],
  ["jp2", "image/jp2"],
  ["jpe", "image/jpeg"],
  ["jpeg", "image/jpeg"],
  ["jpf", "image/jpx"],
  ["jpg", "image/jpeg"],
  ["jpg2", "image/jp2"],
  ["jpgm", "video/jpm"],
  ["jpgv", "video/jpeg"],
  ["jph", "image/jph"],
  ["jpm", "video/jpm"],
  ["jpx", "image/jpx"],
  ["js", "application/javascript"],
  ["json", "application/json"],
  ["json5", "application/json5"],
  ["jsonld", "application/ld+json"],
  // https://jsonlines.org/
  ["jsonl", "application/jsonl"],
  ["jsonml", "application/jsonml+json"],
  ["jsx", "text/jsx"],
  ["jxr", "image/jxr"],
  ["jxra", "image/jxra"],
  ["jxrs", "image/jxrs"],
  ["jxs", "image/jxs"],
  ["jxsc", "image/jxsc"],
  ["jxsi", "image/jxsi"],
  ["jxss", "image/jxss"],
  ["kar", "audio/midi"],
  ["karbon", "application/vnd.kde.karbon"],
  ["kdb", "application/octet-stream"],
  ["kdbx", "application/x-keepass2"],
  ["key", "application/x-iwork-keynote-sffkey"],
  ["kfo", "application/vnd.kde.kformula"],
  ["kia", "application/vnd.kidspiration"],
  ["kml", "application/vnd.google-earth.kml+xml"],
  ["kmz", "application/vnd.google-earth.kmz"],
  ["kne", "application/vnd.kinar"],
  ["knp", "application/vnd.kinar"],
  ["kon", "application/vnd.kde.kontour"],
  ["kpr", "application/vnd.kde.kpresenter"],
  ["kpt", "application/vnd.kde.kpresenter"],
  ["kpxx", "application/vnd.ds-keypoint"],
  ["ksp", "application/vnd.kde.kspread"],
  ["ktr", "application/vnd.kahootz"],
  ["ktx", "image/ktx"],
  ["ktx2", "image/ktx2"],
  ["ktz", "application/vnd.kahootz"],
  ["kwd", "application/vnd.kde.kword"],
  ["kwt", "application/vnd.kde.kword"],
  ["lasxml", "application/vnd.las.las+xml"],
  ["latex", "application/x-latex"],
  ["lbd", "application/vnd.llamagraphics.life-balance.desktop"],
  ["lbe", "application/vnd.llamagraphics.life-balance.exchange+xml"],
  ["les", "application/vnd.hhe.lesson-player"],
  ["less", "text/less"],
  ["lgr", "application/lgr+xml"],
  ["lha", "application/octet-stream"],
  ["link66", "application/vnd.route66.link66+xml"],
  ["list", "text/plain"],
  ["list3820", "application/vnd.ibm.modcap"],
  ["listafp", "application/vnd.ibm.modcap"],
  ["litcoffee", "text/coffeescript"],
  ["lnk", "application/x-ms-shortcut"],
  ["log", "text/plain"],
  ["lostxml", "application/lost+xml"],
  ["lrf", "application/octet-stream"],
  ["lrm", "application/vnd.ms-lrm"],
  ["ltf", "application/vnd.frogans.ltf"],
  ["lua", "text/x-lua"],
  ["luac", "application/x-lua-bytecode"],
  ["lvp", "audio/vnd.lucent.voice"],
  ["lwp", "application/vnd.lotus-wordpro"],
  ["lzh", "application/octet-stream"],
  ["m1v", "video/mpeg"],
  ["m2a", "audio/mpeg"],
  ["m2v", "video/mpeg"],
  ["m3a", "audio/mpeg"],
  ["m3u", "text/plain"],
  ["m3u8", "application/vnd.apple.mpegurl"],
  ["m4a", "audio/x-m4a"],
  ["m4p", "application/mp4"],
  ["m4s", "video/iso.segment"],
  ["m4u", "application/vnd.mpegurl"],
  ["m4v", "video/x-m4v"],
  ["m13", "application/x-msmediaview"],
  ["m14", "application/x-msmediaview"],
  ["m21", "application/mp21"],
  ["ma", "application/mathematica"],
  ["mads", "application/mads+xml"],
  ["maei", "application/mmt-aei+xml"],
  ["mag", "application/vnd.ecowin.chart"],
  ["maker", "application/vnd.framemaker"],
  ["man", "text/troff"],
  ["manifest", "text/cache-manifest"],
  ["map", "application/json"],
  ["mar", "application/octet-stream"],
  ["markdown", "text/markdown"],
  ["mathml", "application/mathml+xml"],
  ["mb", "application/mathematica"],
  ["mbk", "application/vnd.mobius.mbk"],
  ["mbox", "application/mbox"],
  ["mc1", "application/vnd.medcalcdata"],
  ["mcd", "application/vnd.mcd"],
  ["mcurl", "text/vnd.curl.mcurl"],
  ["md", "text/markdown"],
  ["mdb", "application/x-msaccess"],
  ["mdi", "image/vnd.ms-modi"],
  ["mdx", "text/mdx"],
  ["me", "text/troff"],
  ["mesh", "model/mesh"],
  ["meta4", "application/metalink4+xml"],
  ["metalink", "application/metalink+xml"],
  ["mets", "application/mets+xml"],
  ["mfm", "application/vnd.mfmp"],
  ["mft", "application/rpki-manifest"],
  ["mgp", "application/vnd.osgeo.mapguide.package"],
  ["mgz", "application/vnd.proteus.magazine"],
  ["mid", "audio/midi"],
  ["midi", "audio/midi"],
  ["mie", "application/x-mie"],
  ["mif", "application/vnd.mif"],
  ["mime", "message/rfc822"],
  ["mj2", "video/mj2"],
  ["mjp2", "video/mj2"],
  ["mjs", "application/javascript"],
  ["mk3d", "video/x-matroska"],
  ["mka", "audio/x-matroska"],
  ["mkd", "text/x-markdown"],
  ["mks", "video/x-matroska"],
  ["mkv", "video/x-matroska"],
  ["mlp", "application/vnd.dolby.mlp"],
  ["mmd", "application/vnd.chipnuts.karaoke-mmd"],
  ["mmf", "application/vnd.smaf"],
  ["mml", "text/mathml"],
  ["mmr", "image/vnd.fujixerox.edmics-mmr"],
  ["mng", "video/x-mng"],
  ["mny", "application/x-msmoney"],
  ["mobi", "application/x-mobipocket-ebook"],
  ["mods", "application/mods+xml"],
  ["mov", "video/quicktime"],
  ["movie", "video/x-sgi-movie"],
  ["mp2", "audio/mpeg"],
  ["mp2a", "audio/mpeg"],
  ["mp3", "audio/mpeg"],
  ["mp4", "video/mp4"],
  ["mp4a", "audio/mp4"],
  ["mp4s", "application/mp4"],
  ["mp4v", "video/mp4"],
  ["mp21", "application/mp21"],
  ["mpc", "application/vnd.mophun.certificate"],
  ["mpd", "application/dash+xml"],
  ["mpe", "video/mpeg"],
  ["mpeg", "video/mpeg"],
  ["mpg", "video/mpeg"],
  ["mpg4", "video/mp4"],
  ["mpga", "audio/mpeg"],
  ["mpkg", "application/vnd.apple.installer+xml"],
  ["mpm", "application/vnd.blueice.multipass"],
  ["mpn", "application/vnd.mophun.application"],
  ["mpp", "application/vnd.ms-project"],
  ["mpt", "application/vnd.ms-project"],
  ["mpy", "application/vnd.ibm.minipay"],
  ["mqy", "application/vnd.mobius.mqy"],
  ["mrc", "application/marc"],
  ["mrcx", "application/marcxml+xml"],
  ["ms", "text/troff"],
  ["mscml", "application/mediaservercontrol+xml"],
  ["mseed", "application/vnd.fdsn.mseed"],
  ["mseq", "application/vnd.mseq"],
  ["msf", "application/vnd.epson.msf"],
  ["msg", "application/vnd.ms-outlook"],
  ["msh", "model/mesh"],
  ["msi", "application/x-msdownload"],
  ["msl", "application/vnd.mobius.msl"],
  ["msm", "application/octet-stream"],
  ["msp", "application/octet-stream"],
  ["msty", "application/vnd.muvee.style"],
  ["mtl", "model/mtl"],
  ["mts", "model/vnd.mts"],
  ["mus", "application/vnd.musician"],
  ["musd", "application/mmt-usd+xml"],
  ["musicxml", "application/vnd.recordare.musicxml+xml"],
  ["mvb", "application/x-msmediaview"],
  ["mvt", "application/vnd.mapbox-vector-tile"],
  ["mwf", "application/vnd.mfer"],
  ["mxf", "application/mxf"],
  ["mxl", "application/vnd.recordare.musicxml"],
  ["mxmf", "audio/mobile-xmf"],
  ["mxml", "application/xv+xml"],
  ["mxs", "application/vnd.triscape.mxs"],
  ["mxu", "video/vnd.mpegurl"],
  ["n-gage", "application/vnd.nokia.n-gage.symbian.install"],
  ["n3", "text/n3"],
  ["nb", "application/mathematica"],
  ["nbp", "application/vnd.wolfram.player"],
  ["nc", "application/x-netcdf"],
  ["ncx", "application/x-dtbncx+xml"],
  ["nfo", "text/x-nfo"],
  ["ngdat", "application/vnd.nokia.n-gage.data"],
  ["nitf", "application/vnd.nitf"],
  ["nlu", "application/vnd.neurolanguage.nlu"],
  ["nml", "application/vnd.enliven"],
  ["nnd", "application/vnd.noblenet-directory"],
  ["nns", "application/vnd.noblenet-sealer"],
  ["nnw", "application/vnd.noblenet-web"],
  ["npx", "image/vnd.net-fpx"],
  ["nq", "application/n-quads"],
  ["nsc", "application/x-conference"],
  ["nsf", "application/vnd.lotus-notes"],
  ["nt", "application/n-triples"],
  ["ntf", "application/vnd.nitf"],
  ["numbers", "application/x-iwork-numbers-sffnumbers"],
  ["nzb", "application/x-nzb"],
  ["oa2", "application/vnd.fujitsu.oasys2"],
  ["oa3", "application/vnd.fujitsu.oasys3"],
  ["oas", "application/vnd.fujitsu.oasys"],
  ["obd", "application/x-msbinder"],
  ["obgx", "application/vnd.openblox.game+xml"],
  ["obj", "model/obj"],
  ["oda", "application/oda"],
  ["odb", "application/vnd.oasis.opendocument.database"],
  ["odc", "application/vnd.oasis.opendocument.chart"],
  ["odf", "application/vnd.oasis.opendocument.formula"],
  ["odft", "application/vnd.oasis.opendocument.formula-template"],
  ["odg", "application/vnd.oasis.opendocument.graphics"],
  ["odi", "application/vnd.oasis.opendocument.image"],
  ["odm", "application/vnd.oasis.opendocument.text-master"],
  ["odp", "application/vnd.oasis.opendocument.presentation"],
  ["ods", "application/vnd.oasis.opendocument.spreadsheet"],
  ["odt", "application/vnd.oasis.opendocument.text"],
  ["oga", "audio/ogg"],
  ["ogex", "model/vnd.opengex"],
  ["ogg", "audio/ogg"],
  ["ogv", "video/ogg"],
  ["ogx", "application/ogg"],
  ["omdoc", "application/omdoc+xml"],
  ["onepkg", "application/onenote"],
  ["onetmp", "application/onenote"],
  ["onetoc", "application/onenote"],
  ["onetoc2", "application/onenote"],
  ["opf", "application/oebps-package+xml"],
  ["opml", "text/x-opml"],
  ["oprc", "application/vnd.palm"],
  ["opus", "audio/ogg"],
  ["org", "text/x-org"],
  ["osf", "application/vnd.yamaha.openscoreformat"],
  ["osfpvg", "application/vnd.yamaha.openscoreformat.osfpvg+xml"],
  ["osm", "application/vnd.openstreetmap.data+xml"],
  ["otc", "application/vnd.oasis.opendocument.chart-template"],
  ["otf", "font/otf"],
  ["otg", "application/vnd.oasis.opendocument.graphics-template"],
  ["oth", "application/vnd.oasis.opendocument.text-web"],
  ["oti", "application/vnd.oasis.opendocument.image-template"],
  ["otp", "application/vnd.oasis.opendocument.presentation-template"],
  ["ots", "application/vnd.oasis.opendocument.spreadsheet-template"],
  ["ott", "application/vnd.oasis.opendocument.text-template"],
  ["ova", "application/x-virtualbox-ova"],
  ["ovf", "application/x-virtualbox-ovf"],
  ["owl", "application/rdf+xml"],
  ["oxps", "application/oxps"],
  ["oxt", "application/vnd.openofficeorg.extension"],
  ["p", "text/x-pascal"],
  ["p7a", "application/x-pkcs7-signature"],
  ["p7b", "application/x-pkcs7-certificates"],
  ["p7c", "application/pkcs7-mime"],
  ["p7m", "application/pkcs7-mime"],
  ["p7r", "application/x-pkcs7-certreqresp"],
  ["p7s", "application/pkcs7-signature"],
  ["p8", "application/pkcs8"],
  ["p10", "application/x-pkcs10"],
  ["p12", "application/x-pkcs12"],
  ["pac", "application/x-ns-proxy-autoconfig"],
  ["pages", "application/x-iwork-pages-sffpages"],
  ["pas", "text/x-pascal"],
  ["paw", "application/vnd.pawaafile"],
  ["pbd", "application/vnd.powerbuilder6"],
  ["pbm", "image/x-portable-bitmap"],
  ["pcap", "application/vnd.tcpdump.pcap"],
  ["pcf", "application/x-font-pcf"],
  ["pcl", "application/vnd.hp-pcl"],
  ["pclxl", "application/vnd.hp-pclxl"],
  ["pct", "image/x-pict"],
  ["pcurl", "application/vnd.curl.pcurl"],
  ["pcx", "image/x-pcx"],
  ["pdb", "application/x-pilot"],
  ["pde", "text/x-processing"],
  ["pdf", "application/pdf"],
  ["pem", "application/x-x509-user-cert"],
  ["pfa", "application/x-font-type1"],
  ["pfb", "application/x-font-type1"],
  ["pfm", "application/x-font-type1"],
  ["pfr", "application/font-tdpfr"],
  ["pfx", "application/x-pkcs12"],
  ["pgm", "image/x-portable-graymap"],
  ["pgn", "application/x-chess-pgn"],
  ["pgp", "application/pgp"],
  ["php", "application/x-httpd-php"],
  ["php3", "application/x-httpd-php"],
  ["php4", "application/x-httpd-php"],
  ["phps", "application/x-httpd-php-source"],
  ["phtml", "application/x-httpd-php"],
  ["pic", "image/x-pict"],
  ["pkg", "application/octet-stream"],
  ["pki", "application/pkixcmp"],
  ["pkipath", "application/pkix-pkipath"],
  ["pkpass", "application/vnd.apple.pkpass"],
  ["pl", "application/x-perl"],
  ["plb", "application/vnd.3gpp.pic-bw-large"],
  ["plc", "application/vnd.mobius.plc"],
  ["plf", "application/vnd.pocketlearn"],
  ["pls", "application/pls+xml"],
  ["pm", "application/x-perl"],
  ["pml", "application/vnd.ctc-posml"],
  ["png", "image/png"],
  ["pnm", "image/x-portable-anymap"],
  ["portpkg", "application/vnd.macports.portpkg"],
  ["pot", "application/vnd.ms-powerpoint"],
  ["potm", "application/vnd.ms-powerpoint.presentation.macroEnabled.12"],
  ["potx", "application/vnd.openxmlformats-officedocument.presentationml.template"],
  ["ppa", "application/vnd.ms-powerpoint"],
  ["ppam", "application/vnd.ms-powerpoint.addin.macroEnabled.12"],
  ["ppd", "application/vnd.cups-ppd"],
  ["ppm", "image/x-portable-pixmap"],
  ["pps", "application/vnd.ms-powerpoint"],
  ["ppsm", "application/vnd.ms-powerpoint.slideshow.macroEnabled.12"],
  ["ppsx", "application/vnd.openxmlformats-officedocument.presentationml.slideshow"],
  ["ppt", "application/powerpoint"],
  ["pptm", "application/vnd.ms-powerpoint.presentation.macroEnabled.12"],
  ["pptx", "application/vnd.openxmlformats-officedocument.presentationml.presentation"],
  ["pqa", "application/vnd.palm"],
  ["prc", "application/x-pilot"],
  ["pre", "application/vnd.lotus-freelance"],
  ["prf", "application/pics-rules"],
  ["provx", "application/provenance+xml"],
  ["ps", "application/postscript"],
  ["psb", "application/vnd.3gpp.pic-bw-small"],
  ["psd", "application/x-photoshop"],
  ["psf", "application/x-font-linux-psf"],
  ["pskcxml", "application/pskc+xml"],
  ["pti", "image/prs.pti"],
  ["ptid", "application/vnd.pvi.ptid1"],
  ["pub", "application/x-mspublisher"],
  ["pvb", "application/vnd.3gpp.pic-bw-var"],
  ["pwn", "application/vnd.3m.post-it-notes"],
  ["pya", "audio/vnd.ms-playready.media.pya"],
  ["pyv", "video/vnd.ms-playready.media.pyv"],
  ["qam", "application/vnd.epson.quickanime"],
  ["qbo", "application/vnd.intu.qbo"],
  ["qfx", "application/vnd.intu.qfx"],
  ["qps", "application/vnd.publishare-delta-tree"],
  ["qt", "video/quicktime"],
  ["qwd", "application/vnd.quark.quarkxpress"],
  ["qwt", "application/vnd.quark.quarkxpress"],
  ["qxb", "application/vnd.quark.quarkxpress"],
  ["qxd", "application/vnd.quark.quarkxpress"],
  ["qxl", "application/vnd.quark.quarkxpress"],
  ["qxt", "application/vnd.quark.quarkxpress"],
  ["ra", "audio/x-realaudio"],
  ["ram", "audio/x-pn-realaudio"],
  ["raml", "application/raml+yaml"],
  ["rapd", "application/route-apd+xml"],
  ["rar", "application/x-rar"],
  ["ras", "image/x-cmu-raster"],
  ["rcprofile", "application/vnd.ipunplugged.rcprofile"],
  ["rdf", "application/rdf+xml"],
  ["rdz", "application/vnd.data-vision.rdz"],
  ["relo", "application/p2p-overlay+xml"],
  ["rep", "application/vnd.businessobjects"],
  ["res", "application/x-dtbresource+xml"],
  ["rgb", "image/x-rgb"],
  ["rif", "application/reginfo+xml"],
  ["rip", "audio/vnd.rip"],
  ["ris", "application/x-research-info-systems"],
  ["rl", "application/resource-lists+xml"],
  ["rlc", "image/vnd.fujixerox.edmics-rlc"],
  ["rld", "application/resource-lists-diff+xml"],
  ["rm", "audio/x-pn-realaudio"],
  ["rmi", "audio/midi"],
  ["rmp", "audio/x-pn-realaudio-plugin"],
  ["rms", "application/vnd.jcp.javame.midlet-rms"],
  ["rmvb", "application/vnd.rn-realmedia-vbr"],
  ["rnc", "application/relax-ng-compact-syntax"],
  ["rng", "application/xml"],
  ["roa", "application/rpki-roa"],
  ["roff", "text/troff"],
  ["rp9", "application/vnd.cloanto.rp9"],
  ["rpm", "audio/x-pn-realaudio-plugin"],
  ["rpss", "application/vnd.nokia.radio-presets"],
  ["rpst", "application/vnd.nokia.radio-preset"],
  ["rq", "application/sparql-query"],
  ["rs", "application/rls-services+xml"],
  ["rsa", "application/x-pkcs7"],
  ["rsat", "application/atsc-rsat+xml"],
  ["rsd", "application/rsd+xml"],
  ["rsheet", "application/urc-ressheet+xml"],
  ["rss", "application/rss+xml"],
  ["rtf", "text/rtf"],
  ["rtx", "text/richtext"],
  ["run", "application/x-makeself"],
  ["rusd", "application/route-usd+xml"],
  ["rv", "video/vnd.rn-realvideo"],
  ["s", "text/x-asm"],
  ["s3m", "audio/s3m"],
  ["saf", "application/vnd.yamaha.smaf-audio"],
  ["sass", "text/x-sass"],
  ["sbml", "application/sbml+xml"],
  ["sc", "application/vnd.ibm.secure-container"],
  ["scd", "application/x-msschedule"],
  ["scm", "application/vnd.lotus-screencam"],
  ["scq", "application/scvp-cv-request"],
  ["scs", "application/scvp-cv-response"],
  ["scss", "text/x-scss"],
  ["scurl", "text/vnd.curl.scurl"],
  ["sda", "application/vnd.stardivision.draw"],
  ["sdc", "application/vnd.stardivision.calc"],
  ["sdd", "application/vnd.stardivision.impress"],
  ["sdkd", "application/vnd.solent.sdkm+xml"],
  ["sdkm", "application/vnd.solent.sdkm+xml"],
  ["sdp", "application/sdp"],
  ["sdw", "application/vnd.stardivision.writer"],
  ["sea", "application/octet-stream"],
  ["see", "application/vnd.seemail"],
  ["seed", "application/vnd.fdsn.seed"],
  ["sema", "application/vnd.sema"],
  ["semd", "application/vnd.semd"],
  ["semf", "application/vnd.semf"],
  ["senmlx", "application/senml+xml"],
  ["sensmlx", "application/sensml+xml"],
  ["ser", "application/java-serialized-object"],
  ["setpay", "application/set-payment-initiation"],
  ["setreg", "application/set-registration-initiation"],
  ["sfd-hdstx", "application/vnd.hydrostatix.sof-data"],
  ["sfs", "application/vnd.spotfire.sfs"],
  ["sfv", "text/x-sfv"],
  ["sgi", "image/sgi"],
  ["sgl", "application/vnd.stardivision.writer-global"],
  ["sgm", "text/sgml"],
  ["sgml", "text/sgml"],
  ["sh", "application/x-sh"],
  ["shar", "application/x-shar"],
  ["shex", "text/shex"],
  ["shf", "application/shf+xml"],
  ["shtml", "text/html"],
  ["sid", "image/x-mrsid-image"],
  ["sieve", "application/sieve"],
  ["sig", "application/pgp-signature"],
  ["sil", "audio/silk"],
  ["silo", "model/mesh"],
  ["sis", "application/vnd.symbian.install"],
  ["sisx", "application/vnd.symbian.install"],
  ["sit", "application/x-stuffit"],
  ["sitx", "application/x-stuffitx"],
  ["siv", "application/sieve"],
  ["skd", "application/vnd.koan"],
  ["skm", "application/vnd.koan"],
  ["skp", "application/vnd.koan"],
  ["skt", "application/vnd.koan"],
  ["sldm", "application/vnd.ms-powerpoint.slide.macroenabled.12"],
  ["sldx", "application/vnd.openxmlformats-officedocument.presentationml.slide"],
  ["slim", "text/slim"],
  ["slm", "text/slim"],
  ["sls", "application/route-s-tsid+xml"],
  ["slt", "application/vnd.epson.salt"],
  ["sm", "application/vnd.stepmania.stepchart"],
  ["smf", "application/vnd.stardivision.math"],
  ["smi", "application/smil"],
  ["smil", "application/smil"],
  ["smv", "video/x-smv"],
  ["smzip", "application/vnd.stepmania.package"],
  ["snd", "audio/basic"],
  ["snf", "application/x-font-snf"],
  ["so", "application/octet-stream"],
  ["spc", "application/x-pkcs7-certificates"],
  ["spdx", "text/spdx"],
  ["spf", "application/vnd.yamaha.smaf-phrase"],
  ["spl", "application/x-futuresplash"],
  ["spot", "text/vnd.in3d.spot"],
  ["spp", "application/scvp-vp-response"],
  ["spq", "application/scvp-vp-request"],
  ["spx", "audio/ogg"],
  ["sql", "application/x-sql"],
  ["src", "application/x-wais-source"],
  ["srt", "application/x-subrip"],
  ["sru", "application/sru+xml"],
  ["srx", "application/sparql-results+xml"],
  ["ssdl", "application/ssdl+xml"],
  ["sse", "application/vnd.kodak-descriptor"],
  ["ssf", "application/vnd.epson.ssf"],
  ["ssml", "application/ssml+xml"],
  ["sst", "application/octet-stream"],
  ["st", "application/vnd.sailingtracker.track"],
  ["stc", "application/vnd.sun.xml.calc.template"],
  ["std", "application/vnd.sun.xml.draw.template"],
  ["stf", "application/vnd.wt.stf"],
  ["sti", "application/vnd.sun.xml.impress.template"],
  ["stk", "application/hyperstudio"],
  ["stl", "model/stl"],
  ["stpx", "model/step+xml"],
  ["stpxz", "model/step-xml+zip"],
  ["stpz", "model/step+zip"],
  ["str", "application/vnd.pg.format"],
  ["stw", "application/vnd.sun.xml.writer.template"],
  ["styl", "text/stylus"],
  ["stylus", "text/stylus"],
  ["sub", "text/vnd.dvb.subtitle"],
  ["sus", "application/vnd.sus-calendar"],
  ["susp", "application/vnd.sus-calendar"],
  ["sv4cpio", "application/x-sv4cpio"],
  ["sv4crc", "application/x-sv4crc"],
  ["svc", "application/vnd.dvb.service"],
  ["svd", "application/vnd.svd"],
  ["svg", "image/svg+xml"],
  ["svgz", "image/svg+xml"],
  ["swa", "application/x-director"],
  ["swf", "application/x-shockwave-flash"],
  ["swi", "application/vnd.aristanetworks.swi"],
  ["swidtag", "application/swid+xml"],
  ["sxc", "application/vnd.sun.xml.calc"],
  ["sxd", "application/vnd.sun.xml.draw"],
  ["sxg", "application/vnd.sun.xml.writer.global"],
  ["sxi", "application/vnd.sun.xml.impress"],
  ["sxm", "application/vnd.sun.xml.math"],
  ["sxw", "application/vnd.sun.xml.writer"],
  ["t", "text/troff"],
  ["t3", "application/x-t3vm-image"],
  ["t38", "image/t38"],
  ["taglet", "application/vnd.mynfc"],
  ["tao", "application/vnd.tao.intent-module-archive"],
  ["tap", "image/vnd.tencent.tap"],
  ["tar", "application/x-tar"],
  ["tcap", "application/vnd.3gpp2.tcap"],
  ["tcl", "application/x-tcl"],
  ["td", "application/urc-targetdesc+xml"],
  ["teacher", "application/vnd.smart.teacher"],
  ["tei", "application/tei+xml"],
  ["teicorpus", "application/tei+xml"],
  ["tex", "application/x-tex"],
  ["texi", "application/x-texinfo"],
  ["texinfo", "application/x-texinfo"],
  ["text", "text/plain"],
  ["tfi", "application/thraud+xml"],
  ["tfm", "application/x-tex-tfm"],
  ["tfx", "image/tiff-fx"],
  ["tga", "image/x-tga"],
  ["tgz", "application/x-tar"],
  ["thmx", "application/vnd.ms-officetheme"],
  ["tif", "image/tiff"],
  ["tiff", "image/tiff"],
  ["tk", "application/x-tcl"],
  ["tmo", "application/vnd.tmobile-livetv"],
  ["toml", "application/toml"],
  ["torrent", "application/x-bittorrent"],
  ["tpl", "application/vnd.groove-tool-template"],
  ["tpt", "application/vnd.trid.tpt"],
  ["tr", "text/troff"],
  ["tra", "application/vnd.trueapp"],
  ["trig", "application/trig"],
  ["trm", "application/x-msterminal"],
  ["ts", "video/mp2t"],
  ["tsd", "application/timestamped-data"],
  ["tsv", "text/tab-separated-values"],
  ["ttc", "font/collection"],
  ["ttf", "font/ttf"],
  ["ttl", "text/turtle"],
  ["ttml", "application/ttml+xml"],
  ["twd", "application/vnd.simtech-mindmapper"],
  ["twds", "application/vnd.simtech-mindmapper"],
  ["txd", "application/vnd.genomatix.tuxedo"],
  ["txf", "application/vnd.mobius.txf"],
  ["txt", "text/plain"],
  ["u8dsn", "message/global-delivery-status"],
  ["u8hdr", "message/global-headers"],
  ["u8mdn", "message/global-disposition-notification"],
  ["u8msg", "message/global"],
  ["u32", "application/x-authorware-bin"],
  ["ubj", "application/ubjson"],
  ["udeb", "application/x-debian-package"],
  ["ufd", "application/vnd.ufdl"],
  ["ufdl", "application/vnd.ufdl"],
  ["ulx", "application/x-glulx"],
  ["umj", "application/vnd.umajin"],
  ["unityweb", "application/vnd.unity"],
  ["uoml", "application/vnd.uoml+xml"],
  ["uri", "text/uri-list"],
  ["uris", "text/uri-list"],
  ["urls", "text/uri-list"],
  ["usdz", "model/vnd.usdz+zip"],
  ["ustar", "application/x-ustar"],
  ["utz", "application/vnd.uiq.theme"],
  ["uu", "text/x-uuencode"],
  ["uva", "audio/vnd.dece.audio"],
  ["uvd", "application/vnd.dece.data"],
  ["uvf", "application/vnd.dece.data"],
  ["uvg", "image/vnd.dece.graphic"],
  ["uvh", "video/vnd.dece.hd"],
  ["uvi", "image/vnd.dece.graphic"],
  ["uvm", "video/vnd.dece.mobile"],
  ["uvp", "video/vnd.dece.pd"],
  ["uvs", "video/vnd.dece.sd"],
  ["uvt", "application/vnd.dece.ttml+xml"],
  ["uvu", "video/vnd.uvvu.mp4"],
  ["uvv", "video/vnd.dece.video"],
  ["uvva", "audio/vnd.dece.audio"],
  ["uvvd", "application/vnd.dece.data"],
  ["uvvf", "application/vnd.dece.data"],
  ["uvvg", "image/vnd.dece.graphic"],
  ["uvvh", "video/vnd.dece.hd"],
  ["uvvi", "image/vnd.dece.graphic"],
  ["uvvm", "video/vnd.dece.mobile"],
  ["uvvp", "video/vnd.dece.pd"],
  ["uvvs", "video/vnd.dece.sd"],
  ["uvvt", "application/vnd.dece.ttml+xml"],
  ["uvvu", "video/vnd.uvvu.mp4"],
  ["uvvv", "video/vnd.dece.video"],
  ["uvvx", "application/vnd.dece.unspecified"],
  ["uvvz", "application/vnd.dece.zip"],
  ["uvx", "application/vnd.dece.unspecified"],
  ["uvz", "application/vnd.dece.zip"],
  ["vbox", "application/x-virtualbox-vbox"],
  ["vbox-extpack", "application/x-virtualbox-vbox-extpack"],
  ["vcard", "text/vcard"],
  ["vcd", "application/x-cdlink"],
  ["vcf", "text/x-vcard"],
  ["vcg", "application/vnd.groove-vcard"],
  ["vcs", "text/x-vcalendar"],
  ["vcx", "application/vnd.vcx"],
  ["vdi", "application/x-virtualbox-vdi"],
  ["vds", "model/vnd.sap.vds"],
  ["vhd", "application/x-virtualbox-vhd"],
  ["vis", "application/vnd.visionary"],
  ["viv", "video/vnd.vivo"],
  ["vlc", "application/videolan"],
  ["vmdk", "application/x-virtualbox-vmdk"],
  ["vob", "video/x-ms-vob"],
  ["vor", "application/vnd.stardivision.writer"],
  ["vox", "application/x-authorware-bin"],
  ["vrml", "model/vrml"],
  ["vsd", "application/vnd.visio"],
  ["vsf", "application/vnd.vsf"],
  ["vss", "application/vnd.visio"],
  ["vst", "application/vnd.visio"],
  ["vsw", "application/vnd.visio"],
  ["vtf", "image/vnd.valve.source.texture"],
  ["vtt", "text/vtt"],
  ["vtu", "model/vnd.vtu"],
  ["vxml", "application/voicexml+xml"],
  ["w3d", "application/x-director"],
  ["wad", "application/x-doom"],
  ["wadl", "application/vnd.sun.wadl+xml"],
  ["war", "application/java-archive"],
  ["wasm", "application/wasm"],
  ["wav", "audio/x-wav"],
  ["wax", "audio/x-ms-wax"],
  ["wbmp", "image/vnd.wap.wbmp"],
  ["wbs", "application/vnd.criticaltools.wbs+xml"],
  ["wbxml", "application/wbxml"],
  ["wcm", "application/vnd.ms-works"],
  ["wdb", "application/vnd.ms-works"],
  ["wdp", "image/vnd.ms-photo"],
  ["weba", "audio/webm"],
  ["webapp", "application/x-web-app-manifest+json"],
  ["webm", "video/webm"],
  ["webmanifest", "application/manifest+json"],
  ["webp", "image/webp"],
  ["wg", "application/vnd.pmi.widget"],
  ["wgt", "application/widget"],
  ["wks", "application/vnd.ms-works"],
  ["wm", "video/x-ms-wm"],
  ["wma", "audio/x-ms-wma"],
  ["wmd", "application/x-ms-wmd"],
  ["wmf", "image/wmf"],
  ["wml", "text/vnd.wap.wml"],
  ["wmlc", "application/wmlc"],
  ["wmls", "text/vnd.wap.wmlscript"],
  ["wmlsc", "application/vnd.wap.wmlscriptc"],
  ["wmv", "video/x-ms-wmv"],
  ["wmx", "video/x-ms-wmx"],
  ["wmz", "application/x-msmetafile"],
  ["woff", "font/woff"],
  ["woff2", "font/woff2"],
  ["word", "application/msword"],
  ["wpd", "application/vnd.wordperfect"],
  ["wpl", "application/vnd.ms-wpl"],
  ["wps", "application/vnd.ms-works"],
  ["wqd", "application/vnd.wqd"],
  ["wri", "application/x-mswrite"],
  ["wrl", "model/vrml"],
  ["wsc", "message/vnd.wfa.wsc"],
  ["wsdl", "application/wsdl+xml"],
  ["wspolicy", "application/wspolicy+xml"],
  ["wtb", "application/vnd.webturbo"],
  ["wvx", "video/x-ms-wvx"],
  ["x3d", "model/x3d+xml"],
  ["x3db", "model/x3d+fastinfoset"],
  ["x3dbz", "model/x3d+binary"],
  ["x3dv", "model/x3d-vrml"],
  ["x3dvz", "model/x3d+vrml"],
  ["x3dz", "model/x3d+xml"],
  ["x32", "application/x-authorware-bin"],
  ["x_b", "model/vnd.parasolid.transmit.binary"],
  ["x_t", "model/vnd.parasolid.transmit.text"],
  ["xaml", "application/xaml+xml"],
  ["xap", "application/x-silverlight-app"],
  ["xar", "application/vnd.xara"],
  ["xav", "application/xcap-att+xml"],
  ["xbap", "application/x-ms-xbap"],
  ["xbd", "application/vnd.fujixerox.docuworks.binder"],
  ["xbm", "image/x-xbitmap"],
  ["xca", "application/xcap-caps+xml"],
  ["xcs", "application/calendar+xml"],
  ["xdf", "application/xcap-diff+xml"],
  ["xdm", "application/vnd.syncml.dm+xml"],
  ["xdp", "application/vnd.adobe.xdp+xml"],
  ["xdssc", "application/dssc+xml"],
  ["xdw", "application/vnd.fujixerox.docuworks"],
  ["xel", "application/xcap-el+xml"],
  ["xenc", "application/xenc+xml"],
  ["xer", "application/patch-ops-error+xml"],
  ["xfdf", "application/vnd.adobe.xfdf"],
  ["xfdl", "application/vnd.xfdl"],
  ["xht", "application/xhtml+xml"],
  ["xhtml", "application/xhtml+xml"],
  ["xhvml", "application/xv+xml"],
  ["xif", "image/vnd.xiff"],
  ["xl", "application/excel"],
  ["xla", "application/vnd.ms-excel"],
  ["xlam", "application/vnd.ms-excel.addin.macroEnabled.12"],
  ["xlc", "application/vnd.ms-excel"],
  ["xlf", "application/xliff+xml"],
  ["xlm", "application/vnd.ms-excel"],
  ["xls", "application/vnd.ms-excel"],
  ["xlsb", "application/vnd.ms-excel.sheet.binary.macroEnabled.12"],
  ["xlsm", "application/vnd.ms-excel.sheet.macroEnabled.12"],
  ["xlsx", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"],
  ["xlt", "application/vnd.ms-excel"],
  ["xltm", "application/vnd.ms-excel.template.macroEnabled.12"],
  ["xltx", "application/vnd.openxmlformats-officedocument.spreadsheetml.template"],
  ["xlw", "application/vnd.ms-excel"],
  ["xm", "audio/xm"],
  ["xml", "application/xml"],
  ["xns", "application/xcap-ns+xml"],
  ["xo", "application/vnd.olpc-sugar"],
  ["xop", "application/xop+xml"],
  ["xpi", "application/x-xpinstall"],
  ["xpl", "application/xproc+xml"],
  ["xpm", "image/x-xpixmap"],
  ["xpr", "application/vnd.is-xpr"],
  ["xps", "application/vnd.ms-xpsdocument"],
  ["xpw", "application/vnd.intercon.formnet"],
  ["xpx", "application/vnd.intercon.formnet"],
  ["xsd", "application/xml"],
  ["xsl", "application/xml"],
  ["xslt", "application/xslt+xml"],
  ["xsm", "application/vnd.syncml+xml"],
  ["xspf", "application/xspf+xml"],
  ["xul", "application/vnd.mozilla.xul+xml"],
  ["xvm", "application/xv+xml"],
  ["xvml", "application/xv+xml"],
  ["xwd", "image/x-xwindowdump"],
  ["xyz", "chemical/x-xyz"],
  ["xz", "application/x-xz"],
  ["yaml", "text/yaml"],
  ["yang", "application/yang"],
  ["yin", "application/yin+xml"],
  ["yml", "text/yaml"],
  ["ymp", "text/x-suse-ymp"],
  ["z", "application/x-compress"],
  ["z1", "application/x-zmachine"],
  ["z2", "application/x-zmachine"],
  ["z3", "application/x-zmachine"],
  ["z4", "application/x-zmachine"],
  ["z5", "application/x-zmachine"],
  ["z6", "application/x-zmachine"],
  ["z7", "application/x-zmachine"],
  ["z8", "application/x-zmachine"],
  ["zaz", "application/vnd.zzazz.deck+xml"],
  ["zip", "application/zip"],
  ["zir", "application/vnd.zul"],
  ["zirz", "application/vnd.zul"],
  ["zmm", "application/vnd.handheld-entertainment+xml"],
  ["zsh", "text/x-scriptzsh"]
]);
function toFileWithPath(file, path2, h2) {
  const f2 = withMimeType(file);
  const { webkitRelativePath } = file;
  const p2 = typeof path2 === "string" ? path2 : typeof webkitRelativePath === "string" && webkitRelativePath.length > 0 ? webkitRelativePath : `./${file.name}`;
  if (typeof f2.path !== "string") {
    setObjProp(f2, "path", p2);
  }
  if (h2 !== void 0) {
    Object.defineProperty(f2, "handle", {
      value: h2,
      writable: false,
      configurable: false,
      enumerable: true
    });
  }
  setObjProp(f2, "relativePath", p2);
  return f2;
}
function withMimeType(file) {
  const { name: name2 } = file;
  const hasExtension = name2 && name2.lastIndexOf(".") !== -1;
  if (hasExtension && !file.type) {
    const ext = name2.split(".").pop().toLowerCase();
    const type = COMMON_MIME_TYPES.get(ext);
    if (type) {
      Object.defineProperty(file, "type", {
        value: type,
        writable: false,
        configurable: false,
        enumerable: true
      });
    }
  }
  return file;
}
function setObjProp(f2, key, value) {
  Object.defineProperty(f2, key, {
    value,
    writable: false,
    configurable: false,
    enumerable: true
  });
}

// node_modules/file-selector/dist/es2015/file-selector.js
var FILES_TO_IGNORE = [
  // Thumbnail cache files for macOS and Windows
  ".DS_Store",
  // macOs
  "Thumbs.db"
  // Windows
];
function fromEvent(evt) {
  return __awaiter(this, void 0, void 0, function* () {
    if (isObject(evt) && isDataTransfer(evt.dataTransfer)) {
      return getDataTransferFiles(evt.dataTransfer, evt.type);
    } else if (isChangeEvt(evt)) {
      return getInputFiles(evt);
    } else if (Array.isArray(evt) && evt.every((item) => "getFile" in item && typeof item.getFile === "function")) {
      return getFsHandleFiles(evt);
    }
    return [];
  });
}
function isDataTransfer(value) {
  return isObject(value);
}
function isChangeEvt(value) {
  return isObject(value) && isObject(value.target);
}
function isObject(v2) {
  return typeof v2 === "object" && v2 !== null;
}
function getInputFiles(evt) {
  return fromList(evt.target.files).map((file) => toFileWithPath(file));
}
function getFsHandleFiles(handles) {
  return __awaiter(this, void 0, void 0, function* () {
    const files = yield Promise.all(handles.map((h2) => h2.getFile()));
    return files.map((file) => toFileWithPath(file));
  });
}
function getDataTransferFiles(dt2, type) {
  return __awaiter(this, void 0, void 0, function* () {
    if (dt2.items) {
      const items = fromList(dt2.items).filter((item) => item.kind === "file");
      if (type !== "drop") {
        return items;
      }
      const files = yield Promise.all(items.map(toFilePromises));
      return noIgnoredFiles(flatten(files));
    }
    return noIgnoredFiles(fromList(dt2.files).map((file) => toFileWithPath(file)));
  });
}
function noIgnoredFiles(files) {
  return files.filter((file) => FILES_TO_IGNORE.indexOf(file.name) === -1);
}
function fromList(items) {
  if (items === null) {
    return [];
  }
  const files = [];
  for (let i2 = 0; i2 < items.length; i2++) {
    const file = items[i2];
    files.push(file);
  }
  return files;
}
function toFilePromises(item) {
  if (typeof item.webkitGetAsEntry !== "function") {
    return fromDataTransferItem(item);
  }
  const entry = item.webkitGetAsEntry();
  if (entry && entry.isDirectory) {
    return fromDirEntry(entry);
  }
  return fromDataTransferItem(item, entry);
}
function flatten(items) {
  return items.reduce((acc, files) => [
    ...acc,
    ...Array.isArray(files) ? flatten(files) : [files]
  ], []);
}
function fromDataTransferItem(item, entry) {
  return __awaiter(this, void 0, void 0, function* () {
    var _a;
    if (globalThis.isSecureContext && typeof item.getAsFileSystemHandle === "function") {
      const h2 = yield item.getAsFileSystemHandle();
      if (h2 === null) {
        throw new Error(`${item} is not a File`);
      }
      if (h2 !== void 0) {
        const file2 = yield h2.getFile();
        file2.handle = h2;
        return toFileWithPath(file2);
      }
    }
    const file = item.getAsFile();
    if (!file) {
      throw new Error(`${item} is not a File`);
    }
    const fwp = toFileWithPath(file, (_a = entry === null || entry === void 0 ? void 0 : entry.fullPath) !== null && _a !== void 0 ? _a : void 0);
    return fwp;
  });
}
function fromEntry(entry) {
  return __awaiter(this, void 0, void 0, function* () {
    return entry.isDirectory ? fromDirEntry(entry) : fromFileEntry(entry);
  });
}
function fromDirEntry(entry) {
  const reader = entry.createReader();
  return new Promise((resolve, reject) => {
    const entries = [];
    function readEntries() {
      reader.readEntries((batch) => __awaiter(this, void 0, void 0, function* () {
        if (!batch.length) {
          try {
            const files = yield Promise.all(entries);
            resolve(files);
          } catch (err) {
            reject(err);
          }
        } else {
          const items = Promise.all(batch.map(fromEntry));
          entries.push(items);
          readEntries();
        }
      }), (err) => {
        reject(err);
      });
    }
    readEntries();
  });
}
function fromFileEntry(entry) {
  return __awaiter(this, void 0, void 0, function* () {
    return new Promise((resolve, reject) => {
      entry.file((file) => {
        const fwp = toFileWithPath(file, entry.fullPath);
        resolve(fwp);
      }, (err) => {
        reject(err);
      });
    });
  });
}

// node_modules/react-dropzone/dist/es/utils/index.js
var import_attr_accept = __toESM(require_es(), 1);
function _toConsumableArray(arr) {
  return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread();
}
function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _iterableToArray(iter) {
  if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter);
}
function _arrayWithoutHoles(arr) {
  if (Array.isArray(arr)) return _arrayLikeToArray(arr);
}
function ownKeys(object, enumerableOnly) {
  var keys3 = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys3.push.apply(keys3, symbols);
  }
  return keys3;
}
function _objectSpread(target) {
  for (var i2 = 1; i2 < arguments.length; i2++) {
    var source = null != arguments[i2] ? arguments[i2] : {};
    i2 % 2 ? ownKeys(Object(source), true).forEach(function(key) {
      _defineProperty(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
  } else {
    obj[key] = value;
  }
  return obj;
}
function _slicedToArray(arr, i2) {
  return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i2) || _unsupportedIterableToArray(arr, i2) || _nonIterableRest();
}
function _nonIterableRest() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray(o2, minLen) {
  if (!o2) return;
  if (typeof o2 === "string") return _arrayLikeToArray(o2, minLen);
  var n2 = Object.prototype.toString.call(o2).slice(8, -1);
  if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
  if (n2 === "Map" || n2 === "Set") return Array.from(o2);
  if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray(o2, minLen);
}
function _arrayLikeToArray(arr, len) {
  if (len == null || len > arr.length) len = arr.length;
  for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) {
    arr2[i2] = arr[i2];
  }
  return arr2;
}
function _iterableToArrayLimit(arr, i2) {
  var _i = arr == null ? null : typeof Symbol !== "undefined" && arr[Symbol.iterator] || arr["@@iterator"];
  if (_i == null) return;
  var _arr = [];
  var _n = true;
  var _d = false;
  var _s, _e2;
  try {
    for (_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true) {
      _arr.push(_s.value);
      if (i2 && _arr.length === i2) break;
    }
  } catch (err) {
    _d = true;
    _e2 = err;
  } finally {
    try {
      if (!_n && _i["return"] != null) _i["return"]();
    } finally {
      if (_d) throw _e2;
    }
  }
  return _arr;
}
function _arrayWithHoles(arr) {
  if (Array.isArray(arr)) return arr;
}
var accepts = typeof import_attr_accept.default === "function" ? import_attr_accept.default : import_attr_accept.default.default;
var FILE_INVALID_TYPE = "file-invalid-type";
var FILE_TOO_LARGE = "file-too-large";
var FILE_TOO_SMALL = "file-too-small";
var TOO_MANY_FILES = "too-many-files";
var getInvalidTypeRejectionErr = function getInvalidTypeRejectionErr2() {
  var accept = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "";
  var acceptArr = accept.split(",");
  var msg = acceptArr.length > 1 ? "one of ".concat(acceptArr.join(", ")) : acceptArr[0];
  return {
    code: FILE_INVALID_TYPE,
    message: "File type must be ".concat(msg)
  };
};
var getTooLargeRejectionErr = function getTooLargeRejectionErr2(maxSize) {
  return {
    code: FILE_TOO_LARGE,
    message: "File is larger than ".concat(maxSize, " ").concat(maxSize === 1 ? "byte" : "bytes")
  };
};
var getTooSmallRejectionErr = function getTooSmallRejectionErr2(minSize) {
  return {
    code: FILE_TOO_SMALL,
    message: "File is smaller than ".concat(minSize, " ").concat(minSize === 1 ? "byte" : "bytes")
  };
};
var TOO_MANY_FILES_REJECTION = {
  code: TOO_MANY_FILES,
  message: "Too many files"
};
function isDataTransferItemWithEmptyType(file) {
  return file.type === "" && typeof file.getAsFile === "function";
}
function fileAccepted(file, accept) {
  var isAcceptable = file.type === "application/x-moz-file" || accepts(file, accept) || isDataTransferItemWithEmptyType(file);
  return [isAcceptable, isAcceptable ? null : getInvalidTypeRejectionErr(accept)];
}
function fileMatchSize(file, minSize, maxSize) {
  if (isDefined(file.size)) {
    if (isDefined(minSize) && isDefined(maxSize)) {
      if (file.size > maxSize) return [false, getTooLargeRejectionErr(maxSize)];
      if (file.size < minSize) return [false, getTooSmallRejectionErr(minSize)];
    } else if (isDefined(minSize) && file.size < minSize) return [false, getTooSmallRejectionErr(minSize)];
    else if (isDefined(maxSize) && file.size > maxSize) return [false, getTooLargeRejectionErr(maxSize)];
  }
  return [true, null];
}
function isDefined(value) {
  return value !== void 0 && value !== null;
}
function allFilesAccepted(_ref) {
  var files = _ref.files, accept = _ref.accept, minSize = _ref.minSize, maxSize = _ref.maxSize, multiple = _ref.multiple, maxFiles = _ref.maxFiles, validator = _ref.validator;
  if (!multiple && files.length > 1 || multiple && maxFiles >= 1 && files.length > maxFiles) {
    return false;
  }
  return files.every(function(file) {
    var _fileAccepted = fileAccepted(file, accept), _fileAccepted2 = _slicedToArray(_fileAccepted, 1), accepted = _fileAccepted2[0];
    var _fileMatchSize = fileMatchSize(file, minSize, maxSize), _fileMatchSize2 = _slicedToArray(_fileMatchSize, 1), sizeMatch = _fileMatchSize2[0];
    var customErrors = validator ? validator(file) : null;
    return accepted && sizeMatch && !customErrors;
  });
}
function isPropagationStopped(event) {
  if (typeof event.isPropagationStopped === "function") {
    return event.isPropagationStopped();
  } else if (typeof event.cancelBubble !== "undefined") {
    return event.cancelBubble;
  }
  return false;
}
function isEvtWithFiles(event) {
  if (!event.dataTransfer) {
    return !!event.target && !!event.target.files;
  }
  return Array.prototype.some.call(event.dataTransfer.types, function(type) {
    return type === "Files" || type === "application/x-moz-file";
  });
}
function onDocumentDragOver(event) {
  event.preventDefault();
}
function isIe(userAgent) {
  return userAgent.indexOf("MSIE") !== -1 || userAgent.indexOf("Trident/") !== -1;
}
function isEdge(userAgent) {
  return userAgent.indexOf("Edge/") !== -1;
}
function isIeOrEdge() {
  var userAgent = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : window.navigator.userAgent;
  return isIe(userAgent) || isEdge(userAgent);
}
function composeEventHandlers() {
  for (var _len = arguments.length, fns = new Array(_len), _key = 0; _key < _len; _key++) {
    fns[_key] = arguments[_key];
  }
  return function(event) {
    for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      args[_key2 - 1] = arguments[_key2];
    }
    return fns.some(function(fn) {
      if (!isPropagationStopped(event) && fn) {
        fn.apply(void 0, [event].concat(args));
      }
      return isPropagationStopped(event);
    });
  };
}
function canUseFileSystemAccessAPI() {
  return "showOpenFilePicker" in window;
}
function pickerOptionsFromAccept(accept) {
  if (isDefined(accept)) {
    var acceptForPicker = Object.entries(accept).filter(function(_ref2) {
      var _ref3 = _slicedToArray(_ref2, 2), mimeType = _ref3[0], ext = _ref3[1];
      var ok3 = true;
      if (!isMIMEType(mimeType)) {
        console.warn('Skipped "'.concat(mimeType, '" because it is not a valid MIME type. Check https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types/Common_types for a list of valid MIME types.'));
        ok3 = false;
      }
      if (!Array.isArray(ext) || !ext.every(isExt)) {
        console.warn('Skipped "'.concat(mimeType, '" because an invalid file extension was provided.'));
        ok3 = false;
      }
      return ok3;
    }).reduce(function(agg, _ref4) {
      var _ref5 = _slicedToArray(_ref4, 2), mimeType = _ref5[0], ext = _ref5[1];
      return _objectSpread(_objectSpread({}, agg), {}, _defineProperty({}, mimeType, ext));
    }, {});
    return [{
      // description is required due to https://crbug.com/1264708
      description: "Files",
      accept: acceptForPicker
    }];
  }
  return accept;
}
function acceptPropAsAcceptAttr(accept) {
  if (isDefined(accept)) {
    return Object.entries(accept).reduce(function(a2, _ref6) {
      var _ref7 = _slicedToArray(_ref6, 2), mimeType = _ref7[0], ext = _ref7[1];
      return [].concat(_toConsumableArray(a2), [mimeType], _toConsumableArray(ext));
    }, []).filter(function(v2) {
      return isMIMEType(v2) || isExt(v2);
    }).join(",");
  }
  return void 0;
}
function isAbort(v2) {
  return v2 instanceof DOMException && (v2.name === "AbortError" || v2.code === v2.ABORT_ERR);
}
function isSecurityError(v2) {
  return v2 instanceof DOMException && (v2.name === "SecurityError" || v2.code === v2.SECURITY_ERR);
}
function isMIMEType(v2) {
  return v2 === "audio/*" || v2 === "video/*" || v2 === "image/*" || v2 === "text/*" || v2 === "application/*" || /\w+\/[-+.\w]+/g.test(v2);
}
function isExt(v2) {
  return /^.*\.[\w]+$/.test(v2);
}

// node_modules/react-dropzone/dist/es/index.js
var _excluded = ["children"];
var _excluded2 = ["open"];
var _excluded3 = ["refKey", "role", "onKeyDown", "onFocus", "onBlur", "onClick", "onDragEnter", "onDragOver", "onDragLeave", "onDrop"];
var _excluded4 = ["refKey", "onChange", "onClick"];
function _toConsumableArray2(arr) {
  return _arrayWithoutHoles2(arr) || _iterableToArray2(arr) || _unsupportedIterableToArray2(arr) || _nonIterableSpread2();
}
function _nonIterableSpread2() {
  throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _iterableToArray2(iter) {
  if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter);
}
function _arrayWithoutHoles2(arr) {
  if (Array.isArray(arr)) return _arrayLikeToArray2(arr);
}
function _slicedToArray2(arr, i2) {
  return _arrayWithHoles2(arr) || _iterableToArrayLimit2(arr, i2) || _unsupportedIterableToArray2(arr, i2) || _nonIterableRest2();
}
function _nonIterableRest2() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray2(o2, minLen) {
  if (!o2) return;
  if (typeof o2 === "string") return _arrayLikeToArray2(o2, minLen);
  var n2 = Object.prototype.toString.call(o2).slice(8, -1);
  if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
  if (n2 === "Map" || n2 === "Set") return Array.from(o2);
  if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray2(o2, minLen);
}
function _arrayLikeToArray2(arr, len) {
  if (len == null || len > arr.length) len = arr.length;
  for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) {
    arr2[i2] = arr[i2];
  }
  return arr2;
}
function _iterableToArrayLimit2(arr, i2) {
  var _i = arr == null ? null : typeof Symbol !== "undefined" && arr[Symbol.iterator] || arr["@@iterator"];
  if (_i == null) return;
  var _arr = [];
  var _n = true;
  var _d = false;
  var _s, _e2;
  try {
    for (_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true) {
      _arr.push(_s.value);
      if (i2 && _arr.length === i2) break;
    }
  } catch (err) {
    _d = true;
    _e2 = err;
  } finally {
    try {
      if (!_n && _i["return"] != null) _i["return"]();
    } finally {
      if (_d) throw _e2;
    }
  }
  return _arr;
}
function _arrayWithHoles2(arr) {
  if (Array.isArray(arr)) return arr;
}
function ownKeys2(object, enumerableOnly) {
  var keys3 = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys3.push.apply(keys3, symbols);
  }
  return keys3;
}
function _objectSpread2(target) {
  for (var i2 = 1; i2 < arguments.length; i2++) {
    var source = null != arguments[i2] ? arguments[i2] : {};
    i2 % 2 ? ownKeys2(Object(source), true).forEach(function(key) {
      _defineProperty2(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys2(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
function _defineProperty2(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
  } else {
    obj[key] = value;
  }
  return obj;
}
function _objectWithoutProperties(source, excluded) {
  if (source == null) return {};
  var target = _objectWithoutPropertiesLoose(source, excluded);
  var key, i2;
  if (Object.getOwnPropertySymbols) {
    var sourceSymbolKeys = Object.getOwnPropertySymbols(source);
    for (i2 = 0; i2 < sourceSymbolKeys.length; i2++) {
      key = sourceSymbolKeys[i2];
      if (excluded.indexOf(key) >= 0) continue;
      if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue;
      target[key] = source[key];
    }
  }
  return target;
}
function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i2;
  for (i2 = 0; i2 < sourceKeys.length; i2++) {
    key = sourceKeys[i2];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }
  return target;
}
var Dropzone = (0, import_react23.forwardRef)(function(_ref, ref) {
  var children = _ref.children, params = _objectWithoutProperties(_ref, _excluded);
  var _useDropzone = useDropzone(params), open = _useDropzone.open, props = _objectWithoutProperties(_useDropzone, _excluded2);
  (0, import_react23.useImperativeHandle)(ref, function() {
    return {
      open
    };
  }, [open]);
  return import_react23.default.createElement(import_react23.Fragment, null, children(_objectSpread2(_objectSpread2({}, props), {}, {
    open
  })));
});
Dropzone.displayName = "Dropzone";
var defaultProps = {
  disabled: false,
  getFilesFromEvent: fromEvent,
  maxSize: Infinity,
  minSize: 0,
  multiple: true,
  maxFiles: 0,
  preventDropOnDocument: true,
  noClick: false,
  noKeyboard: false,
  noDrag: false,
  noDragEventsBubbling: false,
  validator: null,
  useFsAccessApi: false,
  autoFocus: false
};
Dropzone.defaultProps = defaultProps;
Dropzone.propTypes = {
  /**
   * Render function that exposes the dropzone state and prop getter fns
   *
   * @param {object} params
   * @param {Function} params.getRootProps Returns the props you should apply to the root drop container you render
   * @param {Function} params.getInputProps Returns the props you should apply to hidden file input you render
   * @param {Function} params.open Open the native file selection dialog
   * @param {boolean} params.isFocused Dropzone area is in focus
   * @param {boolean} params.isFileDialogActive File dialog is opened
   * @param {boolean} params.isDragActive Active drag is in progress
   * @param {boolean} params.isDragAccept Dragged files are accepted
   * @param {boolean} params.isDragReject Some dragged files are rejected
   * @param {boolean} params.isDragGlobal Files are being dragged anywhere on the document
   * @param {File[]} params.acceptedFiles Accepted files
   * @param {FileRejection[]} params.fileRejections Rejected files and why they were rejected
   */
  children: import_prop_types.default.func,
  /**
   * Set accepted file types.
   * Checkout https://developer.mozilla.org/en-US/docs/Web/API/window/showOpenFilePicker types option for more information.
   * Keep in mind that mime type determination is not reliable across platforms. CSV files,
   * for example, are reported as text/plain under macOS but as application/vnd.ms-excel under
   * Windows. In some cases there might not be a mime type set at all (https://github.com/react-dropzone/react-dropzone/issues/276).
   */
  accept: import_prop_types.default.objectOf(import_prop_types.default.arrayOf(import_prop_types.default.string)),
  /**
   * Allow drag 'n' drop (or selection from the file dialog) of multiple files
   */
  multiple: import_prop_types.default.bool,
  /**
   * If false, allow dropped items to take over the current browser window
   */
  preventDropOnDocument: import_prop_types.default.bool,
  /**
   * If true, disables click to open the native file selection dialog
   */
  noClick: import_prop_types.default.bool,
  /**
   * If true, disables SPACE/ENTER to open the native file selection dialog.
   * Note that it also stops tracking the focus state.
   */
  noKeyboard: import_prop_types.default.bool,
  /**
   * If true, disables drag 'n' drop
   */
  noDrag: import_prop_types.default.bool,
  /**
   * If true, stops drag event propagation to parents
   */
  noDragEventsBubbling: import_prop_types.default.bool,
  /**
   * Minimum file size (in bytes)
   */
  minSize: import_prop_types.default.number,
  /**
   * Maximum file size (in bytes)
   */
  maxSize: import_prop_types.default.number,
  /**
   * Maximum accepted number of files
   * The default value is 0 which means there is no limitation to how many files are accepted.
   */
  maxFiles: import_prop_types.default.number,
  /**
   * Enable/disable the dropzone
   */
  disabled: import_prop_types.default.bool,
  /**
   * Use this to provide a custom file aggregator
   *
   * @param {(DragEvent|Event|Array<FileSystemFileHandle>)} event A drag event or input change event (if files were selected via the file dialog)
   */
  getFilesFromEvent: import_prop_types.default.func,
  /**
   * Cb for when closing the file dialog with no selection
   */
  onFileDialogCancel: import_prop_types.default.func,
  /**
   * Cb for when opening the file dialog
   */
  onFileDialogOpen: import_prop_types.default.func,
  /**
   * Set to true to use the https://developer.mozilla.org/en-US/docs/Web/API/File_System_Access_API
   * to open the file picker instead of using an `<input type="file">` click event.
   */
  useFsAccessApi: import_prop_types.default.bool,
  /**
   * Set to true to focus the root element on render
   */
  autoFocus: import_prop_types.default.bool,
  /**
   * Cb for when the `dragenter` event occurs.
   *
   * @param {DragEvent} event
   */
  onDragEnter: import_prop_types.default.func,
  /**
   * Cb for when the `dragleave` event occurs
   *
   * @param {DragEvent} event
   */
  onDragLeave: import_prop_types.default.func,
  /**
   * Cb for when the `dragover` event occurs
   *
   * @param {DragEvent} event
   */
  onDragOver: import_prop_types.default.func,
  /**
   * Cb for when the `drop` event occurs.
   * Note that this callback is invoked after the `getFilesFromEvent` callback is done.
   *
   * Files are accepted or rejected based on the `accept`, `multiple`, `minSize` and `maxSize` props.
   * `accept` must be a valid [MIME type](http://www.iana.org/assignments/media-types/media-types.xhtml) according to [input element specification](https://www.w3.org/wiki/HTML/Elements/input/file) or a valid file extension.
   * If `multiple` is set to false and additional files are dropped,
   * all files besides the first will be rejected.
   * Any file which does not have a size in the [`minSize`, `maxSize`] range, will be rejected as well.
   *
   * Note that the `onDrop` callback will always be invoked regardless if the dropped files were accepted or rejected.
   * If you'd like to react to a specific scenario, use the `onDropAccepted`/`onDropRejected` props.
   *
   * `onDrop` will provide you with an array of [File](https://developer.mozilla.org/en-US/docs/Web/API/File) objects which you can then process and send to a server.
   * For example, with [SuperAgent](https://github.com/visionmedia/superagent) as a http/ajax library:
   *
   * ```js
   * function onDrop(acceptedFiles) {
   *   const req = request.post('/upload')
   *   acceptedFiles.forEach(file => {
   *     req.attach(file.name, file)
   *   })
   *   req.end(callback)
   * }
   * ```
   *
   * @param {File[]} acceptedFiles
   * @param {FileRejection[]} fileRejections
   * @param {(DragEvent|Event)} event A drag event or input change event (if files were selected via the file dialog)
   */
  onDrop: import_prop_types.default.func,
  /**
   * Cb for when the `drop` event occurs.
   * Note that if no files are accepted, this callback is not invoked.
   *
   * @param {File[]} files
   * @param {(DragEvent|Event)} event
   */
  onDropAccepted: import_prop_types.default.func,
  /**
   * Cb for when the `drop` event occurs.
   * Note that if no files are rejected, this callback is not invoked.
   *
   * @param {FileRejection[]} fileRejections
   * @param {(DragEvent|Event)} event
   */
  onDropRejected: import_prop_types.default.func,
  /**
   * Cb for when there's some error from any of the promises.
   *
   * @param {Error} error
   */
  onError: import_prop_types.default.func,
  /**
   * Custom validation function. It must return null if there's no errors.
   * @param {File} file
   * @returns {FileError|FileError[]|null}
   */
  validator: import_prop_types.default.func
};
var initialState2 = {
  isFocused: false,
  isFileDialogActive: false,
  isDragActive: false,
  isDragAccept: false,
  isDragReject: false,
  isDragGlobal: false,
  acceptedFiles: [],
  fileRejections: []
};
function useDropzone() {
  var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  var _defaultProps$props = _objectSpread2(_objectSpread2({}, defaultProps), props), accept = _defaultProps$props.accept, disabled = _defaultProps$props.disabled, getFilesFromEvent = _defaultProps$props.getFilesFromEvent, maxSize = _defaultProps$props.maxSize, minSize = _defaultProps$props.minSize, multiple = _defaultProps$props.multiple, maxFiles = _defaultProps$props.maxFiles, onDragEnter = _defaultProps$props.onDragEnter, onDragLeave = _defaultProps$props.onDragLeave, onDragOver = _defaultProps$props.onDragOver, onDrop = _defaultProps$props.onDrop, onDropAccepted = _defaultProps$props.onDropAccepted, onDropRejected = _defaultProps$props.onDropRejected, onFileDialogCancel = _defaultProps$props.onFileDialogCancel, onFileDialogOpen = _defaultProps$props.onFileDialogOpen, useFsAccessApi = _defaultProps$props.useFsAccessApi, autoFocus = _defaultProps$props.autoFocus, preventDropOnDocument = _defaultProps$props.preventDropOnDocument, noClick = _defaultProps$props.noClick, noKeyboard = _defaultProps$props.noKeyboard, noDrag = _defaultProps$props.noDrag, noDragEventsBubbling = _defaultProps$props.noDragEventsBubbling, onError = _defaultProps$props.onError, validator = _defaultProps$props.validator;
  var acceptAttr = (0, import_react23.useMemo)(function() {
    return acceptPropAsAcceptAttr(accept);
  }, [accept]);
  var pickerTypes = (0, import_react23.useMemo)(function() {
    return pickerOptionsFromAccept(accept);
  }, [accept]);
  var onFileDialogOpenCb = (0, import_react23.useMemo)(function() {
    return typeof onFileDialogOpen === "function" ? onFileDialogOpen : noop7;
  }, [onFileDialogOpen]);
  var onFileDialogCancelCb = (0, import_react23.useMemo)(function() {
    return typeof onFileDialogCancel === "function" ? onFileDialogCancel : noop7;
  }, [onFileDialogCancel]);
  var rootRef = (0, import_react23.useRef)(null);
  var inputRef = (0, import_react23.useRef)(null);
  var _useReducer = (0, import_react23.useReducer)(reducer, initialState2), _useReducer2 = _slicedToArray2(_useReducer, 2), state = _useReducer2[0], dispatch = _useReducer2[1];
  var isFocused = state.isFocused, isFileDialogActive = state.isFileDialogActive;
  var fsAccessApiWorksRef = (0, import_react23.useRef)(typeof window !== "undefined" && window.isSecureContext && useFsAccessApi && canUseFileSystemAccessAPI());
  var onWindowFocus = function onWindowFocus2() {
    if (!fsAccessApiWorksRef.current && isFileDialogActive) {
      setTimeout(function() {
        if (inputRef.current) {
          var files = inputRef.current.files;
          if (!files.length) {
            dispatch({
              type: "closeDialog"
            });
            onFileDialogCancelCb();
          }
        }
      }, 300);
    }
  };
  (0, import_react23.useEffect)(function() {
    window.addEventListener("focus", onWindowFocus, false);
    return function() {
      window.removeEventListener("focus", onWindowFocus, false);
    };
  }, [inputRef, isFileDialogActive, onFileDialogCancelCb, fsAccessApiWorksRef]);
  var dragTargetsRef = (0, import_react23.useRef)([]);
  var globalDragTargetsRef = (0, import_react23.useRef)([]);
  var onDocumentDrop = function onDocumentDrop2(event) {
    if (rootRef.current && rootRef.current.contains(event.target)) {
      return;
    }
    event.preventDefault();
    dragTargetsRef.current = [];
  };
  (0, import_react23.useEffect)(function() {
    if (preventDropOnDocument) {
      document.addEventListener("dragover", onDocumentDragOver, false);
      document.addEventListener("drop", onDocumentDrop, false);
    }
    return function() {
      if (preventDropOnDocument) {
        document.removeEventListener("dragover", onDocumentDragOver);
        document.removeEventListener("drop", onDocumentDrop);
      }
    };
  }, [rootRef, preventDropOnDocument]);
  (0, import_react23.useEffect)(function() {
    var onDocumentDragEnter = function onDocumentDragEnter2(event) {
      globalDragTargetsRef.current = [].concat(_toConsumableArray2(globalDragTargetsRef.current), [event.target]);
      if (isEvtWithFiles(event)) {
        dispatch({
          isDragGlobal: true,
          type: "setDragGlobal"
        });
      }
    };
    var onDocumentDragLeave = function onDocumentDragLeave2(event) {
      globalDragTargetsRef.current = globalDragTargetsRef.current.filter(function(el) {
        return el !== event.target && el !== null;
      });
      if (globalDragTargetsRef.current.length > 0) {
        return;
      }
      dispatch({
        isDragGlobal: false,
        type: "setDragGlobal"
      });
    };
    var onDocumentDragEnd = function onDocumentDragEnd2() {
      globalDragTargetsRef.current = [];
      dispatch({
        isDragGlobal: false,
        type: "setDragGlobal"
      });
    };
    var onDocumentDropGlobal = function onDocumentDropGlobal2() {
      globalDragTargetsRef.current = [];
      dispatch({
        isDragGlobal: false,
        type: "setDragGlobal"
      });
    };
    document.addEventListener("dragenter", onDocumentDragEnter, false);
    document.addEventListener("dragleave", onDocumentDragLeave, false);
    document.addEventListener("dragend", onDocumentDragEnd, false);
    document.addEventListener("drop", onDocumentDropGlobal, false);
    return function() {
      document.removeEventListener("dragenter", onDocumentDragEnter);
      document.removeEventListener("dragleave", onDocumentDragLeave);
      document.removeEventListener("dragend", onDocumentDragEnd);
      document.removeEventListener("drop", onDocumentDropGlobal);
    };
  }, [rootRef]);
  (0, import_react23.useEffect)(function() {
    if (!disabled && autoFocus && rootRef.current) {
      rootRef.current.focus();
    }
    return function() {
    };
  }, [rootRef, autoFocus, disabled]);
  var onErrCb = (0, import_react23.useCallback)(function(e) {
    if (onError) {
      onError(e);
    } else {
      console.error(e);
    }
  }, [onError]);
  var onDragEnterCb = (0, import_react23.useCallback)(function(event) {
    event.preventDefault();
    event.persist();
    stopPropagation(event);
    dragTargetsRef.current = [].concat(_toConsumableArray2(dragTargetsRef.current), [event.target]);
    if (isEvtWithFiles(event)) {
      Promise.resolve(getFilesFromEvent(event)).then(function(files) {
        if (isPropagationStopped(event) && !noDragEventsBubbling) {
          return;
        }
        var fileCount = files.length;
        var isDragAccept = fileCount > 0 && allFilesAccepted({
          files,
          accept: acceptAttr,
          minSize,
          maxSize,
          multiple,
          maxFiles,
          validator
        });
        var isDragReject = fileCount > 0 && !isDragAccept;
        dispatch({
          isDragAccept,
          isDragReject,
          isDragActive: true,
          type: "setDraggedFiles"
        });
        if (onDragEnter) {
          onDragEnter(event);
        }
      }).catch(function(e) {
        return onErrCb(e);
      });
    }
  }, [getFilesFromEvent, onDragEnter, onErrCb, noDragEventsBubbling, acceptAttr, minSize, maxSize, multiple, maxFiles, validator]);
  var onDragOverCb = (0, import_react23.useCallback)(function(event) {
    event.preventDefault();
    event.persist();
    stopPropagation(event);
    var hasFiles = isEvtWithFiles(event);
    if (hasFiles && event.dataTransfer) {
      try {
        event.dataTransfer.dropEffect = "copy";
      } catch (_unused) {
      }
    }
    if (hasFiles && onDragOver) {
      onDragOver(event);
    }
    return false;
  }, [onDragOver, noDragEventsBubbling]);
  var onDragLeaveCb = (0, import_react23.useCallback)(function(event) {
    event.preventDefault();
    event.persist();
    stopPropagation(event);
    var targets = dragTargetsRef.current.filter(function(target) {
      return rootRef.current && rootRef.current.contains(target);
    });
    var targetIdx = targets.indexOf(event.target);
    if (targetIdx !== -1) {
      targets.splice(targetIdx, 1);
    }
    dragTargetsRef.current = targets;
    if (targets.length > 0) {
      return;
    }
    dispatch({
      type: "setDraggedFiles",
      isDragActive: false,
      isDragAccept: false,
      isDragReject: false
    });
    if (isEvtWithFiles(event) && onDragLeave) {
      onDragLeave(event);
    }
  }, [rootRef, onDragLeave, noDragEventsBubbling]);
  var setFiles = (0, import_react23.useCallback)(function(files, event) {
    var acceptedFiles = [];
    var fileRejections = [];
    files.forEach(function(file) {
      var _fileAccepted = fileAccepted(file, acceptAttr), _fileAccepted2 = _slicedToArray2(_fileAccepted, 2), accepted = _fileAccepted2[0], acceptError = _fileAccepted2[1];
      var _fileMatchSize = fileMatchSize(file, minSize, maxSize), _fileMatchSize2 = _slicedToArray2(_fileMatchSize, 2), sizeMatch = _fileMatchSize2[0], sizeError = _fileMatchSize2[1];
      var customErrors = validator ? validator(file) : null;
      if (accepted && sizeMatch && !customErrors) {
        acceptedFiles.push(file);
      } else {
        var errors = [acceptError, sizeError];
        if (customErrors) {
          errors = errors.concat(customErrors);
        }
        fileRejections.push({
          file,
          errors: errors.filter(function(e) {
            return e;
          })
        });
      }
    });
    if (!multiple && acceptedFiles.length > 1 || multiple && maxFiles >= 1 && acceptedFiles.length > maxFiles) {
      acceptedFiles.forEach(function(file) {
        fileRejections.push({
          file,
          errors: [TOO_MANY_FILES_REJECTION]
        });
      });
      acceptedFiles.splice(0);
    }
    dispatch({
      acceptedFiles,
      fileRejections,
      isDragReject: fileRejections.length > 0,
      type: "setFiles"
    });
    if (onDrop) {
      onDrop(acceptedFiles, fileRejections, event);
    }
    if (fileRejections.length > 0 && onDropRejected) {
      onDropRejected(fileRejections, event);
    }
    if (acceptedFiles.length > 0 && onDropAccepted) {
      onDropAccepted(acceptedFiles, event);
    }
  }, [dispatch, multiple, acceptAttr, minSize, maxSize, maxFiles, onDrop, onDropAccepted, onDropRejected, validator]);
  var onDropCb = (0, import_react23.useCallback)(function(event) {
    event.preventDefault();
    event.persist();
    stopPropagation(event);
    dragTargetsRef.current = [];
    if (isEvtWithFiles(event)) {
      Promise.resolve(getFilesFromEvent(event)).then(function(files) {
        if (isPropagationStopped(event) && !noDragEventsBubbling) {
          return;
        }
        setFiles(files, event);
      }).catch(function(e) {
        return onErrCb(e);
      });
    }
    dispatch({
      type: "reset"
    });
  }, [getFilesFromEvent, setFiles, onErrCb, noDragEventsBubbling]);
  var openFileDialog = (0, import_react23.useCallback)(function() {
    if (fsAccessApiWorksRef.current) {
      dispatch({
        type: "openDialog"
      });
      onFileDialogOpenCb();
      var opts = {
        multiple,
        types: pickerTypes
      };
      window.showOpenFilePicker(opts).then(function(handles) {
        return getFilesFromEvent(handles);
      }).then(function(files) {
        setFiles(files, null);
        dispatch({
          type: "closeDialog"
        });
      }).catch(function(e) {
        if (isAbort(e)) {
          onFileDialogCancelCb(e);
          dispatch({
            type: "closeDialog"
          });
        } else if (isSecurityError(e)) {
          fsAccessApiWorksRef.current = false;
          if (inputRef.current) {
            inputRef.current.value = null;
            inputRef.current.click();
          } else {
            onErrCb(new Error("Cannot open the file picker because the https://developer.mozilla.org/en-US/docs/Web/API/File_System_Access_API is not supported and no <input> was provided."));
          }
        } else {
          onErrCb(e);
        }
      });
      return;
    }
    if (inputRef.current) {
      dispatch({
        type: "openDialog"
      });
      onFileDialogOpenCb();
      inputRef.current.value = null;
      inputRef.current.click();
    }
  }, [dispatch, onFileDialogOpenCb, onFileDialogCancelCb, useFsAccessApi, setFiles, onErrCb, pickerTypes, multiple]);
  var onKeyDownCb = (0, import_react23.useCallback)(function(event) {
    if (!rootRef.current || !rootRef.current.isEqualNode(event.target)) {
      return;
    }
    if (event.key === " " || event.key === "Enter" || event.keyCode === 32 || event.keyCode === 13) {
      event.preventDefault();
      openFileDialog();
    }
  }, [rootRef, openFileDialog]);
  var onFocusCb = (0, import_react23.useCallback)(function() {
    dispatch({
      type: "focus"
    });
  }, []);
  var onBlurCb = (0, import_react23.useCallback)(function() {
    dispatch({
      type: "blur"
    });
  }, []);
  var onClickCb = (0, import_react23.useCallback)(function() {
    if (noClick) {
      return;
    }
    if (isIeOrEdge()) {
      setTimeout(openFileDialog, 0);
    } else {
      openFileDialog();
    }
  }, [noClick, openFileDialog]);
  var composeHandler = function composeHandler2(fn) {
    return disabled ? null : fn;
  };
  var composeKeyboardHandler = function composeKeyboardHandler2(fn) {
    return noKeyboard ? null : composeHandler(fn);
  };
  var composeDragHandler = function composeDragHandler2(fn) {
    return noDrag ? null : composeHandler(fn);
  };
  var stopPropagation = function stopPropagation2(event) {
    if (noDragEventsBubbling) {
      event.stopPropagation();
    }
  };
  var getRootProps = (0, import_react23.useMemo)(function() {
    return function() {
      var _ref2 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, _ref2$refKey = _ref2.refKey, refKey = _ref2$refKey === void 0 ? "ref" : _ref2$refKey, role = _ref2.role, onKeyDown = _ref2.onKeyDown, onFocus = _ref2.onFocus, onBlur = _ref2.onBlur, onClick = _ref2.onClick, onDragEnter2 = _ref2.onDragEnter, onDragOver2 = _ref2.onDragOver, onDragLeave2 = _ref2.onDragLeave, onDrop2 = _ref2.onDrop, rest = _objectWithoutProperties(_ref2, _excluded3);
      return _objectSpread2(_objectSpread2(_defineProperty2({
        onKeyDown: composeKeyboardHandler(composeEventHandlers(onKeyDown, onKeyDownCb)),
        onFocus: composeKeyboardHandler(composeEventHandlers(onFocus, onFocusCb)),
        onBlur: composeKeyboardHandler(composeEventHandlers(onBlur, onBlurCb)),
        onClick: composeHandler(composeEventHandlers(onClick, onClickCb)),
        onDragEnter: composeDragHandler(composeEventHandlers(onDragEnter2, onDragEnterCb)),
        onDragOver: composeDragHandler(composeEventHandlers(onDragOver2, onDragOverCb)),
        onDragLeave: composeDragHandler(composeEventHandlers(onDragLeave2, onDragLeaveCb)),
        onDrop: composeDragHandler(composeEventHandlers(onDrop2, onDropCb)),
        role: typeof role === "string" && role !== "" ? role : "presentation"
      }, refKey, rootRef), !disabled && !noKeyboard ? {
        tabIndex: 0
      } : {}), rest);
    };
  }, [rootRef, onKeyDownCb, onFocusCb, onBlurCb, onClickCb, onDragEnterCb, onDragOverCb, onDragLeaveCb, onDropCb, noKeyboard, noDrag, disabled]);
  var onInputElementClick = (0, import_react23.useCallback)(function(event) {
    event.stopPropagation();
  }, []);
  var getInputProps = (0, import_react23.useMemo)(function() {
    return function() {
      var _ref3 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, _ref3$refKey = _ref3.refKey, refKey = _ref3$refKey === void 0 ? "ref" : _ref3$refKey, onChange = _ref3.onChange, onClick = _ref3.onClick, rest = _objectWithoutProperties(_ref3, _excluded4);
      var inputProps = _defineProperty2({
        accept: acceptAttr,
        multiple,
        type: "file",
        style: {
          border: 0,
          clip: "rect(0, 0, 0, 0)",
          clipPath: "inset(50%)",
          height: "1px",
          margin: "0 -1px -1px 0",
          overflow: "hidden",
          padding: 0,
          position: "absolute",
          width: "1px",
          whiteSpace: "nowrap"
        },
        onChange: composeHandler(composeEventHandlers(onChange, onDropCb)),
        onClick: composeHandler(composeEventHandlers(onClick, onInputElementClick)),
        tabIndex: -1
      }, refKey, inputRef);
      return _objectSpread2(_objectSpread2({}, inputProps), rest);
    };
  }, [inputRef, accept, multiple, onDropCb, disabled]);
  return _objectSpread2(_objectSpread2({}, state), {}, {
    isFocused: isFocused && !disabled,
    getRootProps,
    getInputProps,
    rootRef,
    inputRef,
    open: composeHandler(openFileDialog)
  });
}
function reducer(state, action) {
  switch (action.type) {
    case "focus":
      return _objectSpread2(_objectSpread2({}, state), {}, {
        isFocused: true
      });
    case "blur":
      return _objectSpread2(_objectSpread2({}, state), {}, {
        isFocused: false
      });
    case "openDialog":
      return _objectSpread2(_objectSpread2({}, initialState2), {}, {
        isFileDialogActive: true
      });
    case "closeDialog":
      return _objectSpread2(_objectSpread2({}, state), {}, {
        isFileDialogActive: false
      });
    case "setDraggedFiles":
      return _objectSpread2(_objectSpread2({}, state), {}, {
        isDragActive: action.isDragActive,
        isDragAccept: action.isDragAccept,
        isDragReject: action.isDragReject
      });
    case "setFiles":
      return _objectSpread2(_objectSpread2({}, state), {}, {
        acceptedFiles: action.acceptedFiles,
        fileRejections: action.fileRejections,
        isDragReject: action.isDragReject
      });
    case "setDragGlobal":
      return _objectSpread2(_objectSpread2({}, state), {}, {
        isDragGlobal: action.isDragGlobal
      });
    case "reset":
      return _objectSpread2({}, initialState2);
    default:
      return state;
  }
}
function noop7() {
}

// node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
  return _extends = Object.assign ? Object.assign.bind() : function(n2) {
    for (var e = 1; e < arguments.length; e++) {
      var t2 = arguments[e];
      for (var r3 in t2) ({}).hasOwnProperty.call(t2, r3) && (n2[r3] = t2[r3]);
    }
    return n2;
  }, _extends.apply(null, arguments);
}

// node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose2(r3, e) {
  if (null == r3) return {};
  var t2 = {};
  for (var n2 in r3) if ({}.hasOwnProperty.call(r3, n2)) {
    if (-1 !== e.indexOf(n2)) continue;
    t2[n2] = r3[n2];
  }
  return t2;
}

// node_modules/react-textarea-autosize/dist/react-textarea-autosize.browser.development.esm.js
var React8 = __toESM(require_react());

// node_modules/use-latest/dist/use-latest.esm.js
var import_react25 = __toESM(require_react());

// node_modules/use-isomorphic-layout-effect/dist/use-isomorphic-layout-effect.browser.esm.js
var import_react24 = __toESM(require_react());
var index4 = import_react24.useLayoutEffect;

// node_modules/use-latest/dist/use-latest.esm.js
var useLatest = function useLatest2(value) {
  var ref = import_react25.default.useRef(value);
  index4(function() {
    ref.current = value;
  });
  return ref;
};

// node_modules/use-composed-ref/dist/use-composed-ref.esm.js
var import_react26 = __toESM(require_react());
var updateRef = function updateRef2(ref, value) {
  if (typeof ref === "function") {
    ref(value);
    return;
  }
  ref.current = value;
};
var useComposedRef = function useComposedRef2(libRef, userRef) {
  var prevUserRef = import_react26.default.useRef();
  return import_react26.default.useCallback(function(instance2) {
    libRef.current = instance2;
    if (prevUserRef.current) {
      updateRef(prevUserRef.current, null);
    }
    prevUserRef.current = userRef;
    if (!userRef) {
      return;
    }
    updateRef(userRef, instance2);
  }, [userRef]);
};

// node_modules/react-textarea-autosize/dist/react-textarea-autosize.browser.development.esm.js
var HIDDEN_TEXTAREA_STYLE = {
  "min-height": "0",
  "max-height": "none",
  height: "0",
  visibility: "hidden",
  overflow: "hidden",
  position: "absolute",
  "z-index": "-1000",
  top: "0",
  right: "0",
  display: "block"
};
var forceHiddenStyles = function forceHiddenStyles2(node2) {
  Object.keys(HIDDEN_TEXTAREA_STYLE).forEach(function(key) {
    node2.style.setProperty(key, HIDDEN_TEXTAREA_STYLE[key], "important");
  });
};
var forceHiddenStyles$1 = forceHiddenStyles;
var hiddenTextarea = null;
var getHeight = function getHeight2(node2, sizingData) {
  var height = node2.scrollHeight;
  if (sizingData.sizingStyle.boxSizing === "border-box") {
    return height + sizingData.borderSize;
  }
  return height - sizingData.paddingSize;
};
function calculateNodeHeight(sizingData, value, minRows, maxRows) {
  if (minRows === void 0) {
    minRows = 1;
  }
  if (maxRows === void 0) {
    maxRows = Infinity;
  }
  if (!hiddenTextarea) {
    hiddenTextarea = document.createElement("textarea");
    hiddenTextarea.setAttribute("tabindex", "-1");
    hiddenTextarea.setAttribute("aria-hidden", "true");
    forceHiddenStyles$1(hiddenTextarea);
  }
  if (hiddenTextarea.parentNode === null) {
    document.body.appendChild(hiddenTextarea);
  }
  var paddingSize = sizingData.paddingSize, borderSize = sizingData.borderSize, sizingStyle = sizingData.sizingStyle;
  var boxSizing = sizingStyle.boxSizing;
  Object.keys(sizingStyle).forEach(function(_key) {
    var key = _key;
    hiddenTextarea.style[key] = sizingStyle[key];
  });
  forceHiddenStyles$1(hiddenTextarea);
  hiddenTextarea.value = value;
  var height = getHeight(hiddenTextarea, sizingData);
  hiddenTextarea.value = value;
  height = getHeight(hiddenTextarea, sizingData);
  hiddenTextarea.value = "x";
  var rowHeight = hiddenTextarea.scrollHeight - paddingSize;
  var minHeight = rowHeight * minRows;
  if (boxSizing === "border-box") {
    minHeight = minHeight + paddingSize + borderSize;
  }
  height = Math.max(minHeight, height);
  var maxHeight = rowHeight * maxRows;
  if (boxSizing === "border-box") {
    maxHeight = maxHeight + paddingSize + borderSize;
  }
  height = Math.min(maxHeight, height);
  return [height, rowHeight];
}
var noop8 = function noop9() {
};
var pick = function pick2(props, obj) {
  return props.reduce(function(acc, prop) {
    acc[prop] = obj[prop];
    return acc;
  }, {});
};
var SIZING_STYLE = [
  "borderBottomWidth",
  "borderLeftWidth",
  "borderRightWidth",
  "borderTopWidth",
  "boxSizing",
  "fontFamily",
  "fontSize",
  "fontStyle",
  "fontWeight",
  "letterSpacing",
  "lineHeight",
  "paddingBottom",
  "paddingLeft",
  "paddingRight",
  "paddingTop",
  // non-standard
  "tabSize",
  "textIndent",
  // non-standard
  "textRendering",
  "textTransform",
  "width",
  "wordBreak",
  "wordSpacing",
  "scrollbarGutter"
];
var isIE = !!document.documentElement.currentStyle;
var getSizingData = function getSizingData2(node2) {
  var style = window.getComputedStyle(node2);
  if (style === null) {
    return null;
  }
  var sizingStyle = pick(SIZING_STYLE, style);
  var boxSizing = sizingStyle.boxSizing;
  if (boxSizing === "") {
    return null;
  }
  if (isIE && boxSizing === "border-box") {
    sizingStyle.width = parseFloat(sizingStyle.width) + parseFloat(sizingStyle.borderRightWidth) + parseFloat(sizingStyle.borderLeftWidth) + parseFloat(sizingStyle.paddingRight) + parseFloat(sizingStyle.paddingLeft) + "px";
  }
  var paddingSize = parseFloat(sizingStyle.paddingBottom) + parseFloat(sizingStyle.paddingTop);
  var borderSize = parseFloat(sizingStyle.borderBottomWidth) + parseFloat(sizingStyle.borderTopWidth);
  return {
    sizingStyle,
    paddingSize,
    borderSize
  };
};
var getSizingData$1 = getSizingData;
function useListener(target, type, listener) {
  var latestListener = useLatest(listener);
  React8.useLayoutEffect(function() {
    var handler = function handler2(ev) {
      return latestListener.current(ev);
    };
    if (!target) {
      return;
    }
    target.addEventListener(type, handler);
    return function() {
      return target.removeEventListener(type, handler);
    };
  }, []);
}
var useFormResetListener = function useFormResetListener2(libRef, listener) {
  useListener(document.body, "reset", function(ev) {
    if (libRef.current.form === ev.target) {
      listener(ev);
    }
  });
};
var useWindowResizeListener = function useWindowResizeListener2(listener) {
  useListener(window, "resize", listener);
};
var useFontsLoadedListener = function useFontsLoadedListener2(listener) {
  useListener(document.fonts, "loadingdone", listener);
};
var _excluded5 = ["cacheMeasurements", "maxRows", "minRows", "onChange", "onHeightChange"];
var TextareaAutosize = function TextareaAutosize2(_ref, userRef) {
  var cacheMeasurements = _ref.cacheMeasurements, maxRows = _ref.maxRows, minRows = _ref.minRows, _ref$onChange = _ref.onChange, onChange = _ref$onChange === void 0 ? noop8 : _ref$onChange, _ref$onHeightChange = _ref.onHeightChange, onHeightChange = _ref$onHeightChange === void 0 ? noop8 : _ref$onHeightChange, props = _objectWithoutPropertiesLoose2(_ref, _excluded5);
  if (props.style) {
    if ("maxHeight" in props.style) {
      throw new Error("Using `style.maxHeight` for <TextareaAutosize/> is not supported. Please use `maxRows`.");
    }
    if ("minHeight" in props.style) {
      throw new Error("Using `style.minHeight` for <TextareaAutosize/> is not supported. Please use `minRows`.");
    }
  }
  var isControlled = props.value !== void 0;
  var libRef = React8.useRef(null);
  var ref = useComposedRef(libRef, userRef);
  var heightRef = React8.useRef(0);
  var measurementsCacheRef = React8.useRef();
  var resizeTextarea = function resizeTextarea2() {
    var node2 = libRef.current;
    var nodeSizingData = cacheMeasurements && measurementsCacheRef.current ? measurementsCacheRef.current : getSizingData$1(node2);
    if (!nodeSizingData) {
      return;
    }
    measurementsCacheRef.current = nodeSizingData;
    var _calculateNodeHeight = calculateNodeHeight(nodeSizingData, node2.value || node2.placeholder || "x", minRows, maxRows), height = _calculateNodeHeight[0], rowHeight = _calculateNodeHeight[1];
    if (heightRef.current !== height) {
      heightRef.current = height;
      node2.style.setProperty("height", height + "px", "important");
      onHeightChange(height, {
        rowHeight
      });
    }
  };
  var handleChange = function handleChange2(event) {
    if (!isControlled) {
      resizeTextarea();
    }
    onChange(event);
  };
  {
    React8.useLayoutEffect(resizeTextarea);
    useFormResetListener(libRef, function() {
      if (!isControlled) {
        var currentValue = libRef.current.value;
        requestAnimationFrame(function() {
          var node2 = libRef.current;
          if (node2 && currentValue !== node2.value) {
            resizeTextarea();
          }
        });
      }
    });
    useWindowResizeListener(resizeTextarea);
    useFontsLoadedListener(resizeTextarea);
    return React8.createElement("textarea", _extends({}, props, {
      onChange: handleChange,
      ref
    }));
  }
};
var index5 = React8.forwardRef(TextareaAutosize);

// node_modules/@virtuoso.dev/react-urx/dist/react-urx.esm.js
var import_react27 = __toESM(require_react());

// node_modules/@virtuoso.dev/urx/dist/urx.esm.js
var PUBLISH = 0;
var SUBSCRIBE = 1;
var RESET = 2;
var VALUE = 4;
function compose(a2, b2) {
  return function(arg) {
    return a2(b2(arg));
  };
}
function thrush(arg, proc) {
  return proc(arg);
}
function curry2to1(proc, arg1) {
  return function(arg2) {
    return proc(arg1, arg2);
  };
}
function curry1to0(proc, arg) {
  return function() {
    return proc(arg);
  };
}
function tap(arg, proc) {
  proc(arg);
  return arg;
}
function tup() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return args;
}
function call(proc) {
  proc();
}
function always(value) {
  return function() {
    return value;
  };
}
function joinProc() {
  for (var _len2 = arguments.length, procs = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    procs[_key2] = arguments[_key2];
  }
  return function() {
    procs.map(call);
  };
}
function noop10() {
}
function subscribe(emitter, subscription) {
  return emitter(SUBSCRIBE, subscription);
}
function publish(publisher, value) {
  publisher(PUBLISH, value);
}
function reset(emitter) {
  emitter(RESET);
}
function getValue(depot) {
  return depot(VALUE);
}
function connect(emitter, publisher) {
  return subscribe(emitter, curry2to1(publisher, PUBLISH));
}
function handleNext(emitter, subscription) {
  var unsub = emitter(SUBSCRIBE, function(value) {
    unsub();
    subscription(value);
  });
  return unsub;
}
function stream() {
  var subscriptions = [];
  return function(action, arg) {
    switch (action) {
      case RESET:
        subscriptions.splice(0, subscriptions.length);
        return;
      case SUBSCRIBE:
        subscriptions.push(arg);
        return function() {
          var indexOf = subscriptions.indexOf(arg);
          if (indexOf > -1) {
            subscriptions.splice(indexOf, 1);
          }
        };
      case PUBLISH:
        subscriptions.slice().forEach(function(subscription) {
          subscription(arg);
        });
        return;
      default:
        throw new Error("unrecognized action " + action);
    }
  };
}
function statefulStream(initial) {
  var value = initial;
  var innerSubject = stream();
  return function(action, arg) {
    switch (action) {
      case SUBSCRIBE:
        var subscription = arg;
        subscription(value);
        break;
      case PUBLISH:
        value = arg;
        break;
      case VALUE:
        return value;
    }
    return innerSubject(action, arg);
  };
}
function eventHandler(emitter) {
  var unsub;
  var currentSubscription;
  var cleanup2 = function cleanup3() {
    return unsub && unsub();
  };
  return function(action, subscription) {
    switch (action) {
      case SUBSCRIBE:
        if (subscription) {
          if (currentSubscription === subscription) {
            return;
          }
          cleanup2();
          currentSubscription = subscription;
          unsub = subscribe(emitter, subscription);
          return unsub;
        } else {
          cleanup2();
          return noop10;
        }
      case RESET:
        cleanup2();
        currentSubscription = null;
        return;
      default:
        throw new Error("unrecognized action " + action);
    }
  };
}
function streamFromEmitter(emitter) {
  return tap(stream(), function(stream2) {
    return connect(emitter, stream2);
  });
}
function statefulStreamFromEmitter(emitter, initial) {
  return tap(statefulStream(initial), function(stream2) {
    return connect(emitter, stream2);
  });
}
function combineOperators() {
  for (var _len = arguments.length, operators = new Array(_len), _key = 0; _key < _len; _key++) {
    operators[_key] = arguments[_key];
  }
  return function(subscriber) {
    return operators.reduceRight(thrush, subscriber);
  };
}
function pipe(source) {
  for (var _len2 = arguments.length, operators = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
    operators[_key2 - 1] = arguments[_key2];
  }
  var project = combineOperators.apply(void 0, operators);
  return function(action, subscription) {
    switch (action) {
      case SUBSCRIBE:
        return subscribe(source, project(subscription));
      case RESET:
        reset(source);
        return;
      default:
        throw new Error("unrecognized action " + action);
    }
  };
}
function defaultComparator(previous3, next) {
  return previous3 === next;
}
function distinctUntilChanged(comparator) {
  if (comparator === void 0) {
    comparator = defaultComparator;
  }
  var current;
  return function(done) {
    return function(next) {
      if (!comparator(current, next)) {
        current = next;
        done(next);
      }
    };
  };
}
function filter(predicate) {
  return function(done) {
    return function(value) {
      predicate(value) && done(value);
    };
  };
}
function map3(project) {
  return function(done) {
    return compose(done, project);
  };
}
function mapTo(value) {
  return function(done) {
    return function() {
      return done(value);
    };
  };
}
function scan(scanner, initial) {
  return function(done) {
    return function(value) {
      return done(initial = scanner(initial, value));
    };
  };
}
function skip(times) {
  return function(done) {
    return function(value) {
      times > 0 ? times-- : done(value);
    };
  };
}
function throttleTime(interval) {
  var currentValue;
  var timeout;
  return function(done) {
    return function(value) {
      currentValue = value;
      if (timeout) {
        return;
      }
      timeout = setTimeout(function() {
        timeout = void 0;
        done(currentValue);
      }, interval);
    };
  };
}
function debounceTime(interval) {
  var currentValue;
  var timeout;
  return function(done) {
    return function(value) {
      currentValue = value;
      if (timeout) {
        clearTimeout(timeout);
      }
      timeout = setTimeout(function() {
        done(currentValue);
      }, interval);
    };
  };
}
function withLatestFrom() {
  for (var _len3 = arguments.length, sources = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
    sources[_key3] = arguments[_key3];
  }
  var values2 = new Array(sources.length);
  var called = 0;
  var pendingCall = null;
  var allCalled = Math.pow(2, sources.length) - 1;
  sources.forEach(function(source, index6) {
    var bit = Math.pow(2, index6);
    subscribe(source, function(value) {
      var prevCalled = called;
      called = called | bit;
      values2[index6] = value;
      if (prevCalled !== allCalled && called === allCalled && pendingCall) {
        pendingCall();
        pendingCall = null;
      }
    });
  });
  return function(done) {
    return function(value) {
      var call2 = function call3() {
        return done([value].concat(values2));
      };
      if (called === allCalled) {
        call2();
      } else {
        pendingCall = call2;
      }
    };
  };
}
function merge2() {
  for (var _len = arguments.length, sources = new Array(_len), _key = 0; _key < _len; _key++) {
    sources[_key] = arguments[_key];
  }
  return function(action, subscription) {
    switch (action) {
      case SUBSCRIBE:
        return joinProc.apply(void 0, sources.map(function(source) {
          return subscribe(source, subscription);
        }));
      case RESET:
        return;
      default:
        throw new Error("unrecognized action " + action);
    }
  };
}
function duc(source, comparator) {
  if (comparator === void 0) {
    comparator = defaultComparator;
  }
  return pipe(source, distinctUntilChanged(comparator));
}
function combineLatest() {
  var innerSubject = stream();
  for (var _len2 = arguments.length, emitters = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    emitters[_key2] = arguments[_key2];
  }
  var values2 = new Array(emitters.length);
  var called = 0;
  var allCalled = Math.pow(2, emitters.length) - 1;
  emitters.forEach(function(source, index6) {
    var bit = Math.pow(2, index6);
    subscribe(source, function(value) {
      values2[index6] = value;
      called = called | bit;
      if (called === allCalled) {
        publish(innerSubject, values2);
      }
    });
  });
  return function(action, subscription) {
    switch (action) {
      case SUBSCRIBE:
        if (called === allCalled) {
          subscription(values2);
        }
        return subscribe(innerSubject, subscription);
      case RESET:
        return reset(innerSubject);
      default:
        throw new Error("unrecognized action " + action);
    }
  };
}
function system(constructor, dependencies, _temp) {
  if (dependencies === void 0) {
    dependencies = [];
  }
  var _ref = _temp === void 0 ? {
    singleton: true
  } : _temp, singleton = _ref.singleton;
  return {
    id: id(),
    constructor,
    dependencies,
    singleton
  };
}
var id = function id2() {
  return /* @__PURE__ */ Symbol();
};
function init2(systemSpec) {
  var singletons = /* @__PURE__ */ new Map();
  var _init = function _init2(_ref2) {
    var id3 = _ref2.id, constructor = _ref2.constructor, dependencies = _ref2.dependencies, singleton = _ref2.singleton;
    if (singleton && singletons.has(id3)) {
      return singletons.get(id3);
    }
    var system2 = constructor(dependencies.map(function(e) {
      return _init2(e);
    }));
    if (singleton) {
      singletons.set(id3, system2);
    }
    return system2;
  };
  return _init(systemSpec);
}

// node_modules/@virtuoso.dev/react-urx/dist/react-urx.esm.js
function _objectWithoutPropertiesLoose3(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i2;
  for (i2 = 0; i2 < sourceKeys.length; i2++) {
    key = sourceKeys[i2];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }
  return target;
}
function _unsupportedIterableToArray3(o2, minLen) {
  if (!o2) return;
  if (typeof o2 === "string") return _arrayLikeToArray3(o2, minLen);
  var n2 = Object.prototype.toString.call(o2).slice(8, -1);
  if (n2 === "Object" && o2.constructor) n2 = o2.constructor.name;
  if (n2 === "Map" || n2 === "Set") return Array.from(o2);
  if (n2 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2)) return _arrayLikeToArray3(o2, minLen);
}
function _arrayLikeToArray3(arr, len) {
  if (len == null || len > arr.length) len = arr.length;
  for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) arr2[i2] = arr[i2];
  return arr2;
}
function _createForOfIteratorHelperLoose(o2, allowArrayLike) {
  var it3 = typeof Symbol !== "undefined" && o2[Symbol.iterator] || o2["@@iterator"];
  if (it3) return (it3 = it3.call(o2)).next.bind(it3);
  if (Array.isArray(o2) || (it3 = _unsupportedIterableToArray3(o2)) || allowArrayLike && o2 && typeof o2.length === "number") {
    if (it3) o2 = it3;
    var i2 = 0;
    return function() {
      if (i2 >= o2.length) return {
        done: true
      };
      return {
        done: false,
        value: o2[i2++]
      };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
var _excluded6 = ["children"];
function omit(keys3, obj) {
  var result = {};
  var index6 = {};
  var idx = 0;
  var len = keys3.length;
  while (idx < len) {
    index6[keys3[idx]] = 1;
    idx += 1;
  }
  for (var prop in obj) {
    if (!index6.hasOwnProperty(prop)) {
      result[prop] = obj[prop];
    }
  }
  return result;
}
var useIsomorphicLayoutEffect = typeof document !== "undefined" ? import_react27.useLayoutEffect : import_react27.useEffect;
function systemToComponent(systemSpec, map4, Root2) {
  var requiredPropNames = Object.keys(map4.required || {});
  var optionalPropNames = Object.keys(map4.optional || {});
  var methodNames = Object.keys(map4.methods || {});
  var eventNames = Object.keys(map4.events || {});
  var Context = (0, import_react27.createContext)({});
  function applyPropsToSystem(system2, props) {
    if (system2["propsReady"]) {
      publish(system2["propsReady"], false);
    }
    for (var _iterator = _createForOfIteratorHelperLoose(requiredPropNames), _step; !(_step = _iterator()).done; ) {
      var requiredPropName = _step.value;
      var stream2 = system2[map4.required[requiredPropName]];
      publish(stream2, props[requiredPropName]);
    }
    for (var _iterator2 = _createForOfIteratorHelperLoose(optionalPropNames), _step2; !(_step2 = _iterator2()).done; ) {
      var optionalPropName = _step2.value;
      if (optionalPropName in props) {
        var _stream = system2[map4.optional[optionalPropName]];
        publish(_stream, props[optionalPropName]);
      }
    }
    if (system2["propsReady"]) {
      publish(system2["propsReady"], true);
    }
  }
  function buildMethods(system2) {
    return methodNames.reduce(function(acc, methodName) {
      acc[methodName] = function(value) {
        var stream2 = system2[map4.methods[methodName]];
        publish(stream2, value);
      };
      return acc;
    }, {});
  }
  function buildEventHandlers(system2) {
    return eventNames.reduce(function(handlers2, eventName) {
      handlers2[eventName] = eventHandler(system2[map4.events[eventName]]);
      return handlers2;
    }, {});
  }
  var Component2 = (0, import_react27.forwardRef)(function(propsWithChildren, ref) {
    var children = propsWithChildren.children, props = _objectWithoutPropertiesLoose3(propsWithChildren, _excluded6);
    var _useState = (0, import_react27.useState)(function() {
      return tap(init2(systemSpec), function(system3) {
        return applyPropsToSystem(system3, props);
      });
    }), system2 = _useState[0];
    var _useState2 = (0, import_react27.useState)(curry1to0(buildEventHandlers, system2)), handlers2 = _useState2[0];
    useIsomorphicLayoutEffect(function() {
      for (var _iterator3 = _createForOfIteratorHelperLoose(eventNames), _step3; !(_step3 = _iterator3()).done; ) {
        var eventName = _step3.value;
        if (eventName in props) {
          subscribe(handlers2[eventName], props[eventName]);
        }
      }
      return function() {
        Object.values(handlers2).map(reset);
      };
    }, [props, handlers2, system2]);
    useIsomorphicLayoutEffect(function() {
      applyPropsToSystem(system2, props);
    });
    (0, import_react27.useImperativeHandle)(ref, always(buildMethods(system2)));
    return (0, import_react27.createElement)(Context.Provider, {
      value: system2
    }, Root2 ? (0, import_react27.createElement)(Root2, omit([].concat(requiredPropNames, optionalPropNames, eventNames), props), children) : children);
  });
  var usePublisher = function usePublisher2(key) {
    return (0, import_react27.useCallback)(curry2to1(publish, (0, import_react27.useContext)(Context)[key]), [key]);
  };
  var useEmitterValue = function useEmitterValue2(key) {
    var context = (0, import_react27.useContext)(Context);
    var source = context[key];
    var _useState3 = (0, import_react27.useState)(curry1to0(getValue, source)), value = _useState3[0], setValue = _useState3[1];
    useIsomorphicLayoutEffect(function() {
      return subscribe(source, function(next) {
        if (next !== value) {
          setValue(always(next));
        }
      });
    }, [source, value]);
    return value;
  };
  var useEmitter = function useEmitter2(key, callback) {
    var context = (0, import_react27.useContext)(Context);
    var source = context[key];
    useIsomorphicLayoutEffect(function() {
      return subscribe(source, callback);
    }, [callback, source]);
  };
  return {
    Component: Component2,
    usePublisher,
    useEmitterValue,
    useEmitter
  };
}

// node_modules/react-virtuoso/dist/index.m.js
var n = __toESM(require_react());
var import_react28 = __toESM(require_react());
var import_react_dom3 = __toESM(require_react_dom());
function c() {
  return c = Object.assign || function(t2) {
    for (var e = 1; e < arguments.length; e++) {
      var n2 = arguments[e];
      for (var o2 in n2) Object.prototype.hasOwnProperty.call(n2, o2) && (t2[o2] = n2[o2]);
    }
    return t2;
  }, c.apply(this, arguments);
}
function m(t2, e) {
  if (null == t2) return {};
  var n2, o2, r3 = {}, i2 = Object.keys(t2);
  for (o2 = 0; o2 < i2.length; o2++) e.indexOf(n2 = i2[o2]) >= 0 || (r3[n2] = t2[n2]);
  return r3;
}
function d(t2, e) {
  (null == e || e > t2.length) && (e = t2.length);
  for (var n2 = 0, o2 = new Array(e); n2 < e; n2++) o2[n2] = t2[n2];
  return o2;
}
function f(t2, e) {
  var n2 = "undefined" != typeof Symbol && t2[Symbol.iterator] || t2["@@iterator"];
  if (n2) return (n2 = n2.call(t2)).next.bind(n2);
  if (Array.isArray(t2) || (n2 = (function(t3, e2) {
    if (t3) {
      if ("string" == typeof t3) return d(t3, e2);
      var n3 = Object.prototype.toString.call(t3).slice(8, -1);
      return "Object" === n3 && t3.constructor && (n3 = t3.constructor.name), "Map" === n3 || "Set" === n3 ? Array.from(t3) : "Arguments" === n3 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n3) ? d(t3, e2) : void 0;
    }
  })(t2)) || e && t2 && "number" == typeof t2.length) {
    n2 && (t2 = n2);
    var o2 = 0;
    return function() {
      return o2 >= t2.length ? { done: true } : { done: false, value: t2[o2++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
var p;
var h;
var g = "undefined" != typeof document ? import_react28.useLayoutEffect : import_react28.useEffect;
!(function(t2) {
  t2[t2.DEBUG = 0] = "DEBUG", t2[t2.INFO = 1] = "INFO", t2[t2.WARN = 2] = "WARN", t2[t2.ERROR = 3] = "ERROR";
})(h || (h = {}));
var v = ((p = {})[h.DEBUG] = "debug", p[h.INFO] = "log", p[h.WARN] = "warn", p[h.ERROR] = "error", p);
var S = system(function() {
  var t2 = statefulStream(h.ERROR);
  return { log: statefulStream(function(n2, o2, r3) {
    var i2;
    void 0 === r3 && (r3 = h.INFO), r3 >= (null != (i2 = ("undefined" == typeof globalThis ? window : globalThis).VIRTUOSO_LOG_LEVEL) ? i2 : getValue(t2)) && console[v[r3]]("%creact-virtuoso: %c%s %o", "color: #0253b3; font-weight: bold", "color: initial", n2, o2);
  }), logLevel: t2 };
}, [], { singleton: true });
function I(t2, e) {
  void 0 === e && (e = true);
  var n2 = (0, import_react28.useRef)(null), o2 = function(t3) {
  };
  if ("undefined" != typeof ResizeObserver) {
    var r3 = new ResizeObserver(function(e2) {
      var n3 = e2[0].target;
      null !== n3.offsetParent && t2(n3);
    });
    o2 = function(t3) {
      t3 && e ? (r3.observe(t3), n2.current = t3) : (n2.current && r3.unobserve(n2.current), n2.current = null);
    };
  }
  return { ref: n2, callbackRef: o2 };
}
function C(t2, e) {
  return void 0 === e && (e = true), I(t2, e).callbackRef;
}
function T(t2, e, n2, o2, r3, i2, a2) {
  return I(function(n3) {
    for (var l2 = (function(t3, e2, n4, o3) {
      var r4 = t3.length;
      if (0 === r4) return null;
      for (var i3 = [], a3 = 0; a3 < r4; a3++) {
        var l3 = t3.item(a3);
        if (l3 && void 0 !== l3.dataset.index) {
          var s3 = parseInt(l3.dataset.index), u4 = parseFloat(l3.dataset.knownSize), c3 = e2(l3, "offsetHeight");
          if (0 === c3 && o3("Zero-sized element, this should not happen", { child: l3 }, h.ERROR), c3 !== u4) {
            var m3 = i3[i3.length - 1];
            0 === i3.length || m3.size !== c3 || m3.endIndex !== s3 - 1 ? i3.push({ startIndex: s3, endIndex: s3, size: c3 }) : i3[i3.length - 1].endIndex++;
          }
        }
      }
      return i3;
    })(n3.children, e, 0, r3), s2 = n3.parentElement; !s2.dataset.virtuosoScroller; ) s2 = s2.parentElement;
    var u3 = "window" === s2.firstElementChild.dataset.viewportType, c2 = a2 ? a2.scrollTop : u3 ? window.pageYOffset || document.documentElement.scrollTop : s2.scrollTop, m2 = a2 ? a2.scrollHeight : u3 ? document.documentElement.scrollHeight : s2.scrollHeight, d2 = a2 ? a2.offsetHeight : u3 ? window.innerHeight : s2.offsetHeight;
    o2({ scrollTop: Math.max(c2, 0), scrollHeight: m2, viewportHeight: d2 }), null == i2 || i2((function(t3, e2, n4) {
      return "normal" === e2 || null != e2 && e2.endsWith("px") || n4("row-gap was not resolved to pixel value correctly", e2, h.WARN), "normal" === e2 ? 0 : parseInt(null != e2 ? e2 : "0", 10);
    })(0, getComputedStyle(n3).rowGap, r3)), null !== l2 && t2(l2);
  }, n2);
}
function w(t2, e) {
  return Math.round(t2.getBoundingClientRect()[e]);
}
function x(t2, e) {
  return Math.abs(t2 - e) < 1.01;
}
function b(t2, n2, o2, l2, s2) {
  void 0 === l2 && (l2 = noop10);
  var c2 = (0, import_react28.useRef)(null), m2 = (0, import_react28.useRef)(null), d2 = (0, import_react28.useRef)(null), f2 = (0, import_react28.useRef)(false), p2 = (0, import_react28.useCallback)(function(e) {
    var o3 = e.target, r3 = o3 === window || o3 === document, i2 = r3 ? window.pageYOffset || document.documentElement.scrollTop : o3.scrollTop, a2 = r3 ? document.documentElement.scrollHeight : o3.scrollHeight, l3 = r3 ? window.innerHeight : o3.offsetHeight, s3 = function() {
      t2({ scrollTop: Math.max(i2, 0), scrollHeight: a2, viewportHeight: l3 });
    };
    f2.current ? (0, import_react_dom3.flushSync)(s3) : s3(), f2.current = false, null !== m2.current && (i2 === m2.current || i2 <= 0 || i2 === a2 - l3) && (m2.current = null, n2(true), d2.current && (clearTimeout(d2.current), d2.current = null));
  }, [t2, n2]);
  return (0, import_react28.useEffect)(function() {
    var t3 = s2 || c2.current;
    return l2(s2 || c2.current), p2({ target: t3 }), t3.addEventListener("scroll", p2, { passive: true }), function() {
      l2(null), t3.removeEventListener("scroll", p2);
    };
  }, [c2, p2, o2, l2, s2]), { scrollerRef: c2, scrollByCallback: function(t3) {
    f2.current = true, c2.current.scrollBy(t3);
  }, scrollToCallback: function(e) {
    var o3 = c2.current;
    if (o3 && (!("offsetHeight" in o3) || 0 !== o3.offsetHeight)) {
      var r3, i2, a2, l3 = "smooth" === e.behavior;
      if (o3 === window ? (i2 = Math.max(w(document.documentElement, "height"), document.documentElement.scrollHeight), r3 = window.innerHeight, a2 = document.documentElement.scrollTop) : (i2 = o3.scrollHeight, r3 = w(o3, "height"), a2 = o3.scrollTop), e.top = Math.ceil(Math.max(Math.min(i2 - r3, e.top), 0)), x(r3, i2) || e.top === a2) return t2({ scrollTop: a2, scrollHeight: i2, viewportHeight: r3 }), void (l3 && n2(true));
      l3 ? (m2.current = e.top, d2.current && clearTimeout(d2.current), d2.current = setTimeout(function() {
        d2.current = null, m2.current = null, n2(true);
      }, 1e3)) : m2.current = null, o3.scrollTo(e);
    }
  } };
}
var y = system(function() {
  var t2 = stream(), n2 = stream(), o2 = statefulStream(0), r3 = stream(), i2 = statefulStream(0), a2 = stream(), l2 = stream(), s2 = statefulStream(0), u3 = statefulStream(0), c2 = statefulStream(0), m2 = statefulStream(0), d2 = stream(), f2 = stream(), p2 = statefulStream(false), h2 = statefulStream(false);
  return connect(pipe(t2, map3(function(t3) {
    return t3.scrollTop;
  })), n2), connect(pipe(t2, map3(function(t3) {
    return t3.scrollHeight;
  })), l2), connect(n2, i2), { scrollContainerState: t2, scrollTop: n2, viewportHeight: a2, headerHeight: s2, fixedHeaderHeight: u3, fixedFooterHeight: c2, footerHeight: m2, scrollHeight: l2, smoothScrollTargetReached: r3, react18ConcurrentRendering: h2, scrollTo: d2, scrollBy: f2, statefulScrollTop: i2, deviation: o2, scrollingInProgress: p2 };
}, [], { singleton: true });
var H = { lvl: 0 };
function E(t2, e, n2, o2, r3) {
  return void 0 === o2 && (o2 = H), void 0 === r3 && (r3 = H), { k: t2, v: e, lvl: n2, l: o2, r: r3 };
}
function R(t2) {
  return t2 === H;
}
function L() {
  return H;
}
function k(t2, e) {
  if (R(t2)) return H;
  var n2 = t2.k, o2 = t2.l, r3 = t2.r;
  if (e === n2) {
    if (R(o2)) return r3;
    if (R(r3)) return o2;
    var i2 = O(o2);
    return A(V(t2, { k: i2[0], v: i2[1], l: M(o2) }));
  }
  return A(V(t2, e < n2 ? { l: k(o2, e) } : { r: k(r3, e) }));
}
function F(t2, e, n2) {
  if (void 0 === n2 && (n2 = "k"), R(t2)) return [-Infinity, void 0];
  if (t2[n2] === e) return [t2.k, t2.v];
  if (t2[n2] < e) {
    var o2 = F(t2.r, e, n2);
    return -Infinity === o2[0] ? [t2.k, t2.v] : o2;
  }
  return F(t2.l, e, n2);
}
function z(t2, e, n2) {
  return R(t2) ? E(e, n2, 1) : e === t2.k ? V(t2, { k: e, v: n2 }) : (function(t3) {
    return D(G(t3));
  })(V(t2, e < t2.k ? { l: z(t2.l, e, n2) } : { r: z(t2.r, e, n2) }));
}
function B(t2, e, n2) {
  if (R(t2)) return [];
  var o2 = t2.k, r3 = t2.v, i2 = t2.r, a2 = [];
  return o2 > e && (a2 = a2.concat(B(t2.l, e, n2))), o2 >= e && o2 <= n2 && a2.push({ k: o2, v: r3 }), o2 <= n2 && (a2 = a2.concat(B(i2, e, n2))), a2;
}
function P(t2) {
  return R(t2) ? [] : [].concat(P(t2.l), [{ k: t2.k, v: t2.v }], P(t2.r));
}
function O(t2) {
  return R(t2.r) ? [t2.k, t2.v] : O(t2.r);
}
function M(t2) {
  return R(t2.r) ? t2.l : A(V(t2, { r: M(t2.r) }));
}
function V(t2, e) {
  return E(void 0 !== e.k ? e.k : t2.k, void 0 !== e.v ? e.v : t2.v, void 0 !== e.lvl ? e.lvl : t2.lvl, void 0 !== e.l ? e.l : t2.l, void 0 !== e.r ? e.r : t2.r);
}
function U(t2) {
  return R(t2) || t2.lvl > t2.r.lvl;
}
function A(t2) {
  var e = t2.l, n2 = t2.r, o2 = t2.lvl;
  if (n2.lvl >= o2 - 1 && e.lvl >= o2 - 1) return t2;
  if (o2 > n2.lvl + 1) {
    if (U(e)) return G(V(t2, { lvl: o2 - 1 }));
    if (R(e) || R(e.r)) throw new Error("Unexpected empty nodes");
    return V(e.r, { l: V(e, { r: e.r.l }), r: V(t2, { l: e.r.r, lvl: o2 - 1 }), lvl: o2 });
  }
  if (U(t2)) return D(V(t2, { lvl: o2 - 1 }));
  if (R(n2) || R(n2.l)) throw new Error("Unexpected empty nodes");
  var r3 = n2.l, i2 = U(r3) ? n2.lvl - 1 : n2.lvl;
  return V(r3, { l: V(t2, { r: r3.l, lvl: o2 - 1 }), r: D(V(n2, { l: r3.r, lvl: i2 })), lvl: r3.lvl + 1 });
}
function W(t2, e, n2) {
  return R(t2) ? [] : N(B(t2, F(t2, e)[0], n2), function(t3) {
    return { index: t3.k, value: t3.v };
  });
}
function N(t2, e) {
  var n2 = t2.length;
  if (0 === n2) return [];
  for (var o2 = e(t2[0]), r3 = o2.index, i2 = o2.value, a2 = [], l2 = 1; l2 < n2; l2++) {
    var s2 = e(t2[l2]), u3 = s2.index, c2 = s2.value;
    a2.push({ start: r3, end: u3 - 1, value: i2 }), r3 = u3, i2 = c2;
  }
  return a2.push({ start: r3, end: Infinity, value: i2 }), a2;
}
function D(t2) {
  var e = t2.r, n2 = t2.lvl;
  return R(e) || R(e.r) || e.lvl !== n2 || e.r.lvl !== n2 ? t2 : V(e, { l: V(t2, { r: e.l }), lvl: n2 + 1 });
}
function G(t2) {
  var e = t2.l;
  return R(e) || e.lvl !== t2.lvl ? t2 : V(e, { r: V(t2, { l: e.r }) });
}
function _(t2, e, n2, o2) {
  void 0 === o2 && (o2 = 0);
  for (var r3 = t2.length - 1; o2 <= r3; ) {
    var i2 = Math.floor((o2 + r3) / 2), a2 = n2(t2[i2], e);
    if (0 === a2) return i2;
    if (-1 === a2) {
      if (r3 - o2 < 2) return i2 - 1;
      r3 = i2 - 1;
    } else {
      if (r3 === o2) return i2;
      o2 = i2 + 1;
    }
  }
  throw new Error("Failed binary finding record in array - " + t2.join(",") + ", searched for " + e);
}
function j(t2, e, n2) {
  return t2[_(t2, e, n2)];
}
var K = system(function() {
  return { recalcInProgress: statefulStream(false) };
}, [], { singleton: true });
function Y(t2) {
  var e = t2.size, n2 = t2.startIndex, o2 = t2.endIndex;
  return function(t3) {
    return t3.start === n2 && (t3.end === o2 || Infinity === t3.end) && t3.value === e;
  };
}
function q(t2, e) {
  var n2 = t2.index;
  return e === n2 ? 0 : e < n2 ? -1 : 1;
}
function Z(t2, e) {
  var n2 = t2.offset;
  return e === n2 ? 0 : e < n2 ? -1 : 1;
}
function J(t2) {
  return { index: t2.index, value: t2 };
}
function $(t2, e, n2, o2) {
  var r3 = t2, i2 = 0, a2 = 0, l2 = 0, s2 = 0;
  if (0 !== e) {
    l2 = r3[s2 = _(r3, e - 1, q)].offset;
    var u3 = F(n2, e - 1);
    i2 = u3[0], a2 = u3[1], r3.length && r3[s2].size === F(n2, e)[1] && (s2 -= 1), r3 = r3.slice(0, s2 + 1);
  } else r3 = [];
  for (var c2, m2 = f(W(n2, e, Infinity)); !(c2 = m2()).done; ) {
    var d2 = c2.value, p2 = d2.start, h2 = d2.value, g2 = p2 - i2, v2 = g2 * a2 + l2 + g2 * o2;
    r3.push({ offset: v2, size: h2, index: p2 }), i2 = p2, l2 = v2, a2 = h2;
  }
  return { offsetTree: r3, lastIndex: i2, lastOffset: l2, lastSize: a2 };
}
function Q(t2, e) {
  var n2 = e[0], o2 = e[1], r3 = e[3];
  n2.length > 0 && (0, e[2])("received item sizes", n2, h.DEBUG);
  var i2 = t2.sizeTree, a2 = i2, l2 = 0;
  if (o2.length > 0 && R(i2) && 2 === n2.length) {
    var s2 = n2[0].size, u3 = n2[1].size;
    a2 = o2.reduce(function(t3, e2) {
      return z(z(t3, e2, s2), e2 + 1, u3);
    }, a2);
  } else {
    var c2 = (function(t3, e2) {
      for (var n3, o3 = R(t3) ? 0 : Infinity, r4 = f(e2); !(n3 = r4()).done; ) {
        var i3 = n3.value, a3 = i3.size, l3 = i3.startIndex, s3 = i3.endIndex;
        if (o3 = Math.min(o3, l3), R(t3)) t3 = z(t3, 0, a3);
        else {
          var u4 = W(t3, l3 - 1, s3 + 1);
          if (!u4.some(Y(i3))) {
            for (var c3, m3 = false, d3 = false, p2 = f(u4); !(c3 = p2()).done; ) {
              var h2 = c3.value, g2 = h2.start, v2 = h2.end, S2 = h2.value;
              m3 ? (s3 >= g2 || a3 === S2) && (t3 = k(t3, g2)) : (d3 = S2 !== a3, m3 = true), v2 > s3 && s3 >= g2 && S2 !== a3 && (t3 = z(t3, s3 + 1, S2));
            }
            d3 && (t3 = z(t3, l3, a3));
          }
        }
      }
      return [t3, o3];
    })(a2, n2);
    a2 = c2[0], l2 = c2[1];
  }
  if (a2 === i2) return t2;
  var m2 = $(t2.offsetTree, l2, a2, r3), d2 = m2.offsetTree;
  return { sizeTree: a2, offsetTree: d2, lastIndex: m2.lastIndex, lastOffset: m2.lastOffset, lastSize: m2.lastSize, groupOffsetTree: o2.reduce(function(t3, e2) {
    return z(t3, e2, X(e2, d2, r3));
  }, L()), groupIndices: o2 };
}
function X(t2, e, n2) {
  if (0 === e.length) return 0;
  var o2 = j(e, t2, q), r3 = t2 - o2.index, i2 = o2.size * r3 + (r3 - 1) * n2 + o2.offset;
  return i2 > 0 ? i2 + n2 : i2;
}
function tt(t2, e, n2) {
  if ((function(t3) {
    return void 0 !== t3.groupIndex;
  })(t2)) return e.groupIndices[t2.groupIndex] + 1;
  var o2 = et("LAST" === t2.index ? n2 : t2.index, e);
  return Math.max(0, o2, Math.min(n2, o2));
}
function et(t2, e) {
  if (!nt(e)) return t2;
  for (var n2 = 0; e.groupIndices[n2] <= t2 + n2; ) n2++;
  return t2 + n2;
}
function nt(t2) {
  return !R(t2.groupOffsetTree);
}
var ot = { offsetHeight: "height", offsetWidth: "width" };
var rt = system(function(t2) {
  var n2 = t2[0].log, o2 = t2[1].recalcInProgress, r3 = stream(), i2 = stream(), a2 = statefulStreamFromEmitter(i2, 0), l2 = stream(), s2 = stream(), u3 = statefulStream(0), m2 = statefulStream([]), d2 = statefulStream(void 0), f2 = statefulStream(void 0), p2 = statefulStream(function(t3, e) {
    return w(t3, ot[e]);
  }), g2 = statefulStream(void 0), v2 = statefulStream(0), S2 = { offsetTree: [], sizeTree: L(), groupOffsetTree: L(), lastIndex: 0, lastOffset: 0, lastSize: 0, groupIndices: [] }, I2 = statefulStreamFromEmitter(pipe(r3, withLatestFrom(m2, n2, v2), scan(Q, S2), distinctUntilChanged()), S2);
  connect(pipe(m2, filter(function(t3) {
    return t3.length > 0;
  }), withLatestFrom(I2, v2), map3(function(t3) {
    var e = t3[0], n3 = t3[1], o3 = t3[2], r4 = e.reduce(function(t4, e2, r5) {
      return z(t4, e2, X(e2, n3.offsetTree, o3) || r5);
    }, L());
    return c({}, n3, { groupIndices: e, groupOffsetTree: r4 });
  })), I2), connect(pipe(i2, withLatestFrom(I2), filter(function(t3) {
    return t3[0] < t3[1].lastIndex;
  }), map3(function(t3) {
    var e = t3[1];
    return [{ startIndex: t3[0], endIndex: e.lastIndex, size: e.lastSize }];
  })), r3), connect(d2, f2);
  var C2 = statefulStreamFromEmitter(pipe(d2, map3(function(t3) {
    return void 0 === t3;
  })), true);
  connect(pipe(f2, filter(function(t3) {
    return void 0 !== t3 && R(getValue(I2).sizeTree);
  }), map3(function(t3) {
    return [{ startIndex: 0, endIndex: 0, size: t3 }];
  })), r3);
  var T2 = streamFromEmitter(pipe(r3, withLatestFrom(I2), scan(function(t3, e) {
    var n3 = e[1];
    return { changed: n3 !== t3.sizes, sizes: n3 };
  }, { changed: false, sizes: S2 }), map3(function(t3) {
    return t3.changed;
  })));
  subscribe(pipe(u3, scan(function(t3, e) {
    return { diff: t3.prev - e, prev: e };
  }, { diff: 0, prev: 0 }), map3(function(t3) {
    return t3.diff;
  })), function(t3) {
    t3 > 0 ? (publish(o2, true), publish(l2, t3)) : t3 < 0 && publish(s2, t3);
  }), subscribe(pipe(u3, withLatestFrom(n2)), function(t3) {
    t3[0] < 0 && (0, t3[1])("`firstItemIndex` prop should not be set to less than zero. If you don't know the total count, just use a very high value", { firstItemIndex: u3 }, h.ERROR);
  });
  var x2 = streamFromEmitter(l2);
  connect(pipe(l2, withLatestFrom(I2), map3(function(t3) {
    var e = t3[0], n3 = t3[1];
    if (n3.groupIndices.length > 0) throw new Error("Virtuoso: prepending items does not work with groups");
    return P(n3.sizeTree).reduce(function(t4, n4) {
      var o3 = n4.k, r4 = n4.v;
      return { ranges: [].concat(t4.ranges, [{ startIndex: t4.prevIndex, endIndex: o3 + e - 1, size: t4.prevSize }]), prevIndex: o3 + e, prevSize: r4 };
    }, { ranges: [], prevIndex: 0, prevSize: n3.lastSize }).ranges;
  })), r3);
  var b2 = streamFromEmitter(pipe(s2, withLatestFrom(I2, v2), map3(function(t3) {
    return X(-t3[0], t3[1].offsetTree, t3[2]);
  })));
  return connect(pipe(s2, withLatestFrom(I2, v2), map3(function(t3) {
    var e = t3[0], n3 = t3[1], o3 = t3[2];
    if (n3.groupIndices.length > 0) throw new Error("Virtuoso: shifting items does not work with groups");
    var r4 = P(n3.sizeTree).reduce(function(t4, n4) {
      var o4 = n4.v;
      return z(t4, Math.max(0, n4.k + e), o4);
    }, L());
    return c({}, n3, { sizeTree: r4 }, $(n3.offsetTree, 0, r4, o3));
  })), I2), { data: g2, totalCount: i2, sizeRanges: r3, groupIndices: m2, defaultItemSize: f2, fixedItemSize: d2, unshiftWith: l2, shiftWith: s2, shiftWithOffset: b2, beforeUnshiftWith: x2, firstItemIndex: u3, gap: v2, sizes: I2, listRefresh: T2, statefulTotalCount: a2, trackItemSizes: C2, itemSize: p2 };
}, tup(S, K), { singleton: true });
var it = "undefined" != typeof document && "scrollBehavior" in document.documentElement.style;
function at(t2) {
  var e = "number" == typeof t2 ? { index: t2 } : t2;
  return e.align || (e.align = "start"), e.behavior && it || (e.behavior = "auto"), e.offset || (e.offset = 0), e;
}
var lt = system(function(t2) {
  var n2 = t2[0], o2 = n2.sizes, r3 = n2.totalCount, i2 = n2.listRefresh, a2 = n2.gap, l2 = t2[1], s2 = l2.scrollingInProgress, u3 = l2.viewportHeight, c2 = l2.scrollTo, m2 = l2.smoothScrollTargetReached, d2 = l2.headerHeight, f2 = l2.footerHeight, p2 = l2.fixedHeaderHeight, g2 = l2.fixedFooterHeight, v2 = t2[2].log, S2 = stream(), I2 = statefulStream(0), C2 = null, T2 = null, w2 = null;
  function x2() {
    C2 && (C2(), C2 = null), w2 && (w2(), w2 = null), T2 && (clearTimeout(T2), T2 = null), publish(s2, false);
  }
  return connect(pipe(S2, withLatestFrom(o2, u3, r3, I2, d2, f2, v2), withLatestFrom(a2, p2, g2), map3(function(t3) {
    var n3 = t3[0], o3 = n3[0], r4 = n3[1], a3 = n3[2], l3 = n3[3], u4 = n3[4], c3 = n3[5], d3 = n3[6], f3 = n3[7], p3 = t3[1], g3 = t3[2], v3 = t3[3], I3 = at(o3), b2 = I3.align, y2 = I3.behavior, H2 = I3.offset, E2 = l3 - 1, R2 = tt(I3, r4, E2), L2 = X(R2, r4.offsetTree, p3) + c3;
    "end" === b2 ? (L2 += g3 + F(r4.sizeTree, R2)[1] - a3 + v3, R2 === E2 && (L2 += d3)) : "center" === b2 ? L2 += (g3 + F(r4.sizeTree, R2)[1] - a3 + v3) / 2 : L2 -= u4, H2 && (L2 += H2);
    var k2 = function(t4) {
      x2(), t4 ? (f3("retrying to scroll to", { location: o3 }, h.DEBUG), publish(S2, o3)) : f3("list did not change, scroll successful", {}, h.DEBUG);
    };
    if (x2(), "smooth" === y2) {
      var z2 = false;
      w2 = subscribe(i2, function(t4) {
        z2 = z2 || t4;
      }), C2 = handleNext(m2, function() {
        k2(z2);
      });
    } else C2 = handleNext(pipe(i2, function(t4) {
      var e = setTimeout(function() {
        t4(false);
      }, 150);
      return function(n4) {
        n4 && (t4(true), clearTimeout(e));
      };
    }), k2);
    return T2 = setTimeout(function() {
      x2();
    }, 1200), publish(s2, true), f3("scrolling from index to", { index: R2, top: L2, behavior: y2 }, h.DEBUG), { top: L2, behavior: y2 };
  })), c2), { scrollToIndex: S2, topListHeight: I2 };
}, tup(rt, y, S), { singleton: true });
var st = "up";
var ut = { atBottom: false, notAtBottomBecause: "NOT_SHOWING_LAST_ITEM", state: { offsetBottom: 0, scrollTop: 0, viewportHeight: 0, scrollHeight: 0 } };
var ct = system(function(t2) {
  var n2 = t2[0], o2 = n2.scrollContainerState, r3 = n2.scrollTop, i2 = n2.viewportHeight, a2 = n2.headerHeight, l2 = n2.footerHeight, s2 = n2.scrollBy, u3 = statefulStream(false), c2 = statefulStream(true), m2 = stream(), d2 = stream(), f2 = statefulStream(4), p2 = statefulStream(0), h2 = statefulStreamFromEmitter(pipe(merge2(pipe(duc(r3), skip(1), mapTo(true)), pipe(duc(r3), skip(1), mapTo(false), debounceTime(100))), distinctUntilChanged()), false), g2 = statefulStreamFromEmitter(pipe(merge2(pipe(s2, mapTo(true)), pipe(s2, mapTo(false), debounceTime(200))), distinctUntilChanged()), false);
  connect(pipe(combineLatest(duc(r3), duc(p2)), map3(function(t3) {
    return t3[0] <= t3[1];
  }), distinctUntilChanged()), c2), connect(pipe(c2, throttleTime(50)), d2);
  var v2 = streamFromEmitter(pipe(combineLatest(o2, duc(i2), duc(a2), duc(l2), duc(f2)), scan(function(t3, e) {
    var n3, o3, r4 = e[0], i3 = r4.scrollTop, a3 = r4.scrollHeight, l3 = e[1], s3 = { viewportHeight: l3, scrollTop: i3, scrollHeight: a3 };
    return i3 + l3 - a3 > -e[4] ? (i3 > t3.state.scrollTop ? (n3 = "SCROLLED_DOWN", o3 = t3.state.scrollTop - i3) : (n3 = "SIZE_DECREASED", o3 = t3.state.scrollTop - i3 || t3.scrollTopDelta), { atBottom: true, state: s3, atBottomBecause: n3, scrollTopDelta: o3 }) : { atBottom: false, notAtBottomBecause: s3.scrollHeight > t3.state.scrollHeight ? "SIZE_INCREASED" : l3 < t3.state.viewportHeight ? "VIEWPORT_HEIGHT_DECREASING" : i3 < t3.state.scrollTop ? "SCROLLING_UPWARDS" : "NOT_FULLY_SCROLLED_TO_LAST_ITEM_BOTTOM", state: s3 };
  }, ut), distinctUntilChanged(function(t3, e) {
    return t3 && t3.atBottom === e.atBottom;
  }))), S2 = statefulStreamFromEmitter(pipe(o2, scan(function(t3, e) {
    var n3 = e.scrollTop, o3 = e.scrollHeight, r4 = e.viewportHeight;
    return x(t3.scrollHeight, o3) ? { scrollTop: n3, scrollHeight: o3, jump: 0, changed: false } : t3.scrollTop !== n3 && o3 - (n3 + r4) < 1 ? { scrollHeight: o3, scrollTop: n3, jump: t3.scrollTop - n3, changed: true } : { scrollHeight: o3, scrollTop: n3, jump: 0, changed: true };
  }, { scrollHeight: 0, jump: 0, scrollTop: 0, changed: false }), filter(function(t3) {
    return t3.changed;
  }), map3(function(t3) {
    return t3.jump;
  })), 0);
  connect(pipe(v2, map3(function(t3) {
    return t3.atBottom;
  })), u3), connect(pipe(u3, throttleTime(50)), m2);
  var I2 = statefulStream("down");
  connect(pipe(o2, map3(function(t3) {
    return t3.scrollTop;
  }), distinctUntilChanged(), scan(function(t3, n3) {
    return getValue(g2) ? { direction: t3.direction, prevScrollTop: n3 } : { direction: n3 < t3.prevScrollTop ? st : "down", prevScrollTop: n3 };
  }, { direction: "down", prevScrollTop: 0 }), map3(function(t3) {
    return t3.direction;
  })), I2), connect(pipe(o2, throttleTime(50), mapTo("none")), I2);
  var C2 = statefulStream(0);
  return connect(pipe(h2, filter(function(t3) {
    return !t3;
  }), mapTo(0)), C2), connect(pipe(r3, throttleTime(100), withLatestFrom(h2), filter(function(t3) {
    return !!t3[1];
  }), scan(function(t3, e) {
    return [t3[1], e[0]];
  }, [0, 0]), map3(function(t3) {
    return t3[1] - t3[0];
  })), C2), { isScrolling: h2, isAtTop: c2, isAtBottom: u3, atBottomState: v2, atTopStateChange: d2, atBottomStateChange: m2, scrollDirection: I2, atBottomThreshold: f2, atTopThreshold: p2, scrollVelocity: C2, lastJumpDueToItemResize: S2 };
}, tup(y));
var mt = system(function(t2) {
  var n2 = t2[0].log, o2 = statefulStream(false), r3 = streamFromEmitter(pipe(o2, filter(function(t3) {
    return t3;
  }), distinctUntilChanged()));
  return subscribe(o2, function(t3) {
    t3 && getValue(n2)("props updated", {}, h.DEBUG);
  }), { propsReady: o2, didMount: r3 };
}, tup(S), { singleton: true });
var dt = system(function(t2) {
  var n2 = t2[0], o2 = n2.sizes, r3 = n2.listRefresh, i2 = n2.defaultItemSize, a2 = t2[1].scrollTop, l2 = t2[2].scrollToIndex, s2 = t2[3].didMount, u3 = statefulStream(true), c2 = statefulStream(0);
  return connect(pipe(s2, withLatestFrom(c2), filter(function(t3) {
    return !!t3[1];
  }), mapTo(false)), u3), subscribe(pipe(combineLatest(r3, s2), withLatestFrom(u3, o2, i2), filter(function(t3) {
    var e = t3[1], n3 = t3[3];
    return t3[0][1] && (!R(t3[2].sizeTree) || void 0 !== n3) && !e;
  }), withLatestFrom(c2)), function(t3) {
    var n3 = t3[1];
    setTimeout(function() {
      handleNext(a2, function() {
        publish(u3, true);
      }), publish(l2, n3);
    });
  }), { scrolledToInitialItem: u3, initialTopMostItemIndex: c2 };
}, tup(rt, y, lt, mt), { singleton: true });
function ft(t2) {
  return !!t2 && ("smooth" === t2 ? "smooth" : "auto");
}
var pt = system(function(t2) {
  var n2 = t2[0], o2 = n2.totalCount, r3 = n2.listRefresh, i2 = t2[1], a2 = i2.isAtBottom, l2 = i2.atBottomState, s2 = t2[2].scrollToIndex, u3 = t2[3].scrolledToInitialItem, c2 = t2[4], m2 = c2.propsReady, d2 = c2.didMount, f2 = t2[5].log, p2 = t2[6].scrollingInProgress, g2 = statefulStream(false), v2 = stream(), S2 = null;
  function I2(t3) {
    publish(s2, { index: "LAST", align: "end", behavior: t3 });
  }
  function C2(t3) {
    var n3 = handleNext(l2, function(n4) {
      !t3 || n4.atBottom || "SIZE_INCREASED" !== n4.notAtBottomBecause || S2 || (getValue(f2)("scrolling to bottom due to increased size", {}, h.DEBUG), I2("auto"));
    });
    setTimeout(n3, 100);
  }
  return subscribe(pipe(combineLatest(pipe(duc(o2), skip(1)), d2), withLatestFrom(duc(g2), a2, u3, p2), map3(function(t3) {
    var e = t3[0], n3 = e[0], o3 = e[1] && t3[3], r4 = "auto";
    return o3 && (r4 = (function(t4, e2) {
      return "function" == typeof t4 ? ft(t4(e2)) : e2 && ft(t4);
    })(t3[1], t3[2] || t3[4]), o3 = o3 && !!r4), { totalCount: n3, shouldFollow: o3, followOutputBehavior: r4 };
  }), filter(function(t3) {
    return t3.shouldFollow;
  })), function(t3) {
    var n3 = t3.totalCount, o3 = t3.followOutputBehavior;
    S2 && (S2(), S2 = null), S2 = handleNext(r3, function() {
      getValue(f2)("following output to ", { totalCount: n3 }, h.DEBUG), I2(o3), S2 = null;
    });
  }), subscribe(pipe(combineLatest(duc(g2), o2, m2), filter(function(t3) {
    return t3[0] && t3[2];
  }), scan(function(t3, e) {
    var n3 = e[1];
    return { refreshed: t3.value === n3, value: n3 };
  }, { refreshed: false, value: 0 }), filter(function(t3) {
    return t3.refreshed;
  }), withLatestFrom(g2, o2)), function(t3) {
    C2(false !== t3[1]);
  }), subscribe(v2, function() {
    C2(false !== getValue(g2));
  }), subscribe(combineLatest(duc(g2), l2), function(t3) {
    var e = t3[1];
    t3[0] && !e.atBottom && "VIEWPORT_HEIGHT_DECREASING" === e.notAtBottomBecause && I2("auto");
  }), { followOutput: g2, autoscrollToBottom: v2 };
}, tup(rt, ct, lt, dt, mt, S, y));
function ht(t2) {
  return t2.reduce(function(t3, e) {
    return t3.groupIndices.push(t3.totalCount), t3.totalCount += e + 1, t3;
  }, { totalCount: 0, groupIndices: [] });
}
var gt = system(function(t2) {
  var n2 = t2[0], o2 = n2.totalCount, r3 = n2.groupIndices, i2 = n2.sizes, a2 = t2[1], l2 = a2.scrollTop, s2 = a2.headerHeight, u3 = stream(), c2 = stream(), m2 = streamFromEmitter(pipe(u3, map3(ht)));
  return connect(pipe(m2, map3(function(t3) {
    return t3.totalCount;
  })), o2), connect(pipe(m2, map3(function(t3) {
    return t3.groupIndices;
  })), r3), connect(pipe(combineLatest(l2, i2, s2), filter(function(t3) {
    return nt(t3[1]);
  }), map3(function(t3) {
    return F(t3[1].groupOffsetTree, Math.max(t3[0] - t3[2], 0), "v")[0];
  }), distinctUntilChanged(), map3(function(t3) {
    return [t3];
  })), c2), { groupCounts: u3, topItemsIndexes: c2 };
}, tup(rt, y));
function vt(t2, e) {
  return !(!t2 || t2[0] !== e[0] || t2[1] !== e[1]);
}
function St(t2, e) {
  return !(!t2 || t2.startIndex !== e.startIndex || t2.endIndex !== e.endIndex);
}
function It(t2, e, n2) {
  return "number" == typeof t2 ? n2 === st && "top" === e || "down" === n2 && "bottom" === e ? t2 : 0 : n2 === st ? "top" === e ? t2.main : t2.reverse : "bottom" === e ? t2.main : t2.reverse;
}
function Ct(t2, e) {
  return "number" == typeof t2 ? t2 : t2[e] || 0;
}
var Tt = system(function(t2) {
  var n2 = t2[0], o2 = n2.scrollTop, r3 = n2.viewportHeight, i2 = n2.deviation, a2 = n2.headerHeight, l2 = n2.fixedHeaderHeight, s2 = stream(), u3 = statefulStream(0), c2 = statefulStream(0), m2 = statefulStream(0), d2 = statefulStreamFromEmitter(pipe(combineLatest(duc(o2), duc(r3), duc(a2), duc(s2, vt), duc(m2), duc(u3), duc(l2), duc(i2), duc(c2)), map3(function(t3) {
    var e = t3[0], n3 = t3[1], o3 = t3[2], r4 = t3[3], i3 = r4[0], a3 = r4[1], l3 = t3[4], s3 = t3[6], u4 = t3[7], c3 = t3[8], m3 = e - u4, d3 = t3[5] + s3, f2 = Math.max(o3 - m3, 0), p2 = "none", h2 = Ct(c3, "top"), g2 = Ct(c3, "bottom");
    return i3 -= u4, a3 += o3 + s3, (i3 += o3 + s3) > e + d3 - h2 && (p2 = st), (a3 -= u4) < e - f2 + n3 + g2 && (p2 = "down"), "none" !== p2 ? [Math.max(m3 - o3 - It(l3, "top", p2) - h2, 0), m3 - f2 - s3 + n3 + It(l3, "bottom", p2) + g2] : null;
  }), filter(function(t3) {
    return null != t3;
  }), distinctUntilChanged(vt)), [0, 0]);
  return { listBoundary: s2, overscan: m2, topListHeight: u3, increaseViewportBy: c2, visibleRange: d2 };
}, tup(y), { singleton: true });
var wt = { items: [], topItems: [], offsetTop: 0, offsetBottom: 0, top: 0, bottom: 0, topListHeight: 0, totalCount: 0, firstItemIndex: 0 };
function xt(t2, e, n2) {
  if (0 === t2.length) return [];
  if (!nt(e)) return t2.map(function(t3) {
    return c({}, t3, { index: t3.index + n2, originalIndex: t3.index });
  });
  for (var o2, r3 = [], i2 = W(e.groupOffsetTree, t2[0].index, t2[t2.length - 1].index), a2 = void 0, l2 = 0, s2 = f(t2); !(o2 = s2()).done; ) {
    var u3 = o2.value;
    (!a2 || a2.end < u3.index) && (a2 = i2.shift(), l2 = e.groupIndices.indexOf(a2.start)), r3.push(c({}, u3.index === a2.start ? { type: "group", index: l2 } : { index: u3.index - (l2 + 1) + n2, groupIndex: l2 }, { size: u3.size, offset: u3.offset, originalIndex: u3.index, data: u3.data }));
  }
  return r3;
}
function bt(t2, e, n2, o2, r3, i2) {
  var a2 = 0, l2 = 0;
  if (t2.length > 0) {
    a2 = t2[0].offset;
    var s2 = t2[t2.length - 1];
    l2 = s2.offset + s2.size;
  }
  var u3 = n2 - r3.lastIndex, c2 = a2, m2 = r3.lastOffset + u3 * r3.lastSize + (u3 - 1) * o2 - l2;
  return { items: xt(t2, r3, i2), topItems: xt(e, r3, i2), topListHeight: e.reduce(function(t3, e2) {
    return e2.size + t3;
  }, 0), offsetTop: a2, offsetBottom: m2, top: c2, bottom: l2, totalCount: n2, firstItemIndex: i2 };
}
var yt = system(function(t2) {
  var n2 = t2[0], o2 = n2.sizes, r3 = n2.totalCount, i2 = n2.data, a2 = n2.firstItemIndex, l2 = n2.gap, s2 = t2[1], u3 = t2[2], m2 = u3.visibleRange, d2 = u3.listBoundary, p2 = u3.topListHeight, h2 = t2[3], g2 = h2.scrolledToInitialItem, v2 = h2.initialTopMostItemIndex, S2 = t2[4].topListHeight, I2 = t2[5], C2 = t2[6].didMount, T2 = t2[7].recalcInProgress, w2 = statefulStream([]), x2 = stream();
  connect(s2.topItemsIndexes, w2);
  var b2 = statefulStreamFromEmitter(pipe(combineLatest(C2, T2, duc(m2, vt), duc(r3), duc(o2), duc(v2), g2, duc(w2), duc(a2), duc(l2), i2), filter(function(t3) {
    return t3[0] && !t3[1];
  }), map3(function(t3) {
    var n3 = t3[2], o3 = n3[0], r4 = n3[1], i3 = t3[3], a3 = t3[5], l3 = t3[6], s3 = t3[7], u4 = t3[8], m3 = t3[9], d3 = t3[10], p3 = t3[4], h3 = p3.sizeTree, g3 = p3.offsetTree;
    if (0 === i3 || 0 === o3 && 0 === r4) return c({}, wt, { totalCount: i3 });
    if (R(h3)) return bt((function(t4, e, n4) {
      if (nt(e)) {
        var o4 = et(t4, e);
        return [{ index: F(e.groupOffsetTree, o4)[0], size: 0, offset: 0 }, { index: o4, size: 0, offset: 0, data: n4 && n4[0] }];
      }
      return [{ index: t4, size: 0, offset: 0, data: n4 && n4[0] }];
    })((function(t4, e) {
      return "number" == typeof t4 ? t4 : "LAST" === t4.index ? e - 1 : t4.index;
    })(a3, i3), p3, d3), [], i3, m3, p3, u4);
    var v3 = [];
    if (s3.length > 0) for (var S3, I3 = s3[0], C3 = s3[s3.length - 1], T3 = 0, w3 = f(W(h3, I3, C3)); !(S3 = w3()).done; ) for (var x3 = S3.value, b3 = x3.value, y2 = Math.max(x3.start, I3), H2 = Math.min(x3.end, C3), E2 = y2; E2 <= H2; E2++) v3.push({ index: E2, size: b3, offset: T3, data: d3 && d3[E2] }), T3 += b3;
    if (!l3) return bt([], v3, i3, m3, p3, u4);
    var L2 = s3.length > 0 ? s3[s3.length - 1] + 1 : 0, k2 = (function(t4, e, n4, o4) {
      return void 0 === o4 && (o4 = 0), o4 > 0 && (e = Math.max(e, j(t4, o4, q).offset)), N((i4 = n4, l4 = _(r5 = t4, e, a4 = Z), s4 = _(r5, i4, a4, l4), r5.slice(l4, s4 + 1)), J);
      var r5, i4, a4, l4, s4;
    })(g3, o3, r4, L2);
    if (0 === k2.length) return null;
    var z2 = i3 - 1;
    return bt(tap([], function(t4) {
      for (var e, n4 = f(k2); !(e = n4()).done; ) {
        var i4 = e.value, a4 = i4.value, l4 = a4.offset, s4 = i4.start, u5 = a4.size;
        if (a4.offset < o3) {
          var c2 = (s4 += Math.floor((o3 - a4.offset + m3) / (u5 + m3))) - i4.start;
          l4 += c2 * u5 + c2 * m3;
        }
        s4 < L2 && (l4 += (L2 - s4) * u5, s4 = L2);
        for (var p4 = Math.min(i4.end, z2), h4 = s4; h4 <= p4 && !(l4 >= r4); h4++) t4.push({ index: h4, size: u5, offset: l4, data: d3 && d3[h4] }), l4 += u5 + m3;
      }
    }), v3, i3, m3, p3, u4);
  }), filter(function(t3) {
    return null !== t3;
  }), distinctUntilChanged()), wt);
  return connect(pipe(i2, filter(function(t3) {
    return void 0 !== t3;
  }), map3(function(t3) {
    return t3.length;
  })), r3), connect(pipe(b2, map3(function(t3) {
    return t3.topListHeight;
  })), S2), connect(S2, p2), connect(pipe(b2, map3(function(t3) {
    return [t3.top, t3.bottom];
  })), d2), connect(pipe(b2, map3(function(t3) {
    return t3.items;
  })), x2), c({ listState: b2, topItemsIndexes: w2, endReached: streamFromEmitter(pipe(b2, filter(function(t3) {
    return t3.items.length > 0;
  }), withLatestFrom(r3, i2), filter(function(t3) {
    var e = t3[0].items;
    return e[e.length - 1].originalIndex === t3[1] - 1;
  }), map3(function(t3) {
    return [t3[1] - 1, t3[2]];
  }), distinctUntilChanged(vt), map3(function(t3) {
    return t3[0];
  }))), startReached: streamFromEmitter(pipe(b2, throttleTime(200), filter(function(t3) {
    var e = t3.items;
    return e.length > 0 && e[0].originalIndex === t3.topItems.length;
  }), map3(function(t3) {
    return t3.items[0].index;
  }), distinctUntilChanged())), rangeChanged: streamFromEmitter(pipe(b2, filter(function(t3) {
    return t3.items.length > 0;
  }), map3(function(t3) {
    for (var e = t3.items, n3 = 0, o3 = e.length - 1; "group" === e[n3].type && n3 < o3; ) n3++;
    for (; "group" === e[o3].type && o3 > n3; ) o3--;
    return { startIndex: e[n3].index, endIndex: e[o3].index };
  }), distinctUntilChanged(St))), itemsRendered: x2 }, I2);
}, tup(rt, gt, Tt, dt, lt, ct, mt, K), { singleton: true });
var Ht = system(function(t2) {
  var n2 = t2[0], o2 = n2.sizes, r3 = n2.firstItemIndex, i2 = n2.data, a2 = n2.gap, l2 = t2[1].listState, s2 = t2[2].didMount, u3 = statefulStream(0);
  return connect(pipe(s2, withLatestFrom(u3), filter(function(t3) {
    return 0 !== t3[1];
  }), withLatestFrom(o2, r3, a2, i2), map3(function(t3) {
    var e = t3[0][1], n3 = t3[1], o3 = t3[2], r4 = t3[3], i3 = t3[4], a3 = void 0 === i3 ? [] : i3, l3 = 0;
    if (n3.groupIndices.length > 0) for (var s3, u4 = f(n3.groupIndices); !((s3 = u4()).done || s3.value - l3 >= e); ) l3++;
    var c2 = e + l3;
    return bt(Array.from({ length: c2 }).map(function(t4, e2) {
      return { index: e2, size: 0, offset: 0, data: a3[e2] };
    }), [], c2, r4, n3, o3);
  })), l2), { initialItemCount: u3 };
}, tup(rt, yt, mt), { singleton: true });
var Et = system(function(t2) {
  var n2 = t2[0].scrollVelocity, o2 = statefulStream(false), r3 = stream(), i2 = statefulStream(false);
  return connect(pipe(n2, withLatestFrom(i2, o2, r3), filter(function(t3) {
    return !!t3[1];
  }), map3(function(t3) {
    var e = t3[0], n3 = t3[1], o3 = t3[2], r4 = t3[3], i3 = n3.enter;
    if (o3) {
      if ((0, n3.exit)(e, r4)) return false;
    } else if (i3(e, r4)) return true;
    return o3;
  }), distinctUntilChanged()), o2), subscribe(pipe(combineLatest(o2, n2, r3), withLatestFrom(i2)), function(t3) {
    var e = t3[0], n3 = t3[1];
    return e[0] && n3 && n3.change && n3.change(e[1], e[2]);
  }), { isSeeking: o2, scrollSeekConfiguration: i2, scrollVelocity: n2, scrollSeekRangeChanged: r3 };
}, tup(ct), { singleton: true });
var Rt = system(function(t2) {
  var n2 = t2[0].topItemsIndexes, o2 = statefulStream(0);
  return connect(pipe(o2, filter(function(t3) {
    return t3 > 0;
  }), map3(function(t3) {
    return Array.from({ length: t3 }).map(function(t4, e) {
      return e;
    });
  })), n2), { topItemCount: o2 };
}, tup(yt));
var Lt = system(function(t2) {
  var n2 = t2[0], o2 = n2.footerHeight, r3 = n2.headerHeight, i2 = n2.fixedHeaderHeight, a2 = n2.fixedFooterHeight, l2 = t2[1].listState, s2 = stream(), u3 = statefulStreamFromEmitter(pipe(combineLatest(o2, a2, r3, i2, l2), map3(function(t3) {
    var e = t3[4];
    return t3[0] + t3[1] + t3[2] + t3[3] + e.offsetBottom + e.bottom;
  })), 0);
  return connect(duc(u3), s2), { totalListHeight: u3, totalListHeightChanged: s2 };
}, tup(y, yt), { singleton: true });
function kt(t2) {
  var e, n2 = false;
  return function() {
    return n2 || (n2 = true, e = t2()), e;
  };
}
var Ft = kt(function() {
  return /iP(ad|hone|od).+Version\/[\d.]+.*Safari/i.test(navigator.userAgent);
});
var zt = system(function(t2) {
  var n2 = t2[0], o2 = n2.scrollBy, r3 = n2.scrollTop, i2 = n2.deviation, a2 = n2.scrollingInProgress, l2 = t2[1], s2 = l2.isScrolling, u3 = l2.isAtBottom, c2 = l2.scrollDirection, m2 = t2[3], d2 = m2.beforeUnshiftWith, f2 = m2.shiftWithOffset, p2 = m2.sizes, g2 = m2.gap, v2 = t2[4].log, S2 = t2[5].recalcInProgress, I2 = streamFromEmitter(pipe(t2[2].listState, withLatestFrom(l2.lastJumpDueToItemResize), scan(function(t3, e) {
    var n3 = t3[1], o3 = e[0], r4 = o3.items, i3 = o3.totalCount, a3 = o3.bottom + o3.offsetBottom, l3 = 0;
    return t3[2] === i3 && n3.length > 0 && r4.length > 0 && (0 === r4[0].originalIndex && 0 === n3[0].originalIndex || 0 != (l3 = a3 - t3[3]) && (l3 += e[1])), [l3, r4, i3, a3];
  }, [0, [], 0, 0]), filter(function(t3) {
    return 0 !== t3[0];
  }), withLatestFrom(r3, c2, a2, u3, v2), filter(function(t3) {
    return !t3[3] && 0 !== t3[1] && t3[2] === st;
  }), map3(function(t3) {
    var e = t3[0][0];
    return (0, t3[5])("Upward scrolling compensation", { amount: e }, h.DEBUG), e;
  })));
  function C2(t3) {
    t3 > 0 ? (publish(o2, { top: -t3, behavior: "auto" }), publish(i2, 0)) : (publish(i2, 0), publish(o2, { top: -t3, behavior: "auto" }));
  }
  return subscribe(pipe(I2, withLatestFrom(i2, s2)), function(t3) {
    var n3 = t3[0], o3 = t3[1];
    t3[2] && Ft() ? publish(i2, o3 - n3) : C2(-n3);
  }), subscribe(pipe(combineLatest(statefulStreamFromEmitter(s2, false), i2, S2), filter(function(t3) {
    return !t3[0] && !t3[2] && 0 !== t3[1];
  }), map3(function(t3) {
    return t3[1];
  }), throttleTime(1)), C2), connect(pipe(f2, map3(function(t3) {
    return { top: -t3 };
  })), o2), subscribe(pipe(d2, withLatestFrom(p2, g2), map3(function(t3) {
    var e = t3[0];
    return e * t3[1].lastSize + e * t3[2];
  })), function(t3) {
    publish(i2, t3), requestAnimationFrame(function() {
      publish(o2, { top: t3 }), requestAnimationFrame(function() {
        publish(i2, 0), publish(S2, false);
      });
    });
  }), { deviation: i2 };
}, tup(y, ct, yt, rt, S, K));
var Bt = system(function(t2) {
  var n2 = t2[0].totalListHeight, o2 = t2[1].didMount, r3 = t2[2].scrollTo, i2 = statefulStream(0);
  return subscribe(pipe(o2, withLatestFrom(i2), filter(function(t3) {
    return 0 !== t3[1];
  }), map3(function(t3) {
    return { top: t3[1] };
  })), function(t3) {
    handleNext(pipe(n2, filter(function(t4) {
      return 0 !== t4;
    })), function() {
      setTimeout(function() {
        publish(r3, t3);
      });
    });
  }), { initialScrollTop: i2 };
}, tup(Lt, mt, y), { singleton: true });
var Pt = system(function(t2) {
  var n2 = t2[0].viewportHeight, o2 = t2[1].totalListHeight, r3 = statefulStream(false);
  return { alignToBottom: r3, paddingTopAddition: statefulStreamFromEmitter(pipe(combineLatest(r3, n2, o2), filter(function(t3) {
    return t3[0];
  }), map3(function(t3) {
    return Math.max(0, t3[1] - t3[2]);
  }), distinctUntilChanged()), 0) };
}, tup(y, Lt), { singleton: true });
var Ot = system(function(t2) {
  var n2 = t2[0], o2 = n2.scrollTo, r3 = n2.scrollContainerState, i2 = stream(), a2 = stream(), l2 = stream(), s2 = statefulStream(false), u3 = statefulStream(void 0);
  return connect(pipe(combineLatest(i2, a2), map3(function(t3) {
    var e = t3[0], n3 = e.viewportHeight, o3 = e.scrollHeight;
    return { scrollTop: Math.max(0, e.scrollTop - t3[1].offsetTop), scrollHeight: o3, viewportHeight: n3 };
  })), r3), connect(pipe(o2, withLatestFrom(a2), map3(function(t3) {
    var e = t3[0];
    return c({}, e, { top: e.top + t3[1].offsetTop });
  })), l2), { useWindowScroll: s2, customScrollParent: u3, windowScrollContainerState: i2, windowViewportRect: a2, windowScrollTo: l2 };
}, tup(y));
var Mt = ["done", "behavior", "align"];
var Vt = system(function(t2) {
  var n2 = t2[0], o2 = n2.sizes, r3 = n2.totalCount, i2 = n2.gap, a2 = t2[1], l2 = a2.scrollTop, s2 = a2.viewportHeight, u3 = a2.headerHeight, d2 = a2.fixedHeaderHeight, f2 = a2.fixedFooterHeight, p2 = a2.scrollingInProgress, h2 = t2[2].scrollToIndex, g2 = stream();
  return connect(pipe(g2, withLatestFrom(o2, s2, r3, u3, d2, f2, l2), withLatestFrom(i2), map3(function(t3) {
    var n3 = t3[0], o3 = n3[0], r4 = n3[1], i3 = n3[2], a3 = n3[3], l3 = n3[4], s3 = n3[5], u4 = n3[6], d3 = n3[7], f3 = t3[1], h3 = o3.done, g3 = o3.behavior, v2 = o3.align, S2 = m(o3, Mt), I2 = null, C2 = tt(o3, r4, a3 - 1), T2 = X(C2, r4.offsetTree, f3) + l3 + s3;
    return T2 < d3 + s3 ? I2 = c({}, S2, { behavior: g3, align: null != v2 ? v2 : "start" }) : T2 + F(r4.sizeTree, C2)[1] > d3 + i3 - u4 && (I2 = c({}, S2, { behavior: g3, align: null != v2 ? v2 : "end" })), I2 ? h3 && handleNext(pipe(p2, skip(1), filter(function(t4) {
      return false === t4;
    })), h3) : h3 && h3(), I2;
  }), filter(function(t3) {
    return null !== t3;
  })), h2), { scrollIntoView: g2 };
}, tup(rt, y, lt, yt, S), { singleton: true });
var Ut = ["listState", "topItemsIndexes"];
var At = system(function(t2) {
  return c({}, t2[0], t2[1], t2[2], t2[3], t2[4], t2[5], t2[6], t2[7], t2[8]);
}, tup(Tt, Ht, mt, Et, Lt, Bt, Pt, Ot, Vt));
var Wt = system(function(t2) {
  var n2 = t2[0], o2 = n2.totalCount, r3 = n2.sizeRanges, i2 = n2.fixedItemSize, a2 = n2.defaultItemSize, l2 = n2.trackItemSizes, s2 = n2.itemSize, u3 = n2.data, d2 = n2.firstItemIndex, f2 = n2.groupIndices, p2 = n2.statefulTotalCount, h2 = n2.gap, g2 = t2[1], v2 = g2.initialTopMostItemIndex, S2 = g2.scrolledToInitialItem, I2 = t2[2], C2 = t2[3], T2 = t2[4], w2 = T2.listState, x2 = T2.topItemsIndexes, b2 = m(T2, Ut), y2 = t2[5].scrollToIndex, H2 = t2[7].topItemCount, E2 = t2[8].groupCounts, R2 = t2[9], L2 = t2[10];
  return connect(b2.rangeChanged, R2.scrollSeekRangeChanged), connect(pipe(R2.windowViewportRect, map3(function(t3) {
    return t3.visibleHeight;
  })), I2.viewportHeight), c({ totalCount: o2, data: u3, firstItemIndex: d2, sizeRanges: r3, initialTopMostItemIndex: v2, scrolledToInitialItem: S2, topItemsIndexes: x2, topItemCount: H2, groupCounts: E2, fixedItemHeight: i2, defaultItemHeight: a2, gap: h2 }, C2, { statefulTotalCount: p2, listState: w2, scrollToIndex: y2, trackItemSizes: l2, itemSize: s2, groupIndices: f2 }, b2, R2, I2, L2);
}, tup(rt, dt, y, pt, yt, lt, zt, Rt, gt, At, S));
var Nt = kt(function() {
  if ("undefined" == typeof document) return "sticky";
  var t2 = document.createElement("div");
  return t2.style.position = "-webkit-sticky", "-webkit-sticky" === t2.style.position ? "-webkit-sticky" : "sticky";
});
function Dt(t2, e) {
  var n2 = (0, import_react28.useRef)(null), o2 = (0, import_react28.useCallback)(function(o3) {
    if (null !== o3 && o3.offsetParent) {
      var r3, i2, a2 = o3.getBoundingClientRect(), l3 = a2.width;
      if (e) {
        var s3 = e.getBoundingClientRect(), u4 = a2.top - s3.top;
        r3 = s3.height - Math.max(0, u4), i2 = u4 + e.scrollTop;
      } else r3 = window.innerHeight - Math.max(0, a2.top), i2 = a2.top + window.pageYOffset;
      n2.current = { offsetTop: i2, visibleHeight: r3, visibleWidth: l3 }, t2(n2.current);
    }
  }, [t2, e]), l2 = I(o2), s2 = l2.callbackRef, u3 = l2.ref, c2 = (0, import_react28.useCallback)(function() {
    o2(u3.current);
  }, [o2, u3]);
  return (0, import_react28.useEffect)(function() {
    if (e) {
      e.addEventListener("scroll", c2);
      var t3 = new ResizeObserver(c2);
      return t3.observe(e), function() {
        e.removeEventListener("scroll", c2), t3.unobserve(e);
      };
    }
    return window.addEventListener("scroll", c2), window.addEventListener("resize", c2), function() {
      window.removeEventListener("scroll", c2), window.removeEventListener("resize", c2);
    };
  }, [c2, e]), s2;
}
var Gt = n.createContext(void 0);
var _t = ["placeholder"];
var jt = ["style", "children"];
var Kt = ["style", "children"];
function Yt(t2) {
  return t2;
}
var qt = system(function() {
  var t2 = statefulStream(function(t3) {
    return "Item " + t3;
  }), n2 = statefulStream(null), o2 = statefulStream(function(t3) {
    return "Group " + t3;
  }), r3 = statefulStream({}), i2 = statefulStream(Yt), a2 = statefulStream("div"), l2 = statefulStream(noop10), s2 = function(t3, n3) {
    return void 0 === n3 && (n3 = null), statefulStreamFromEmitter(pipe(r3, map3(function(e) {
      return e[t3];
    }), distinctUntilChanged()), n3);
  };
  return { context: n2, itemContent: t2, groupContent: o2, components: r3, computeItemKey: i2, headerFooterTag: a2, scrollerRef: l2, FooterComponent: s2("Footer"), HeaderComponent: s2("Header"), TopItemListComponent: s2("TopItemList"), ListComponent: s2("List", "div"), ItemComponent: s2("Item", "div"), GroupComponent: s2("Group", "div"), ScrollerComponent: s2("Scroller", "div"), EmptyPlaceholder: s2("EmptyPlaceholder"), ScrollSeekPlaceholder: s2("ScrollSeekPlaceholder") };
});
function Zt(t2, n2) {
  var o2 = stream();
  return subscribe(o2, function() {
    return console.warn("react-virtuoso: You are using a deprecated property. " + n2, "color: red;", "color: inherit;", "color: blue;");
  }), connect(o2, t2), o2;
}
var Jt = system(function(t2) {
  var n2 = t2[0], o2 = t2[1], r3 = { item: Zt(o2.itemContent, "Rename the %citem%c prop to %citemContent."), group: Zt(o2.groupContent, "Rename the %cgroup%c prop to %cgroupContent."), topItems: Zt(n2.topItemCount, "Rename the %ctopItems%c prop to %ctopItemCount."), itemHeight: Zt(n2.fixedItemHeight, "Rename the %citemHeight%c prop to %cfixedItemHeight."), scrollingStateChange: Zt(n2.isScrolling, "Rename the %cscrollingStateChange%c prop to %cisScrolling."), adjustForPrependedItems: stream(), maxHeightCacheSize: stream(), footer: stream(), header: stream(), HeaderContainer: stream(), FooterContainer: stream(), ItemContainer: stream(), ScrollContainer: stream(), GroupContainer: stream(), ListContainer: stream(), emptyComponent: stream(), scrollSeek: stream() };
  function i2(t3, n3, r4) {
    connect(pipe(t3, withLatestFrom(o2.components), map3(function(t4) {
      var e, o3 = t4[0], i3 = t4[1];
      return console.warn("react-virtuoso: " + r4 + " property is deprecated. Pass components." + n3 + " instead."), c({}, i3, ((e = {})[n3] = o3, e));
    })), o2.components);
  }
  return subscribe(r3.adjustForPrependedItems, function() {
    console.warn("react-virtuoso: adjustForPrependedItems is no longer supported. Use the firstItemIndex property instead - https://virtuoso.dev/prepend-items.", "color: red;", "color: inherit;", "color: blue;");
  }), subscribe(r3.maxHeightCacheSize, function() {
    console.warn("react-virtuoso: maxHeightCacheSize is no longer necessary. Setting it has no effect - remove it from your code.");
  }), subscribe(r3.HeaderContainer, function() {
    console.warn("react-virtuoso: HeaderContainer is deprecated. Use headerFooterTag if you want to change the wrapper of the header component and pass components.Header to change its contents.");
  }), subscribe(r3.FooterContainer, function() {
    console.warn("react-virtuoso: FooterContainer is deprecated. Use headerFooterTag if you want to change the wrapper of the footer component and pass components.Footer to change its contents.");
  }), subscribe(r3.scrollSeek, function(t3) {
    var r4 = t3.placeholder, i3 = m(t3, _t);
    console.warn("react-virtuoso: scrollSeek property is deprecated. Pass scrollSeekConfiguration and specify the placeholder in components.ScrollSeekPlaceholder instead."), publish(o2.components, c({}, getValue(o2.components), { ScrollSeekPlaceholder: r4 })), publish(n2.scrollSeekConfiguration, i3);
  }), i2(r3.footer, "Footer", "footer"), i2(r3.header, "Header", "header"), i2(r3.ItemContainer, "Item", "ItemContainer"), i2(r3.ListContainer, "List", "ListContainer"), i2(r3.ScrollContainer, "Scroller", "ScrollContainer"), i2(r3.emptyComponent, "EmptyPlaceholder", "emptyComponent"), i2(r3.GroupContainer, "Group", "GroupContainer"), c({}, n2, o2, r3);
}, tup(Wt, qt));
var $t = function(t2) {
  return n.createElement("div", { style: { height: t2.height } });
};
var Qt = { position: Nt(), zIndex: 1, overflowAnchor: "none" };
var Xt = { overflowAnchor: "none" };
var te = n.memo(function(t2) {
  var o2 = t2.showTopList, r3 = void 0 !== o2 && o2, i2 = he("listState"), a2 = pe("sizeRanges"), s2 = he("useWindowScroll"), u3 = he("customScrollParent"), m2 = pe("windowScrollContainerState"), d2 = pe("scrollContainerState"), f2 = u3 || s2 ? m2 : d2, p2 = he("itemContent"), h2 = he("context"), g2 = he("groupContent"), v2 = he("trackItemSizes"), S2 = he("itemSize"), I2 = he("log"), C2 = pe("gap"), w2 = T(a2, S2, v2, r3 ? noop10 : f2, I2, C2, u3).callbackRef, x2 = n.useState(0), b2 = x2[0], y2 = x2[1];
  ge("deviation", function(t3) {
    b2 !== t3 && y2(t3);
  });
  var H2 = he("EmptyPlaceholder"), E2 = he("ScrollSeekPlaceholder") || $t, R2 = he("ListComponent"), L2 = he("ItemComponent"), k2 = he("GroupComponent"), F2 = he("computeItemKey"), z2 = he("isSeeking"), B2 = he("groupIndices").length > 0, P2 = he("paddingTopAddition"), O2 = r3 ? {} : { boxSizing: "border-box", paddingTop: i2.offsetTop + P2, paddingBottom: i2.offsetBottom, marginTop: b2 };
  return !r3 && 0 === i2.totalCount && H2 ? (0, import_react28.createElement)(H2, re2(H2, h2)) : (0, import_react28.createElement)(R2, c({}, re2(R2, h2), { ref: w2, style: O2, "data-test-id": r3 ? "virtuoso-top-item-list" : "virtuoso-item-list" }), (r3 ? i2.topItems : i2.items).map(function(t3) {
    var e = t3.originalIndex, n2 = F2(e + i2.firstItemIndex, t3.data, h2);
    return z2 ? (0, import_react28.createElement)(E2, c({}, re2(E2, h2), { key: n2, index: t3.index, height: t3.size, type: t3.type || "item" }, "group" === t3.type ? {} : { groupIndex: t3.groupIndex })) : "group" === t3.type ? (0, import_react28.createElement)(k2, c({}, re2(k2, h2), { key: n2, "data-index": e, "data-known-size": t3.size, "data-item-index": t3.index, style: Qt }), g2(t3.index)) : (0, import_react28.createElement)(L2, c({}, re2(L2, h2), { key: n2, "data-index": e, "data-known-size": t3.size, "data-item-index": t3.index, "data-item-group-index": t3.groupIndex, style: Xt }), B2 ? p2(t3.index, t3.groupIndex, t3.data, h2) : p2(t3.index, t3.data, h2));
  }));
});
var ee = { height: "100%", outline: "none", overflowY: "auto", position: "relative", WebkitOverflowScrolling: "touch" };
var ne = { width: "100%", height: "100%", position: "absolute", top: 0 };
var oe = { width: "100%", position: Nt(), top: 0 };
function re2(t2, e) {
  if ("string" != typeof t2) return { context: e };
}
var ie = n.memo(function() {
  var t2 = he("HeaderComponent"), e = pe("headerHeight"), n2 = he("headerFooterTag"), o2 = C(function(t3) {
    return e(w(t3, "height"));
  }), r3 = he("context");
  return t2 ? (0, import_react28.createElement)(n2, { ref: o2 }, (0, import_react28.createElement)(t2, re2(t2, r3))) : null;
});
var ae = n.memo(function() {
  var t2 = he("FooterComponent"), e = pe("footerHeight"), n2 = he("headerFooterTag"), o2 = C(function(t3) {
    return e(w(t3, "height"));
  }), r3 = he("context");
  return t2 ? (0, import_react28.createElement)(n2, { ref: o2 }, (0, import_react28.createElement)(t2, re2(t2, r3))) : null;
});
function le(t2) {
  var e = t2.usePublisher, o2 = t2.useEmitter, r3 = t2.useEmitterValue;
  return n.memo(function(t3) {
    var n2 = t3.style, i2 = t3.children, a2 = m(t3, jt), s2 = e("scrollContainerState"), u3 = r3("ScrollerComponent"), d2 = e("smoothScrollTargetReached"), f2 = r3("scrollerRef"), p2 = r3("context"), h2 = b(s2, d2, u3, f2), g2 = h2.scrollerRef, v2 = h2.scrollByCallback;
    return o2("scrollTo", h2.scrollToCallback), o2("scrollBy", v2), (0, import_react28.createElement)(u3, c({ ref: g2, style: c({}, ee, n2), "data-test-id": "virtuoso-scroller", "data-virtuoso-scroller": true, tabIndex: 0 }, a2, re2(u3, p2)), i2);
  });
}
function se(t2) {
  var o2 = t2.usePublisher, r3 = t2.useEmitter, i2 = t2.useEmitterValue;
  return n.memo(function(t3) {
    var n2 = t3.style, a2 = t3.children, s2 = m(t3, Kt), u3 = o2("windowScrollContainerState"), d2 = i2("ScrollerComponent"), f2 = o2("smoothScrollTargetReached"), p2 = i2("totalListHeight"), h2 = i2("deviation"), v2 = i2("customScrollParent"), S2 = i2("context"), I2 = b(u3, f2, d2, noop10, v2), C2 = I2.scrollerRef, T2 = I2.scrollByCallback, w2 = I2.scrollToCallback;
    return g(function() {
      return C2.current = v2 || window, function() {
        C2.current = null;
      };
    }, [C2, v2]), r3("windowScrollTo", w2), r3("scrollBy", T2), (0, import_react28.createElement)(d2, c({ style: c({ position: "relative" }, n2, 0 !== p2 ? { height: p2 + h2 } : {}), "data-virtuoso-scroller": true }, s2, re2(d2, S2)), a2);
  });
}
var ue = function(t2) {
  var o2 = t2.children, r3 = (0, import_react28.useContext)(Gt), i2 = pe("viewportHeight"), a2 = pe("fixedItemHeight"), l2 = C(compose(i2, function(t3) {
    return w(t3, "height");
  }));
  return n.useEffect(function() {
    r3 && (i2(r3.viewportHeight), a2(r3.itemHeight));
  }, [r3, i2, a2]), n.createElement("div", { style: ne, ref: l2, "data-viewport-type": "element" }, o2);
};
var ce = function(t2) {
  var e = t2.children, o2 = (0, import_react28.useContext)(Gt), r3 = pe("windowViewportRect"), i2 = pe("fixedItemHeight"), a2 = he("customScrollParent"), l2 = Dt(r3, a2);
  return n.useEffect(function() {
    o2 && (i2(o2.itemHeight), r3({ offsetTop: 0, visibleHeight: o2.viewportHeight, visibleWidth: 100 }));
  }, [o2, r3, i2]), n.createElement("div", { ref: l2, style: ne, "data-viewport-type": "window" }, e);
};
var me = function(t2) {
  var e = t2.children, n2 = he("TopItemListComponent"), o2 = he("headerHeight"), r3 = c({}, oe, { marginTop: o2 + "px" }), i2 = he("context");
  return (0, import_react28.createElement)(n2 || "div", { style: r3, context: i2 }, e);
};
var de = systemToComponent(Jt, { required: {}, optional: { context: "context", followOutput: "followOutput", firstItemIndex: "firstItemIndex", itemContent: "itemContent", groupContent: "groupContent", overscan: "overscan", increaseViewportBy: "increaseViewportBy", totalCount: "totalCount", topItemCount: "topItemCount", initialTopMostItemIndex: "initialTopMostItemIndex", components: "components", groupCounts: "groupCounts", atBottomThreshold: "atBottomThreshold", atTopThreshold: "atTopThreshold", computeItemKey: "computeItemKey", defaultItemHeight: "defaultItemHeight", fixedItemHeight: "fixedItemHeight", itemSize: "itemSize", scrollSeekConfiguration: "scrollSeekConfiguration", headerFooterTag: "headerFooterTag", data: "data", initialItemCount: "initialItemCount", initialScrollTop: "initialScrollTop", alignToBottom: "alignToBottom", useWindowScroll: "useWindowScroll", customScrollParent: "customScrollParent", scrollerRef: "scrollerRef", logLevel: "logLevel", react18ConcurrentRendering: "react18ConcurrentRendering", item: "item", group: "group", topItems: "topItems", itemHeight: "itemHeight", scrollingStateChange: "scrollingStateChange", maxHeightCacheSize: "maxHeightCacheSize", footer: "footer", header: "header", ItemContainer: "ItemContainer", ScrollContainer: "ScrollContainer", ListContainer: "ListContainer", GroupContainer: "GroupContainer", emptyComponent: "emptyComponent", HeaderContainer: "HeaderContainer", FooterContainer: "FooterContainer", scrollSeek: "scrollSeek" }, methods: { scrollToIndex: "scrollToIndex", scrollIntoView: "scrollIntoView", scrollTo: "scrollTo", scrollBy: "scrollBy", adjustForPrependedItems: "adjustForPrependedItems", autoscrollToBottom: "autoscrollToBottom" }, events: { isScrolling: "isScrolling", endReached: "endReached", startReached: "startReached", rangeChanged: "rangeChanged", atBottomStateChange: "atBottomStateChange", atTopStateChange: "atTopStateChange", totalListHeightChanged: "totalListHeightChanged", itemsRendered: "itemsRendered", groupIndices: "groupIndices" } }, n.memo(function(t2) {
  var e = he("useWindowScroll"), o2 = he("topItemsIndexes").length > 0, r3 = he("customScrollParent"), i2 = r3 || e ? ce : ue;
  return n.createElement(r3 || e ? Se : ve, c({}, t2), n.createElement(i2, null, n.createElement(ie, null), n.createElement(te, null), n.createElement(ae, null)), o2 && n.createElement(me, null, n.createElement(te, { showTopList: true })));
}));
var fe = de.Component;
var pe = de.usePublisher;
var he = de.useEmitterValue;
var ge = de.useEmitter;
var ve = le({ usePublisher: pe, useEmitterValue: he, useEmitter: ge });
var Se = se({ usePublisher: pe, useEmitterValue: he, useEmitter: ge });
var Ie = { items: [], offsetBottom: 0, offsetTop: 0, top: 0, bottom: 0, itemHeight: 0, itemWidth: 0 };
var Ce = { items: [{ index: 0 }], offsetBottom: 0, offsetTop: 0, top: 0, bottom: 0, itemHeight: 0, itemWidth: 0 };
var Te = Math.round;
var we = Math.ceil;
var xe = Math.floor;
var be = Math.min;
var ye = Math.max;
function He(t2, e) {
  return Array.from({ length: e - t2 + 1 }).map(function(e2, n2) {
    return { index: n2 + t2 };
  });
}
function Ee(t2, e) {
  return t2 && t2.column === e.column && t2.row === e.row;
}
var Re = system(function(t2) {
  var n2 = t2[0], o2 = n2.overscan, r3 = n2.visibleRange, i2 = n2.listBoundary, a2 = t2[1], l2 = a2.scrollTop, s2 = a2.viewportHeight, u3 = a2.scrollBy, m2 = a2.scrollTo, d2 = a2.smoothScrollTargetReached, f2 = a2.scrollContainerState, p2 = t2[2], h2 = t2[3], g2 = t2[4], v2 = g2.propsReady, S2 = g2.didMount, I2 = t2[5], C2 = I2.windowViewportRect, T2 = I2.windowScrollTo, w2 = I2.useWindowScroll, x2 = I2.customScrollParent, b2 = I2.windowScrollContainerState, y2 = t2[6], H2 = statefulStream(0), E2 = statefulStream(0), R2 = statefulStream(Ie), L2 = statefulStream({ height: 0, width: 0 }), k2 = statefulStream({ height: 0, width: 0 }), F2 = stream(), z2 = stream(), B2 = statefulStream(0), P2 = statefulStream({ row: 0, column: 0 });
  connect(pipe(S2, withLatestFrom(E2), filter(function(t3) {
    return 0 !== t3[1];
  }), map3(function(t3) {
    return { items: He(0, t3[1] - 1), top: 0, bottom: 0, offsetBottom: 0, offsetTop: 0, itemHeight: 0, itemWidth: 0 };
  })), R2), connect(pipe(combineLatest(duc(H2), r3, duc(P2, Ee), duc(k2, function(t3, e) {
    return t3 && t3.width === e.width && t3.height === e.height;
  })), withLatestFrom(L2), map3(function(t3) {
    var e = t3[0], n3 = e[0], o3 = e[1], r4 = o3[0], i3 = o3[1], a3 = e[2], l3 = e[3], s3 = t3[1], u4 = a3.row, c2 = l3.height, m3 = l3.width, d3 = s3.width;
    if (0 === n3 || 0 === d3) return Ie;
    if (0 === m3) return Ce;
    var f3 = Fe(d3, m3, a3.column), p3 = f3 * xe((r4 + u4) / (c2 + u4)), h3 = f3 * we((i3 + u4) / (c2 + u4)) - 1;
    h3 = ye(0, be(n3 - 1, h3));
    var g3 = He(p3 = be(h3, ye(0, p3)), h3), v3 = Le(s3, a3, l3, g3), S3 = v3.top, I3 = v3.bottom, C3 = we(n3 / f3);
    return { items: g3, offsetTop: S3, offsetBottom: C3 * c2 + (C3 - 1) * u4 - I3, top: S3, bottom: I3, itemHeight: c2, itemWidth: m3 };
  })), R2), connect(pipe(L2, map3(function(t3) {
    return t3.height;
  })), s2), connect(pipe(combineLatest(L2, k2, R2, P2), map3(function(t3) {
    var e = Le(t3[0], t3[3], t3[1], t3[2].items);
    return [e.top, e.bottom];
  }), distinctUntilChanged(vt)), i2);
  var O2 = streamFromEmitter(pipe(duc(R2), filter(function(t3) {
    return t3.items.length > 0;
  }), withLatestFrom(H2), filter(function(t3) {
    var e = t3[0].items;
    return e[e.length - 1].index === t3[1] - 1;
  }), map3(function(t3) {
    return t3[1] - 1;
  }), distinctUntilChanged())), M2 = streamFromEmitter(pipe(duc(R2), filter(function(t3) {
    var e = t3.items;
    return e.length > 0 && 0 === e[0].index;
  }), mapTo(0), distinctUntilChanged())), V2 = streamFromEmitter(pipe(duc(R2), filter(function(t3) {
    return t3.items.length > 0;
  }), map3(function(t3) {
    var e = t3.items;
    return { startIndex: e[0].index, endIndex: e[e.length - 1].index };
  }), distinctUntilChanged(St)));
  connect(V2, h2.scrollSeekRangeChanged), connect(pipe(F2, withLatestFrom(L2, k2, H2, P2), map3(function(t3) {
    var e = t3[1], n3 = t3[2], o3 = t3[3], r4 = t3[4], i3 = at(t3[0]), a3 = i3.align, l3 = i3.behavior, s3 = i3.offset, u4 = i3.index;
    "LAST" === u4 && (u4 = o3 - 1);
    var c2 = ke(e, r4, n3, u4 = ye(0, u4, be(o3 - 1, u4)));
    return "end" === a3 ? c2 = Te(c2 - e.height + n3.height) : "center" === a3 && (c2 = Te(c2 - e.height / 2 + n3.height / 2)), s3 && (c2 += s3), { top: c2, behavior: l3 };
  })), m2);
  var U2 = statefulStreamFromEmitter(pipe(R2, map3(function(t3) {
    return t3.offsetBottom + t3.bottom;
  })), 0);
  return connect(pipe(C2, map3(function(t3) {
    return { width: t3.visibleWidth, height: t3.visibleHeight };
  })), L2), c({ totalCount: H2, viewportDimensions: L2, itemDimensions: k2, scrollTop: l2, scrollHeight: z2, overscan: o2, scrollBy: u3, scrollTo: m2, scrollToIndex: F2, smoothScrollTargetReached: d2, windowViewportRect: C2, windowScrollTo: T2, useWindowScroll: w2, customScrollParent: x2, windowScrollContainerState: b2, deviation: B2, scrollContainerState: f2, initialItemCount: E2, gap: P2 }, h2, { gridState: R2, totalListHeight: U2 }, p2, { startReached: M2, endReached: O2, rangeChanged: V2, propsReady: v2 }, y2);
}, tup(Tt, y, ct, Et, mt, Ot, S));
function Le(t2, e, n2, o2) {
  var r3 = n2.height;
  return void 0 === r3 || 0 === o2.length ? { top: 0, bottom: 0 } : { top: ke(t2, e, n2, o2[0].index), bottom: ke(t2, e, n2, o2[o2.length - 1].index) + r3 };
}
function ke(t2, e, n2, o2) {
  var r3 = Fe(t2.width, n2.width, e.column), i2 = xe(o2 / r3), a2 = i2 * n2.height + ye(0, i2 - 1) * e.row;
  return a2 > 0 ? a2 + e.row : a2;
}
function Fe(t2, e, n2) {
  return ye(1, xe((t2 + n2) / (e + n2)));
}
var ze = ["placeholder"];
var Be = system(function() {
  var t2 = statefulStream(function(t3) {
    return "Item " + t3;
  }), n2 = statefulStream({}), o2 = statefulStream(null), r3 = statefulStream("virtuoso-grid-item"), i2 = statefulStream("virtuoso-grid-list"), a2 = statefulStream(Yt), l2 = statefulStream(noop10), s2 = function(t3, o3) {
    return void 0 === o3 && (o3 = null), statefulStreamFromEmitter(pipe(n2, map3(function(e) {
      return e[t3];
    }), distinctUntilChanged()), o3);
  };
  return { context: o2, itemContent: t2, components: n2, computeItemKey: a2, itemClassName: r3, listClassName: i2, scrollerRef: l2, ListComponent: s2("List", "div"), ItemComponent: s2("Item", "div"), ScrollerComponent: s2("Scroller", "div"), ScrollSeekPlaceholder: s2("ScrollSeekPlaceholder", "div") };
});
var Pe = system(function(t2) {
  var n2 = t2[0], o2 = t2[1], r3 = { item: Zt(o2.itemContent, "Rename the %citem%c prop to %citemContent."), ItemContainer: stream(), ScrollContainer: stream(), ListContainer: stream(), emptyComponent: stream(), scrollSeek: stream() };
  function i2(t3, n3, r4) {
    connect(pipe(t3, withLatestFrom(o2.components), map3(function(t4) {
      var e, o3 = t4[0], i3 = t4[1];
      return console.warn("react-virtuoso: " + r4 + " property is deprecated. Pass components." + n3 + " instead."), c({}, i3, ((e = {})[n3] = o3, e));
    })), o2.components);
  }
  return subscribe(r3.scrollSeek, function(t3) {
    var r4 = t3.placeholder, i3 = m(t3, ze);
    console.warn("react-virtuoso: scrollSeek property is deprecated. Pass scrollSeekConfiguration and specify the placeholder in components.ScrollSeekPlaceholder instead."), publish(o2.components, c({}, getValue(o2.components), { ScrollSeekPlaceholder: r4 })), publish(n2.scrollSeekConfiguration, i3);
  }), i2(r3.ItemContainer, "Item", "ItemContainer"), i2(r3.ListContainer, "List", "ListContainer"), i2(r3.ScrollContainer, "Scroller", "ScrollContainer"), c({}, n2, o2, r3);
}, tup(Re, Be));
var Oe = n.memo(function() {
  var t2 = Ne("gridState"), e = Ne("listClassName"), n2 = Ne("itemClassName"), o2 = Ne("itemContent"), r3 = Ne("computeItemKey"), i2 = Ne("isSeeking"), a2 = We("scrollHeight"), s2 = Ne("ItemComponent"), u3 = Ne("ListComponent"), m2 = Ne("ScrollSeekPlaceholder"), d2 = Ne("context"), f2 = We("itemDimensions"), p2 = We("gap"), h2 = Ne("log"), g2 = C(function(t3) {
    a2(t3.parentElement.parentElement.scrollHeight);
    var e2 = t3.firstChild;
    e2 && f2(e2.getBoundingClientRect()), p2({ row: je("row-gap", getComputedStyle(t3).rowGap, h2), column: je("column-gap", getComputedStyle(t3).columnGap, h2) });
  });
  return (0, import_react28.createElement)(u3, c({ ref: g2, className: e }, re2(u3, d2), { style: { paddingTop: t2.offsetTop, paddingBottom: t2.offsetBottom } }), t2.items.map(function(e2) {
    var a3 = r3(e2.index);
    return i2 ? (0, import_react28.createElement)(m2, c({ key: a3 }, re2(m2, d2), { index: e2.index, height: t2.itemHeight, width: t2.itemWidth })) : (0, import_react28.createElement)(s2, c({}, re2(s2, d2), { className: n2, "data-index": e2.index, key: a3 }), o2(e2.index, d2));
  }));
});
var Me = function(t2) {
  var e = t2.children, o2 = We("viewportDimensions"), r3 = C(function(t3) {
    o2(t3.getBoundingClientRect());
  });
  return n.createElement("div", { style: ne, ref: r3 }, e);
};
var Ve = function(t2) {
  var e = t2.children, o2 = Dt(We("windowViewportRect"), Ne("customScrollParent"));
  return n.createElement("div", { ref: o2, style: ne }, e);
};
var Ue = systemToComponent(Pe, { optional: { context: "context", totalCount: "totalCount", overscan: "overscan", itemContent: "itemContent", components: "components", computeItemKey: "computeItemKey", initialItemCount: "initialItemCount", scrollSeekConfiguration: "scrollSeekConfiguration", listClassName: "listClassName", itemClassName: "itemClassName", useWindowScroll: "useWindowScroll", customScrollParent: "customScrollParent", scrollerRef: "scrollerRef", item: "item", ItemContainer: "ItemContainer", ScrollContainer: "ScrollContainer", ListContainer: "ListContainer", scrollSeek: "scrollSeek" }, methods: { scrollTo: "scrollTo", scrollBy: "scrollBy", scrollToIndex: "scrollToIndex" }, events: { isScrolling: "isScrolling", endReached: "endReached", startReached: "startReached", rangeChanged: "rangeChanged", atBottomStateChange: "atBottomStateChange", atTopStateChange: "atTopStateChange" } }, n.memo(function(t2) {
  var e = c({}, t2), o2 = Ne("useWindowScroll"), r3 = Ne("customScrollParent"), i2 = r3 || o2 ? Ve : Me;
  return n.createElement(r3 || o2 ? _e : Ge, c({}, e), n.createElement(i2, null, n.createElement(Oe, null)));
}));
var Ae = Ue.Component;
var We = Ue.usePublisher;
var Ne = Ue.useEmitterValue;
var De = Ue.useEmitter;
var Ge = le({ usePublisher: We, useEmitterValue: Ne, useEmitter: De });
var _e = se({ usePublisher: We, useEmitterValue: Ne, useEmitter: De });
function je(t2, e, n2) {
  return "normal" === e || null != e && e.endsWith("px") || n2(t2 + " was not resolved to pixel value correctly", e, h.WARN), "normal" === e ? 0 : parseInt(null != e ? e : "0", 10);
}
var Ke = system(function() {
  var t2 = statefulStream(function(t3) {
    return n.createElement("td", null, "Item $", t3);
  }), o2 = statefulStream(null), r3 = statefulStream(null), i2 = statefulStream(null), a2 = statefulStream({}), l2 = statefulStream(Yt), s2 = statefulStream(noop10), u3 = function(t3, n2) {
    return void 0 === n2 && (n2 = null), statefulStreamFromEmitter(pipe(a2, map3(function(e) {
      return e[t3];
    }), distinctUntilChanged()), n2);
  };
  return { context: o2, itemContent: t2, fixedHeaderContent: r3, fixedFooterContent: i2, components: a2, computeItemKey: l2, scrollerRef: s2, TableComponent: u3("Table", "table"), TableHeadComponent: u3("TableHead", "thead"), TableFooterComponent: u3("TableFoot", "tfoot"), TableBodyComponent: u3("TableBody", "tbody"), TableRowComponent: u3("TableRow", "tr"), ScrollerComponent: u3("Scroller", "div"), EmptyPlaceholder: u3("EmptyPlaceholder"), ScrollSeekPlaceholder: u3("ScrollSeekPlaceholder"), FillerRow: u3("FillerRow") };
});
var Ye = system(function(t2) {
  return c({}, t2[0], t2[1]);
}, tup(Wt, Ke));
var qe = function(t2) {
  return n.createElement("tr", null, n.createElement("td", { style: { height: t2.height } }));
};
var Ze = function(t2) {
  return n.createElement("tr", null, n.createElement("td", { style: { height: t2.height, padding: 0, border: 0 } }));
};
var Je = n.memo(function() {
  var t2 = nn("listState"), e = en("sizeRanges"), o2 = nn("useWindowScroll"), r3 = nn("customScrollParent"), i2 = en("windowScrollContainerState"), a2 = en("scrollContainerState"), s2 = r3 || o2 ? i2 : a2, u3 = nn("itemContent"), m2 = nn("trackItemSizes"), d2 = T(e, nn("itemSize"), m2, s2, nn("log"), void 0, r3), f2 = d2.callbackRef, p2 = d2.ref, h2 = n.useState(0), g2 = h2[0], v2 = h2[1];
  on("deviation", function(t3) {
    g2 !== t3 && (p2.current.style.marginTop = t3 + "px", v2(t3));
  });
  var S2 = nn("EmptyPlaceholder"), I2 = nn("ScrollSeekPlaceholder") || qe, C2 = nn("FillerRow") || Ze, w2 = nn("TableBodyComponent"), x2 = nn("TableRowComponent"), b2 = nn("computeItemKey"), y2 = nn("isSeeking"), H2 = nn("paddingTopAddition"), E2 = nn("firstItemIndex"), R2 = nn("statefulTotalCount"), L2 = nn("context");
  if (0 === R2 && S2) return (0, import_react28.createElement)(S2, re2(S2, L2));
  var k2 = t2.offsetTop + H2 + g2, F2 = t2.offsetBottom, z2 = k2 > 0 ? n.createElement(C2, { height: k2, key: "padding-top" }) : null, B2 = F2 > 0 ? n.createElement(C2, { height: F2, key: "padding-bottom" }) : null, P2 = t2.items.map(function(t3) {
    var e2 = t3.originalIndex, n2 = b2(e2 + E2, t3.data, L2);
    return y2 ? (0, import_react28.createElement)(I2, c({}, re2(I2, L2), { key: n2, index: t3.index, height: t3.size, type: t3.type || "item" })) : (0, import_react28.createElement)(x2, c({}, re2(x2, L2), { key: n2, "data-index": e2, "data-known-size": t3.size, "data-item-index": t3.index, style: { overflowAnchor: "none" } }), u3(t3.index, t3.data, L2));
  });
  return (0, import_react28.createElement)(w2, c({ ref: f2, "data-test-id": "virtuoso-item-list" }, re2(w2, L2)), [z2].concat(P2, [B2]));
});
var $e = function(t2) {
  var o2 = t2.children, r3 = (0, import_react28.useContext)(Gt), i2 = en("viewportHeight"), a2 = en("fixedItemHeight"), l2 = C(compose(i2, function(t3) {
    return w(t3, "height");
  }));
  return n.useEffect(function() {
    r3 && (i2(r3.viewportHeight), a2(r3.itemHeight));
  }, [r3, i2, a2]), n.createElement("div", { style: ne, ref: l2, "data-viewport-type": "element" }, o2);
};
var Qe = function(t2) {
  var e = t2.children, o2 = (0, import_react28.useContext)(Gt), r3 = en("windowViewportRect"), i2 = en("fixedItemHeight"), a2 = nn("customScrollParent"), l2 = Dt(r3, a2);
  return n.useEffect(function() {
    o2 && (i2(o2.itemHeight), r3({ offsetTop: 0, visibleHeight: o2.viewportHeight, visibleWidth: 100 }));
  }, [o2, r3, i2]), n.createElement("div", { ref: l2, style: ne, "data-viewport-type": "window" }, e);
};
var Xe = systemToComponent(Ye, { required: {}, optional: { context: "context", followOutput: "followOutput", firstItemIndex: "firstItemIndex", itemContent: "itemContent", fixedHeaderContent: "fixedHeaderContent", fixedFooterContent: "fixedFooterContent", overscan: "overscan", increaseViewportBy: "increaseViewportBy", totalCount: "totalCount", topItemCount: "topItemCount", initialTopMostItemIndex: "initialTopMostItemIndex", components: "components", groupCounts: "groupCounts", atBottomThreshold: "atBottomThreshold", atTopThreshold: "atTopThreshold", computeItemKey: "computeItemKey", defaultItemHeight: "defaultItemHeight", fixedItemHeight: "fixedItemHeight", itemSize: "itemSize", scrollSeekConfiguration: "scrollSeekConfiguration", data: "data", initialItemCount: "initialItemCount", initialScrollTop: "initialScrollTop", alignToBottom: "alignToBottom", useWindowScroll: "useWindowScroll", customScrollParent: "customScrollParent", scrollerRef: "scrollerRef", logLevel: "logLevel", react18ConcurrentRendering: "react18ConcurrentRendering" }, methods: { scrollToIndex: "scrollToIndex", scrollIntoView: "scrollIntoView", scrollTo: "scrollTo", scrollBy: "scrollBy" }, events: { isScrolling: "isScrolling", endReached: "endReached", startReached: "startReached", rangeChanged: "rangeChanged", atBottomStateChange: "atBottomStateChange", atTopStateChange: "atTopStateChange", totalListHeightChanged: "totalListHeightChanged", itemsRendered: "itemsRendered", groupIndices: "groupIndices" } }, n.memo(function(t2) {
  var o2 = nn("useWindowScroll"), r3 = nn("customScrollParent"), i2 = en("fixedHeaderHeight"), a2 = en("fixedFooterHeight"), l2 = nn("fixedHeaderContent"), s2 = nn("fixedFooterContent"), u3 = nn("context"), m2 = C(compose(i2, function(t3) {
    return w(t3, "height");
  })), d2 = C(compose(a2, function(t3) {
    return w(t3, "height");
  })), f2 = r3 || o2 ? an : rn, p2 = r3 || o2 ? Qe : $e, h2 = nn("TableComponent"), g2 = nn("TableHeadComponent"), v2 = nn("TableFooterComponent"), S2 = l2 ? n.createElement(g2, c({ key: "TableHead", style: { zIndex: 1, position: "sticky", top: 0 }, ref: m2 }, re2(g2, u3)), l2()) : null, I2 = s2 ? n.createElement(v2, c({ key: "TableFoot", style: { zIndex: 1, position: "sticky", bottom: 0 }, ref: d2 }, re2(v2, u3)), s2()) : null;
  return n.createElement(f2, c({}, t2), n.createElement(p2, null, n.createElement(h2, c({ style: { borderSpacing: 0 } }, re2(h2, u3)), [S2, n.createElement(Je, { key: "TableBody" }), I2])));
}));
var tn = Xe.Component;
var en = Xe.usePublisher;
var nn = Xe.useEmitterValue;
var on = Xe.useEmitter;
var rn = le({ usePublisher: en, useEmitterValue: nn, useEmitter: on });
var an = se({ usePublisher: en, useEmitterValue: nn, useEmitter: on });
var ln = fe;

// node_modules/stream-chat-react/dist/es/index.mjs
var AIStates = {
  Error: "AI_STATE_ERROR",
  ExternalSources: "AI_STATE_EXTERNAL_SOURCES",
  Generating: "AI_STATE_GENERATING",
  Idle: "AI_STATE_IDLE",
  Stop: "AI_STATE_STOP",
  Thinking: "AI_STATE_THINKING"
};
var useAIState = (channel) => {
  const [aiState, setAiState] = (0, import_react29.useState)(AIStates.Idle);
  (0, import_react29.useEffect)(() => {
    if (!channel) {
      return;
    }
    const indicatorChangedListener = channel.on("ai_indicator.update", (event) => {
      const { cid } = event;
      const state = event.ai_state;
      if (channel.cid === cid) {
        setAiState(state);
      }
    });
    const indicatorClearedListener = channel.on("ai_indicator.clear", (event) => {
      const { cid } = event;
      if (channel.cid === cid) {
        setAiState(AIStates.Idle);
      }
    });
    const indicatorStoppedListener = channel.on("ai_indicator.stop", (event) => {
      const { cid } = event;
      if (channel.cid === cid) {
        setAiState(AIStates.Stop);
      }
    });
    return () => {
      indicatorChangedListener.unsubscribe();
      indicatorClearedListener.unsubscribe();
      indicatorStoppedListener.unsubscribe();
    };
  }, [channel]);
  return { aiState };
};
var DialogManager = class {
  constructor({ closeOnClickOutside = true, id: id3 } = {}) {
    this.state = new StateStore({
      dialogsById: {}
    });
    this.remove = (id22) => {
      const dialog = this.get(id22);
      if (!dialog) return;
      if (dialog.removalTimeout) {
        clearTimeout(dialog.removalTimeout);
      }
      this.state.next((current) => {
        const newDialogs = { ...current.dialogsById };
        delete newDialogs[id22];
        return {
          ...current,
          dialogsById: newDialogs
        };
      });
    };
    this.closeOnClickOutside = closeOnClickOutside;
    this.id = id3 ?? nanoid();
  }
  get openDialogCount() {
    return Object.values(this.state.getLatestValue().dialogsById).reduce(
      (count2, dialog) => {
        if (dialog.isOpen) return count2 + 1;
        return count2;
      },
      0
    );
  }
  get(id3) {
    return this.state.getLatestValue().dialogsById[id3];
  }
  getOrCreate({ closeOnClickOutside, id: id3 }) {
    let dialog = this.get(id3);
    if (!dialog) {
      dialog = {
        close: () => {
          this.close(id3);
        },
        closeOnClickOutside,
        id: id3,
        isOpen: false,
        open: () => {
          this.open({ id: id3 });
        },
        removalTimeout: void 0,
        remove: () => {
          this.remove(id3);
        },
        toggle: (closeAll = false) => {
          this.toggle({ id: id3 }, closeAll);
        }
      };
      this.state.next((current) => ({
        ...current,
        dialogsById: { ...current.dialogsById, [id3]: dialog }
      }));
    }
    const shouldUpdateDialogSettings = dialog.closeOnClickOutside !== closeOnClickOutside || !!dialog.removalTimeout;
    if (shouldUpdateDialogSettings) {
      if (dialog.removalTimeout) clearTimeout(dialog.removalTimeout);
      this.state.next((current) => ({
        ...current,
        dialogsById: {
          ...current.dialogsById,
          [id3]: {
            ...current.dialogsById[id3],
            closeOnClickOutside,
            removalTimeout: void 0
          }
        }
      }));
    }
    return dialog;
  }
  open(params, closeRest) {
    const dialog = this.getOrCreate(params);
    if (dialog.isOpen) return;
    if (closeRest) {
      this.closeAll();
    }
    this.state.next((current) => ({
      ...current,
      dialogsById: { ...current.dialogsById, [dialog.id]: { ...dialog, isOpen: true } }
    }));
  }
  close(id3) {
    const dialog = this.state.getLatestValue().dialogsById[id3];
    if (!dialog?.isOpen) return;
    this.state.next((current) => ({
      ...current,
      dialogsById: { ...current.dialogsById, [dialog.id]: { ...dialog, isOpen: false } }
    }));
  }
  closeAll() {
    Object.values(this.state.getLatestValue().dialogsById).forEach(
      (dialog) => dialog.close()
    );
  }
  toggle(params, closeAll = false) {
    if (this.state.getLatestValue().dialogsById[params.id]?.isOpen) {
      this.close(params.id);
    } else {
      this.open(params, closeAll);
    }
  }
  /**
   * Marks the dialog state as unused. If the dialog id is referenced again quickly,
   * the state will not be removed. Otherwise, the state will be removed after
   * a short timeout.
   */
  markForRemoval(id3) {
    const dialog = this.get(id3);
    if (!dialog) {
      return;
    }
    this.state.next((current) => ({
      ...current,
      dialogsById: {
        ...current.dialogsById,
        [id3]: {
          ...dialog,
          removalTimeout: setTimeout(() => {
            this.remove(id3);
          }, 16)
        }
      }
    }));
  }
  cancelPendingRemoval(id3) {
    const dialog = this.get(id3);
    if (!dialog?.removalTimeout) {
      return;
    }
    clearTimeout(dialog.removalTimeout);
    this.state.next((current) => ({
      ...current,
      dialogsById: {
        ...current.dialogsById,
        [id3]: {
          ...current.dialogsById[id3],
          removalTimeout: void 0
        }
      }
    }));
  }
};
var useDialog = ({
  closeOnClickOutside,
  dialogManagerId,
  id: id3
}) => {
  const { dialogManager } = useDialogManager({ dialogManagerId });
  (0, import_react29.useEffect)(() => {
    dialogManager.cancelPendingRemoval(id3);
    return () => {
      dialogManager.markForRemoval(id3);
    };
  }, [dialogManager, id3]);
  return dialogManager.getOrCreate({ closeOnClickOutside, id: id3 });
};
var useDialogOnNearestManager = ({ id: id3 }) => {
  const { dialogManager } = useNearestDialogManagerContext() ?? {};
  const dialog = useDialog({ dialogManagerId: dialogManager?.id, id: id3 });
  return {
    dialog,
    dialogManager
  };
};
var modalDialogId = "modal-dialog";
var useModalDialog = () => useDialog({ dialogManagerId: modalDialogManagerId, id: modalDialogId });
var useDialogIsOpen = (id3, dialogManagerId) => {
  const { dialogManager } = useDialogManager({ dialogManagerId });
  const dialogIsOpenSelector = (0, import_react29.useCallback)(
    ({ dialogsById }) => ({ isOpen: !!dialogsById[id3]?.isOpen }),
    [id3]
  );
  return useStateStore(dialogManager.state, dialogIsOpenSelector).isOpen;
};
var useModalDialogIsOpen = () => useDialogIsOpen(modalDialogId, modalDialogManagerId);
var openedDialogCountSelector = (nextValue) => ({
  openedDialogCount: Object.values(nextValue.dialogsById).reduce((count2, dialog) => {
    if (dialog.isOpen) return count2 + 1;
    return count2;
  }, 0)
});
var useOpenedDialogCount = ({
  dialogManagerId
} = {}) => {
  const { dialogManager } = useDialogManager({ dialogManagerId });
  return useStateStore(dialogManager.state, openedDialogCountSelector).openedDialogCount;
};
var Portal = ({
  children,
  getPortalDestination,
  isOpen
}) => {
  const [portalDestination, setPortalDestination] = (0, import_react29.useState)(null);
  (0, import_react29.useLayoutEffect)(() => {
    const destination = getPortalDestination();
    if (!destination || !isOpen) return;
    setPortalDestination(destination);
  }, [getPortalDestination, isOpen]);
  if (!portalDestination) return null;
  return (0, import_react_dom4.createPortal)(children, portalDestination);
};
var shouldCloseOnOutsideClick = ({
  dialog,
  managerCloseOnClickOutside
}) => dialog.closeOnClickOutside ?? managerCloseOnClickOutside;
var DialogPortalDestination = () => {
  const { dialogManager } = useNearestDialogManagerContext() ?? {};
  const openedDialogCount = useOpenedDialogCount({ dialogManagerId: dialogManager?.id });
  const [destinationRoot, setDestinationRoot] = (0, import_react29.useState)(null);
  (0, import_react29.useEffect)(() => {
    if (!destinationRoot || !dialogManager) return;
    const handleDocumentClick = (event) => {
      const target = event.target;
      const clickedOverlay = target.closest?.(
        "[data-str-chat__portal-id]"
      );
      if (clickedOverlay && clickedOverlay !== destinationRoot) return;
      if (target !== destinationRoot && destinationRoot.contains(target)) return;
      setTimeout(() => {
        Object.values(dialogManager.state.getLatestValue().dialogsById).forEach(
          (dialog) => {
            if (!dialog.isOpen) return;
            if (!shouldCloseOnOutsideClick({
              dialog,
              managerCloseOnClickOutside: dialogManager.closeOnClickOutside
            }))
              return;
            dialogManager.close(dialog.id);
          }
        );
      }, 0);
    };
    document.addEventListener("click", handleDocumentClick, { capture: true });
    return () => {
      document.removeEventListener("click", handleDocumentClick, { capture: true });
    };
  }, [destinationRoot, dialogManager]);
  if (!openedDialogCount) return null;
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__dialog-overlay",
      "data-str-chat__portal-id": dialogManager?.id,
      "data-testid": "str-chat__dialog-overlay",
      ref: setDestinationRoot,
      style: {
        "--str-chat__dialog-overlay-height": openedDialogCount > 0 ? "100%" : "0"
      }
    }
  );
};
var DialogPortalEntry = ({
  children,
  dialogId: dialogId2,
  dialogManagerId
}) => {
  const { dialogManager } = useDialogManager({ dialogId: dialogId2, dialogManagerId });
  const dialogIsOpen = useDialogIsOpen(dialogId2, dialogManagerId);
  const getPortalDestination = (0, import_react29.useCallback)(
    () => document.querySelector(`div[data-str-chat__portal-id="${dialogManager.id}"]`),
    [dialogManager.id]
  );
  return (0, import_jsx_runtime4.jsx)(Portal, { getPortalDestination, isOpen: dialogIsOpen, children });
};
var dialogManagersRegistry = new StateStore({});
var getDialogManager = (id3) => dialogManagersRegistry.getLatestValue()[id3];
var getOrCreateDialogManager = ({
  closeOnClickOutside,
  id: id3
}) => {
  let manager = getDialogManager(id3);
  if (!manager) {
    manager = new DialogManager({ closeOnClickOutside, id: id3 });
    dialogManagersRegistry.partialNext({ [id3]: manager });
  } else if (typeof closeOnClickOutside === "boolean") {
    manager.closeOnClickOutside = closeOnClickOutside;
  }
  return manager;
};
var removeDialogManager = (id3) => {
  if (!getDialogManager(id3)) return;
  dialogManagersRegistry.partialNext({ [id3]: void 0 });
};
var DialogManagerProviderContext = import_react29.default.createContext(void 0);
var DialogManagerProvider = ({
  children,
  closeOnClickOutside,
  id: id3
}) => {
  const [dialogManager, setDialogManager] = (0, import_react29.useState)(() => {
    if (id3) return getDialogManager(id3) ?? null;
    return new DialogManager({ closeOnClickOutside });
  });
  (0, import_react29.useEffect)(() => {
    if (!id3) return;
    setDialogManager(getOrCreateDialogManager({ closeOnClickOutside, id: id3 }));
    return () => {
      removeDialogManager(id3);
      setDialogManager(null);
    };
  }, [closeOnClickOutside, id3]);
  if (!dialogManager) return null;
  return (0, import_jsx_runtime4.jsxs)(DialogManagerProviderContext.Provider, { value: { dialogManager }, children: [
    children,
    (0, import_jsx_runtime4.jsx)(DialogPortalDestination, {})
  ] });
};
var getManagerFromStore = ({
  dialogId: dialogId2,
  dialogManagerId,
  newState,
  previousState
}) => {
  let managerInNewState;
  let managerInPrevState;
  if (dialogManagerId) {
    if (!dialogId2) {
      managerInNewState = newState[dialogManagerId];
      managerInPrevState = previousState?.[dialogManagerId];
    } else {
      if (newState[dialogManagerId]?.get(dialogId2)) {
        managerInNewState = newState[dialogManagerId];
      }
      if (previousState?.[dialogManagerId]?.get(dialogId2)) {
        managerInPrevState = previousState[dialogManagerId];
      }
    }
  } else if (dialogId2) {
    managerInNewState = Object.values(newState).find(
      (dialogMng) => dialogId2 && dialogMng?.get(dialogId2)
    );
    managerInPrevState = previousState && Object.values(previousState).find(
      (dialogMng) => dialogId2 && dialogMng?.get(dialogId2)
    );
  }
  return { managerInNewState, managerInPrevState };
};
var useDialogManager = ({
  dialogId: dialogId2,
  dialogManagerId
} = {}) => {
  const nearestDialogManagerContext = (0, import_react29.useContext)(DialogManagerProviderContext);
  const [dialogManagerContext, setDialogManagerContext] = (0, import_react29.useState)(() => {
    const { managerInNewState } = getManagerFromStore({
      dialogId: dialogId2,
      dialogManagerId,
      newState: dialogManagersRegistry.getLatestValue(),
      previousState: void 0
    });
    return managerInNewState ? { dialogManager: managerInNewState } : nearestDialogManagerContext;
  });
  (0, import_react29.useEffect)(() => {
    if (!dialogId2 && !dialogManagerId) return;
    const unsubscribe = dialogManagersRegistry.subscribeWithSelector(
      (state) => state,
      (newState, previousState) => {
        const { managerInNewState, managerInPrevState } = getManagerFromStore({
          dialogId: dialogId2,
          dialogManagerId,
          newState,
          previousState
        });
        if (!managerInPrevState || managerInNewState?.id !== managerInPrevState.id) {
          setDialogManagerContext((prevState) => {
            if (prevState?.dialogManager.id === managerInNewState?.id) return prevState;
            return {
              dialogManager: managerInNewState || nearestDialogManagerContext?.dialogManager
            };
          });
        }
      }
    );
    return () => {
      unsubscribe();
    };
  }, [dialogId2, dialogManagerId, nearestDialogManagerContext?.dialogManager]);
  if (!dialogManagerContext?.dialogManager) {
    console.warn(
      `Dialog manager (manager id: ${dialogManagerId}, dialog id: ${dialogId2}) is not available`
    );
  }
  return dialogManagerContext;
};
var modalDialogManagerId = "modal-dialog-manager";
var ModalDialogManagerProvider = ({ children }) => (0, import_jsx_runtime4.jsx)(DialogManagerProvider, { id: modalDialogManagerId, children });
var useModalDialogManager = () => (0, import_react29.useMemo)(() => getDialogManager(modalDialogManagerId), []);
var useNearestDialogManagerContext = () => (0, import_react29.useContext)(DialogManagerProviderContext);
var MessageContext = import_react29.default.createContext(
  void 0
);
var MessageProvider = ({
  children,
  value
}) => (0, import_jsx_runtime4.jsx)(MessageContext.Provider, { value, children });
var useMessageContext = (_componentName) => {
  const contextValue = (0, import_react29.useContext)(MessageContext);
  if (!contextValue) {
    return {};
  }
  return contextValue;
};
var snapshots = /* @__PURE__ */ new WeakMap();
var savePreEditSnapshot = (messageComposer) => {
  if (snapshots.has(messageComposer)) return;
  const composerState = messageComposer.state.getLatestValue();
  const textState = messageComposer.textComposer.state.getLatestValue();
  const attachmentState = messageComposer.attachmentManager.state.getLatestValue();
  const linkPreviewState = messageComposer.linkPreviewsManager.state.getLatestValue();
  const locationState = messageComposer.locationComposer.state.getLatestValue();
  const pollState = messageComposer.pollComposer.state.getLatestValue();
  const customDataState = messageComposer.customDataManager.state.getLatestValue();
  snapshots.set(messageComposer, () => {
    messageComposer.state.next(composerState);
    messageComposer.textComposer.state.next(textState);
    messageComposer.attachmentManager.state.next(attachmentState);
    messageComposer.linkPreviewsManager.state.next(linkPreviewState);
    messageComposer.locationComposer.state.next(locationState);
    messageComposer.pollComposer.state.next(pollState);
    messageComposer.customDataManager.state.next(customDataState);
  });
};
var restorePreEditSnapshot = (messageComposer) => {
  const restore = snapshots.get(messageComposer);
  snapshots.delete(messageComposer);
  if (restore) {
    restore();
  } else {
    messageComposer.clear();
  }
};
var discardPreEditSnapshot = (messageComposer) => {
  snapshots.delete(messageComposer);
};
var MessageBounceContext = (0, import_react29.createContext)(
  void 0
);
function useMessageBounceContext(componentName) {
  const contextValue = (0, import_react29.useContext)(MessageBounceContext);
  if (!contextValue) {
    console.warn(
      `The useMessageBounceContext hook was called outside of the MessageBounceContext provider. The errored call is located in the ${componentName} component.`
    );
    return {};
  }
  return contextValue;
}
function MessageBounceProvider({ children }) {
  const messageComposer = useMessageComposerController();
  const { handleRetry: doHandleRetry, message } = useMessageContext();
  if (!isMessageBounced(message)) {
    console.warn(
      `The MessageBounceProvider was rendered for a message that is not bounced. Have you missed the "isMessageBounced" check?`
    );
  }
  const { removeMessage } = useChannelActionContext("MessageBounceProvider");
  const handleDelete2 = (0, import_react29.useCallback)(() => {
    removeMessage(message);
  }, [message, removeMessage]);
  const handleEdit = (0, import_react29.useCallback)(
    (e) => {
      e.preventDefault();
      savePreEditSnapshot(messageComposer);
      messageComposer.initState({ composition: message });
    },
    [message, messageComposer]
  );
  const handleRetry = (0, import_react29.useCallback)(() => {
    doHandleRetry(message);
  }, [doHandleRetry, message]);
  const value = (0, import_react29.useMemo)(
    () => ({
      handleDelete: handleDelete2,
      handleEdit,
      handleRetry,
      message
    }),
    [handleDelete2, handleEdit, handleRetry, message]
  );
  return (0, import_jsx_runtime4.jsx)(MessageBounceContext.Provider, { value, children });
}
var MessageListContext = (0, import_react29.createContext)(
  void 0
);
var MessageListContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime4.jsx)(MessageListContext.Provider, { value, children });
var useMessageListContext = (componentName) => {
  const contextValue = (0, import_react29.useContext)(MessageListContext);
  if (!contextValue) {
    console.warn(
      `The useMessageListContext hook was called outside of the MessageListContext provider. Make sure this hook is called within the MessageList component. The errored call is located in the ${componentName} component.`
    );
    return {};
  }
  return contextValue;
};
var getTranslatedMessageText = ({
  language,
  message
}) => message?.i18n?.[`${language}_text`];
var defaultContextValue = {
  getTranslationView: (_messageId, hasI18n) => hasI18n ? "translated" : "original",
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  setTranslationView: () => {
  }
};
var MessageTranslationViewContext = (0, import_react29.createContext)(defaultContextValue);
var MessageTranslationViewProvider = ({
  children
}) => {
  const [viewByMessageId, setViewByMessageId] = (0, import_react29.useState)(
    {}
  );
  const setTranslationView = (0, import_react29.useCallback)((messageId, view) => {
    setViewByMessageId((prev) => ({ ...prev, [messageId]: view }));
  }, []);
  const getTranslationView = (0, import_react29.useCallback)(
    (messageId, hasI18n) => viewByMessageId[messageId] ?? (hasI18n ? "translated" : "original"),
    [viewByMessageId]
  );
  const stableValue = import_react29.default.useMemo(
    () => ({ getTranslationView, setTranslationView }),
    [getTranslationView, setTranslationView]
  );
  return (0, import_jsx_runtime4.jsx)(MessageTranslationViewContext.Provider, { value: stableValue, children });
};
var useMessageTranslationViewContext = () => {
  const context = (0, import_react29.useContext)(MessageTranslationViewContext);
  return context ?? defaultContextValue;
};
var ModalContext = import_react29.default.createContext(void 0);
var ModalContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime4.jsx)(ModalContext.Provider, { value, children });
var useModalContext = () => {
  const contextValue = (0, import_react29.useContext)(ModalContext);
  if (!contextValue) {
    console.warn(
      `The useModalContext hook was called outside of the ModalContext provider. Make sure this hook is called within a child of the GlobalModal.`
    );
    return { close: () => null };
  }
  return contextValue;
};
var PollContext = import_react29.default.createContext(void 0);
var PollProvider = ({
  children,
  poll
}) => poll ? (0, import_jsx_runtime4.jsx)(PollContext.Provider, { value: { poll }, children }) : null;
var usePollContext = () => {
  const contextValue = (0, import_react29.useContext)(PollContext);
  return contextValue;
};
function WithComponents({
  children,
  overrides
}) {
  const parentOverrides = (0, import_react29.useContext)(ComponentContext);
  const actualOverrides = { ...parentOverrides, ...overrides };
  return (0, import_jsx_runtime4.jsx)(ComponentContext.Provider, { value: actualOverrides, children });
}
var AIStateIndicator = ({
  channel: channelFromProps
}) => {
  const { t: t2 } = useTranslationContext();
  const { channel: channelFromContext } = useChannelStateContext("AIStateIndicator");
  const channel = channelFromProps || channelFromContext;
  const { aiState } = useAIState(channel);
  const allowedStates = {
    [AIStates.Thinking]: t2("Thinking..."),
    [AIStates.Generating]: t2("Generating...")
  };
  return aiState in allowedStates ? (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__ai-state-indicator-container", children: (0, import_jsx_runtime4.jsx)("p", { className: "str-chat__ai-state-indicator-text", children: allowedStates[aiState] }) }) : null;
};
var DownloadButton = ({
  assetUrl,
  className,
  suggestedFileName,
  tooltipTitle
}) => {
  const { t: t2 } = useTranslationContext();
  if (!assetUrl) return null;
  const href = (0, import_sanitize_url.sanitizeUrl)(assetUrl);
  if (!href) return null;
  return (0, import_jsx_runtime4.jsx)(
    "a",
    {
      "aria-label": t2("aria/Download attachment"),
      className: clsx_default(
        "str-chat__button",
        "str-chat__button--secondary",
        "str-chat__button--outline",
        "str-chat__button--circular",
        "str-chat__button--size-sm",
        "str-chat__audio-attachment-download-button",
        className
      ),
      download: suggestedFileName ?? "",
      href,
      rel: "noopener noreferrer",
      target: "_blank",
      title: tooltipTitle ?? t2("Download Attachment"),
      children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__button__content", children: (0, import_jsx_runtime4.jsx)(IconDownload, { className: "str-chat__icon str-chat__audio-attachment-download-button__icon" }) })
    }
  );
};
function prettifyFileSize(bytes, precision = 3) {
  const units = ["B", "kB", "MB", "GB"];
  const exponent = bytes === 0 ? 0 : Math.min(Math.floor(Math.log(bytes) / Math.log(1024)), units.length - 1);
  const mantissa = bytes / 1024 ** exponent;
  const formattedMantissa = precision === 0 ? Math.round(mantissa).toString() : mantissa.toPrecision(precision);
  return `${formattedMantissa} ${units[exponent]}`;
}
var FileSizeIndicator = ({
  fileSize,
  maximumFractionDigits
}) => {
  const actualFileSize = typeof fileSize === "string" ? parseFloat(fileSize) : fileSize;
  if (typeof actualFileSize === "undefined" || !Number.isFinite(Number(actualFileSize))) {
    return null;
  }
  return (0, import_jsx_runtime4.jsx)(
    "span",
    {
      className: "str-chat__message-attachment-file--item-size",
      "data-testid": "file-size-indicator",
      children: prettifyFileSize(actualFileSize, maximumFractionDigits)
    }
  );
};
var useNotifications = (options) => {
  const { client } = useChatContext();
  const selector2 = (0, import_react29.useCallback)(
    (state) => {
      const notifications2 = state.notifications;
      const panel = options?.panel;
      const byPanel = panel ? notifications2.filter(
        (notification) => isNotificationForPanel(notification, panel, {
          fallbackPanel: options?.fallbackPanel
        })
      ) : notifications2;
      return {
        notifications: options?.filter ? byPanel.filter(options.filter) : byPanel
      };
    },
    [options?.fallbackPanel, options?.filter, options?.panel]
  );
  const { notifications } = useStateStore(client.notifications.store, selector2);
  return notifications;
};
var PlayButton = ({ className, isPlaying, ...props }) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    appearance: "outline",
    circular: true,
    className: clsx_default("str-chat__button-play", className),
    "data-testid": isPlaying ? "pause-audio" : "play-audio",
    size: "sm",
    variant: "secondary",
    ...props,
    children: isPlaying ? (0, import_jsx_runtime4.jsx)(IconPauseFill, {}) : (0, import_jsx_runtime4.jsx)(IconPlayFill, {})
  }
);
var Anonymous$b = "Anonym";
var Archive$b = "Archivieren";
var Attach$b = "Anhängen";
var Attachment$c = "Anhang";
var Back$b = "Back";
var Cancel$b = "Abbrechen";
var Channels$b = "Kanäle";
var Chats$b = "Chats";
var Close$b = "Schließen";
var Commands$b = "Befehle";
var Create$b = "Erstellen";
var Delete$b = "Löschen";
var Delivered$b = "Zugestellt";
var Edited$b = "Bearbeitet";
var End$b = "Beenden";
var File$c = "Datei";
var fileCount_one$9 = "1 datei";
var fileCount_other$b = "{{ count }} dateien";
var Flag$b = "Melden";
var Image$c = "Bild";
var imageCount_one$9 = "Bild";
var imageCount_other$b = "{{ count }} bilder";
var Link$b = "Link";
var linkCount_one$9 = "Link";
var linkCount_other$b = "{{ count }} links";
var live$b = "live";
var Location$b = "Standort";
var Menu$b = "Menü";
var Mute$b = "Stummschalten";
var New$b = "Neu";
var Offline$b = "Offline";
var Ok$b = "OK";
var Online$b = "Online";
var Options$b = "Optionen";
var Original$b = "Original";
var Photo$b = "Foto";
var Pin$b = "Anheften";
var Poll$c = "Umfrage";
var Question$b = "Frage";
var Reply$b = "Antworten";
var replyCount_one$b = "1 Antwort";
var replyCount_other$b = "{{ count }} Antworten";
var Resend$b = "Erneut senden";
var Search$c = "Suche";
var searchResultsCount_one$b = "1 Ergebnis";
var searchResultsCount_other$b = "{{ count }} Ergebnisse";
var Send$b = "Senden";
var Sent$b = "Gesendet";
var Share$b = "Teilen";
var Shuffle$b = "Mischen";
var Submit$b = "Absenden";
var Thread$c = "Thread";
var Threads$b = "Diskussionen";
var totalVoteCount_one$9 = "1 Stimme insgesamt";
var totalVoteCount_other$b = "{{ count }} Stimmen insgesamt";
var Translated$b = "Übersetzt";
var Unarchive$b = "Archivierung aufheben";
var Unmute$b = "Stummschaltung aufheben";
var Unpin$b = "Anheftung aufheben";
var Update$b = "Aktualisieren";
var Video$b = "Video";
var videoCount_one$9 = "Video";
var videoCount_other$b = "{{ count }} videos";
var View$b = "Ansehen";
var voiceMessageCount_one$9 = "Sprachnachricht";
var voiceMessageCount_other$b = "{{ count }} sprachnachrichten";
var Votes$b = "Stimmen";
var You$b = "Du";
var deTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} und {{moreCount}} mehr",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} und {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} Datei",
  "{{ count }} files_other": "{{ count }} Dateien",
  "{{ count }} people are typing_one": "{{ count }} Person tippt",
  "{{ count }} people are typing_many": "{{ count }} Personen tippen",
  "{{ count }} people are typing_other": "{{ count }} Personen tippen",
  "{{ count }} photos_one": "{{ count }} Foto",
  "{{ count }} photos_other": "{{ count }} Fotos",
  "{{ count }} reactions_one": "{{ count }} Reaktion",
  "{{ count }} reactions_other": "{{ count }} Reaktionen",
  "{{ count }} videos_one": "{{ count }} Video",
  "{{ count }} videos_other": "{{ count }} Videos",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} und {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} mehr",
  "{{ memberCount }} members": "{{ memberCount }} Mitglieder",
  "{{ typing }} are typing": "{{ typing }} tippen",
  "{{ typing }} is typing": "{{ typing }} tippt",
  "{{ user }} has been muted": "{{ user }} wurde stummgeschaltet",
  "{{ user }} has been unmuted": "Die Stummschaltung von {{ user }} wurde aufgehoben",
  "{{ user }} is typing...": "{{ user }} tippt...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} und {{ user }} tippen...",
  "{{ users }} and more are typing...": "{{ users }} und mehr tippen...",
  "{{ watcherCount }} online": "{{ watcherCount }} online",
  "{{count}} new messages_one": "{{count}} neue Nachricht",
  "{{count}} new messages_other": "{{count}} neue Nachrichten",
  "{{count}} unread_one": "{{count}} ungelesen",
  "{{count}} unread_other": "{{count}} ungelesen",
  "{{count}} votes_one": "{{count}} Stimme",
  "{{count}} votes_other": "{{count}} Stimmen",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} weitere Option",
  "+{{count}} more options_other": "+{{count}} weitere Optionen",
  "🏙 Attachment...": "🏙 Anhang...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} hat erstellt: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} hat abgestimmt: {{pollOptionText}}",
  "📍Shared location": "📍Geteilter Standort",
  "Add a comment": "Einen Kommentar hinzufügen",
  "Add an option": "Eine Option hinzufügen",
  "Add reaction": "Reaktion hinzufügen",
  "All results loaded": "Alle Ergebnisse geladen",
  "Allow access to camera": "Zugriff auf Kamera erlauben",
  "Allow access to microphone": "Zugriff auf Mikrofon erlauben",
  "Allow comments": "Kommentare erlauben",
  "Allow option suggestion": "Optionsvorschläge erlauben",
  "Allow others to add comments": "Anderen das Hinzufügen von Kommentaren erlauben",
  "Also send as a direct message": "Auch als Direktnachricht senden",
  "Also send in channel": "Auch im Kanal senden",
  "Also sent in channel": "Auch im Kanal gesendet",
  "An error has occurred during recording": "Ein Fehler ist während der Aufnahme aufgetreten",
  "An error has occurred during the recording processing": "Ein Fehler ist während der Aufnahmeverarbeitung aufgetreten",
  Anonymous: Anonymous$b,
  "Anonymous poll": "Anonyme Umfrage",
  Archive: Archive$b,
  "Are you sure you want to delete this message?": "Sind Sie sicher, dass Sie diese Nachricht löschen möchten?",
  "aria/Attachment": "Anhang",
  "aria/Block User": "Benutzer blockieren",
  "aria/Bookmark Message": "Nachricht für später speichern",
  "aria/Cancel Reply": "Antwort abbrechen",
  "aria/Cancel upload": "Upload abbrechen",
  "aria/Channel list": "Kanalliste",
  "aria/Channel search results": "Kanalsuchergebnisse",
  "aria/Close thread": "Thread schließen",
  "aria/Collapse sidebar": "Seitenleiste einklappen",
  "aria/Copy Message Text": "Nachrichtentext kopieren",
  "aria/Delete Message": "Nachricht löschen",
  "aria/Download attachment": "Anhang herunterladen",
  "aria/Edit Message": "Nachricht bearbeiten",
  "aria/Emoji picker": "Emoji-Auswahl",
  "aria/Expand sidebar": "Seitenleiste einblenden",
  "aria/File input": "Dateieingabe",
  "aria/File upload": "Datei hochladen",
  "aria/Flag Message": "Nachricht melden",
  "aria/Image failed to load": "Bild konnte nicht geladen werden",
  "aria/Image input": "Bildeingabe",
  "aria/Load More Channels": "Mehr Kanäle laden",
  "aria/Mark Message Unread": "Als ungelesen markieren",
  "aria/Menu": "Menü",
  "aria/Message Options": "Nachrichtenoptionen",
  "aria/Mute User": "Benutzer stummschalten",
  "aria/Open Attachment Selector": "Anhang-Auswahl öffnen",
  "aria/Open Menu": "Menü öffnen",
  "aria/Open Message Actions Menu": "Nachrichtenaktionsmenü öffnen",
  "aria/Open Reaction Selector": "Reaktionsauswahl öffnen",
  "aria/Open Thread": "Thread öffnen",
  "aria/Percent complete": "{{percent}} Prozent abgeschlossen",
  "aria/Pin Message": "Nachricht anheften",
  "aria/Quote Message": "Nachricht zitieren",
  "aria/Reaction list": "Reaktionsliste",
  "aria/Remind Me Message": "Erinnern",
  "aria/Remind Me Options": "Erinnerungsoptionen",
  "aria/Remove attachment": "Anhang entfernen",
  "aria/Remove location attachment": "Standortanhang entfernen",
  "aria/Remove Reminder": "Erinnerung entfernen",
  "aria/Remove Save For Later": "„Später ansehen“ entfernen",
  "aria/Resend Message": "Nachricht erneut senden",
  "aria/Retry upload": "Upload erneut versuchen",
  "aria/Search results": "Suchergebnisse",
  "aria/Search results header filter button": "Suchergebnisse-Kopfzeilen-Filterbutton",
  "aria/Send": "Senden",
  "aria/Show preview": "Vorschau anzeigen",
  "aria/Stop AI Generation": "KI-Generierung stoppen",
  "aria/Unblock User": "Benutzer entsperren",
  "aria/Unmute User": "Stummschaltung aufheben",
  "aria/Unpin Message": "Anheftung aufheben",
  "Ask a question": "Eine Frage stellen",
  Attach: Attach$b,
  "Attach files": "Dateien anhängen",
  Attachment: Attachment$c,
  "Attachment upload blocked due to {{reason}}": "Anhang-Upload blockiert wegen {{reason}}",
  "Attachment upload failed due to {{reason}}": "Anhang-Upload fehlgeschlagen wegen {{reason}}",
  Back: Back$b,
  "ban-command-args": "[@Benutzername] [Text]",
  "ban-command-description": "Einen Benutzer verbannen",
  "Block User": "Benutzer blockieren",
  Cancel: Cancel$b,
  "Cannot seek in the recording": "In der Aufnahme kann nicht gesucht werden",
  "Channel archived": "Kanal archiviert",
  "Channel Missing": "Kanal fehlt",
  "Channel muted": "Kanal stummgeschaltet",
  "Channel pinned": "Kanal angeheftet",
  "Channel unarchived": "Kanal dearchiviert",
  "Channel unmuted": "Stummschaltung des Kanals aufgehoben",
  "Channel unpinned": "Kanal nicht mehr angeheftet",
  Channels: Channels$b,
  Chats: Chats$b,
  "Choose between 2 to 10 options": "Wähle zwischen 2 und 10 Optionen",
  Close: Close$b,
  "Close emoji picker": "Emoji-Auswahl schließen",
  Commands: Commands$b,
  "Commands matching": "Übereinstimmende Befehle",
  "Connection failure, reconnecting now...": "Verbindungsfehler, Wiederherstellung der Verbindung...",
  "Copy Message": "Nachricht kopieren",
  Create: Create$b,
  "Create poll": "Umfrage erstellen",
  "Current location": "Aktueller Standort",
  Delete: Delete$b,
  "Delete for me": "Für mich löschen",
  "Delete message": "Nachricht löschen",
  Delivered: Delivered$b,
  "Direct message": "Direktnachricht",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Möchten Sie diese Umfrage jetzt beenden? Niemand wird mehr in dieser Umfrage abstimmen können.",
  "Download All": "Alle herunterladen",
  "Download Attachment": "Anhang herunterladen",
  "Download attachment {{ name }}": "Anhang {{ name }} herunterladen",
  "Drag your files here": "Ziehen Sie Ihre Dateien hierher",
  "Drag your files here to add to your post": "Ziehen Sie Ihre Dateien hierher, um sie Ihrem Beitrag hinzuzufügen",
  "Due {{ timeLeft }}": "Fällig {{ timeLeft }}",
  "Due since {{ dueSince }}": "Fällig seit {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Nachricht bearbeiten",
  "Edit message request failed": "Anfrage zum Bearbeiten der Nachricht fehlgeschlagen",
  Edited: Edited$b,
  "Emoji matching": "Passende Emojis",
  "Empty message...": "Leere Nachricht...",
  End: End$b,
  "End poll": "Umfrage beenden",
  "End this poll?": "Umfrage beenden?",
  "End vote": "Abstimmung beenden",
  "Enforce unique vote is enabled": "Eindeutige Abstimmung ist aktiviert",
  "Error": "Fehler",
  "Error adding flag": "Fehler beim Hinzufügen des Flags",
  "Error connecting to chat, refresh the page to try again.": "Verbindungsfehler zum Chat, aktualisieren Sie die Seite, um es erneut zu versuchen.",
  "Error deleting message": "Fehler beim Löschen der Nachricht",
  "Error fetching reactions": "Fehler beim Laden von Reaktionen",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Fehler beim Markieren der Nachricht als ungelesen. Kann keine älteren ungelesenen Nachrichten markieren als die neuesten 100 Kanalnachrichten.",
  "Error muting a user ...": "Fehler beim Stummschalten eines Nutzers.",
  "Error pinning message": "Fehler beim Pinnen der Nachricht",
  "Error removing message pin": "Fehler beim Entfernen der gepinnten Nachricht",
  "Error reproducing the recording": "Fehler bei der Wiedergabe der Aufnahme",
  "Error starting recording": "Fehler beim Starten der Aufnahme",
  "Error unmuting a user ...": "Fehler beim Aufheben der Stummschaltung eines Nutzers ...",
  "Error uploading attachment": "Fehler beim Hochladen des Anhangs",
  "Error uploading file": "Fehler beim Hochladen der Datei",
  "Error uploading image": "Fehler beim Hochladen des Bildes",
  "Error: {{ errorMessage }}": "Fehler: {{ errorMessage }}",
  "Failed to block user": "Benutzer konnte nicht blockiert werden",
  "Failed to create the poll": "Fehler beim Erstellen der Umfrage",
  "Failed to create the poll due to {{reason}}": "Die Umfrage konnte aufgrund von {{reason}} nicht erstellt werden",
  "Failed to delete the message": "Nachricht konnte nicht gelöscht werden",
  "Failed to end the poll": "Umfrage konnte nicht beendet werden",
  "Failed to end the poll due to {{reason}}": "Umfrage konnte aufgrund von {{reason}} nicht beendet werden",
  "Failed to jump to the first unread message": "Fehler beim Springen zur ersten ungelesenen Nachricht",
  "Failed to leave channel": "Kanal konnte nicht verlassen werden",
  "Failed to mark channel as read": "Fehler beim Markieren des Kanals als gelesen",
  "Failed to play the recording": "Wiedergabe der Aufnahme fehlgeschlagen",
  "Failed to retrieve location": "Standort konnte nicht abgerufen werden",
  "Failed to share location": "Standort konnte nicht geteilt werden",
  "Failed to update channel archive status": "Archivierungsstatus des Kanals konnte nicht aktualisiert werden",
  "Failed to update channel mute status": "Stummschaltungsstatus des Kanals konnte nicht aktualisiert werden",
  "Failed to update channel pinned status": "Anheftstatus des Kanals konnte nicht aktualisiert werden",
  File: File$c,
  "File is required for upload attachment": "Datei ist für den Anhang-Upload erforderlich",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "Datei ist zu groß: {{ size }}, maximale Upload-Größe beträgt {{ limit }}",
  "File too large": "Datei ist zu groß",
  fileCount_one: fileCount_one$9,
  fileCount_other: fileCount_other$b,
  Flag: Flag$b,
  "Generating...": "Generieren...",
  "giphy-command-args": "[Text]",
  "giphy-command-description": "Poste ein zufälliges Gif in den Kanal",
  "Hide who voted": "Verbergen, wer abgestimmt hat",
  Image: Image$c,
  imageCount_one: imageCount_one$9,
  imageCount_other: imageCount_other$b,
  "Instant commands": "Instant commands",
  "language/af": "Afrikaans",
  "language/am": "Amharisch",
  "language/ar": "Arabisch",
  "language/az": "Aserbaidschanisch",
  "language/bg": "Bulgarisch",
  "language/bn": "Bengalisch",
  "language/bs": "Bosnisch",
  "language/cs": "Tschechisch",
  "language/da": "Dänisch",
  "language/de": "Deutsch",
  "language/el": "Griechisch",
  "language/en": "Englisch",
  "language/es": "Spanisch",
  "language/es-MX": "Spanisch (Mexiko)",
  "language/et": "Estnisch",
  "language/fa": "Persisch",
  "language/fa-AF": "Dari",
  "language/fi": "Finnisch",
  "language/fr": "Französisch",
  "language/fr-CA": "Französisch (Kanada)",
  "language/ha": "Hausa",
  "language/he": "Hebräisch",
  "language/hi": "Hindi",
  "language/hr": "Kroatisch",
  "language/ht": "Haitianisches Kreolisch",
  "language/hu": "Ungarisch",
  "language/id": "Indonesisch",
  "language/it": "Italienisch",
  "language/ja": "Japanisch",
  "language/ka": "Georgisch",
  "language/ko": "Koreanisch",
  "language/lt": "Litauisch",
  "language/lv": "Lettisch",
  "language/ms": "Malaiisch",
  "language/nl": "Niederländisch",
  "language/no": "Norwegisch",
  "language/pl": "Polnisch",
  "language/ps": "Paschtu",
  "language/pt": "Portugiesisch",
  "language/ro": "Rumänisch",
  "language/ru": "Russisch",
  "language/sk": "Slowakisch",
  "language/sl": "Slowenisch",
  "language/so": "Somali",
  "language/sq": "Albanisch",
  "language/sr": "Serbisch",
  "language/sv": "Schwedisch",
  "language/sw": "Swahili",
  "language/ta": "Tamil",
  "language/th": "Thailändisch",
  "language/tl": "Tagalog",
  "language/tr": "Türkisch",
  "language/uk": "Ukrainisch",
  "language/ur": "Urdu",
  "language/vi": "Vietnamesisch",
  "language/zh": "Chinesisch (Vereinfacht)",
  "language/zh-TW": "Chinesisch (Traditionell)",
  "Leave Channel": "Kanal verlassen",
  "Left channel": "Kanal verlassen",
  "Let others add options": "Andere Optionen hinzufügen lassen",
  "Limit votes per person": "Stimmen pro Person begrenzen",
  Link: Link$b,
  linkCount_one: linkCount_one$9,
  linkCount_other: linkCount_other$b,
  live: live$b,
  "Live for {{duration}}": "Live für {{duration}}",
  "Live location": "Live-Standort",
  "Live until {{ timestamp }}": "Live bis {{ timestamp }}",
  "Load more": "Mehr laden",
  "Local upload attachment missing local id": "Lokaler Upload-Anhang hat keine lokale ID",
  Location: Location$b,
  "Location sharing ended": "Standortfreigabe beendet",
  "Location: {{ coordinates }}": "Standort: {{ coordinates }}",
  "Mark as unread": "Als ungelesen markieren",
  "Maximum number of votes (from 2 to 10)": "Maximale Anzahl der Stimmen (von 2 bis 10)",
  Menu: Menu$b,
  "Message deleted": "Nachricht gelöscht",
  "Message failed to send": "Nachricht konnte nicht gesendet werden",
  "Message has been successfully flagged": "Nachricht wurde erfolgreich gemeldet",
  "Message marked as unread": "Nachricht als ungelesen markiert",
  "Message pinned": "Nachricht angeheftet",
  "Message unpinned": "Nachricht nicht mehr angeheftet",
  "Message was blocked by moderation policies": "Nachricht wurde durch moderationsrichtlinien blockiert",
  "Messages have been marked unread.": "Nachrichten wurden als ungelesen markiert.",
  "Missing permissions to upload the attachment": "Fehlende Berechtigungen zum Hochladen des Anhangs",
  "Multiple votes": "Mehrfachstimmen",
  Mute: Mute$b,
  "mute-command-args": "[@Benutzername]",
  "mute-command-description": "Stummschalten eines Benutzers",
  "network error": "Netzwerkfehler",
  New: New$b,
  "New Messages!": "Neue Nachrichten!",
  "Next image": "Nächstes Bild",
  "No chats here yet…": "Noch keine Chats hier...",
  "No conversations yet": "Noch keine Unterhaltungen",
  "No items exist": "Keine Elemente vorhanden",
  "No results found": "Keine Ergebnisse gefunden",
  "Nobody will be able to vote in this poll anymore.": "Niemand kann mehr in dieser Umfrage abstimmen.",
  "Nothing yet...": "Noch nichts...",
  Offline: Offline$b,
  Ok: Ok$b,
  Online: Online$b,
  "Only numbers are allowed": "Nur Zahlen sind erlaubt",
  "Only visible to you": "Nur für dich sichtbar",
  "Open emoji picker": "Emoji-Auswahl öffnen",
  "Open gallery at image {{ index }}": "Galerie bei Bild {{ index }} öffnen",
  "Open image in gallery": "Bild in Galerie öffnen",
  "Option already exists": "Option existiert bereits",
  "Option is empty": "Option ist leer",
  Options: Options$b,
  Original: Original$b,
  "People matching": "Passende Personen",
  Photo: Photo$b,
  Pin: Pin$b,
  "Pinned by {{ name }}": "Angeheftet von {{ name }}",
  "Pinned by You": "Von Ihnen angeheftet",
  "placeholder/PollComment": "Ihr Kommentar",
  "placeholder/PollOptionSuggestion": "Neue Option eingeben",
  "Play video": "Video abspielen",
  Poll: Poll$c,
  "Poll comments": "Umfragekommentare",
  "Poll ended": "Umfrage beendet",
  "Poll options": "Umfrageoptionen",
  "Poll results": "Umfrageergebnisse",
  "Previous image": "Vorheriges Bild",
  Question: Question$b,
  "Question {{ optionOrderNumber}}": "Frage {{ optionOrderNumber}}",
  "Question is required": "Frage ist erforderlich",
  "Quote Reply": "Zitat-Antwort",
  "Reached the vote limit. Remove an existing vote first.": "Das Abstimmungslimit wurde erreicht. Entfernen Sie zuerst eine bestehende Stimme.",
  "Recording format is not supported and cannot be reproduced": "Aufnahmeformat wird nicht unterstützt und kann nicht wiedergegeben werden",
  "Remind me": "Erinnern",
  "Remind Me": "Erinnern",
  "Reminder set": "Erinnerung gesetzt",
  "Remove reminder": "Erinnerung entfernen",
  "Remove save for later": "„Später ansehen“ entfernen",
  "Replied to a thread": "In einem Thread geantwortet",
  Reply: Reply$b,
  "Reply to {{ authorName }}": "Antwort an {{ authorName }}",
  "Reply to a message to start a thread": "Antworte auf eine Nachricht, um einen Thread zu starten",
  "Reply to Message": "Auf Nachricht antworten",
  replyCount_one: replyCount_one$b,
  replyCount_other: replyCount_other$b,
  Resend: Resend$b,
  "Retry upload": "Upload erneut versuchen",
  "Save for later": "Für später speichern",
  "Saved for later": "Für später gespeichert",
  Search: Search$c,
  "Search GIFs": "Search GIFs",
  "search-results-header-filter-source-button-label--channels": "Kanäle",
  "search-results-header-filter-source-button-label--messages": "Nachrichten",
  "search-results-header-filter-source-button-label--users": "Benutzer",
  "Searching for {{ searchSourceType }}...": "Suche nach {{ searchSourceType }}...",
  "Searching...": "Suchen...",
  searchResultsCount_one: searchResultsCount_one$b,
  searchResultsCount_other: searchResultsCount_other$b,
  "Select a thread to continue the conversation": "Wähle einen Thread aus, um die Unterhaltung fortzusetzen",
  "Select more than one option": "Mehr als eine Option auswählen",
  "Select one": "Eine auswählen",
  "Select one or more": "Eine oder mehrere auswählen",
  "Select up to {{count}}_one": "Bis zu {{count}} auswählen",
  "Select up to {{count}}_other": "Bis zu {{count}} auswählen",
  Send: Send$b,
  "Send a message to start the conversation": "Senden Sie eine Nachricht, um die Unterhaltung zu beginnen",
  "Send Anyway": "Trotzdem senden",
  "Send message request failed": "Senden der Nachrichtenanfrage fehlgeschlagen",
  "Send poll": "Umfrage senden",
  "Sending...": "Senden...",
  Sent: Sent$b,
  Share: Share$b,
  "Share live location for": "Live-Standort teilen für",
  "Share Location": "Standort teilen",
  "Shared live location": "Geteilter Live-Standort",
  "Shared location": "Geteilter Standort",
  Shuffle: Shuffle$b,
  "size limit": "Größenbeschränkung",
  "Slow Mode ON": "Langsamer Modus EIN",
  "Slow mode, wait {{ seconds }}s...": "Langsamer Modus, warte {{ seconds }}s...",
  "Some of the files will not be accepted": "Einige der Dateien werden nicht akzeptiert",
  "Start typing to search": "Tippen Sie, um zu suchen",
  "Stop sharing": "Teilen beenden",
  Submit: Submit$b,
  "Suggest an option": "Eine Option vorschlagen",
  "Tap to remove": "Tippen zum Entfernen",
  "Thinking...": "Denken...",
  "this content could not be displayed": "Dieser Inhalt konnte nicht angezeigt werden",
  "This field cannot be empty or contain only spaces": "Dieses Feld darf nicht leer sein oder nur Leerzeichen enthalten",
  "This message did not meet our content guidelines": "Diese Nachricht entsprach nicht unseren Inhaltsrichtlinien",
  Thread: Thread$c,
  "Thread has not been found": "Thread wurde nicht gefunden",
  "Thread reply": "Thread-Antwort",
  "Thread Reply": "Thread-Antwort",
  "ThreadListUnseenThreadsBanner/loading": "Laden...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} ungelesener Thread",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} ungelesene Threads",
  Threads: Threads$b,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Gestern]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Heute]", "nextDay": "[Morgen]", "lastDay": "[Gestern]", "nextWeek": "dddd", "lastWeek": "[Letzte] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Heute",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Gestern",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Um mit der Aufnahme zu beginnen, erlauben Sie den Zugriff auf die Kamera in Ihrem Browser",
  "To start recording, allow the microphone access in your browser": "Um mit der Aufnahme zu beginnen, erlauben Sie den Zugriff auf das Mikrofon in Ihrem Browser",
  totalVoteCount_one: totalVoteCount_one$9,
  totalVoteCount_other: totalVoteCount_other$b,
  Translated: Translated$b,
  "Translated from {{ language }}": "Übersetzung aus {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Geben Sie eine Zahl von 2 bis 10 ein",
  "Type your message": "Nachricht eingeben",
  Unarchive: Unarchive$b,
  "unban-command-args": "[@Benutzername]",
  "unban-command-description": "Einen Benutzer entbannen",
  "Unblock User": "Benutzer entsperren",
  "unknown error": "Unbekannter Fehler",
  Unmute: Unmute$b,
  "unmute-command-args": "[@Benutzername]",
  "unmute-command-description": "Stummschaltung eines Benutzers aufheben",
  Unpin: Unpin$b,
  "Unread messages": "Ungelesene Nachrichten",
  "Unsupported attachment": "Nicht unterstützter Anhang",
  "unsupported file type": "Nicht unterstützter Dateityp",
  Update: Update$b,
  "Update your comment": "Ihren Kommentar aktualisieren",
  "Upload blocked": "Upload blockiert",
  "Upload error": "Upload-Fehler",
  "Upload failed": "Upload fehlgeschlagen",
  'Upload type: "{{ type }}" is not allowed': 'Upload-Typ: "{{ type }}" ist nicht erlaubt',
  "User blocked": "Benutzer blockiert",
  "User unblocked": "Blockierung des Benutzers aufgehoben",
  "User uploaded content": "Vom Benutzer hochgeladener Inhalt",
  Video: Video$b,
  videoCount_one: videoCount_one$9,
  videoCount_other: videoCount_other$b,
  View: View$b,
  "View {{count}} comments_one": "{{count}} Kommentar anzeigen",
  "View {{count}} comments_other": "{{count}} Kommentare anzeigen",
  "View all": "Alle anzeigen",
  "View original": "Original anzeigen",
  "View results": "Ergebnisse anzeigen",
  "View translation": "Übersetzung anzeigen",
  "Voice message": "Sprachnachricht",
  "Voice message {{ duration }}": "Sprachnachricht {{ duration }}",
  "Voice message deleted": "Sprachnachricht gelöscht",
  voiceMessageCount_one: voiceMessageCount_one$9,
  voiceMessageCount_other: voiceMessageCount_other$b,
  "Vote ended": "Abstimmung beendet",
  Votes: Votes$b,
  "Wait until all attachments have uploaded": "Bitte warten, bis alle Anhänge hochgeladen wurden",
  "Waiting for network…": "Warte auf Netzwerk…",
  You: You$b,
  "You've reached the maximum number of files": "Die maximale Anzahl an Dateien ist erreicht"
};
var Anonymous$a = "Anonymous";
var Archive$a = "Archive";
var Attach$a = "Attach";
var Attachment$b = "Attachment";
var Back$a = "Back";
var Cancel$a = "Cancel";
var Channels$a = "Channels";
var Chats$a = "Chats";
var Close$a = "Close";
var Commands$a = "Commands";
var Create$a = "Create";
var Delete$a = "Delete";
var Delivered$a = "Delivered";
var Edited$a = "Edited";
var End$a = "End";
var File$b = "File";
var fileCount_one$8 = "File";
var fileCount_other$a = "{{ count }} files";
var Flag$a = "Flag";
var Image$b = "Image";
var imageCount_one$8 = "Image";
var imageCount_other$a = "{{ count }} images";
var Link$a = "Link";
var linkCount_one$8 = "Link";
var linkCount_other$a = "{{ count }} links";
var live$a = "live";
var Location$a = "Location";
var Menu$a = "Menu";
var Mute$a = "Mute";
var New$a = "New";
var Offline$a = "Offline";
var Ok$a = "Ok";
var Online$a = "Online";
var Options$a = "Options";
var Original$a = "Original";
var Photo$a = "Photo";
var Pin$a = "Pin";
var Poll$b = "Poll";
var Question$a = "Question";
var Reply$a = "Reply";
var replyCount_one$a = "1 reply";
var replyCount_other$a = "{{ count }} replies";
var Resend$a = "Resend";
var Search$b = "Search";
var searchResultsCount_one$a = "1 result";
var searchResultsCount_other$a = "{{ count }} results";
var Send$a = "Send";
var Sent$a = "Sent";
var Share$a = "Share";
var Shuffle$a = "Shuffle";
var Submit$a = "Submit";
var Thread$b = "Thread";
var Threads$a = "Threads";
var totalVoteCount_one$8 = "1 vote total";
var totalVoteCount_other$a = "{{ count }} votes total";
var Translated$a = "Translated";
var Unarchive$a = "Unarchive";
var Unmute$a = "Unmute";
var Unpin$a = "Unpin";
var Update$a = "Update";
var Video$a = "Video";
var videoCount_one$8 = "Video";
var videoCount_other$a = "{{ count }} videos";
var View$a = "View";
var voiceMessageCount_one$8 = "Voice message";
var voiceMessageCount_other$a = "{{ count }} voice messages";
var Votes$a = "Votes";
var You$a = "You";
var enTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} and {{ moreCount }} more",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }}, and {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} file",
  "{{ count }} files_other": "{{ count }} files",
  "{{ count }} people are typing_one": "{{ count }} person is typing",
  "{{ count }} people are typing_many": "{{ count }} people are typing",
  "{{ count }} people are typing_other": "{{ count }} people are typing",
  "{{ count }} photos_one": "{{ count }} photo",
  "{{ count }} photos_other": "{{ count }} photos",
  "{{ count }} reactions_one": "{{ count }} reaction",
  "{{ count }} reactions_other": "{{ count }} reactions",
  "{{ count }} videos_one": "{{ count }} video",
  "{{ count }} videos_other": "{{ count }} videos",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} and {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} more",
  "{{ memberCount }} members": "{{ memberCount }} members",
  "{{ typing }} are typing": "{{ typing }} are typing",
  "{{ typing }} is typing": "{{ typing }} is typing",
  "{{ user }} has been muted": "{{ user }} has been muted",
  "{{ user }} has been unmuted": "{{ user }} has been unmuted",
  "{{ user }} is typing...": "{{ user }} is typing...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} and {{ user }} are typing...",
  "{{ users }} and more are typing...": "{{ users }} and more are typing...",
  "{{ watcherCount }} online": "{{ watcherCount }} online",
  "{{count}} new messages_one": "{{count}} new message",
  "{{count}} new messages_other": "{{count}} new messages",
  "{{count}} unread_one": "{{count}} unread",
  "{{count}} unread_other": "{{count}} unread",
  "{{count}} votes_one": "{{count}} vote",
  "{{count}} votes_other": "{{count}} votes",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} more option",
  "+{{count}} more options_other": "+{{count}} more options",
  "🏙 Attachment...": "🏙 Attachment...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} created: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} voted: {{pollOptionText}}",
  "📍Shared location": "📍Shared location",
  "Add a comment": "Add a Comment",
  "Add an option": "Add an Option",
  "Add reaction": "Add reaction",
  "All results loaded": "All results loaded",
  "Allow access to camera": "Allow access to camera",
  "Allow access to microphone": "Allow access to microphone",
  "Allow comments": "Allow comments",
  "Allow option suggestion": "Allow option suggestion",
  "Allow others to add comments": "Allow Others to Add Comments",
  "Also send as a direct message": "Also send as a direct message",
  "Also send in channel": "Also send in channel",
  "Also sent in channel": "Also sent in channel",
  "An error has occurred during recording": "An error has occurred during recording",
  "An error has occurred during the recording processing": "An error has occurred during the recording processing",
  Anonymous: Anonymous$a,
  "Anonymous poll": "Anonymous Poll",
  Archive: Archive$a,
  "Are you sure you want to delete this message?": "Are you sure you want to delete this message?",
  "aria/Attachment": "Attachment",
  "aria/Block User": "Block User",
  "aria/Bookmark Message": "Bookmark Message",
  "aria/Cancel Reply": "Cancel Reply",
  "aria/Cancel upload": "Cancel upload",
  "aria/Channel list": "Channel list",
  "aria/Channel search results": "Channel search results",
  "aria/Close thread": "Close thread",
  "aria/Collapse sidebar": "Collapse sidebar",
  "aria/Copy Message Text": "Copy Message Text",
  "aria/Delete Message": "Delete Message",
  "aria/Download attachment": "Download attachment",
  "aria/Edit Message": "Edit Message",
  "aria/Emoji picker": "Emoji picker",
  "aria/Expand sidebar": "Expand sidebar",
  "aria/File input": "File input",
  "aria/File upload": "File upload",
  "aria/Flag Message": "Flag Message",
  "aria/Image failed to load": "Image failed to load",
  "aria/Image input": "Image input",
  "aria/Load More Channels": "Load More Channels",
  "aria/Mark Message Unread": "Mark Message Unread",
  "aria/Menu": "Menu",
  "aria/Message Options": "Message Options",
  "aria/Mute User": "Mute User",
  "aria/Open Attachment Selector": "Open Attachment Selector",
  "aria/Open Menu": "Open Menu",
  "aria/Open Message Actions Menu": "Open Message Actions Menu",
  "aria/Open Reaction Selector": "Open Reaction Selector",
  "aria/Open Thread": "Open Thread",
  "aria/Percent complete": "{{percent}} percent complete",
  "aria/Pin Message": "Pin Message",
  "aria/Quote Message": "Quote Message",
  "aria/Reaction list": "Reaction list",
  "aria/Remind Me Message": "Remind Me Message",
  "aria/Remind Me Options": "aria/Remind Me Options",
  "aria/Remove attachment": "Remove attachment",
  "aria/Remove location attachment": "Remove location attachment",
  "aria/Remove Reminder": "Remove Reminder",
  "aria/Remove Save For Later": "Remove Save For Later",
  "aria/Resend Message": "Resend Message",
  "aria/Retry upload": "Retry upload",
  "aria/Search results": "Search results",
  "aria/Search results header filter button": "Search results header filter button",
  "aria/Send": "Send",
  "aria/Show preview": "Show preview",
  "aria/Stop AI Generation": "Stop AI Generation",
  "aria/Unblock User": "Unblock User",
  "aria/Unmute User": "Unmute User",
  "aria/Unpin Message": "Unpin Message",
  "Ask a question": "Ask a Question",
  Attach: Attach$a,
  "Attach files": "Attach files",
  Attachment: Attachment$b,
  "Attachment upload blocked due to {{reason}}": "Attachment upload blocked due to {{reason}}",
  "Attachment upload failed due to {{reason}}": "Attachment upload failed due to {{reason}}",
  Back: Back$a,
  "ban-command-args": "[@username] [text]",
  "ban-command-description": "Ban a user",
  "Block User": "Block User",
  Cancel: Cancel$a,
  "Cannot seek in the recording": "Cannot seek in the recording",
  "Channel archived": "Channel archived",
  "Channel Missing": "Channel Missing",
  "Channel muted": "Channel muted",
  "Channel pinned": "Channel pinned",
  "Channel unarchived": "Channel unarchived",
  "Channel unmuted": "Channel unmuted",
  "Channel unpinned": "Channel unpinned",
  Channels: Channels$a,
  Chats: Chats$a,
  "Choose between 2 to 10 options": "Choose Between 2 to 10 Options",
  Close: Close$a,
  "Close emoji picker": "Close emoji picker",
  Commands: Commands$a,
  "Commands matching": "Commands matching",
  "Connection failure, reconnecting now...": "Connection failure, reconnecting now...",
  "Copy Message": "Copy Message",
  Create: Create$a,
  "Create poll": "Create Poll",
  "Current location": "Current location",
  Delete: Delete$a,
  "Delete for me": "Delete for me",
  "Delete message": "Delete message",
  Delivered: Delivered$a,
  "Direct message": "Direct message",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.",
  "Download All": "Download All",
  "Download Attachment": "Download Attachment",
  "Download attachment {{ name }}": "Download attachment {{ name }}",
  "Drag your files here": "Drag your files here",
  "Drag your files here to add to your post": "Drag your files here to add to your post",
  "Due {{ timeLeft }}": "Due {{ timeLeft }}",
  "Due since {{ dueSince }}": "Due since {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Edit Message",
  "Edit message request failed": "Edit message request failed",
  Edited: Edited$a,
  "Emoji matching": "Emoji matching",
  "Empty message...": "Empty message...",
  End: End$a,
  "End poll": "End Poll",
  "End this poll?": "End this Poll?",
  "End vote": "End Vote",
  "Enforce unique vote is enabled": "Enforce unique vote is enabled",
  "Error": "Error",
  "Error adding flag": "Error adding flag",
  "Error connecting to chat, refresh the page to try again.": "Error connecting to chat, refresh the page to try again.",
  "Error deleting message": "Error deleting message",
  "Error fetching reactions": "Error loading reactions",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.",
  "Error muting a user ...": "Error muting a user ...",
  "Error pinning message": "Error pinning message",
  "Error removing message pin": "Error removing message pin",
  "Error reproducing the recording": "Error reproducing the recording",
  "Error starting recording": "Error starting recording",
  "Error unmuting a user ...": "Error unmuting a user ...",
  "Error uploading attachment": "Error uploading attachment",
  "Error uploading file": "Error uploading file",
  "Error uploading image": "Error uploading image",
  "Error: {{ errorMessage }}": "Error: {{ errorMessage }}",
  "Failed to block user": "Failed to block user",
  "Failed to create the poll": "Failed to create the poll",
  "Failed to create the poll due to {{reason}}": "Failed to create the poll due to {{reason}}",
  "Failed to delete the message": "Failed to delete the message",
  "Failed to end the poll": "Failed to end the poll",
  "Failed to end the poll due to {{reason}}": "Failed to end the poll due to {{reason}}",
  "Failed to jump to the first unread message": "Failed to jump to the first unread message",
  "Failed to leave channel": "Failed to leave channel",
  "Failed to mark channel as read": "Failed to mark channel as read",
  "Failed to play the recording": "Failed to play the recording",
  "Failed to retrieve location": "Failed to retrieve location",
  "Failed to share location": "Failed to share location",
  "Failed to update channel archive status": "Failed to update channel archive status",
  "Failed to update channel mute status": "Failed to update channel mute status",
  "Failed to update channel pinned status": "Failed to update channel pinned status",
  File: File$b,
  "File is required for upload attachment": "File is required for upload attachment",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "File is too large: {{ size }}, maximum upload size is {{ limit }}",
  "File too large": "File too large",
  fileCount_one: fileCount_one$8,
  fileCount_other: fileCount_other$a,
  Flag: Flag$a,
  "Generating...": "Generating...",
  "giphy-command-args": "[text]",
  "giphy-command-description": "Post a random gif to the channel",
  "Hide who voted": "Hide Who Voted",
  Image: Image$b,
  imageCount_one: imageCount_one$8,
  imageCount_other: imageCount_other$a,
  "Instant commands": "Instant commands",
  "language/af": "Afrikaans",
  "language/am": "Amharic",
  "language/ar": "Arabic",
  "language/az": "Azerbaijani",
  "language/bg": "Bulgarian",
  "language/bn": "Bengali",
  "language/bs": "Bosnian",
  "language/cs": "Czech",
  "language/da": "Danish",
  "language/de": "German",
  "language/el": "Greek",
  "language/en": "English",
  "language/es": "Spanish",
  "language/es-MX": "Spanish (Mexico)",
  "language/et": "Estonian",
  "language/fa": "Persian",
  "language/fa-AF": "Dari",
  "language/fi": "Finnish",
  "language/fr": "French",
  "language/fr-CA": "French (Canada)",
  "language/ha": "Hausa",
  "language/he": "Hebrew",
  "language/hi": "Hindi",
  "language/hr": "Croatian",
  "language/ht": "Haitian Creole",
  "language/hu": "Hungarian",
  "language/id": "Indonesian",
  "language/it": "Italian",
  "language/ja": "Japanese",
  "language/ka": "Georgian",
  "language/ko": "Korean",
  "language/lt": "Lithuanian",
  "language/lv": "Latvian",
  "language/ms": "Malay",
  "language/nl": "Dutch",
  "language/no": "Norwegian",
  "language/pl": "Polish",
  "language/ps": "Pashto",
  "language/pt": "Portuguese",
  "language/ro": "Romanian",
  "language/ru": "Russian",
  "language/sk": "Slovak",
  "language/sl": "Slovenian",
  "language/so": "Somali",
  "language/sq": "Albanian",
  "language/sr": "Serbian",
  "language/sv": "Swedish",
  "language/sw": "Swahili",
  "language/ta": "Tamil",
  "language/th": "Thai",
  "language/tl": "Tagalog",
  "language/tr": "Turkish",
  "language/uk": "Ukrainian",
  "language/ur": "Urdu",
  "language/vi": "Vietnamese",
  "language/zh": "Chinese (Simplified)",
  "language/zh-TW": "Chinese (Traditional)",
  "Leave Channel": "Leave Channel",
  "Left channel": "Left channel",
  "Let others add options": "Let Others Add Options",
  "Limit votes per person": "Limit Votes per Person",
  Link: Link$a,
  linkCount_one: linkCount_one$8,
  linkCount_other: linkCount_other$a,
  live: live$a,
  "Live for {{duration}}": "Live for {{duration}}",
  "Live location": "Live location",
  "Live until {{ timestamp }}": "Live until {{ timestamp }}",
  "Load more": "Load more",
  "Local upload attachment missing local id": "Local upload attachment missing local id",
  Location: Location$a,
  "Location sharing ended": "Location sharing ended",
  "Location: {{ coordinates }}": "Location: {{ coordinates }}",
  "Mark as unread": "Mark as unread",
  "Maximum number of votes (from 2 to 10)": "Maximum number of votes (from 2 to 10)",
  Menu: Menu$a,
  "Message deleted": "Message deleted",
  "Message failed to send": "Message failed to send",
  "Message has been successfully flagged": "Message has been successfully flagged",
  "Message marked as unread": "Message marked as unread",
  "Message pinned": "Message pinned",
  "Message unpinned": "Message unpinned",
  "Message was blocked by moderation policies": "Message was blocked by moderation policies",
  "Messages have been marked unread.": "Messages have been marked unread.",
  "Missing permissions to upload the attachment": "Missing permissions to upload the attachment",
  "Multiple votes": "Multiple Votes",
  Mute: Mute$a,
  "mute-command-args": "[@username]",
  "mute-command-description": "Mute a user",
  "network error": "network error",
  New: New$a,
  "New Messages!": "New Messages!",
  "Next image": "Next image",
  "No chats here yet…": "No chats here yet…",
  "No conversations yet": "No conversations yet",
  "No items exist": "No items exist",
  "No results found": "No results found",
  "Nobody will be able to vote in this poll anymore.": "Nobody will be able to vote in this poll anymore.",
  "Nothing yet...": "Nothing yet...",
  Offline: Offline$a,
  Ok: Ok$a,
  Online: Online$a,
  "Only numbers are allowed": "Only numbers are allowed",
  "Only visible to you": "Only visible to you",
  "Open emoji picker": "Open emoji picker",
  "Open gallery at image {{ index }}": "Open gallery at image {{ index }}",
  "Open image in gallery": "Open image in gallery",
  "Option already exists": "Option already exists",
  "Option is empty": "Option is empty",
  Options: Options$a,
  Original: Original$a,
  "People matching": "People matching",
  Photo: Photo$a,
  Pin: Pin$a,
  "Pinned by {{ name }}": "Pinned by {{ name }}",
  "Pinned by You": "Pinned by You",
  "placeholder/PollComment": "Your comment",
  "placeholder/PollOptionSuggestion": "Enter a new option",
  "Play video": "Play video",
  Poll: Poll$b,
  "Poll comments": "Poll Comments",
  "Poll ended": "Poll Ended",
  "Poll options": "Poll Options",
  "Poll results": "Poll Results",
  "Previous image": "Previous image",
  Question: Question$a,
  "Question {{ optionOrderNumber}}": "Question {{ optionOrderNumber}}",
  "Question is required": "Question is required",
  "Quote Reply": "Quote Reply",
  "Reached the vote limit. Remove an existing vote first.": "Reached the vote limit. Remove an existing vote first.",
  "Recording format is not supported and cannot be reproduced": "Recording format is not supported and cannot be reproduced",
  "Remind me": "Remind me",
  "Remind Me": "Remind Me",
  "Reminder set": "Reminder set",
  "Remove reminder": "Remove reminder",
  "Remove save for later": "Remove save for later",
  "Replied to a thread": "Replied to a thread",
  Reply: Reply$a,
  "Reply to {{ authorName }}": "Reply to {{ authorName }}",
  "Reply to a message to start a thread": "Reply to a message to start a thread",
  "Reply to Message": "Reply to Message",
  replyCount_one: replyCount_one$a,
  replyCount_other: replyCount_other$a,
  Resend: Resend$a,
  "Retry upload": "Retry upload",
  "Save for later": "Save for later",
  "Saved for later": "Saved for later",
  Search: Search$b,
  "Search GIFs": "Search GIFs",
  "search-results-header-filter-source-button-label--channels": "channels",
  "search-results-header-filter-source-button-label--messages": "messages",
  "search-results-header-filter-source-button-label--users": "users",
  "Searching for {{ searchSourceType }}...": "Searching for {{ searchSourceType }}...",
  "Searching...": "Searching...",
  searchResultsCount_one: searchResultsCount_one$a,
  searchResultsCount_other: searchResultsCount_other$a,
  "Select a thread to continue the conversation": "Select a thread to continue the conversation",
  "Select more than one option": "Select More Than One Option",
  "Select one": "Select one",
  "Select one or more": "Select one or more",
  "Select up to {{count}}_one": "Select up to {{count}}",
  "Select up to {{count}}_other": "Select up to {{count}}",
  Send: Send$a,
  "Send a message to start the conversation": "Send a message to start the conversation",
  "Send Anyway": "Send Anyway",
  "Send message request failed": "Send message request failed",
  "Send poll": "Send Poll",
  "Sending...": "Sending...",
  Sent: Sent$a,
  Share: Share$a,
  "Share live location for": "Share live location for",
  "Share Location": "Share Location",
  "Shared live location": "Shared live location",
  "Shared location": "Shared location",
  Shuffle: Shuffle$a,
  "size limit": "size limit",
  "Slow Mode ON": "Slow Mode ON",
  "Slow mode, wait {{ seconds }}s...": "Slow mode, wait {{ seconds }}s...",
  "Some of the files will not be accepted": "Some of the files will not be accepted",
  "Start typing to search": "Start typing to search",
  "Stop sharing": "Stop sharing",
  Submit: Submit$a,
  "Suggest an option": "Suggest an Option",
  "Tap to remove": "Tap to remove",
  "Thinking...": "Thinking...",
  "this content could not be displayed": "this content could not be displayed",
  "This field cannot be empty or contain only spaces": "This field cannot be empty or contain only spaces",
  "This message did not meet our content guidelines": "This message did not meet our content guidelines",
  Thread: Thread$b,
  "Thread has not been found": "Thread has not been found",
  "Thread reply": "Thread reply",
  "Thread Reply": "Thread Reply",
  "ThreadListUnseenThreadsBanner/loading": "Loading...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} unread thread",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} unread threads",
  Threads: Threads$a,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Yesterday]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today]", "nextDay": "[Tomorrow]", "lastDay": "[Yesterday]", "nextWeek": "dddd", "lastWeek": "[Last] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Today",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Yesterday",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "To start recording, allow the camera access in your browser",
  "To start recording, allow the microphone access in your browser": "To start recording, allow the microphone access in your browser",
  totalVoteCount_one: totalVoteCount_one$8,
  totalVoteCount_other: totalVoteCount_other$a,
  Translated: Translated$a,
  "Translated from {{ language }}": "Translated from {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Type a number from 2 to 10",
  "Type your message": "Type your message",
  Unarchive: Unarchive$a,
  "unban-command-args": "[@username]",
  "unban-command-description": "Unban a user",
  "Unblock User": "Unblock User",
  "unknown error": "unknown error",
  Unmute: Unmute$a,
  "unmute-command-args": "[@username]",
  "unmute-command-description": "Unmute a user",
  Unpin: Unpin$a,
  "Unread messages": "Unread messages",
  "Unsupported attachment": "Unsupported attachment",
  "unsupported file type": "unsupported file type",
  Update: Update$a,
  "Update your comment": "Update Your Comment",
  "Upload blocked": "Upload blocked",
  "Upload error": "Upload error",
  "Upload failed": "Upload failed",
  'Upload type: "{{ type }}" is not allowed': 'Upload type: "{{ type }}" is not allowed',
  "User blocked": "User blocked",
  "User unblocked": "User unblocked",
  "User uploaded content": "User uploaded content",
  Video: Video$a,
  videoCount_one: videoCount_one$8,
  videoCount_other: videoCount_other$a,
  View: View$a,
  "View {{count}} comments_one": "View {{count}} Comment",
  "View {{count}} comments_other": "View {{count}} Comments",
  "View all": "View all",
  "View original": "View original",
  "View results": "View Results",
  "View translation": "View translation",
  "Voice message": "Voice message",
  "Voice message {{ duration }}": "Voice message {{ duration }}",
  "Voice message deleted": "Voice message deleted",
  voiceMessageCount_one: voiceMessageCount_one$8,
  voiceMessageCount_other: voiceMessageCount_other$a,
  "Vote ended": "Vote ended",
  Votes: Votes$a,
  "Wait until all attachments have uploaded": "Wait until all attachments have uploaded",
  "Waiting for network…": "Waiting for network…",
  You: You$a,
  "You've reached the maximum number of files": "You've reached the maximum number of files"
};
var Anonymous$9 = "Anónimo";
var Archive$9 = "Archivo";
var Attach$9 = "Adjuntar";
var Attachment$a = "Archivo adjunto";
var Back$9 = "Atrás";
var Cancel$9 = "Cancelar";
var Channels$9 = "Canales";
var Chats$9 = "Chats";
var Close$9 = "Cerrar";
var Commands$9 = "Comandos";
var Create$9 = "Crear";
var Delete$9 = "Borrar";
var Delivered$9 = "Entregado";
var Edited$9 = "Editado";
var End$9 = "Final";
var File$a = "Archivo";
var fileCount_one$7 = "1 archivo";
var fileCount_many$4 = "{{ count }} archivos";
var fileCount_other$9 = "{{ count }} archivos";
var Flag$9 = "Marcar";
var Image$a = "Imagen";
var imageCount_one$7 = "Imagen";
var imageCount_many$4 = "{{ count }} imágenes";
var imageCount_other$9 = "{{ count }} imágenes";
var Link$9 = "Enlace";
var linkCount_one$7 = "Enlace";
var linkCount_many$4 = "{{ count }} enlaces";
var linkCount_other$9 = "{{ count }} enlaces";
var live$9 = "En vivo";
var Location$9 = "Ubicación";
var Menu$9 = "Menú";
var Mute$9 = "Silenciar";
var New$9 = "Nuevo";
var Offline$9 = "Desconectado";
var Ok$9 = "Aceptar";
var Online$9 = "En línea";
var Options$9 = "Opciones";
var Original$9 = "Original";
var Photo$9 = "Foto";
var Pin$9 = "Fijar";
var Poll$a = "Encuesta";
var Question$9 = "Pregunta";
var Reply$9 = "Responder";
var replyCount_one$9 = "1 respuesta";
var replyCount_many$4 = "{{ count }} respuestas";
var replyCount_other$9 = "{{ count }} respuestas";
var Resend$9 = "Reenviar";
var Search$a = "Buscar";
var searchResultsCount_one$9 = "1 resultado";
var searchResultsCount_many$4 = "{{ count }} resultados";
var searchResultsCount_other$9 = "{{ count }} resultados";
var Send$9 = "Enviar";
var Sent$9 = "Enviado";
var Share$9 = "Compartir";
var Shuffle$9 = "Mezclar";
var Submit$9 = "Enviar";
var Thread$a = "Hilo";
var Threads$9 = "Hilos";
var totalVoteCount_one$7 = "1 voto en total";
var totalVoteCount_many$4 = "{{ count }} votos en total";
var totalVoteCount_other$9 = "{{ count }} votos en total";
var Translated$9 = "Traducido";
var Unarchive$9 = "Desarchivar";
var Unmute$9 = "Activar sonido";
var Unpin$9 = "Desfijar";
var Update$9 = "Actualizar";
var Video$9 = "Vídeo";
var videoCount_one$7 = "Video";
var videoCount_many$4 = "{{ count }} videos";
var videoCount_other$9 = "{{ count }} videos";
var View$9 = "Ver";
var voiceMessageCount_one$7 = "Mensaje de voz";
var voiceMessageCount_many$4 = "{{ count }} mensajes de voz";
var voiceMessageCount_other$9 = "{{ count }} mensajes de voz";
var Votes$9 = "Votos";
var You$9 = "Tú";
var esTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} y {{ moreCount }} más",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} y {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} archivo",
  "{{ count }} files_many": "{{ count }} archivos",
  "{{ count }} files_other": "{{ count }} archivos",
  "{{ count }} people are typing_one": "{{ count }} persona está escribiendo",
  "{{ count }} people are typing_many": "{{ count }} personas están escribiendo",
  "{{ count }} people are typing_other": "{{ count }} personas están escribiendo",
  "{{ count }} photos_one": "{{ count }} foto",
  "{{ count }} photos_many": "{{ count }} fotos",
  "{{ count }} photos_other": "{{ count }} fotos",
  "{{ count }} reactions_one": "{{ count }} reacción",
  "{{ count }} reactions_many": "{{ count }} reacciones",
  "{{ count }} reactions_other": "{{ count }} reacciones",
  "{{ count }} videos_one": "{{ count }} vídeo",
  "{{ count }} videos_many": "{{ count }} vídeos",
  "{{ count }} videos_other": "{{ count }} vídeos",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} y {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} más",
  "{{ memberCount }} members": "{{ memberCount }} miembros",
  "{{ typing }} are typing": "{{ typing }} están escribiendo",
  "{{ typing }} is typing": "{{ typing }} está escribiendo",
  "{{ user }} has been muted": "{{ user }} ha sido silenciado",
  "{{ user }} has been unmuted": "Se ha desactivado el silencio de {{ user }}",
  "{{ user }} is typing...": "{{ user }} está escribiendo...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} y {{ user }} están escribiendo...",
  "{{ users }} and more are typing...": "{{ users }} y más están escribiendo...",
  "{{ watcherCount }} online": "{{ watcherCount }} en línea",
  "{{count}} new messages_one": "{{count}} nuevo mensaje",
  "{{count}} new messages_many": "{{count}} nuevos mensajes",
  "{{count}} new messages_other": "{{count}} nuevos mensajes",
  "{{count}} unread_one": "{{count}} no leído",
  "{{count}} unread_many": "{{count}} no leídos",
  "{{count}} unread_other": "{{count}} no leídos",
  "{{count}} votes_one": "1 voto",
  "{{count}} votes_many": "{{count}} votos",
  "{{count}} votes_other": "{{count}} votos",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} opción más",
  "+{{count}} more options_many": "+{{count}} opciones más",
  "+{{count}} more options_other": "+{{count}} opciones más",
  "🏙 Attachment...": "🏙 Adjunto...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} creó: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} votó: {{pollOptionText}}",
  "📍Shared location": "📍Ubicación compartida",
  "Add a comment": "Agregar un comentario",
  "Add an option": "Agregar una opción",
  "Add reaction": "Añadir reacción",
  "All results loaded": "Todos los resultados cargados",
  "Allow access to camera": "Permitir acceso a la cámara",
  "Allow access to microphone": "Permitir acceso al micrófono",
  "Allow comments": "Permitir comentarios",
  "Allow option suggestion": "Permitir sugerencia de opciones",
  "Allow others to add comments": "Permitir que otros añadan comentarios",
  "Also send as a direct message": "También enviar como mensaje directo",
  "Also send in channel": "También enviar en el canal",
  "Also sent in channel": "También enviado en el canal",
  "An error has occurred during recording": "Se ha producido un error durante la grabación",
  "An error has occurred during the recording processing": "Se ha producido un error durante el procesamiento de la grabación",
  Anonymous: Anonymous$9,
  "Anonymous poll": "Encuesta anónima",
  Archive: Archive$9,
  "Are you sure you want to delete this message?": "¿Estás seguro de que quieres eliminar este mensaje?",
  "aria/Attachment": "Adjunto",
  "aria/Block User": "Bloquear usuario",
  "aria/Bookmark Message": "Guardar mensaje",
  "aria/Cancel Reply": "Cancelar respuesta",
  "aria/Cancel upload": "Cancelar carga",
  "aria/Channel list": "Lista de canales",
  "aria/Channel search results": "Resultados de búsqueda de canales",
  "aria/Close thread": "Cerrar hilo",
  "aria/Collapse sidebar": "Contraer barra lateral",
  "aria/Copy Message Text": "Copiar texto del mensaje",
  "aria/Delete Message": "Eliminar mensaje",
  "aria/Download attachment": "Descargar adjunto",
  "aria/Edit Message": "Editar mensaje",
  "aria/Emoji picker": "Selector de emojis",
  "aria/Expand sidebar": "Expandir barra lateral",
  "aria/File input": "Entrada de archivo",
  "aria/File upload": "Carga de archivo",
  "aria/Flag Message": "Marcar mensaje",
  "aria/Image failed to load": "Error al cargar la imagen",
  "aria/Image input": "Entrada de imagen",
  "aria/Load More Channels": "Cargar más canales",
  "aria/Mark Message Unread": "Marcar como no leído",
  "aria/Menu": "Menú",
  "aria/Message Options": "Opciones de mensaje",
  "aria/Mute User": "Silenciar usuario",
  "aria/Open Attachment Selector": "Abrir selector de adjuntos",
  "aria/Open Menu": "Abrir menú",
  "aria/Open Message Actions Menu": "Abrir menú de acciones de mensaje",
  "aria/Open Reaction Selector": "Abrir selector de reacciones",
  "aria/Open Thread": "Abrir hilo",
  "aria/Percent complete": "{{percent}} por ciento completado",
  "aria/Pin Message": "Fijar mensaje",
  "aria/Quote Message": "Citar mensaje",
  "aria/Reaction list": "Lista de reacciones",
  "aria/Remind Me Message": "Recordarme",
  "aria/Remind Me Options": "Opciones de recordatorio",
  "aria/Remove attachment": "Eliminar adjunto",
  "aria/Remove location attachment": "Eliminar adjunto de ubicación",
  "aria/Remove Reminder": "Quitar recordatorio",
  "aria/Remove Save For Later": "Quitar guardar para después",
  "aria/Resend Message": "Reenviar mensaje",
  "aria/Retry upload": "Reintentar carga",
  "aria/Search results": "Resultados de búsqueda",
  "aria/Search results header filter button": "Botón de filtro del encabezado de resultados de búsqueda",
  "aria/Send": "Enviar",
  "aria/Show preview": "Mostrar vista previa",
  "aria/Stop AI Generation": "Detener generación de IA",
  "aria/Unblock User": "Desbloquear usuario",
  "aria/Unmute User": "Activar sonido",
  "aria/Unpin Message": "Desfijar mensaje",
  "Ask a question": "Hacer una pregunta",
  Attach: Attach$9,
  "Attach files": "Adjuntar archivos",
  Attachment: Attachment$a,
  "Attachment upload blocked due to {{reason}}": "Carga de adjunto bloqueada debido a {{reason}}",
  "Attachment upload failed due to {{reason}}": "Carga de adjunto fallida debido a {{reason}}",
  Back: Back$9,
  "ban-command-args": "[@usuario] [texto]",
  "ban-command-description": "Prohibir a un usuario",
  "Block User": "Bloquear usuario",
  Cancel: Cancel$9,
  "Cannot seek in the recording": "No se puede buscar en la grabación",
  "Channel archived": "Canal archivado",
  "Channel Missing": "Falta canal",
  "Channel muted": "Canal silenciado",
  "Channel pinned": "Canal fijado",
  "Channel unarchived": "Canal desarchivado",
  "Channel unmuted": "Silencio del canal desactivado",
  "Channel unpinned": "Canal desanclado",
  Channels: Channels$9,
  Chats: Chats$9,
  "Choose between 2 to 10 options": "Elige entre 2 y 10 opciones",
  Close: Close$9,
  "Close emoji picker": "Cerrar el selector de emojis",
  Commands: Commands$9,
  "Commands matching": "Coincidencia de comandos",
  "Connection failure, reconnecting now...": "Fallo de conexión, reconectando ahora...",
  "Copy Message": "Copiar mensaje",
  Create: Create$9,
  "Create poll": "Crear encuesta",
  "Current location": "Ubicación actual",
  Delete: Delete$9,
  "Delete for me": "Eliminar para mí",
  "Delete message": "Eliminar mensaje",
  Delivered: Delivered$9,
  "Direct message": "Mensaje directo",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "¿Quieres terminar esta encuesta ahora? Nadie podrá votar más en esta encuesta.",
  "Download All": "Descargar todo",
  "Download Attachment": "Descargar archivo adjunto",
  "Download attachment {{ name }}": "Descargar adjunto {{ name }}",
  "Drag your files here": "Arrastra tus archivos aquí",
  "Drag your files here to add to your post": "Arrastra tus archivos aquí para agregarlos a tu publicación",
  "Due {{ timeLeft }}": "Vence en {{ timeLeft }}",
  "Due since {{ dueSince }}": "Vencido desde {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Editar mensaje",
  "Edit message request failed": "Error al editar la solicitud de mensaje",
  Edited: Edited$9,
  "Emoji matching": "Coincidencia de emoji",
  "Empty message...": "Mensaje vacío...",
  End: End$9,
  "End poll": "Terminar encuesta",
  "End this poll?": "¿Terminar esta encuesta?",
  "End vote": "Finalizar votación",
  "Enforce unique vote is enabled": "El voto único está habilitado",
  "Error": "Error",
  "Error adding flag": "Error al agregar la bandera",
  "Error connecting to chat, refresh the page to try again.": "Error al conectarse al chat, actualice la página para volver a intentarlo.",
  "Error deleting message": "Error al eliminar el mensaje",
  "Error fetching reactions": "Error al cargar las reacciones",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Error al marcar el mensaje como no leído. No se pueden marcar mensajes no leídos más antiguos que los últimos 100 mensajes del canal.",
  "Error muting a user ...": "Error al silenciar el usuario...",
  "Error pinning message": "Error al fijar el mensaje",
  "Error removing message pin": "Error al quitar el pin del mensaje",
  "Error reproducing the recording": "Error al reproducir la grabación",
  "Error starting recording": "Error al iniciar la grabación",
  "Error unmuting a user ...": "Error al desactivar el silencio del usuario...",
  "Error uploading attachment": "Error al subir el archivo adjunto",
  "Error uploading file": "Error al cargar el archivo",
  "Error uploading image": "Error al subir la imagen",
  "Error: {{ errorMessage }}": "Error: {{ errorMessage }}",
  "Failed to block user": "No se pudo bloquear al usuario",
  "Failed to create the poll": "Error al crear la encuesta",
  "Failed to create the poll due to {{reason}}": "No se pudo crear la encuesta debido a {{reason}}",
  "Failed to delete the message": "No se pudo eliminar el mensaje",
  "Failed to end the poll": "No se pudo terminar la encuesta",
  "Failed to end the poll due to {{reason}}": "No se pudo terminar la encuesta debido a {{reason}}",
  "Failed to jump to the first unread message": "Error al saltar al primer mensaje no leído",
  "Failed to leave channel": "No se pudo salir del canal",
  "Failed to mark channel as read": "Error al marcar el canal como leído",
  "Failed to play the recording": "No se pudo reproducir la grabación",
  "Failed to retrieve location": "No se pudo obtener la ubicación",
  "Failed to share location": "No se pudo compartir la ubicación",
  "Failed to update channel archive status": "No se pudo actualizar el estado de archivo del canal",
  "Failed to update channel mute status": "No se pudo actualizar el estado de silencio del canal",
  "Failed to update channel pinned status": "No se pudo actualizar el estado de fijación del canal",
  File: File$a,
  "File is required for upload attachment": "Se requiere un archivo para subir el adjunto",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "El archivo es demasiado grande: {{ size }}, el tamaño máximo de carga es de {{ limit }}",
  "File too large": "Archivo demasiado grande",
  fileCount_one: fileCount_one$7,
  fileCount_many: fileCount_many$4,
  fileCount_other: fileCount_other$9,
  Flag: Flag$9,
  "Generating...": "Generando...",
  "giphy-command-args": "[texto]",
  "giphy-command-description": "Publicar un gif aleatorio en el canal",
  "Hide who voted": "Ocultar quién votó",
  Image: Image$a,
  imageCount_one: imageCount_one$7,
  imageCount_many: imageCount_many$4,
  imageCount_other: imageCount_other$9,
  "Instant commands": "Comandos instantáneos",
  "language/af": "Afrikáans",
  "language/am": "Amárico",
  "language/ar": "Árabe",
  "language/az": "Azerbaiyano",
  "language/bg": "Búlgaro",
  "language/bn": "Bengalí",
  "language/bs": "Bosnio",
  "language/cs": "Checo",
  "language/da": "Danés",
  "language/de": "Alemán",
  "language/el": "Griego",
  "language/en": "Inglés",
  "language/es": "Español",
  "language/es-MX": "Español (México)",
  "language/et": "Estonio",
  "language/fa": "Persa",
  "language/fa-AF": "Dari",
  "language/fi": "Finlandés",
  "language/fr": "Francés",
  "language/fr-CA": "Francés (Canadá)",
  "language/ha": "Hausa",
  "language/he": "Hebreo",
  "language/hi": "Hindi",
  "language/hr": "Croata",
  "language/ht": "Criollo haitiano",
  "language/hu": "Húngaro",
  "language/id": "Indonesio",
  "language/it": "Italiano",
  "language/ja": "Japonés",
  "language/ka": "Georgiano",
  "language/ko": "Coreano",
  "language/lt": "Lituano",
  "language/lv": "Letón",
  "language/ms": "Malayo",
  "language/nl": "Neerlandés",
  "language/no": "Noruego",
  "language/pl": "Polaco",
  "language/ps": "Pastún",
  "language/pt": "Portugués",
  "language/ro": "Rumano",
  "language/ru": "Ruso",
  "language/sk": "Eslovaco",
  "language/sl": "Esloveno",
  "language/so": "Somalí",
  "language/sq": "Albanés",
  "language/sr": "Serbio",
  "language/sv": "Sueco",
  "language/sw": "Suajili",
  "language/ta": "Tamil",
  "language/th": "Tailandés",
  "language/tl": "Tagalo",
  "language/tr": "Turco",
  "language/uk": "Ucraniano",
  "language/ur": "Urdu",
  "language/vi": "Vietnamita",
  "language/zh": "Chino (simplificado)",
  "language/zh-TW": "Chino (tradicional)",
  "Leave Channel": "Abandonar canal",
  "Left channel": "Canal abandonado",
  "Let others add options": "Permitir que otros añadan opciones",
  "Limit votes per person": "Limitar votos por persona",
  Link: Link$9,
  linkCount_one: linkCount_one$7,
  linkCount_many: linkCount_many$4,
  linkCount_other: linkCount_other$9,
  live: live$9,
  "Live for {{duration}}": "En vivo durante {{duration}}",
  "Live location": "Ubicación en vivo",
  "Live until {{ timestamp }}": "En vivo hasta {{ timestamp }}",
  "Load more": "Cargar más",
  "Local upload attachment missing local id": "El adjunto de subida local no tiene id local",
  Location: Location$9,
  "Location sharing ended": "Compartir ubicación terminado",
  "Location: {{ coordinates }}": "Ubicación: {{ coordinates }}",
  "Mark as unread": "Marcar como no leído",
  "Maximum number of votes (from 2 to 10)": "Número máximo de votos (de 2 a 10)",
  Menu: Menu$9,
  "Message deleted": "Mensaje eliminado",
  "Message failed to send": "No se pudo enviar el mensaje",
  "Message has been successfully flagged": "El mensaje se marcó correctamente",
  "Message marked as unread": "Mensaje marcado como no leído",
  "Message pinned": "Mensaje fijado",
  "Message unpinned": "Mensaje desanclado",
  "Message was blocked by moderation policies": "El mensaje fue bloqueado por las políticas de moderación",
  "Messages have been marked unread.": "Los mensajes han sido marcados como no leídos.",
  "Missing permissions to upload the attachment": "Faltan permisos para subir el archivo adjunto",
  "Multiple votes": "Votos múltiples",
  Mute: Mute$9,
  "mute-command-args": "[@usuario]",
  "mute-command-description": "Silenciar a un usuario",
  "network error": "error de red",
  New: New$9,
  "New Messages!": "¡Nuevos mensajes!",
  "Next image": "Siguiente imagen",
  "No chats here yet…": "Aún no hay mensajes aquí...",
  "No conversations yet": "Aún no hay conversaciones",
  "No items exist": "No existen elementos",
  "No results found": "No se han encontrado resultados",
  "Nobody will be able to vote in this poll anymore.": "Nadie podrá votar en esta encuesta.",
  "Nothing yet...": "Nada aún...",
  Offline: Offline$9,
  Ok: Ok$9,
  Online: Online$9,
  "Only numbers are allowed": "Solo se permiten números",
  "Only visible to you": "Solo visible para ti",
  "Open emoji picker": "Abrir el selector de emojis",
  "Open gallery at image {{ index }}": "Abrir galería en la imagen {{ index }}",
  "Open image in gallery": "Abrir imagen en la galería",
  "Option already exists": "La opción ya existe",
  "Option is empty": "La opción está vacía",
  Options: Options$9,
  Original: Original$9,
  "People matching": "Personas que coinciden",
  Photo: Photo$9,
  Pin: Pin$9,
  "Pinned by {{ name }}": "Fijado por {{ name }}",
  "Pinned by You": "Anclado por ti",
  "placeholder/PollComment": "Tu comentario",
  "placeholder/PollOptionSuggestion": "Introduce una nueva opción",
  "Play video": "Reproducir video",
  Poll: Poll$a,
  "Poll comments": "Comentarios de la encuesta",
  "Poll ended": "Encuesta finalizada",
  "Poll options": "Opciones de la encuesta",
  "Poll results": "Resultados de la encuesta",
  "Previous image": "Imagen anterior",
  Question: Question$9,
  "Question {{ optionOrderNumber}}": "Pregunta {{ optionOrderNumber}}",
  "Question is required": "La pregunta es obligatoria",
  "Quote Reply": "Responder con cita",
  "Reached the vote limit. Remove an existing vote first.": "Se ha alcanzado el límite de votos. Elimina un voto existente primero.",
  "Recording format is not supported and cannot be reproduced": "El formato de grabación no es compatible y no se puede reproducir",
  "Remind me": "Recordarme",
  "Remind Me": "Recordarme",
  "Reminder set": "Recordatorio establecido",
  "Remove reminder": "Eliminar recordatorio",
  "Remove save for later": "Quitar guardar para después",
  "Replied to a thread": "Respondió en un hilo",
  Reply: Reply$9,
  "Reply to {{ authorName }}": "Responder a {{ authorName }}",
  "Reply to a message to start a thread": "Responde a un mensaje para iniciar un hilo",
  "Reply to Message": "Responder al mensaje",
  replyCount_one: replyCount_one$9,
  replyCount_many: replyCount_many$4,
  replyCount_other: replyCount_other$9,
  Resend: Resend$9,
  "Retry upload": "Reintentar la carga",
  "Save for later": "Guardar para más tarde",
  "Saved for later": "Guardado para más tarde",
  Search: Search$a,
  "Search GIFs": "Buscar GIFs",
  "search-results-header-filter-source-button-label--channels": "canales",
  "search-results-header-filter-source-button-label--messages": "mensajes",
  "search-results-header-filter-source-button-label--users": "usuarios",
  "Searching for {{ searchSourceType }}...": "Buscando {{ searchSourceType }}...",
  "Searching...": "Buscando...",
  searchResultsCount_one: searchResultsCount_one$9,
  searchResultsCount_many: searchResultsCount_many$4,
  searchResultsCount_other: searchResultsCount_other$9,
  "Select a thread to continue the conversation": "Selecciona un hilo para continuar la conversación",
  "Select more than one option": "Seleccionar más de una opción",
  "Select one": "Seleccionar uno",
  "Select one or more": "Seleccionar uno o más",
  "Select up to {{count}}_one": "Selecciona hasta {{count}}",
  "Select up to {{count}}_many": "Selecciona hasta {{count}}",
  "Select up to {{count}}_other": "Selecciona hasta {{count}}",
  Send: Send$9,
  "Send a message to start the conversation": "Envía un mensaje para iniciar la conversación",
  "Send Anyway": "Enviar de todos modos",
  "Send message request failed": "Error al enviar la solicitud de mensaje",
  "Send poll": "Enviar encuesta",
  "Sending...": "Enviando...",
  Sent: Sent$9,
  Share: Share$9,
  "Share live location for": "Compartir ubicación en vivo durante",
  "Share Location": "Compartir ubicación",
  "Shared live location": "Ubicación en vivo compartida",
  "Shared location": "Ubicación compartida",
  Shuffle: Shuffle$9,
  "size limit": "límite de tamaño",
  "Slow Mode ON": "Modo lento activado",
  "Slow mode, wait {{ seconds }}s...": "Modo lento, espera {{ seconds }} s...",
  "Some of the files will not be accepted": "Algunos archivos no serán aceptados",
  "Start typing to search": "Empieza a escribir para buscar",
  "Stop sharing": "Dejar de compartir",
  Submit: Submit$9,
  "Suggest an option": "Sugerir una opción",
  "Tap to remove": "Toca para quitar",
  "Thinking...": "Pensando...",
  "this content could not be displayed": "Este contenido no se pudo mostrar",
  "This field cannot be empty or contain only spaces": "Este campo no puede estar vacío o contener solo espacios",
  "This message did not meet our content guidelines": "Este mensaje no cumple con nuestras directrices de contenido",
  Thread: Thread$a,
  "Thread has not been found": "No se ha encontrado el hilo",
  "Thread reply": "Respuesta en hilo",
  "Thread Reply": "Respuesta en hilo",
  "ThreadListUnseenThreadsBanner/loading": "Cargando...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} hilo no leído",
  "ThreadListUnseenThreadsBanner/unreadThreads_many": "{{ count }} hilos no leídos",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} hilos no leídos",
  Threads: Threads$9,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Ayer]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Hoy]", "nextDay": "[Mañana]", "lastDay": "[Ayer]", "nextWeek": "dddd", "lastWeek": "[Último] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Hoy",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Ayer",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Para comenzar a grabar, permita el acceso a la cámara en su navegador",
  "To start recording, allow the microphone access in your browser": "Para comenzar a grabar, permita el acceso al micrófono en su navegador",
  totalVoteCount_one: totalVoteCount_one$7,
  totalVoteCount_many: totalVoteCount_many$4,
  totalVoteCount_other: totalVoteCount_other$9,
  Translated: Translated$9,
  "Translated from {{ language }}": "Traducido de {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Escribe un número del 2 al 10",
  "Type your message": "Escribe tu mensaje",
  Unarchive: Unarchive$9,
  "unban-command-args": "[@usuario]",
  "unban-command-description": "Quitar la prohibición a un usuario",
  "Unblock User": "Desbloquear usuario",
  "unknown error": "error desconocido",
  Unmute: Unmute$9,
  "unmute-command-args": "[@usuario]",
  "unmute-command-description": "Desactivar el silencio de un usuario",
  Unpin: Unpin$9,
  "Unread messages": "Mensajes no leídos",
  "Unsupported attachment": "Adjunto no compatible",
  "unsupported file type": "tipo de archivo no compatible",
  Update: Update$9,
  "Update your comment": "Actualizar tu comentario",
  "Upload blocked": "Carga bloqueada",
  "Upload error": "Error de carga",
  "Upload failed": "Carga fallida",
  'Upload type: "{{ type }}" is not allowed': 'Tipo de carga: "{{ type }}" no está permitido',
  "User blocked": "Usuario bloqueado",
  "User unblocked": "Usuario desbloqueado",
  "User uploaded content": "Contenido subido por el usuario",
  Video: Video$9,
  videoCount_one: videoCount_one$7,
  videoCount_many: videoCount_many$4,
  videoCount_other: videoCount_other$9,
  View: View$9,
  "View {{count}} comments_one": "Ver {{count}} comentario",
  "View {{count}} comments_many": "Ver {{count}} comentarios",
  "View {{count}} comments_other": "Ver {{count}} comentarios",
  "View all": "Ver todo",
  "View original": "Ver original",
  "View results": "Ver resultados",
  "View translation": "Ver traducción",
  "Voice message": "Mensaje de voz",
  "Voice message {{ duration }}": "Mensaje de voz {{ duration }}",
  "Voice message deleted": "Mensaje de voz eliminado",
  voiceMessageCount_one: voiceMessageCount_one$7,
  voiceMessageCount_many: voiceMessageCount_many$4,
  voiceMessageCount_other: voiceMessageCount_other$9,
  "Vote ended": "Votación finalizada",
  Votes: Votes$9,
  "Wait until all attachments have uploaded": "Espere hasta que se hayan cargado todos los archivos adjuntos",
  "Waiting for network…": "Esperando red…",
  You: You$9,
  "You've reached the maximum number of files": "Has alcanzado el número máximo de archivos"
};
var Anonymous$8 = "Anonyme";
var Archive$8 = "Archiver";
var Attach$8 = "Joindre";
var Attachment$9 = "Pièce jointe";
var Back$8 = "Retour";
var Cancel$8 = "Annuler";
var Channels$8 = "Canaux";
var Chats$8 = "Discussions";
var Close$8 = "Fermer";
var Commands$8 = "Commandes";
var Create$8 = "Créer";
var Delete$8 = "Supprimer";
var Delivered$8 = "Publié";
var Edited$8 = "Modifié";
var End$8 = "Fin";
var File$9 = "Fichier";
var fileCount_one$6 = "1 fichier";
var fileCount_many$3 = "{{ count }} fichiers";
var fileCount_other$8 = "{{ count }} fichiers";
var Flag$8 = "Signaler";
var Image$9 = "Image";
var imageCount_one$6 = "Photo";
var imageCount_many$3 = "{{ count }} photos";
var imageCount_other$8 = "{{ count }} photos";
var Link$8 = "Lien";
var linkCount_one$6 = "Lien";
var linkCount_many$3 = "{{ count }} liens";
var linkCount_other$8 = "{{ count }} liens";
var live$8 = "en direct";
var Location$8 = "Emplacement";
var Menu$8 = "Menu";
var Mute$8 = "Muet";
var New$8 = "Nouveau";
var Offline$8 = "Hors ligne";
var Ok$8 = "D'accord";
var Online$8 = "En ligne";
var Options$8 = "Options";
var Original$8 = "Original";
var Photo$8 = "Photo";
var Pin$8 = "Épingler";
var Poll$9 = "Sondage";
var Question$8 = "Question";
var Reply$8 = "Répondre";
var replyCount_one$8 = "1 réponse";
var replyCount_many$3 = "{{ count }} réponses";
var replyCount_other$8 = "{{ count }} réponses";
var Resend$8 = "Renvoyer";
var Search$9 = "Rechercher";
var searchResultsCount_one$8 = "1 résultat";
var searchResultsCount_many$3 = "{{ count }} résultats";
var searchResultsCount_other$8 = "{{ count }} résultats";
var Send$8 = "Envoyer";
var Sent$8 = "Envoyé";
var Share$8 = "Partager";
var Shuffle$8 = "Mélanger";
var Submit$8 = "Envoyer";
var Thread$9 = "Fil de discussion";
var Threads$8 = "Fils";
var totalVoteCount_one$6 = "1 vote au total";
var totalVoteCount_many$3 = "{{ count }} votes au total";
var totalVoteCount_other$8 = "{{ count }} votes au total";
var Translated$8 = "Traduit";
var Unarchive$8 = "Désarchiver";
var Unmute$8 = "Désactiver muet";
var Unpin$8 = "Détacher";
var Update$8 = "Mettre à jour";
var Video$8 = "Vidéo";
var videoCount_one$6 = "Vidéo";
var videoCount_many$3 = "{{ count }} vidéos";
var videoCount_other$8 = "{{ count }} vidéos";
var View$8 = "Voir";
var voiceMessageCount_one$6 = "Mémo vocal";
var voiceMessageCount_many$3 = "{{ count }} mémos vocaux";
var voiceMessageCount_other$8 = "{{ count }} mémos vocaux";
var Votes$8 = "Votes";
var You$8 = "Vous";
var frTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} et {{ moreCount }} autres",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} et {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} fichier",
  "{{ count }} files_many": "{{ count }} fichiers",
  "{{ count }} files_other": "{{ count }} fichiers",
  "{{ count }} people are typing_one": "{{ count }} personne écrit",
  "{{ count }} people are typing_many": "{{ count }} personnes écrivent",
  "{{ count }} people are typing_other": "{{ count }} personnes écrivent",
  "{{ count }} photos_one": "{{ count }} photo",
  "{{ count }} photos_many": "{{ count }} photos",
  "{{ count }} photos_other": "{{ count }} photos",
  "{{ count }} reactions_one": "{{ count }} réaction",
  "{{ count }} reactions_many": "{{ count }} réactions",
  "{{ count }} reactions_other": "{{ count }} réactions",
  "{{ count }} videos_one": "{{ count }} vidéo",
  "{{ count }} videos_many": "{{ count }} vidéos",
  "{{ count }} videos_other": "{{ count }} vidéos",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} et {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} supplémentaires",
  "{{ memberCount }} members": "{{ memberCount }} membres",
  "{{ typing }} are typing": "{{ typing }} écrivent",
  "{{ typing }} is typing": "{{ typing }} écrit",
  "{{ user }} has been muted": "{{ user }} a été mis en sourdine",
  "{{ user }} has been unmuted": "{{ user }} n'est plus en sourdine",
  "{{ user }} is typing...": "{{ user }} est en train d'écrire...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} et {{ user }} sont en train d'écrire...",
  "{{ users }} and more are typing...": "{{ users }} et plus sont en train d'écrire...",
  "{{ watcherCount }} online": "{{ watcherCount }} en ligne",
  "{{count}} new messages_one": "{{count}} nouveau message",
  "{{count}} new messages_many": "{{count}} nouveaux messages",
  "{{count}} new messages_other": "{{count}} nouveaux messages",
  "{{count}} unread_one": "{{count}} non lu",
  "{{count}} unread_many": "{{count}} non lus",
  "{{count}} unread_other": "{{count}} non lus",
  "{{count}} votes_one": "{{count}} vote",
  "{{count}} votes_many": "{{count}} votes",
  "{{count}} votes_other": "{{count}} votes",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} option de plus",
  "+{{count}} more options_many": "+{{count}} options de plus",
  "+{{count}} more options_other": "+{{count}} options de plus",
  "🏙 Attachment...": "🏙 Pièce jointe...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} a créé : {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} a voté : {{pollOptionText}}",
  "📍Shared location": "📍Emplacement partagé",
  "Add a comment": "Ajouter un commentaire",
  "Add an option": "Ajouter une option",
  "Add reaction": "Ajouter une réaction",
  "All results loaded": "Tous les résultats sont chargés",
  "Allow access to camera": "Autoriser l'accès à la caméra",
  "Allow access to microphone": "Autoriser l'accès au microphone",
  "Allow comments": "Autoriser les commentaires",
  "Allow option suggestion": "Autoriser la suggestion d'options",
  "Allow others to add comments": "Permettre à d'autres d'ajouter des commentaires",
  "Also send as a direct message": "Également envoyer en message direct",
  "Also send in channel": "Également envoyer dans le canal",
  "Also sent in channel": "Également envoyé dans le canal",
  "An error has occurred during recording": "Une erreur s'est produite pendant l'enregistrement",
  "An error has occurred during the recording processing": "Une erreur s'est produite pendant le traitement de l'enregistrement",
  Anonymous: Anonymous$8,
  "Anonymous poll": "Sondage anonyme",
  Archive: Archive$8,
  "Are you sure you want to delete this message?": "Êtes-vous sûr de vouloir supprimer ce message ?",
  "aria/Attachment": "Pièce jointe",
  "aria/Block User": "Bloquer l'utilisateur",
  "aria/Bookmark Message": "Enregistrer le message",
  "aria/Cancel Reply": "Annuler la réponse",
  "aria/Cancel upload": "Annuler le téléchargement",
  "aria/Channel list": "Liste des canaux",
  "aria/Channel search results": "Résultats de recherche de canaux",
  "aria/Close thread": "Fermer le fil",
  "aria/Collapse sidebar": "Réduire la barre latérale",
  "aria/Copy Message Text": "Copier le texte du message",
  "aria/Delete Message": "Supprimer le message",
  "aria/Download attachment": "Télécharger la pièce jointe",
  "aria/Edit Message": "Éditer un message",
  "aria/Emoji picker": "Sélecteur d'émojis",
  "aria/Expand sidebar": "Développer la barre latérale",
  "aria/File input": "Entrée de fichier",
  "aria/File upload": "Téléchargement de fichier",
  "aria/Flag Message": "Signaler le message",
  "aria/Image failed to load": "Échec du chargement de l'image",
  "aria/Image input": "Entrée d'image",
  "aria/Load More Channels": "Charger plus de canaux",
  "aria/Mark Message Unread": "Marquer comme non lu",
  "aria/Menu": "Menu",
  "aria/Message Options": "Options du message",
  "aria/Mute User": "Mettre en sourdine",
  "aria/Open Attachment Selector": "Ouvrir le sélecteur de pièces jointes",
  "aria/Open Menu": "Ouvrir le menu",
  "aria/Open Message Actions Menu": "Ouvrir le menu des actions du message",
  "aria/Open Reaction Selector": "Ouvrir le sélecteur de réactions",
  "aria/Open Thread": "Ouvrir le fil",
  "aria/Percent complete": "{{percent}} pour cent terminé",
  "aria/Pin Message": "Épingler le message",
  "aria/Quote Message": "Citer le message",
  "aria/Reaction list": "Liste des réactions",
  "aria/Remind Me Message": "Me rappeler",
  "aria/Remind Me Options": "Options de rappel",
  "aria/Remove attachment": "Supprimer la pièce jointe",
  "aria/Remove location attachment": "Supprimer la pièce jointe d'emplacement",
  "aria/Remove Reminder": "Supprimer le rappel",
  "aria/Remove Save For Later": "Supprimer « Enregistrer pour plus tard »",
  "aria/Resend Message": "Renvoyer le message",
  "aria/Retry upload": "Réessayer le téléchargement",
  "aria/Search results": "Résultats de recherche",
  "aria/Search results header filter button": "Bouton de filtre d'en-tête des résultats de recherche",
  "aria/Send": "Envoyer",
  "aria/Show preview": "Afficher l'aperçu",
  "aria/Stop AI Generation": "Arrêter la génération d'IA",
  "aria/Unblock User": "Débloquer l'utilisateur",
  "aria/Unmute User": "Désactiver muet",
  "aria/Unpin Message": "Détacher le message",
  "Ask a question": "Poser une question",
  Attach: Attach$8,
  "Attach files": "Joindre des fichiers",
  Attachment: Attachment$9,
  "Attachment upload blocked due to {{reason}}": "Téléchargement de pièce jointe bloqué en raison de {{reason}}",
  "Attachment upload failed due to {{reason}}": "Échec du téléchargement de la pièce jointe en raison de {{reason}}",
  Back: Back$8,
  "ban-command-args": "[@nomdutilisateur] [texte]",
  "ban-command-description": "Bannir un utilisateur",
  "Block User": "Bloquer l'utilisateur",
  Cancel: Cancel$8,
  "Cannot seek in the recording": "Impossible de rechercher dans l'enregistrement",
  "Channel archived": "Canal archivé",
  "Channel Missing": "Canal Manquant",
  "Channel muted": "Canal mis en sourdine",
  "Channel pinned": "Canal épinglé",
  "Channel unarchived": "Canal désarchivé",
  "Channel unmuted": "Sourdine du canal désactivée",
  "Channel unpinned": "Canal désépinglé",
  Channels: Channels$8,
  Chats: Chats$8,
  "Choose between 2 to 10 options": "Choisir entre 2 et 10 options",
  Close: Close$8,
  "Close emoji picker": "Fermer le sélecteur d'émojis",
  Commands: Commands$8,
  "Commands matching": "Correspondance des commandes",
  "Connection failure, reconnecting now...": "Échec de la connexion, reconnexion en cours...",
  "Copy Message": "Copier le message",
  Create: Create$8,
  "Create poll": "Créer un sondage",
  "Current location": "Emplacement actuel",
  Delete: Delete$8,
  "Delete for me": "Supprimer pour moi",
  "Delete message": "Supprimer le message",
  Delivered: Delivered$8,
  "Direct message": "Message direct",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Voulez-vous terminer ce sondage maintenant ? Personne ne pourra plus voter dans ce sondage.",
  "Download All": "Tout télécharger",
  "Download Attachment": "Télécharger la pièce jointe",
  "Download attachment {{ name }}": "Télécharger la pièce jointe {{ name }}",
  "Drag your files here": "Glissez vos fichiers ici",
  "Drag your files here to add to your post": "Glissez vos fichiers ici pour les ajouter à votre publication",
  "Due {{ timeLeft }}": "Échéance dans {{ timeLeft }}",
  "Due since {{ dueSince }}": "Échéance depuis {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Éditer un message",
  "Edit message request failed": "Échec de la demande de modification du message",
  Edited: Edited$8,
  "Emoji matching": "Correspondance d'émojis",
  "Empty message...": "Message vide...",
  End: End$8,
  "End poll": "Terminer le sondage",
  "End this poll?": "Terminer ce sondage ?",
  "End vote": "Fin du vote",
  "Enforce unique vote is enabled": "Le vote unique est activé",
  "Error": "Erreur",
  "Error adding flag": "Erreur lors de l'ajout du signalement",
  "Error connecting to chat, refresh the page to try again.": "Erreur de connexion au chat, rafraîchissez la page pour réessayer.",
  "Error deleting message": "Erreur lors de la suppression du message",
  "Error fetching reactions": "Erreur lors du chargement des réactions",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Erreur lors de la marque du message comme non lu. Impossible de marquer des messages non lus plus anciens que les 100 derniers messages du canal.",
  "Error muting a user ...": "Erreur lors de la mise en sourdine d'un utilisateur...",
  "Error pinning message": "Erreur lors de l'épinglage du message",
  "Error removing message pin": "Erreur lors du retrait de l'épinglage du message",
  "Error reproducing the recording": "Erreur lors de la reproduction de l'enregistrement",
  "Error starting recording": "Erreur lors du démarrage de l'enregistrement",
  "Error unmuting a user ...": "Erreur lors du démarrage de la sourdine d'un utilisateur ...",
  "Error uploading attachment": "Erreur lors du téléchargement de la pièce jointe",
  "Error uploading file": "Erreur lors du téléchargement du fichier",
  "Error uploading image": "Erreur lors de l'envoi de l'image",
  "Error: {{ errorMessage }}": "Erreur : {{ errorMessage }}",
  "Failed to block user": "Impossible de bloquer l'utilisateur",
  "Failed to create the poll": "Échec de la création du sondage",
  "Failed to create the poll due to {{reason}}": "Échec de la création du sondage en raison de {{reason}}",
  "Failed to delete the message": "Échec de la suppression du message",
  "Failed to end the poll": "Impossible de terminer le sondage",
  "Failed to end the poll due to {{reason}}": "Impossible de terminer le sondage en raison de {{reason}}",
  "Failed to jump to the first unread message": "Échec du saut vers le premier message non lu",
  "Failed to leave channel": "Impossible de quitter le canal",
  "Failed to mark channel as read": "Échec du marquage du canal comme lu",
  "Failed to play the recording": "Impossible de lire l'enregistrement",
  "Failed to retrieve location": "Impossible de récupérer l'emplacement",
  "Failed to share location": "Impossible de partager l'emplacement",
  "Failed to update channel archive status": "Impossible de mettre à jour l'état d'archivage du canal",
  "Failed to update channel mute status": "Impossible de mettre à jour l'état de la mise en sourdine du canal",
  "Failed to update channel pinned status": "Impossible de mettre à jour l'état d'épinglage du canal",
  File: File$9,
  "File is required for upload attachment": "Un fichier est requis pour joindre une pièce",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "Le fichier est trop volumineux : {{ size }}, la taille maximale de téléchargement est de {{ limit }}",
  "File too large": "Fichier trop volumineux",
  fileCount_one: fileCount_one$6,
  fileCount_many: fileCount_many$3,
  fileCount_other: fileCount_other$8,
  Flag: Flag$8,
  "Generating...": "Génération...",
  "giphy-command-args": "[texte]",
  "giphy-command-description": "Poster un GIF aléatoire dans le canal",
  "Hide who voted": "Masquer qui a voté",
  Image: Image$9,
  imageCount_one: imageCount_one$6,
  imageCount_many: imageCount_many$3,
  imageCount_other: imageCount_other$8,
  "Instant commands": "Commandes instantanées",
  "language/af": "Afrikaans",
  "language/am": "Amharique",
  "language/ar": "Arabe",
  "language/az": "Azéri",
  "language/bg": "Bulgare",
  "language/bn": "Bengali",
  "language/bs": "Bosnien",
  "language/cs": "Tchèque",
  "language/da": "Danois",
  "language/de": "Allemand",
  "language/el": "Grec",
  "language/en": "Anglais",
  "language/es": "Espagnol",
  "language/es-MX": "Espagnol (Mexique)",
  "language/et": "Estonien",
  "language/fa": "Persan",
  "language/fa-AF": "Dari",
  "language/fi": "Finnois",
  "language/fr": "Français",
  "language/fr-CA": "Français (Canada)",
  "language/ha": "Haoussa",
  "language/he": "Hébreu",
  "language/hi": "Hindi",
  "language/hr": "Croate",
  "language/ht": "Créole haïtien",
  "language/hu": "Hongrois",
  "language/id": "Indonésien",
  "language/it": "Italien",
  "language/ja": "Japonais",
  "language/ka": "Géorgien",
  "language/ko": "Coréen",
  "language/lt": "Lituanien",
  "language/lv": "Letton",
  "language/ms": "Malais",
  "language/nl": "Néerlandais",
  "language/no": "Norvégien",
  "language/pl": "Polonais",
  "language/ps": "Pachto",
  "language/pt": "Portugais",
  "language/ro": "Roumain",
  "language/ru": "Russe",
  "language/sk": "Slovaque",
  "language/sl": "Slovène",
  "language/so": "Somali",
  "language/sq": "Albanais",
  "language/sr": "Serbe",
  "language/sv": "Suédois",
  "language/sw": "Swahili",
  "language/ta": "Tamoul",
  "language/th": "Thaï",
  "language/tl": "Tagalog",
  "language/tr": "Turc",
  "language/uk": "Ukrainien",
  "language/ur": "Ourdou",
  "language/vi": "Vietnamien",
  "language/zh": "Chinois (simplifié)",
  "language/zh-TW": "Chinois (traditionnel)",
  "Leave Channel": "Quitter le canal",
  "Left channel": "Canal quitté",
  "Let others add options": "Permettre à d'autres d'ajouter des options",
  "Limit votes per person": "Limiter les votes par personne",
  Link: Link$8,
  linkCount_one: linkCount_one$6,
  linkCount_many: linkCount_many$3,
  linkCount_other: linkCount_other$8,
  live: live$8,
  "Live for {{duration}}": "En direct pendant {{duration}}",
  "Live location": "Emplacement en direct",
  "Live until {{ timestamp }}": "En direct jusqu'à {{ timestamp }}",
  "Load more": "Charger plus",
  "Local upload attachment missing local id": "Pièce jointe locale sans identifiant local",
  Location: Location$8,
  "Location sharing ended": "Partage d'emplacement terminé",
  "Location: {{ coordinates }}": "Emplacement : {{ coordinates }}",
  "Mark as unread": "Marquer comme non lu",
  "Maximum number of votes (from 2 to 10)": "Nombre maximum de votes (de 2 à 10)",
  Menu: Menu$8,
  "Message deleted": "Message supprimé",
  "Message failed to send": "Échec de l'envoi du message",
  "Message has been successfully flagged": "Le message a été signalé avec succès",
  "Message marked as unread": "Message marqué comme non lu",
  "Message pinned": "Message épinglé",
  "Message unpinned": "Message désépinglé",
  "Message was blocked by moderation policies": "Le message a été bloqué par les politiques de modération",
  "Messages have been marked unread.": "Les messages ont été marqués comme non lus.",
  "Missing permissions to upload the attachment": "Autorisations manquantes pour télécharger la pièce jointe",
  "Multiple votes": "Votes multiples",
  Mute: Mute$8,
  "mute-command-args": "[@nomdutilisateur]",
  "mute-command-description": "Muter un utilisateur",
  "network error": "erreur réseau",
  New: New$8,
  "New Messages!": "Nouveaux Messages!",
  "Next image": "Image suivante",
  "No chats here yet…": "Pas encore de messages ici...",
  "No conversations yet": "Aucune conversation pour le moment",
  "No items exist": "Aucun élément",
  "No results found": "Aucun résultat trouvé",
  "Nobody will be able to vote in this poll anymore.": "Personne ne pourra plus voter dans ce sondage.",
  "Nothing yet...": "Rien pour l'instant...",
  Offline: Offline$8,
  Ok: Ok$8,
  Online: Online$8,
  "Only numbers are allowed": "Seuls les chiffres sont autorisés",
  "Only visible to you": "Visible uniquement pour vous",
  "Open emoji picker": "Ouvrir le sélecteur d'émojis",
  "Open gallery at image {{ index }}": "Ouvrir la galerie à l'image {{ index }}",
  "Open image in gallery": "Ouvrir l'image dans la galerie",
  "Option already exists": "L'option existe déjà",
  "Option is empty": "L'option est vide",
  Options: Options$8,
  Original: Original$8,
  "People matching": "Correspondance de personnes",
  Photo: Photo$8,
  Pin: Pin$8,
  "Pinned by {{ name }}": "Épinglé par {{ name }}",
  "Pinned by You": "Épinglé par vous",
  "placeholder/PollComment": "Votre commentaire",
  "placeholder/PollOptionSuggestion": "Saisir une nouvelle option",
  "Play video": "Lire la vidéo",
  Poll: Poll$9,
  "Poll comments": "Commentaires du sondage",
  "Poll ended": "Sondage terminé",
  "Poll options": "Options du sondage",
  "Poll results": "Résultats du sondage",
  "Previous image": "Image précédente",
  Question: Question$8,
  "Question {{ optionOrderNumber}}": "Question {{ optionOrderNumber}}",
  "Question is required": "La question est obligatoire",
  "Quote Reply": "Répondre par citation",
  "Reached the vote limit. Remove an existing vote first.": "La limite de votes a été atteinte. Supprimez d'abord un vote existant.",
  "Recording format is not supported and cannot be reproduced": "Le format d'enregistrement n'est pas pris en charge et ne peut pas être reproduit",
  "Remind me": "Me rappeler",
  "Remind Me": "Me rappeler",
  "Reminder set": "Rappel défini",
  "Remove reminder": "Supprimer le rappel",
  "Remove save for later": "Supprimer « Enregistrer pour plus tard »",
  "Replied to a thread": "A répondu à un fil",
  Reply: Reply$8,
  "Reply to {{ authorName }}": "Répondre à {{ authorName }}",
  "Reply to a message to start a thread": "Répondez à un message pour démarrer un fil",
  "Reply to Message": "Répondre au message",
  replyCount_one: replyCount_one$8,
  replyCount_many: replyCount_many$3,
  replyCount_other: replyCount_other$8,
  Resend: Resend$8,
  "Retry upload": "Réessayer le téléchargement",
  "Save for later": "Enregistrer pour plus tard",
  "Saved for later": "Enregistré pour plus tard",
  Search: Search$9,
  "Search GIFs": "Rechercher des GIFs",
  "search-results-header-filter-source-button-label--channels": "canaux",
  "search-results-header-filter-source-button-label--messages": "messages",
  "search-results-header-filter-source-button-label--users": "utilisateurs",
  "Searching for {{ searchSourceType }}...": "Recherche de {{ searchSourceType }}...",
  "Searching...": "Recherche en cours...",
  searchResultsCount_one: searchResultsCount_one$8,
  searchResultsCount_many: searchResultsCount_many$3,
  searchResultsCount_other: searchResultsCount_other$8,
  "Select a thread to continue the conversation": "Sélectionnez un fil pour continuer la conversation",
  "Select more than one option": "Sélectionner plus d'une option",
  "Select one": "Sélectionner un",
  "Select one or more": "Sélectionner un ou plusieurs",
  "Select up to {{count}}_one": "Sélectionner jusqu'à {{count}}",
  "Select up to {{count}}_many": "Sélectionner jusqu'à {{count}}",
  "Select up to {{count}}_other": "Sélectionner jusqu'à {{count}}",
  Send: Send$8,
  "Send a message to start the conversation": "Envoyez un message pour commencer la conversation",
  "Send Anyway": "Envoyer quand même",
  "Send message request failed": "Échec de la demande d'envoi de message",
  "Send poll": "Envoyer le sondage",
  "Sending...": "Envoi en cours...",
  Sent: Sent$8,
  Share: Share$8,
  "Share live location for": "Partager l'emplacement en direct pendant",
  "Share Location": "Partager l'emplacement",
  "Shared live location": "Emplacement en direct partagé",
  "Shared location": "Position partagée",
  Shuffle: Shuffle$8,
  "size limit": "limite de taille",
  "Slow Mode ON": "Mode lent activé",
  "Slow mode, wait {{ seconds }}s...": "Mode lent, attendez {{ seconds }} s...",
  "Some of the files will not be accepted": "Certains fichiers ne seront pas acceptés",
  "Start typing to search": "Commencez à taper pour rechercher",
  "Stop sharing": "Arrêter de partager",
  Submit: Submit$8,
  "Suggest an option": "Suggérer une option",
  "Tap to remove": "Appuyez pour retirer",
  "Thinking...": "Réflexion...",
  "this content could not be displayed": "ce contenu n'a pas pu être affiché",
  "This field cannot be empty or contain only spaces": "Ce champ ne peut pas être vide ou contenir uniquement des espaces",
  "This message did not meet our content guidelines": "Ce message ne respecte pas nos directives de contenu",
  Thread: Thread$9,
  "Thread has not been found": "Le fil de discussion n'a pas été trouvé",
  "Thread reply": "Réponse dans le fil",
  "Thread Reply": "Réponse dans le fil",
  "ThreadListUnseenThreadsBanner/loading": "Chargement...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} fil non lu",
  "ThreadListUnseenThreadsBanner/unreadThreads_many": "{{ count }} fils non lus",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} fils non lus",
  Threads: Threads$8,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Hier]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": `{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Aujourd'hui]", "nextDay": "[Demain]", "lastDay": "[Hier]", "nextWeek": "dddd", "lastWeek": "[Dernier] dddd", "sameElse": "ddd, D MMM" }) }}`,
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Aujourd'hui",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Hier",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Pour commencer l'enregistrement, autorisez l'accès à la caméra dans votre navigateur",
  "To start recording, allow the microphone access in your browser": "Pour commencer l'enregistrement, autorisez l'accès au microphone dans votre navigateur",
  totalVoteCount_one: totalVoteCount_one$6,
  totalVoteCount_many: totalVoteCount_many$3,
  totalVoteCount_other: totalVoteCount_other$8,
  Translated: Translated$8,
  "Translated from {{ language }}": "Traduit du {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Tapez un nombre de 2 à 10",
  "Type your message": "Tapez votre message",
  Unarchive: Unarchive$8,
  "unban-command-args": "[@nomdutilisateur]",
  "unban-command-description": "Débannir un utilisateur",
  "Unblock User": "Débloquer l'utilisateur",
  "unknown error": "erreur inconnue",
  Unmute: Unmute$8,
  "unmute-command-args": "[@nomdutilisateur]",
  "unmute-command-description": "Démuter un utilisateur",
  Unpin: Unpin$8,
  "Unread messages": "Messages non lus",
  "Unsupported attachment": "Pièce jointe non prise en charge",
  "unsupported file type": "type de fichier non pris en charge",
  Update: Update$8,
  "Update your comment": "Mettre à jour votre commentaire",
  "Upload blocked": "Téléversement bloqué",
  "Upload error": "Erreur de téléversement",
  "Upload failed": "Échec du téléversement",
  'Upload type: "{{ type }}" is not allowed': `Le type de fichier : "{{ type }}" n'est pas autorisé`,
  "User blocked": "Utilisateur bloqué",
  "User unblocked": "Utilisateur débloqué",
  "User uploaded content": "Contenu téléchargé par l'utilisateur",
  Video: Video$8,
  videoCount_one: videoCount_one$6,
  videoCount_many: videoCount_many$3,
  videoCount_other: videoCount_other$8,
  View: View$8,
  "View {{count}} comments_one": "Voir {{count}} commentaire",
  "View {{count}} comments_many": "Voir {{count}} commentaires",
  "View {{count}} comments_other": "Voir {{count}} commentaires",
  "View all": "Tout voir",
  "View original": "Voir l'original",
  "View results": "Voir les résultats",
  "View translation": "Voir la traduction",
  "Voice message": "Message vocal",
  "Voice message {{ duration }}": "Message vocal {{ duration }}",
  "Voice message deleted": "Message vocal supprimé",
  voiceMessageCount_one: voiceMessageCount_one$6,
  voiceMessageCount_many: voiceMessageCount_many$3,
  voiceMessageCount_other: voiceMessageCount_other$8,
  "Vote ended": "Vote terminé",
  Votes: Votes$8,
  "Wait until all attachments have uploaded": "Attendez que toutes les pièces jointes soient téléchargées",
  "Waiting for network…": "En attente du réseau…",
  You: You$8,
  "You've reached the maximum number of files": "Vous avez atteint le nombre maximal de fichiers"
};
var Anonymous$7 = "गुमनाम";
var Archive$7 = "आर्काइव";
var Attach$7 = "संलग्न करें";
var Attachment$8 = "अनुलग्नक";
var Back$7 = "वापस";
var Cancel$7 = "रद्द करें";
var Channels$7 = "चैनल";
var Chats$7 = "चैट";
var Close$7 = "बंद करे";
var Commands$7 = "कमांड";
var Create$7 = "बनाएँ";
var Delete$7 = "डिलीट";
var Delivered$7 = "पहुंच गया";
var Edited$7 = "संपादित";
var End$7 = "समाप्त";
var File$8 = "फ़ाइल";
var fileCount_one$5 = "1 फ़ाइल";
var fileCount_other$7 = "{{ count }} फ़ाइलें";
var Flag$7 = "फ्लैग करे";
var Image$8 = "छवि";
var imageCount_one$5 = "1 छवि";
var imageCount_other$7 = "{{ count }} छवियाँ";
var Link$7 = "लिंक";
var linkCount_one$5 = "1 लिंक";
var linkCount_other$7 = "{{ count }} लिंक";
var live$7 = "लाइव";
var Location$7 = "स्थान";
var Menu$7 = "मेन्यू";
var Mute$7 = "म्यूट करे";
var New$7 = "नए";
var Offline$7 = "ऑफलाइन";
var Ok$7 = "ठीक है";
var Online$7 = "ऑनलाइन";
var Options$7 = "विकल्प";
var Original$7 = "मूल";
var Photo$7 = "फ़ोटो";
var Pin$7 = "पिन";
var Poll$8 = "मतदान";
var Question$7 = "प्रश्न";
var Reply$7 = "जवाब दे दो";
var replyCount_one$7 = "1 रिप्लाई";
var replyCount_other$7 = "{{ count }} रिप्लाई";
var Resend$7 = "फिर से भेजें";
var Search$8 = "खोज";
var searchResultsCount_one$7 = "1 परिणाम";
var searchResultsCount_other$7 = "{{ count }} परिणाम";
var Send$7 = "भेजे";
var Sent$7 = "भेजा गया";
var Share$7 = "साझा करें";
var Shuffle$7 = "मिश्रित करें";
var Submit$7 = "जमा करें";
var Thread$8 = "रिप्लाई थ्रेड";
var Threads$7 = "थ्रेड्स";
var totalVoteCount_one$5 = "कुल 1 वोट";
var totalVoteCount_other$7 = "कुल {{ count }} वोट";
var Translated$7 = "अनुवादित";
var Unarchive$7 = "अनआर्काइव";
var Unmute$7 = "अनम्यूट";
var Unpin$7 = "अनपिन";
var Update$7 = "अपडेट करें";
var Video$7 = "वीडियो";
var videoCount_one$5 = "1 वीडियो";
var videoCount_other$7 = "{{ count }} वीडियो";
var View$7 = "देखें";
var voiceMessageCount_one$5 = "1 ध्वनि संदेश";
var voiceMessageCount_other$7 = "{{ count }} ध्वनि संदेश";
var Votes$7 = "वोट";
var You$7 = "आप";
var hiTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} और {{ moreCount }} और",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} और {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} फ़ाइल",
  "{{ count }} files_other": "{{ count }} फ़ाइलें",
  "{{ count }} people are typing_one": "{{ count }} व्यक्ति टाइप कर रहा है",
  "{{ count }} people are typing_many": "{{ count }} लोग टाइप कर रहे हैं",
  "{{ count }} people are typing_other": "{{ count }} लोग टाइप कर रहे हैं",
  "{{ count }} photos_one": "{{ count }} फ़ोटो",
  "{{ count }} photos_other": "{{ count }} फ़ोटो",
  "{{ count }} reactions_one": "{{ count }} प्रतिक्रिया",
  "{{ count }} reactions_other": "{{ count }} प्रतिक्रियाएं",
  "{{ count }} videos_one": "{{ count }} वीडियो",
  "{{ count }} videos_other": "{{ count }} वीडियो",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} और {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} और",
  "{{ memberCount }} members": "{{ memberCount }} मेंबर्स",
  "{{ typing }} are typing": "{{ typing }} टाइप कर रहे हैं",
  "{{ typing }} is typing": "{{ typing }} टाइप कर रहा है",
  "{{ user }} has been muted": "{{ user }} को म्यूट कर दिया गया है",
  "{{ user }} has been unmuted": "{{ user }} को अनम्यूट कर दिया गया है",
  "{{ user }} is typing...": "{{ user }} टाइप कर रहा है...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} और {{ user }} टाइप कर रहे हैं...",
  "{{ users }} and more are typing...": "{{ users }} और अधिक टाइप कर रहे हैं...",
  "{{ watcherCount }} online": "{{ watcherCount }} ऑनलाइन",
  "{{count}} new messages_one": "{{count}} नया संदेश",
  "{{count}} new messages_other": "{{count}} नए संदेश",
  "{{count}} unread_one": "{{count}} अपठित",
  "{{count}} unread_other": "{{count}} अपठित",
  "{{count}} votes_one": "{{count}} वोट",
  "{{count}} votes_other": "{{count}} वोट",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} और विकल्प",
  "+{{count}} more options_other": "+{{count}} और विकल्प",
  "🏙 Attachment...": "🏙 अटैचमेंट",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} ने बनाया: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} ने वोट दिया: {{pollOptionText}}",
  "📍Shared location": "📍साझा किया गया स्थान",
  "Add a comment": "एक टिप्पणी जोड़ें",
  "Add an option": "एक विकल्प जोड़ें",
  "Add reaction": "प्रतिक्रिया जोड़ें",
  "All results loaded": "सभी परिणाम लोड हो गए",
  "Allow access to camera": "कैमरा तक पहुँच दें",
  "Allow access to microphone": "माइक्रोफ़ोन तक पहुँच दें",
  "Allow comments": "टिप्पणियाँ की अनुमति दें",
  "Allow option suggestion": "विकल्प सुझाव की अनुमति दें",
  "Allow others to add comments": "दूसरों को टिप्पणी जोड़ने दें",
  "Also send as a direct message": "सीधे संदेश के रूप में भी भेजें",
  "Also send in channel": "चैनल में भी भेजें",
  "Also sent in channel": "चैनल में भी भेजा गया",
  "An error has occurred during recording": "रेकॉर्डिंग के दौरान एक त्रुटि आ गई है",
  "An error has occurred during the recording processing": "रेकॉर्डिंग प्रोसेसिंग के दौरान एक त्रुटि आ गई है",
  Anonymous: Anonymous$7,
  "Anonymous poll": "गुमनाम मतदान",
  Archive: Archive$7,
  "Are you sure you want to delete this message?": "क्या आप वाकई इस संदेश को हटाना चाहते हैं?",
  "aria/Attachment": "अनुलग्नक",
  "aria/Block User": "उपयोगकर्ता को ब्लॉक करें",
  "aria/Bookmark Message": "संदेश बुकमार्क करें",
  "aria/Cancel Reply": "उत्तर रद्द करें",
  "aria/Cancel upload": "अपलोड रद्द करें",
  "aria/Channel list": "चैनल सूची",
  "aria/Channel search results": "चैनल खोज परिणाम",
  "aria/Close thread": "थ्रेड बंद करें",
  "aria/Collapse sidebar": "साइडबार संक्षिप्त करें",
  "aria/Copy Message Text": "संदेश की टेक्स्ट कॉपी करें",
  "aria/Delete Message": "संदेश डिलीट करें",
  "aria/Download attachment": "अनुलग्नक डाउनलोड करें",
  "aria/Edit Message": "मैसेज में बदलाव करे",
  "aria/Emoji picker": "इमोजी चुनने वाला",
  "aria/Expand sidebar": "साइडबार विस्तारित करें",
  "aria/File input": "फ़ाइल इनपुट",
  "aria/File upload": "फ़ाइल अपलोड",
  "aria/Flag Message": "संदेश फ्लैग करें",
  "aria/Image failed to load": "छवि लोड होने में विफल",
  "aria/Image input": "छवि इनपुट",
  "aria/Load More Channels": "और चैनल लोड करें",
  "aria/Mark Message Unread": "अपठित चिह्नित करें",
  "aria/Menu": "मेन्यू",
  "aria/Message Options": "संदेश विकल्प",
  "aria/Mute User": "उपयोगकर्ता म्यूट करें",
  "aria/Open Attachment Selector": "अटैचमेंट चयनकर्ता खोलें",
  "aria/Open Menu": "मेन्यू खोलें",
  "aria/Open Message Actions Menu": "संदेश क्रिया मेन्यू खोलें",
  "aria/Open Reaction Selector": "प्रतिक्रिया चयनकर्ता खोलें",
  "aria/Open Thread": "थ्रेड खोलें",
  "aria/Percent complete": "{{percent}} प्रतिशत पूर्ण",
  "aria/Pin Message": "संदेश पिन करें",
  "aria/Quote Message": "संदेश उद्धरण",
  "aria/Reaction list": "प्रतिक्रिया सूची",
  "aria/Remind Me Message": "मुझे याद दिलाएं",
  "aria/Remind Me Options": "रिमाइंडर विकल्प",
  "aria/Remove attachment": "संलग्नक हटाएं",
  "aria/Remove location attachment": "स्थान संलग्नक हटाएं",
  "aria/Remove Reminder": "अनुस्मारक हटाएं",
  "aria/Remove Save For Later": "बाद में देखें हटाएं",
  "aria/Resend Message": "संदेश फिर से भेजें",
  "aria/Retry upload": "अपलोड पुनः प्रयास करें",
  "aria/Search results": "खोज परिणाम",
  "aria/Search results header filter button": "खोज परिणाम हेडर फ़िल्टर बटन",
  "aria/Send": "भेजें",
  "aria/Show preview": "पूर्वावलोकन दिखाएं",
  "aria/Stop AI Generation": "एआई जनरेशन रोकें",
  "aria/Unblock User": "उपयोगकर्ता अनब्लॉक करें",
  "aria/Unmute User": "उपयोगकर्ता अनम्यूट करें",
  "aria/Unpin Message": "संदेश अनपिन करें",
  "Ask a question": "एक प्रश्न पूछें",
  Attach: Attach$7,
  "Attach files": "फाइल्स अटैच करे",
  Attachment: Attachment$8,
  "Attachment upload blocked due to {{reason}}": "{{reason}} के कारण अटैचमेंट अपलोड ब्लॉक किया गया",
  "Attachment upload failed due to {{reason}}": "{{reason}} के कारण अटैचमेंट अपलोड विफल रहा",
  Back: Back$7,
  "ban-command-args": "[@उपयोगकर्तनाम] [पाठ]",
  "ban-command-description": "एक उपयोगकर्ता को प्रतिषेधित करें",
  "Block User": "उपयोगकर्ता को ब्लॉक करें",
  Cancel: Cancel$7,
  "Cannot seek in the recording": "रेकॉर्डिंग में खोज नहीं की जा सकती",
  "Channel archived": "चैनल संग्रहीत किया गया",
  "Channel Missing": "चैनल उपलब्ध नहीं है",
  "Channel muted": "चैनल म्यूट किया गया",
  "Channel pinned": "चैनल पिन किया गया",
  "Channel unarchived": "चैनल असंग्रहीत किया गया",
  "Channel unmuted": "चैनल अनम्यूट किया गया",
  "Channel unpinned": "चैनल अनपिन किया गया",
  Channels: Channels$7,
  Chats: Chats$7,
  "Choose between 2 to 10 options": "2 से 10 विकल्प चुनें",
  Close: Close$7,
  "Close emoji picker": "इमोजी पिकर बंद करें",
  Commands: Commands$7,
  "Commands matching": "मेल खाती है",
  "Connection failure, reconnecting now...": "कनेक्शन विफल रहा, अब पुनः कनेक्ट हो रहा है ...",
  "Copy Message": "संदेश कॉपी करें",
  Create: Create$7,
  "Create poll": "मतदान बनाएँ",
  "Current location": "वर्तमान स्थान",
  Delete: Delete$7,
  "Delete for me": "मेरे लिए डिलीट करें",
  "Delete message": "संदेश हटाएं",
  Delivered: Delivered$7,
  "Direct message": "प्रत्यक्ष संदेश",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "क्या आप अभी इस पोल को समाप्त करना चाहते हैं? इस पोल में अब कोई भी वोट नहीं कर पाएगा।",
  "Download All": "सब डाउनलोड करें",
  "Download Attachment": "अटैचमेंट डाउनलोड करें",
  "Download attachment {{ name }}": "अनुलग्नक {{ name }} डाउनलोड करें",
  "Drag your files here": "अपनी फ़ाइलें यहाँ खींचें",
  "Drag your files here to add to your post": "अपनी फ़ाइलें यहाँ खींचें और अपने पोस्ट में जोड़ने के लिए",
  "Due {{ timeLeft }}": "{{ timeLeft }} में देय",
  "Due since {{ dueSince }}": "{{ dueSince }} से देय",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "मैसेज में बदलाव करे",
  "Edit message request failed": "संदेश संपादित करने का अनुरोध विफल रहा",
  Edited: Edited$7,
  "Emoji matching": "इमोजी मिलान",
  "Empty message...": "खाली संदेश ...",
  End: End$7,
  "End poll": "पोल समाप्त करें",
  "End this poll?": "इस पोल को समाप्त करें?",
  "End vote": "मत समाप्त करें",
  "Enforce unique vote is enabled": "अनोखा वोट सक्षम है",
  "Error": "त्रुटि",
  "Error adding flag": "ध्वज जोड़ने में त्रुटि",
  "Error connecting to chat, refresh the page to try again.": "चैट से कनेक्ट करने में त्रुटि, पेज को रिफ्रेश करें",
  "Error deleting message": "संदेश हटाने में त्रुटि",
  "Error fetching reactions": "प्रतिक्रियाएँ लोड करने में त्रुटि",
  "Error marking message unread": "संदेश को अपठित चिह्नित करने में त्रुटि",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "संदेश को अपठित मार्क करने में त्रुटि। सबसे नए 100 चैनल संदेश से पहले के सभी अपठित संदेशों को अपठित मार्क नहीं किया जा सकता है।",
  "Error muting a user ...": "यूजर को म्यूट करने का प्रयास फेल हुआ",
  "Error pinning message": "संदेश को पिन करने में त्रुटि",
  "Error removing message pin": "संदेश पिन निकालने में त्रुटि",
  "Error reproducing the recording": "रिकॉर्डिंग पुन: उत्पन्न करने में त्रुटि",
  "Error starting recording": "रेकॉर्डिंग शुरू करने में त्रुटि",
  "Error unmuting a user ...": "यूजर को अनम्यूट करने का प्रयास फेल हुआ",
  "Error uploading attachment": "अटैचमेंट अपलोड करते समय त्रुटि",
  "Error uploading file": "फ़ाइल अपलोड करने में त्रुटि",
  "Error uploading image": "छवि अपलोड करने में त्रुटि",
  "Error: {{ errorMessage }}": "फेल: {{ errorMessage }}",
  "Failed to block user": "उपयोगकर्ता को ब्लॉक करने में विफल",
  "Failed to create the poll": "मतदान बनाने में विफल",
  "Failed to create the poll due to {{reason}}": "मतदान {{reason}} के कारण नहीं बन सका",
  "Failed to delete the message": "संदेश हटाने में विफल",
  "Failed to end the poll": "पोल समाप्त करने में विफल",
  "Failed to end the poll due to {{reason}}": "{{reason}} के कारण पोल समाप्त करने में विफल",
  "Failed to jump to the first unread message": "पहले अपठित संदेश पर जाने में विफल",
  "Failed to leave channel": "चैनल छोड़ने में विफल",
  "Failed to mark channel as read": "चैनल को पढ़ा हुआ चिह्नित करने में विफल।",
  "Failed to play the recording": "रेकॉर्डिंग प्ले करने में विफल",
  "Failed to retrieve location": "स्थान प्राप्त करने में विफल",
  "Failed to share location": "स्थान साझा करने में विफल",
  "Failed to update channel archive status": "चैनल के आर्काइव स्थिति को अपडेट करने में विफल",
  "Failed to update channel mute status": "चैनल की म्यूट स्थिति को अपडेट करने में विफल",
  "Failed to update channel pinned status": "चैनल की पिन स्थिति को अपडेट करने में विफल",
  File: File$8,
  "File is required for upload attachment": "अटैचमेंट अपलोड के लिए फ़ाइल आवश्यक है",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "फ़ाइल बहुत बड़ी है: {{ size }}, अधिकतम अपलोड साइज़ {{ limit }} है",
  "File too large": "फ़ाइल बहुत बड़ी है",
  fileCount_one: fileCount_one$5,
  fileCount_other: fileCount_other$7,
  Flag: Flag$7,
  "Generating...": "बना रहा है...",
  "giphy-command-args": "[पाठ]",
  "giphy-command-description": "चैनल पर एक क्रॉफिल जीआइएफ पोस्ट करें",
  "Hide who voted": "किसने वोट दिया छिपाएं",
  Image: Image$8,
  imageCount_one: imageCount_one$5,
  imageCount_other: imageCount_other$7,
  "Instant commands": "तत्काल कमांड",
  "language/af": "अफ्रीकी",
  "language/am": "अम्हारिक",
  "language/ar": "अरबी",
  "language/az": "अज़रबैजानी",
  "language/bg": "बुल्गारियाई",
  "language/bn": "बंगाली",
  "language/bs": "बोस्नियाई",
  "language/cs": "चेक",
  "language/da": "डेनिश",
  "language/de": "जर्मन",
  "language/el": "यूनानी",
  "language/en": "अंग्रेज़ी",
  "language/es": "स्पेनिश",
  "language/es-MX": "स्पेनिश (मेक्सिको)",
  "language/et": "एस्तोनियाई",
  "language/fa": "फ़ारसी",
  "language/fa-AF": "दरी",
  "language/fi": "फ़िनिश",
  "language/fr": "फ़्रेंच",
  "language/fr-CA": "फ़्रेंच (कनाडा)",
  "language/ha": "हौसा",
  "language/he": "हिब्रू",
  "language/hi": "हिंदी",
  "language/hr": "क्रोएशियाई",
  "language/ht": "हैतियाई क्रियोल",
  "language/hu": "हंगेरी",
  "language/id": "इंडोनेशियाई",
  "language/it": "इतालवी",
  "language/ja": "जापानी",
  "language/ka": "जॉर्जियाई",
  "language/ko": "कोरियाई",
  "language/lt": "लिथुआनियाई",
  "language/lv": "लातवियाई",
  "language/ms": "मलय",
  "language/nl": "डच",
  "language/no": "नॉर्वेजियाई",
  "language/pl": "पोलिश",
  "language/ps": "पश्तो",
  "language/pt": "पुर्तगाली",
  "language/ro": "रोमानियाई",
  "language/ru": "रूसी",
  "language/sk": "स्लोवाक",
  "language/sl": "स्लोवेनियाई",
  "language/so": "सोमाली",
  "language/sq": "अल्बानियाई",
  "language/sr": "सर्बियाई",
  "language/sv": "स्वीडिश",
  "language/sw": "स्वाहिली",
  "language/ta": "तमिल",
  "language/th": "थाई",
  "language/tl": "तागालोग",
  "language/tr": "तुर्की",
  "language/uk": "यूक्रेनियाई",
  "language/ur": "उर्दू",
  "language/vi": "वियतनामी",
  "language/zh": "चीनी (सरलीकृत)",
  "language/zh-TW": "चीनी (पारंपरिक)",
  "Leave Channel": "चैनल छोड़ें",
  "Left channel": "चैनल छोड़ दिया गया",
  "Let others add options": "दूसरों को विकल्प जोड़ने दें",
  "Limit votes per person": "प्रति व्यक्ति वोट सीमित करें",
  Link: Link$7,
  linkCount_one: linkCount_one$5,
  linkCount_other: linkCount_other$7,
  live: live$7,
  "Live for {{duration}}": "{{duration}} के लिए लाइव",
  "Live location": "लाइव स्थान",
  "Live until {{ timestamp }}": "{{ timestamp }} तक लाइव",
  "Load more": "और लोड करें",
  "Local upload attachment missing local id": "लोकल अपलोड अटैचमेंट में लोकल आईडी नहीं है",
  Location: Location$7,
  "Location sharing ended": "स्थान साझा करना समाप्त",
  "Location: {{ coordinates }}": "स्थान: {{ coordinates }}",
  "Mark as unread": "अपठित चिह्नित करें",
  "Maximum number of votes (from 2 to 10)": "अधिकतम वोटों की संख्या (2 से 10)",
  Menu: Menu$7,
  "Message deleted": "मैसेज हटा दिया गया",
  "Message failed to send": "संदेश भेजने में विफल",
  "Message has been successfully flagged": "मैसेज को फ्लैग कर दिया गया है",
  "Message marked as unread": "संदेश को अपठित के रूप में चिह्नित किया गया",
  "Message pinned": "संदेश पिन किया गया",
  "Message unpinned": "संदेश अनपिन किया गया",
  "Message was blocked by moderation policies": "संदेश को मॉडरेशन नीतियों द्वारा ब्लॉक कर दिया गया है",
  "Messages have been marked unread.": "संदेशों को अपठित चिह्नित किया गया है।",
  "Missing permissions to upload the attachment": "अटैचमेंट अपलोड करने के लिए अनुमतियां गायब",
  "Multiple votes": "कई वोट",
  Mute: Mute$7,
  "mute-command-args": "[@उपयोगकर्तनाम]",
  "mute-command-description": "एक उपयोगकर्ता को म्यूट करें",
  "network error": "नेटवर्क त्रुटि",
  New: New$7,
  "New Messages!": "नए मैसेज!",
  "Next image": "अगली छवि",
  "No chats here yet…": "यहां अभी तक कोई चैट नहीं...",
  "No conversations yet": "अभी तक कोई बातचीत नहीं है",
  "No items exist": "कोई आइटम मौजूद नहीं है",
  "No results found": "कोई परिणाम नहीं मिला",
  "Nobody will be able to vote in this poll anymore.": "अब कोई भी इस मतदान में मतदान नहीं कर सकेगा।",
  "Nothing yet...": "कोई मैसेज नहीं है",
  Offline: Offline$7,
  Ok: Ok$7,
  Online: Online$7,
  "Only numbers are allowed": "केवल संख्याएँ अनुमत हैं",
  "Only visible to you": "केवल आपको दिखाई देता है",
  "Open emoji picker": "इमोजी पिकर खोलिये",
  "Open gallery at image {{ index }}": "गैलरी को छवि {{ index }} पर खोलें",
  "Open image in gallery": "छवि को गैलरी में खोलें",
  "Option already exists": "विकल्प पहले से मौजूद है",
  "Option is empty": "विकल्प खाली है",
  Options: Options$7,
  Original: Original$7,
  "People matching": "मेल खाते लोग",
  Photo: Photo$7,
  Pin: Pin$7,
  "Pinned by {{ name }}": "{{ name }} द्वारा पिन किया गया",
  "Pinned by You": "आपके द्वारा पिन किया गया",
  "placeholder/PollComment": "आपकी टिप्पणी",
  "placeholder/PollOptionSuggestion": "नया विकल्प दर्ज करें",
  "Play video": "वीडियो चलाएं",
  Poll: Poll$8,
  "Poll comments": "मतदान टिप्पणियाँ",
  "Poll ended": "पोल समाप्त",
  "Poll options": "मतदान विकल्प",
  "Poll results": "मतदान परिणाम",
  "Previous image": "पिछली छवि",
  Question: Question$7,
  "Question {{ optionOrderNumber}}": "प्रश्न {{ optionOrderNumber}}",
  "Question is required": "प्रश्न आवश्यक है",
  "Quote Reply": "उद्धरण जवाब",
  "Reached the vote limit. Remove an existing vote first.": "मतदान सीमा तक पहुंच गया। पहले एक मौजूदा वोट हटाएं।",
  "Recording format is not supported and cannot be reproduced": "रेकॉर्डिंग फ़ॉर्मेट समर्थित नहीं है और पुनः उत्पन्न नहीं किया जा सकता",
  "Remind me": "मुझे याद दिलाएं",
  "Remind Me": "मुझे याद दिलाएं",
  "Reminder set": "अनुस्मारक सेट किया गया",
  "Remove reminder": "रिमाइंडर हटाएं",
  "Remove save for later": "बाद में देखें हटाएं",
  "Replied to a thread": "थ्रेड में जवाब दिया",
  Reply: Reply$7,
  "Reply to {{ authorName }}": "{{ authorName }} को जवाब दें",
  "Reply to a message to start a thread": "थ्रेड शुरू करने के लिए किसी संदेश का जवाब दें",
  "Reply to Message": "संदेश का जवाब दें",
  replyCount_one: replyCount_one$7,
  replyCount_other: replyCount_other$7,
  Resend: Resend$7,
  "Retry upload": "अपलोड फिर से करें",
  "Save for later": "बाद के लिए सहेजें",
  "Saved for later": "बाद के लिए सहेजा गया",
  Search: Search$8,
  "Search GIFs": "GIF खोजें",
  "search-results-header-filter-source-button-label--channels": "चैनल्स",
  "search-results-header-filter-source-button-label--messages": "संदेश",
  "search-results-header-filter-source-button-label--users": "उपयोगकर्ता",
  "Searching for {{ searchSourceType }}...": "{{ searchSourceType }} खोज रहे हैं...",
  "Searching...": "खोज कर...",
  searchResultsCount_one: searchResultsCount_one$7,
  searchResultsCount_other: searchResultsCount_other$7,
  "Select a thread to continue the conversation": "बातचीत जारी रखने के लिए एक थ्रेड चुनें",
  "Select more than one option": "एक से अधिक विकल्प चुनें",
  "Select one": "एक चुनें",
  "Select one or more": "एक या अधिक चुनें",
  "Select up to {{count}}_one": "अधिकतम {{count}} तक चुनें",
  "Select up to {{count}}_other": "अधिकतम {{count}} तक चुनें",
  Send: Send$7,
  "Send a message to start the conversation": "बातचीत शुरू करने के लिए संदेश भेजें",
  "Send Anyway": "वैसे भी भेजें",
  "Send message request failed": "संदेश भेजने का अनुरोध विफल रहा",
  "Send poll": "पोल भेजें",
  "Sending...": "भेजा जा रहा है",
  Sent: Sent$7,
  Share: Share$7,
  "Share live location for": "लाइव स्थान साझा करें",
  "Share Location": "स्थान साझा करें",
  "Shared live location": "साझा किया गया लाइव स्थान",
  "Shared location": "साझा स्थान",
  Shuffle: Shuffle$7,
  "size limit": "आकार सीमा",
  "Slow Mode ON": "स्लो मोड ऑन",
  "Slow mode, wait {{ seconds }}s...": "स्लो मोड, {{ seconds }} सेकंड प्रतीक्षा करें...",
  "Some of the files will not be accepted": "कुछ फ़ाइलें स्वीकार नहीं की जाएंगी",
  "Start typing to search": "खोजने के लिए टाइप करना शुरू करें",
  "Stop sharing": "साझा करना बंद करें",
  Submit: Submit$7,
  "Suggest an option": "एक विकल्प सुझाव दें",
  "Tap to remove": "हटाने के लिए टैप करें",
  "Thinking...": "सोच रहा है...",
  "this content could not be displayed": "यह कॉन्टेंट लोड नहीं हो पाया",
  "This field cannot be empty or contain only spaces": "यह फ़ील्ड खाली नहीं हो सकता या केवल रिक्त स्थान नहीं रख सकता",
  "This message did not meet our content guidelines": "यह संदेश हमारे सामग्री दिशानिर्देशों के अनुरूप नहीं था",
  Thread: Thread$8,
  "Thread has not been found": "थ्रेड नहीं मिला",
  "Thread reply": "थ्रेड में उत्तर",
  "Thread Reply": "थ्रेड में उत्तर",
  "ThreadListUnseenThreadsBanner/loading": "लोड हो रहा है...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} अपठित थ्रेड",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} अपठित थ्रेड",
  Threads: Threads$7,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[बीता कल]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[आज]", "nextDay": "[कल]", "lastDay": "[बीता कल]", "nextWeek": "dddd", "lastWeek": "[पिछला] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "आज",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "बीता कल",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "रिकॉर्डिंग शुरू करने के लिए, अपने ब्राउज़र में कैमरा तक पहुँच दें",
  "To start recording, allow the microphone access in your browser": "रिकॉर्डिंग शुरू करने के लिए, अपने ब्राउज़र में माइक्रोफ़ोन तक पहुँच दें",
  totalVoteCount_one: totalVoteCount_one$5,
  totalVoteCount_other: totalVoteCount_other$7,
  Translated: Translated$7,
  "Translated from {{ language }}": "{{ language }} से अनुवादित",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "2 से 10 तक का एक नंबर टाइप करें",
  "Type your message": "अपना मैसेज लिखे",
  Unarchive: Unarchive$7,
  "unban-command-args": "[@उपयोगकर्तनाम]",
  "unban-command-description": "एक उपयोगकर्ता को प्रतिषेध से मुक्त करें",
  "Unblock User": "उपयोगकर्ता अनब्लॉक करें",
  "unknown error": "अज्ञात त्रुटि",
  Unmute: Unmute$7,
  "unmute-command-args": "[@उपयोगकर्तनाम]",
  "unmute-command-description": "एक उपयोगकर्ता को अनम्यूट करें",
  Unpin: Unpin$7,
  "Unread messages": "अपठित संदेश",
  "Unsupported attachment": "असमर्थित अटैचमेंट",
  "unsupported file type": "असमर्थित फ़ाइल प्रकार",
  Update: Update$7,
  "Update your comment": "अपने टिप्पणी को अपडेट करें",
  "Upload blocked": "अपलोड अवरुद्ध",
  "Upload error": "अपलोड त्रुटि",
  "Upload failed": "अपलोड विफल",
  'Upload type: "{{ type }}" is not allowed': 'अपलोड प्रकार: "{{ type }}" की अनुमति नहीं है',
  "User blocked": "उपयोगकर्ता अवरुद्ध किया गया",
  "User unblocked": "उपयोगकर्ता अनब्लॉक किया गया",
  "User uploaded content": "उपयोगकर्ता अपलोड की गई सामग्री",
  Video: Video$7,
  videoCount_one: videoCount_one$5,
  videoCount_other: videoCount_other$7,
  View: View$7,
  "View {{count}} comments_one": "देखें {{count}} टिप्पणी",
  "View {{count}} comments_other": "देखें {{count}} टिप्पणियाँ",
  "View all": "सभी देखें",
  "View original": "मूल देखें",
  "View results": "परिणाम देखें",
  "View translation": "अनुवाद देखें",
  "Voice message": "आवाज संदेश",
  "Voice message {{ duration }}": "वॉइस संदेश {{ duration }}",
  "Voice message deleted": "वॉइस संदेश हटा दिया गया",
  voiceMessageCount_one: voiceMessageCount_one$5,
  voiceMessageCount_other: voiceMessageCount_other$7,
  "Vote ended": "मतदान समाप्त",
  Votes: Votes$7,
  "Wait until all attachments have uploaded": "सभी अटैचमेंट अपलोड होने तक प्रतीक्षा करें",
  "Waiting for network…": "नेटवर्क की प्रतीक्षा…",
  You: You$7,
  "You've reached the maximum number of files": "आप अधिकतम फ़ाइलों तक पहुँच गए हैं"
};
var Anonymous$6 = "Anonimo";
var Archive$6 = "Archivia";
var Attach$6 = "Allega";
var Attachment$7 = "Allegato";
var Back$6 = "Indietro";
var Cancel$6 = "Annulla";
var Channels$6 = "Canali";
var Chats$6 = "Chat";
var Close$6 = "Chiudi";
var Commands$6 = "Comandi";
var Create$6 = "Crea";
var Delete$6 = "Elimina";
var Delivered$6 = "Consegnato";
var Edited$6 = "Modificato";
var End$6 = "Fine";
var File$7 = "File";
var fileCount_one$4 = "1 file";
var fileCount_many$2 = "{{ count }} file";
var fileCount_other$6 = "{{ count }} file";
var Flag$6 = "Segnala";
var Image$7 = "Immagine";
var imageCount_one$4 = "Immagine";
var imageCount_many$2 = "{{ count }} immagini";
var imageCount_other$6 = "{{ count }} immagini";
var Link$6 = "Collegamento";
var linkCount_one$4 = "Link";
var linkCount_many$2 = "{{ count }} link";
var linkCount_other$6 = "{{ count }} link";
var live$6 = "live";
var Location$6 = "Posizione";
var Menu$6 = "Menù";
var Mute$6 = "Silenzia";
var New$6 = "Nuovo";
var Offline$6 = "Offline";
var Ok$6 = "OK";
var Online$6 = "Online";
var Options$6 = "Opzioni";
var Original$6 = "Originale";
var Photo$6 = "Foto";
var Pin$6 = "Appunta";
var Poll$7 = "Sondaggio";
var Question$6 = "Domanda";
var Reply$6 = "Rispondi";
var replyCount_one$6 = "Una risposta";
var replyCount_many$2 = "{{ count }} risposte";
var replyCount_other$6 = "{{ count }} risposte";
var Resend$6 = "Invia di nuovo";
var Search$7 = "Cerca";
var searchResultsCount_one$6 = "1 risultato";
var searchResultsCount_many$2 = "{{ count }} risultati";
var searchResultsCount_other$6 = "{{ count }} risultati";
var Send$6 = "Invia";
var Sent$6 = "Inviato";
var Share$6 = "Condividi";
var Shuffle$6 = "Mescolare";
var Submit$6 = "Invia";
var Thread$7 = "Discussione";
var Threads$6 = "Thread";
var totalVoteCount_one$4 = "1 voto in totale";
var totalVoteCount_many$2 = "{{ count }} voti in totale";
var totalVoteCount_other$6 = "{{ count }} voti in totale";
var Translated$6 = "Tradotto";
var Unarchive$6 = "Ripristina";
var Unmute$6 = "Riattiva il notifiche";
var Unpin$6 = "Sblocca";
var Update$6 = "Aggiorna";
var Video$6 = "Video";
var videoCount_one$4 = "Video";
var videoCount_many$2 = "{{ count }} video";
var videoCount_other$6 = "{{ count }} video";
var View$6 = "Visualizza";
var voiceMessageCount_one$4 = "Messaggio vocale";
var voiceMessageCount_many$2 = "{{ count }} messaggi vocali";
var voiceMessageCount_other$6 = "{{ count }} messaggi vocali";
var Votes$6 = "Voti";
var You$6 = "Tu";
var itTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} e altri {{ moreCount }}",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} e {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} file",
  "{{ count }} files_many": "{{ count }} file",
  "{{ count }} files_other": "{{ count }} file",
  "{{ count }} people are typing_one": "{{ count }} persona sta scrivendo",
  "{{ count }} people are typing_many": "{{ count }} persone stanno scrivendo",
  "{{ count }} people are typing_other": "{{ count }} persone stanno scrivendo",
  "{{ count }} photos_one": "{{ count }} foto",
  "{{ count }} photos_many": "{{ count }} foto",
  "{{ count }} photos_other": "{{ count }} foto",
  "{{ count }} reactions_one": "{{ count }} reazione",
  "{{ count }} reactions_many": "{{ count }} reazioni",
  "{{ count }} reactions_other": "{{ count }} reazioni",
  "{{ count }} videos_one": "{{ count }} video",
  "{{ count }} videos_many": "{{ count }} video",
  "{{ count }} videos_other": "{{ count }} video",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} e {{ secondUser }}",
  "{{ imageCount }} more": "+ {{ imageCount }}",
  "{{ memberCount }} members": "{{ memberCount }} membri",
  "{{ typing }} are typing": "{{ typing }} stanno scrivendo",
  "{{ typing }} is typing": "{{ typing }} sta scrivendo",
  "{{ user }} has been muted": "{{ user }} è stato silenziato",
  "{{ user }} has been unmuted": "Notifiche riattivate per {{ user }}",
  "{{ user }} is typing...": "{{ user }} sta digitando...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} e {{ user }} stanno digitando...",
  "{{ users }} and more are typing...": "{{ users }} e altri stanno digitando...",
  "{{ watcherCount }} online": "{{ watcherCount }} online",
  "{{count}} new messages_one": "{{count}} nuovo messaggio",
  "{{count}} new messages_many": "{{count}} nuovi messaggi",
  "{{count}} new messages_other": "{{count}} nuovi messaggi",
  "{{count}} unread_one": "{{count}} non letto",
  "{{count}} unread_many": "{{count}} non letti",
  "{{count}} unread_other": "{{count}} non letti",
  "{{count}} votes_one": "{{count}} voto",
  "{{count}} votes_many": "{{count}} voti",
  "{{count}} votes_other": "{{count}} voti",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} altra opzione",
  "+{{count}} more options_many": "+{{count}} altre opzioni",
  "+{{count}} more options_other": "+{{count}} altre opzioni",
  "🏙 Attachment...": "🏙 Allegato...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} ha creato: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} ha votato: {{pollOptionText}}",
  "📍Shared location": "📍Posizione condivisa",
  "Add a comment": "Aggiungi un commento",
  "Add an option": "Aggiungi un'opzione",
  "Add reaction": "Aggiungi reazione",
  "All results loaded": "Tutti i risultati caricati",
  "Allow access to camera": "Consenti l'accesso alla fotocamera",
  "Allow access to microphone": "Consenti l'accesso al microfono",
  "Allow comments": "Consenti i commenti",
  "Allow option suggestion": "Consenti il suggerimento di opzioni",
  "Allow others to add comments": "Consenti ad altri di aggiungere commenti",
  "Also send as a direct message": "Invia anche come messaggio diretto",
  "Also send in channel": "Invia anche nel canale",
  "Also sent in channel": "Inviato anche nel canale",
  "An error has occurred during recording": "Si è verificato un errore durante la registrazione",
  "An error has occurred during the recording processing": "Si è verificato un errore durante l'elaborazione della registrazione",
  Anonymous: Anonymous$6,
  "Anonymous poll": "Sondaggio anonimo",
  Archive: Archive$6,
  "Are you sure you want to delete this message?": "Sei sicuro di voler eliminare questo messaggio?",
  "aria/Attachment": "Allegato",
  "aria/Block User": "Blocca utente",
  "aria/Bookmark Message": "Salva messaggio",
  "aria/Cancel Reply": "Annulla risposta",
  "aria/Cancel upload": "Annulla caricamento",
  "aria/Channel list": "Elenco dei canali",
  "aria/Channel search results": "Risultati della ricerca dei canali",
  "aria/Close thread": "Chiudi discussione",
  "aria/Collapse sidebar": "Comprimi barra laterale",
  "aria/Copy Message Text": "Copia testo messaggio",
  "aria/Delete Message": "Elimina messaggio",
  "aria/Download attachment": "Scarica l'allegato",
  "aria/Edit Message": "Modifica messaggio",
  "aria/Emoji picker": "Selettore di emoji",
  "aria/Expand sidebar": "Espandi barra laterale",
  "aria/File input": "Input di file",
  "aria/File upload": "Caricamento di file",
  "aria/Flag Message": "Segnala messaggio",
  "aria/Image failed to load": "Caricamento immagine non riuscito",
  "aria/Image input": "Input di immagine",
  "aria/Load More Channels": "Carica altri canali",
  "aria/Mark Message Unread": "Contrassegna come non letto",
  "aria/Menu": "Menu",
  "aria/Message Options": "Opzioni di messaggio",
  "aria/Mute User": "Mute utente",
  "aria/Open Attachment Selector": "Apri selettore allegati",
  "aria/Open Menu": "Apri menu",
  "aria/Open Message Actions Menu": "Apri il menu delle azioni di messaggio",
  "aria/Open Reaction Selector": "Apri il selettore di reazione",
  "aria/Open Thread": "Apri discussione",
  "aria/Percent complete": "{{percent}} percento completato",
  "aria/Pin Message": "Appunta messaggio",
  "aria/Quote Message": "Citazione messaggio",
  "aria/Reaction list": "Elenco delle reazioni",
  "aria/Remind Me Message": "Ricordami",
  "aria/Remind Me Options": "Opzioni promemoria",
  "aria/Remove attachment": "Rimuovi allegato",
  "aria/Remove location attachment": "Rimuovi allegato posizione",
  "aria/Remove Reminder": "Rimuovi promemoria",
  "aria/Remove Save For Later": "Rimuovi Salva per dopo",
  "aria/Resend Message": "Invia di nuovo messaggio",
  "aria/Retry upload": "Riprova caricamento",
  "aria/Search results": "Risultati della ricerca",
  "aria/Search results header filter button": "Pulsante filtro intestazione risultati ricerca",
  "aria/Send": "Invia",
  "aria/Show preview": "Mostra anteprima",
  "aria/Stop AI Generation": "Interrompi generazione IA",
  "aria/Unblock User": "Sblocca utente",
  "aria/Unmute User": "Riattiva il notifiche",
  "aria/Unpin Message": "Rimuovi messaggio appuntato",
  "Ask a question": "Fai una domanda",
  Attach: Attach$6,
  "Attach files": "Allega file",
  Attachment: Attachment$7,
  "Attachment upload blocked due to {{reason}}": "Caricamento allegato bloccato a causa di {{reason}}",
  "Attachment upload failed due to {{reason}}": "Caricamento allegato fallito a causa di {{reason}}",
  Back: Back$6,
  "ban-command-args": "[@nomeutente] [testo]",
  "ban-command-description": "Vietare un utente",
  "Block User": "Blocca utente",
  Cancel: Cancel$6,
  "Cannot seek in the recording": "Impossibile cercare nella registrazione",
  "Channel archived": "Canale archiviato",
  "Channel Missing": "Il canale non esiste",
  "Channel muted": "Canale silenziato",
  "Channel pinned": "Canale fissato",
  "Channel unarchived": "Archiviazione del canale annullata",
  "Channel unmuted": "Canale non più silenziato",
  "Channel unpinned": "Canale rimosso dai fissati",
  Channels: Channels$6,
  Chats: Chats$6,
  "Choose between 2 to 10 options": "Scegli tra 2 e 10 opzioni",
  Close: Close$6,
  "Close emoji picker": "Chiudi il selettore di emoji",
  Commands: Commands$6,
  "Commands matching": "Comandi corrispondenti",
  "Connection failure, reconnecting now...": "Errore di connessione, riconnessione in corso...",
  "Copy Message": "Copia messaggio",
  Create: Create$6,
  "Create poll": "Crea sondaggio",
  "Current location": "Posizione attuale",
  Delete: Delete$6,
  "Delete for me": "Elimina per me",
  "Delete message": "Elimina messaggio",
  Delivered: Delivered$6,
  "Direct message": "Messaggio diretto",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Vuoi terminare questo sondaggio ora? Nessuno potrà più votare in questo sondaggio.",
  "Download All": "Scarica tutto",
  "Download Attachment": "Scarica allegato",
  "Download attachment {{ name }}": "Scarica l'allegato {{ name }}",
  "Drag your files here": "Trascina i tuoi file qui",
  "Drag your files here to add to your post": "Trascina i tuoi file qui per aggiungerli al tuo post",
  "Due {{ timeLeft }}": "Scadenza tra {{ timeLeft }}",
  "Due since {{ dueSince }}": "Scaduto dal {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Modifica messaggio",
  "Edit message request failed": "Richiesta di modifica del messaggio non riuscita",
  Edited: Edited$6,
  "Emoji matching": "Abbinamento emoji",
  "Empty message...": "Messaggio vuoto...",
  End: End$6,
  "End poll": "Termina sondaggio",
  "End this poll?": "Terminare questo sondaggio?",
  "End vote": "Termina il voto",
  "Enforce unique vote is enabled": "Il voto unico è abilitato",
  "Error": "Errore",
  "Error adding flag": "Errore durante l'aggiunta del flag",
  "Error connecting to chat, refresh the page to try again.": "Errore di connessione alla chat, aggiorna la pagina per riprovare.",
  "Error deleting message": "Errore durante l'eliminazione del messaggio",
  "Error fetching reactions": "Errore nel caricamento delle reazioni",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Errore durante la marcatura del messaggio come non letto. Impossibile marcare messaggi non letti più vecchi dei più recenti 100 messaggi del canale.",
  "Error muting a user ...": "Errore nel silenziare un utente ...",
  "Error pinning message": "Errore durante il blocco del messaggio",
  "Error removing message pin": "Errore durante la rimozione del PIN del messaggio",
  "Error reproducing the recording": "Errore durante la riproduzione della registrazione",
  "Error starting recording": "Errore durante l'avvio della registrazione",
  "Error unmuting a user ...": "Errore nel riattivare un utente ...",
  "Error uploading attachment": "Errore durante il caricamento dell'allegato",
  "Error uploading file": "Errore durante il caricamento del file",
  "Error uploading image": "Errore durante il caricamento dell'immagine",
  "Error: {{ errorMessage }}": "Errore: {{ errorMessage }}",
  "Failed to block user": "Impossibile bloccare l'utente",
  "Failed to create the poll": "Impossibile creare il sondaggio",
  "Failed to create the poll due to {{reason}}": "Impossibile creare il sondaggio a causa di {{reason}}",
  "Failed to delete the message": "Impossibile eliminare il messaggio",
  "Failed to end the poll": "Impossibile terminare il sondaggio",
  "Failed to end the poll due to {{reason}}": "Impossibile terminare il sondaggio a causa di {{reason}}",
  "Failed to jump to the first unread message": "Impossibile passare al primo messaggio non letto",
  "Failed to leave channel": "Impossibile lasciare il canale",
  "Failed to mark channel as read": "Impossibile contrassegnare il canale come letto",
  "Failed to play the recording": "Impossibile riprodurre la registrazione",
  "Failed to retrieve location": "Impossibile recuperare la posizione",
  "Failed to share location": "Impossibile condividere la posizione",
  "Failed to update channel archive status": "Impossibile aggiornare lo stato di archiviazione del canale",
  "Failed to update channel mute status": "Impossibile aggiornare lo stato di silenziamento del canale",
  "Failed to update channel pinned status": "Impossibile aggiornare lo stato di blocco del canale",
  File: File$7,
  "File is required for upload attachment": "È richiesto un file per caricare l'allegato",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "Il file è troppo grande: {{ size }}, la dimensione massima di caricamento è {{ limit }}",
  "File too large": "File troppo grande",
  fileCount_one: fileCount_one$4,
  fileCount_many: fileCount_many$2,
  fileCount_other: fileCount_other$6,
  Flag: Flag$6,
  "Generating...": "Generando...",
  "giphy-command-args": "[testo]",
  "giphy-command-description": "Pubblica un gif casuale sul canale",
  "Hide who voted": "Nascondi chi ha votato",
  Image: Image$7,
  imageCount_one: imageCount_one$4,
  imageCount_many: imageCount_many$2,
  imageCount_other: imageCount_other$6,
  "Instant commands": "Comandi istantanei",
  "language/af": "Afrikaans",
  "language/am": "Amarico",
  "language/ar": "Arabo",
  "language/az": "Azero",
  "language/bg": "Bulgaro",
  "language/bn": "Bengalese",
  "language/bs": "Bosniaco",
  "language/cs": "Ceco",
  "language/da": "Danese",
  "language/de": "Tedesco",
  "language/el": "Greco",
  "language/en": "Inglese",
  "language/es": "Spagnolo",
  "language/es-MX": "Spagnolo (Messico)",
  "language/et": "Estone",
  "language/fa": "Persiano",
  "language/fa-AF": "Dari",
  "language/fi": "Finlandese",
  "language/fr": "Francese",
  "language/fr-CA": "Francese (Canada)",
  "language/ha": "Hausa",
  "language/he": "Ebraico",
  "language/hi": "Hindi",
  "language/hr": "Croato",
  "language/ht": "Creolo haitiano",
  "language/hu": "Ungherese",
  "language/id": "Indonesiano",
  "language/it": "Italiano",
  "language/ja": "Giapponese",
  "language/ka": "Georgiano",
  "language/ko": "Coreano",
  "language/lt": "Lituano",
  "language/lv": "Lettone",
  "language/ms": "Malese",
  "language/nl": "Olandese",
  "language/no": "Norvegese",
  "language/pl": "Polacco",
  "language/ps": "Pashto",
  "language/pt": "Portoghese",
  "language/ro": "Rumeno",
  "language/ru": "Russo",
  "language/sk": "Slovacco",
  "language/sl": "Sloveno",
  "language/so": "Somalo",
  "language/sq": "Albanese",
  "language/sr": "Serbo",
  "language/sv": "Svedese",
  "language/sw": "Swahili",
  "language/ta": "Tamil",
  "language/th": "Thai",
  "language/tl": "Tagalog",
  "language/tr": "Turco",
  "language/uk": "Ucraino",
  "language/ur": "Urdu",
  "language/vi": "Vietnamita",
  "language/zh": "Cinese (semplificato)",
  "language/zh-TW": "Cinese (tradizionale)",
  "Leave Channel": "Lascia il canale",
  "Left channel": "Canale lasciato",
  "Let others add options": "Lascia che altri aggiungano opzioni",
  "Limit votes per person": "Limita i voti per persona",
  Link: Link$6,
  linkCount_one: linkCount_one$4,
  linkCount_many: linkCount_many$2,
  linkCount_other: linkCount_other$6,
  live: live$6,
  "Live for {{duration}}": "Live per {{duration}}",
  "Live location": "Posizione live",
  "Live until {{ timestamp }}": "Live fino a {{ timestamp }}",
  "Load more": "Carica di più",
  "Local upload attachment missing local id": "Allegato di caricamento locale senza id locale",
  Location: Location$6,
  "Location sharing ended": "Condivisione posizione terminata",
  "Location: {{ coordinates }}": "Posizione: {{ coordinates }}",
  "Mark as unread": "Contrassegna come non letto",
  "Maximum number of votes (from 2 to 10)": "Numero massimo di voti (da 2 a 10)",
  Menu: Menu$6,
  "Message deleted": "Messaggio cancellato",
  "Message failed to send": "Invio del messaggio non riuscito",
  "Message has been successfully flagged": "Il messaggio è stato segnalato con successo",
  "Message marked as unread": "Messaggio contrassegnato come non letto",
  "Message pinned": "Messaggio bloccato",
  "Message unpinned": "Messaggio rimosso dai fissati",
  "Message was blocked by moderation policies": "Il messaggio è stato bloccato dalle politiche di moderazione",
  "Messages have been marked unread.": "I messaggi sono stati contrassegnati come non letti.",
  "Missing permissions to upload the attachment": "Autorizzazioni mancanti per caricare l'allegato",
  "Multiple votes": "Voti multipli",
  Mute: Mute$6,
  "mute-command-args": "[@nomeutente]",
  "mute-command-description": "Silenzia un utente",
  "network error": "errore di rete",
  New: New$6,
  "New Messages!": "Nuovi messaggi!",
  "Next image": "Immagine successiva",
  "No chats here yet…": "Non ci sono ancora messaggi qui...",
  "No conversations yet": "Ancora nessuna conversazione",
  "No items exist": "Nessun elemento presente",
  "No results found": "Nessun risultato trovato",
  "Nobody will be able to vote in this poll anymore.": "Nessuno potrà più votare in questo sondaggio.",
  "Nothing yet...": "Ancora niente...",
  Offline: Offline$6,
  Ok: Ok$6,
  Online: Online$6,
  "Only numbers are allowed": "Sono consentiti solo numeri",
  "Only visible to you": "Visibile solo per te",
  "Open emoji picker": "Apri il selettore di emoji",
  "Open gallery at image {{ index }}": "Apri la galleria all'immagine {{ index }}",
  "Open image in gallery": "Apri immagine nella galleria",
  "Option already exists": "L'opzione esiste già",
  "Option is empty": "L'opzione è vuota",
  Options: Options$6,
  Original: Original$6,
  "People matching": "Persone che corrispondono",
  Photo: Photo$6,
  Pin: Pin$6,
  "Pinned by {{ name }}": "Appuntato da {{ name }}",
  "Pinned by You": "Fissato da te",
  "placeholder/PollComment": "Il tuo commento",
  "placeholder/PollOptionSuggestion": "Inserisci una nuova opzione",
  "Play video": "Riproduci video",
  Poll: Poll$7,
  "Poll comments": "Commenti del sondaggio",
  "Poll ended": "Sondaggio terminato",
  "Poll options": "Opzioni del sondaggio",
  "Poll results": "Risultati del sondaggio",
  "Previous image": "Immagine precedente",
  Question: Question$6,
  "Question {{ optionOrderNumber}}": "Domanda {{ optionOrderNumber}}",
  "Question is required": "La domanda è obbligatoria",
  "Quote Reply": "Rispondi con citazione",
  "Reached the vote limit. Remove an existing vote first.": "Raggiunto il limite di voti. Rimuovi prima un voto esistente.",
  "Recording format is not supported and cannot be reproduced": "Il formato di registrazione non è supportato e non può essere riprodotto",
  "Remind me": "Promemoria",
  "Remind Me": "Ricordami",
  "Reminder set": "Promemoria impostato",
  "Remove reminder": "Rimuovi promemoria",
  "Remove save for later": "Rimuovi Salva per dopo",
  "Replied to a thread": "Ha risposto in un thread",
  Reply: Reply$6,
  "Reply to {{ authorName }}": "Rispondi a {{ authorName }}",
  "Reply to a message to start a thread": "Rispondi a un messaggio per avviare un thread",
  "Reply to Message": "Rispondi al messaggio",
  replyCount_one: replyCount_one$6,
  replyCount_many: replyCount_many$2,
  replyCount_other: replyCount_other$6,
  Resend: Resend$6,
  "Retry upload": "Riprova caricamento",
  "Save for later": "Salva per dopo",
  "Saved for later": "Salvato per dopo",
  Search: Search$7,
  "Search GIFs": "Cerca GIF",
  "search-results-header-filter-source-button-label--channels": "canali",
  "search-results-header-filter-source-button-label--messages": "messaggi",
  "search-results-header-filter-source-button-label--users": "utenti",
  "Searching for {{ searchSourceType }}...": "Ricerca di {{ searchSourceType }}...",
  "Searching...": "Ricerca in corso...",
  searchResultsCount_one: searchResultsCount_one$6,
  searchResultsCount_many: searchResultsCount_many$2,
  searchResultsCount_other: searchResultsCount_other$6,
  "Select a thread to continue the conversation": "Seleziona un thread per continuare la conversazione",
  "Select more than one option": "Seleziona più di un'opzione",
  "Select one": "Seleziona uno",
  "Select one or more": "Seleziona uno o più",
  "Select up to {{count}}_one": "Seleziona fino a {{count}}",
  "Select up to {{count}}_many": "Seleziona fino a {{count}}",
  "Select up to {{count}}_other": "Seleziona fino a {{count}}",
  Send: Send$6,
  "Send a message to start the conversation": "Invia un messaggio per iniziare la conversazione",
  "Send Anyway": "Invia comunque",
  "Send message request failed": "Richiesta di invio messaggio non riuscita",
  "Send poll": "Invia sondaggio",
  "Sending...": "Invio in corso...",
  Sent: Sent$6,
  Share: Share$6,
  "Share live location for": "Condividi posizione live per",
  "Share Location": "Condividi posizione",
  "Shared live location": "Posizione live condivisa",
  "Shared location": "Posizione condivisa",
  Shuffle: Shuffle$6,
  "size limit": "limite di dimensione",
  "Slow Mode ON": "Modalità lenta attivata",
  "Slow mode, wait {{ seconds }}s...": "Modalità lenta, attendi {{ seconds }} s...",
  "Some of the files will not be accepted": "Alcuni dei file non saranno accettati",
  "Start typing to search": "Inizia a digitare per cercare",
  "Stop sharing": "Ferma condivisione",
  Submit: Submit$6,
  "Suggest an option": "Suggerisci un'opzione",
  "Tap to remove": "Tocca per rimuovere",
  "Thinking...": "Pensando...",
  "this content could not be displayed": "questo contenuto non può essere mostrato",
  "This field cannot be empty or contain only spaces": "Questo campo non può essere vuoto o contenere solo spazi",
  "This message did not meet our content guidelines": "Questo messaggio non soddisfa le nostre linee guida sui contenuti",
  Thread: Thread$7,
  "Thread has not been found": "Discussione non trovata",
  "Thread reply": "Risposta nella discussione",
  "Thread Reply": "Risposta nella discussione",
  "ThreadListUnseenThreadsBanner/loading": "Caricamento...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} thread non letto",
  "ThreadListUnseenThreadsBanner/unreadThreads_many": "{{ count }} thread non letti",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} thread non letti",
  Threads: Threads$6,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Ieri]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Oggi]", "nextDay": "[Domani]", "lastDay": "[Ieri]", "nextWeek": "dddd", "lastWeek": "[Scorsa] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Oggi",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Ieri",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Per iniziare a registrare, consenti l'accesso alla fotocamera nel tuo browser",
  "To start recording, allow the microphone access in your browser": "Per iniziare a registrare, consenti l'accesso al microfono nel tuo browser",
  totalVoteCount_one: totalVoteCount_one$4,
  totalVoteCount_many: totalVoteCount_many$2,
  totalVoteCount_other: totalVoteCount_other$6,
  Translated: Translated$6,
  "Translated from {{ language }}": "Tradotto da {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Digita un numero da 2 a 10",
  "Type your message": "Scrivi il tuo messaggio",
  Unarchive: Unarchive$6,
  "unban-command-args": "[@nomeutente]",
  "unban-command-description": "Togliere il divieto a un utente",
  "Unblock User": "Sblocca utente",
  "unknown error": "errore sconosciuto",
  Unmute: Unmute$6,
  "unmute-command-args": "[@nomeutente]",
  "unmute-command-description": "Togliere il silenzio a un utente",
  Unpin: Unpin$6,
  "Unread messages": "Messaggi non letti",
  "Unsupported attachment": "Allegato non supportato",
  "unsupported file type": "tipo di file non supportato",
  Update: Update$6,
  "Update your comment": "Aggiorna il tuo commento",
  "Upload blocked": "Caricamento bloccato",
  "Upload error": "Errore di caricamento",
  "Upload failed": "Caricamento non riuscito",
  'Upload type: "{{ type }}" is not allowed': 'Tipo di caricamento: "{{ type }}" non è consentito',
  "User blocked": "Utente bloccato",
  "User unblocked": "Utente sbloccato",
  "User uploaded content": "Contenuto caricato dall'utente",
  Video: Video$6,
  videoCount_one: videoCount_one$4,
  videoCount_many: videoCount_many$2,
  videoCount_other: videoCount_other$6,
  View: View$6,
  "View {{count}} comments_one": "Visualizza {{count}} commento",
  "View {{count}} comments_many": "Visualizza {{count}} commenti",
  "View {{count}} comments_other": "Visualizza {{count}} commenti",
  "View all": "Visualizza tutto",
  "View original": "Visualizza originale",
  "View results": "Vedi risultati",
  "View translation": "Visualizza traduzione",
  "Voice message": "Messaggio vocale",
  "Voice message {{ duration }}": "Messaggio vocale {{ duration }}",
  "Voice message deleted": "Messaggio vocale eliminato",
  voiceMessageCount_one: voiceMessageCount_one$4,
  voiceMessageCount_many: voiceMessageCount_many$2,
  voiceMessageCount_other: voiceMessageCount_other$6,
  "Vote ended": "Voto terminato",
  Votes: Votes$6,
  "Wait until all attachments have uploaded": "Attendi il caricamento di tutti gli allegati",
  "Waiting for network…": "In attesa della rete…",
  You: You$6,
  "You've reached the maximum number of files": "Hai raggiunto il numero massimo di file"
};
var Anonymous$5 = "匿名";
var Archive$5 = "アーカイブ";
var Attach$5 = "添付";
var Attachment$6 = "添付ファイル";
var Back$5 = "戻る";
var Cancel$5 = "キャンセル";
var Channels$5 = "チャンネル";
var Chats$5 = "チャット";
var Close$5 = "閉める";
var Commands$5 = "コマンド";
var Create$5 = "作成";
var Delete$5 = "消去";
var Delivered$5 = "配信しました";
var Edited$5 = "編集済み";
var End$5 = "終了";
var File$6 = "ファイル";
var fileCount_other$5 = "{{ count }}件のファイル";
var Flag$5 = "フラグ";
var Image$6 = "画像";
var imageCount_other$5 = "{{ count }}件の画像";
var Link$5 = "リンク";
var linkCount_other$5 = "{{ count }}件のリンク";
var live$5 = "ライブ";
var Location$5 = "位置情報";
var Menu$5 = "メニュー";
var Mute$5 = "無音";
var New$5 = "新しい";
var Offline$5 = "オフライン";
var Ok$5 = "OK";
var Online$5 = "オンライン";
var Options$5 = "オプション";
var Original$5 = "原文";
var Photo$5 = "写真";
var Pin$5 = "ピン";
var Poll$6 = "投票";
var Question$5 = "質問";
var Reply$5 = "返事";
var replyCount_one$5 = "1件の返信";
var replyCount_other$5 = "{{ count }} 返信";
var Resend$5 = "再送信";
var Search$6 = "探す";
var searchResultsCount_one$5 = "1件の結果";
var searchResultsCount_other$5 = "{{ count }}件の結果";
var Send$5 = "送信";
var Sent$5 = "送信済み";
var Share$5 = "共有";
var Shuffle$5 = "シャッフル";
var Submit$5 = "送信";
var Thread$6 = "スレッド";
var Threads$5 = "スレッド";
var totalVoteCount_other$5 = "合計{{ count }}票";
var Translated$5 = "翻訳済み";
var Unarchive$5 = "アーカイブ解除";
var Unmute$5 = "無音を解除する";
var Unpin$5 = "ピンを解除する";
var Update$5 = "更新";
var Video$5 = "動画";
var videoCount_other$5 = "{{ count }}件の動画";
var View$5 = "表示";
var voiceMessageCount_other$5 = "{{ count }}件のボイスメッセージ";
var Votes$5 = "投票";
var You$5 = "あなた";
var jaTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} と {{ moreCount }} 他人",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} と {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} ファイル",
  "{{ count }} files_other": "{{ count }} ファイル",
  "{{ count }} people are typing_one": "{{ count }}人が入力中です",
  "{{ count }} people are typing_many": "{{ count }}人が入力中です",
  "{{ count }} people are typing_other": "{{ count }}人が入力中です",
  "{{ count }} photos_one": "{{ count }} 写真",
  "{{ count }} photos_other": "{{ count }} 写真",
  "{{ count }} reactions_other": "{{ count }}件のリアクション",
  "{{ count }} videos_one": "{{ count }} 動画",
  "{{ count }} videos_other": "{{ count }} 動画",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} と {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} イメージ",
  "{{ memberCount }} members": "{{ memberCount }} メンバー",
  "{{ typing }} are typing": "{{ typing }}が入力中です",
  "{{ typing }} is typing": "{{ typing }}が入力中です",
  "{{ user }} has been muted": "{{ user }} 無音されています",
  "{{ user }} has been unmuted": "{{ user }} 無音されていません",
  "{{ user }} is typing...": "{{ user }} が入力中...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} と {{ user }} が入力中...",
  "{{ users }} and more are typing...": "{{ users }} とその他が入力中...",
  "{{ watcherCount }} online": "{{ watcherCount }} オンライン",
  "{{count}} new messages_one": "{{count}}件の新しいメッセージ",
  "{{count}} new messages_other": "{{count}}件の新しいメッセージ",
  "{{count}} unread_one": "{{count}} 未読",
  "{{count}} unread_other": "{{count}} 未読",
  "{{count}} votes_one": "{{count}} 票",
  "{{count}} votes_other": "{{count}} 票",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "あと{{count}}件のオプション",
  "+{{count}} more options_other": "あと{{count}}件のオプション",
  "🏙 Attachment...": "🏙 アタッチメント...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} が作成: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} が投票: {{pollOptionText}}",
  "📍Shared location": "📍共有された位置情報",
  "Add a comment": "コメントを追加",
  "Add an option": "オプションを追加",
  "Add reaction": "リアクションを追加",
  "All results loaded": "すべての結果が読み込まれました",
  "Allow access to camera": "カメラへのアクセスを許可する",
  "Allow access to microphone": "マイクロフォンへのアクセスを許可する",
  "Allow comments": "コメントを許可",
  "Allow option suggestion": "オプションの提案を許可",
  "Allow others to add comments": "他の人にコメントを追加することを許可する",
  "Also send as a direct message": "ダイレクトメッセージとしても送信",
  "Also send in channel": "チャンネルにも送信",
  "Also sent in channel": "チャンネルにも送信済み",
  "An error has occurred during recording": "録音中にエラーが発生しました",
  "An error has occurred during the recording processing": "録音処理中にエラーが発生しました",
  Anonymous: Anonymous$5,
  "Anonymous poll": "匿名投票",
  Archive: Archive$5,
  "Are you sure you want to delete this message?": "このメッセージを削除してもよろしいですか？",
  "aria/Attachment": "添付ファイル",
  "aria/Block User": "ユーザーをブロック",
  "aria/Bookmark Message": "メッセージをブックマーク",
  "aria/Cancel Reply": "返信をキャンセル",
  "aria/Cancel upload": "アップロードをキャンセル",
  "aria/Channel list": "チャンネル一覧",
  "aria/Channel search results": "チャンネル検索結果",
  "aria/Close thread": "スレッドを閉じる",
  "aria/Collapse sidebar": "サイドバーを折りたたむ",
  "aria/Copy Message Text": "メッセージテキストをコピー",
  "aria/Delete Message": "メッセージを削除",
  "aria/Download attachment": "添付ファイルをダウンロード",
  "aria/Edit Message": "メッセージを編集",
  "aria/Emoji picker": "絵文字ピッカー",
  "aria/Expand sidebar": "サイドバーを展開",
  "aria/File input": "ファイル入力",
  "aria/File upload": "ファイルアップロード",
  "aria/Flag Message": "メッセージをフラグ",
  "aria/Image failed to load": "画像の読み込みに失敗しました",
  "aria/Image input": "画像入力",
  "aria/Load More Channels": "さらにチャンネルを読み込む",
  "aria/Mark Message Unread": "未読としてマーク",
  "aria/Menu": "メニュー",
  "aria/Message Options": "メッセージオプション",
  "aria/Mute User": "ユーザーをミュート",
  "aria/Open Attachment Selector": "添付ファイル選択を開く",
  "aria/Open Menu": "メニューを開く",
  "aria/Open Message Actions Menu": "メッセージアクションメニューを開く",
  "aria/Open Reaction Selector": "リアクションセレクターを開く",
  "aria/Open Thread": "スレッドを開く",
  "aria/Percent complete": "{{percent}}パーセント完了",
  "aria/Pin Message": "メッセージをピン",
  "aria/Quote Message": "メッセージを引用",
  "aria/Reaction list": "リアクション一覧",
  "aria/Remind Me Message": "リマインダー",
  "aria/Remind Me Options": "リマインダーオプション",
  "aria/Remove attachment": "添付ファイルを削除",
  "aria/Remove location attachment": "位置情報の添付ファイルを削除",
  "aria/Remove Reminder": "リマインダーを削除",
  "aria/Remove Save For Later": "「後で見る」を削除",
  "aria/Resend Message": "メッセージを再送信",
  "aria/Retry upload": "アップロードを再試行",
  "aria/Search results": "検索結果",
  "aria/Search results header filter button": "検索結果ヘッダーフィルターボタン",
  "aria/Send": "送信",
  "aria/Show preview": "プレビューを表示",
  "aria/Stop AI Generation": "AI生成を停止",
  "aria/Unblock User": "ユーザーのブロックを解除",
  "aria/Unmute User": "無音を解除する",
  "aria/Unpin Message": "ピンを解除",
  "Ask a question": "質問する",
  Attach: Attach$5,
  "Attach files": "ファイルを添付する",
  Attachment: Attachment$6,
  "Attachment upload blocked due to {{reason}}": "{{reason}}のため添付ファイルのアップロードがブロックされました",
  "Attachment upload failed due to {{reason}}": "{{reason}}のため添付ファイルのアップロードに失敗しました",
  Back: Back$5,
  "ban-command-args": "[@ユーザ名] [テキスト]",
  "ban-command-description": "ユーザーを禁止する",
  "Block User": "ユーザーをブロック",
  Cancel: Cancel$5,
  "Cannot seek in the recording": "録音中にシークできません",
  "Channel archived": "チャンネルをアーカイブしました",
  "Channel Missing": "チャネルがありません",
  "Channel muted": "チャンネルをミュートしました",
  "Channel pinned": "チャンネルをピン留めしました",
  "Channel unarchived": "チャンネルのアーカイブを解除しました",
  "Channel unmuted": "チャンネルのミュートを解除しました",
  "Channel unpinned": "チャンネルのピン留めを解除しました",
  Channels: Channels$5,
  Chats: Chats$5,
  "Choose between 2 to 10 options": "2〜10の選択肢から選ぶ",
  Close: Close$5,
  "Close emoji picker": "絵文字ピッカーを閉める",
  Commands: Commands$5,
  "Commands matching": "一致するコマンド",
  "Connection failure, reconnecting now...": "接続が失敗しました。再接続中...",
  "Copy Message": "メッセージをコピー",
  Create: Create$5,
  "Create poll": "投票を作成",
  "Current location": "現在の位置",
  Delete: Delete$5,
  "Delete for me": "自分用に削除",
  "Delete message": "メッセージを削除",
  Delivered: Delivered$5,
  "Direct message": "ダイレクトメッセージ",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "このアンケートを今終了しますか？終了すると、誰も投票できなくなります。",
  "Download All": "すべてダウンロード",
  "Download Attachment": "添付ファイルをダウンロード",
  "Download attachment {{ name }}": "添付ファイル {{ name }} をダウンロード",
  "Drag your files here": "ここにファイルをドラッグ",
  "Drag your files here to add to your post": "投稿に追加するためにここにファイルをドラッグ",
  "Due {{ timeLeft }}": "{{ timeLeft }}に期限切れ",
  "Due since {{ dueSince }}": "{{ dueSince }}から期限切れ",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "メッセージを編集",
  "Edit message request failed": "メッセージの編集要求が失敗しました",
  Edited: Edited$5,
  "Emoji matching": "絵文字マッチング",
  "Empty message...": "空のメッセージ...",
  End: End$5,
  "End poll": "アンケートを終了",
  "End this poll?": "このアンケートを終了しますか？",
  "End vote": "投票を終了",
  "Enforce unique vote is enabled": "一意の投票が有効になっています",
  "Error": "エラー",
  "Error adding flag": "フラグを追加のエラーが発生しました",
  "Error connecting to chat, refresh the page to try again.": "チャットへの接続ができませんでした。ページを更新してください。",
  "Error deleting message": "メッセージを削除するエラーが発生しました",
  "Error fetching reactions": "反応の読み込みエラー",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "メッセージを未読にする際にエラーが発生しました。最新の100件のチャンネルメッセージより古い未読メッセージはマークできません。",
  "Error muting a user ...": "ユーザーを無音するエラーが発生しました...",
  "Error pinning message": "メッセージをピンのエラーが発生しました",
  "Error removing message pin": "メッセージのピンを削除のエラーが発生しました",
  "Error reproducing the recording": "録音の再生中にエラーが発生しました",
  "Error starting recording": "録音の開始時にエラーが発生しました",
  "Error unmuting a user ...": "ユーザーの無音解除のエラーが発生しました...",
  "Error uploading attachment": "添付ファイルのアップロード中にエラーが発生しました",
  "Error uploading file": "ファイルをアップロードのエラーが発生しました",
  "Error uploading image": "画像をアップロードのエラーが発生しました",
  "Error: {{ errorMessage }}": "エラー: {{ errorMessage }}",
  "Failed to block user": "ユーザーのブロックに失敗しました",
  "Failed to create the poll": "投票の作成に失敗しました",
  "Failed to create the poll due to {{reason}}": "{{reason}} のため投票の作成に失敗しました",
  "Failed to delete the message": "メッセージの削除に失敗しました",
  "Failed to end the poll": "アンケートの終了に失敗しました",
  "Failed to end the poll due to {{reason}}": "{{reason}}のためアンケートの終了に失敗しました",
  "Failed to jump to the first unread message": "最初の未読メッセージにジャンプできませんでした",
  "Failed to leave channel": "チャンネルの退出に失敗しました",
  "Failed to mark channel as read": "チャンネルを既読にすることができませんでした",
  "Failed to play the recording": "録音の再生に失敗しました",
  "Failed to retrieve location": "位置情報の取得に失敗しました",
  "Failed to share location": "位置情報の共有に失敗しました",
  "Failed to update channel archive status": "チャンネルのアーカイブ状態の更新に失敗しました",
  "Failed to update channel mute status": "チャンネルのミュート状態の更新に失敗しました",
  "Failed to update channel pinned status": "チャンネルのピン状態の更新に失敗しました",
  File: File$6,
  "File is required for upload attachment": "添付ファイルのアップロードにはファイルが必要です",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "ファイルが大きすぎます：{{ size }}、最大アップロードサイズは{{ limit }}です",
  "File too large": "ファイルが大きすぎます",
  fileCount_other: fileCount_other$5,
  Flag: Flag$5,
  "Generating...": "生成中...",
  "giphy-command-args": "[テキスト]",
  "giphy-command-description": "チャンネルにランダムなGIFを投稿する",
  "Hide who voted": "誰が投票したかを非表示にする",
  Image: Image$6,
  imageCount_other: imageCount_other$5,
  "Instant commands": "インスタントコマンド",
  "language/af": "アフリカース語",
  "language/am": "アムハラ語",
  "language/ar": "アラビア語",
  "language/az": "アゼルバイジャン語",
  "language/bg": "ブルガリア語",
  "language/bn": "ベンガル語",
  "language/bs": "ボスニア語",
  "language/cs": "チェコ語",
  "language/da": "デンマーク語",
  "language/de": "ドイツ語",
  "language/el": "ギリシャ語",
  "language/en": "英語",
  "language/es": "スペイン語",
  "language/es-MX": "スペイン語（メキシコ）",
  "language/et": "エストニア語",
  "language/fa": "ペルシア語",
  "language/fa-AF": "ダリー語",
  "language/fi": "フィンランド語",
  "language/fr": "フランス語",
  "language/fr-CA": "フランス語（カナダ）",
  "language/ha": "ハウサ語",
  "language/he": "ヘブライ語",
  "language/hi": "ヒンディー語",
  "language/hr": "クロアチア語",
  "language/ht": "ハイチ・クレオール語",
  "language/hu": "ハンガリー語",
  "language/id": "インドネシア語",
  "language/it": "イタリア語",
  "language/ja": "日本語",
  "language/ka": "グルジア語",
  "language/ko": "韓国語",
  "language/lt": "リトアニア語",
  "language/lv": "ラトビア語",
  "language/ms": "マレー語",
  "language/nl": "オランダ語",
  "language/no": "ノルウェー語",
  "language/pl": "ポーランド語",
  "language/ps": "パシュトー語",
  "language/pt": "ポルトガル語",
  "language/ro": "ルーマニア語",
  "language/ru": "ロシア語",
  "language/sk": "スロバキア語",
  "language/sl": "スロベニア語",
  "language/so": "ソマリ語",
  "language/sq": "アルバニア語",
  "language/sr": "セルビア語",
  "language/sv": "スウェーデン語",
  "language/sw": "スワヒリ語",
  "language/ta": "タミル語",
  "language/th": "タイ語",
  "language/tl": "タガログ語",
  "language/tr": "トルコ語",
  "language/uk": "ウクライナ語",
  "language/ur": "ウルドゥー語",
  "language/vi": "ベトナム語",
  "language/zh": "中国語（簡体字）",
  "language/zh-TW": "中国語（繁体字）",
  "Leave Channel": "チャンネルを退出",
  "Left channel": "チャンネルを退出しました",
  "Let others add options": "他の人が選択肢を追加できるようにする",
  "Limit votes per person": "1人あたりの投票数を制限する",
  Link: Link$5,
  linkCount_other: linkCount_other$5,
  live: live$5,
  "Live for {{duration}}": "{{duration}}間ライブ",
  "Live location": "ライブ位置情報",
  "Live until {{ timestamp }}": "{{ timestamp }}までライブ",
  "Load more": "もっと読み込む",
  "Local upload attachment missing local id": "ローカルアップロード添付にローカルIDがありません",
  Location: Location$5,
  "Location sharing ended": "位置情報の共有が終了しました",
  "Location: {{ coordinates }}": "位置: {{ coordinates }}",
  "Mark as unread": "未読としてマーク",
  "Maximum number of votes (from 2 to 10)": "最大投票数（2から10まで）",
  Menu: Menu$5,
  "Message deleted": "メッセージが削除されました",
  "Message failed to send": "メッセージの送信に失敗しました",
  "Message has been successfully flagged": "メッセージに正常にフラグが付けられました",
  "Message marked as unread": "メッセージを未読にしました",
  "Message pinned": "メッセージにピンが付けられました",
  "Message unpinned": "メッセージのピン留めを解除しました",
  "Message was blocked by moderation policies": "メッセージはモデレーションポリシーによってブロックされました",
  "Messages have been marked unread.": "メッセージは未読としてマークされました。",
  "Missing permissions to upload the attachment": "添付ファイルをアップロードするための許可がありません",
  "Multiple votes": "複数投票",
  Mute: Mute$5,
  "mute-command-args": "[@ユーザ名]",
  "mute-command-description": "ユーザーをミュートする",
  "network error": "ネットワークエラー",
  New: New$5,
  "New Messages!": "新しいメッセージ!",
  "Next image": "次の画像",
  "No chats here yet…": "ここにはまだチャットはありません…",
  "No conversations yet": "まだ会話はありません",
  "No items exist": "項目がありません",
  "No results found": "結果が見つかりません",
  "Nobody will be able to vote in this poll anymore.": "この投票では、誰も投票できなくなります。",
  "Nothing yet...": "まだ何もありません...",
  Offline: Offline$5,
  Ok: Ok$5,
  Online: Online$5,
  "Only numbers are allowed": "数字のみ許可されています",
  "Only visible to you": "あなただけに表示",
  "Open emoji picker": "絵文字ピッカーを開く",
  "Open gallery at image {{ index }}": "画像 {{ index }} でギャラリーを開く",
  "Open image in gallery": "画像をギャラリーで開く",
  "Option already exists": "オプションは既に存在します",
  "Option is empty": "オプションが空です",
  Options: Options$5,
  Original: Original$5,
  "People matching": "一致する人",
  Photo: Photo$5,
  Pin: Pin$5,
  "Pinned by {{ name }}": "{{ name }}がピンしました",
  "Pinned by You": "あなたがピン留めしました",
  "placeholder/PollComment": "コメント",
  "placeholder/PollOptionSuggestion": "新しい選択肢を入力",
  "Play video": "動画を再生",
  Poll: Poll$6,
  "Poll comments": "投票コメント",
  "Poll ended": "アンケート終了",
  "Poll options": "投票オプション",
  "Poll results": "投票結果",
  "Previous image": "前の画像",
  Question: Question$5,
  "Question {{ optionOrderNumber}}": "質問 {{ optionOrderNumber}}",
  "Question is required": "質問は必須です",
  "Quote Reply": "引用返信",
  "Reached the vote limit. Remove an existing vote first.": "投票制限に達しました。既存の投票を先に削除してください。",
  "Recording format is not supported and cannot be reproduced": "録音形式はサポートされておらず、再生できません",
  "Remind me": "リマインド",
  "Remind Me": "リマインダー",
  "Reminder set": "リマインダーを設定しました",
  "Remove reminder": "リマインダーを削除",
  "Remove save for later": "「後で見る」を削除",
  "Replied to a thread": "スレッドに返信しました",
  Reply: Reply$5,
  "Reply to {{ authorName }}": "{{ authorName }} に返信",
  "Reply to a message to start a thread": "メッセージに返信してスレッドを開始してください",
  "Reply to Message": "メッセージに返信",
  replyCount_one: replyCount_one$5,
  replyCount_other: replyCount_other$5,
  Resend: Resend$5,
  "Retry upload": "アップロードを再試行",
  "Save for later": "後で保存",
  "Saved for later": "後で保存済み",
  Search: Search$6,
  "Search GIFs": "GIFを検索",
  "search-results-header-filter-source-button-label--channels": "チャンネル",
  "search-results-header-filter-source-button-label--messages": "メッセージ",
  "search-results-header-filter-source-button-label--users": "ユーザー",
  "Searching for {{ searchSourceType }}...": "{{ searchSourceType }}を検索中...",
  "Searching...": "検索中...",
  searchResultsCount_one: searchResultsCount_one$5,
  searchResultsCount_other: searchResultsCount_other$5,
  "Select a thread to continue the conversation": "会話を続けるにはスレッドを選択してください",
  "Select more than one option": "複数の選択肢を選ぶ",
  "Select one": "1つ選択",
  "Select one or more": "1つ以上選択",
  "Select up to {{count}}_one": "最大{{count}}まで選択",
  "Select up to {{count}}_other": "最大{{count}}まで選択",
  Send: Send$5,
  "Send a message to start the conversation": "メッセージを送って会話を始めましょう",
  "Send Anyway": "とにかく送信する",
  "Send message request failed": "メッセージ送信リクエストが失敗しました",
  "Send poll": "アンケートを送信",
  "Sending...": "送信中...",
  Sent: Sent$5,
  Share: Share$5,
  "Share live location for": "ライブ位置情報を共有",
  "Share Location": "位置情報を共有",
  "Shared live location": "共有されたライブ位置情報",
  "Shared location": "共有された位置情報",
  Shuffle: Shuffle$5,
  "size limit": "サイズ制限",
  "Slow Mode ON": "スローモードオン",
  "Slow mode, wait {{ seconds }}s...": "スローモード、{{ seconds }}秒お待ちください...",
  "Some of the files will not be accepted": "一部のファイルは受け付けられません",
  "Start typing to search": "検索するには入力を開始してください",
  "Stop sharing": "共有を停止",
  Submit: Submit$5,
  "Suggest an option": "オプションを提案",
  "Tap to remove": "タップして削除",
  "Thinking...": "考え中...",
  "this content could not be displayed": "このコンテンツは表示できませんでした",
  "This field cannot be empty or contain only spaces": "このフィールドは空にすることはできません。また、空白文字のみを含むこともできません",
  "This message did not meet our content guidelines": "このメッセージはコンテンツガイドラインに適合していません",
  Thread: Thread$6,
  "Thread has not been found": "スレッドが見つかりませんでした",
  "Thread reply": "スレッドの返信",
  "Thread Reply": "スレッドの返信",
  "ThreadListUnseenThreadsBanner/loading": "読み込み中...",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }}件の未読スレッド",
  Threads: Threads$5,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[昨日]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[今日]", "nextDay": "[明日]", "lastDay": "[昨日]", "nextWeek": "dddd", "lastWeek": "[先週の] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "今日",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "昨日",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "録音を開始するには、ブラウザーでカメラへのアクセスを許可してください",
  "To start recording, allow the microphone access in your browser": "録音を開始するには、ブラウザーでマイクロフォンへのアクセスを許可してください",
  totalVoteCount_other: totalVoteCount_other$5,
  Translated: Translated$5,
  "Translated from {{ language }}": "{{ language }}から翻訳",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "2から10までの数字を入力してください",
  "Type your message": "メッセージを入力してください",
  Unarchive: Unarchive$5,
  "unban-command-args": "[@ユーザ名]",
  "unban-command-description": "ユーザーの禁止を解除する",
  "Unblock User": "ユーザーのブロックを解除",
  "unknown error": "不明なエラー",
  Unmute: Unmute$5,
  "unmute-command-args": "[@ユーザ名]",
  "unmute-command-description": "ユーザーのミュートを解除する",
  Unpin: Unpin$5,
  "Unread messages": "未読メッセージ",
  "Unsupported attachment": "サポートされていない添付ファイル",
  "unsupported file type": "サポートされていないファイル形式",
  Update: Update$5,
  "Update your comment": "コメントを更新",
  "Upload blocked": "アップロードがブロックされました",
  "Upload error": "アップロードエラー",
  "Upload failed": "アップロードに失敗しました",
  'Upload type: "{{ type }}" is not allowed': 'アップロードタイプ："{{ type }}"は許可されていません',
  "User blocked": "ユーザーをブロックしました",
  "User unblocked": "ユーザーのブロックを解除しました",
  "User uploaded content": "ユーザーがアップロードしたコンテンツ",
  Video: Video$5,
  videoCount_other: videoCount_other$5,
  View: View$5,
  "View {{count}} comments_one": "{{count}} コメントを表示",
  "View {{count}} comments_other": "{{count}} コメントを表示",
  "View all": "すべて表示",
  "View original": "原文を表示",
  "View results": "結果を表示",
  "View translation": "翻訳を表示",
  "Voice message": "ボイスメッセージ",
  "Voice message {{ duration }}": "ボイスメッセージ {{ duration }}",
  "Voice message deleted": "ボイスメッセージが削除されました",
  voiceMessageCount_other: voiceMessageCount_other$5,
  "Vote ended": "投票が終了しました",
  Votes: Votes$5,
  "Wait until all attachments have uploaded": "すべての添付ファイルがアップロードされるまでお待ちください",
  "Waiting for network…": "ネットワークを待機中…",
  You: You$5,
  "You've reached the maximum number of files": "ファイルの最大数に達しました"
};
var Anonymous$4 = "익명";
var Archive$4 = "아카이브";
var Attach$4 = "첨부";
var Attachment$5 = "첨부 파일";
var Back$4 = "뒤로";
var Cancel$4 = "취소";
var Channels$4 = "채널";
var Chats$4 = "채팅";
var Close$4 = "닫기";
var Commands$4 = "명령어";
var Create$4 = "생성";
var Delete$4 = "삭제";
var Delivered$4 = "배달됨";
var Edited$4 = "편집됨";
var End$4 = "종료";
var File$5 = "파일";
var fileCount_other$4 = "파일 {{ count }}개";
var Flag$4 = "플래그";
var Image$5 = "이미지";
var imageCount_other$4 = "이미지 {{ count }}개";
var Link$4 = "링크";
var linkCount_other$4 = "링크 {{ count }}개";
var live$4 = "라이브";
var Location$4 = "위치";
var Menu$4 = "메뉴";
var Mute$4 = "무음";
var New$4 = "새로운";
var Offline$4 = "오프라인";
var Ok$4 = "확인";
var Online$4 = "온라인";
var Options$4 = "옵션";
var Original$4 = "원문";
var Photo$4 = "사진";
var Pin$4 = "핀";
var Poll$5 = "투표";
var Question$4 = "질문";
var Reply$4 = "답장";
var replyCount_one$4 = "답장 1개";
var replyCount_other$4 = "{{ count }} 답장";
var Resend$4 = "다시 보내기";
var Search$5 = "찾다";
var searchResultsCount_one$4 = "1개의 결과";
var searchResultsCount_other$4 = "{{ count }}개 결과";
var Send$4 = "보내다";
var Sent$4 = "전송됨";
var Share$4 = "공유";
var Shuffle$4 = "셔플";
var Submit$4 = "제출";
var Thread$5 = "스레드";
var Threads$4 = "스레드";
var totalVoteCount_other$4 = "총 {{ count }}표";
var Translated$4 = "번역됨";
var Unarchive$4 = "아카이브 해제";
var Unmute$4 = "음소거 해제";
var Unpin$4 = "핀 해제";
var Update$4 = "업데이트";
var Video$4 = "동영상";
var videoCount_other$4 = "동영상 {{ count }}개";
var View$4 = "보기";
var voiceMessageCount_other$4 = "음성 메시지 {{ count }}개";
var Votes$4 = "투표";
var You$4 = "당신";
var koTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} 그리고 {{ moreCount }}명 더",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} 그리고 {{ lastUser }}",
  "{{ count }} files_one": "{{ count }}개 파일",
  "{{ count }} files_other": "{{ count }}개 파일",
  "{{ count }} people are typing_one": "{{ count }}명이 입력 중입니다",
  "{{ count }} people are typing_many": "{{ count }}명이 입력 중입니다",
  "{{ count }} people are typing_other": "{{ count }}명이 입력 중입니다",
  "{{ count }} photos_one": "{{ count }}개 사진",
  "{{ count }} photos_other": "{{ count }}개 사진",
  "{{ count }} reactions_other": "{{ count }}개 반응",
  "{{ count }} videos_one": "{{ count }}개 동영상",
  "{{ count }} videos_other": "{{ count }}개 동영상",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} 그리고 {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }}개 더",
  "{{ memberCount }} members": "{{ memberCount }}명",
  "{{ typing }} are typing": "{{ typing }} 입력 중입니다",
  "{{ typing }} is typing": "{{ typing }} 입력 중입니다",
  "{{ user }} has been muted": "{{ user }} 음소거되었습니다",
  "{{ user }} has been unmuted": "{{ user }} 음소거가 해제되었습니다",
  "{{ user }} is typing...": "{{ user }}이(가) 입력 중입니다...",
  "{{ users }} and {{ user }} are typing...": "{{ users }}와(과) {{ user }}이(가) 입력 중입니다...",
  "{{ users }} and more are typing...": "{{ users }}와(과) 더 많은 사람들이 입력 중입니다...",
  "{{ watcherCount }} online": "{{ watcherCount }} 온라인",
  "{{count}} new messages_one": "{{count}}개의 새 메시지",
  "{{count}} new messages_other": "{{count}}개의 새 메시지",
  "{{count}} unread_one": "{{count}} 읽지 않음",
  "{{count}} unread_other": "{{count}} 읽지 않음",
  "{{count}} votes_one": "{{count}} 투표",
  "{{count}} votes_other": "{{count}} 투표",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "옵션 {{count}}개 더",
  "+{{count}} more options_other": "옵션 {{count}}개 더",
  "🏙 Attachment...": "🏙 부착...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}}이(가) 생성함: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}}이(가) 투표함: {{pollOptionText}}",
  "📍Shared location": "📍공유된 위치",
  "Add a comment": "댓글 추가",
  "Add an option": "옵션 추가",
  "Add reaction": "반응 추가",
  "All results loaded": "모든 결과가 로드되었습니다",
  "Allow access to camera": "카메라에 대한 액세스 허용",
  "Allow access to microphone": "마이크로폰에 대한 액세스 허용",
  "Allow comments": "댓글 허용",
  "Allow option suggestion": "옵션 제안 허용",
  "Allow others to add comments": "다른 사람이 댓글을 추가할 수 있도록 허용",
  "Also send as a direct message": "다이렉트 메시지로도 보내기",
  "Also send in channel": "채널에도 보내기",
  "Also sent in channel": "채널에도 전송됨",
  "An error has occurred during recording": "녹음 중 오류가 발생했습니다",
  "An error has occurred during the recording processing": "녹음 처리 중 오류가 발생했습니다",
  Anonymous: Anonymous$4,
  "Anonymous poll": "익명 투표",
  Archive: Archive$4,
  "Are you sure you want to delete this message?": "이 메시지를 삭제하시겠습니까?",
  "aria/Attachment": "첨부 파일",
  "aria/Block User": "사용자 차단",
  "aria/Bookmark Message": "메시지 북마크",
  "aria/Cancel Reply": "답장 취소",
  "aria/Cancel upload": "업로드 취소",
  "aria/Channel list": "채널 목록",
  "aria/Channel search results": "채널 검색 결과",
  "aria/Close thread": "스레드 닫기",
  "aria/Collapse sidebar": "사이드바 접기",
  "aria/Copy Message Text": "메시지 텍스트 복사",
  "aria/Delete Message": "메시지 삭제",
  "aria/Download attachment": "첨부 파일 다운로드",
  "aria/Edit Message": "메시지 수정",
  "aria/Emoji picker": "이모지 선택기",
  "aria/Expand sidebar": "사이드바 확장",
  "aria/File input": "파일 입력",
  "aria/File upload": "파일 업로드",
  "aria/Flag Message": "메시지 신고",
  "aria/Image failed to load": "이미지를 불러오지 못했습니다",
  "aria/Image input": "이미지 입력",
  "aria/Load More Channels": "더 많은 채널 불러오기",
  "aria/Mark Message Unread": "읽지 않음으로 표시",
  "aria/Menu": "메뉴",
  "aria/Message Options": "메시지 옵션",
  "aria/Mute User": "사용자 음소거",
  "aria/Open Attachment Selector": "첨부 파일 선택기 열기",
  "aria/Open Menu": "메뉴 열기",
  "aria/Open Message Actions Menu": "메시지 액션 메뉴 열기",
  "aria/Open Reaction Selector": "반응 선택기 열기",
  "aria/Open Thread": "스레드 열기",
  "aria/Percent complete": "{{percent}}퍼센트 완료",
  "aria/Pin Message": "메시지 고정",
  "aria/Quote Message": "메시지 인용",
  "aria/Reaction list": "반응 목록",
  "aria/Remind Me Message": "알림 설정",
  "aria/Remind Me Options": "알림 옵션",
  "aria/Remove attachment": "첨부 파일 제거",
  "aria/Remove location attachment": "위치 첨부 파일 제거",
  "aria/Remove Reminder": "알림 제거",
  "aria/Remove Save For Later": "나중에 보기 제거",
  "aria/Resend Message": "메시지 다시 보내기",
  "aria/Retry upload": "업로드 다시 시도",
  "aria/Search results": "검색 결과",
  "aria/Search results header filter button": "검색 결과 헤더 필터 버튼",
  "aria/Send": "보내기",
  "aria/Show preview": "미리보기 표시",
  "aria/Stop AI Generation": "AI 생성 중지",
  "aria/Unblock User": "사용자 차단 해제",
  "aria/Unmute User": "음소거 해제",
  "aria/Unpin Message": "핀 해제",
  "Ask a question": "질문하기",
  Attach: Attach$4,
  "Attach files": "파일 첨부",
  Attachment: Attachment$5,
  "Attachment upload blocked due to {{reason}}": "{{reason}}로 인해 첨부 파일 업로드가 차단되었습니다",
  "Attachment upload failed due to {{reason}}": "{{reason}}로 인해 첨부 파일 업로드가 실패했습니다",
  Back: Back$4,
  "ban-command-args": "[@사용자이름] [텍스트]",
  "ban-command-description": "사용자를 차단",
  "Block User": "사용자 차단",
  Cancel: Cancel$4,
  "Cannot seek in the recording": "녹음에서 찾을 수 없습니다",
  "Channel archived": "채널이 보관됨",
  "Channel Missing": "채널 누락",
  "Channel muted": "채널이 음소거됨",
  "Channel pinned": "채널이 고정됨",
  "Channel unarchived": "채널 보관이 해제됨",
  "Channel unmuted": "채널 음소거가 해제됨",
  "Channel unpinned": "채널 고정이 해제됨",
  Channels: Channels$4,
  Chats: Chats$4,
  "Choose between 2 to 10 options": "2~10개의 선택지 중에서 선택",
  Close: Close$4,
  "Close emoji picker": "이모티콘 선택기 닫기",
  Commands: Commands$4,
  "Commands matching": "일치하는 명령",
  "Connection failure, reconnecting now...": "연결 실패, 지금 다시 연결 중...",
  "Copy Message": "메시지 복사",
  Create: Create$4,
  "Create poll": "투표 생성",
  "Current location": "현재 위치",
  Delete: Delete$4,
  "Delete for me": "나만 삭제",
  "Delete message": "메시지 삭제",
  Delivered: Delivered$4,
  "Direct message": "다이렉트 메시지",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "지금 이 투표를 종료하시겠습니까? 종료 후에는 더 이상 투표할 수 없습니다.",
  "Download All": "모두 다운로드",
  "Download Attachment": "첨부 파일 다운로드",
  "Download attachment {{ name }}": "첨부 파일 {{ name }} 다운로드",
  "Drag your files here": "여기로 파일을 끌어다 놓으세요",
  "Drag your files here to add to your post": "게시물에 추가하려면 파일을 여기로 끌어다 놓으세요",
  "Due {{ timeLeft }}": "{{ timeLeft }}에 기한",
  "Due since {{ dueSince }}": "{{ dueSince }}부터 기한",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "메시지 수정",
  "Edit message request failed": "메시지 수정 요청 실패",
  Edited: Edited$4,
  "Emoji matching": "이모티콘 매칭",
  "Empty message...": "빈 메시지...",
  End: End$4,
  "End poll": "투표 종료",
  "End this poll?": "이 투표를 종료하시겠습니까?",
  "End vote": "투표 종료",
  "Enforce unique vote is enabled": "고유 투표가 활성화되었습니다",
  "Error": "오류",
  "Error adding flag": "플래그를 추가하는 동안 오류가 발생했습니다.",
  "Error connecting to chat, refresh the page to try again.": "채팅에 연결하는 동안 오류가 발생했습니다. 페이지를 새로고침하여 다시 시도하세요.",
  "Error deleting message": "메시지를 삭제하는 중에 오류가 발생했습니다.",
  "Error fetching reactions": "반응 로딩 오류.",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "메시지를 읽지 않음으로 표시하는 중 오류가 발생했습니다. 가장 최근 100개의 채널 메시지보다 오래된 읽지 않은 메시지는 표시할 수 없습니다.",
  "Error muting a user ...": "사용자를 음소거하는 중에 오류가 발생했습니다...",
  "Error pinning message": "메시지를 핀하는 중에 오류가 발생했습니다.",
  "Error removing message pin": "메시지 핀을 제거하는 중에 오류가 발생했습니다.",
  "Error reproducing the recording": "녹음 재생 중 오류 발생",
  "Error starting recording": "녹음 시작 중 오류가 발생했습니다",
  "Error unmuting a user ...": "사용자 음소거 해제 중 오류 발생...",
  "Error uploading attachment": "첨부 파일 업로드 중 오류가 발생했습니다",
  "Error uploading file": "파일 업로드 오류",
  "Error uploading image": "이미지를 업로드하는 동안 오류가 발생했습니다.",
  "Error: {{ errorMessage }}": "오류: {{ errorMessage }}",
  "Failed to block user": "사용자 차단에 실패했습니다",
  "Failed to create the poll": "투표 생성 실패",
  "Failed to create the poll due to {{reason}}": "{{reason}} 때문에 투표를 생성하지 못했습니다",
  "Failed to delete the message": "메시지 삭제에 실패했습니다",
  "Failed to end the poll": "투표 종료에 실패했습니다",
  "Failed to end the poll due to {{reason}}": "{{reason}}(으)로 인해 투표 종료에 실패했습니다",
  "Failed to jump to the first unread message": "첫 번째 읽지 않은 메시지로 이동하지 못했습니다",
  "Failed to leave channel": "채널 나가기에 실패했습니다",
  "Failed to mark channel as read": "채널을 읽음으로 표시하는 데 실패했습니다",
  "Failed to play the recording": "녹음을 재생하지 못했습니다",
  "Failed to retrieve location": "위치를 가져오지 못했습니다",
  "Failed to share location": "위치를 공유하지 못했습니다",
  "Failed to update channel archive status": "채널 아카이브 상태 업데이트에 실패했습니다",
  "Failed to update channel mute status": "채널 음소거 상태 업데이트에 실패했습니다",
  "Failed to update channel pinned status": "채널 고정 상태 업데이트에 실패했습니다",
  File: File$5,
  "File is required for upload attachment": "첨부 파일 업로드에 파일이 필요합니다",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "파일이 너무 큽니다: {{ size }}, 최대 업로드 크기는 {{ limit }}입니다",
  "File too large": "파일이 너무 큽니다",
  fileCount_other: fileCount_other$4,
  Flag: Flag$4,
  "Generating...": "생성 중...",
  "giphy-command-args": "[텍스트]",
  "giphy-command-description": "채널에 무작위 GIF 게시",
  "Hide who voted": "누가 투표했는지 숨기기",
  Image: Image$5,
  imageCount_other: imageCount_other$4,
  "Instant commands": "즉시 명령어",
  "language/af": "아프리칸스어",
  "language/am": "암하라어",
  "language/ar": "아랍어",
  "language/az": "아제르바이잔어",
  "language/bg": "불가리아어",
  "language/bn": "벵골어",
  "language/bs": "보스니아어",
  "language/cs": "체코어",
  "language/da": "덴마크어",
  "language/de": "독일어",
  "language/el": "그리스어",
  "language/en": "영어",
  "language/es": "스페인어",
  "language/es-MX": "스페인어(멕시코)",
  "language/et": "에스토니아어",
  "language/fa": "페르시아어",
  "language/fa-AF": "다리어",
  "language/fi": "핀란드어",
  "language/fr": "프랑스어",
  "language/fr-CA": "프랑스어(캐나다)",
  "language/ha": "하우사어",
  "language/he": "히브리어",
  "language/hi": "힌디어",
  "language/hr": "크로아티아어",
  "language/ht": "아이티 크리올어",
  "language/hu": "헝가리어",
  "language/id": "인도네시아어",
  "language/it": "이탈리아어",
  "language/ja": "일본어",
  "language/ka": "조지아어",
  "language/ko": "한국어",
  "language/lt": "리투아니아어",
  "language/lv": "라트비아어",
  "language/ms": "말레이어",
  "language/nl": "네덜란드어",
  "language/no": "노르웨이어",
  "language/pl": "폴란드어",
  "language/ps": "파슈토어",
  "language/pt": "포르투갈어",
  "language/ro": "루마니아어",
  "language/ru": "러시아어",
  "language/sk": "슬로바키아어",
  "language/sl": "슬로베니아어",
  "language/so": "소말리아어",
  "language/sq": "알바니아어",
  "language/sr": "세르비아어",
  "language/sv": "스웨덴어",
  "language/sw": "스와힐리어",
  "language/ta": "타밀어",
  "language/th": "태국어",
  "language/tl": "타갈로그어",
  "language/tr": "터키어",
  "language/uk": "우크라이나어",
  "language/ur": "우르두어",
  "language/vi": "베트남어",
  "language/zh": "중국어(간체)",
  "language/zh-TW": "중국어(번체)",
  "Leave Channel": "채널 나가기",
  "Left channel": "채널을 나갔습니다",
  "Let others add options": "다른 사람이 선택지를 추가할 수 있도록 허용",
  "Limit votes per person": "1인당 투표 수 제한",
  Link: Link$4,
  linkCount_other: linkCount_other$4,
  live: live$4,
  "Live for {{duration}}": "{{duration}} 동안 라이브",
  "Live location": "라이브 위치",
  "Live until {{ timestamp }}": "{{ timestamp }}까지 라이브",
  "Load more": "더 불러오기",
  "Local upload attachment missing local id": "로컬 업로드 첨부에 로컬 ID가 없습니다",
  Location: Location$4,
  "Location sharing ended": "위치 공유가 종료되었습니다",
  "Location: {{ coordinates }}": "위치: {{ coordinates }}",
  "Mark as unread": "읽지 않음으로 표시",
  "Maximum number of votes (from 2 to 10)": "최대 투표 수 (2에서 10까지)",
  Menu: Menu$4,
  "Message deleted": "메시지가 삭제되었습니다.",
  "Message failed to send": "메시지 전송 실패",
  "Message has been successfully flagged": "메시지에 플래그가 지정되었습니다.",
  "Message marked as unread": "메시지를 읽지 않음으로 표시했습니다",
  "Message pinned": "메시지 핀했습니다",
  "Message unpinned": "메시지 고정이 해제됨",
  "Message was blocked by moderation policies": "메시지가 관리 정책에 의해 차단되었습니다.",
  "Messages have been marked unread.": "메시지가 읽지 않음으로 표시되었습니다.",
  "Missing permissions to upload the attachment": "첨부 파일을 업로드하려면 권한이 필요합니다",
  "Multiple votes": "복수 투표",
  Mute: Mute$4,
  "mute-command-args": "[@사용자이름]",
  "mute-command-description": "사용자 음소거",
  "network error": "네트워크 오류",
  New: New$4,
  "New Messages!": "새 메시지!",
  "Next image": "다음 이미지",
  "No chats here yet…": "아직 채팅이 없습니다...",
  "No conversations yet": "아직 대화가 없습니다.",
  "No items exist": "항목이 없습니다.",
  "No results found": "검색 결과가 없습니다",
  "Nobody will be able to vote in this poll anymore.": "이 투표에 더 이상 아무도 투표할 수 없습니다.",
  "Nothing yet...": "아직 아무것도...",
  Offline: Offline$4,
  Ok: Ok$4,
  Online: Online$4,
  "Only numbers are allowed": "숫자만 입력 가능합니다",
  "Only visible to you": "당신에게만 표시됨",
  "Open emoji picker": "이모지 선택기 열기",
  "Open gallery at image {{ index }}": "이미지 {{ index }}에서 갤러리 열기",
  "Open image in gallery": "갤러리에서 이미지 열기",
  "Option already exists": "옵션이 이미 존재합니다",
  "Option is empty": "옵션이 비어 있습니다",
  Options: Options$4,
  Original: Original$4,
  "People matching": "일치하는 사람",
  Photo: Photo$4,
  Pin: Pin$4,
  "Pinned by {{ name }}": "{{ name }}님이 핀함",
  "Pinned by You": "내가 고정함",
  "placeholder/PollComment": "댓글",
  "placeholder/PollOptionSuggestion": "새 옵션 입력",
  "Play video": "동영상 재생",
  Poll: Poll$5,
  "Poll comments": "투표 댓글",
  "Poll ended": "투표 종료됨",
  "Poll options": "투표 옵션",
  "Poll results": "투표 결과",
  "Previous image": "이전 이미지",
  Question: Question$4,
  "Question {{ optionOrderNumber}}": "질문 {{ optionOrderNumber}}",
  "Question is required": "질문이 필요합니다",
  "Quote Reply": "인용 답장",
  "Reached the vote limit. Remove an existing vote first.": "투표 한도에 도달했습니다. 기존 투표를 먼저 제거하세요.",
  "Recording format is not supported and cannot be reproduced": "녹음 형식이 지원되지 않으므로 재생할 수 없습니다",
  "Remind me": "알림",
  "Remind Me": "알림 설정",
  "Reminder set": "알림 설정됨",
  "Remove reminder": "알림 제거",
  "Remove save for later": "나중에 보기 제거",
  "Replied to a thread": "스레드에 답글을 남겼습니다",
  Reply: Reply$4,
  "Reply to {{ authorName }}": "{{ authorName }}님에게 답장",
  "Reply to a message to start a thread": "스레드를 시작하려면 메시지에 답장하세요",
  "Reply to Message": "메시지에 답장",
  replyCount_one: replyCount_one$4,
  replyCount_other: replyCount_other$4,
  Resend: Resend$4,
  "Retry upload": "업로드 다시 시도",
  "Save for later": "나중에 저장",
  "Saved for later": "나중에 저장됨",
  Search: Search$5,
  "Search GIFs": "GIF 검색",
  "search-results-header-filter-source-button-label--channels": "채널",
  "search-results-header-filter-source-button-label--messages": "메시지",
  "search-results-header-filter-source-button-label--users": "사용자",
  "Searching for {{ searchSourceType }}...": "{{ searchSourceType }} 검색 중...",
  "Searching...": "수색...",
  searchResultsCount_one: searchResultsCount_one$4,
  searchResultsCount_other: searchResultsCount_other$4,
  "Select a thread to continue the conversation": "대화를 계속하려면 스레드를 선택하세요",
  "Select more than one option": "하나 이상의 선택지 선택",
  "Select one": "하나 선택",
  "Select one or more": "하나 이상 선택",
  "Select up to {{count}}_one": "{{count}}개까지 선택",
  "Select up to {{count}}_other": "{{count}}개까지 선택",
  Send: Send$4,
  "Send a message to start the conversation": "대화를 시작하려면 메시지를 보내세요",
  "Send Anyway": "어쨌든 보내기",
  "Send message request failed": "메시지 보내기 요청 실패",
  "Send poll": "투표 보내기",
  "Sending...": "배상중...",
  Sent: Sent$4,
  Share: Share$4,
  "Share live location for": "라이브 위치 공유",
  "Share Location": "위치 공유",
  "Shared live location": "공유된 라이브 위치",
  "Shared location": "공유된 위치",
  Shuffle: Shuffle$4,
  "size limit": "크기 제한",
  "Slow Mode ON": "슬로우 모드 켜짐",
  "Slow mode, wait {{ seconds }}s...": "슬로우 모드, {{ seconds }}초 기다려 주세요...",
  "Some of the files will not be accepted": "일부 파일은 허용되지 않을 수 있습니다",
  "Start typing to search": "검색하려면 입력을 시작하세요",
  "Stop sharing": "공유 중지",
  Submit: Submit$4,
  "Suggest an option": "옵션 제안",
  "Tap to remove": "제거하려면 탭하세요",
  "Thinking...": "생각 중...",
  "this content could not be displayed": "이 콘텐츠를 표시할 수 없습니다",
  "This field cannot be empty or contain only spaces": "이 필드는 비워둘 수 없으며 공백만 포함할 수도 없습니다",
  "This message did not meet our content guidelines": "이 메시지는 콘텐츠 가이드라인을 충족하지 않습니다.",
  Thread: Thread$5,
  "Thread has not been found": "스레드를 찾을 수 없습니다",
  "Thread reply": "스레드 답장",
  "Thread Reply": "스레드 답장",
  "ThreadListUnseenThreadsBanner/loading": "로딩 중...",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "읽지 않은 스레드 {{ count }}개",
  Threads: Threads$4,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[어제]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[오늘]", "nextDay": "[내일]", "lastDay": "[어제]", "nextWeek": "dddd", "lastWeek": "[지난] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "오늘",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "어제",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "브라우저에서 카메라 액세스를 허용하여 녹음을 시작합니다",
  "To start recording, allow the microphone access in your browser": "브라우저에서 마이크로폰 액세스를 허용하여 녹음을 시작합니다",
  totalVoteCount_other: totalVoteCount_other$4,
  Translated: Translated$4,
  "Translated from {{ language }}": "{{ language }}(으)로 번역됨",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "2에서 10 사이의 숫자를 입력하세요",
  "Type your message": "메시지 입력",
  Unarchive: Unarchive$4,
  "unban-command-args": "[@사용자이름]",
  "unban-command-description": "사용자 차단 해제",
  "Unblock User": "사용자 차단 해제",
  "unknown error": "알 수 없는 오류",
  Unmute: Unmute$4,
  "unmute-command-args": "[@사용자이름]",
  "unmute-command-description": "사용자 음소거 해제",
  Unpin: Unpin$4,
  "Unread messages": "읽지 않은 메시지",
  "Unsupported attachment": "지원되지 않는 첨부 파일",
  "unsupported file type": "지원되지 않는 파일 형식",
  Update: Update$4,
  "Update your comment": "댓글 업데이트",
  "Upload blocked": "업로드가 차단되었습니다",
  "Upload error": "업로드 오류",
  "Upload failed": "업로드에 실패했습니다",
  'Upload type: "{{ type }}" is not allowed': '업로드 유형: "{{ type }}"은(는) 허용되지 않습니다.',
  "User blocked": "사용자가 차단됨",
  "User unblocked": "사용자 차단이 해제됨",
  "User uploaded content": "사용자 업로드 콘텐츠",
  Video: Video$4,
  videoCount_other: videoCount_other$4,
  View: View$4,
  "View {{count}} comments_one": "{{count}}개의 댓글 보기",
  "View {{count}} comments_other": "{{count}}개의 댓글 보기",
  "View all": "전체 보기",
  "View original": "원문 보기",
  "View results": "결과 보기",
  "View translation": "번역 보기",
  "Voice message": "음성 메시지",
  "Voice message {{ duration }}": "음성 메시지 {{ duration }}",
  "Voice message deleted": "음성 메시지가 삭제됨",
  voiceMessageCount_other: voiceMessageCount_other$4,
  "Vote ended": "투표 종료",
  Votes: Votes$4,
  "Wait until all attachments have uploaded": "모든 첨부 파일이 업로드될 때까지 기다립니다.",
  "Waiting for network…": "네트워크 대기 중…",
  You: You$4,
  "You've reached the maximum number of files": "최대 파일 수에 도달했습니다."
};
var Anonymous$3 = "Anoniem";
var Archive$3 = "Archief";
var Attach$3 = "Bijvoegen";
var Attachment$4 = "Bijlage";
var Back$3 = "Terug";
var Cancel$3 = "Annuleer";
var Channels$3 = "Kanalen";
var Chats$3 = "Chats";
var Close$3 = "Sluit";
var Commands$3 = "Commando's";
var Create$3 = "Maak";
var Delete$3 = "Verwijder";
var Delivered$3 = "Afgeleverd";
var Edited$3 = "Bewerkt";
var End$3 = "Einde";
var File$4 = "Bestand";
var fileCount_one$3 = "1 bestand";
var fileCount_other$3 = "{{ count }} bestanden";
var Flag$3 = "Markeer";
var Image$4 = "Afbeelding";
var imageCount_one$3 = "Afbeelding";
var imageCount_other$3 = "{{ count }} afbeeldingen";
var Link$3 = "Link";
var linkCount_one$3 = "Link";
var linkCount_other$3 = "{{ count }} links";
var live$3 = "live";
var Location$3 = "Locatie";
var Menu$3 = "Menu";
var Mute$3 = "Dempen";
var New$3 = "Nieuwe";
var Offline$3 = "Offline";
var Ok$3 = "Oké";
var Online$3 = "Online";
var Options$3 = "Opties";
var Original$3 = "Origineel";
var Photo$3 = "Foto";
var Pin$3 = "Vastmaken";
var Poll$4 = "Peiling";
var Question$3 = "Vraag";
var Reply$3 = "Antwoord";
var replyCount_one$3 = "1 antwoord";
var replyCount_other$3 = "{{ count }} antwoorden";
var Resend$3 = "Opnieuw verzenden";
var Search$4 = "Zoeken";
var searchResultsCount_one$3 = "1 resultaat";
var searchResultsCount_other$3 = "{{ count }} resultaten";
var Send$3 = "Verstuur";
var Sent$3 = "Verzonden";
var Share$3 = "Delen";
var Shuffle$3 = "Schudden";
var Submit$3 = "Versturen";
var Thread$4 = "Draadje";
var Threads$3 = "Discussies";
var totalVoteCount_one$3 = "1 stem in totaal";
var totalVoteCount_other$3 = "{{ count }} stemmen in totaal";
var Translated$3 = "Vertaald";
var Unarchive$3 = "Uit archief halen";
var Unmute$3 = "Dempen opheffen";
var Unpin$3 = "Losmaken";
var Update$3 = "Bijwerken";
var Video$3 = "Video";
var videoCount_one$3 = "Video";
var videoCount_other$3 = "{{ count }} video's";
var View$3 = "Bekijken";
var voiceMessageCount_one$3 = "Spraakbericht";
var voiceMessageCount_other$3 = "{{ count }} spraakberichten";
var Votes$3 = "Stemmen";
var You$3 = "Jij";
var nlTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} en {{ moreCount }} meer",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} en {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} bestand",
  "{{ count }} files_other": "{{ count }} bestanden",
  "{{ count }} people are typing_one": "{{ count }} persoon typt",
  "{{ count }} people are typing_many": "{{ count }} personen typen",
  "{{ count }} people are typing_other": "{{ count }} personen typen",
  "{{ count }} photos_one": "{{ count }} foto",
  "{{ count }} photos_other": "{{ count }} foto's",
  "{{ count }} reactions_one": "{{ count }} reactie",
  "{{ count }} reactions_other": "{{ count }} reacties",
  "{{ count }} videos_one": "{{ count }} video",
  "{{ count }} videos_other": "{{ count }} video's",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} en {{ secondUser }}",
  "{{ imageCount }} more": "+{{ imageCount }}",
  "{{ memberCount }} members": "{{ memberCount }} deelnemers",
  "{{ typing }} are typing": "{{ typing }} typen",
  "{{ typing }} is typing": "{{ typing }} typt",
  "{{ user }} has been muted": "{{ user }} is gedempt",
  "{{ user }} has been unmuted": "{{ user }} is niet meer gedempt",
  "{{ user }} is typing...": "{{ user }} is aan het typen...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} en {{ user }} zijn aan het typen...",
  "{{ users }} and more are typing...": "{{ users }} en meer zijn aan het typen...",
  "{{ watcherCount }} online": "{{ watcherCount }} online",
  "{{count}} new messages_one": "{{count}} nieuw bericht",
  "{{count}} new messages_other": "{{count}} nieuwe berichten",
  "{{count}} unread_one": "{{count}} ongelezen",
  "{{count}} unread_other": "{{count}} ongelezen",
  "{{count}} votes_one": "{{count}} stem",
  "{{count}} votes_other": "{{count}} stemmen",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} meer optie",
  "+{{count}} more options_other": "+{{count}} meer opties",
  "🏙 Attachment...": "🏙 Bijlage...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} heeft gemaakt: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} heeft gestemd: {{pollOptionText}}",
  "📍Shared location": "📍Gedeelde locatie",
  "Add a comment": "Voeg een opmerking toe",
  "Add an option": "Voeg een optie toe",
  "Add reaction": "Reactie toevoegen",
  "All results loaded": "Alle resultaten geladen",
  "Allow access to camera": "Toegang tot camera toestaan",
  "Allow access to microphone": "Toegang tot microfoon toestaan",
  "Allow comments": "Sta opmerkingen toe",
  "Allow option suggestion": "Sta optie-suggesties toe",
  "Allow others to add comments": "Sta anderen toe om opmerkingen toe te voegen",
  "Also send as a direct message": "Ook als direct bericht versturen",
  "Also send in channel": "Ook in kanaal versturen",
  "Also sent in channel": "Ook in kanaal verzonden",
  "An error has occurred during recording": "Er is een fout opgetreden tijdens het opnemen",
  "An error has occurred during the recording processing": "Er is een fout opgetreden tijdens de verwerking van de opname",
  Anonymous: Anonymous$3,
  "Anonymous poll": "Anonieme peiling",
  Archive: Archive$3,
  "Are you sure you want to delete this message?": "Weet je zeker dat je dit bericht wilt verwijderen?",
  "aria/Attachment": "Bijlage",
  "aria/Block User": "Gebruiker blokkeren",
  "aria/Bookmark Message": "Bericht bookmarken",
  "aria/Cancel Reply": "Antwoord annuleren",
  "aria/Cancel upload": "Upload annuleren",
  "aria/Channel list": "Kanaallijst",
  "aria/Channel search results": "Zoekresultaten voor kanalen",
  "aria/Close thread": "Draad sluiten",
  "aria/Collapse sidebar": "Zijbalk samenklappen",
  "aria/Copy Message Text": "Berichttekst kopiëren",
  "aria/Delete Message": "Bericht verwijderen",
  "aria/Download attachment": "Bijlage downloaden",
  "aria/Edit Message": "Bericht bewerken",
  "aria/Emoji picker": "Emoji kiezer",
  "aria/Expand sidebar": "Zijbalken uitvouwen",
  "aria/File input": "Bestandsinvoer",
  "aria/File upload": "Bestand uploaden",
  "aria/Flag Message": "Bericht markeren",
  "aria/Image failed to load": "Afbeelding laden mislukt",
  "aria/Image input": "Afbeelding invoeren",
  "aria/Load More Channels": "Meer kanalen laden",
  "aria/Mark Message Unread": "Markeren als ongelezen",
  "aria/Menu": "Menu",
  "aria/Message Options": "Berichtopties",
  "aria/Mute User": "Gebruiker dempen",
  "aria/Open Attachment Selector": "Open bijlage selector",
  "aria/Open Menu": "Menu openen",
  "aria/Open Message Actions Menu": "Menu voor berichtacties openen",
  "aria/Open Reaction Selector": "Reactiekiezer openen",
  "aria/Open Thread": "Draad openen",
  "aria/Percent complete": "{{percent}} procent voltooid",
  "aria/Pin Message": "Bericht vastmaken",
  "aria/Quote Message": "Bericht citeren",
  "aria/Reaction list": "Reactielijst",
  "aria/Remind Me Message": "Herinner mij",
  "aria/Remind Me Options": "Herinneringsopties",
  "aria/Remove attachment": "Bijlage verwijderen",
  "aria/Remove location attachment": "Locatie bijlage verwijderen",
  "aria/Remove Reminder": "Herinnering verwijderen",
  "aria/Remove Save For Later": "Verwijder 'Bewaren voor later'",
  "aria/Resend Message": "Bericht opnieuw verzenden",
  "aria/Retry upload": "Upload opnieuw proberen",
  "aria/Search results": "Zoekresultaten",
  "aria/Search results header filter button": "Zoekresultaten header filter knop",
  "aria/Send": "Verzenden",
  "aria/Show preview": "Voorbeeld tonen",
  "aria/Stop AI Generation": "AI-generatie stoppen",
  "aria/Unblock User": "Gebruiker deblokkeren",
  "aria/Unmute User": "Dempen opheffen",
  "aria/Unpin Message": "Losmaken",
  "Ask a question": "Stel een vraag",
  Attach: Attach$3,
  "Attach files": "Bijlage toevoegen",
  Attachment: Attachment$4,
  "Attachment upload blocked due to {{reason}}": "Bijlage upload geblokkeerd vanwege {{reason}}",
  "Attachment upload failed due to {{reason}}": "Bijlage upload mislukt vanwege {{reason}}",
  Back: Back$3,
  "ban-command-args": "[@gebruikersnaam] [tekst]",
  "ban-command-description": "Een gebruiker verbannen",
  "Block User": "Gebruiker blokkeren",
  Cancel: Cancel$3,
  "Cannot seek in the recording": "Kan niet zoeken in de opname",
  "Channel archived": "Kanaal gearchiveerd",
  "Channel Missing": "Kanaal niet gevonden",
  "Channel muted": "Kanaal gedempt",
  "Channel pinned": "Kanaal vastgezet",
  "Channel unarchived": "Kanaal uit archief gehaald",
  "Channel unmuted": "Dempen van kanaal opgeheven",
  "Channel unpinned": "Kanaal losgemaakt",
  Channels: Channels$3,
  Chats: Chats$3,
  "Choose between 2 to 10 options": "Kies tussen 2 en 10 opties",
  Close: Close$3,
  "Close emoji picker": "Sluit de emoji-kiezer",
  Commands: Commands$3,
  "Commands matching": "Bijpassende opdrachten",
  "Connection failure, reconnecting now...": "Verbindingsfout, opnieuw verbinden...",
  "Copy Message": "Bericht kopiëren",
  Create: Create$3,
  "Create poll": "Maak peiling",
  "Current location": "Huidige locatie",
  Delete: Delete$3,
  "Delete for me": "Voor mij verwijderen",
  "Delete message": "Bericht verwijderen",
  Delivered: Delivered$3,
  "Direct message": "Direct bericht",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Wil je deze peiling nu beëindigen? Niemand kan daarna meer op deze peiling stemmen.",
  "Download All": "Alles downloaden",
  "Download Attachment": "Bijlage downloaden",
  "Download attachment {{ name }}": "Bijlage {{ name }} downloaden",
  "Drag your files here": "Sleep je bestanden hier naartoe",
  "Drag your files here to add to your post": "Sleep je bestanden hier naartoe om aan je bericht toe te voegen",
  "Due {{ timeLeft }}": "Vervallen in {{ timeLeft }}",
  "Due since {{ dueSince }}": "Vervallen sinds {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Bericht bewerken",
  "Edit message request failed": "Verzoek om bericht bewerken mislukt",
  Edited: Edited$3,
  "Emoji matching": "Emoji-overeenkomsten",
  "Empty message...": "Leeg bericht...",
  End: End$3,
  "End poll": "Peiling beëindigen",
  "End this poll?": "Peiling beëindigen?",
  "End vote": "Einde stem",
  "Enforce unique vote is enabled": "Unieke stem is ingeschakeld",
  "Error": "Fout",
  "Error adding flag": "Fout bij toevoegen van vlag",
  "Error connecting to chat, refresh the page to try again.": "Fout bij het verbinden, ververs de pagina om nogmaals te proberen",
  "Error deleting message": "Fout bij verwijderen van bericht",
  "Error fetching reactions": "Fout bij het laden van reacties",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Fout bij markeren van bericht als ongelezen. Kan geen oudere ongelezen berichten markeren dan de nieuwste 100 kanaalberichten.",
  "Error muting a user ...": "Fout bij het muten van de gebruiker",
  "Error pinning message": "Fout bij vastzetten van bericht",
  "Error removing message pin": "Fout bij verwijderen van berichtpin",
  "Error reproducing the recording": "Fout bij het afspelen van de opname",
  "Error starting recording": "Fout bij het starten van de opname",
  "Error unmuting a user ...": "Fout bij het unmuten van de gebruiker",
  "Error uploading attachment": "Fout bij het uploaden van de bijlage",
  "Error uploading file": "Fout bij uploaden bestand",
  "Error uploading image": "Fout bij uploaden afbeelding",
  "Error: {{ errorMessage }}": "Fout: {{ errorMessage }}",
  "Failed to block user": "Gebruiker blokkeren mislukt",
  "Failed to create the poll": "Fout bij het maken van de peiling",
  "Failed to create the poll due to {{reason}}": "Peiling kon niet worden aangemaakt vanwege {{reason}}",
  "Failed to delete the message": "Bericht verwijderen mislukt",
  "Failed to end the poll": "Peiling kon niet worden beëindigd",
  "Failed to end the poll due to {{reason}}": "Peiling kon niet worden beëindigd vanwege {{reason}}",
  "Failed to jump to the first unread message": "Niet gelukt om naar het eerste ongelezen bericht te springen",
  "Failed to leave channel": "Kanaal verlaten mislukt",
  "Failed to mark channel as read": "Kanaal kon niet als gelezen worden gemarkeerd",
  "Failed to play the recording": "Kan de opname niet afspelen",
  "Failed to retrieve location": "Locatie kon niet worden opgehaald",
  "Failed to share location": "Locatie kon niet worden gedeeld",
  "Failed to update channel archive status": "Archiefstatus van kanaal kon niet worden bijgewerkt",
  "Failed to update channel mute status": "Muteerstatus van kanaal kon niet worden bijgewerkt",
  "Failed to update channel pinned status": "Vastgezette status van kanaal kon niet worden bijgewerkt",
  File: File$4,
  "File is required for upload attachment": "Bestand is vereist voor het uploaden van een bijlage",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "Bestand is te groot: {{ size }}, maximale uploadgrootte is {{ limit }}",
  "File too large": "Bestand is te groot",
  fileCount_one: fileCount_one$3,
  fileCount_other: fileCount_other$3,
  Flag: Flag$3,
  "Generating...": "Genereren...",
  "giphy-command-args": "[tekst]",
  "giphy-command-description": "Plaats een willekeurige gif in het kanaal",
  "Hide who voted": "Verberg wie heeft gestemd",
  Image: Image$4,
  imageCount_one: imageCount_one$3,
  imageCount_other: imageCount_other$3,
  "Instant commands": "Snelle opdrachten",
  "language/af": "Afrikaans",
  "language/am": "Amhaars",
  "language/ar": "Arabisch",
  "language/az": "Azerbeidzjaans",
  "language/bg": "Bulgaars",
  "language/bn": "Bengaals",
  "language/bs": "Bosnisch",
  "language/cs": "Tsjechisch",
  "language/da": "Deens",
  "language/de": "Duits",
  "language/el": "Grieks",
  "language/en": "Engels",
  "language/es": "Spaans",
  "language/es-MX": "Spaans (Mexico)",
  "language/et": "Estlands",
  "language/fa": "Perzisch",
  "language/fa-AF": "Dari",
  "language/fi": "Fins",
  "language/fr": "Frans",
  "language/fr-CA": "Frans (Canada)",
  "language/ha": "Hausa",
  "language/he": "Hebreeuws",
  "language/hi": "Hindi",
  "language/hr": "Kroatisch",
  "language/ht": "Haïtiaans Creools",
  "language/hu": "Hongaars",
  "language/id": "Indonesisch",
  "language/it": "Italiaans",
  "language/ja": "Japans",
  "language/ka": "Georgisch",
  "language/ko": "Koreaans",
  "language/lt": "Litouws",
  "language/lv": "Letlands",
  "language/ms": "Maleis",
  "language/nl": "Nederlands",
  "language/no": "Noors",
  "language/pl": "Pools",
  "language/ps": "Pasjtoe",
  "language/pt": "Portugees",
  "language/ro": "Roemeens",
  "language/ru": "Russisch",
  "language/sk": "Slowaaks",
  "language/sl": "Sloveens",
  "language/so": "Somali",
  "language/sq": "Albanees",
  "language/sr": "Servisch",
  "language/sv": "Zweeds",
  "language/sw": "Swahili",
  "language/ta": "Tamil",
  "language/th": "Thai",
  "language/tl": "Tagalog",
  "language/tr": "Turks",
  "language/uk": "Oekraïens",
  "language/ur": "Urdu",
  "language/vi": "Vietnamees",
  "language/zh": "Chinees (vereenvoudigd)",
  "language/zh-TW": "Chinees (traditioneel)",
  "Leave Channel": "Kanaal verlaten",
  "Left channel": "Kanaal verlaten",
  "Let others add options": "Laat anderen opties toevoegen",
  "Limit votes per person": "Stemmen per persoon beperken",
  Link: Link$3,
  linkCount_one: linkCount_one$3,
  linkCount_other: linkCount_other$3,
  live: live$3,
  "Live for {{duration}}": "Live voor {{duration}}",
  "Live location": "Live locatie",
  "Live until {{ timestamp }}": "Live tot {{ timestamp }}",
  "Load more": "Meer laden",
  "Local upload attachment missing local id": "Lokale uploadbijlage mist lokale id",
  Location: Location$3,
  "Location sharing ended": "Locatie delen beëindigd",
  "Location: {{ coordinates }}": "Locatie: {{ coordinates }}",
  "Mark as unread": "Markeren als ongelezen",
  "Maximum number of votes (from 2 to 10)": "Maximaal aantal stemmen (van 2 tot 10)",
  Menu: Menu$3,
  "Message deleted": "Bericht verwijderd",
  "Message failed to send": "Bericht kon niet worden verzonden",
  "Message has been successfully flagged": "Bericht is succesvol gemarkeerd",
  "Message marked as unread": "Bericht gemarkeerd als ongelezen",
  "Message pinned": "Bericht vastgezet",
  "Message unpinned": "Bericht losgemaakt",
  "Message was blocked by moderation policies": "Bericht is geblokkeerd door moderatiebeleid",
  "Messages have been marked unread.": "Berichten zijn gemarkeerd als ongelezen.",
  "Missing permissions to upload the attachment": "Missende toestemmingen om de bijlage te uploaden",
  "Multiple votes": "Meerdere stemmen",
  Mute: Mute$3,
  "mute-command-args": "[@gebruikersnaam]",
  "mute-command-description": "Een gebruiker dempen",
  "network error": "netwerkfout",
  New: New$3,
  "New Messages!": "Nieuwe Berichten!",
  "Next image": "Volgende afbeelding",
  "No chats here yet…": "Nog geen chats hier...",
  "No conversations yet": "Nog geen gesprekken",
  "No items exist": "Er zijn geen items",
  "No results found": "Geen resultaten gevonden",
  "Nobody will be able to vote in this poll anymore.": "Niemand kan meer stemmen in deze peiling.",
  "Nothing yet...": "Nog niets ...",
  Offline: Offline$3,
  Ok: Ok$3,
  Online: Online$3,
  "Only numbers are allowed": "Alleen nummers zijn toegestaan",
  "Only visible to you": "Alleen zichtbaar voor jou",
  "Open emoji picker": "Emoji-kiezer openen",
  "Open gallery at image {{ index }}": "Galerij openen bij afbeelding {{ index }}",
  "Open image in gallery": "Afbeelding openen in galerij",
  "Option already exists": "Optie bestaat al",
  "Option is empty": "Optie is leeg",
  Options: Options$3,
  Original: Original$3,
  "People matching": "Mensen die matchen",
  Photo: Photo$3,
  Pin: Pin$3,
  "Pinned by {{ name }}": "Vastgemaakt door {{ name }}",
  "Pinned by You": "Door jou vastgezet",
  "placeholder/PollComment": "Jouw reactie",
  "placeholder/PollOptionSuggestion": "Voer een nieuwe optie in",
  "Play video": "Video afspelen",
  Poll: Poll$4,
  "Poll comments": "Peiling opmerkingen",
  "Poll ended": "Peiling beëindigd",
  "Poll options": "Peiling opties",
  "Poll results": "Peiling resultaten",
  "Previous image": "Vorige afbeelding",
  Question: Question$3,
  "Question {{ optionOrderNumber}}": "Vraag {{ optionOrderNumber}}",
  "Question is required": "Vraag is verplicht",
  "Quote Reply": "Citaatantwoord",
  "Reached the vote limit. Remove an existing vote first.": "Stemlimiet bereikt. Verwijder eerst een bestaande stem.",
  "Recording format is not supported and cannot be reproduced": "Opnameformaat wordt niet ondersteund en kan niet worden gereproduceerd",
  "Remind me": "Herinner me",
  "Remind Me": "Herinner mij",
  "Reminder set": "Herinnering ingesteld",
  "Remove reminder": "Herinnering verwijderen",
  "Remove save for later": "Verwijder 'Bewaren voor later'",
  "Replied to a thread": "Heeft gereageerd in een thread",
  Reply: Reply$3,
  "Reply to {{ authorName }}": "Antwoord aan {{ authorName }}",
  "Reply to a message to start a thread": "Beantwoord een bericht om een thread te starten",
  "Reply to Message": "Antwoord op bericht",
  replyCount_one: replyCount_one$3,
  replyCount_other: replyCount_other$3,
  Resend: Resend$3,
  "Retry upload": "Upload opnieuw proberen",
  "Save for later": "Bewaren voor later",
  "Saved for later": "Bewaard voor later",
  Search: Search$4,
  "Search GIFs": "GIF's zoeken",
  "search-results-header-filter-source-button-label--channels": "kanalen",
  "search-results-header-filter-source-button-label--messages": "berichten",
  "search-results-header-filter-source-button-label--users": "gebruikers",
  "Searching for {{ searchSourceType }}...": "Zoeken naar {{ searchSourceType }}...",
  "Searching...": "Zoeken...",
  searchResultsCount_one: searchResultsCount_one$3,
  searchResultsCount_other: searchResultsCount_other$3,
  "Select a thread to continue the conversation": "Selecteer een thread om het gesprek voort te zetten",
  "Select more than one option": "Selecteer meer dan één optie",
  "Select one": "Selecteer er een",
  "Select one or more": "Selecteer een of meer",
  "Select up to {{count}}_one": "Selecteer tot {{count}}",
  "Select up to {{count}}_other": "Selecteer tot {{count}}",
  Send: Send$3,
  "Send a message to start the conversation": "Stuur een bericht om het gesprek te beginnen",
  "Send Anyway": "Toch versturen",
  "Send message request failed": "Verzoek om bericht te verzenden mislukt",
  "Send poll": "Peiling versturen",
  "Sending...": "Aan het verzenden...",
  Sent: Sent$3,
  Share: Share$3,
  "Share live location for": "Live locatie delen voor",
  "Share Location": "Locatie delen",
  "Shared live location": "Gedeelde live locatie",
  "Shared location": "Gedeelde locatie",
  Shuffle: Shuffle$3,
  "size limit": "grootte limiet",
  "Slow Mode ON": "Langzame modus aan",
  "Slow mode, wait {{ seconds }}s...": "Langzame modus, wacht {{ seconds }}s...",
  "Slow wait, wait {{ seconds }}s": "Langzame modus, wacht {{ seconds }}s",
  "Slow wait, wait {{ seconds }}s...": "Langzame modus, wacht {{ seconds }}s...",
  "Some of the files will not be accepted": "Sommige bestanden zullen niet worden geaccepteerd",
  "Start typing to search": "Begin met typen om te zoeken",
  "Stop sharing": "Delen stoppen",
  Submit: Submit$3,
  "Suggest an option": "Stel een optie voor",
  "Tap to remove": "Tik om te verwijderen",
  "Thinking...": "Denken...",
  "this content could not be displayed": "Deze inhoud kan niet weergegeven worden",
  "This field cannot be empty or contain only spaces": "Dit veld mag niet leeg zijn of alleen spaties bevatten",
  "This message did not meet our content guidelines": "Dit bericht voldeed niet aan onze inhoudsrichtlijnen",
  Thread: Thread$4,
  "Thread has not been found": "Draadje niet gevonden",
  "Thread reply": "Draadje antwoord",
  "Thread Reply": "Draadje antwoord",
  "ThreadListUnseenThreadsBanner/loading": "Laden...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} ongelezen thread",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} ongelezen threads",
  Threads: Threads$3,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Gisteren]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Vandaag]", "nextDay": "[Morgen]", "lastDay": "[Gisteren]", "nextWeek": "dddd", "lastWeek": "[Laatste] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Vandaag",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Gisteren",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Om te beginnen met opnemen, sta toegang tot de camera toe in uw browser",
  "To start recording, allow the microphone access in your browser": "Om te beginnen met opnemen, sta toegang tot de microfoon toe in uw browser",
  totalVoteCount_one: totalVoteCount_one$3,
  totalVoteCount_other: totalVoteCount_other$3,
  Translated: Translated$3,
  "Translated from {{ language }}": "Vertaald uit {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Typ een getal van 2 tot 10",
  "Type your message": "Type je bericht",
  Unarchive: Unarchive$3,
  "unban-command-args": "[@gebruikersnaam]",
  "unban-command-description": "Een gebruiker debannen",
  "Unblock User": "Gebruiker deblokkeren",
  "unknown error": "onbekende fout",
  Unmute: Unmute$3,
  "unmute-command-args": "[@gebruikersnaam]",
  "unmute-command-description": "Een gebruiker niet meer dempen",
  Unpin: Unpin$3,
  "Unread messages": "Ongelezen berichten",
  "Unsupported attachment": "Niet-ondersteunde bijlage",
  "unsupported file type": "niet-ondersteund bestandstype",
  Update: Update$3,
  "Update your comment": "Werk je opmerking bij",
  "Upload blocked": "Upload geblokkeerd",
  "Upload error": "Uploadfout",
  "Upload failed": "Upload mislukt",
  'Upload type: "{{ type }}" is not allowed': 'Uploadtype: "{{ type }}" is niet toegestaan',
  "User blocked": "Gebruiker geblokkeerd",
  "User unblocked": "Gebruiker gedeblokkeerd",
  "User uploaded content": "Gebruikersgeüploade inhoud",
  Video: Video$3,
  videoCount_one: videoCount_one$3,
  videoCount_other: videoCount_other$3,
  View: View$3,
  "View {{count}} comments_one": "Bekijk {{count}} opmerkingen",
  "View {{count}} comments_other": "Bekijk {{count}} opmerkingen",
  "View all": "Alles bekijken",
  "View original": "Origineel bekijken",
  "View results": "Bekijk resultaten",
  "View translation": "Vertaling bekijken",
  "Voice message": "Spraakbericht",
  "Voice message {{ duration }}": "Spraakbericht {{ duration }}",
  "Voice message deleted": "Spraakbericht verwijderd",
  voiceMessageCount_one: voiceMessageCount_one$3,
  voiceMessageCount_other: voiceMessageCount_other$3,
  "Vote ended": "Stemmen beëindigd",
  Votes: Votes$3,
  "Wait until all attachments have uploaded": "Wacht tot alle bijlagen zijn geüpload",
  "Waiting for network…": "Wachten op netwerk…",
  You: You$3,
  "You've reached the maximum number of files": "Je hebt het maximale aantal bestanden bereikt"
};
var Anonymous$2 = "Anônimo";
var Archive$2 = "Arquivar";
var Attach$2 = "Anexar";
var Attachment$3 = "Anexo";
var Back$2 = "Voltar";
var Cancel$2 = "Cancelar";
var Channels$2 = "Canais";
var Chats$2 = "Conversas";
var Close$2 = "Fechar";
var Commands$2 = "Comandos";
var Create$2 = "Criar";
var Delete$2 = "Excluir";
var Delivered$2 = "Entregue";
var Edited$2 = "Editada";
var End$2 = "Fim";
var File$3 = "Arquivo";
var fileCount_one$2 = "1 arquivo";
var fileCount_many$1 = "{{ count }} arquivos";
var fileCount_other$2 = "{{ count }} arquivos";
var Flag$2 = "Reportar";
var Image$3 = "Imagem";
var imageCount_one$2 = "Imagem";
var imageCount_many$1 = "{{ count }} imagens";
var imageCount_other$2 = "{{ count }} imagens";
var Link$2 = "Link";
var linkCount_one$2 = "Link";
var linkCount_many$1 = "{{ count }} links";
var linkCount_other$2 = "{{ count }} links";
var live$2 = "ao vivo";
var Location$2 = "Localização";
var Menu$2 = "Menu";
var Mute$2 = "Silenciar";
var New$2 = "Novo";
var Offline$2 = "Offline";
var Ok$2 = "OK";
var Online$2 = "Online";
var Options$2 = "Opções";
var Original$2 = "Original";
var Photo$2 = "Foto";
var Pin$2 = "Fixar";
var Poll$3 = "Enquete";
var Question$2 = "Pergunta";
var Reply$2 = "Responder";
var replyCount_one$2 = "1 resposta";
var replyCount_many$1 = "{{ count }} respostas";
var replyCount_other$2 = "{{ count }} respostas";
var Resend$2 = "Reenviar";
var Search$3 = "Buscar";
var searchResultsCount_one$2 = "1 resultado";
var searchResultsCount_many$1 = "{{ count }} resultados";
var searchResultsCount_other$2 = "{{ count }} resultados";
var Send$2 = "Enviar";
var Sent$2 = "Enviado";
var Share$2 = "Compartilhar";
var Shuffle$2 = "Embaralhar";
var Submit$2 = "Enviar";
var Thread$3 = "Fio";
var Threads$2 = "Tópicos";
var totalVoteCount_one$2 = "1 voto no total";
var totalVoteCount_many$1 = "{{ count }} votos no total";
var totalVoteCount_other$2 = "{{ count }} votos no total";
var Translated$2 = "Traduzido";
var Unarchive$2 = "Desarquivar";
var Unmute$2 = "Ativar som";
var Unpin$2 = "Desfixar";
var Update$2 = "Atualizar";
var Video$2 = "Vídeo";
var videoCount_one$2 = "Vídeo";
var videoCount_many$1 = "{{ count }} vídeos";
var videoCount_other$2 = "{{ count }} vídeos";
var View$2 = "Ver";
var voiceMessageCount_one$2 = "Mensagem de voz";
var voiceMessageCount_many$1 = "{{ count }} mensagens de voz";
var voiceMessageCount_other$2 = "{{ count }} mensagens de voz";
var Votes$2 = "Votos";
var You$2 = "Você";
var ptTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} e mais {{ moreCount }}",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} e {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} arquivo",
  "{{ count }} files_many": "{{ count }} arquivos",
  "{{ count }} files_other": "{{ count }} arquivos",
  "{{ count }} people are typing_one": "{{ count }} pessoa está digitando",
  "{{ count }} people are typing_many": "{{ count }} pessoas estão digitando",
  "{{ count }} people are typing_other": "{{ count }} pessoas estão digitando",
  "{{ count }} photos_one": "{{ count }} foto",
  "{{ count }} photos_many": "{{ count }} fotos",
  "{{ count }} photos_other": "{{ count }} fotos",
  "{{ count }} reactions_one": "{{ count }} reação",
  "{{ count }} reactions_many": "{{ count }} reações",
  "{{ count }} reactions_other": "{{ count }} reações",
  "{{ count }} videos_one": "{{ count }} vídeo",
  "{{ count }} videos_many": "{{ count }} vídeos",
  "{{ count }} videos_other": "{{ count }} vídeos",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} e {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} mais",
  "{{ memberCount }} members": "{{ memberCount }} membros",
  "{{ typing }} are typing": "{{ typing }} estão digitando",
  "{{ typing }} is typing": "{{ typing }} está digitando",
  "{{ user }} has been muted": "{{ user }} foi silenciado",
  "{{ user }} has been unmuted": "{{ user }} foi reativado",
  "{{ user }} is typing...": "{{ user }} está digitando...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} e {{ user }} estão digitando...",
  "{{ users }} and more are typing...": "{{ users }} e mais estão digitando...",
  "{{ watcherCount }} online": "{{ watcherCount }} online",
  "{{count}} new messages_one": "{{count}} nova mensagem",
  "{{count}} new messages_many": "{{count}} novas mensagens",
  "{{count}} new messages_other": "{{count}} novas mensagens",
  "{{count}} unread_one": "{{count}} não lido",
  "{{count}} unread_many": "{{count}} não lidos",
  "{{count}} unread_other": "{{count}} não lidos",
  "{{count}} votes_one": "{{count}} voto",
  "{{count}} votes_many": "{{count}} votos",
  "{{count}} votes_other": "{{count}} votos",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} opção a mais",
  "+{{count}} more options_many": "+{{count}} opções a mais",
  "+{{count}} more options_other": "+{{count}} opções a mais",
  "🏙 Attachment...": "🏙 Anexo...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} criou: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} votou: {{pollOptionText}}",
  "📍Shared location": "📍Localização compartilhada",
  "Add a comment": "Adicionar um comentário",
  "Add an option": "Adicionar uma opção",
  "Add reaction": "Adicionar reação",
  "All results loaded": "Todos os resultados carregados",
  "Allow access to camera": "Permitir acesso à câmera",
  "Allow access to microphone": "Permitir acesso ao microfone",
  "Allow comments": "Permitir comentários",
  "Allow option suggestion": "Permitir sugestão de opção",
  "Allow others to add comments": "Permitir que outros adicionem comentários",
  "Also send as a direct message": "Também enviar como mensagem direta",
  "Also send in channel": "Também enviar no canal",
  "Also sent in channel": "Também enviado no canal",
  "An error has occurred during recording": "Ocorreu um erro durante a gravação",
  "An error has occurred during the recording processing": "Ocorreu um erro durante o processamento da gravação",
  Anonymous: Anonymous$2,
  "Anonymous poll": "Enquete anônima",
  Archive: Archive$2,
  "Are you sure you want to delete this message?": "Tem certeza de que deseja excluir esta mensagem?",
  "aria/Attachment": "Anexo",
  "aria/Block User": "Bloquear usuário",
  "aria/Bookmark Message": "Marcar mensagem",
  "aria/Cancel Reply": "Cancelar resposta",
  "aria/Cancel upload": "Cancelar upload",
  "aria/Channel list": "Lista de canais",
  "aria/Channel search results": "Resultados de pesquisa de canais",
  "aria/Close thread": "Fechar tópico",
  "aria/Collapse sidebar": "Recolher barra lateral",
  "aria/Copy Message Text": "Copiar texto da mensagem",
  "aria/Delete Message": "Excluir mensagem",
  "aria/Download attachment": "Baixar anexo",
  "aria/Edit Message": "Editar Mensagem",
  "aria/Emoji picker": "Seletor de emojis",
  "aria/Expand sidebar": "Expandir barra lateral",
  "aria/File input": "Entrada de arquivo",
  "aria/File upload": "Carregar arquivo",
  "aria/Flag Message": "Reportar mensagem",
  "aria/Image failed to load": "Falha ao carregar a imagem",
  "aria/Image input": "Entrada de imagem",
  "aria/Load More Channels": "Carregar mais canais",
  "aria/Mark Message Unread": "Marcar como não lida",
  "aria/Menu": "Menu",
  "aria/Message Options": "Opções de mensagem",
  "aria/Mute User": "Silenciar usuário",
  "aria/Open Attachment Selector": "Abrir seletor de anexos",
  "aria/Open Menu": "Abrir menu",
  "aria/Open Message Actions Menu": "Abrir menu de ações de mensagem",
  "aria/Open Reaction Selector": "Abrir seletor de reações",
  "aria/Open Thread": "Abrir tópico",
  "aria/Percent complete": "{{percent}} por cento concluído",
  "aria/Pin Message": "Fixar mensagem",
  "aria/Quote Message": "Citar mensagem",
  "aria/Reaction list": "Lista de reações",
  "aria/Remind Me Message": "Lembrar-me",
  "aria/Remind Me Options": "Opções de lembrete",
  "aria/Remove attachment": "Remover anexo",
  "aria/Remove location attachment": "Remover anexo de localização",
  "aria/Remove Reminder": "Remover lembrete",
  "aria/Remove Save For Later": "Remover Salvar para depois",
  "aria/Resend Message": "Reenviar mensagem",
  "aria/Retry upload": "Tentar upload novamente",
  "aria/Search results": "Resultados da pesquisa",
  "aria/Search results header filter button": "Botão de filtro do cabeçalho dos resultados da pesquisa",
  "aria/Send": "Enviar",
  "aria/Show preview": "Mostrar prévia",
  "aria/Stop AI Generation": "Parar geração de IA",
  "aria/Unblock User": "Desbloquear usuário",
  "aria/Unmute User": "Ativar som",
  "aria/Unpin Message": "Desfixar mensagem",
  "Ask a question": "Faça uma pergunta",
  Attach: Attach$2,
  "Attach files": "Anexar arquivos",
  Attachment: Attachment$3,
  "Attachment upload blocked due to {{reason}}": "Upload de anexo bloqueado devido a {{reason}}",
  "Attachment upload failed due to {{reason}}": "Upload de anexo falhou devido a {{reason}}",
  Back: Back$2,
  "ban-command-args": "[@nomedeusuário] [texto]",
  "ban-command-description": "Banir um usuário",
  "Block User": "Bloquear usuário",
  Cancel: Cancel$2,
  "Cannot seek in the recording": "Não é possível buscar na gravação",
  "Channel archived": "Canal arquivado",
  "Channel Missing": "Canal ausente",
  "Channel muted": "Canal silenciado",
  "Channel pinned": "Canal fixado",
  "Channel unarchived": "Canal desarquivado",
  "Channel unmuted": "Silêncio do canal desativado",
  "Channel unpinned": "Canal desafixado",
  Channels: Channels$2,
  Chats: Chats$2,
  "Choose between 2 to 10 options": "Escolha entre 2 a 10 opções",
  Close: Close$2,
  "Close emoji picker": "Fechar seletor de emoji",
  Commands: Commands$2,
  "Commands matching": "Comandos correspondentes",
  "Connection failure, reconnecting now...": "Falha de conexão, reconectando agora...",
  "Copy Message": "Copiar mensagem",
  Create: Create$2,
  "Create poll": "Criar enquete",
  "Current location": "Localização atual",
  Delete: Delete$2,
  "Delete for me": "Excluir para mim",
  "Delete message": "Excluir mensagem",
  Delivered: Delivered$2,
  "Direct message": "Mensagem direta",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Deseja encerrar esta enquete agora? Ninguém poderá mais votar nesta enquete.",
  "Download All": "Baixar tudo",
  "Download Attachment": "Baixar anexo",
  "Download attachment {{ name }}": "Baixar anexo {{ name }}",
  "Drag your files here": "Arraste seus arquivos aqui",
  "Drag your files here to add to your post": "Arraste seus arquivos aqui para adicionar ao seu post",
  "Due {{ timeLeft }}": "Vence em {{ timeLeft }}",
  "Due since {{ dueSince }}": "Vencido desde {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Editar Mensagem",
  "Edit message request failed": "O pedido de edição da mensagem falhou",
  Edited: Edited$2,
  "Emoji matching": "Emoji correspondente",
  "Empty message...": "Mensagem vazia...",
  End: End$2,
  "End poll": "Encerrar enquete",
  "End this poll?": "Encerrar esta enquete?",
  "End vote": "Encerrar votação",
  "Enforce unique vote is enabled": "Voto único está habilitado",
  "Error": "Erro",
  "Error adding flag": "Erro ao reportar",
  "Error connecting to chat, refresh the page to try again.": "Erro ao conectar ao bate-papo, atualize a página para tentar novamente.",
  "Error deleting message": "Erro ao deletar mensagem",
  "Error fetching reactions": "Erro ao carregar reações",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Erro ao marcar a mensagem como não lida. Não é possível marcar mensagens não lidas mais antigas do que as 100 mensagens mais recentes do canal.",
  "Error muting a user ...": "Erro ao silenciar um usuário...",
  "Error pinning message": "Erro ao fixar mensagem",
  "Error removing message pin": "Erro ao remover o PIN da mensagem",
  "Error reproducing the recording": "Erro ao reproduzir a gravação",
  "Error starting recording": "Erro ao iniciar a gravação",
  "Error unmuting a user ...": "Erro ao ativar o som de um usuário...",
  "Error uploading attachment": "Erro ao carregar o anexo",
  "Error uploading file": "Erro ao enviar arquivo",
  "Error uploading image": "Erro ao carregar a imagem",
  "Error: {{ errorMessage }}": "Erro: {{ errorMessage }}",
  "Failed to block user": "Falha ao bloquear o usuário",
  "Failed to create the poll": "Falha ao criar a pesquisa",
  "Failed to create the poll due to {{reason}}": "Falha ao criar a enquete devido a {{reason}}",
  "Failed to delete the message": "Falha ao excluir a mensagem",
  "Failed to end the poll": "Falha ao encerrar a enquete",
  "Failed to end the poll due to {{reason}}": "Falha ao encerrar a enquete devido a {{reason}}",
  "Failed to jump to the first unread message": "Falha ao pular para a primeira mensagem não lida",
  "Failed to leave channel": "Falha ao sair do canal",
  "Failed to mark channel as read": "Falha ao marcar o canal como lido",
  "Failed to play the recording": "Falha ao reproduzir a gravação",
  "Failed to retrieve location": "Falha ao obter localização",
  "Failed to share location": "Falha ao compartilhar localização",
  "Failed to update channel archive status": "Falha ao atualizar o status de arquivamento do canal",
  "Failed to update channel mute status": "Falha ao atualizar o status de mudo do canal",
  "Failed to update channel pinned status": "Falha ao atualizar o status de fixação do canal",
  File: File$3,
  "File is required for upload attachment": "Arquivo é necessário para enviar o anexo",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "O arquivo é muito grande: {{ size }}, o tamanho máximo de upload é {{ limit }}",
  "File too large": "Arquivo muito grande",
  fileCount_one: fileCount_one$2,
  fileCount_many: fileCount_many$1,
  fileCount_other: fileCount_other$2,
  Flag: Flag$2,
  "Generating...": "Gerando...",
  "giphy-command-args": "[texto]",
  "giphy-command-description": "Postar um gif aleatório no canal",
  "Hide who voted": "Ocultar quem votou",
  Image: Image$3,
  imageCount_one: imageCount_one$2,
  imageCount_many: imageCount_many$1,
  imageCount_other: imageCount_other$2,
  "Instant commands": "Comandos instantâneos",
  "language/af": "Africâner",
  "language/am": "Amárico",
  "language/ar": "Árabe",
  "language/az": "Azerbaijano",
  "language/bg": "Búlgaro",
  "language/bn": "Bengali",
  "language/bs": "Bósnio",
  "language/cs": "Tcheco",
  "language/da": "Dinamarquês",
  "language/de": "Alemão",
  "language/el": "Grego",
  "language/en": "Inglês",
  "language/es": "Espanhol",
  "language/es-MX": "Espanhol (México)",
  "language/et": "Estoniano",
  "language/fa": "Persa",
  "language/fa-AF": "Dari",
  "language/fi": "Finlandês",
  "language/fr": "Francês",
  "language/fr-CA": "Francês (Canadá)",
  "language/ha": "Hauçá",
  "language/he": "Hebraico",
  "language/hi": "Hindi",
  "language/hr": "Croata",
  "language/ht": "Crioulo haitiano",
  "language/hu": "Húngaro",
  "language/id": "Indonésio",
  "language/it": "Italiano",
  "language/ja": "Japonês",
  "language/ka": "Georgiano",
  "language/ko": "Coreano",
  "language/lt": "Lituano",
  "language/lv": "Letão",
  "language/ms": "Malaio",
  "language/nl": "Holandês",
  "language/no": "Norueguês",
  "language/pl": "Polonês",
  "language/ps": "Pashto",
  "language/pt": "Português",
  "language/ro": "Romeno",
  "language/ru": "Russo",
  "language/sk": "Eslovaco",
  "language/sl": "Esloveno",
  "language/so": "Somali",
  "language/sq": "Albanês",
  "language/sr": "Sérvio",
  "language/sv": "Sueco",
  "language/sw": "Suaíli",
  "language/ta": "Tâmil",
  "language/th": "Tailandês",
  "language/tl": "Tagalo",
  "language/tr": "Turco",
  "language/uk": "Ucraniano",
  "language/ur": "Urdu",
  "language/vi": "Vietnamita",
  "language/zh": "Chinês (simplificado)",
  "language/zh-TW": "Chinês (tradicional)",
  "Leave Channel": "Sair do canal",
  "Left channel": "Canal abandonado",
  "Let others add options": "Permitir que outros adicionem opções",
  "Limit votes per person": "Limitar votos por pessoa",
  Link: Link$2,
  linkCount_one: linkCount_one$2,
  linkCount_many: linkCount_many$1,
  linkCount_other: linkCount_other$2,
  live: live$2,
  "Live for {{duration}}": "Ao vivo por {{duration}}",
  "Live location": "Localização ao vivo",
  "Live until {{ timestamp }}": "Ao vivo até {{ timestamp }}",
  "Load more": "Carregar mais",
  "Local upload attachment missing local id": "Anexo de envio local sem id local",
  Location: Location$2,
  "Location sharing ended": "Compartilhamento de localização encerrado",
  "Location: {{ coordinates }}": "Localização: {{ coordinates }}",
  "Mark as unread": "Marcar como não lida",
  "Maximum number of votes (from 2 to 10)": "Número máximo de votos (de 2 a 10)",
  Menu: Menu$2,
  "Message deleted": "Mensagem apagada",
  "Message failed to send": "Falha ao enviar a mensagem",
  "Message has been successfully flagged": "A mensagem foi reportada com sucesso",
  "Message marked as unread": "Mensagem marcada como não lida",
  "Message pinned": "Mensagem fixada",
  "Message unpinned": "Mensagem desafixada",
  "Message was blocked by moderation policies": "A mensagem foi bloqueada pelas políticas de moderação",
  "Messages have been marked unread.": "Mensagens foram marcadas como não lidas.",
  "Missing permissions to upload the attachment": "Faltando permissões para enviar o anexo",
  "Multiple votes": "Votos múltiplos",
  Mute: Mute$2,
  "mute-command-args": "[@nomedeusuário]",
  "mute-command-description": "Silenciar um usuário",
  "network error": "erro de rede",
  New: New$2,
  "New Messages!": "Novas Mensagens!",
  "Next image": "Próxima imagem",
  "No chats here yet…": "Ainda não há conversas aqui...",
  "No conversations yet": "Ainda não há conversas",
  "No items exist": "Não existem itens",
  "No results found": "Nenhum resultado encontrado",
  "Nobody will be able to vote in this poll anymore.": "Ninguém mais poderá votar nesta pesquisa.",
  "Nothing yet...": "Nada ainda...",
  Offline: Offline$2,
  Ok: Ok$2,
  Online: Online$2,
  "Only numbers are allowed": "Apenas números são permitidos",
  "Only visible to you": "Visível apenas para você",
  "Open emoji picker": "Abrir seletor de emoji",
  "Open gallery at image {{ index }}": "Abrir galeria na imagem {{ index }}",
  "Open image in gallery": "Abrir imagem na galeria",
  "Option already exists": "Opção já existe",
  "Option is empty": "A opção está vazia",
  Options: Options$2,
  Original: Original$2,
  "People matching": "Pessoas correspondentes",
  Photo: Photo$2,
  Pin: Pin$2,
  "Pinned by {{ name }}": "Fixado por {{ name }}",
  "Pinned by You": "Fixado por você",
  "placeholder/PollComment": "O seu comentário",
  "placeholder/PollOptionSuggestion": "Introduza uma nova opção",
  "Play video": "Reproduzir vídeo",
  Poll: Poll$3,
  "Poll comments": "Comentários da pesquisa",
  "Poll ended": "Enquete encerrada",
  "Poll options": "Opções da pesquisa",
  "Poll results": "Resultados da pesquisa",
  "Previous image": "Imagem anterior",
  Question: Question$2,
  "Question {{ optionOrderNumber}}": "Pergunta {{ optionOrderNumber}}",
  "Question is required": "A pergunta é obrigatória",
  "Quote Reply": "Responder com citação",
  "Reached the vote limit. Remove an existing vote first.": "Limite de votos atingido. Remova um voto existente primeiro.",
  "Recording format is not supported and cannot be reproduced": "Formato de gravação não é suportado e não pode ser reproduzido",
  "Remind me": "Lembrar-me",
  "Remind Me": "Lembrar-me",
  "Reminder set": "Lembrete definido",
  "Remove reminder": "Remover lembrete",
  "Remove save for later": "Remover Salvar para depois",
  "Replied to a thread": "Respondeu em um tópico",
  Reply: Reply$2,
  "Reply to {{ authorName }}": "Responder a {{ authorName }}",
  "Reply to a message to start a thread": "Responda a uma mensagem para iniciar um thread",
  "Reply to Message": "Responder à mensagem",
  replyCount_one: replyCount_one$2,
  replyCount_many: replyCount_many$1,
  replyCount_other: replyCount_other$2,
  Resend: Resend$2,
  "Retry upload": "Tentar enviar novamente",
  "Save for later": "Salvar para depois",
  "Saved for later": "Salvo para depois",
  Search: Search$3,
  "Search GIFs": "Pesquisar GIFs",
  "search-results-header-filter-source-button-label--channels": "canais",
  "search-results-header-filter-source-button-label--messages": "mensagens",
  "search-results-header-filter-source-button-label--users": "usuários",
  "Searching for {{ searchSourceType }}...": "Buscando {{ searchSourceType }}...",
  "Searching...": "Buscando...",
  searchResultsCount_one: searchResultsCount_one$2,
  searchResultsCount_many: searchResultsCount_many$1,
  searchResultsCount_other: searchResultsCount_other$2,
  "Select a thread to continue the conversation": "Selecione uma thread para continuar a conversa",
  "Select more than one option": "Selecionar mais de uma opção",
  "Select one": "Selecionar um",
  "Select one or more": "Selecionar um ou mais",
  "Select up to {{count}}_one": "Selecionar até {{count}}",
  "Select up to {{count}}_many": "Selecionar até {{count}}",
  "Select up to {{count}}_other": "Selecionar até {{count}}",
  Send: Send$2,
  "Send a message to start the conversation": "Envie uma mensagem para iniciar a conversa",
  "Send Anyway": "Enviar de qualquer forma",
  "Send message request failed": "O pedido de envio da mensagem falhou",
  "Send poll": "Enviar enquete",
  "Sending...": "Enviando...",
  Sent: Sent$2,
  Share: Share$2,
  "Share live location for": "Compartilhar localização ao vivo por",
  "Share Location": "Compartilhar localização",
  "Shared live location": "Localização ao vivo compartilhada",
  "Shared location": "Localização compartilhada",
  Shuffle: Shuffle$2,
  "size limit": "limite de tamanho",
  "Slow Mode ON": "Modo lento LIGADO",
  "Slow mode, wait {{ seconds }}s...": "Modo lento, aguarde {{ seconds }} s...",
  "Some of the files will not be accepted": "Alguns arquivos não serão aceitos",
  "Start typing to search": "Comece a digitar para pesquisar",
  "Stop sharing": "Parar de compartilhar",
  Submit: Submit$2,
  "Suggest an option": "Sugerir uma opção",
  "Tap to remove": "Toque para remover",
  "Thinking...": "Pensando...",
  "this content could not be displayed": "este conteúdo não pôde ser exibido",
  "This field cannot be empty or contain only spaces": "Este campo não pode estar vazio ou conter apenas espaços",
  "This message did not meet our content guidelines": "Esta mensagem não corresponde às nossas diretrizes de conteúdo",
  Thread: Thread$3,
  "Thread has not been found": "Fio não encontrado",
  "Thread reply": "Resposta no fio",
  "Thread Reply": "Resposta no fio",
  "ThreadListUnseenThreadsBanner/loading": "Carregando...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} tópico não lido",
  "ThreadListUnseenThreadsBanner/unreadThreads_many": "{{ count }} tópicos não lidos",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} tópicos não lidos",
  Threads: Threads$2,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Ontem]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Hoje]", "nextDay": "[Amanhã]", "lastDay": "[Ontem]", "nextWeek": "dddd", "lastWeek": "[Último] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Hoje",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Ontem",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Para começar a gravar, permita o acesso à câmera no seu navegador",
  "To start recording, allow the microphone access in your browser": "Para começar a gravar, permita o acesso ao microfone no seu navegador",
  totalVoteCount_one: totalVoteCount_one$2,
  totalVoteCount_many: totalVoteCount_many$1,
  totalVoteCount_other: totalVoteCount_other$2,
  Translated: Translated$2,
  "Translated from {{ language }}": "Traduzido de {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Digite um número de 2 a 10",
  "Type your message": "Digite sua mensagem",
  Unarchive: Unarchive$2,
  "unban-command-args": "[@nomedeusuário]",
  "unban-command-description": "Desbanir um usuário",
  "Unblock User": "Desbloquear usuário",
  "unknown error": "erro desconhecido",
  Unmute: Unmute$2,
  "unmute-command-args": "[@nomedeusuário]",
  "unmute-command-description": "Retirar o silenciamento de um usuário",
  Unpin: Unpin$2,
  "Unread messages": "Mensagens não lidas",
  "Unsupported attachment": "Anexo não suportado",
  "unsupported file type": "tipo de arquivo não suportado",
  Update: Update$2,
  "Update your comment": "Atualizar seu comentário",
  "Upload blocked": "Envio bloqueado",
  "Upload error": "Erro no envio",
  "Upload failed": "Falha no envio",
  'Upload type: "{{ type }}" is not allowed': 'Tipo de upload: "{{ type }}" não é permitido',
  "User blocked": "Usuário bloqueado",
  "User unblocked": "Usuário desbloqueado",
  "User uploaded content": "Conteúdo enviado pelo usuário",
  Video: Video$2,
  videoCount_one: videoCount_one$2,
  videoCount_many: videoCount_many$1,
  videoCount_other: videoCount_other$2,
  View: View$2,
  "View {{count}} comments_one": "Ver {{count}} comentário",
  "View {{count}} comments_many": "Ver {{count}} comentários",
  "View {{count}} comments_other": "Ver {{count}} comentários",
  "View all": "Ver tudo",
  "View original": "Ver original",
  "View results": "Ver resultados",
  "View translation": "Ver tradução",
  "Voice message": "Mensagem de voz",
  "Voice message {{ duration }}": "Mensagem de voz {{ duration }}",
  "Voice message deleted": "Mensagem de voz excluída",
  voiceMessageCount_one: voiceMessageCount_one$2,
  voiceMessageCount_many: voiceMessageCount_many$1,
  voiceMessageCount_other: voiceMessageCount_other$2,
  "Vote ended": "Votação encerrada",
  Votes: Votes$2,
  "Wait until all attachments have uploaded": "Espere até que todos os anexos tenham sido carregados",
  "Waiting for network…": "Aguardando rede…",
  You: You$2,
  "You've reached the maximum number of files": "Você atingiu o número máximo de arquivos"
};
var Anonymous$1 = "Аноним";
var Archive$1 = "Aрхивировать";
var Attach$1 = "Прикрепить";
var Attachment$2 = "Вложение";
var Back$1 = "Назад";
var Cancel$1 = "Отмена";
var Channels$1 = "Каналы";
var Chats$1 = "Чаты";
var Close$1 = "Закрыть";
var Commands$1 = "Команды";
var Create$1 = "Создать";
var Delete$1 = "Удалить";
var Delivered$1 = "Отправлено";
var Edited$1 = "Отредактировано";
var End$1 = "Конец";
var File$2 = "Файл";
var fileCount_four = "{{ count }} файла";
var fileCount_one$1 = "{{ count }} файл";
var fileCount_few = "{{ count }} файла";
var fileCount_many = "{{ count }} файлов";
var fileCount_other$1 = "{{ count }} файлов";
var fileCount_three = "{{ count }} файла";
var fileCount_two = "{{ count }} файла";
var Flag$1 = "Пожаловаться";
var Image$2 = "Изображение";
var imageCount_one$1 = "{{ count }} изображение";
var imageCount_few = "{{ count }} изображения";
var imageCount_many = "{{ count }} изображений";
var imageCount_other$1 = "{{ count }} изображений";
var Link$1 = "Линк";
var linkCount_one$1 = "{{ count }} линк";
var linkCount_few = "{{ count }} линка";
var linkCount_many = "{{ count }} линков";
var linkCount_other$1 = "{{ count }} линков";
var live$1 = "В прямом эфире";
var Location$1 = "Местоположение";
var Menu$1 = "Меню";
var Mute$1 = "Отключить уведомления";
var New$1 = "Новые";
var Offline$1 = "Не в сети";
var Ok$1 = "Ок";
var Online$1 = "В сети";
var Options$1 = "Варианты";
var Original$1 = "Оригинал";
var Photo$1 = "Фото";
var Pin$1 = "Закрепить";
var Poll$2 = "Опрос";
var Question$1 = "Вопрос";
var Reply$1 = "Ответить";
var replyCount_one$1 = "1 ответ";
var replyCount_few = "{{ count }} ответов";
var replyCount_many = "{{ count }} ответов";
var replyCount_other$1 = "{{ count }} ответов";
var Resend$1 = "Отправить повторно";
var Search$2 = "Поиск";
var searchResultsCount_one$1 = "1 результат";
var searchResultsCount_few = "{{ count }} результата";
var searchResultsCount_many = "{{ count }} результатов";
var searchResultsCount_other$1 = "{{ count }} результатов";
var Send$1 = "Отправить";
var Sent$1 = "Отправлено";
var Share$1 = "Поделиться";
var Shuffle$1 = "Перемешать";
var Submit$1 = "Отправить";
var Thread$2 = "Ветка";
var Threads$1 = "Треды";
var totalVoteCount_one$1 = "1 голос всего";
var totalVoteCount_few = "{{ count }} голоса всего";
var totalVoteCount_many = "{{ count }} голосов всего";
var totalVoteCount_other$1 = "{{ count }} голосов всего";
var Translated$1 = "Переведено";
var Unarchive$1 = "Удалить из архива";
var Unmute$1 = "Включить уведомления";
var Unpin$1 = "Открепить";
var Update$1 = "Обновить";
var Video$1 = "Видео";
var videoCount_one$1 = "{{ count }} видео";
var videoCount_few = "{{ count }} видео";
var videoCount_many = "{{ count }} видео";
var videoCount_other$1 = "{{ count }} видео";
var View$1 = "Просмотр";
var voiceMessageCount_one$1 = "{{ count }} голосовое сообщение";
var voiceMessageCount_few = "{{ count }} голосовых сообщения";
var voiceMessageCount_many = "{{ count }} голосовых сообщений";
var voiceMessageCount_other$1 = "{{ count }} голосовых сообщений";
var Votes$1 = "Голоса";
var You$1 = "Вы";
var ruTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} и {{ moreCount }} еще",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} и {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} файл",
  "{{ count }} files_few": "{{ count }} файла",
  "{{ count }} files_many": "{{ count }} файлов",
  "{{ count }} files_other": "{{ count }} файла",
  "{{ count }} people are typing_one": "{{ count }} человек печатает",
  "{{ count }} people are typing_few": "{{ count }} человека печатают",
  "{{ count }} people are typing_many": "{{ count }} человек печатают",
  "{{ count }} people are typing_other": "{{ count }} человека печатают",
  "{{ count }} photos_one": "{{ count }} фото",
  "{{ count }} photos_few": "{{ count }} фото",
  "{{ count }} photos_many": "{{ count }} фото",
  "{{ count }} photos_other": "{{ count }} фото",
  "{{ count }} reactions_one": "{{ count }} реакция",
  "{{ count }} reactions_few": "{{ count }} реакции",
  "{{ count }} reactions_many": "{{ count }} реакций",
  "{{ count }} reactions_other": "{{ count }} реакций",
  "{{ count }} videos_one": "{{ count }} видео",
  "{{ count }} videos_few": "{{ count }} видео",
  "{{ count }} videos_many": "{{ count }} видео",
  "{{ count }} videos_other": "{{ count }} видео",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} и {{ secondUser }}",
  "{{ imageCount }} more": "Ещё {{ imageCount }}",
  "{{ memberCount }} members": "{{ memberCount }} участников",
  "{{ typing }} are typing": "{{ typing }} печатают",
  "{{ typing }} is typing": "{{ typing }} печатает",
  "{{ user }} has been muted": "Вы отписались от уведомлений от {{ user }}",
  "{{ user }} has been unmuted": "Уведомления от {{ user }} были включены",
  "{{ user }} is typing...": "{{ user }} печатает...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} и {{ user }} печатают...",
  "{{ users }} and more are typing...": "{{ users }} и другие печатают...",
  "{{ watcherCount }} online": "{{ watcherCount }} в сети",
  "{{count}} new messages_one": "{{count}} новое сообщение",
  "{{count}} new messages_few": "{{count}} новых сообщения",
  "{{count}} new messages_many": "{{count}} новых сообщений",
  "{{count}} new messages_other": "{{count}} новых сообщений",
  "{{count}} unread_one": "{{count}} непрочитанное",
  "{{count}} unread_few": "{{count}} непрочитанных",
  "{{count}} unread_many": "{{count}} непрочитанных",
  "{{count}} unread_other": "{{count}} непрочитанных",
  "{{count}} votes_one": "{{count}} голос",
  "{{count}} votes_few": "{{count}} голоса",
  "{{count}} votes_many": "{{count}} голосов",
  "{{count}} votes_other": "{{count}} голосов",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+ещё {{count}} вариант",
  "+{{count}} more options_few": "+ещё {{count}} варианта",
  "+{{count}} more options_many": "+ещё {{count}} вариантов",
  "+{{count}} more options_other": "+ещё {{count}} вариантов",
  "🏙 Attachment...": "🏙 Вложение...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} создал(а): {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} проголосовал(а): {{pollOptionText}}",
  "📍Shared location": "📍Общее местоположение",
  "Add a comment": "Добавить комментарий",
  "Add an option": "Добавить вариант",
  "Add reaction": "Добавить реакцию",
  "All results loaded": "Все результаты загружены",
  "Allow access to camera": "Разрешить доступ к камере",
  "Allow access to microphone": "Разрешить доступ к микрофону",
  "Allow comments": "Разрешить комментарии",
  "Allow option suggestion": "Разрешить предложение вариантов",
  "Allow others to add comments": "Разрешить другим добавлять комментарии",
  "Also send as a direct message": "Также отправить как личное сообщение",
  "Also send in channel": "Также отправить в канал",
  "Also sent in channel": "Также отправлено в канал",
  "An error has occurred during recording": "Произошла ошибка во время записи",
  "An error has occurred during the recording processing": "Произошла ошибка во время обработки записи",
  Anonymous: Anonymous$1,
  "Anonymous poll": "Анонимный опрос",
  Archive: Archive$1,
  "Are you sure you want to delete this message?": "Вы уверены, что хотите удалить это сообщение?",
  "aria/Attachment": "Вложение",
  "aria/Block User": "Заблокировать пользователя",
  "aria/Bookmark Message": "Сохранить сообщение",
  "aria/Cancel Reply": "Отменить ответ",
  "aria/Cancel upload": "Отменить загрузку",
  "aria/Channel list": "Список каналов",
  "aria/Channel search results": "Результаты поиска по каналам",
  "aria/Close thread": "Закрыть тему",
  "aria/Collapse sidebar": "Свернуть боковую панель",
  "aria/Copy Message Text": "Копировать текст сообщения",
  "aria/Delete Message": "Удалить сообщение",
  "aria/Download attachment": "Скачать вложение",
  "aria/Edit Message": "Редактировать сообщение",
  "aria/Emoji picker": "Выбор эмодзи",
  "aria/Expand sidebar": "Развернуть боковую панель",
  "aria/File input": "Ввод файла",
  "aria/File upload": "Загрузка файла",
  "aria/Flag Message": "Пожаловаться на сообщение",
  "aria/Image failed to load": "Не удалось загрузить изображение",
  "aria/Image input": "Ввод изображения",
  "aria/Load More Channels": "Загрузить больше каналов",
  "aria/Mark Message Unread": "Отметить как непрочитанное",
  "aria/Menu": "Меню",
  "aria/Message Options": "Параметры сообщения",
  "aria/Mute User": "Отключить уведомления",
  "aria/Open Attachment Selector": "Открыть выбор вложений",
  "aria/Open Menu": "Открыть меню",
  "aria/Open Message Actions Menu": "Открыть меню действий с сообщениями",
  "aria/Open Reaction Selector": "Открыть селектор реакций",
  "aria/Open Thread": "Открыть тему",
  "aria/Percent complete": "{{percent}} процентов завершено",
  "aria/Pin Message": "Закрепить сообщение",
  "aria/Quote Message": "Цитировать сообщение",
  "aria/Reaction list": "Список реакций",
  "aria/Remind Me Message": "Напомнить мне",
  "aria/Remind Me Options": "Параметры напоминания",
  "aria/Remove attachment": "Удалить вложение",
  "aria/Remove location attachment": "Удалить вложение местоположения",
  "aria/Remove Reminder": "Удалить напоминание",
  "aria/Remove Save For Later": "Удалить «Сохранить на потом»",
  "aria/Resend Message": "Отправить сообщение повторно",
  "aria/Retry upload": "Повторить загрузку",
  "aria/Search results": "Результаты поиска",
  "aria/Search results header filter button": "Кнопка фильтра заголовка результатов поиска",
  "aria/Send": "Отправить",
  "aria/Show preview": "Показать предпросмотр",
  "aria/Stop AI Generation": "Остановить генерацию ИИ",
  "aria/Unblock User": "Разблокировать пользователя",
  "aria/Unmute User": "Включить уведомления",
  "aria/Unpin Message": "Открепить сообщение",
  "Ask a question": "Задать вопрос",
  Attach: Attach$1,
  "Attach files": "Прикрепить файлы",
  Attachment: Attachment$2,
  "Attachment upload blocked due to {{reason}}": "Загрузка вложения заблокирована из-за {{reason}}",
  "Attachment upload failed due to {{reason}}": "Загрузка вложения не удалась из-за {{reason}}",
  Back: Back$1,
  "ban-command-args": "[@имяпользователя] [текст]",
  "ban-command-description": "Заблокировать пользователя",
  "Block User": "Заблокировать пользователя",
  Cancel: Cancel$1,
  "Cannot seek in the recording": "Невозможно осуществить поиск в записи",
  "Channel archived": "Канал в архиве",
  "Channel Missing": "Канал не найден",
  "Channel muted": "Канал заглушён",
  "Channel pinned": "Канал закреплён",
  "Channel unarchived": "Канал извлечён из архива",
  "Channel unmuted": "Заглушение канала снято",
  "Channel unpinned": "Канал откреплён",
  Channels: Channels$1,
  Chats: Chats$1,
  "Choose between 2 to 10 options": "Выберите от 2 до 10 вариантов",
  Close: Close$1,
  "Close emoji picker": "Закрыть окно выбора смайлов",
  Commands: Commands$1,
  "Commands matching": "Соответствие команд",
  "Connection failure, reconnecting now...": "Ошибка соединения, переподключение...",
  "Copy Message": "Копировать сообщение",
  Create: Create$1,
  "Create poll": "Создать опрос",
  "Current location": "Текущее местоположение",
  Delete: Delete$1,
  "Delete for me": "Удалить для меня",
  "Delete message": "Удалить сообщение",
  Delivered: Delivered$1,
  "Direct message": "Личное сообщение",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Вы хотите завершить этот опрос сейчас? После этого никто не сможет голосовать в этом опросе.",
  "Download All": "Скачать всё",
  "Download Attachment": "Скачать вложение",
  "Download attachment {{ name }}": "Скачать вложение {{ name }}",
  "Drag your files here": "Перетащите ваши файлы сюда",
  "Drag your files here to add to your post": "Перетащите ваши файлы сюда, чтобы добавить их в ваш пост",
  "Due {{ timeLeft }}": "Просрочено в {{ timeLeft }}",
  "Due since {{ dueSince }}": "Просрочено с {{ dueSince }}",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Редактировать сообщение",
  "Edit message request failed": "Не удалось изменить запрос сообщения",
  Edited: Edited$1,
  "Emoji matching": "Соответствие эмодзи",
  "Empty message...": "Пустое сообщение...",
  End: End$1,
  "End poll": "Завершить опрос",
  "End this poll?": "Завершить этот опрос?",
  "End vote": "Закончить голосование",
  "Enforce unique vote is enabled": "Уникальное голосование включено",
  "Error": "Ошибка",
  "Error adding flag": "Ошибка добавления флага",
  "Error connecting to chat, refresh the page to try again.": "Ошибка подключения к чату, обновите страницу чтобы попробовать снова.",
  "Error deleting message": "Ошибка при удалении сообщения",
  "Error fetching reactions": "Ошибка при загрузке реакций",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Ошибка при отметке сообщения как непрочитанного. Невозможно отметить как непрочитанные сообщения старше последних 100 сообщений в канале.",
  "Error muting a user ...": "Ошибка отключения уведомлений от пользователя...",
  "Error pinning message": "Сообщение об ошибке при закреплении",
  "Error removing message pin": "Ошибка при удалении булавки сообщения",
  "Error reproducing the recording": "Ошибка воспроизведения записи",
  "Error starting recording": "Ошибка при запуске записи",
  "Error unmuting a user ...": "Ошибка включения уведомлений...",
  "Error uploading attachment": "Ошибка при загрузке вложения",
  "Error uploading file": "Ошибка при загрузке файла",
  "Error uploading image": "Ошибка загрузки изображения",
  "Error: {{ errorMessage }}": "Ошибка: {{ errorMessage }}",
  "Failed to block user": "Не удалось заблокировать пользователя",
  "Failed to create the poll": "Не удалось создать опрос",
  "Failed to create the poll due to {{reason}}": "Не удалось создать опрос из-за {{reason}}",
  "Failed to delete the message": "Не удалось удалить сообщение",
  "Failed to end the poll": "Не удалось завершить опрос",
  "Failed to end the poll due to {{reason}}": "Не удалось завершить опрос из-за {{reason}}",
  "Failed to jump to the first unread message": "Не удалось перейти к первому непрочитанному сообщению",
  "Failed to leave channel": "Не удалось покинуть канал",
  "Failed to mark channel as read": "Не удалось пометить канал как прочитанный",
  "Failed to play the recording": "Не удалось воспроизвести запись",
  "Failed to retrieve location": "Не удалось получить местоположение",
  "Failed to share location": "Не удалось поделиться местоположением",
  "Failed to update channel archive status": "Не удалось обновить статус архивации канала",
  "Failed to update channel mute status": "Не удалось обновить статус отключения звука канала",
  "Failed to update channel pinned status": "Не удалось обновить статус закрепления канала",
  File: File$2,
  "File is required for upload attachment": "Для загрузки вложения требуется файл",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "Файл слишком большой: {{ size }}, максимальный размер загрузки составляет {{ limit }}",
  "File too large": "Файл слишком большой",
  fileCount_four,
  fileCount_one: fileCount_one$1,
  fileCount_few,
  fileCount_many,
  fileCount_other: fileCount_other$1,
  fileCount_three,
  fileCount_two,
  Flag: Flag$1,
  "Generating...": "Генерирую...",
  "giphy-command-args": "[текст]",
  "giphy-command-description": "Опубликовать случайную GIF-анимацию в канале",
  "Hide who voted": "Скрыть, кто голосовал",
  Image: Image$2,
  imageCount_one: imageCount_one$1,
  imageCount_few,
  imageCount_many,
  imageCount_other: imageCount_other$1,
  "Instant commands": "Мгновенные команды",
  "language/af": "Африкаанс",
  "language/am": "Амхарский",
  "language/ar": "Арабский",
  "language/az": "Азербайджанский",
  "language/bg": "Болгарский",
  "language/bn": "Бенгальский",
  "language/bs": "Боснийский",
  "language/cs": "Чешский",
  "language/da": "Датский",
  "language/de": "Немецкий",
  "language/el": "Греческий",
  "language/en": "Английский",
  "language/es": "Испанский",
  "language/es-MX": "Испанский (Мексика)",
  "language/et": "Эстонский",
  "language/fa": "Персидский",
  "language/fa-AF": "Дари",
  "language/fi": "Финский",
  "language/fr": "Французский",
  "language/fr-CA": "Французский (Канада)",
  "language/ha": "Хауса",
  "language/he": "Иврит",
  "language/hi": "Хинди",
  "language/hr": "Хорватский",
  "language/ht": "Гаитянский креольский",
  "language/hu": "Венгерский",
  "language/id": "Индонезийский",
  "language/it": "Итальянский",
  "language/ja": "Японский",
  "language/ka": "Грузинский",
  "language/ko": "Корейский",
  "language/lt": "Литовский",
  "language/lv": "Латышский",
  "language/ms": "Малайский",
  "language/nl": "Нидерландский",
  "language/no": "Норвежский",
  "language/pl": "Польский",
  "language/ps": "Пушту",
  "language/pt": "Португальский",
  "language/ro": "Румынский",
  "language/ru": "Русский",
  "language/sk": "Словацкий",
  "language/sl": "Словенский",
  "language/so": "Сомали",
  "language/sq": "Албанский",
  "language/sr": "Сербский",
  "language/sv": "Шведский",
  "language/sw": "Суахили",
  "language/ta": "Тамильский",
  "language/th": "Тайский",
  "language/tl": "Тагальский",
  "language/tr": "Турецкий",
  "language/uk": "Украинский",
  "language/ur": "Урду",
  "language/vi": "Вьетнамский",
  "language/zh": "Китайский (упрощённый)",
  "language/zh-TW": "Китайский (традиционный)",
  "Leave Channel": "Покинуть канал",
  "Left channel": "Канал покинут",
  "Let others add options": "Разрешить другим добавлять варианты",
  "Limit votes per person": "Ограничить голоса на человека",
  Link: Link$1,
  linkCount_one: linkCount_one$1,
  linkCount_few,
  linkCount_many,
  linkCount_other: linkCount_other$1,
  live: live$1,
  "Live for {{duration}}": "В прямом эфире {{duration}}",
  "Live location": "Местоположение в прямом эфире",
  "Live until {{ timestamp }}": "В прямом эфире до {{ timestamp }}",
  "Load more": "Загрузить больше",
  "Local upload attachment missing local id": "У локального вложения нет локального id",
  Location: Location$1,
  "Location sharing ended": "Обмен местоположением завершен",
  "Location: {{ coordinates }}": "Местоположение: {{ coordinates }}",
  "Mark as unread": "Отметить как непрочитанное",
  "Maximum number of votes (from 2 to 10)": "Максимальное количество голосов (от 2 до 10)",
  Menu: Menu$1,
  "Message deleted": "Сообщение удалено",
  "Message failed to send": "Не удалось отправить сообщение",
  "Message has been successfully flagged": "Жалоба на сообщение была принята",
  "Message marked as unread": "Сообщение помечено как непрочитанное",
  "Message pinned": "Сообщение закреплено",
  "Message unpinned": "Сообщение откреплено",
  "Message was blocked by moderation policies": "Сообщение было заблокировано модерацией",
  "Messages have been marked unread.": "Сообщения были отмечены как непрочитанные.",
  "Missing permissions to upload the attachment": "Отсутствуют разрешения для загрузки вложения",
  "Multiple votes": "Несколько голосов",
  Mute: Mute$1,
  "mute-command-args": "[@имяпользователя]",
  "mute-command-description": "Выключить микрофон у пользователя",
  "network error": "ошибка сети",
  New: New$1,
  "New Messages!": "Новые сообщения!",
  "Next image": "Следующее изображение",
  "No chats here yet…": "Здесь еще нет чатов...",
  "No conversations yet": "Пока нет бесед",
  "No items exist": "Элементов нет",
  "No results found": "Результаты не найдены",
  "Nobody will be able to vote in this poll anymore.": "Никто больше не сможет голосовать в этом опросе.",
  "Nothing yet...": "Пока ничего нет...",
  Offline: Offline$1,
  Ok: Ok$1,
  Online: Online$1,
  "Only numbers are allowed": "Разрешены только цифры",
  "Only visible to you": "Видно только вам",
  "Open emoji picker": "Открыть выбор смайлов",
  "Open gallery at image {{ index }}": "Открыть галерею на изображении {{ index }}",
  "Open image in gallery": "Открыть изображение в галерее",
  "Option already exists": "Вариант уже существует",
  "Option is empty": "Вариант пуст",
  Options: Options$1,
  Original: Original$1,
  "People matching": "Совпадающие люди",
  Photo: Photo$1,
  Pin: Pin$1,
  "Pinned by {{ name }}": "Закреплено: {{ name }}",
  "Pinned by You": "Закреплено вами",
  "placeholder/PollComment": "Ваш комментарий",
  "placeholder/PollOptionSuggestion": "Введите новый вариант",
  "Play video": "Воспроизвести видео",
  Poll: Poll$2,
  "Poll comments": "Комментарии к опросу",
  "Poll ended": "Опрос завершён",
  "Poll options": "Опции опроса",
  "Poll results": "Результаты опроса",
  "Previous image": "Предыдущее изображение",
  Question: Question$1,
  "Question {{ optionOrderNumber}}": "Вопрос {{ optionOrderNumber}}",
  "Question is required": "Вопрос обязателен",
  "Quote Reply": "Ответ со цитатой",
  "Reached the vote limit. Remove an existing vote first.": "Достигнут лимит голосов. Сначала удалите существующий голос.",
  "Recording format is not supported and cannot be reproduced": "Формат записи не поддерживается и не может быть воспроизведен",
  "Remind me": "Напомнить мне",
  "Remind Me": "Напомнить мне",
  "Reminder set": "Напоминание установлено",
  "Remove reminder": "Удалить напоминание",
  "Remove save for later": "Удалить «Сохранить на потом»",
  "Replied to a thread": "Ответил в ветке",
  Reply: Reply$1,
  "Reply to {{ authorName }}": "Ответить {{ authorName }}",
  "Reply to a message to start a thread": "Ответьте на сообщение, чтобы начать тред",
  "Reply to Message": "Ответить на сообщение",
  replyCount_one: replyCount_one$1,
  replyCount_few,
  replyCount_many,
  replyCount_other: replyCount_other$1,
  Resend: Resend$1,
  "Retry upload": "Повторить загрузку",
  "Save for later": "Сохранить на потом",
  "Saved for later": "Сохранено на потом",
  Search: Search$2,
  "Search GIFs": "Поиск GIF",
  "search-results-header-filter-source-button-label--channels": "каналы",
  "search-results-header-filter-source-button-label--messages": "сообщения",
  "search-results-header-filter-source-button-label--users": "пользователи",
  "Searching for {{ searchSourceType }}...": "Поиск {{ searchSourceType }}...",
  "Searching...": "Ищем...",
  searchResultsCount_one: searchResultsCount_one$1,
  searchResultsCount_few,
  searchResultsCount_many,
  searchResultsCount_other: searchResultsCount_other$1,
  "Select a thread to continue the conversation": "Выберите тред, чтобы продолжить беседу",
  "Select more than one option": "Выберите более одного варианта",
  "Select one": "Выберите один",
  "Select one or more": "Выберите один или несколько",
  "Select up to {{count}}_one": "Выберите до {{count}}",
  "Select up to {{count}}_few": "Выберите до {{count}}",
  "Select up to {{count}}_many": "Выберите до {{count}}",
  "Select up to {{count}}_other": "Выберите до {{count}}",
  Send: Send$1,
  "Send a message to start the conversation": "Отправьте сообщение, чтобы начать разговор",
  "Send Anyway": "Мне всё равно, отправить",
  "Send message request failed": "Не удалось отправить запрос на отправку сообщения",
  "Send poll": "Отправить опрос",
  "Sending...": "Отправка...",
  Sent: Sent$1,
  Share: Share$1,
  "Share live location for": "Поделиться местоположением в прямом эфире на",
  "Share Location": "Поделиться местоположением",
  "Shared live location": "Общее местоположение в прямом эфире",
  "Shared location": "Общее местоположение",
  Shuffle: Shuffle$1,
  "size limit": "ограничение размера",
  "Slow Mode ON": "Медленный режим включен",
  "Slow mode, wait {{ seconds }}s...": "Медленный режим: подождите {{ seconds }} с...",
  "Some of the files will not be accepted": "Некоторые файлы не будут приняты",
  "Start typing to search": "Начните вводить для поиска",
  "Stop sharing": "Прекратить делиться",
  Submit: Submit$1,
  "Suggest an option": "Предложить вариант",
  "Tap to remove": "Нажмите, чтобы удалить",
  "Thinking...": "Думаю...",
  "this content could not be displayed": "Этот контент не может быть отображен в данный момент",
  "This field cannot be empty or contain only spaces": "Это поле не может быть пустым или содержать только пробелы",
  "This message did not meet our content guidelines": "Сообщение не соответствует правилам",
  Thread: Thread$2,
  "Thread has not been found": "Ветка не найдена",
  "Thread reply": "Ответ в ветке",
  "Thread Reply": "Ответ в ветке",
  "ThreadListUnseenThreadsBanner/loading": "Загрузка...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} непрочитанная ветка",
  "ThreadListUnseenThreadsBanner/unreadThreads_few": "{{ count }} непрочитанные ветки",
  "ThreadListUnseenThreadsBanner/unreadThreads_many": "{{ count }} непрочитанных веток",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} непрочитанных веток",
  Threads: Threads$1,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Вчера]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Сегодня]", "nextDay": "[Завтра]", "lastDay": "[Вчера]", "nextWeek": "dddd", "lastWeek": "[В прошлый] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Сегодня",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Вчера",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Для начала записи разрешите доступ к камере в вашем браузере",
  "To start recording, allow the microphone access in your browser": "Для начала записи разрешите доступ к микрофону в вашем браузере",
  totalVoteCount_one: totalVoteCount_one$1,
  totalVoteCount_few,
  totalVoteCount_many,
  totalVoteCount_other: totalVoteCount_other$1,
  Translated: Translated$1,
  "Translated from {{ language }}": "Переведено с {{ language }}",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "Введите число от 2 до 10",
  "Type your message": "Ваше сообщение",
  Unarchive: Unarchive$1,
  "unban-command-args": "[@имяпользователя]",
  "unban-command-description": "Разблокировать пользователя",
  "Unblock User": "Разблокировать пользователя",
  "unknown error": "неизвестная ошибка",
  Unmute: Unmute$1,
  "unmute-command-args": "[@имяпользователя]",
  "unmute-command-description": "Включить микрофон у пользователя",
  Unpin: Unpin$1,
  "Unread messages": "Непрочитанные сообщения",
  "Unsupported attachment": "Неподдерживаемое вложение",
  "unsupported file type": "неподдерживаемый тип файла",
  Update: Update$1,
  "Update your comment": "Обновите ваш комментарий",
  "Upload blocked": "Загрузка заблокирована",
  "Upload error": "Ошибка загрузки",
  "Upload failed": "Загрузка не удалась",
  'Upload type: "{{ type }}" is not allowed': 'Тип загрузки: "{{ type }}" не разрешен',
  "User blocked": "Пользователь заблокирован",
  "User unblocked": "Пользователь разблокирован",
  "User uploaded content": "Пользователь загрузил контент",
  Video: Video$1,
  videoCount_one: videoCount_one$1,
  videoCount_few,
  videoCount_many,
  videoCount_other: videoCount_other$1,
  View: View$1,
  "View {{count}} comments_one": "Просмотреть {{count}} комментарий",
  "View {{count}} comments_few": "Просмотреть {{count}} комментариев",
  "View {{count}} comments_many": "Просмотреть {{count}} комментариев",
  "View {{count}} comments_other": "Просмотреть {{count}} комментариев",
  "View all": "Показать все",
  "View original": "Показать оригинал",
  "View results": "Посмотреть результаты",
  "View translation": "Показать перевод",
  "Voice message": "Голосовое сообщение",
  "Voice message {{ duration }}": "Голосовое сообщение {{ duration }}",
  "Voice message deleted": "Голосовое сообщение удалено",
  voiceMessageCount_one: voiceMessageCount_one$1,
  voiceMessageCount_few,
  voiceMessageCount_many,
  voiceMessageCount_other: voiceMessageCount_other$1,
  "Vote ended": "Голосование завершено",
  Votes: Votes$1,
  "Wait until all attachments have uploaded": "Подождите, пока все вложения загрузятся",
  "Waiting for network…": "Ожидание сети…",
  You: You$1,
  "You've reached the maximum number of files": "Вы достигли максимального количества файлов"
};
var Anonymous = "Anonim";
var Archive = "Arşivle";
var Attach = "Ekle";
var Attachment$1 = "Ek";
var Back = "Geri";
var Cancel = "İptal";
var Channels = "Kanallar";
var Chats = "Sohbetler";
var Close = "Kapat";
var Commands = "Komutlar";
var Create = "Oluştur";
var Delete = "Sil";
var Delivered = "İletildi";
var Edited = "Düzenlendi";
var End = "Son";
var File$1 = "Dosya";
var fileCount_one = "1 dosya";
var fileCount_other = "{{ count }} dosya";
var Flag = "Bayrak";
var Image$1 = "Görsel";
var imageCount_one = "Görsel";
var imageCount_other = "{{ count }} görsel";
var Link = "Bağlantı";
var linkCount_one = "Bağlantı";
var linkCount_other = "{{ count }} bağlantı";
var live = "canlı";
var Location = "Konum";
var Menu = "Menü";
var Mute = "Sessiz";
var New = "Yeni";
var Offline = "Çevrimdışı";
var Ok = "Tamam";
var Online = "Çevrimiçi";
var Options = "Seçenekler";
var Original = "Orijinal";
var Photo = "Fotoğraf";
var Pin = "Sabitle";
var Poll$1 = "Anket";
var Question = "Soru";
var Reply = "Cevapla";
var replyCount_one = "1 cevap";
var replyCount_other = "{{ count }} cevap";
var Resend = "Tekrar gönder";
var Search$1 = "Arama";
var searchResultsCount_one = "1 sonuç";
var searchResultsCount_other = "{{ count }} sonuç";
var Send = "Gönder";
var Sent = "Gönderildi";
var Share = "Paylaş";
var Shuffle = "Karıştır";
var Submit = "Gönder";
var Thread$1 = "Konu";
var Threads = "İleti dizileri";
var totalVoteCount_one = "toplam 1 oy";
var totalVoteCount_other = "toplam {{ count }} oy";
var Translated = "Çevrildi";
var Unarchive = "Arşivden çıkar";
var Unmute = "Sesini aç";
var Unpin = "Sabitlemeyi kaldır";
var Update = "Güncelle";
var Video = "Video";
var videoCount_one = "Video";
var videoCount_other = "{{ count }} video";
var View = "Görüntüle";
var voiceMessageCount_one = "Sesli mesaj";
var voiceMessageCount_other = "{{ count }} sesli mesaj";
var Votes = "Oylar";
var You = "Sen";
var trTranslations = {
  "{{ commaSeparatedUsers }} and {{ moreCount }} more": "{{ commaSeparatedUsers }} ve {{ moreCount }} daha",
  "{{ commaSeparatedUsers }}, and {{ lastUser }}": "{{ commaSeparatedUsers }} ve {{ lastUser }}",
  "{{ count }} files_one": "{{ count }} dosya",
  "{{ count }} files_other": "{{ count }} dosya",
  "{{ count }} people are typing_one": "{{ count }} kişi yazıyor",
  "{{ count }} people are typing_many": "{{ count }} kişi yazıyor",
  "{{ count }} people are typing_other": "{{ count }} kişi yazıyor",
  "{{ count }} photos_one": "{{ count }} fotoğraf",
  "{{ count }} photos_other": "{{ count }} fotoğraf",
  "{{ count }} reactions_one": "{{ count }} tepki",
  "{{ count }} reactions_other": "{{ count }} tepki",
  "{{ count }} videos_one": "{{ count }} video",
  "{{ count }} videos_other": "{{ count }} video",
  "{{ firstUser }} and {{ secondUser }}": "{{ firstUser }} ve {{ secondUser }}",
  "{{ imageCount }} more": "{{ imageCount }} adet daha",
  "{{ memberCount }} members": "{{ memberCount }} üye",
  "{{ typing }} are typing": "{{ typing }} yazıyor",
  "{{ typing }} is typing": "{{ typing }} yazıyor",
  "{{ user }} has been muted": "{{ user }} sessize alındı",
  "{{ user }} has been unmuted": "{{ user }} sesi açıldı",
  "{{ user }} is typing...": "{{ user }} yazıyor...",
  "{{ users }} and {{ user }} are typing...": "{{ users }} ve {{ user }} yazıyor...",
  "{{ users }} and more are typing...": "{{ users }} ve diğerleri yazıyor...",
  "{{ watcherCount }} online": "{{ watcherCount }} çevrimiçi",
  "{{count}} new messages_one": "{{count}} yeni mesaj",
  "{{count}} new messages_other": "{{count}} yeni mesaj",
  "{{count}} unread_one": "{{count}} okunmamış",
  "{{count}} unread_other": "{{count}} okunmamış",
  "{{count}} votes_one": "{{count}} oy",
  "{{count}} votes_other": "{{count}} oy",
  "+{{ imageCount }}": "+{{ imageCount }}",
  "+{{count}} more options_one": "+{{count}} seçenek daha",
  "+{{count}} more options_other": "+{{count}} seçenek daha",
  "🏙 Attachment...": "🏙 Ek...",
  "📊 {{createdBy}} created: {{ pollName}}": "📊 {{createdBy}} oluşturdu: {{ pollName}}",
  "📊 {{votedBy}} voted: {{pollOptionText}}": "📊 {{votedBy}} oy verdi: {{pollOptionText}}",
  "📍Shared location": "📍Paylaşılan konum",
  "Add a comment": "Yorum ekle",
  "Add an option": "Bir seçenek ekle",
  "Add reaction": "Tepki ekle",
  "All results loaded": "Tüm sonuçlar yüklendi",
  "Allow access to camera": "Kameraya erişime izin ver",
  "Allow access to microphone": "Mikrofona erişime izin ver",
  "Allow comments": "Yorumlara izin ver",
  "Allow option suggestion": "Seçenek önerisine izin ver",
  "Allow others to add comments": "Diğerlerinin yorum eklemesine izin ver",
  "Also send as a direct message": "Ayrıca doğrudan mesaj olarak gönder",
  "Also send in channel": "Ayrıca kanala gönder",
  "Also sent in channel": "Kanala da gönderildi",
  "An error has occurred during recording": "Kayıt sırasında bir hata oluştu",
  "An error has occurred during the recording processing": "Kayıt işlemi sırasında bir hata oluştu",
  Anonymous,
  "Anonymous poll": "Anonim anket",
  Archive,
  "Are you sure you want to delete this message?": "Bu mesajı silmek istediğinizden emin misiniz?",
  "aria/Attachment": "Ek",
  "aria/Block User": "Kullanıcıyı engelle",
  "aria/Bookmark Message": "Mesajı yer imi ekle",
  "aria/Cancel Reply": "Cevabı İptal Et",
  "aria/Cancel upload": "Yüklemeyi İptal Et",
  "aria/Channel list": "Kanal listesi",
  "aria/Channel search results": "Kanal arama sonuçları",
  "aria/Close thread": "Konuyu kapat",
  "aria/Collapse sidebar": "Kenar çubuğunu daralt",
  "aria/Copy Message Text": "Mesaj metnini kopyala",
  "aria/Delete Message": "Mesajı sil",
  "aria/Download attachment": "Ek indir",
  "aria/Edit Message": "Mesajı Düzenle",
  "aria/Emoji picker": "Emoji seçici",
  "aria/Expand sidebar": "Kenar çubuğunu genişlet",
  "aria/File input": "Dosya girişi",
  "aria/File upload": "Dosya yükleme",
  "aria/Flag Message": "Mesajı bayrakla",
  "aria/Image failed to load": "Görsel yüklenemedi",
  "aria/Image input": "Resim girişi",
  "aria/Load More Channels": "Daha Fazla Kanal Yükle",
  "aria/Mark Message Unread": "Okunmamış olarak işaretle",
  "aria/Menu": "Menü",
  "aria/Message Options": "Mesaj Seçenekleri",
  "aria/Mute User": "Kullanıcıyı sustur",
  "aria/Open Attachment Selector": "Ek Seçiciyi Aç",
  "aria/Open Menu": "Menüyü Aç",
  "aria/Open Message Actions Menu": "Mesaj İşlemleri Menüsünü Aç",
  "aria/Open Reaction Selector": "Tepki Seçiciyi Aç",
  "aria/Open Thread": "Konuyu Aç",
  "aria/Percent complete": "Yüzde {{percent}} tamamlandı",
  "aria/Pin Message": "Mesajı sabitle",
  "aria/Quote Message": "Mesajı alıntıla",
  "aria/Reaction list": "Tepki listesi",
  "aria/Remind Me Message": "Hatırlat",
  "aria/Remind Me Options": "Hatırlatma seçenekleri",
  "aria/Remove attachment": "Eki kaldır",
  "aria/Remove location attachment": "Konum ekini kaldır",
  "aria/Remove Reminder": "Hatırlatıcıyı kaldır",
  "aria/Remove Save For Later": "Sonraya kaydet'i kaldır",
  "aria/Resend Message": "Mesajı tekrar gönder",
  "aria/Retry upload": "Yüklemeyi Tekrar Dene",
  "aria/Search results": "Arama sonuçları",
  "aria/Search results header filter button": "Arama sonuçları başlık filtre düğmesi",
  "aria/Send": "Gönder",
  "aria/Show preview": "Önizlemeyi göster",
  "aria/Stop AI Generation": "Yapay Zeka Üretimini Durdur",
  "aria/Unblock User": "Kullanıcının engelini kaldır",
  "aria/Unmute User": "Sesini aç",
  "aria/Unpin Message": "Sabitlemeyi kaldır",
  "Ask a question": "Bir soru sor",
  Attach,
  "Attach files": "Dosya ekle",
  Attachment: Attachment$1,
  "Attachment upload blocked due to {{reason}}": "{{reason}} nedeniyle ek yükleme engellendi",
  "Attachment upload failed due to {{reason}}": "{{reason}} nedeniyle ek yükleme başarısız oldu",
  Back,
  "ban-command-args": "[@kullanıcıadı] [metin]",
  "ban-command-description": "Bir kullanıcıyı yasakla",
  "Block User": "Kullanıcıyı engelle",
  Cancel,
  "Cannot seek in the recording": "Kayıtta arama yapılamıyor",
  "Channel archived": "Kanal arşivlendi",
  "Channel Missing": "Kanal bulunamıyor",
  "Channel muted": "Kanal sessize alındı",
  "Channel pinned": "Kanal sabitlendi",
  "Channel unarchived": "Kanal arşivden çıkarıldı",
  "Channel unmuted": "Kanal sesi açıldı",
  "Channel unpinned": "Kanal sabitlemesi kaldırıldı",
  Channels,
  Chats,
  "Choose between 2 to 10 options": "2 ile 10 seçenek arasından seçin",
  Close,
  "Close emoji picker": "Emoji seçiciyi kapat",
  Commands,
  "Commands matching": "Eşleşen komutlar",
  "Connection failure, reconnecting now...": "Bağlantı hatası, tekrar bağlanılıyor...",
  "Copy Message": "Mesajı kopyala",
  Create,
  "Create poll": "Anket oluştur",
  "Current location": "Mevcut konum",
  Delete,
  "Delete for me": "Benim için sil",
  "Delete message": "Mesajı sil",
  Delivered,
  "Direct message": "Doğrudan mesaj",
  "Do you want to end this poll now? Nobody will be able to vote in this poll anymore.": "Bu anketi şimdi sonlandırmak istiyor musunuz? Sonlandırdıktan sonra bu ankette kimse oy kullanamayacak.",
  "Download All": "Tümünü indir",
  "Download Attachment": "Eki indir",
  "Download attachment {{ name }}": "Ek {{ name }}'i indir",
  "Drag your files here": "Dosyalarınızı buraya sürükleyin",
  "Drag your files here to add to your post": "Gönderinize eklemek için dosyalarınızı buraya sürükleyin",
  "Due {{ timeLeft }}": "{{ timeLeft }} içinde süresi dolacak",
  "Due since {{ dueSince }}": "{{ dueSince }}'den beri süresi dolmuş",
  "duration/Message reminder": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Remind Me": "{{ milliseconds | durationFormatter(withSuffix: true) }}",
  "duration/Share Location": "{{ milliseconds | durationFormatter }}",
  "Edit Message": "Mesajı Düzenle",
  "Edit message request failed": "Mesaj düzenleme isteği başarısız oldu",
  Edited,
  "Emoji matching": "Emoji eşleştirme",
  "Empty message...": "Boş mesaj...",
  End,
  "End poll": "Anketi sonlandır",
  "End this poll?": "Bu anketi sonlandır?",
  "End vote": "Oyu bitir",
  "Enforce unique vote is enabled": "Benzersiz oy etkinleştirildi",
  "Error": "Hata",
  "Error adding flag": "Bayrak eklenirken hata oluştu",
  "Error connecting to chat, refresh the page to try again.": "Bağlantı hatası, sayfayı yenileyip tekrar deneyin.",
  "Error deleting message": "Mesaj silinirken hata oluştu",
  "Error fetching reactions": "Reaksiyonlar alınırken hata oluştu",
  "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages.": "Mesajı okunmamış olarak işaretleme hatası. En yeni 100 kanal mesajından daha eski okunmamış mesajları işaretleme yapılamaz.",
  "Error muting a user ...": "Kullanıcıyı sessize alırken hata oluştu ...",
  "Error pinning message": "Mesaj sabitlenirken hata oluştu",
  "Error removing message pin": "Mesaj PIN'i kaldırılırken hata oluştu",
  "Error reproducing the recording": "Kaydı yeniden üretme hatası",
  "Error starting recording": "Kayıt başlatılırken hata oluştu",
  "Error unmuting a user ...": "Kullanıcının sesini açarken hata oluştu ...",
  "Error uploading attachment": "Ek yüklenirken hata oluştu",
  "Error uploading file": "Dosya yüklenirken hata oluştu",
  "Error uploading image": "Resmi yüklerken hata",
  "Error: {{ errorMessage }}": "Hata: {{ errorMessage }}",
  "Failed to block user": "Kullanıcı engellenemedi",
  "Failed to create the poll": "Anket oluşturulurken hata oluştu",
  "Failed to create the poll due to {{reason}}": "{{reason}} nedeniyle anket oluşturulamadı",
  "Failed to delete the message": "Mesaj silinemedi",
  "Failed to end the poll": "Anket sonlandırılamadı",
  "Failed to end the poll due to {{reason}}": "{{reason}} nedeniyle anket sonlandırılamadı",
  "Failed to jump to the first unread message": "İlk okunmamış mesaja atlamada hata oluştu",
  "Failed to leave channel": "Kanaldan çıkılamadı",
  "Failed to mark channel as read": "Kanalı okundu olarak işaretleme başarısız oldu",
  "Failed to play the recording": "Kayıt oynatılamadı",
  "Failed to retrieve location": "Konum alınamadı",
  "Failed to share location": "Konum paylaşılamadı",
  "Failed to update channel archive status": "Kanalın arşiv durumu güncellenemedi",
  "Failed to update channel mute status": "Kanalın sessiz durumu güncellenemedi",
  "Failed to update channel pinned status": "Kanalın sabitleme durumu güncellenemedi",
  File: File$1,
  "File is required for upload attachment": "Ek yüklemek için dosya gerekli",
  "File is too large: {{ size }}, maximum upload size is {{ limit }}": "Dosya çok büyük: {{ size }}, maksimum yükleme boyutu {{ limit }}",
  "File too large": "Dosya çok büyük",
  fileCount_one,
  fileCount_other,
  Flag,
  "Generating...": "Oluşturuluyor...",
  "giphy-command-args": "[metin]",
  "giphy-command-description": "Rastgele bir gif'i kanala gönder",
  "Hide who voted": "Kimin oy verdiğini gizle",
  Image: Image$1,
  imageCount_one,
  imageCount_other,
  "Instant commands": "Anlık komutlar",
  "language/af": "Afrikanca",
  "language/am": "Amharca",
  "language/ar": "Arapça",
  "language/az": "Azerice",
  "language/bg": "Bulgarca",
  "language/bn": "Bengalce",
  "language/bs": "Boşnakça",
  "language/cs": "Çekçe",
  "language/da": "Danca",
  "language/de": "Almanca",
  "language/el": "Yunanca",
  "language/en": "İngilizce",
  "language/es": "İspanyolca",
  "language/es-MX": "İspanyolca (Meksika)",
  "language/et": "Estonyaca",
  "language/fa": "Farsça",
  "language/fa-AF": "Darice",
  "language/fi": "Fince",
  "language/fr": "Fransızca",
  "language/fr-CA": "Fransızca (Kanada)",
  "language/ha": "Hausa",
  "language/he": "İbranice",
  "language/hi": "Hintçe",
  "language/hr": "Hırvatça",
  "language/ht": "Haiti Kreolcesi",
  "language/hu": "Macarca",
  "language/id": "Endonezce",
  "language/it": "İtalyanca",
  "language/ja": "Japonca",
  "language/ka": "Gürcüce",
  "language/ko": "Korece",
  "language/lt": "Litvanca",
  "language/lv": "Letonca",
  "language/ms": "Malayca",
  "language/nl": "Felemenkçe",
  "language/no": "Norveççe",
  "language/pl": "Lehçe",
  "language/ps": "Peştuca",
  "language/pt": "Portekizce",
  "language/ro": "Romence",
  "language/ru": "Rusça",
  "language/sk": "Slovakça",
  "language/sl": "Slovence",
  "language/so": "Somalice",
  "language/sq": "Arnavutça",
  "language/sr": "Sırpça",
  "language/sv": "İsveççe",
  "language/sw": "Svahilice",
  "language/ta": "Tamilce",
  "language/th": "Tayca",
  "language/tl": "Tagalogca",
  "language/tr": "Türkçe",
  "language/uk": "Ukraynaca",
  "language/ur": "Urduca",
  "language/vi": "Vietnamca",
  "language/zh": "Çince (basitleştirilmiş)",
  "language/zh-TW": "Çince (geleneksel)",
  "Leave Channel": "Kanaldan ayrıl",
  "Left channel": "Kanaldan ayrıldınız",
  "Let others add options": "Başkalarının seçenek eklemesine izin ver",
  "Limit votes per person": "Kişi başına oy sınırı",
  Link,
  linkCount_one,
  linkCount_other,
  live,
  "Live for {{duration}}": "{{duration}} boyunca canlı",
  "Live location": "Canlı konum",
  "Live until {{ timestamp }}": "{{ timestamp }}'e kadar canlı",
  "Load more": "Daha fazla yükle",
  "Local upload attachment missing local id": "Yerel yükleme ekinde yerel kimlik eksik",
  Location,
  "Location sharing ended": "Konum paylaşımı sona erdi",
  "Location: {{ coordinates }}": "Konum: {{ coordinates }}",
  "Mark as unread": "Okunmamış olarak işaretle",
  "Maximum number of votes (from 2 to 10)": "Maksimum oy sayısı (2 ile 10 arası)",
  Menu,
  "Message deleted": "Mesaj silindi",
  "Message failed to send": "Mesaj gönderilemedi",
  "Message has been successfully flagged": "Mesaj başarıyla bayraklandı",
  "Message marked as unread": "Mesaj okunmadı olarak işaretlendi",
  "Message pinned": "Mesaj sabitlendi",
  "Message unpinned": "Mesaj sabitlemesi kaldırıldı",
  "Message was blocked by moderation policies": "Mesaj moderasyon politikaları tarafından engellendi",
  "Messages have been marked unread.": "Mesajlar okunmamış olarak işaretlendi.",
  "Missing permissions to upload the attachment": "Ek yüklemek için izinler eksik",
  "Multiple votes": "Çoklu oy",
  Mute,
  "mute-command-args": "[@kullanıcıadı]",
  "mute-command-description": "Bir kullanıcının sesini kapat",
  "network error": "ağ hatası",
  New,
  "New Messages!": "Yeni Mesajlar!",
  "Next image": "Sonraki görsel",
  "No chats here yet…": "Henüz burada sohbet yok...",
  "No conversations yet": "Henüz konuşma yok",
  "No items exist": "Hiç öğe yok",
  "No results found": "Sonuç bulunamadı",
  "Nobody will be able to vote in this poll anymore.": "Artık bu ankette kimse oy kullanamayacak.",
  "Nothing yet...": "Şimdilik hiçbir şey...",
  Offline,
  Ok,
  Online,
  "Only numbers are allowed": "Sadece sayılar kullanılabilir",
  "Only visible to you": "Sadece sana görünür",
  "Open emoji picker": "Emoji klavyesini aç",
  "Open gallery at image {{ index }}": "Galeriyi {{ index }}. görselde aç",
  "Open image in gallery": "Görseli galeride aç",
  "Option already exists": "Seçenek zaten mevcut",
  "Option is empty": "Seçenek boş",
  Options,
  Original,
  "People matching": "Eşleşen kişiler",
  Photo,
  Pin,
  "Pinned by {{ name }}": "{{ name }} sabitledi",
  "Pinned by You": "Sizin sabitlediğiniz",
  "placeholder/PollComment": "Yorumunuz",
  "placeholder/PollOptionSuggestion": "Yeni bir seçenek girin",
  "Play video": "Videoyu oynat",
  Poll: Poll$1,
  "Poll comments": "Anket yorumları",
  "Poll ended": "Anket sonlandı",
  "Poll options": "Anket seçenekleri",
  "Poll results": "Anket sonuçları",
  "Previous image": "Önceki görsel",
  Question,
  "Question {{ optionOrderNumber}}": "Soru {{ optionOrderNumber}}",
  "Question is required": "Soru gereklidir",
  "Quote Reply": "Alıntıyla yanıtla",
  "Reached the vote limit. Remove an existing vote first.": "Oylama sınırına ulaşıldı. Önce mevcut bir oyu kaldırın.",
  "Recording format is not supported and cannot be reproduced": "Kayıt formatı desteklenmiyor ve çoğaltılamıyor",
  "Remind me": "Bana hatırlat",
  "Remind Me": "Hatırlat",
  "Reminder set": "Hatırlatıcı ayarlandı",
  "Remove reminder": "Hatırlatıcıyı kaldır",
  "Remove save for later": "Sonraya kaydet'i kaldır",
  "Replied to a thread": "Bir iş parçacığına yanıt verdi",
  Reply,
  "Reply to {{ authorName }}": "{{ authorName }} kişisine yanıt ver",
  "Reply to a message to start a thread": "Bir thread başlatmak için bir mesaja yanıt verin",
  "Reply to Message": "Mesaja Cevapla",
  replyCount_one,
  replyCount_other,
  Resend,
  "Retry upload": "Yüklemeyi yeniden dene",
  "Save for later": "Daha sonra kaydet",
  "Saved for later": "Daha sonra kaydedildi",
  Search: Search$1,
  "Search GIFs": "GIF ara",
  "search-results-header-filter-source-button-label--channels": "kanallar",
  "search-results-header-filter-source-button-label--messages": "mesajlar",
  "search-results-header-filter-source-button-label--users": "kullanıcılar",
  "Searching for {{ searchSourceType }}...": "{{ searchSourceType }} aranıyor...",
  "Searching...": "Aranıyor...",
  searchResultsCount_one,
  searchResultsCount_other,
  "Select a thread to continue the conversation": "Sohbeti sürdürmek için bir ileti dizisi seçin",
  "Select more than one option": "Birden fazla seçenek seçin",
  "Select one": "Birini seçin",
  "Select one or more": "Bir veya daha fazlasını seçin",
  "Select up to {{count}}_one": "En fazla {{count}}'yi seçin",
  "Select up to {{count}}_other": "En fazla {{count}}'yi seçin",
  Send,
  "Send a message to start the conversation": "Sohbete başlamak için bir mesaj gönderin",
  "Send Anyway": "Yine de gönder",
  "Send message request failed": "Mesaj gönderme isteği başarısız oldu",
  "Send poll": "Anketi gönder",
  "Sending...": "Gönderiliyor...",
  Sent,
  Share,
  "Share live location for": "Canlı konum paylaş",
  "Share Location": "Konum Paylaş",
  "Shared live location": "Paylaşılan canlı konum",
  "Shared location": "Paylaşılan konum",
  Shuffle,
  "size limit": "boyut sınırı",
  "Slow Mode ON": "Yavaş Mod Açık",
  "Slow mode, wait {{ seconds }}s...": "Yavaş mod, {{ seconds }} sn bekleyin...",
  "Some of the files will not be accepted": "Bazı dosyalar kabul edilmeyecek",
  "Start typing to search": "Aramak için yazmaya başlayın",
  "Stop sharing": "Paylaşımı durdur",
  Submit,
  "Suggest an option": "Bir seçenek önerin",
  "Tap to remove": "Kaldırmak için dokunun",
  "Thinking...": "Düşünüyor...",
  "this content could not be displayed": "bu içerik gösterilemiyor",
  "This field cannot be empty or contain only spaces": "Bu alan boş olamaz veya sadece boşluk içeremez",
  "This message did not meet our content guidelines": "Bu mesaj içerik yönergelerimize uygun değil",
  Thread: Thread$1,
  "Thread has not been found": "Konu bulunamadı",
  "Thread reply": "Konu yanıtı",
  "Thread Reply": "Konu yanıtı",
  "ThreadListUnseenThreadsBanner/loading": "Yükleniyor...",
  "ThreadListUnseenThreadsBanner/unreadThreads_one": "{{ count }} okunmamış ileti dizisi",
  "ThreadListUnseenThreadsBanner/unreadThreads_other": "{{ count }} okunmamış ileti dizisi",
  Threads,
  "timestamp/ChannelPreviewTimestamp": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "LT", "lastDay": "[Dün]", "lastWeek": "dddd", "sameElse": "L" }) }}',
  "timestamp/DateSeparator": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Bugün]", "nextDay": "[Yarın]", "lastDay": "[Dün]", "nextWeek": "dddd", "lastWeek": "[Geçen] dddd", "sameElse": "ddd, D MMM" }) }}',
  "timestamp/LiveLocation": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/MessageTimestamp": "{{ timestamp | timestampFormatter(calendar: false; format: HH:mm) }}",
  "timestamp/PollVote": "{{ timestamp | timestampFormatter(relativeCompact: true) }}",
  "timestamp/PollVoteTooltip": "{{ timestamp | timestampFormatter(calendar: true) }}",
  "timestamp/relativeDaysAgo": "{{ count }}d ago",
  "timestamp/relativeToday": "Bugün",
  "timestamp/relativeWeeksAgo": "{{ count }}w ago",
  "timestamp/relativeYesterday": "Dün",
  "timestamp/ReminderNotification": '{{ timestamp | timestampFormatter(calendar: true; calendarFormats: { "sameDay": "[Today] [at] HH:mm", "nextDay": "[Tomorrow] [at] HH:mm", "lastDay": "[Yesterday] [at] HH:mm", "nextWeek": "dddd [at] HH:mm", "lastWeek": "[Last] dddd [at] HH:mm", "sameElse": "ddd, D MMM [at] HH:mm" }) }}',
  "timestamp/SystemMessage": "{{ timestamp | timestampFormatter(format: dddd L) }}",
  "To start recording, allow the camera access in your browser": "Kayıt yapmaya başlamak için tarayıcınızda kameraya erişime izin verin",
  "To start recording, allow the microphone access in your browser": "Kayıt yapmaya başlamak için tarayıcınızda mikrofona erişime izin verin",
  totalVoteCount_one,
  totalVoteCount_other,
  Translated,
  "Translated from {{ language }}": "{{ language }} dilinden çevrildi",
  "translationBuilderTopic/notification": "{{value, notification}}",
  "Type a number from 2 to 10": "2 ile 10 arasında bir sayı yazın",
  "Type your message": "Mesajınızı yazın",
  Unarchive,
  "unban-command-args": "[@kullanıcıadı]",
  "unban-command-description": "Bir kullanıcının yasağını kaldır",
  "Unblock User": "Kullanıcının engelini kaldır",
  "unknown error": "bilinmeyen hata",
  Unmute,
  "unmute-command-args": "[@kullanıcıadı]",
  "unmute-command-description": "Bir kullanıcının sesini aç",
  Unpin,
  "Unread messages": "Okunmamış mesajlar",
  "Unsupported attachment": "Desteklenmeyen ek",
  "unsupported file type": "desteklenmeyen dosya türü",
  Update,
  "Update your comment": "Yorumunuzu güncelleyin",
  "Upload blocked": "Yükleme engellendi",
  "Upload error": "Yükleme hatası",
  "Upload failed": "Yükleme başarısız oldu",
  'Upload type: "{{ type }}" is not allowed': 'Yükleme türü: "{{ type }}" izin verilmez',
  "User blocked": "Kullanıcı engellendi",
  "User unblocked": "Kullanıcının engeli kaldırıldı",
  "User uploaded content": "Kullanıcı tarafından yüklenen içerik",
  Video,
  videoCount_one,
  videoCount_other,
  View,
  "View {{count}} comments_one": "{{count}} yorumu görüntüle",
  "View {{count}} comments_other": "{{count}} yorumu görüntüle",
  "View all": "Tümünü görüntüle",
  "View original": "Orijinali görüntüle",
  "View results": "Sonuçları görüntüle",
  "View translation": "Çeviriyi görüntüle",
  "Voice message": "Sesli mesaj",
  "Voice message {{ duration }}": "Sesli mesaj {{ duration }}",
  "Voice message deleted": "Sesli mesaj silindi",
  voiceMessageCount_one,
  voiceMessageCount_other,
  "Vote ended": "Oylama sona erdi",
  Votes,
  "Wait until all attachments have uploaded": "Tüm ekler yüklenene kadar bekleyin",
  "Waiting for network…": "Ağ bekleniyor…",
  You,
  "You've reached the maximum number of files": "Maksimum dosya sayısına ulaştınız"
};
var updateLocale$2 = { exports: {} };
var updateLocale$1 = updateLocale$2.exports;
var hasRequiredUpdateLocale;
function requireUpdateLocale() {
  if (hasRequiredUpdateLocale) return updateLocale$2.exports;
  hasRequiredUpdateLocale = 1;
  (function(module, exports$1) {
    !(function(e, n2) {
      module.exports = n2();
    })(updateLocale$1, function() {
      return function(e, n2, t2) {
        t2.updateLocale = function(e2, n22) {
          var o2 = t2.Ls[e2];
          if (o2) return (n22 ? Object.keys(n22) : []).forEach(function(e3) {
            o2[e3] = n22[e3];
          }), o2;
        };
      };
    });
  })(updateLocale$2);
  return updateLocale$2.exports;
}
var updateLocaleExports = requireUpdateLocale();
var updateLocale = getDefaultExportFromCjs(updateLocaleExports);
var localeData$2 = { exports: {} };
var localeData$1 = localeData$2.exports;
var hasRequiredLocaleData;
function requireLocaleData() {
  if (hasRequiredLocaleData) return localeData$2.exports;
  hasRequiredLocaleData = 1;
  (function(module, exports$1) {
    !(function(n2, t2) {
      module.exports = t2();
    })(localeData$1, function() {
      return function(n2, t2, e) {
        var r3 = function(n22) {
          return n22 && (n22.indexOf ? n22 : n22.s);
        }, o2 = function(n22, t22, e2, o22, u3) {
          var a22 = n22.name ? n22 : n22.$locale(), i2 = r3(a22[t22]), s2 = r3(a22[e2]), d2 = i2 || s2.map(function(n3) {
            return n3.substr(0, o22);
          });
          if (!u3) return d2;
          var f2 = a22.weekStart;
          return d2.map(function(n3, t3) {
            return d2[(t3 + (f2 || 0)) % 7];
          });
        }, u22 = function() {
          return e.Ls[e.locale()];
        }, a2 = function(n22, t22) {
          return n22.formats[t22] || (function(n3) {
            return n3.replace(/(\[[^\]]+])|(MMMM|MM|DD|dddd)/g, function(n4, t3, e2) {
              return t3 || e2.slice(1);
            });
          })(n22.formats[t22.toUpperCase()]);
        };
        t2.prototype.localeData = function() {
          return (function() {
            var n22 = this;
            return { months: function(t22) {
              return t22 ? t22.format("MMMM") : o2(n22, "months");
            }, monthsShort: function(t22) {
              return t22 ? t22.format("MMM") : o2(n22, "monthsShort", "months", 3);
            }, firstDayOfWeek: function() {
              return n22.$locale().weekStart || 0;
            }, weekdays: function(t22) {
              return t22 ? t22.format("dddd") : o2(n22, "weekdays");
            }, weekdaysMin: function(t22) {
              return t22 ? t22.format("dd") : o2(n22, "weekdaysMin", "weekdays", 2);
            }, weekdaysShort: function(t22) {
              return t22 ? t22.format("ddd") : o2(n22, "weekdaysShort", "weekdays", 3);
            }, longDateFormat: function(t22) {
              return a2(n22.$locale(), t22);
            }, meridiem: this.$locale().meridiem, ordinal: this.$locale().ordinal };
          }).bind(this)();
        }, e.localeData = function() {
          var n22 = u22();
          return { firstDayOfWeek: function() {
            return n22.weekStart || 0;
          }, weekdays: function() {
            return e.weekdays();
          }, weekdaysShort: function() {
            return e.weekdaysShort();
          }, weekdaysMin: function() {
            return e.weekdaysMin();
          }, months: function() {
            return e.months();
          }, monthsShort: function() {
            return e.monthsShort();
          }, longDateFormat: function(t22) {
            return a2(n22, t22);
          }, meridiem: n22.meridiem, ordinal: n22.ordinal };
        }, e.months = function() {
          return o2(u22(), "months");
        }, e.monthsShort = function() {
          return o2(u22(), "monthsShort", "months", 3);
        }, e.weekdays = function(n22) {
          return o2(u22(), "weekdays", null, null, n22);
        }, e.weekdaysShort = function(n22) {
          return o2(u22(), "weekdaysShort", "weekdays", 3, n22);
        }, e.weekdaysMin = function(n22) {
          return o2(u22(), "weekdaysMin", "weekdays", 2, n22);
        };
      };
    });
  })(localeData$2);
  return localeData$2.exports;
}
var localeDataExports = requireLocaleData();
var localeData = getDefaultExportFromCjs(localeDataExports);
var relativeTime$2 = { exports: {} };
var relativeTime$1 = relativeTime$2.exports;
var hasRequiredRelativeTime;
function requireRelativeTime() {
  if (hasRequiredRelativeTime) return relativeTime$2.exports;
  hasRequiredRelativeTime = 1;
  (function(module, exports$1) {
    !(function(r3, t2) {
      module.exports = t2();
    })(relativeTime$1, function() {
      return function(r3, t2, e) {
        r3 = r3 || {};
        var n2 = t2.prototype, o2 = { future: "in %s", past: "%s ago", s: "a few seconds", m: "a minute", mm: "%d minutes", h: "an hour", hh: "%d hours", d: "a day", dd: "%d days", M: "a month", MM: "%d months", y: "a year", yy: "%d years" };
        function i2(r22, t22, e2, o22) {
          return n2.fromToBase(r22, t22, e2, o22);
        }
        e.en.relativeTime = o2, n2.fromToBase = function(t22, n22, i22, d22, u22) {
          for (var a2, f2, s2, l2 = i22.$locale().relativeTime || o2, h2 = r3.thresholds || [{ l: "s", r: 44, d: "second" }, { l: "m", r: 89 }, { l: "mm", r: 44, d: "minute" }, { l: "h", r: 89 }, { l: "hh", r: 21, d: "hour" }, { l: "d", r: 35 }, { l: "dd", r: 25, d: "day" }, { l: "M", r: 45 }, { l: "MM", r: 10, d: "month" }, { l: "y", r: 17 }, { l: "yy", d: "year" }], m2 = h2.length, c2 = 0; c2 < m2; c2 += 1) {
            var y2 = h2[c2];
            y2.d && (a2 = d22 ? e(t22).diff(i22, y2.d, true) : i22.diff(t22, y2.d, true));
            var p2 = (r3.rounding || Math.round)(Math.abs(a2));
            if (s2 = a2 > 0, p2 <= y2.r || !y2.r) {
              p2 <= 1 && c2 > 0 && (y2 = h2[c2 - 1]);
              var v2 = l2[y2.l];
              u22 && (p2 = u22("" + p2)), f2 = "string" == typeof v2 ? v2.replace("%d", p2) : v2(p2, n22, y2.l, s2);
              break;
            }
          }
          if (n22) return f2;
          var M2 = s2 ? l2.future : l2.past;
          return "function" == typeof M2 ? M2(f2) : M2.replace("%s", f2);
        }, n2.to = function(r22, t22) {
          return i2(r22, t22, this, true);
        }, n2.from = function(r22, t22) {
          return i2(r22, t22, this);
        };
        var d2 = function(r22) {
          return r22.$u ? e.utc() : e();
        };
        n2.toNow = function(r22) {
          return this.to(d2(this), r22);
        }, n2.fromNow = function(r22) {
          return this.from(d2(this), r22);
        };
      };
    });
  })(relativeTime$2);
  return relativeTime$2.exports;
}
var relativeTimeExports = requireRelativeTime();
var relativeTime = getDefaultExportFromCjs(relativeTimeExports);
var duration$2 = { exports: {} };
var duration$1 = duration$2.exports;
var hasRequiredDuration;
function requireDuration() {
  if (hasRequiredDuration) return duration$2.exports;
  hasRequiredDuration = 1;
  (function(module, exports$1) {
    !(function(t2, s2) {
      module.exports = s2();
    })(duration$1, function() {
      var t2, s2, n2 = /\[([^\]]+)]|Y{1,4}|M{1,4}|D{1,2}|d{1,4}|H{1,2}|h{1,2}|a|A|m{1,2}|s{1,2}|Z{1,2}|SSS/g, i2 = /^(-|\+)?P(?:([-+]?[0-9,.]*)Y)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)W)?(?:([-+]?[0-9,.]*)D)?(?:T(?:([-+]?[0-9,.]*)H)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)S)?)?$/, e = { years: 31536e6, months: 2592e6, days: 864e5, hours: 36e5, minutes: 6e4, seconds: 1e3, milliseconds: 1, weeks: 6048e5 }, r3 = function(t22) {
        return t22 instanceof c2;
      }, o2 = function(t22, s22, n22) {
        return new c2(t22, n22, s22.$l);
      }, u22 = function(t22) {
        return s2.p(t22) + "s";
      }, h2 = function(t22) {
        return t22 < 0;
      }, a2 = function(t22) {
        return h2(t22) ? Math.ceil(t22) : Math.floor(t22);
      }, d2 = function(t22, s22) {
        return t22 ? h2(t22) ? { negative: true, format: "" + (function(t3) {
          return Math.abs(t3);
        })(t22) + s22 } : { negative: false, format: "" + t22 + s22 } : { negative: false, format: "" };
      }, c2 = (function() {
        function h22(t22, s22, n22) {
          var r22 = this;
          if (this.$d = {}, this.$l = n22, s22) return o2(t22 * e[u22(s22)], this);
          if ("number" == typeof t22) return this.$ms = t22, this.parseFromMilliseconds(), this;
          if ("object" == typeof t22) return Object.keys(t22).forEach(function(s3) {
            r22.$d[u22(s3)] = t22[s3];
          }), this.calMilliseconds(), this;
          if ("string" == typeof t22) {
            var h3 = t22.match(i2);
            if (h3) return this.$d.years = h3[2], this.$d.months = h3[3], this.$d.weeks = h3[4], this.$d.days = h3[5], this.$d.hours = h3[6], this.$d.minutes = h3[7], this.$d.seconds = h3[8], this.calMilliseconds(), this;
          }
          return this;
        }
        var c10 = h22.prototype;
        return c10.calMilliseconds = function() {
          var t22 = this;
          this.$ms = Object.keys(this.$d).reduce(function(s22, n22) {
            return s22 + (t22.$d[n22] || 0) * e[n22];
          }, 0);
        }, c10.parseFromMilliseconds = function() {
          var t22 = this.$ms;
          this.$d.years = a2(t22 / 31536e6), t22 %= 31536e6, this.$d.months = a2(t22 / 2592e6), t22 %= 2592e6, this.$d.days = a2(t22 / 864e5), t22 %= 864e5, this.$d.hours = a2(t22 / 36e5), t22 %= 36e5, this.$d.minutes = a2(t22 / 6e4), t22 %= 6e4, this.$d.seconds = a2(t22 / 1e3), t22 %= 1e3, this.$d.milliseconds = t22;
        }, c10.toISOString = function() {
          var t22 = d2(this.$d.years, "Y"), s22 = d2(this.$d.months, "M"), n22 = +this.$d.days || 0;
          this.$d.weeks && (n22 += 7 * this.$d.weeks);
          var i22 = d2(n22, "D"), e2 = d2(this.$d.hours, "H"), r22 = d2(this.$d.minutes, "M"), o22 = this.$d.seconds || 0;
          this.$d.milliseconds && (o22 += this.$d.milliseconds / 1e3);
          var u3 = d2(o22, "S"), h3 = t22.negative || s22.negative || i22.negative || e2.negative || r22.negative || u3.negative, a22 = e2.format || r22.format || u3.format ? "T" : "", c11 = (h3 ? "-" : "") + "P" + t22.format + s22.format + i22.format + a22 + e2.format + r22.format + u3.format;
          return "P" === c11 || "-P" === c11 ? "P0D" : c11;
        }, c10.toJSON = function() {
          return this.toISOString();
        }, c10.format = function(t22) {
          var i22 = t22 || "YYYY-MM-DDTHH:mm:ss", e2 = { Y: this.$d.years, YY: s2.s(this.$d.years, 2, "0"), YYYY: s2.s(this.$d.years, 4, "0"), M: this.$d.months, MM: s2.s(this.$d.months, 2, "0"), D: this.$d.days, DD: s2.s(this.$d.days, 2, "0"), H: this.$d.hours, HH: s2.s(this.$d.hours, 2, "0"), m: this.$d.minutes, mm: s2.s(this.$d.minutes, 2, "0"), s: this.$d.seconds, ss: s2.s(this.$d.seconds, 2, "0"), SSS: s2.s(this.$d.milliseconds, 3, "0") };
          return i22.replace(n2, function(t3, s22) {
            return s22 || String(e2[t3]);
          });
        }, c10.as = function(t22) {
          return this.$ms / e[u22(t22)];
        }, c10.get = function(t22) {
          var s22 = this.$ms, n22 = u22(t22);
          return "milliseconds" === n22 ? s22 %= 1e3 : s22 = "weeks" === n22 ? a2(s22 / e[n22]) : this.$d[n22], 0 === s22 ? 0 : s22;
        }, c10.add = function(t22, s22, n22) {
          var i22;
          return i22 = s22 ? t22 * e[u22(s22)] : r3(t22) ? t22.$ms : o2(t22, this).$ms, o2(this.$ms + i22 * (n22 ? -1 : 1), this);
        }, c10.subtract = function(t22, s22) {
          return this.add(t22, s22, true);
        }, c10.locale = function(t22) {
          var s22 = this.clone();
          return s22.$l = t22, s22;
        }, c10.clone = function() {
          return o2(this.$ms, this);
        }, c10.humanize = function(s22) {
          return t2().add(this.$ms, "ms").locale(this.$l).fromNow(!s22);
        }, c10.milliseconds = function() {
          return this.get("milliseconds");
        }, c10.asMilliseconds = function() {
          return this.as("milliseconds");
        }, c10.seconds = function() {
          return this.get("seconds");
        }, c10.asSeconds = function() {
          return this.as("seconds");
        }, c10.minutes = function() {
          return this.get("minutes");
        }, c10.asMinutes = function() {
          return this.as("minutes");
        }, c10.hours = function() {
          return this.get("hours");
        }, c10.asHours = function() {
          return this.as("hours");
        }, c10.days = function() {
          return this.get("days");
        }, c10.asDays = function() {
          return this.as("days");
        }, c10.weeks = function() {
          return this.get("weeks");
        }, c10.asWeeks = function() {
          return this.as("weeks");
        }, c10.months = function() {
          return this.get("months");
        }, c10.asMonths = function() {
          return this.as("months");
        }, c10.years = function() {
          return this.get("years");
        }, c10.asYears = function() {
          return this.as("years");
        }, h22;
      })();
      return function(n22, i22, e2) {
        t2 = e2, s2 = e2().$utils(), e2.duration = function(t22, s22) {
          var n3 = e2.locale();
          return o2(t22, { $l: n3 }, s22);
        }, e2.isDuration = r3;
        var u3 = i22.prototype.add, h22 = i22.prototype.subtract;
        i22.prototype.add = function(t22, s22) {
          return r3(t22) && (t22 = t22.asMilliseconds()), u3.bind(this)(t22, s22);
        }, i22.prototype.subtract = function(t22, s22) {
          return r3(t22) && (t22 = t22.asMilliseconds()), h22.bind(this)(t22, s22);
        };
      };
    });
  })(duration$2);
  return duration$2.exports;
}
var durationExports = requireDuration();
var duration = getDefaultExportFromCjs(durationExports);
var utc$2 = { exports: {} };
var utc$1 = utc$2.exports;
var hasRequiredUtc;
function requireUtc() {
  if (hasRequiredUtc) return utc$2.exports;
  hasRequiredUtc = 1;
  (function(module, exports$1) {
    !(function(t2, i2) {
      module.exports = i2();
    })(utc$1, function() {
      return function(t2, i2, e) {
        var s2 = i2.prototype;
        e.utc = function(t22) {
          return new i2({ date: t22, utc: true, args: arguments });
        }, s2.utc = function(t22) {
          var i22 = e(this.toDate(), { locale: this.$L, utc: true });
          return t22 ? i22.add(this.utcOffset(), "minute") : i22;
        }, s2.local = function() {
          return e(this.toDate(), { locale: this.$L, utc: false });
        };
        var f2 = s2.parse;
        s2.parse = function(t22) {
          t22.utc && (this.$u = true), this.$utils().u(t22.$offset) || (this.$offset = t22.$offset), f2.call(this, t22);
        };
        var n2 = s2.init;
        s2.init = function() {
          if (this.$u) {
            var t22 = this.$d;
            this.$y = t22.getUTCFullYear(), this.$M = t22.getUTCMonth(), this.$D = t22.getUTCDate(), this.$W = t22.getUTCDay(), this.$H = t22.getUTCHours(), this.$m = t22.getUTCMinutes(), this.$s = t22.getUTCSeconds(), this.$ms = t22.getUTCMilliseconds();
          } else n2.call(this);
        };
        var u22 = s2.utcOffset;
        s2.utcOffset = function(t22, i22) {
          var e2 = this.$utils().u;
          if (e2(t22)) return this.$u ? 0 : e2(this.$offset) ? u22.call(this) : this.$offset;
          var s22 = Math.abs(t22) <= 16 ? 60 * t22 : t22, f22 = this;
          if (i22) return f22.$offset = s22, f22.$u = 0 === t22, f22;
          if (0 !== t22) {
            var n22 = this.$u ? this.toDate().getTimezoneOffset() : -1 * this.utcOffset();
            (f22 = this.local().add(s22 + n22, "minute")).$offset = s22, f22.$x.$localOffset = n22;
          } else f22 = this.utc();
          return f22;
        };
        var o2 = s2.format;
        s2.format = function(t22) {
          var i22 = t22 || (this.$u ? "YYYY-MM-DDTHH:mm:ss[Z]" : "");
          return o2.call(this, i22);
        }, s2.valueOf = function() {
          var t22 = this.$utils().u(this.$offset) ? 0 : this.$offset + (this.$x.$localOffset || (/* @__PURE__ */ new Date()).getTimezoneOffset());
          return this.$d.valueOf() - 6e4 * t22;
        }, s2.isUTC = function() {
          return !!this.$u;
        }, s2.toISOString = function() {
          return this.toDate().toISOString();
        }, s2.toString = function() {
          return this.toDate().toUTCString();
        };
        var r3 = s2.toDate;
        s2.toDate = function(t22) {
          return "s" === t22 && this.$offset ? e(this.format("YYYY-MM-DD HH:mm:ss:SSS")).toDate() : r3.call(this);
        };
        var a2 = s2.diff;
        s2.diff = function(t22, i22, s22) {
          if (t22 && this.$u === t22.$u) return a2.call(this, t22, i22, s22);
          var f22 = this.local(), n22 = e(t22).local();
          return a2.call(f22, n22, i22, s22);
        };
      };
    });
  })(utc$2);
  return utc$2.exports;
}
var utcExports = requireUtc();
var utc = getDefaultExportFromCjs(utcExports);
var timezone$2 = { exports: {} };
var timezone$1 = timezone$2.exports;
var hasRequiredTimezone;
function requireTimezone() {
  if (hasRequiredTimezone) return timezone$2.exports;
  hasRequiredTimezone = 1;
  (function(module, exports$1) {
    !(function(t2, e) {
      module.exports = e();
    })(timezone$1, function() {
      var t2 = { year: 0, month: 1, day: 2, hour: 3, minute: 4, second: 5 }, e = {};
      return function(n2, i2, r3) {
        var o2, u22 = r3().utcOffset(), a2 = function(t22, n22, i22) {
          void 0 === i22 && (i22 = {});
          var r22 = new Date(t22);
          return (function(t3, n3) {
            void 0 === n3 && (n3 = {});
            var i3 = n3.timeZoneName || "short", r32 = t3 + "|" + i3, o22 = e[r32];
            return o22 || (o22 = new Intl.DateTimeFormat("en-US", { hour12: false, timeZone: t3, year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", second: "2-digit", timeZoneName: i3 }), e[r32] = o22), o22;
          })(n22, i22).formatToParts(r22);
        }, f2 = function(e2, n22) {
          for (var i22 = a2(e2, n22), o22 = [], u3 = 0; u3 < i22.length; u3 += 1) {
            var f22 = i22[u3], s22 = f22.type, m22 = f22.value, c2 = t2[s22];
            c2 >= 0 && (o22[c2] = parseInt(m22, 10));
          }
          var d2 = o22[3], v2 = 24 === d2 ? 0 : d2, h2 = o22[0] + "-" + o22[1] + "-" + o22[2] + " " + v2 + ":" + o22[4] + ":" + o22[5] + ":000", l2 = +e2;
          return (r3.utc(h2).valueOf() - (l2 -= l2 % 1e3)) / 6e4;
        }, s2 = i2.prototype;
        s2.tz = function(t22, e2) {
          void 0 === t22 && (t22 = o2);
          var n22 = this.utcOffset(), i22 = this.toDate().toLocaleString("en-US", { timeZone: t22 }), a22 = Math.round((this.toDate() - new Date(i22)) / 1e3 / 60), f22 = r3(i22).$set("millisecond", this.$ms).utcOffset(u22 - a22, true);
          if (e2) {
            var s22 = f22.utcOffset();
            f22 = f22.add(n22 - s22, "minute");
          }
          return f22.$x.$timezone = t22, f22;
        }, s2.offsetName = function(t22) {
          var e2 = this.$x.$timezone || r3.tz.guess(), n22 = a2(this.valueOf(), e2, { timeZoneName: t22 }).find(function(t3) {
            return "timezonename" === t3.type.toLowerCase();
          });
          return n22 && n22.value;
        };
        var m2 = s2.startOf;
        s2.startOf = function(t22, e2) {
          if (!this.$x || !this.$x.$timezone) return m2.call(this, t22, e2);
          var n22 = r3(this.format("YYYY-MM-DD HH:mm:ss:SSS"));
          return m2.call(n22, t22, e2).tz(this.$x.$timezone, true);
        }, r3.tz = function(t22, e2, n22) {
          var i22 = n22 && e2, u3 = n22 || e2 || o2, a22 = f2(+r3(), u3);
          if ("string" != typeof t22) return r3(t22).tz(u3);
          var s22 = (function(t3, e3, n3) {
            var i3 = t3 - 60 * e3 * 1e3, r22 = f2(i3, n3);
            if (e3 === r22) return [i3, e3];
            var o22 = f2(i3 -= 60 * (r22 - e3) * 1e3, n3);
            return r22 === o22 ? [i3, r22] : [t3 - 60 * Math.min(r22, o22) * 1e3, Math.max(r22, o22)];
          })(r3.utc(t22, i22).valueOf(), a22, u3), m22 = s22[0], c2 = s22[1], d2 = r3(m22).utcOffset(c2);
          return d2.$x.$timezone = u3, d2;
        }, r3.tz.guess = function() {
          return Intl.DateTimeFormat().resolvedOptions().timeZone;
        }, r3.tz.setDefault = function(t22) {
          o2 = t22;
        };
      };
    });
  })(timezone$2);
  return timezone$2.exports;
}
var timezoneExports = requireTimezone();
var timezone = getDefaultExportFromCjs(timezoneExports);
var TranslationTopic = class {
  constructor(options) {
    this.options = options;
    this.translators = /* @__PURE__ */ new Map();
    this.setTranslator = (name2, translator) => {
      this.translators.set(name2, translator);
    };
    this.removeTranslator = (name2) => {
      this.translators.delete(name2);
    };
    this.i18next = options.i18next;
    if (options.translators) {
      Object.entries(options.translators).forEach(([name2, translator]) => {
        this.setTranslator(name2, translator);
      });
    }
  }
};
var forwardTranslation = ({ value }) => value;
var TranslationBuilder = class {
  constructor(i18next) {
    this.i18next = i18next;
    this.topics = /* @__PURE__ */ new Map();
    this.translatorRegistrationsBuffer = {};
    this.registerTopic = (name2, Topic) => {
      let topic = this.topics.get(name2);
      if (!topic) {
        topic = new Topic({ i18next: this.i18next });
        this.topics.set(name2, topic);
        this.i18next.use({
          name: name2,
          process: (value, key, options) => {
            const topic2 = this.topics.get(name2);
            if (!topic2) return value;
            return topic2.translate(value, key, options);
          },
          type: "postProcessor"
        });
      }
      const additionalTranslatorsToRegister = this.translatorRegistrationsBuffer[name2];
      if (additionalTranslatorsToRegister) {
        Object.entries(additionalTranslatorsToRegister).forEach(
          ([translatorName, translator]) => {
            topic.setTranslator(translatorName, translator);
          }
        );
        delete this.translatorRegistrationsBuffer[name2];
      }
      return topic;
    };
    this.disableTopic = (topicName) => {
      const topic = this.topics.get(topicName);
      if (!topic) return;
      this.i18next.use({
        name: topicName,
        process: forwardTranslation,
        type: "postProcessor"
      });
      this.topics.delete(topicName);
    };
    this.getTopic = (topicName) => this.topics.get(topicName);
  }
  registerTranslators(topicName, translators) {
    const topic = this.getTopic(topicName);
    if (!topic) {
      if (!this.translatorRegistrationsBuffer[topicName])
        this.translatorRegistrationsBuffer[topicName] = {};
      Object.entries(translators).forEach(([translatorName, translator]) => {
        this.translatorRegistrationsBuffer[topicName][translatorName] = translator;
      });
      return;
    }
    Object.entries(translators).forEach(([name2, translator]) => {
      topic.setTranslator(name2, translator);
    });
  }
  removeTranslators(topicName, translators) {
    const topic = this.getTopic(topicName);
    if (this.translatorRegistrationsBuffer[topicName]) {
      translators.forEach((translatorName) => {
        delete this.translatorRegistrationsBuffer[topicName][translatorName];
      });
    }
    if (!topic) return;
    translators.forEach((name2) => {
      topic.removeTranslator(name2);
    });
  }
};
var normalizeReason = (notification) => {
  const reason = notification?.metadata?.reason;
  if (typeof reason !== "string" || !reason.length) return void 0;
  return reason.toLowerCase();
};
var withReasonFallback = ({
  fallbackTranslationKey,
  notification,
  reasonTranslationKey,
  t: t2
}) => {
  const reason = normalizeReason(notification);
  if (!reason) return t2(fallbackTranslationKey);
  return t2(reasonTranslationKey, { reason });
};
var translateAttachmentUploadBlocked = ({ options: { notification }, t: t2 }) => {
  const rawReason = notification?.metadata?.reason;
  let reason = t2("unsupported file type");
  if (typeof rawReason !== "string") reason = t2("unknown error");
  if (rawReason === "size_limit") reason = t2("size limit");
  return t2("Attachment upload blocked due to {{reason}}", { reason });
};
var translateAttachmentUploadFailed = ({ options: { notification }, t: t2 }) => withReasonFallback({
  fallbackTranslationKey: "Error uploading attachment",
  notification,
  reasonTranslationKey: "Attachment upload failed due to {{reason}}",
  t: t2
});
var translatePollCreateFailed = ({
  options: { notification },
  t: t2
}) => withReasonFallback({
  fallbackTranslationKey: "Failed to create the poll",
  notification,
  reasonTranslationKey: "Failed to create the poll due to {{reason}}",
  t: t2
});
var translatePollEndFailed = ({
  options: { notification },
  t: t2
}) => withReasonFallback({
  fallbackTranslationKey: "Failed to end the poll",
  notification,
  reasonTranslationKey: "Failed to end the poll due to {{reason}}",
  t: t2
});
var translateBrowserAudioPlaybackError = ({ options: { notification }, t: t2 }) => notification?.message ? t2(notification.message) : t2("Error reproducing the recording");
var translatorsByNotificationType = {
  "api:attachment:upload:failed": translateAttachmentUploadFailed,
  "api:location:create:failed": ({ t: t2 }) => t2("Failed to share location"),
  "api:location:share:failed": ({ t: t2 }) => t2("Failed to share location"),
  "api:poll:create:failed": translatePollCreateFailed,
  "api:poll:end:failed": translatePollEndFailed,
  "api:poll:end:success": ({ t: t2 }) => t2("Poll ended"),
  "api:reply:search:failed": ({ t: t2 }) => t2("Thread has not been found"),
  "browser:audio:playback:error": translateBrowserAudioPlaybackError,
  "browser:location:get:failed": ({ t: t2 }) => t2("Failed to retrieve location"),
  "channel:jumpToFirstUnread:failed": ({ t: t2 }) => t2("Failed to jump to the first unread message"),
  "validation:attachment:file:missing": ({ t: t2 }) => t2("File is required for upload attachment"),
  "validation:attachment:id:missing": ({ t: t2 }) => t2("Local upload attachment missing local id"),
  "validation:attachment:upload:blocked": translateAttachmentUploadBlocked,
  "validation:attachment:upload:in-progress": ({ t: t2 }) => t2("Wait until all attachments have uploaded"),
  "validation:poll:castVote:limit": ({ t: t2 }) => t2("Reached the vote limit. Remove an existing vote first.")
};
var translateByNotificationType = ({
  options: { notification },
  ...params
}) => {
  if (!notification?.type) return null;
  const translator = translatorsByNotificationType[notification.type];
  if (!translator) return null;
  return translator({ ...params, options: { notification } });
};
var defaultNotificationTranslators = {
  "*": translateByNotificationType
};
var NotificationTranslationTopic = class extends TranslationTopic {
  constructor({ i18next, translators }) {
    super({ i18next, translators: defaultNotificationTranslators });
    this.translate = (value, key, options) => {
      const { notification } = options;
      if (!notification) return value;
      const byType = notification.type ? this.translators.get(notification.type) : void 0;
      if (byType) return byType({ key, options, t: this.i18next.t, value }) || value;
      const byFallback = this.translators.get("*");
      const translated = byFallback?.({ key, options, t: this.i18next.t, value }) ?? null;
      if (translated) return translated;
      if (!notification.message) return value;
      return this.i18next.t(notification.message, {
        ...notification.metadata ?? {},
        value: notification.message
      });
    };
    if (translators) {
      Object.entries(translators).forEach(([name2, translator]) => {
        this.setTranslator(name2, translator);
      });
    }
  }
};
var de$1 = { exports: {} };
var de2 = de$1.exports;
var hasRequiredDe;
function requireDe() {
  if (hasRequiredDe) return de$1.exports;
  hasRequiredDe = 1;
  (function(module, exports$1) {
    !(function(e, n2) {
      module.exports = n2(import_dayjs2.default);
    })(de2, function(e) {
      e = e && e.hasOwnProperty("default") ? e.default : e;
      var n2 = { s: "ein paar Sekunden", m: ["eine Minute", "einer Minute"], mm: "%d Minuten", h: ["eine Stunde", "einer Stunde"], hh: "%d Stunden", d: ["ein Tag", "einem Tag"], dd: ["%d Tage", "%d Tagen"], M: ["ein Monat", "einem Monat"], MM: ["%d Monate", "%d Monaten"], y: ["ein Jahr", "einem Jahr"], yy: ["%d Jahre", "%d Jahren"] };
      function t2(e2, t22, a22) {
        var r3 = n2[a22];
        return Array.isArray(r3) && (r3 = r3[t22 ? 0 : 1]), r3.replace("%d", e2);
      }
      var a2 = { name: "de", weekdays: "Sonntag_Montag_Dienstag_Mittwoch_Donnerstag_Freitag_Samstag".split("_"), weekdaysShort: "So._Mo._Di._Mi._Do._Fr._Sa.".split("_"), weekdaysMin: "So_Mo_Di_Mi_Do_Fr_Sa".split("_"), months: "Januar_Februar_März_April_Mai_Juni_Juli_August_September_Oktober_November_Dezember".split("_"), monthsShort: "Jan_Feb_März_Apr_Mai_Juni_Juli_Aug_Sept_Okt_Nov_Dez".split("_"), ordinal: function(e2) {
        return e2 + ".";
      }, weekStart: 1, yearStart: 4, formats: { LTS: "HH:mm:ss", LT: "HH:mm", L: "DD.MM.YYYY", LL: "D. MMMM YYYY", LLL: "D. MMMM YYYY HH:mm", LLLL: "dddd, D. MMMM YYYY HH:mm" }, relativeTime: { future: "in %s", past: "vor %s", s: t2, m: t2, mm: t2, h: t2, hh: t2, d: t2, dd: t2, M: t2, MM: t2, y: t2, yy: t2 } };
      return e.locale(a2, null, true), a2;
    });
  })(de$1);
  return de$1.exports;
}
requireDe();
var es$1 = { exports: {} };
var es = es$1.exports;
var hasRequiredEs;
function requireEs() {
  if (hasRequiredEs) return es$1.exports;
  hasRequiredEs = 1;
  (function(module, exports$1) {
    !(function(e, s2) {
      module.exports = s2(import_dayjs2.default);
    })(es, function(e) {
      e = e && e.hasOwnProperty("default") ? e.default : e;
      var s2 = { name: "es", monthsShort: "ene_feb_mar_abr_may_jun_jul_ago_sep_oct_nov_dic".split("_"), weekdays: "domingo_lunes_martes_miércoles_jueves_viernes_sábado".split("_"), weekdaysShort: "dom._lun._mar._mié._jue._vie._sáb.".split("_"), weekdaysMin: "do_lu_ma_mi_ju_vi_sá".split("_"), months: "Enero_Febrero_Marzo_Abril_Mayo_Junio_Julio_Agosto_Septiembre_Octubre_Noviembre_Diciembre".split("_"), weekStart: 1, formats: { LT: "H:mm", LTS: "H:mm:ss", L: "DD/MM/YYYY", LL: "D [de] MMMM [de] YYYY", LLL: "D [de] MMMM [de] YYYY H:mm", LLLL: "dddd, D [de] MMMM [de] YYYY H:mm" }, relativeTime: { future: "en %s", past: "hace %s", s: "unos segundos", m: "un minuto", mm: "%d minutos", h: "una hora", hh: "%d horas", d: "un día", dd: "%d días", M: "un mes", MM: "%d meses", y: "un año", yy: "%d años" }, ordinal: function(e2) {
        return e2 + "º";
      } };
      return e.locale(s2, null, true), s2;
    });
  })(es$1);
  return es$1.exports;
}
requireEs();
var fr$1 = { exports: {} };
var fr = fr$1.exports;
var hasRequiredFr;
function requireFr() {
  if (hasRequiredFr) return fr$1.exports;
  hasRequiredFr = 1;
  (function(module, exports$1) {
    !(function(e, _2) {
      module.exports = _2(import_dayjs2.default);
    })(fr, function(e) {
      e = e && e.hasOwnProperty("default") ? e.default : e;
      var _2 = { name: "fr", weekdays: "dimanche_lundi_mardi_mercredi_jeudi_vendredi_samedi".split("_"), weekdaysShort: "dim._lun._mar._mer._jeu._ven._sam.".split("_"), weekdaysMin: "di_lu_ma_me_je_ve_sa".split("_"), months: "janvier_février_mars_avril_mai_juin_juillet_août_septembre_octobre_novembre_décembre".split("_"), monthsShort: "janv._févr._mars_avr._mai_juin_juil._août_sept._oct._nov._déc.".split("_"), weekStart: 1, yearStart: 4, formats: { LT: "HH:mm", LTS: "HH:mm:ss", L: "DD/MM/YYYY", LL: "D MMMM YYYY", LLL: "D MMMM YYYY HH:mm", LLLL: "dddd D MMMM YYYY HH:mm" }, relativeTime: { future: "dans %s", past: "il y a %s", s: "quelques secondes", m: "une minute", mm: "%d minutes", h: "une heure", hh: "%d heures", d: "un jour", dd: "%d jours", M: "un mois", MM: "%d mois", y: "un an", yy: "%d ans" }, ordinal: function(e2) {
        return "" + e2 + (1 === e2 ? "er" : "");
      } };
      return e.locale(_2, null, true), _2;
    });
  })(fr$1);
  return fr$1.exports;
}
requireFr();
var hi$1 = { exports: {} };
var hi = hi$1.exports;
var hasRequiredHi;
function requireHi() {
  if (hasRequiredHi) return hi$1.exports;
  hasRequiredHi = 1;
  (function(module, exports$1) {
    !(function(_2, e) {
      module.exports = e(import_dayjs2.default);
    })(hi, function(_2) {
      _2 = _2 && _2.hasOwnProperty("default") ? _2.default : _2;
      var e = { name: "hi", weekdays: "रविवार_सोमवार_मंगलवार_बुधवार_गुरूवार_शुक्रवार_शनिवार".split("_"), months: "जनवरी_फ़रवरी_मार्च_अप्रैल_मई_जून_जुलाई_अगस्त_सितम्बर_अक्टूबर_नवम्बर_दिसम्बर".split("_"), weekdaysShort: "रवि_सोम_मंगल_बुध_गुरू_शुक्र_शनि".split("_"), monthsShort: "जन._फ़र._मार्च_अप्रै._मई_जून_जुल._अग._सित._अक्टू._नव._दिस.".split("_"), weekdaysMin: "र_सो_मं_बु_गु_शु_श".split("_"), ordinal: function(_22) {
        return _22;
      }, formats: { LT: "A h:mm बजे", LTS: "A h:mm:ss बजे", L: "DD/MM/YYYY", LL: "D MMMM YYYY", LLL: "D MMMM YYYY, A h:mm बजे", LLLL: "dddd, D MMMM YYYY, A h:mm बजे" }, relativeTime: { future: "%s में", past: "%s पहले", s: "कुछ ही क्षण", m: "एक मिनट", mm: "%d मिनट", h: "एक घंटा", hh: "%d घंटे", d: "एक दिन", dd: "%d दिन", M: "एक महीने", MM: "%d महीने", y: "एक वर्ष", yy: "%d वर्ष" } };
      return _2.locale(e, null, true), e;
    });
  })(hi$1);
  return hi$1.exports;
}
requireHi();
var it$1 = { exports: {} };
var it2 = it$1.exports;
var hasRequiredIt;
function requireIt() {
  if (hasRequiredIt) return it$1.exports;
  hasRequiredIt = 1;
  (function(module, exports$1) {
    !(function(e, o2) {
      module.exports = o2(import_dayjs2.default);
    })(it2, function(e) {
      e = e && e.hasOwnProperty("default") ? e.default : e;
      var o2 = { name: "it", weekdays: "domenica_lunedì_martedì_mercoledì_giovedì_venerdì_sabato".split("_"), weekdaysShort: "dom_lun_mar_mer_gio_ven_sab".split("_"), weekdaysMin: "do_lu_ma_me_gi_ve_sa".split("_"), months: "gennaio_febbraio_marzo_aprile_maggio_giugno_luglio_agosto_settembre_ottobre_novembre_dicembre".split("_"), weekStart: 1, monthsShort: "gen_feb_mar_apr_mag_giu_lug_ago_set_ott_nov_dic".split("_"), formats: { LT: "HH:mm", LTS: "HH:mm:ss", L: "DD/MM/YYYY", LL: "D MMMM YYYY", LLL: "D MMMM YYYY HH:mm", LLLL: "dddd D MMMM YYYY HH:mm" }, relativeTime: { future: "tra %s", past: "%s fa", s: "qualche secondo", m: "un minuto", mm: "%d minuti", h: "un' ora", hh: "%d ore", d: "un giorno", dd: "%d giorni", M: "un mese", MM: "%d mesi", y: "un anno", yy: "%d anni" }, ordinal: function(e2) {
        return e2 + "º";
      } };
      return e.locale(o2, null, true), o2;
    });
  })(it$1);
  return it$1.exports;
}
requireIt();
var ja$1 = { exports: {} };
var ja = ja$1.exports;
var hasRequiredJa;
function requireJa() {
  if (hasRequiredJa) return ja$1.exports;
  hasRequiredJa = 1;
  (function(module, exports$1) {
    !(function(_2, e) {
      module.exports = e(import_dayjs2.default);
    })(ja, function(_2) {
      _2 = _2 && _2.hasOwnProperty("default") ? _2.default : _2;
      var e = { name: "ja", weekdays: "日曜日_月曜日_火曜日_水曜日_木曜日_金曜日_土曜日".split("_"), weekdaysShort: "日_月_火_水_木_金_土".split("_"), weekdaysMin: "日_月_火_水_木_金_土".split("_"), months: "1月_2月_3月_4月_5月_6月_7月_8月_9月_10月_11月_12月".split("_"), monthsShort: "1月_2月_3月_4月_5月_6月_7月_8月_9月_10月_11月_12月".split("_"), ordinal: function(_22) {
        return _22 + "日";
      }, formats: { LT: "HH:mm", LTS: "HH:mm:ss", L: "YYYY/MM/DD", LL: "YYYY年M月D日", LLL: "YYYY年M月D日 HH:mm", LLLL: "YYYY年M月D日 dddd HH:mm", l: "YYYY/MM/DD", ll: "YYYY年M月D日", lll: "YYYY年M月D日 HH:mm", llll: "YYYY年M月D日(ddd) HH:mm" }, meridiem: function(_22) {
        return _22 < 12 ? "午前" : "午後";
      }, relativeTime: { future: "%s後", past: "%s前", s: "数秒", m: "1分", mm: "%d分", h: "1時間", hh: "%d時間", d: "1日", dd: "%d日", M: "1ヶ月", MM: "%dヶ月", y: "1年", yy: "%d年" } };
      return _2.locale(e, null, true), e;
    });
  })(ja$1);
  return ja$1.exports;
}
requireJa();
var ko$1 = { exports: {} };
var ko = ko$1.exports;
var hasRequiredKo;
function requireKo() {
  if (hasRequiredKo) return ko$1.exports;
  hasRequiredKo = 1;
  (function(module, exports$1) {
    !(function(_2, e) {
      module.exports = e(import_dayjs2.default);
    })(ko, function(_2) {
      _2 = _2 && _2.hasOwnProperty("default") ? _2.default : _2;
      var e = { name: "ko", weekdays: "일요일_월요일_화요일_수요일_목요일_금요일_토요일".split("_"), weekdaysShort: "일_월_화_수_목_금_토".split("_"), weekdaysMin: "일_월_화_수_목_금_토".split("_"), months: "1월_2월_3월_4월_5월_6월_7월_8월_9월_10월_11월_12월".split("_"), monthsShort: "1월_2월_3월_4월_5월_6월_7월_8월_9월_10월_11월_12월".split("_"), ordinal: function(_22) {
        return _22;
      }, formats: { LT: "A h:mm", LTS: "A h:mm:ss", L: "YYYY.MM.DD.", LL: "YYYY년 MMMM D일", LLL: "YYYY년 MMMM D일 A h:mm", LLLL: "YYYY년 MMMM D일 dddd A h:mm", l: "YYYY.MM.DD.", ll: "YYYY년 MMMM D일", lll: "YYYY년 MMMM D일 A h:mm", llll: "YYYY년 MMMM D일 dddd A h:mm" }, meridiem: function(_22) {
        return _22 < 12 ? "오전" : "오후";
      }, relativeTime: { future: "%s 후", past: "%s 전", s: "몇 초", m: "1분", mm: "%d분", h: "한 시간", hh: "%d시간", d: "하루", dd: "%d일", M: "한 달", MM: "%d달", y: "일 년", yy: "%d년" } };
      return _2.locale(e, null, true), e;
    });
  })(ko$1);
  return ko$1.exports;
}
requireKo();
var nl$1 = { exports: {} };
var nl = nl$1.exports;
var hasRequiredNl;
function requireNl() {
  if (hasRequiredNl) return nl$1.exports;
  hasRequiredNl = 1;
  (function(module, exports$1) {
    !(function(e, a2) {
      module.exports = a2(import_dayjs2.default);
    })(nl, function(e) {
      e = e && e.hasOwnProperty("default") ? e.default : e;
      var a2 = { name: "nl", weekdays: "zondag_maandag_dinsdag_woensdag_donderdag_vrijdag_zaterdag".split("_"), weekdaysShort: "zo._ma._di._wo._do._vr._za.".split("_"), weekdaysMin: "zo_ma_di_wo_do_vr_za".split("_"), months: "januari_februari_maart_april_mei_juni_juli_augustus_september_oktober_november_december".split("_"), monthsShort: "jan_feb_mrt_apr_mei_jun_jul_aug_sep_okt_nov_dec".split("_"), ordinal: function(e2) {
        return e2 + ".";
      }, weekStart: 1, yearStart: 4, formats: { LT: "HH:mm", LTS: "HH:mm:ss", L: "DD-MM-YYYY", LL: "D MMMM YYYY", LLL: "D MMMM YYYY HH:mm", LLLL: "dddd D MMMM YYYY HH:mm" }, relativeTime: { future: "over %s", past: "%s geleden", s: "een paar seconden", m: "een minuut", mm: "%d minuten", h: "een uur", hh: "%d uur", d: "een dag", dd: "%d dagen", M: "een maand", MM: "%d maanden", y: "een jaar", yy: "%d jaar" } };
      return e.locale(a2, null, true), a2;
    });
  })(nl$1);
  return nl$1.exports;
}
requireNl();
var pt$1 = { exports: {} };
var pt2 = pt$1.exports;
var hasRequiredPt;
function requirePt() {
  if (hasRequiredPt) return pt$1.exports;
  hasRequiredPt = 1;
  (function(module, exports$1) {
    !(function(e, a2) {
      module.exports = a2(import_dayjs2.default);
    })(pt2, function(e) {
      e = e && e.hasOwnProperty("default") ? e.default : e;
      var a2 = { name: "pt", weekdays: "Domingo_Segunda-feira_Terça-feira_Quarta-feira_Quinta-feira_Sexta-feira_Sábado".split("_"), weekdaysShort: "Dom_Seg_Ter_Qua_Qui_Sex_Sab".split("_"), weekdaysMin: "Do_2ª_3ª_4ª_5ª_6ª_Sa".split("_"), months: "Janeiro_Fevereiro_Março_Abril_Maio_Junho_Julho_Agosto_Setembro_Outubro_Novembro_Dezembro".split("_"), monthsShort: "Jan_Fev_Mar_Abr_Mai_Jun_Jul_Ago_Set_Out_Nov_Dez".split("_"), ordinal: function(e2) {
        return e2 + "º";
      }, weekStart: 1, yearStart: 4, formats: { LT: "HH:mm", LTS: "HH:mm:ss", L: "DD/MM/YYYY", LL: "D [de] MMMM [de] YYYY", LLL: "D [de] MMMM [de] YYYY [às] HH:mm", LLLL: "dddd, D [de] MMMM [de] YYYY [às] HH:mm" }, relativeTime: { future: "em %s", past: "há %s", s: "alguns segundos", m: "um minuto", mm: "%d minutos", h: "uma hora", hh: "%d horas", d: "um dia", dd: "%d dias", M: "um mês", MM: "%d meses", y: "um ano", yy: "%d anos" } };
      return e.locale(a2, null, true), a2;
    });
  })(pt$1);
  return pt$1.exports;
}
requirePt();
var ru$1 = { exports: {} };
var ru = ru$1.exports;
var hasRequiredRu;
function requireRu() {
  if (hasRequiredRu) return ru$1.exports;
  hasRequiredRu = 1;
  (function(module, exports$1) {
    !(function(_2, t2) {
      module.exports = t2(import_dayjs2.default);
    })(ru, function(_2) {
      _2 = _2 && _2.hasOwnProperty("default") ? _2.default : _2;
      var t2 = "января_февраля_марта_апреля_мая_июня_июля_августа_сентября_октября_ноября_декабря".split("_"), e = "январь_февраль_март_апрель_май_июнь_июль_август_сентябрь_октябрь_ноябрь_декабрь".split("_"), n2 = "янв._февр._мар._апр._мая_июня_июля_авг._сент._окт._нояб._дек.".split("_"), s2 = "янв._февр._март_апр._май_июнь_июль_авг._сент._окт._нояб._дек.".split("_"), r3 = /D[oD]?(\[[^[\]]*\]|\s)+MMMM?/;
      function o2(_22, t22, e2) {
        var n22, s22;
        return "m" === e2 ? t22 ? "минута" : "минуту" : _22 + " " + (n22 = +_22, s22 = { mm: t22 ? "минута_минуты_минут" : "минуту_минуты_минут", hh: "час_часа_часов", dd: "день_дня_дней", MM: "месяц_месяца_месяцев", yy: "год_года_лет" }[e2].split("_"), n22 % 10 == 1 && n22 % 100 != 11 ? s22[0] : n22 % 10 >= 2 && n22 % 10 <= 4 && (n22 % 100 < 10 || n22 % 100 >= 20) ? s22[1] : s22[2]);
      }
      var d2 = function(_22, n22) {
        return r3.test(n22) ? t2[_22.month()] : e[_22.month()];
      };
      d2.s = e, d2.f = t2;
      var i2 = function(_22, t22) {
        return r3.test(t22) ? n2[_22.month()] : s2[_22.month()];
      };
      i2.s = s2, i2.f = n2;
      var m2 = { name: "ru", weekdays: "воскресенье_понедельник_вторник_среда_четверг_пятница_суббота".split("_"), weekdaysShort: "вск_пнд_втр_срд_чтв_птн_сбт".split("_"), weekdaysMin: "вс_пн_вт_ср_чт_пт_сб".split("_"), months: d2, monthsShort: i2, weekStart: 1, formats: { LT: "H:mm", LTS: "H:mm:ss", L: "DD.MM.YYYY", LL: "D MMMM YYYY г.", LLL: "D MMMM YYYY г., H:mm", LLLL: "dddd, D MMMM YYYY г., H:mm" }, relativeTime: { future: "через %s", past: "%s назад", s: "несколько секунд", m: o2, mm: o2, h: "час", hh: o2, d: "день", dd: o2, M: "месяц", MM: o2, y: "год", yy: o2 }, ordinal: function(_22) {
        return _22;
      } };
      return _2.locale(m2, null, true), m2;
    });
  })(ru$1);
  return ru$1.exports;
}
requireRu();
var tr$1 = { exports: {} };
var tr = tr$1.exports;
var hasRequiredTr;
function requireTr() {
  if (hasRequiredTr) return tr$1.exports;
  hasRequiredTr = 1;
  (function(module, exports$1) {
    !(function(a2, e) {
      module.exports = e(import_dayjs2.default);
    })(tr, function(a2) {
      a2 = a2 && a2.hasOwnProperty("default") ? a2.default : a2;
      var e = { name: "tr", weekdays: "Pazar_Pazartesi_Salı_Çarşamba_Perşembe_Cuma_Cumartesi".split("_"), weekdaysShort: "Paz_Pts_Sal_Çar_Per_Cum_Cts".split("_"), weekdaysMin: "Pz_Pt_Sa_Ça_Pe_Cu_Ct".split("_"), months: "Ocak_Şubat_Mart_Nisan_Mayıs_Haziran_Temmuz_Ağustos_Eylül_Ekim_Kasım_Aralık".split("_"), monthsShort: "Oca_Şub_Mar_Nis_May_Haz_Tem_Ağu_Eyl_Eki_Kas_Ara".split("_"), weekStart: 1, formats: { LT: "HH:mm", LTS: "HH:mm:ss", L: "DD.MM.YYYY", LL: "D MMMM YYYY", LLL: "D MMMM YYYY HH:mm", LLLL: "dddd, D MMMM YYYY HH:mm" }, relativeTime: { future: "%s sonra", past: "%s önce", s: "birkaç saniye", m: "bir dakika", mm: "%d dakika", h: "bir saat", hh: "%d saat", d: "bir gün", dd: "%d gün", M: "bir ay", MM: "%d ay", y: "bir yıl", yy: "%d yıl" }, ordinal: function(a22) {
        return a22 + ".";
      } };
      return a2.locale(e, null, true), e;
    });
  })(tr$1);
  return tr$1.exports;
}
requireTr();
var en$1 = { exports: {} };
var en2 = en$1.exports;
var hasRequiredEn;
function requireEn() {
  if (hasRequiredEn) return en$1.exports;
  hasRequiredEn = 1;
  (function(module, exports$1) {
    !(function(e, n2) {
      module.exports = n2();
    })(en2, function() {
      return { name: "en", weekdays: "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), months: "January_February_March_April_May_June_July_August_September_October_November_December".split("_") };
    });
  })(en$1);
  return en$1.exports;
}
requireEn();
var defaultNS = "translation";
var defaultLng = "en";
import_dayjs2.default.extend(updateLocale);
import_dayjs2.default.extend(utc);
import_dayjs2.default.extend(timezone);
import_dayjs2.default.updateLocale("de", {
  calendar: {
    lastDay: "[gestern um] LT",
    lastWeek: "[letzten] dddd [um] LT",
    nextDay: "[morgen um] LT",
    nextWeek: "dddd [um] LT",
    sameDay: "[heute um] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("es", {
  calendar: {
    lastDay: "[ayer a las] LT",
    lastWeek: "[pasado] dddd [a] LT",
    nextDay: "[mañana a] LT",
    nextWeek: "dddd [a] LT",
    sameDay: "[hoy a las] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("fr", {
  calendar: {
    lastDay: "[Hier à] LT",
    lastWeek: "dddd [dernier à] LT",
    nextDay: "[Demain à] LT",
    nextWeek: "dddd [à] LT",
    sameDay: "[Aujourd'hui à] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("hi", {
  calendar: {
    lastDay: "[कल] LT",
    lastWeek: "[पिछले] dddd, LT",
    nextDay: "[कल] LT",
    nextWeek: "dddd, LT",
    sameDay: "[आज] LT",
    sameElse: "L"
  },
  // Hindi notation for meridiems are quite fuzzy in practice. While there exists
  // a rigid notion of a 'Pahar' it is not used as rigidly in modern Hindi.
  meridiem(hour) {
    if (hour < 4) {
      return "रात";
    } else if (hour < 10) {
      return "सुबह";
    } else if (hour < 17) {
      return "दोपहर";
    } else if (hour < 20) {
      return "शाम";
    } else {
      return "रात";
    }
  },
  meridiemHour(hour, meridiem) {
    if (hour === 12) {
      hour = 0;
    }
    if (meridiem === "रात") {
      return hour < 4 ? hour : hour + 12;
    } else if (meridiem === "सुबह") {
      return hour;
    } else if (meridiem === "दोपहर") {
      return hour >= 10 ? hour : hour + 12;
    } else if (meridiem === "शाम") {
      return hour + 12;
    }
    return hour;
  },
  meridiemParse: /रात|सुबह|दोपहर|शाम/
});
import_dayjs2.default.updateLocale("it", {
  calendar: {
    lastDay: "[Ieri alle] LT",
    lastWeek: "[lo scorso] dddd [alle] LT",
    nextDay: "[Domani alle] LT",
    nextWeek: "dddd [alle] LT",
    sameDay: "[Oggi alle] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("ja", {
  calendar: {
    lastDay: "[昨日] LT",
    lastWeek: "dddd LT",
    nextDay: "[明日] LT",
    nextWeek: "[次の] dddd LT",
    sameDay: "[今日] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("ko", {
  calendar: {
    lastDay: "[어제] LT",
    lastWeek: "[지난] dddd LT",
    nextDay: "[내일] LT",
    nextWeek: "dddd LT",
    sameDay: "[오늘] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("nl", {
  calendar: {
    lastDay: "[gisteren om] LT",
    lastWeek: "[afgelopen] dddd [om] LT",
    nextDay: "[morgen om] LT",
    nextWeek: "dddd [om] LT",
    sameDay: "[vandaag om] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("pt", {
  calendar: {
    lastDay: "[ontem às] LT",
    lastWeek: "dddd [passada às] LT",
    nextDay: "[amanhã às] LT",
    nextWeek: "dddd [às] LT",
    sameDay: "[hoje às] LT",
    sameElse: "L"
  }
});
import_dayjs2.default.updateLocale("ru", {
  calendar: {
    lastDay: "[Вчера, в] LT",
    nextDay: "[Завтра, в] LT",
    sameDay: "[Сегодня, в] LT"
  }
});
import_dayjs2.default.updateLocale("tr", {
  calendar: {
    lastDay: "[dün] LT",
    lastWeek: "[geçen] dddd [saat] LT",
    nextDay: "[yarın saat] LT",
    nextWeek: "[gelecek] dddd [saat] LT",
    sameDay: "[bugün saat] LT",
    sameElse: "L"
  }
});
var en_locale = {
  formats: {},
  months: [
    "January",
    "February",
    "March",
    "April",
    "May",
    "June",
    "July",
    "August",
    "September",
    "October",
    "November",
    "December"
  ],
  relativeTime: {},
  weekdays: [
    "Sunday",
    "Monday",
    "Tuesday",
    "Wednesday",
    "Thursday",
    "Friday",
    "Saturday"
  ]
};
var isDayJs = (dateTimeParser) => dateTimeParser.extend !== void 0;
var supportsTz = (dateTimeParser) => dateTimeParser.tz !== void 0;
var defaultStreami18nOptions = {
  DateTimeParser: import_dayjs2.default,
  dayjsLocaleConfigForLanguage: null,
  debug: false,
  disableDateTimeTranslations: false,
  language: "en",
  logger: (message) => console.warn(message),
  /**
   * Key in the translationBuilderTopics has to match postProcessorName in the translation value.
   *
   * {
   *   "key": "{{value, postProcessorName}}"
   * }
   *
   * At least the default topics will be supported.
   */
  translationBuilderTopics: {
    notification: NotificationTranslationTopic
  }
};
var Streami18n = class {
  /**
   * Constructor accepts following options:
   *  - language (String) default: 'en'
   *    Language code e.g., en, tr
   *
   *  - translationsForLanguage (object)
   *    Translations object. Please check src/i18n/en.json for example.
   *
   *  - disableDateTimeTranslations (boolean) default: false
   *    Disable translations for date-times
   *
   *  - debug (boolean) default: false
   *    Enable debug mode in internal i18n class
   *
   *  - logger (function) default: () => {}
   *    Logger function to log warnings/errors from this class
   *
   *  - dayjsLocaleConfigForLanguage (object) default: 'enConfig'
   *    [Config object](https://momentjs.com/docs/#/i18n/changing-locale/) for internal moment object,
   *    corresponding to language (param)
   *
   *  - DateTimeParser (function) Moment or Dayjs instance/function.
   *    Make sure to load all the required locales in this Moment or Dayjs instance that you will be provide to Streami18n
   *
   * @param {*} options
   */
  constructor(options = {}) {
    this.i18nInstance = instance.createInstance();
    this.translationBuilderTopics = {};
    this.Dayjs = null;
    this.setLanguageCallback = () => null;
    this.initialized = false;
    this.t = defaultTranslatorFunction;
    this.translations = {
      de: { [defaultNS]: deTranslations },
      en: { [defaultNS]: enTranslations },
      es: { [defaultNS]: esTranslations },
      fr: { [defaultNS]: frTranslations },
      hi: { [defaultNS]: hiTranslations },
      it: { [defaultNS]: itTranslations },
      ja: { [defaultNS]: jaTranslations },
      ko: { [defaultNS]: koTranslations },
      nl: { [defaultNS]: nlTranslations },
      pt: { [defaultNS]: ptTranslations },
      ru: { [defaultNS]: ruTranslations },
      tr: { [defaultNS]: trTranslations }
    };
    this.dayjsLocales = {};
    this.formatters = predefinedFormatters;
    this.localeExists = (language) => {
      if (this.isCustomDateTimeParser) return true;
      return Object.keys(import_dayjs2.default.Ls).indexOf(language) > -1;
    };
    this.validateCurrentLanguage = () => {
      const availableLanguages = Object.keys(this.translations);
      if (availableLanguages.indexOf(this.currentLanguage) === -1) {
        this.logger(
          `Streami18n: '${this.currentLanguage}' language is not registered. Please make sure to call streami18n.registerTranslation('${this.currentLanguage}', {...}) or use one the built-in supported languages - ${this.getAvailableLanguages()}`
        );
        this.currentLanguage = defaultLng;
      }
    };
    this.geti18Instance = () => this.i18nInstance;
    this.getAvailableLanguages = () => Object.keys(this.translations);
    this.getTranslations = () => this.translations;
    const finalOptions = {
      ...defaultStreami18nOptions,
      ...options
    };
    this.logger = finalOptions.logger;
    this.currentLanguage = finalOptions.language;
    this.DateTimeParser = finalOptions.DateTimeParser;
    this.timezone = finalOptions.timezone;
    this.formatters = { ...predefinedFormatters, ...options?.formatters };
    this.translationBuilder = new TranslationBuilder(this.i18nInstance);
    this.translationBuilderTopics = {
      ...defaultStreami18nOptions.translationBuilderTopics,
      ...options.translationBuilderTopics
    };
    try {
      if (this.DateTimeParser && isDayJs(this.DateTimeParser)) {
        this.DateTimeParser.extend(LocalizedFormat);
        this.DateTimeParser.extend(calendar);
        this.DateTimeParser.extend(localeData);
        this.DateTimeParser.extend(relativeTime);
        this.DateTimeParser.extend(duration);
      }
    } catch (error2) {
      throw Error(
        `Streami18n: Looks like you wanted to provide Dayjs instance, but something went wrong while adding plugins ${error2}`
      );
    }
    this.isCustomDateTimeParser = !!options.DateTimeParser;
    const translationsForLanguage = finalOptions.translationsForLanguage;
    if (translationsForLanguage) {
      this.translations[this.currentLanguage] = {
        [defaultNS]: this.translations[this.currentLanguage] && this.translations[this.currentLanguage][defaultNS] ? {
          ...this.translations[this.currentLanguage][defaultNS],
          ...translationsForLanguage
        } : translationsForLanguage
      };
    }
    if (!this.translations[this.currentLanguage]) {
      this.translations[this.currentLanguage] = {
        [defaultNS]: {}
      };
    }
    this.i18nextConfig = {
      debug: finalOptions.debug,
      fallbackLng: false,
      interpolation: { escapeValue: false, formatSeparator: "|" },
      keySeparator: false,
      lng: this.currentLanguage,
      nsSeparator: false
    };
    const postProcess = Object.keys(this.translationBuilderTopics);
    if (postProcess.length > 0) {
      this.i18nextConfig.postProcess = postProcess;
    }
    if (finalOptions.parseMissingKeyHandler) {
      this.i18nextConfig.parseMissingKeyHandler = finalOptions.parseMissingKeyHandler;
    }
    this.validateCurrentLanguage();
    const dayjsLocaleConfigForLanguage = finalOptions.dayjsLocaleConfigForLanguage;
    if (dayjsLocaleConfigForLanguage) {
      this.addOrUpdateLocale(this.currentLanguage, {
        ...dayjsLocaleConfigForLanguage
      });
    } else if (!this.localeExists(this.currentLanguage)) {
      this.logger(
        `Streami18n: Streami18n(...) - Locale config for ${this.currentLanguage} does not exist in momentjs.Please import the locale file using "import 'moment/locale/${this.currentLanguage}';" in your app or register the locale config with Streami18n using registerTranslation(language, translation, customDayjsLocale)`
      );
    }
    this.tDateTimeParser = (timestamp) => {
      const language = finalOptions.disableDateTimeTranslations || !this.localeExists(this.currentLanguage) ? defaultLng : this.currentLanguage;
      if (isDayJs(this.DateTimeParser)) {
        return supportsTz(this.DateTimeParser) ? this.DateTimeParser(timestamp).tz(this.timezone).locale(language) : this.DateTimeParser(timestamp).locale(language);
      }
      if (supportsTz(this.DateTimeParser) && this.timezone) {
        return this.DateTimeParser(timestamp).tz(this.timezone).locale(language);
      }
      return this.DateTimeParser(timestamp).locale(language);
    };
  }
  /**
   * Initializes the i18next instance with configuration (which enables natural language as default keys)
   */
  async init() {
    this.validateCurrentLanguage();
    try {
      this.t = await this.i18nInstance.init({
        ...this.i18nextConfig,
        lng: this.currentLanguage,
        resources: this.translations
      });
      this.initialized = true;
      if (this.formatters) {
        Object.entries(this.formatters).forEach(([name2, formatterFactory]) => {
          if (!formatterFactory) return;
          this.i18nInstance.services.formatter?.add(name2, formatterFactory(this));
        });
      }
      Object.entries(this.translationBuilderTopics).forEach(
        ([topic, TranslationTopic2]) => {
          this.translationBuilder.registerTopic(topic, TranslationTopic2);
        }
      );
    } catch (error2) {
      this.logger(`Something went wrong with init: ${JSON.stringify(error2)}`);
    }
    return {
      t: this.t,
      tDateTimeParser: this.tDateTimeParser
    };
  }
  /**
   * Returns current version translator function.
   */
  async getTranslators() {
    if (!this.initialized) {
      if (this.dayjsLocales[this.currentLanguage]) {
        this.addOrUpdateLocale(
          this.currentLanguage,
          this.dayjsLocales[this.currentLanguage]
        );
      }
      return await this.init();
    } else {
      return {
        t: this.t,
        tDateTimeParser: this.tDateTimeParser
      };
    }
  }
  registerTranslation(language, translation, customDayjsLocale) {
    if (!translation) {
      this.logger(
        `Streami18n: registerTranslation(language, translation, customDayjsLocale) called without translation`
      );
      return;
    }
    if (!this.translations[language]) {
      this.translations[language] = { [defaultNS]: translation };
    } else {
      this.translations[language][defaultNS] = translation;
    }
    if (customDayjsLocale) {
      this.dayjsLocales[language] = { ...customDayjsLocale };
    } else if (!this.localeExists(language)) {
      this.logger(
        `Streami18n: registerTranslation(language, translation, customDayjsLocale) - Locale config for ${language} does not exist in Dayjs.Please import the locale file using "import 'dayjs/locale/${language}';" in your app or register the locale config with Streami18n using registerTranslation(language, translation, customDayjsLocale)`
      );
    }
    if (this.initialized) {
      this.i18nInstance.addResources(language, defaultNS, translation);
    }
  }
  addOrUpdateLocale(key, config) {
    if (this.localeExists(key)) {
      import_dayjs2.default.updateLocale(key, { ...config });
    } else {
      import_dayjs2.default.locale({ name: key, ...en_locale, ...config }, void 0, true);
    }
  }
  async setLanguage(language) {
    this.currentLanguage = language;
    if (!this.initialized) return;
    try {
      const t2 = await this.i18nInstance.changeLanguage(language);
      if (this.dayjsLocales[language]) {
        this.addOrUpdateLocale(
          this.currentLanguage,
          this.dayjsLocales[this.currentLanguage]
        );
      }
      this.setLanguageCallback(t2);
      return t2;
    } catch (error2) {
      this.logger(`Failed to set language: ${JSON.stringify(error2)}`);
      return this.t;
    }
  }
  registerSetLanguageCallback(callback) {
    this.setLanguageCallback = callback;
  }
};
var LoadingItems = () => (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__channel-list-item-container", children: (0, import_jsx_runtime4.jsxs)(
  "div",
  {
    "aria-hidden": "true",
    className: "str-chat__channel-list-item str-chat__channel-list-item--loading",
    children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__loading-channels-avatar" }),
      (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-list-item-data str-chat__channel-list-item-data--loading", children: [
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__loading-channels-username" }),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__loading-channels-status" })
      ] })
    ]
  }
) });
var LoadingChannels = () => (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__loading-channels", children: [
  (0, import_jsx_runtime4.jsx)(LoadingItems, {}),
  (0, import_jsx_runtime4.jsx)(LoadingItems, {}),
  (0, import_jsx_runtime4.jsx)(LoadingItems, {}),
  (0, import_jsx_runtime4.jsx)(LoadingItems, {}),
  (0, import_jsx_runtime4.jsx)(LoadingItems, {})
] });
var LoadingIndicator = (props) => (0, import_jsx_runtime4.jsx)(IconLoading, { ...props, className: "str-chat__loading-indicator" });
var RING_RADIUS = 12;
var RING_CIRCUMFERENCE = 2 * Math.PI * RING_RADIUS;
var CircularProgressIndicator = ({ percent }) => {
  const { t: t2 } = useTranslationContext("CircularProgressIndicator");
  const dashOffset = RING_CIRCUMFERENCE * (1 - percent / 100);
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__circular-progress-indicator str-chat__progress-indicator", children: (0, import_jsx_runtime4.jsxs)(
    "svg",
    {
      "aria-label": t2("aria/Percent complete", { percent }),
      "aria-valuemax": 100,
      "aria-valuemin": 0,
      "aria-valuenow": percent,
      "data-testid": "circular-progress-ring",
      height: "100%",
      role: "progressbar",
      viewBox: "0 0 32 32",
      width: "100%",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime4.jsx)(
          "circle",
          {
            cx: "16",
            cy: "16",
            fill: "none",
            r: RING_RADIUS,
            stroke: "currentColor",
            strokeOpacity: 0.35,
            strokeWidth: "2.5"
          }
        ),
        (0, import_jsx_runtime4.jsx)(
          "circle",
          {
            cx: "16",
            cy: "16",
            fill: "none",
            r: RING_RADIUS,
            stroke: "currentColor",
            strokeDasharray: RING_CIRCUMFERENCE,
            strokeDashoffset: dashOffset,
            strokeLinecap: "round",
            strokeWidth: "2.5",
            transform: "rotate(-90 16 16)"
          }
        )
      ]
    }
  ) });
};
var UploadProgressIndicator = ({
  uploadProgress
}) => {
  const {
    LoadingIndicator: LoadingIndicator$1 = LoadingIndicator,
    ProgressIndicator = CircularProgressIndicator
  } = useComponentContext();
  if (uploadProgress === void 0) {
    return (0, import_jsx_runtime4.jsx)(LoadingIndicator$1, { "data-testid": "loading-indicator" });
  }
  return (0, import_jsx_runtime4.jsx)(ProgressIndicator, { percent: uploadProgress });
};
var UploadedSizeIndicator = ({
  fullBytes,
  uploadedBytes
}) => {
  const { FileSizeIndicator: FileSizeIndicator$1 = FileSizeIndicator } = useComponentContext();
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__attachment-preview-file__upload-size-fraction",
      "data-testid": "upload-size-fraction",
      children: [
        (0, import_jsx_runtime4.jsx)(FileSizeIndicator$1, { fileSize: uploadedBytes }),
        " ",
        ` / `,
        (0, import_jsx_runtime4.jsx)(FileSizeIndicator$1, { fileSize: fullBytes })
      ]
    }
  );
};
var useStableCallback = (callback) => {
  const ref = (0, import_react29.useRef)(callback);
  ref.current = callback;
  return (0, import_react29.useCallback)((...args) => ref.current(...args), []);
};
var handleActionWarning = `Action handler was called, but it is missing one of its required arguments. 
Make sure the ChannelAction and ChannelState contexts are properly set and the hook is initialized with a valid message.`;
function useActionHandler(message) {
  const { removeMessage, updateMessage } = useChannelActionContext("useActionHandler");
  const { channel } = useChannelStateContext("useActionHandler");
  return useStableCallback(async (dataOrName, value, event) => {
    if (event) event.preventDefault();
    if (!message || !updateMessage || !removeMessage || !channel) {
      console.warn(handleActionWarning);
      return;
    }
    const messageId = message.id;
    let formData = {};
    if (typeof dataOrName === "string") {
      formData[dataOrName] = value;
    } else {
      formData = { ...dataOrName };
    }
    if (messageId) {
      const data = await channel.sendAction(messageId, formData);
      if (data?.message) {
        updateMessage(data.message);
      } else {
        removeMessage(message);
      }
    }
  });
}
var useDeleteHandler = (message) => {
  const { deleteMessage, removeMessage, updateMessage } = useChannelActionContext("useDeleteHandler");
  const { client } = useChatContext("useDeleteHandler");
  return async (options) => {
    if (!message) {
      return;
    }
    if (message.type === "error" || isNetworkSendFailure(message)) {
      removeMessage?.(message);
      return;
    }
    if (!message.id || !client || !updateMessage) {
      return;
    }
    const deletedMessage = await deleteMessage(message, options);
    updateMessage(deletedMessage);
  };
};
var missingUseFlagHandlerParameterWarning = "useFlagHandler was called but it is missing one or more necessary parameters.";
var useFlagHandler = (message) => {
  const { client } = useChatContext("useFlagHandler");
  const { t: t2 } = useTranslationContext("useFlagHandler");
  return async (event) => {
    event.preventDefault();
    if (!client || !t2 || !message?.id) {
      console.warn(missingUseFlagHandlerParameterWarning);
      return;
    }
    await client.flagMessage(message.id);
  };
};
function createEventHandler(fn, message) {
  return (event) => {
    if (typeof fn !== "function" || !message?.mentioned_users?.length) {
      return;
    }
    fn(event, message.mentioned_users);
  };
}
var useMentionsHandler = (message, customMentionHandler) => {
  const {
    onMentionsClick: contextOnMentionsClick,
    onMentionsHover: contextOnMentionsHover
  } = useChannelActionContext("useMentionsHandler");
  const onMentionsClick = customMentionHandler?.onMentionsClick || contextOnMentionsClick || (() => null);
  const onMentionsHover = customMentionHandler?.onMentionsHover || contextOnMentionsHover || (() => null);
  return {
    onMentionsClick: createEventHandler(onMentionsClick, message),
    onMentionsHover: createEventHandler(onMentionsHover, message)
  };
};
var useMarkUnreadHandler = (message) => {
  const { channel } = useChannelStateContext("useMarkUnreadHandler");
  return async (event) => {
    event.preventDefault();
    if (!message?.id) {
      console.warn("Mark unread handler does not have access to message id");
      return;
    }
    await channel.markUnread({ message_id: message.id });
  };
};
var missingUseMuteHandlerParamsWarning = "useMuteHandler was called but it is missing one or more necessary parameter.";
var useMuteHandler = (message) => {
  const { mutes } = useChannelStateContext("useMuteHandler");
  const { client } = useChatContext("useMuteHandler");
  const { t: t2 } = useTranslationContext("useMuteHandler");
  return async (event) => {
    event.preventDefault();
    if (!t2 || !message?.user || !client) {
      console.warn(missingUseMuteHandlerParamsWarning);
      return;
    }
    if (!isUserMuted(message, mutes)) {
      await client.muteUser(message.user.id);
    } else {
      await client.unmuteUser(message.user.id);
    }
  };
};
var useOpenThreadHandler = (message, customOpenThread) => {
  const { openThread: channelOpenThread } = useChannelActionContext("useOpenThreadHandler");
  const openThread = customOpenThread || channelOpenThread;
  return (event) => {
    if (!openThread || !message) {
      console.warn(
        "Open thread handler was called but it is missing one of its parameters"
      );
      return;
    }
    openThread(message, event);
  };
};
var usePinHandler = (message) => {
  const { updateMessage } = useChannelActionContext("usePinHandler");
  const { channelCapabilities = {} } = useChannelStateContext("usePinHandler");
  const { client } = useChatContext("usePinHandler");
  const canPin = !!channelCapabilities["pin-message"];
  const handlePin = async (event) => {
    event.preventDefault();
    if (!message) return;
    if (!message.pinned) {
      try {
        const optimisticMessage = {
          ...message,
          pinned: true,
          pinned_at: /* @__PURE__ */ new Date(),
          pinned_by: client.user
        };
        updateMessage(optimisticMessage);
        await client.pinMessage(message);
      } catch (e) {
        updateMessage(message);
      }
    } else {
      try {
        const optimisticMessage = {
          ...message,
          pin_expires: null,
          pinned: false,
          pinned_at: null,
          pinned_by: null
        };
        updateMessage(optimisticMessage);
        await client.unpinMessage(message);
      } catch (e) {
        updateMessage(message);
      }
    }
  };
  return { canPin, handlePin };
};
var reactionHandlerWarning = `Reaction handler was called, but it is missing one of its required arguments.
Make sure the ChannelAction and ChannelState contexts are properly set and the hook is initialized with a valid message.`;
var useReactionHandler = (message) => {
  const thread = useThreadContext();
  const { updateMessage } = useChannelActionContext("useReactionHandler");
  const { channel, channelCapabilities } = useChannelStateContext("useReactionHandler");
  const { client } = useChatContext("useReactionHandler");
  const createMessagePreview = (0, import_react29.useCallback)(
    (add, reaction, message2) => {
      const newReactionGroups = message2?.reaction_groups || {};
      const reactionType = reaction.type;
      const hasReaction = !!newReactionGroups[reactionType];
      if (add) {
        const timestamp = (/* @__PURE__ */ new Date()).toISOString();
        newReactionGroups[reactionType] = hasReaction ? {
          ...newReactionGroups[reactionType],
          count: newReactionGroups[reactionType].count + 1
        } : {
          count: 1,
          first_reaction_at: timestamp,
          last_reaction_at: timestamp,
          sum_scores: 1
        };
      } else {
        if (hasReaction && newReactionGroups[reactionType].count > 1) {
          newReactionGroups[reactionType] = {
            ...newReactionGroups[reactionType],
            count: newReactionGroups[reactionType].count - 1
          };
        } else {
          delete newReactionGroups[reactionType];
        }
      }
      const newReactions = add ? [reaction, ...message2?.latest_reactions || []] : message2.latest_reactions?.filter(
        (item) => !(item.type === reaction.type && item.user_id === reaction.user_id)
      );
      const newOwnReactions = add ? [reaction, ...message2?.own_reactions || []] : message2?.own_reactions?.filter((item) => item.type !== reaction.type);
      return {
        ...message2,
        latest_reactions: newReactions || message2.latest_reactions,
        own_reactions: newOwnReactions,
        reaction_groups: newReactionGroups
      };
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [client.user, client.userID]
  );
  const createReactionPreview = (type) => ({
    message_id: message?.id,
    score: 1,
    type,
    user: client.user,
    user_id: client.user?.id
  });
  const toggleReaction = (0, import_lodash3.default)(async (id3, type, add) => {
    if (!message || !channelCapabilities["send-reaction"]) return;
    const newReaction = createReactionPreview(type);
    const tempMessage = createMessagePreview(add, newReaction, message);
    try {
      updateMessage(tempMessage);
      thread?.upsertReplyLocally({ message: tempMessage });
      const messageResponse = add ? await channel.sendReaction(id3, { type }) : await channel.deleteReaction(id3, type);
      updateMessage(messageResponse.message);
    } catch (error2) {
      updateMessage(message);
      thread?.upsertReplyLocally({ message });
    }
  }, 1e3);
  return async (reactionType, event) => {
    if (event?.preventDefault) {
      event.preventDefault();
    }
    if (!message) {
      return console.warn(reactionHandlerWarning);
    }
    let userExistingReaction = null;
    if (message.own_reactions) {
      message.own_reactions.forEach((reaction) => {
        if (reaction.user && client.userID === reaction.user.id && reaction.type === reactionType) {
          userExistingReaction = reaction;
        } else if (reaction.user && client.userID !== reaction.user.id) {
          console.warn(
            `message.own_reactions contained reactions from a different user, this indicates a bug`
          );
        }
      });
    }
    try {
      if (userExistingReaction) {
        await toggleReaction(message.id, userExistingReaction.type, false);
      } else {
        await toggleReaction(message.id, reactionType, true);
      }
    } catch (error2) {
      console.log({ error: error2 });
    }
  };
};
var useRetryHandler = (customRetrySendMessage) => {
  const { retrySendMessage: contextRetrySendMessage } = useChannelActionContext("useRetryHandler");
  const retrySendMessage = customRetrySendMessage || contextRetrySendMessage;
  return async (message) => {
    if (message) {
      await retrySendMessage(message);
    }
  };
};
var useUserHandler = (message, eventHandlers) => ({
  onUserClick: (event) => {
    if (typeof eventHandlers?.onUserClickHandler !== "function" || !message?.user) {
      return;
    }
    eventHandlers.onUserClickHandler(event, message.user);
  },
  onUserHover: (event) => {
    if (typeof eventHandlers?.onUserHoverHandler !== "function" || !message?.user) {
      return;
    }
    eventHandlers.onUserHoverHandler(event, message.user);
  }
});
var useUserRole = (message, disableQuotedMessages) => {
  const { channel, channelCapabilities = {} } = useChannelStateContext("useUserRole");
  const { client } = useChatContext("useUserRole");
  const isAdmin = client.user?.role === "admin" || channel.state.membership.role === "admin";
  const isOwner = channel.state.membership.role === "owner";
  const isModerator = client.user?.role === "channel_moderator" || channel.state.membership.role === "channel_moderator" || channel.state.membership.role === "moderator" || channel.state.membership.is_moderator === true || channel.state.membership.channel_role === "channel_moderator";
  const isMyMessage = client.userID === message.user?.id;
  const canEdit = !message.poll && message.command !== "giphy" && (channelCapabilities["update-any-message"] || isMyMessage && channelCapabilities["update-own-message"]);
  const canDelete = channelCapabilities["delete-any-message"] || isMyMessage && channelCapabilities["delete-own-message"];
  const canFlag = !isMyMessage && channelCapabilities["flag-message"];
  const canMute = !isMyMessage && channelCapabilities["mute-channel"];
  const canBlockUser = !isMyMessage;
  const canMarkUnread = !isMyMessage && channelCapabilities["read-events"];
  const canQuote = !disableQuotedMessages && channelCapabilities["quote-message"];
  const canReact = channelCapabilities["send-reaction"];
  const canReply = channelCapabilities["send-reply"];
  const canSendMessage = channelCapabilities["send-message"];
  return {
    canBlockUser,
    canDelete,
    canEdit,
    canFlag,
    canMarkUnread,
    canMute,
    canQuote,
    canReact,
    canReply,
    canSendMessage,
    isAdmin,
    isModerator,
    isMyMessage,
    isOwner
  };
};
var MAX_MESSAGE_REACTIONS_TO_FETCH = 1e3;
function useReactionsFetcher(message) {
  const { client } = useChatContext("useRectionsFetcher");
  return useStableCallback(
    (reactionType, sort) => fetchMessageReactions(client, message.id, reactionType, sort)
  );
}
async function fetchMessageReactions(client, messageId, reactionType, sort) {
  const reactions = [];
  const limit = 25;
  let next;
  let hasNext = true;
  while (hasNext && reactions.length < MAX_MESSAGE_REACTIONS_TO_FETCH) {
    const response = await client.queryReactions(
      messageId,
      reactionType ? { type: reactionType } : {},
      sort,
      { limit, next }
    );
    reactions.push(...response.reactions);
    next = response.next;
    hasNext = Boolean(next);
  }
  return reactions;
}
var DEFAULT_LETTER_INTERVAL = 30;
var DEFAULT_RENDERING_LETTER_COUNT = 2;
var useMessageTextStreaming = ({
  renderingLetterCount = DEFAULT_RENDERING_LETTER_COUNT,
  streamingLetterIntervalMs = DEFAULT_LETTER_INTERVAL,
  text: text22
}) => {
  const [streamedMessageText, setStreamedMessageText] = (0, import_react29.useState)(text22);
  const textCursor = (0, import_react29.useRef)(text22.length);
  (0, import_react29.useEffect)(() => {
    const textLength = text22.length;
    const interval = setInterval(() => {
      if (!text22 || textCursor.current >= textLength) {
        clearInterval(interval);
        return;
      }
      const newCursorValue = textCursor.current + renderingLetterCount;
      const newText = text22.substring(0, newCursorValue);
      textCursor.current += newText.length - textCursor.current;
      setStreamedMessageText(newText);
    }, streamingLetterIntervalMs);
    return () => {
      clearInterval(interval);
    };
  }, [streamingLetterIntervalMs, renderingLetterCount, text22]);
  const skipAnimation = useStableCallback(() => {
    textCursor.current = text22.length;
    setStreamedMessageText(text22);
  });
  return { skipAnimation, streamedMessageText };
};
var useMessageReminder = (messageId) => {
  const { client } = useChatContext();
  const reminderSelector = (0, import_react29.useCallback)(
    (state) => ({
      reminder: state.reminders.get(messageId)
    }),
    [messageId]
  );
  const { reminder } = useStateStore(client.reminders.state, reminderSelector);
  return reminder;
};
var Root = (0, import_react29.forwardRef)(function AlertRoot({ children, className, ...props }, ref) {
  return (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__alert-root", className), ref, children });
});
var Header$1 = (0, import_react29.forwardRef)(function AlertHeader({ children, className, description, Icon, title, ...props }, ref) {
  return (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__alert-header", className), ref, children: title ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    Icon && (0, import_jsx_runtime4.jsx)(Icon, {}),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__alert-header__copy", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__alert-header__title", children: title }),
      description && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__alert-header__description", children: description })
    ] })
  ] }) : children });
});
var Actions = (0, import_react29.forwardRef)(function AlertActions({ children, className, ...props }, ref) {
  return (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__alert-actions", className), ref, children });
});
var Alert = {
  Actions,
  Header: Header$1,
  Root
};
function useDialogAnchor({
  allowFlip,
  offset: offset4,
  open,
  placement,
  referenceElement,
  updateKey,
  updatePositionOnContentResize = false
}) {
  const [popperElement, setPopperElement] = (0, import_react29.useState)(null);
  const [stabilisedChosenPlacement, setStabilisedChosenPlacement] = (0, import_react29.useState)(null);
  const {
    placement: chosenPlacement,
    refs,
    strategy,
    update,
    x: x2,
    y: y2
  } = usePopoverPosition({
    allowFlip,
    freeze: true,
    offset: offset4,
    placement: stabilisedChosenPlacement ?? placement
  });
  if (!stabilisedChosenPlacement && popperElement && placement !== chosenPlacement) {
    setStabilisedChosenPlacement(chosenPlacement);
  } else if (stabilisedChosenPlacement && !popperElement) {
    setStabilisedChosenPlacement(null);
  }
  const frozenReferenceRef = (0, import_react29.useRef)(null);
  if (open && referenceElement && !frozenReferenceRef.current) {
    frozenReferenceRef.current = referenceElement;
  }
  if (!open) {
    frozenReferenceRef.current = null;
  }
  const effectiveReference = open ? frozenReferenceRef.current : referenceElement;
  (0, import_react29.useEffect)(() => {
    refs.setReference(effectiveReference);
  }, [effectiveReference, refs]);
  (0, import_react29.useEffect)(() => {
    refs.setFloating(popperElement);
  }, [popperElement, refs]);
  (0, import_react29.useEffect)(() => {
    if (open && popperElement && effectiveReference) {
      update?.();
    }
  }, [open, placement, popperElement, update, updateKey, effectiveReference]);
  (0, import_react29.useEffect)(() => {
    if (!popperElement || !updatePositionOnContentResize) return;
    const resizeObserver = new ResizeObserver(update);
    resizeObserver.observe(popperElement);
    return () => {
      resizeObserver.disconnect();
    };
  }, [popperElement, update, updatePositionOnContentResize]);
  if (popperElement && !open) {
    setPopperElement(null);
  }
  return {
    placement: stabilisedChosenPlacement ?? chosenPlacement,
    setPopperElement,
    styles: {
      left: x2 ?? 0,
      position: strategy,
      top: y2 ?? 0
    }
  };
}
var DialogAnchor = ({
  allowFlip = true,
  children,
  className,
  closeOnClickOutside,
  closeTransitionMs = 0,
  dialogManagerId,
  focus = true,
  id: id3,
  offset: offset4,
  placement = "auto",
  referenceElement = null,
  tabIndex,
  trapFocus,
  updateKey,
  updatePositionOnContentResize,
  ...restDivProps
}) => {
  const dialog = useDialog({ closeOnClickOutside, dialogManagerId, id: id3 });
  const open = useDialogIsOpen(id3, dialogManagerId);
  const [shouldRender, setShouldRender] = (0, import_react29.useState)(open);
  const closeTimeoutRef = (0, import_react29.useRef)(null);
  const isClosing = !open && shouldRender;
  (0, import_react29.useEffect)(() => {
    if (open) {
      setShouldRender(true);
      if (closeTimeoutRef.current) {
        clearTimeout(closeTimeoutRef.current);
        closeTimeoutRef.current = null;
      }
      return;
    }
    if (!shouldRender) return;
    if (!closeTransitionMs) {
      setShouldRender(false);
      return;
    }
    closeTimeoutRef.current = setTimeout(() => {
      setShouldRender(false);
      closeTimeoutRef.current = null;
    }, closeTransitionMs);
  }, [closeTransitionMs, open, shouldRender]);
  (0, import_react29.useEffect)(
    () => () => {
      if (closeTimeoutRef.current) clearTimeout(closeTimeoutRef.current);
    },
    []
  );
  const {
    placement: chosenPlacement,
    setPopperElement,
    styles
  } = useDialogAnchor({
    allowFlip,
    offset: offset4,
    open: shouldRender,
    placement,
    referenceElement,
    updateKey,
    updatePositionOnContentResize
  });
  (0, import_react29.useEffect)(() => {
    if (!open) return;
    const hideOnEscape = (event) => {
      if (event.key !== "Escape") return;
      dialog?.close();
    };
    document.addEventListener("keyup", hideOnEscape);
    return () => {
      document.removeEventListener("keyup", hideOnEscape);
    };
  }, [dialog, open]);
  if (!shouldRender) {
    return null;
  }
  return (0, import_jsx_runtime4.jsx)(DialogPortalEntry, { dialogId: id3, dialogManagerId, children: (0, import_jsx_runtime4.jsx)($535772f9d2c1f38d$export$20e40289641fbbb6, { autoFocus: focus, contain: trapFocus, restoreFocus: true, children: (0, import_jsx_runtime4.jsx)(
    "div",
    {
      ...restDivProps,
      className: clsx_default("str-chat__dialog-contents", className),
      "data-str-chat-dialog-state": isClosing ? "closing" : "open",
      "data-str-chat-placement": chosenPlacement,
      "data-testid": "str-chat__dialog-contents",
      ref: setPopperElement,
      style: styles,
      tabIndex: typeof tabIndex !== "undefined" ? tabIndex : 0,
      children
    }
  ) }) });
};
var Callout = ({
  children,
  className,
  dialogManagerId,
  id: dialogId2,
  onClose,
  ...anchorProps
}) => {
  const { CalloutDialog = DefaultCalloutDialog } = useComponentContext();
  const dialogIsOpen = useDialogIsOpen(dialogId2, dialogManagerId);
  return (0, import_jsx_runtime4.jsx)(DialogAnchor, { ...anchorProps, dialogManagerId, id: dialogId2, children: dialogIsOpen && (0, import_jsx_runtime4.jsx)(CalloutDialog, { className, onClose, children }) });
};
var DefaultCalloutDialog = ({ children, className, onClose }) => (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__callout", children: [
  children,
  (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "ghost",
      circular: true,
      className: clsx_default(className, "str-chat__callout__close-button"),
      onClick: onClose,
      size: "sm",
      variant: "secondary",
      children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
    }
  )
] });
var getInitials = (name2) => {
  const regex = new RegExp("(\\p{L}{1})\\p{L}+", "gu");
  if (!name2 || name2.trim().length === 0) {
    return "";
  }
  const initials = Array.from(name2?.matchAll(regex) || []);
  if (!initials.length) {
    return "";
  }
  const startInitial = initials.at(0)[1];
  const endInitial = initials.length > 1 ? initials.at(-1)[1] : "";
  return `${startInitial}${endInitial}`;
};
var Avatar = ({
  className,
  imageUrl,
  isOnline,
  size: size4,
  userName,
  ...rest
}) => {
  const [error2, setError] = (0, import_react29.useState)(false);
  (0, import_react29.useEffect)(() => () => setError(false), [imageUrl]);
  const nameString = userName?.toString() || "";
  const sizeAwareInitials = (0, import_react29.useMemo)(() => {
    const initials = getInitials(nameString);
    if (size4 === "sm" || size4 === "xs") {
      return initials.charAt(0);
    }
    return initials;
  }, [nameString, size4]);
  const showImage = typeof imageUrl === "string" && !error2;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default(`str-chat__avatar`, className, {
        "str-chat__avatar--multiple-letters": sizeAwareInitials.length > 1,
        "str-chat__avatar--no-letters": !sizeAwareInitials.length,
        "str-chat__avatar--one-letter": sizeAwareInitials.length === 1,
        [`str-chat__avatar--size-${size4}`]: typeof size4 === "string"
      }),
      "data-testid": "avatar",
      role: "button",
      title: userName,
      ...rest,
      children: [
        typeof isOnline === "boolean" && (0, import_jsx_runtime4.jsx)(
          "div",
          {
            className: clsx_default("str-chat__avatar-status-badge", {
              "str-chat__avatar-status-badge--offline": !isOnline,
              "str-chat__avatar-status-badge--online": isOnline
            })
          }
        ),
        showImage ? (0, import_jsx_runtime4.jsx)(
          "img",
          {
            alt: sizeAwareInitials,
            className: "str-chat__avatar-image",
            "data-testid": "avatar-img",
            onError: () => setError(true),
            src: imageUrl
          }
        ) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          !!sizeAwareInitials.length && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__avatar-initials", "data-testid": "avatar-fallback", children: sizeAwareInitials }),
          !sizeAwareInitials.length && (0, import_jsx_runtime4.jsx)(IconUser, {})
        ] })
      ]
    }
  );
};
var Badge = ({
  children,
  className,
  size: size4 = "md",
  variant = "default",
  ...spanProps
}) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    ...spanProps,
    className: clsx_default(
      "str-chat__badge",
      `str-chat__badge--variant-${variant}`,
      { [`str-chat__badge--size-${size4}`]: size4 },
      className
    ),
    children
  }
);
var ErrorBadge = ({
  className,
  size: size4 = "sm",
  ...rest
}) => (0, import_jsx_runtime4.jsx)(Badge, { ...rest, className, size: size4, variant: "error", children: (0, import_jsx_runtime4.jsx)(IconExclamationMarkFill, {}) });
function AvatarStack({
  badgeSize,
  capLimit = 3,
  component: Component2 = "div",
  displayInfo = [],
  size: size4
}) {
  const { Avatar: Avatar$1 = Avatar } = useComponentContext(AvatarStack.name);
  const displayInfoToRender = (0, import_react29.useMemo)(
    () => displayInfo.length > capLimit ? displayInfo.slice(0, capLimit) : displayInfo,
    [displayInfo, capLimit]
  );
  const overflowCount = displayInfo.length - displayInfoToRender.length;
  if (!displayInfo.length) {
    return null;
  }
  return (0, import_jsx_runtime4.jsxs)(
    Component2,
    {
      className: clsx_default("str-chat__avatar-stack", {
        [`str-chat__avatar-stack--size-${size4}`]: typeof size4 === "string"
      }),
      "data-testid": "avatar-stack",
      children: [
        displayInfoToRender.map((info, index6) => (0, import_jsx_runtime4.jsx)(
          Avatar$1,
          {
            imageUrl: info.imageUrl,
            size: size4,
            userName: info.userName
          },
          info.id ?? `${info.userName}-${info.imageUrl}-${index6}`
        )),
        typeof overflowCount === "number" && overflowCount > 0 && (0, import_jsx_runtime4.jsxs)(
          Badge,
          {
            className: "str-chat__avatar-stack__count-badge",
            "data-testid": "avatar-stack-count-badge",
            size: badgeSize ?? size4,
            variant: "counter",
            children: [
              "+",
              overflowCount
            ]
          }
        )
      ]
    }
  );
}
var ChannelAvatar = ({
  displayMembers,
  imageUrl,
  size: size4,
  userName,
  ...sharedProps
}) => {
  const displayInfo = (0, import_react29.useMemo)(() => {
    if (displayMembers && displayMembers.length > 0) {
      return displayMembers;
    }
    return [
      {
        imageUrl,
        userName
      }
    ];
  }, [displayMembers, imageUrl, userName]);
  return (0, import_jsx_runtime4.jsx)(GroupAvatar, { displayMembers: displayInfo, size: size4, ...sharedProps });
};
var GroupAvatar = ({
  badgeSize,
  className,
  displayMembers = [],
  isOnline,
  size: size4,
  ...rest
}) => {
  const displayMembersToRender = (0, import_react29.useMemo)(
    () => displayMembers.length > 4 ? displayMembers.slice(0, 2) : displayMembers,
    [displayMembers]
  );
  const overflowCount = displayMembers.length - displayMembersToRender.length;
  if (displayMembers.length < 2) {
    const firstUser = displayMembers[0];
    return (0, import_jsx_runtime4.jsx)(
      Avatar,
      {
        imageUrl: firstUser?.imageUrl,
        isOnline,
        size: size4,
        userName: firstUser?.userName,
        ...rest
      }
    );
  }
  let avatarSize = null;
  if (size4 === "2xl") {
    avatarSize = "lg";
  } else if (size4 === "xl") {
    avatarSize = "md";
  } else if (size4 === "lg") {
    avatarSize = "sm";
  }
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default(
        "str-chat__avatar-group",
        {
          "str-chat__avatar-group--offline": typeof isOnline === "boolean" && !isOnline,
          "str-chat__avatar-group--online": typeof isOnline === "boolean" && isOnline,
          [`str-chat__avatar-group--size-${size4}`]: typeof size4 === "string"
        },
        className
      ),
      "data-testid": "group-avatar",
      role: "button",
      ...rest,
      children: [
        displayMembersToRender.map(({ id: id3, imageUrl, userName }, index6) => (0, import_jsx_runtime4.jsx)(
          Avatar,
          {
            imageUrl,
            size: avatarSize,
            userName
          },
          id3 || `${userName}-${imageUrl}-${index6}`
        )),
        typeof overflowCount === "number" && overflowCount > 0 && (0, import_jsx_runtime4.jsxs)(
          Badge,
          {
            className: "str-chat__avatar-group__count-badge",
            "data-testid": "group-avatar-count-badge",
            size: badgeSize,
            variant: "counter",
            children: [
              "+",
              overflowCount
            ]
          }
        )
      ]
    }
  );
};
var BaseContextMenuButton = ({
  children,
  className,
  details,
  hasSubMenu,
  Icon,
  label,
  SubmenuIcon = IconChevronRight,
  variant,
  ...props
}) => (0, import_jsx_runtime4.jsxs)(
  "button",
  {
    ...props,
    className: clsx_default(
      "str-chat__context-menu__button",
      {
        "str-chat__context-menu__button--with-submenu": hasSubMenu,
        [`str-chat__context-menu__button--${variant}`]: typeof variant === "string"
      },
      className
    ),
    type: "button",
    children: [
      Icon && (0, import_jsx_runtime4.jsx)(Icon, { className: "str-chat__context-menu__button__icon" }),
      label ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__context-menu__button__label", children: label }),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__context-menu__button__details", children: details })
      ] }) : (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__context-menu__button__label", children }),
      !!hasSubMenu && (0, import_jsx_runtime4.jsx)(SubmenuIcon, { className: "str-chat__context-menu__button__submenu-icon" })
    ]
  }
);
var UserContextMenuButton = ({
  children,
  className,
  imageUrl,
  userName,
  ...props
}) => (0, import_jsx_runtime4.jsxs)(
  "button",
  {
    ...props,
    className: clsx_default(
      "str-chat__context-menu__button str-chat__user-context-menu__button",
      className
    ),
    type: "button",
    children: [
      (0, import_jsx_runtime4.jsx)(Avatar, { imageUrl, size: "sm", userName }),
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__context-menu__button__label", children: children ?? userName })
    ]
  }
);
var EmojiContextMenuButton = ({
  children,
  className,
  emoji,
  label,
  ...props
}) => (0, import_jsx_runtime4.jsxs)(
  "button",
  {
    ...props,
    className: clsx_default(
      "str-chat__context-menu__button str-chat__emoji-context-menu__button",
      className
    ),
    type: "button",
    children: [
      (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__context-menu__button__emoji str-chat__emoji-item--entity", children: emoji }),
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__context-menu__button__label", children: children ?? label })
    ]
  }
);
var ContextMenuButtonWithSubmenu = ({
  children,
  className,
  Submenu,
  submenuContainerProps,
  submenuPlacement = "right-start",
  submenuRollAxis = "x",
  ...buttonProps
}) => {
  const { className: submenuClassName, ...submenuContainerRestProps } = submenuContainerProps ?? {};
  const buttonRef = (0, import_react29.useRef)(null);
  const [dialogContainer, setDialogContainer] = (0, import_react29.useState)(null);
  const keepSubmenuOpenFlag = (0, import_react29.useRef)(false);
  const dialogCloseTimeout = (0, import_react29.useRef)(null);
  const dialogId2 = (0, import_react29.useMemo)(() => `submenu-${Math.random().toString(36).slice(2)}`, []);
  const { dialog, dialogManager } = useDialogOnNearestManager({ id: dialogId2 });
  const dialogIsOpen = useDialogIsOpen(dialogId2, dialogManager?.id);
  const {
    placement: chosenPlacement,
    setPopperElement,
    styles
  } = useDialogAnchor({
    offset: 8,
    open: dialogIsOpen,
    placement: submenuPlacement,
    referenceElement: buttonRef.current
  });
  const closeDialogLazily = (0, import_react29.useCallback)(() => {
    if (dialogCloseTimeout.current) clearTimeout(dialogCloseTimeout.current);
    dialogCloseTimeout.current = setTimeout(() => {
      if (keepSubmenuOpenFlag.current) return;
      dialog.close();
    }, 100);
  }, [dialog]);
  const keepSubmenuOpen = (0, import_react29.useCallback)(() => {
    keepSubmenuOpenFlag.current = true;
  }, []);
  const allowToCloseSubmenu = (0, import_react29.useCallback)(() => {
    keepSubmenuOpenFlag.current = false;
  }, []);
  const closeSubmenu = (0, import_react29.useCallback)(() => {
    allowToCloseSubmenu();
    closeDialogLazily();
  }, [allowToCloseSubmenu, closeDialogLazily]);
  const handleClose = (0, import_react29.useCallback)(
    (event) => {
      const parentButton = buttonRef.current;
      if (!dialogIsOpen || !parentButton) return;
      event.stopPropagation();
      closeDialogLazily();
      parentButton.focus();
    },
    [closeDialogLazily, dialogIsOpen, buttonRef]
  );
  const handleFocusParentButton = () => {
    if (dialogIsOpen) return;
    dialog.open();
    keepSubmenuOpen();
  };
  (0, import_react29.useEffect)(() => {
    const parentButton = buttonRef.current;
    if (!dialogIsOpen || !parentButton) return;
    const hideOnEscape = (event) => {
      if (event.key !== "Escape") return;
      handleClose(event);
      closeSubmenu();
    };
    document.addEventListener("keyup", hideOnEscape, { capture: true });
    return () => {
      document.removeEventListener("keyup", hideOnEscape, { capture: true });
    };
  }, [dialogIsOpen, handleClose, closeSubmenu]);
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      BaseContextMenuButton,
      {
        "aria-selected": "false",
        className: clsx_default(className, "str_chat__button-with-submenu", {
          "str_chat__button-with-submenu--submenu-open": dialogIsOpen
        }),
        hasSubMenu: true,
        onBlur: closeSubmenu,
        onClick: (event) => {
          event.stopPropagation();
          dialog.toggle();
        },
        onFocus: handleFocusParentButton,
        onMouseEnter: handleFocusParentButton,
        onMouseLeave: closeSubmenu,
        role: "option",
        ...buttonProps,
        ref: buttonRef,
        children
      }
    ),
    dialogIsOpen && (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: clsx_default("str-chat__context-menu__submenu-container", submenuClassName),
        "data-str-chat-placement": chosenPlacement,
        "data-str-chat-roll-axis": submenuRollAxis,
        onBlur: (event) => {
          const isBlurredDescendant = event.relatedTarget instanceof Node && dialogContainer?.contains(event.relatedTarget);
          if (isBlurredDescendant) return;
          closeSubmenu();
        },
        onFocus: keepSubmenuOpen,
        onMouseEnter: keepSubmenuOpen,
        onMouseLeave: closeSubmenu,
        ref: (element4) => {
          setPopperElement(element4);
          setDialogContainer(element4);
        },
        style: styles,
        tabIndex: -1,
        ...submenuContainerRestProps,
        children: (0, import_jsx_runtime4.jsx)(Submenu, {})
      }
    )
  ] });
};
var ContextMenuButton = (props) => {
  const {
    Submenu,
    submenuContainerProps,
    submenuPlacement,
    submenuRollAxis,
    ...buttonProps
  } = props;
  const [isFocused, setIsFocused] = (0, import_react29.useState)(false);
  if (Submenu) {
    return (0, import_jsx_runtime4.jsx)(
      ContextMenuButtonWithSubmenu,
      {
        ...buttonProps,
        Submenu,
        submenuContainerProps,
        submenuPlacement,
        submenuRollAxis
      }
    );
  }
  const { onBlur, onFocus, ...baseButtonProps } = buttonProps;
  return (0, import_jsx_runtime4.jsx)(
    BaseContextMenuButton,
    {
      ...baseButtonProps,
      "aria-selected": isFocused ? "true" : "false",
      onBlur: (e) => {
        setIsFocused(false);
        onBlur?.(e);
      },
      onFocus: (e) => {
        setIsFocused(true);
        onFocus?.(e);
      }
    }
  );
};
var ContextMenuBackButton = ({
  children,
  className,
  ...props
}) => (0, import_jsx_runtime4.jsx)(
  "button",
  {
    ...props,
    className: clsx_default("str-chat__context-menu__back-button", className),
    type: "button",
    children
  }
);
var ContextMenuHeader = ({
  children,
  className,
  ...props
}) => (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__context-menu__header", className), children });
var ContextMenuBody = ({
  children,
  className,
  ...props
}) => (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__context-menu__body", className), children });
var ContextMenuRoot = import_react29.default.forwardRef(
  function ContextMenuRoot2({ className, ...props }, ref) {
    return (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__context-menu", className), ref });
  }
);
var ContextMenuContext = import_react29.default.createContext(
  void 0
);
var useContextMenuContext = () => (0, import_react29.useContext)(ContextMenuContext);
function ContextMenuContent({
  anchorReferenceElement,
  backLabel = "Back",
  children,
  className,
  enableAnimations = true,
  Header: Header2,
  items,
  ItemsWrapper,
  menuClassName,
  onClose,
  onMenuLevelChange,
  transitionDirection,
  ...props
}) {
  const rootLevel = (0, import_react29.useMemo)(
    () => ({
      Header: Header2,
      items,
      ItemsWrapper,
      menuClassName
    }),
    [Header2, items, ItemsWrapper, menuClassName]
  );
  const [menuStack, setMenuStack] = (0, import_react29.useState)(() => [rootLevel]);
  const [menuBodyAnimationKey, setMenuBodyAnimationKey] = (0, import_react29.useState)(0);
  const activeMenu = menuStack[menuStack.length - 1];
  const ActiveMenuItemsWrapper = activeMenu.ItemsWrapper ?? import_react29.default.Fragment;
  const closeMenu = (0, import_react29.useCallback)(() => {
    onClose?.();
  }, [onClose]);
  const openSubmenu = (0, import_react29.useCallback)(
    ({
      Header: Header22,
      ItemsWrapper: SubmenuItemsWrapper,
      menuClassName: menuClassName2,
      Submenu
    }) => {
      const nextLevel = {
        Header: Header22,
        ItemsWrapper: SubmenuItemsWrapper ?? ItemsWrapper,
        menuClassName: menuClassName2,
        Submenu
      };
      setMenuStack((current) => [...current, nextLevel]);
    },
    [ItemsWrapper]
  );
  const returnToParentMenu = (0, import_react29.useCallback)(() => {
    setMenuStack((current) => {
      if (current.length <= 1) return current;
      return current.slice(0, -1);
    });
  }, []);
  (0, import_react29.useEffect)(() => {
    setMenuStack((current) => {
      if (current.length === 1 && current[0] === rootLevel) return current;
      return [rootLevel];
    });
  }, [rootLevel]);
  (0, import_react29.useEffect)(() => {
    onMenuLevelChange?.(menuStack.length);
  }, [menuStack.length, onMenuLevelChange]);
  (0, import_react29.useEffect)(() => {
    if (!transitionDirection) return;
    setMenuBodyAnimationKey((value) => value + 1);
  }, [transitionDirection, menuStack.length]);
  return (0, import_jsx_runtime4.jsx)(
    ContextMenuContext.Provider,
    {
      value: { anchorReferenceElement, closeMenu, openSubmenu, returnToParentMenu },
      children: (0, import_jsx_runtime4.jsxs)(
        ContextMenuRoot,
        {
          className: clsx_default(className, activeMenu.menuClassName),
          "data-str-chat-enable-animations": enableAnimations,
          ...props,
          children: [
            activeMenu.Header ? (0, import_jsx_runtime4.jsx)(activeMenu.Header, {}) : menuStack.length > 1 ? (0, import_jsx_runtime4.jsx)(ContextMenuHeader, { children: (0, import_jsx_runtime4.jsxs)(ContextMenuBackButton, { onClick: returnToParentMenu, children: [
              (0, import_jsx_runtime4.jsx)(IconChevronLeft, {}),
              (0, import_jsx_runtime4.jsx)("span", { children: backLabel })
            ] }) }) : null,
            (0, import_jsx_runtime4.jsx)(
              ContextMenuBody,
              {
                className: clsx_default({
                  "str-chat__context-menu__body--submenu-backward": transitionDirection === "backward",
                  "str-chat__context-menu__body--submenu-forward": transitionDirection === "forward"
                }),
                children: activeMenu.Submenu ? (0, import_jsx_runtime4.jsx)(activeMenu.Submenu, {}) : (0, import_jsx_runtime4.jsx)(ActiveMenuItemsWrapper, { children: typeof children !== "undefined" ? children : activeMenu.items?.map((Item2, index6) => (0, import_jsx_runtime4.jsx)(Item2, {}, `context-menu-item-${index6}`)) })
              },
              `context-menu-body-${menuStack.length}-${menuBodyAnimationKey}`
            )
          ]
        }
      )
    }
  );
}
var ContextMenu = (props) => {
  const { ContextMenuContent: ContextMenuContentComponent = ContextMenuContent } = useComponentContext();
  const {
    allowFlip,
    closeOnClickOutside,
    closeTransitionMs = 130,
    dialogManagerId,
    focus,
    id: id3,
    offset: offset4 = 8,
    placement,
    referenceElement,
    submenuTransitionDurationMs,
    tabIndex,
    trapFocus,
    ...menuProps
  } = props;
  const resolvedSubmenuTransitionDurationMs = submenuTransitionDurationMs ?? 460;
  const isAnchored = id3 != null;
  const [menuLevel, setMenuLevel] = (0, import_react29.useState)(1);
  const [transitionDirection, setTransitionDirection] = (0, import_react29.useState)(void 0);
  const [contentResetToken, setContentResetToken] = (0, import_react29.useState)(0);
  const transitionTimeoutRef = (0, import_react29.useRef)(null);
  const previousMenuLevelRef = (0, import_react29.useRef)(1);
  const open = useDialogIsOpen(id3 ?? "", dialogManagerId);
  const previousOpenRef = (0, import_react29.useRef)(open);
  (0, import_react29.useEffect)(() => {
    if (!isAnchored) return;
    if (previousOpenRef.current && !open) {
      setMenuLevel(1);
      setTransitionDirection(void 0);
      setContentResetToken((value) => value + 1);
      previousMenuLevelRef.current = 1;
      if (transitionTimeoutRef.current) {
        clearTimeout(transitionTimeoutRef.current);
        transitionTimeoutRef.current = null;
      }
    }
    previousOpenRef.current = open;
  }, [isAnchored, open]);
  (0, import_react29.useEffect)(
    () => () => {
      if (transitionTimeoutRef.current) {
        clearTimeout(transitionTimeoutRef.current);
      }
    },
    []
  );
  const handleMenuLevelChange = (0, import_react29.useCallback)(
    (level) => {
      if (isAnchored) {
        const previousLevel = previousMenuLevelRef.current;
        if (level !== previousLevel) {
          setTransitionDirection(level > previousLevel ? "forward" : "backward");
          if (transitionTimeoutRef.current) clearTimeout(transitionTimeoutRef.current);
          transitionTimeoutRef.current = setTimeout(() => {
            setTransitionDirection(void 0);
            transitionTimeoutRef.current = null;
          }, resolvedSubmenuTransitionDurationMs);
        }
        previousMenuLevelRef.current = level;
        setMenuLevel(level);
        return;
      }
      menuProps.onMenuLevelChange?.(level);
    },
    [isAnchored, menuProps, resolvedSubmenuTransitionDurationMs]
  );
  const content3 = (0, import_react29.createElement)(
    ContextMenuContentComponent,
    {
      anchorReferenceElement: isAnchored ? referenceElement : void 0,
      ...menuProps,
      key: `context-menu-content-${contentResetToken}`,
      onMenuLevelChange: handleMenuLevelChange,
      transitionDirection
    }
  );
  if (isAnchored) {
    const {
      backLabel: _b,
      enableAnimations: _ea,
      Header: _h,
      items: _i,
      ItemsWrapper: _w,
      menuClassName: _m,
      onClose: _c,
      onMenuLevelChange: _l,
      ...anchorDivProps
    } = menuProps;
    return (0, import_jsx_runtime4.jsx)(
      DialogAnchor,
      {
        allowFlip,
        closeOnClickOutside,
        closeTransitionMs,
        dialogManagerId,
        focus,
        id: id3,
        offset: offset4,
        placement,
        referenceElement,
        tabIndex,
        trapFocus,
        updateKey: menuLevel,
        ...anchorDivProps,
        children: content3
      }
    );
  }
  return content3;
};
var PromptRoot = ({ children, className, ...props }) => (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__prompt", className), children });
var PromptHeader = ({ className, close, description, title }) => (0, import_jsx_runtime4.jsxs)("div", { className: clsx_default("str-chat__prompt__header", className), children: [
  (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__prompt__header__title-group", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__prompt__header__title", children: title }),
    description != null && description !== "" && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__prompt__header__description", children: description })
  ] }),
  close && (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "ghost",
      circular: true,
      className: "str-chat__prompt__header__close-button",
      onClick: close,
      size: "sm",
      variant: "secondary",
      children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
    }
  )
] });
var PromptBody = ({ children, className }) => (0, import_jsx_runtime4.jsx)("div", { className: clsx_default("str-chat__prompt__body", className), children });
var PromptFooter = ({ children, className }) => (0, import_jsx_runtime4.jsx)("div", { className: clsx_default("str-chat__prompt__footer", className), children });
var PromptFooterControls = ({ children, className }) => (0, import_jsx_runtime4.jsx)("div", { className: clsx_default("str-chat__prompt__footer__controls", className), children });
var PromptFooterControlsButtonSecondary = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    appearance: "ghost",
    className: clsx_default("str-chat__prompt__footer__controls-button", className),
    size: "md",
    variant: "secondary",
    ...props
  }
);
var PromptFooterControlsButtonPrimary = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    appearance: "solid",
    className: clsx_default("str-chat__prompt__footer__controls-button", className),
    size: "md",
    variant: "primary",
    ...props
  }
);
var Prompt = {
  Body: PromptBody,
  Footer: PromptFooter,
  FooterControls: PromptFooterControls,
  FooterControlsButtonPrimary: PromptFooterControlsButtonPrimary,
  FooterControlsButtonSecondary: PromptFooterControlsButtonSecondary,
  Header: PromptHeader,
  Root: PromptRoot
};
var ViewerRoot = ({ children, className, ...props }) => (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__viewer", className), children });
var ViewerHeader = ({
  className,
  close,
  description,
  goBack,
  title
}) => (0, import_jsx_runtime4.jsxs)("div", { className: clsx_default("str-chat__viewer__header", className), children: [
  goBack && (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "ghost",
      circular: true,
      className: "str-chat__viewer__header__go-back-button",
      onClick: goBack,
      size: "sm",
      variant: "secondary",
      children: (0, import_jsx_runtime4.jsx)(IconArrowLeft, {})
    }
  ),
  (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__viewer__header__title-group", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__viewer__header__title", children: title }),
    description != null && description !== "" && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__viewer__header__description", children: description })
  ] }),
  close && (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "ghost",
      circular: true,
      className: "str-chat__viewer__header__close-button",
      onClick: close,
      size: "sm",
      variant: "secondary",
      children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
    }
  )
] });
var ViewerBody = ({ children, className }) => (0, import_jsx_runtime4.jsx)("div", { className: clsx_default("str-chat__viewer__body", className), children });
var ViewerFooter = ({ children, className }) => (0, import_jsx_runtime4.jsx)("div", { className: clsx_default("str-chat__viewer__footer", className), children });
var ViewerFooterControls = ({ children, className }) => (0, import_jsx_runtime4.jsx)("div", { className: clsx_default("str-chat__viewer__footer__controls", className), children });
var ViewerFooterControlsButtonSecondary = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    appearance: "ghost",
    className: clsx_default("str-chat__viewer__footer__controls-button", className),
    size: "md",
    variant: "secondary",
    ...props
  }
);
var ViewerFooterControlsButtonPrimary = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    appearance: "solid",
    className: clsx_default("str-chat__viewer__footer__controls-button", className),
    size: "md",
    variant: "primary",
    ...props
  }
);
var Viewer = {
  Body: ViewerBody,
  Footer: ViewerFooter,
  FooterControls: ViewerFooterControls,
  FooterControlsButtonPrimary: ViewerFooterControlsButtonPrimary,
  FooterControlsButtonSecondary: ViewerFooterControlsButtonSecondary,
  Header: ViewerHeader,
  Root: ViewerRoot
};
var GlobalModal = ({
  children,
  className,
  CloseButtonOnOverlay,
  onClose,
  onCloseAttempt,
  open
}) => {
  const dialog = useModalDialog();
  const isOpen = useModalDialogIsOpen();
  const overlayRef = (0, import_react29.useRef)(null);
  const closeButtonRef = (0, import_react29.useRef)(null);
  const closingRef = (0, import_react29.useRef)(false);
  const { theme } = useChatContext("GlobalModal");
  const maybeClose = (0, import_react29.useCallback)(
    (source, event) => {
      const allow = onCloseAttempt?.(source, event);
      if (allow !== false) {
        dialog.close();
        closingRef.current = true;
        onClose?.(event);
      }
    },
    [dialog, onClose, onCloseAttempt]
  );
  const modalContextValue = (0, import_react29.useMemo)(
    () => ({
      close: () => maybeClose("button", {})
    }),
    [maybeClose]
  );
  const handleClick = (event) => {
    const target = event.target;
    if (closeButtonRef.current?.contains(target)) {
      maybeClose("button", event);
    } else if (overlayRef.current === target) {
      maybeClose("overlay", event);
    }
  };
  (0, import_react29.useEffect)(() => {
    if (!open || !isOpen) return;
    const handleKeyDown = (event) => {
      if (event.key === "Escape") maybeClose("escape", event);
    };
    document.addEventListener("keydown", handleKeyDown);
    return () => document.removeEventListener("keydown", handleKeyDown);
  }, [isOpen, maybeClose, open]);
  (0, import_react29.useEffect)(() => {
    if (!open) {
      closingRef.current = false;
    }
    if (open && !isOpen && !closingRef.current) {
      dialog.open();
    }
  }, [dialog, isOpen, open]);
  if (!open || !isOpen) return null;
  return (0, import_jsx_runtime4.jsx)(DialogPortalEntry, { dialogId: modalDialogId, dialogManagerId: modalDialogManagerId, children: (0, import_jsx_runtime4.jsx)(ModalContextProvider, { value: modalContextValue, children: (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default(
        "str-chat",
        theme,
        "str-chat__modal str-chat-react__modal str-chat__modal--open",
        className
      ),
      onClick: handleClick,
      ref: overlayRef,
      children: [
        (0, import_jsx_runtime4.jsx)($535772f9d2c1f38d$export$20e40289641fbbb6, { autoFocus: true, contain: true, children }),
        CloseButtonOnOverlay && (0, import_jsx_runtime4.jsx)(CloseButtonOnOverlay, { onClick: handleClick, ref: closeButtonRef })
      ]
    }
  ) }) });
};
function MessageBounceModal({
  MessageBouncePrompt: MessageBouncePrompt2,
  ...modalProps
}) {
  const { Modal = GlobalModal } = useComponentContext();
  return (0, import_jsx_runtime4.jsx)(Modal, { className: "str-chat__message-bounce-modal", ...modalProps, children: (0, import_jsx_runtime4.jsx)(MessageBounceProvider, { children: (0, import_jsx_runtime4.jsx)(MessageBouncePrompt2, {}) }) });
}
function MessageBouncePrompt({ children }) {
  const { handleDelete: handleDelete2, handleEdit, handleRetry } = useMessageBounceContext();
  const { t: t2 } = useTranslationContext();
  const { close } = useModalContext();
  function createHandler(handle2) {
    return (e) => {
      handle2(e);
      close();
    };
  }
  return (0, import_jsx_runtime4.jsxs)(
    Alert.Root,
    {
      className: "str-chat__message-bounce-alert",
      "data-testid": "message-bounce-prompt",
      children: [
        (0, import_jsx_runtime4.jsx)(
          Alert.Header,
          {
            className: "str-chat__message-bounce-alert-header",
            Icon: IconExclamationMark,
            title: !children ? t2("This message did not meet our content guidelines") : void 0,
            children
          }
        ),
        (0, import_jsx_runtime4.jsxs)(Alert.Actions, { className: "str-chat__message-bounce-actions", children: [
          (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "outline",
              className: "str-chat__message-bounce-delete",
              "data-testid": "message-bounce-delete",
              onClick: createHandler(handleDelete2),
              size: "md",
              variant: "danger",
              children: t2("Delete")
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "outline",
              className: "str-chat__message-bounce-edit",
              "data-testid": "message-bounce-edit",
              onClick: createHandler(handleEdit),
              size: "md",
              variant: "secondary",
              children: t2("Edit Message")
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "outline",
              className: "str-chat__message-bounce-send",
              "data-testid": "message-bounce-send",
              onClick: createHandler(handleRetry),
              size: "md",
              variant: "secondary",
              children: t2("Send Anyway")
            }
          )
        ] })
      ]
    }
  );
}
var MessageBubble = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__message-bubble", className) });
var MessageDeletedBubble = () => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)(MessageBubble, { "data-testid": "message-deleted-bubble", children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-text", children: [
    (0, import_jsx_runtime4.jsx)(IconNoSign, {}),
    (0, import_jsx_runtime4.jsx)("span", { children: t2("Message deleted") })
  ] }) });
};
var MessageBlocked = () => {
  const { message } = useMessageContext();
  const { t: t2 } = useTranslationContext("MessageBlocked");
  const { isMyMessage } = useUserRole(message);
  const messageClasses = clsx_default(
    "str-chat__message str-chat__message--blocked",
    message.type,
    {
      "str-chat__message--me": isMyMessage,
      "str-chat__message--other": !isMyMessage
    }
  );
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: messageClasses,
      "data-testid": "message-blocked-component",
      children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message--blocked-inner", children: t2("Message was blocked by moderation policies") })
    },
    message.id
  );
};
var useBaseMessageActionSetFilter = (messageActionSet, disable2 = false) => {
  const { initialMessage: isInitialMessage, message } = useMessageContext();
  const { channelConfig } = useChannelStateContext();
  const messageIsDeleted = isMessageDeleted(message);
  const {
    canBlockUser,
    canDelete,
    canEdit,
    canFlag,
    canMarkUnread,
    canMute,
    canQuote,
    canReact,
    canReply,
    canSendMessage
  } = useUserRole(message);
  const isMessageThreadReply = typeof message.parent_id === "string";
  const isBounced = isMessageBounced(message);
  const allowRetry = isMessageErrorRetryable(message);
  const hasNetworkSendFailure = isNetworkSendFailure(message);
  return (0, import_react29.useMemo)(() => {
    if (disable2) return messageActionSet;
    if (isBounced || isInitialMessage || // not sure whether this thing even works anymore
    !message.type || message.type === "system" || message.type === "ephemeral" || message.status === "sending")
      return [];
    return messageActionSet.filter((action) => {
      if (action.placement === "quick-dropdown-toggle") return true;
      const type = action.type;
      if (ACTIONS_NOT_WORKING_IN_THREAD.includes(type) && isMessageThreadReply)
        return false;
      if (message.error) {
        return type === "resendMessage" && canSendMessage && (allowRetry || isBounced) || type === "edit" && (isBounced && canEdit || hasNetworkSendFailure) || type === "delete" && !messageIsDeleted && (isBounced && canDelete || hasNetworkSendFailure);
      }
      if (type === "resendMessage" || type === "blockUser" && !canBlockUser || type === "copyMessageText" && !message.text || type === "download" && !message.attachments?.some(
        (attachment) => isFileAttachment(attachment) || isImageAttachment(attachment) || isVideoAttachment(attachment) || isAudioAttachment(attachment) || isVoiceRecordingAttachment(attachment)
      ) || type === "delete" && (!canDelete || messageIsDeleted) || type === "edit" && !canEdit || type === "flag" && !canFlag || type === "markUnread" && !canMarkUnread || type === "mute" && !canMute || type === "quote" && !canQuote || type === "react" && !canReact || type === "reply" && !canReply || type === "remindMe" && !channelConfig?.["user_message_reminders"] || type === "saveForLater" && !channelConfig?.["user_message_reminders"])
        return false;
      return true;
    });
  }, [
    allowRetry,
    canBlockUser,
    canDelete,
    canEdit,
    canFlag,
    canMarkUnread,
    canMute,
    canQuote,
    canReact,
    canReply,
    canSendMessage,
    channelConfig,
    isBounced,
    isInitialMessage,
    messageIsDeleted,
    isMessageThreadReply,
    message.error,
    message.attachments,
    message.status,
    message.text,
    message.type,
    disable2,
    hasNetworkSendFailure,
    messageActionSet
  ]);
};
var MessageAlsoSentInChannelIndicator = () => {
  const { addNotification } = useNotificationApi();
  const { t: t2 } = useTranslationContext();
  const { channel } = useChannelStateContext();
  const { jumpToMessage, openThread } = useChannelActionContext();
  const { message, threadList } = useMessageContext();
  const targetMessageRef = (0, import_react29.useRef)(void 0);
  const queryParent = () => channel.getClient().search({ cid: channel.cid }, { id: message.parent_id }).then(({ results }) => {
    if (!results.length) {
      throw new Error("Thread has not been found");
    }
    targetMessageRef.current = formatMessage(results[0].message);
  }).catch((error2) => {
    addNotification({
      context: { threadReply: message },
      emitter: "MessageIsThreadReplyInChannelButtonIndicator",
      error: error2,
      message: t2("Thread has not been found"),
      severity: "error",
      type: "api:reply:search:failed"
    });
  });
  const jumpToReplyInChannelMessages = async (id3) => {
    await jumpToMessage(id3);
  };
  (0, import_react29.useEffect)(() => {
    if (targetMessageRef.current || targetMessageRef.current === null || !message.parent_id)
      return;
    const localMessage = channel.state.findMessage(message.parent_id);
    if (localMessage) {
      targetMessageRef.current = localMessage;
      return;
    }
  }, [channel, message]);
  const handleClickViewReference = async () => {
    if (!targetMessageRef.current) {
      if (threadList) {
        await jumpToReplyInChannelMessages(message.id);
        return;
      } else await queryParent();
    }
    const target = targetMessageRef.current;
    if (!target) {
      targetMessageRef.current = null;
      return;
    }
    if (threadList) await jumpToReplyInChannelMessages(message.id);
    else openThread(target);
  };
  if (!message?.show_in_channel) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-also-sent-in-channel", role: "status", children: [
    (0, import_jsx_runtime4.jsx)(IconArrowUpRight, {}),
    (0, import_jsx_runtime4.jsx)("span", { children: threadList ? t2("Also sent in channel") : t2("Replied to a thread") }),
    (0, import_jsx_runtime4.jsx)("span", { children: " · " }),
    (0, import_jsx_runtime4.jsx)(
      "button",
      {
        className: "str-chat__message-also-sent-in-channel__link-button",
        onClick: handleClickViewReference,
        type: "button",
        children: t2("View")
      }
    )
  ] });
};
function UnMemoizedMessageRepliesCountButton(props) {
  const { AvatarStack: AvatarStack$1 = AvatarStack } = useComponentContext(
    MessageRepliesCountButton.name
  );
  const {
    labelPlural,
    labelSingle,
    onClick,
    reply_count: replyCount = 0,
    thread_participants: threadParticipants = []
  } = props;
  const { channelCapabilities } = useChannelStateContext();
  const { t: t2 } = useTranslationContext("MessageRepliesCountButton");
  const avatarStackDisplayInfo = (0, import_react29.useMemo)(
    () => threadParticipants.slice(0, 3).map((participant) => ({
      imageUrl: participant.image,
      userName: participant.name || participant.id
    })),
    [threadParticipants]
  );
  if (!replyCount) return null;
  let replyCountText = t2("replyCount", { count: replyCount });
  if (labelPlural && replyCount > 1) {
    replyCountText = `${replyCount} ${labelPlural}`;
  } else if (labelSingle) {
    replyCountText = `1 ${labelSingle}`;
  }
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-replies-count-button-wrapper", children: (0, import_jsx_runtime4.jsxs)(
    "button",
    {
      className: "str-chat__message-replies-count-button",
      "data-testid": "replies-count-button",
      disabled: !channelCapabilities["send-reply"],
      onClick,
      children: [
        replyCountText,
        (0, import_jsx_runtime4.jsx)(AvatarStack$1, { displayInfo: avatarStackDisplayInfo, size: "xs" })
      ]
    }
  ) });
}
var MessageRepliesCountButton = import_react29.default.memo(
  UnMemoizedMessageRepliesCountButton
);
var PinIndicator = ({ message }) => {
  const { t: t2 } = useTranslationContext();
  const { client } = useChatContext();
  if (!message) return null;
  const isOwnPin = !!message.pinned_by?.id && message.pinned_by.id === client.user?.id;
  const name2 = message.pinned_by?.name ?? message.pinned_by?.id ?? "";
  const label = isOwnPin ? t2("Pinned by You") : name2 ? t2("Pinned by {{ name }}", { name: name2 }) : t2("Message pinned");
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-pin-indicator", children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-pin-indicator__content", children: [
    (0, import_jsx_runtime4.jsx)("span", { "aria-hidden": true, className: "str-chat__message-pin-indicator__icon", children: (0, import_jsx_runtime4.jsx)(IconPin, {}) }),
    (0, import_jsx_runtime4.jsx)("span", { children: label })
  ] }) });
};
var Tooltip = ({ children, className, ...rest }) => (0, import_jsx_runtime4.jsx)("div", { className: clsx_default("str-chat__tooltip", className), ...rest, children });
var PopperTooltip = ({
  children,
  className,
  offset: offset4 = [0, 10],
  placement = "top",
  referenceElement,
  visible = false
}) => {
  const [popperElement, setPopperElement] = (0, import_react29.useState)(null);
  const {
    placement: resolvedPlacement,
    refs,
    strategy,
    x: x2,
    y: y2
  } = usePopoverPosition({
    offset: offset4,
    placement
  });
  (0, import_react29.useEffect)(() => {
    refs.setReference(referenceElement);
  }, [referenceElement, refs]);
  (0, import_react29.useEffect)(() => {
    refs.setFloating(popperElement);
  }, [popperElement, refs]);
  if (!visible) return null;
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default("str-chat__tooltip", className),
      "data-placement": resolvedPlacement,
      ref: setPopperElement,
      style: { left: x2 ?? 0, position: strategy, top: y2 ?? 0 },
      children
    }
  );
};
var useEnterLeaveHandlers = ({
  onMouseEnter,
  onMouseLeave
} = {}) => {
  const [tooltipVisible, setTooltipVisible] = (0, import_react29.useState)(false);
  const handleEnter = (0, import_react29.useCallback)(
    (e) => {
      setTooltipVisible(true);
      onMouseEnter?.(e);
    },
    [onMouseEnter]
  );
  const handleLeave = (0, import_react29.useCallback)(
    (e) => {
      setTooltipVisible(false);
      onMouseLeave?.(e);
    },
    [onMouseLeave]
  );
  return { handleEnter, handleLeave, tooltipVisible };
};
var UnMemoizedMessageStatus = (props) => {
  const {
    MessageDeliveredStatus,
    MessageReadStatus,
    MessageSendingStatus,
    MessageSentStatus,
    messageType = "simple",
    tooltipUserNameMapper = mapToUserNameOrId
  } = props;
  const { handleEnter, handleLeave, tooltipVisible } = useEnterLeaveHandlers();
  const { client } = useChatContext("MessageStatus");
  const {
    deliveredTo,
    isMyMessage,
    lastOwnMessage,
    message,
    readBy,
    returnAllReadData,
    threadList
  } = useMessageContext();
  const { t: t2 } = useTranslationContext("MessageStatus");
  const [referenceElement, setReferenceElement] = (0, import_react29.useState)(null);
  if (!isMyMessage() || message.type === "error") return null;
  const justReadByMe = readBy?.length === 1 && readBy[0].id === client.user?.id;
  const deliveredOnlyToMe = deliveredTo?.length === 1 && deliveredTo[0].id === client.user?.id;
  const sending = message.status === "sending";
  const read = !!(readBy?.length && !justReadByMe && !threadList);
  const delivered = !!(deliveredTo?.length && !deliveredOnlyToMe && !read && !threadList);
  const sent = (returnAllReadData || lastOwnMessage?.id === message.id) && message.status === "received" && !delivered && !read && !threadList;
  const readersWithoutOwnUser = read ? readBy.filter((item) => item.id !== client.user?.id) : [];
  return (0, import_jsx_runtime4.jsxs)(
    "span",
    {
      className: clsx_default(
        `str-chat__message-${messageType}-status str-chat__message-status`,
        {
          "str-chat__message-status-delivered": delivered,
          "str-chat__message-status-read-by": read,
          "str-chat__message-status-sending": sending,
          "str-chat__message-status-sent": sent
        }
      ),
      "data-testid": clsx_default({
        "message-status-delivered": delivered,
        "message-status-read-by": read,
        "message-status-sending": sending,
        "message-status-sent": sent
      }),
      onMouseEnter: handleEnter,
      onMouseLeave: handleLeave,
      ref: setReferenceElement,
      children: [
        sending && (MessageSendingStatus ? (0, import_jsx_runtime4.jsx)(MessageSendingStatus, {}) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          (0, import_jsx_runtime4.jsx)(
            PopperTooltip,
            {
              offset: [0, 5],
              referenceElement,
              visible: tooltipVisible,
              children: t2("Sending...")
            }
          ),
          (0, import_jsx_runtime4.jsx)(IconClock, { className: "str-chat__message-status-sending" })
        ] })),
        sent && (MessageSentStatus ? (0, import_jsx_runtime4.jsx)(MessageSentStatus, {}) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          (0, import_jsx_runtime4.jsx)(
            PopperTooltip,
            {
              offset: [0, 5],
              referenceElement,
              visible: tooltipVisible,
              children: t2("Sent")
            }
          ),
          (0, import_jsx_runtime4.jsx)(IconCheckmark1Small, { className: "str-chat__message-status-sent" })
        ] })),
        delivered && (MessageDeliveredStatus ? (0, import_jsx_runtime4.jsx)(MessageDeliveredStatus, {}) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          (0, import_jsx_runtime4.jsx)(
            PopperTooltip,
            {
              offset: [0, 5],
              referenceElement,
              visible: tooltipVisible,
              children: t2("Delivered")
            }
          ),
          (0, import_jsx_runtime4.jsx)(IconChecks, { className: "str-chat__message-status-delivered" })
        ] })),
        read && (MessageReadStatus ? (0, import_jsx_runtime4.jsx)(MessageReadStatus, {}) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          (0, import_jsx_runtime4.jsx)(
            PopperTooltip,
            {
              offset: [0, 5],
              referenceElement,
              visible: tooltipVisible,
              children: getReadByTooltipText(readBy, t2, client, tooltipUserNameMapper)
            }
          ),
          (0, import_jsx_runtime4.jsx)(IconChecks, { className: "str-chat__message-status-read" }),
          readersWithoutOwnUser.length > 1 && (0, import_jsx_runtime4.jsx)(
            "span",
            {
              className: `str-chat__message-${messageType}-status-number`,
              "data-testid": "message-status-read-by-many",
              children: readersWithoutOwnUser.length
            }
          )
        ] }))
      ]
    }
  );
};
var MessageStatus = import_react29.default.memo(
  UnMemoizedMessageStatus
);
function escapeRegExp(text22) {
  return text22.replace(/[-[\]{}()*+?.,/\\^$|#]/g, "\\$&");
}
var detectHttp = /(http(s?):\/\/)?(www\.)?/;
var messageCodeBlocks = (message) => {
  const codeRegex = /```[a-z]*\n[\s\S]*?\n```|`[a-z]*[\s\S]*?`/gm;
  const matches2 = message.match(codeRegex);
  return matches2 || [];
};
var matchMarkdownLinks = (message) => {
  const regexMdLinks = /\[([^[]+)\](\(.*\))/gm;
  const matches2 = message.match(regexMdLinks);
  const singleMatch = /\[([^[]+)\]\((.*)\)/;
  const links = matches2 ? matches2.map((match) => {
    const i2 = singleMatch.exec(match);
    return i2 && [i2[1], i2[2]];
  }) : [];
  return links.flat();
};
var emojiMarkdownPlugin = () => {
  const replace2 = (match) => u("element", { properties: {}, tagName: "emoji" }, [u("text", match)]);
  const transform2 = (node2) => findAndReplace2(node2, [(0, import_emoji_regex2.default)(), replace2]);
  return transform2;
};
var mentionsMarkdownPlugin = (mentioned_users) => () => {
  const mentioned_usernames = mentioned_users.map((user) => user.name || user.id).filter(Boolean).map(escapeRegExp);
  const mentionedUsersRegex = new RegExp(
    mentioned_usernames.map((username) => `@${username}`).join("|"),
    "g"
  );
  const replace2 = (match) => {
    const usernameOrId = match.replace("@", "");
    const user = mentioned_users.find(
      ({ id: id3, name: name2 }) => name2 === usernameOrId || id3 === usernameOrId
    );
    return u("element", { mentionedUser: user, properties: {}, tagName: "mention" }, [
      u("text", match)
    ]);
  };
  const transform2 = (tree) => {
    if (!mentioned_usernames.length) return;
    visit(tree, (node2, index6, parent) => {
      if (typeof index6 === "undefined") return;
      if (!parent) return;
      const nextChild = parent.children.at(index6 + 1);
      const nextChildHref = nextChild?.properties?.href;
      if (node2.type === "text" && // text value has to have @ sign at the end of the string
      // and no other characters except whitespace can precede it
      // valid cases:   "text @", "@", " @"
      // invalid cases: "text@", "@text",
      /.?\s?@$|^@$/.test(node2.value) && nextChildHref?.startsWith("mailto:")) {
        const newTextValue = node2.value.replace(/@$/, "");
        const username = nextChildHref.replace("mailto:", "");
        parent.children[index6] = u("text", newTextValue);
        parent.children[index6 + 1] = u("text", `@${username}`);
      }
    });
    findAndReplace2(tree, [mentionedUsersRegex, replace2]);
  };
  return transform2;
};
var visitor = (node2) => {
  if (node2.type !== "html") return;
  node2.type = "text";
};
var transform = (tree) => {
  visit(tree, visitor);
};
var htmlToTextPlugin = () => transform;
var text7 = (value) => ({ type: "text", value });
function imageToLink({ getTextLabelFrom = "url" } = {}) {
  return (tree) => {
    const visitor2 = (node2, index6, parent) => {
      if (parent == null || index6 == null) return;
      const label = node2[getTextLabelFrom] ?? node2.url;
      const link3 = {
        children: [text7(label)],
        title: node2.title ?? node2.alt ?? node2.url,
        type: "link",
        url: node2.url
      };
      parent.children.splice(index6, 1, link3);
      return [SKIP, index6 + 1];
    };
    visit(tree, "image", visitor2);
  };
}
function isParentWithChildren(node2) {
  const maybe = node2;
  return Array.isArray(maybe.children);
}
function brNode() {
  return { data: { hName: "br" }, type: "thematicBreak" };
}
var keepLineBreaksPlugin = () => (tree) => {
  visit(
    tree,
    // visit needs a Unist parent-like root
    isParentWithChildren,
    // limit to parents with children
    (parent) => {
      const children = parent.children;
      if (children.length < 2) return;
      const out = [];
      for (let i2 = 0; i2 < children.length; i2++) {
        const curr = children[i2];
        out.push(curr);
        if (i2 === children.length - 1) break;
        const next = children[i2 + 1];
        const currEndLine = curr.position && curr.position.end ? curr.position.end.line : void 0;
        const nextStartLine = next.position && next.position.start ? next.position.start.line : void 0;
        if (typeof currEndLine !== "number" || typeof nextStartLine !== "number") {
          continue;
        }
        const extraBlankLines = Math.max(0, nextStartLine - currEndLine - 1);
        if (extraBlankLines > 0) {
          for (let k2 = 0; k2 < extraBlankLines; k2++) {
            out.push(brNode());
          }
        }
      }
      parent.children = out;
    }
  );
};
var INS_REGEX = /\+\+(\S(?:[\s\S]*?\S)?)\+\+/g;
var IGNORE_NODE_TYPES = /* @__PURE__ */ new Set([
  "code",
  "inlineCode",
  "link",
  "linkReference",
  "definition",
  "math",
  "inlineMath"
]);
var plusPlusToEmphasis = () => {
  const visitor2 = (node2, index6, parent) => {
    if (IGNORE_NODE_TYPES.has(node2.type)) return SKIP;
    if (node2.type !== "text" || parent == null || typeof index6 !== "number") return;
    const value = node2.value;
    INS_REGEX.lastIndex = 0;
    let match;
    let last = 0;
    const out = [];
    while (match = INS_REGEX.exec(value)) {
      const [full, inner] = match;
      const start2 = match.index;
      if (start2 > last) out.push({ type: "text", value: value.slice(last, start2) });
      out.push({
        children: [{ type: "text", value: inner }],
        data: { hName: "ins" },
        type: "emphasis"
      });
      last = start2 + full.length;
    }
    if (out.length === 0) return;
    if (last < value.length) out.push({ type: "text", value: value.slice(last) });
    parent.children.splice(index6, 1, ...out);
    return [SKIP, index6 + out.length];
  };
  return (tree) => visit(tree, visitor2);
};
var remarkIgnoreMarkdown = () => (tree, file) => {
  const source = String(file.value ?? "");
  const text22 = { type: "text", value: source };
  const paragraph3 = { children: [text22], type: "paragraph" };
  tree.children = [paragraph3];
};
var Anchor = ({ children, href }) => {
  const isEmail = href?.startsWith("mailto:");
  const isUrl2 = href?.startsWith("http");
  if (!href || !isEmail && !isUrl2) return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children });
  return (0, import_jsx_runtime4.jsx)(
    "a",
    {
      className: clsx_default({ "str-chat__message-url-link": isUrl2 }),
      href,
      rel: "nofollow noreferrer noopener",
      target: "_blank",
      children
    }
  );
};
var Emoji = ({ children }) => (0, import_jsx_runtime4.jsx)("span", { className: "inline-text-emoji", "data-testid": "inline-text-emoji", children });
var Mention = ({ children, node: { mentionedUser } }) => (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__message-mention", "data-user-id": mentionedUser.id, children });
var NullComponent = () => null;
var ErrorBoundary = class extends import_react29.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  componentDidCatch(error2, information) {
    console.error(error2, information);
  }
  render() {
    if (this.state.hasError) {
      return this.props.fallback;
    }
    return this.props.children;
  }
};
var defaultAllowedTagNames = [
  "html",
  "text",
  "br",
  "p",
  "em",
  "strong",
  "a",
  "ol",
  "ul",
  "li",
  "code",
  "pre",
  "blockquote",
  "del",
  "table",
  "thead",
  "tbody",
  "th",
  "tr",
  "td",
  "tfoot",
  // custom types (tagNames)
  "emoji",
  "mention",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "ins"
];
function formatUrlForDisplay(url) {
  try {
    return decodeURIComponent(url).replace(detectHttp, "");
  } catch (e) {
    return url;
  }
}
function encodeDecode(url) {
  try {
    return encodeURI(decodeURIComponent(url));
  } catch (error2) {
    return url;
  }
}
var urlTransform = (uri) => uri.startsWith("app://") ? uri : defaultUrlTransform(uri);
var getPluginsForward = (plugins) => plugins;
var markDownRenderers = {
  a: Anchor,
  emoji: Emoji,
  mention: Mention
};
var renderText = (text22, mentionedUsers, {
  allowedTagNames = defaultAllowedTagNames,
  customMarkDownRenderers,
  getRehypePlugins = getPluginsForward,
  getRemarkPlugins = getPluginsForward
} = {}) => {
  if (!text22) return null;
  if (text22.trim().length === 1) return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: text22 });
  let newText = text22;
  const markdownLinks = matchMarkdownLinks(newText);
  const codeBlocks = messageCodeBlocks(newText);
  const links = [...find(newText, "email"), ...find(newText, "url")];
  for (let i2 = links.length - 1; i2 >= 0; i2--) {
    const { end, href, start: start2, type, value } = links[i2];
    const linkIsInBlock = codeBlocks.some((block) => block?.includes(value));
    const noParsingNeeded = markdownLinks && markdownLinks.filter((text222) => {
      const strippedHref = href?.replace(detectHttp, "");
      const strippedText = text222?.replace(detectHttp, "");
      if (!strippedHref || !strippedText) return false;
      return strippedHref.includes(strippedText) || strippedText.includes(strippedHref);
    });
    if (noParsingNeeded.length > 0 || linkIsInBlock) continue;
    try {
      if (type === "email" && mentionedUsers) {
        const emailMatchesWithName = mentionedUsers.find((u22) => u22.name === value);
        if (emailMatchesWithName) {
          const isMention = newText.charAt(start2 - 1) === "@";
          newText = newText.slice(0, start2) + (isMention ? value : `[${value}](${encodeDecode(href)})`) + newText.slice(end);
        }
      } else {
        const displayLink = type === "email" ? value : formatUrlForDisplay(href);
        newText = newText.slice(0, start2) + `[${displayLink}](${encodeDecode(href)})` + newText.slice(end);
      }
    } catch (e) {
    }
  }
  const remarkPlugins2 = [
    htmlToTextPlugin,
    keepLineBreaksPlugin,
    [remarkGfm, { singleTilde: false }],
    plusPlusToEmphasis,
    imageToLink
  ];
  const rehypePlugins = [emojiMarkdownPlugin];
  if (mentionedUsers?.length) {
    rehypePlugins.push(mentionsMarkdownPlugin(mentionedUsers));
  }
  return (0, import_jsx_runtime4.jsx)(ErrorBoundary, { fallback: (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: text22 }), children: (0, import_jsx_runtime4.jsx)(
    Markdown,
    {
      allowedElements: allowedTagNames,
      components: {
        ...markDownRenderers,
        ...customMarkDownRenderers
      },
      rehypePlugins: getRehypePlugins(rehypePlugins),
      remarkPlugins: getRemarkPlugins(remarkPlugins2),
      skipHtml: true,
      unwrapDisallowed: true,
      urlTransform,
      children: newText
    }
  ) });
};
var UnMemoizedMessageTextComponent = (props) => {
  const {
    customInnerClass,
    customWrapperClass = "",
    message: propMessage,
    renderText: propsRenderText
  } = props;
  const {
    message: contextMessage,
    onMentionsClickMessage,
    onMentionsHoverMessage,
    renderText: contextRenderText,
    translationView = "translated",
    unsafeHTML
  } = useMessageContext();
  const renderText$1 = propsRenderText ?? contextRenderText ?? renderText;
  const { userLanguage } = useTranslationContext("MessageText");
  const message = propMessage || contextMessage;
  const hasAttachment = messageHasAttachments(message);
  const messageTextToRender = translationView === "original" ? message.text : getTranslatedMessageText({ language: userLanguage, message }) || message.text;
  const messageText = (0, import_react29.useMemo)(
    () => renderText$1(messageTextToRender, message.mentioned_users),
    [message.mentioned_users, messageTextToRender, renderText$1]
  );
  const wrapperClass = customWrapperClass || "str-chat__message-text";
  const innerClass = customInnerClass;
  if (!messageTextToRender) return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: wrapperClass, tabIndex: 0, children: (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default(innerClass, {
        [` str-chat__message-text-inner--is-emoji`]: messageTextHasEmojisOnly(message) && !message.quoted_message,
        [`str-chat__message-text-inner--has-attachment`]: hasAttachment
      }),
      "data-testid": "message-text-inner-wrapper",
      onClick: onMentionsClickMessage,
      onMouseOver: onMentionsHoverMessage,
      children: unsafeHTML && message.html ? (0, import_jsx_runtime4.jsx)("div", { dangerouslySetInnerHTML: { __html: message.html } }) : (0, import_jsx_runtime4.jsx)("div", { children: messageText })
    }
  ) });
};
var MessageText = import_react29.default.memo(
  UnMemoizedMessageTextComponent
);
function Timestamp(props) {
  const { calendar: calendar2, calendarFormats, customClass, format, timestamp } = props;
  const { formatDate } = useMessageContext();
  const { t: t2, tDateTimeParser } = useTranslationContext("MessageTimestamp");
  const normalizedTimestamp = timestamp && isDate(timestamp) ? timestamp.toISOString() : timestamp;
  const when = (0, import_react29.useMemo)(
    () => getDateString({
      calendar: calendar2,
      calendarFormats,
      format,
      formatDate,
      messageCreatedAt: normalizedTimestamp,
      t: t2,
      tDateTimeParser,
      timestampTranslationKey: "timestamp/MessageTimestamp"
    }),
    [
      calendar2,
      calendarFormats,
      format,
      formatDate,
      normalizedTimestamp,
      t2,
      tDateTimeParser
    ]
  );
  if (!when) {
    return null;
  }
  return (0, import_jsx_runtime4.jsx)(
    "time",
    {
      className: customClass,
      dateTime: normalizedTimestamp,
      title: normalizedTimestamp,
      children: when
    }
  );
}
var UnMemoizedMessageTimestamp = (props) => {
  const { message: propMessage, ...timestampProps } = props;
  const { message: contextMessage } = useMessageContext();
  const { Timestamp: Timestamp$1 = Timestamp } = useComponentContext();
  const message = propMessage || contextMessage;
  return (0, import_jsx_runtime4.jsx)(Timestamp$1, { timestamp: message.created_at, ...timestampProps });
};
var MessageTimestamp = import_react29.default.memo(
  UnMemoizedMessageTimestamp
);
var UnMemoizedMessageEditedIndicator = (props) => {
  const { customClass, message: propMessage, ...timestampProps } = props;
  const { message: contextMessage } = useMessageContext();
  const { t: t2 } = useTranslationContext("MessageEditedIndicator");
  const { Timestamp: Timestamp$1 = Timestamp } = useComponentContext();
  const message = propMessage ?? contextMessage;
  const [referenceElement, setReferenceElement] = (0, import_react29.useState)(null);
  const { handleEnter, handleLeave, tooltipVisible } = useEnterLeaveHandlers();
  if (!message?.message_text_updated_at) {
    return null;
  }
  return (0, import_jsx_runtime4.jsxs)(
    "span",
    {
      className: customClass ?? "str-chat__message-edited-indicator",
      "data-testid": "message-edited-indicator",
      onMouseEnter: handleEnter,
      onMouseLeave: handleLeave,
      ref: setReferenceElement,
      children: [
        t2("Edited"),
        (0, import_jsx_runtime4.jsx)(
          PopperTooltip,
          {
            offset: [0, 5],
            placement: "top",
            referenceElement,
            visible: tooltipVisible,
            children: (0, import_jsx_runtime4.jsx)(Timestamp$1, { timestamp: message.message_text_updated_at, ...timestampProps })
          }
        )
      ]
    }
  );
};
var MessageEditedIndicator = import_react29.default.memo(
  UnMemoizedMessageEditedIndicator
);
var MessageTranslationIndicator = ({
  message: propMessage
}) => {
  const { t: t2, userLanguage } = useTranslationContext();
  const {
    message: contextMessage,
    setTranslationView,
    translationView
  } = useMessageContext();
  const message = propMessage ?? contextMessage;
  const translatedTextForUser = (0, import_react29.useMemo)(
    () => getTranslatedMessageText({ language: userLanguage, message }),
    [userLanguage, message]
  );
  const hasTranslationForUserLanguage = (0, import_react29.useMemo)(
    () => translatedTextForUser != null && message?.text !== void 0 && translatedTextForUser !== message.text,
    [translatedTextForUser, message?.text]
  );
  const viewingOriginal = (0, import_react29.useMemo)(
    () => translationView === "original" || translationView === void 0 && !hasTranslationForUserLanguage,
    [translationView, hasTranslationForUserLanguage]
  );
  const handleToggle = (0, import_react29.useCallback)(() => {
    setTranslationView?.(viewingOriginal ? "translated" : "original");
  }, [setTranslationView, viewingOriginal]);
  const sourceLanguageName = (0, import_react29.useMemo)(() => {
    const sourceLanguageCode = message?.i18n?.language;
    if (!sourceLanguageCode) return "";
    const languageKey = "language/" + sourceLanguageCode;
    const translatedName = t2(languageKey);
    return translatedName && translatedName !== languageKey ? translatedName : sourceLanguageCode;
  }, [message?.i18n?.language, t2]);
  if (!message?.i18n || !setTranslationView) return null;
  if (!hasTranslationForUserLanguage) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-translation-indicator", children: [
    (0, import_jsx_runtime4.jsx)(IconTranslate, {}),
    (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__message-translation-indicator__sign", children: viewingOriginal ? t2("Original") : sourceLanguageName ? t2("Translated from {{ language }}", { language: sourceLanguageName }) : t2("Translated") }),
    (0, import_jsx_runtime4.jsx)("span", { children: " · " }),
    (0, import_jsx_runtime4.jsx)(
      Button,
      {
        className: "str-chat__message-translation-indicator__translation-toggle",
        onClick: handleToggle,
        type: "button",
        children: viewingOriginal ? t2("View translation") : t2("View original")
      }
    )
  ] });
};
var stateSelector$1 = (state) => ({
  attachments: state.attachments
});
var useAttachmentManagerState = () => {
  const { attachmentManager } = useMessageComposerController();
  const { attachments } = useStateStore(attachmentManager.state, stateSelector$1);
  return {
    attachments,
    availableUploadSlots: attachmentManager.availableUploadSlots,
    blockedUploadsCount: attachmentManager.blockedUploadsCount,
    failedUploadsCount: attachmentManager.failedUploadsCount,
    isUploadEnabled: attachmentManager.isUploadEnabled,
    pendingUploadsCount: attachmentManager.pendingUploadsCount,
    successfulUploadsCount: attachmentManager.successfulUploadsCount,
    uploadsInProgressCount: attachmentManager.uploadsInProgressCount
  };
};
var attachmentManagerStateSelector$2 = (state) => ({
  attachments: state.attachments
});
var pollComposerStateSelector$4 = (state) => ({
  poll: state.data
});
var locationComposerStateSelector$1 = (state) => ({
  location: state.location
});
var useAttachmentsForPreview = () => {
  const { attachmentManager, locationComposer, pollComposer } = useMessageComposerController();
  const { attachments } = useStateStore(
    attachmentManager.state,
    attachmentManagerStateSelector$2
  );
  const { poll } = useStateStore(pollComposer.state, pollComposerStateSelector$4);
  const { location: location2 } = useStateStore(
    locationComposer.state,
    locationComposerStateSelector$1
  );
  return {
    attachments,
    location: location2,
    poll
  };
};
var useCanCreatePoll = () => {
  const { pollComposer } = useMessageComposerController();
  const [canCreatePoll, setCanCreatePoll] = (0, import_react29.useState)(pollComposer.canCreatePoll);
  (0, import_react29.useEffect)(
    () => pollComposer.state.subscribe(() => {
      setCanCreatePoll(pollComposer.canCreatePoll);
    }),
    [pollComposer]
  );
  return canCreatePoll;
};
var cooldownTimerStateSelector2 = (state) => ({
  cooldownRemaining: state.cooldownRemaining
});
var useCooldownRemaining = () => {
  const { channel } = useChannelStateContext();
  return useStateStore(channel.cooldownTimer.state, cooldownTimerStateSelector2).cooldownRemaining ?? 0;
};
var useTextareaRef = (props) => {
  const { focus } = props;
  const textareaRef = (0, import_react29.useRef)(void 0);
  (0, import_react29.useEffect)(() => {
    if (focus && textareaRef.current) {
      textareaRef.current.focus();
    }
  }, [focus]);
  return {
    textareaRef
  };
};
var takeStateSnapshot = (messageComposer) => {
  const textComposerState = messageComposer.textComposer.state.getLatestValue();
  const attachmentManagerState = messageComposer.attachmentManager.state.getLatestValue();
  const linkPreviewsManagerState = messageComposer.linkPreviewsManager.state.getLatestValue();
  const pollComposerState = messageComposer.pollComposer.state.getLatestValue();
  const customDataManagerState = messageComposer.customDataManager.state.getLatestValue();
  const state = messageComposer.state.getLatestValue();
  return () => {
    messageComposer.state.next(state);
    messageComposer.textComposer.state.next(textComposerState);
    messageComposer.attachmentManager.state.next(attachmentManagerState);
    messageComposer.linkPreviewsManager.state.next(linkPreviewsManagerState);
    messageComposer.pollComposer.state.next(pollComposerState);
    messageComposer.customDataManager.state.next(customDataManagerState);
  };
};
var useSubmitHandler = (props) => {
  const { overrideSubmitHandler } = props;
  const { addNotification } = useNotificationApi();
  const { editMessage, sendMessage } = useChannelActionContext("useSubmitHandler");
  const { t: t2 } = useTranslationContext("useSubmitHandler");
  const messageComposer = useMessageComposerController();
  const handleSubmit = (0, import_react29.useCallback)(
    async (event) => {
      event?.preventDefault();
      const composition = await messageComposer.compose();
      if (!composition || !composition.message) return;
      const { localMessage, message, sendOptions } = composition;
      if (messageComposer.editedMessage && localMessage.type !== "error") {
        try {
          await editMessage(localMessage, sendOptions);
          discardPreEditSnapshot(messageComposer);
          messageComposer.clear();
        } catch (err) {
          addNotification({
            emitter: "MessageComposer",
            incident: {
              domain: "api",
              entity: "message",
              operation: "edit"
            },
            message: t2("Edit message request failed"),
            severity: "error"
          });
        }
      } else {
        const restoreComposerStateSnapshot = takeStateSnapshot(messageComposer);
        try {
          const sentPollMessage = !!message.poll_id;
          if (sentPollMessage) {
            messageComposer.state.partialNext({
              id: MessageComposer.generateId(),
              pollId: null
            });
          } else {
            messageComposer.clear();
          }
          if (overrideSubmitHandler) {
            await overrideSubmitHandler({
              cid: messageComposer.channel.cid,
              localMessage,
              message,
              sendOptions
            });
          } else {
            await sendMessage({ localMessage, message, options: sendOptions });
          }
          if (messageComposer.config.text.publishTypingEvents)
            await messageComposer.channel.stopTyping();
        } catch (err) {
          restoreComposerStateSnapshot();
          addNotification({
            emitter: "MessageComposer",
            incident: {
              domain: "api",
              entity: "message",
              operation: "send"
            },
            message: t2("Send message request failed"),
            severity: "error"
          });
        }
      }
    },
    [
      addNotification,
      editMessage,
      messageComposer,
      overrideSubmitHandler,
      sendMessage,
      t2
    ]
  );
  return { handleSubmit };
};
var attachmentManagerConfigStateSelector$1 = (state) => ({
  acceptedFiles: state.attachments.acceptedFiles,
  maxNumberOfFilesPerMessage: state.attachments.maxNumberOfFilesPerMessage
});
var FileInput = (0, import_react29.forwardRef)(function FileInput2({ onFileChange, resetOnChange = true, ...rest }, ref) {
  const handleInputChange = useHandleFileChangeWrapper(resetOnChange, onFileChange);
  return (0, import_jsx_runtime4.jsx)("input", { onChange: handleInputChange, ref, type: "file", ...rest });
});
var UploadFileInput = (0, import_react29.forwardRef)(function UploadFileInput2({
  className,
  onFileChange: onFileChangeCustom,
  ...props
}, ref) {
  const { t: t2 } = useTranslationContext("UploadFileInput");
  const { textareaRef } = useMessageComposerContext();
  const messageComposer = useMessageComposerController();
  const { attachmentManager } = messageComposer;
  const { isUploadEnabled } = useAttachmentManagerState();
  const { acceptedFiles, maxNumberOfFilesPerMessage } = useStateStore(
    messageComposer.configState,
    attachmentManagerConfigStateSelector$1
  );
  const isCooldownActive = useIsCooldownActive();
  const id3 = (0, import_react29.useMemo)(() => nanoid(), []);
  const onFileChange = (0, import_react29.useCallback)(
    (files) => {
      attachmentManager.uploadFiles(files);
      textareaRef.current?.focus();
      onFileChangeCustom?.(files);
    },
    [onFileChangeCustom, attachmentManager, textareaRef]
  );
  return (0, import_jsx_runtime4.jsx)(
    FileInput,
    {
      accept: acceptedFiles?.join(","),
      "aria-label": t2("aria/File upload"),
      "data-testid": "file-input",
      disabled: !isUploadEnabled || isCooldownActive,
      id: id3,
      multiple: maxNumberOfFilesPerMessage > 1,
      ...props,
      className: clsx_default("str-chat__file-input", className),
      onFileChange,
      ref
    }
  );
});
var usePasteHandler = () => {
  const { attachmentManager, textComposer } = useMessageComposerController();
  const onPaste = (0, import_react29.useCallback)(
    (clipboardEvent) => {
      (async (event) => {
        const { items } = event.clipboardData;
        event.preventDefault();
        let plainTextPromise = void 0;
        for (let i2 = 0; i2 < items.length; i2++) {
          const item = items[i2];
          if (item.kind === "string" && item.type === "text/plain") {
            plainTextPromise = new Promise((resolve) => {
              item.getAsString((string3) => {
                resolve(string3);
              });
            });
            break;
          }
        }
        const fileLikes = await dataTransferItemsToFiles(Array.from(items));
        if (plainTextPromise) {
          const pastedText = await plainTextPromise;
          textComposer.insertText({ text: pastedText });
        } else {
          attachmentManager.uploadFiles(fileLikes);
        }
      })(clipboardEvent);
    },
    [attachmentManager, textComposer]
  );
  return { onPaste };
};
var Subscription = class {
  constructor(unsubscribe) {
    this.closed = false;
    this._unsubscribe = unsubscribe;
  }
  unsubscribe() {
    this.closed = true;
    this._unsubscribe?.();
  }
};
function createObserver(observerOrNext) {
  return typeof observerOrNext === "function" ? { next: observerOrNext } : observerOrNext;
}
var Observable = class {
  constructor(producer) {
    this._closed = false;
    if (producer) this._producer = producer;
  }
  get closed() {
    return this._closed;
  }
  subscribe(observerOrNext) {
    const observer = createObserver(observerOrNext);
    if (!this.closed) {
      this._producer?.(observer);
    }
    return new Subscription(() => {
      this._closed = true;
    });
  }
};
var Subject = class extends Observable {
  constructor() {
    super();
    this._observers = /* @__PURE__ */ new Map();
    this._observerCounter = 0;
  }
  get observers() {
    return Array.from(this._observers.values());
  }
  next(value) {
    if (this.closed) return;
    const observers = this.observers;
    for (let i2 = 0; i2 < observers.length; i2++) {
      observers[i2].next(value);
    }
  }
  error(err) {
    if (this.closed) return;
    this.thrownError = err;
    const { observers } = this;
    for (let i2 = 0; i2 < observers.length; i2++) {
      observers[i2].error?.(err);
    }
    this._observers.clear();
  }
  complete() {
    if (this.closed) return;
    this._closed = true;
    const { observers } = this;
    for (let i2 = 0; i2 < observers.length; i2++) {
      observers[i2].complete?.();
    }
    this._observers.clear();
  }
  subscribe(observerOrNext) {
    const observer = createObserver(observerOrNext);
    if (this.thrownError || this.closed) {
      const subscription = new Subscription();
      subscription.closed = true;
      return subscription;
    }
    const observerId = this._observerCounter++;
    this._observers.set(observerId, observer);
    return new Subscription(() => {
      this._observers.delete(observerId);
    });
  }
  unsubscribe() {
    this._closed = true;
    this._observers.clear();
  }
};
var BehaviorSubject = class extends Subject {
  constructor(_value) {
    super();
    this._value = _value;
  }
  get value() {
    const { _value, thrownError } = this;
    if (thrownError) {
      throw thrownError;
    }
    return _value;
  }
  subscribe(observerOrNext) {
    const observer = createObserver(observerOrNext);
    const subscription = super.subscribe(observerOrNext);
    if (!subscription.closed) observer.next(this._value);
    return subscription;
  }
  next(value) {
    super.next(this._value = value);
  }
};
var RecordingPermission = ((RecordingPermission2) => {
  RecordingPermission2["CAM"] = "camera";
  RecordingPermission2["MIC"] = "microphone";
  return RecordingPermission2;
})(RecordingPermission || {});
var MEDIA_TO_PERMISSION = {
  audio: "microphone",
  video: "camera"
  /* CAM */
};
var BrowserPermission = class {
  constructor({ mediaType }) {
    this.state = new BehaviorSubject(void 0);
    this.status = new BehaviorSubject(void 0);
    this.error = new Subject();
    this.changeSubscriptions = [];
    this.name = MEDIA_TO_PERMISSION[mediaType];
  }
  get isWatching() {
    return this.changeSubscriptions.some((subscription) => !subscription.closed);
  }
  async watch() {
    if (!this.status.value) {
      await this.check();
      if (!this.status.value) return;
    }
    const status = this.status.value;
    const handlePermissionChange = (e) => {
      const { state } = e.target;
      this.state.next(state);
    };
    status.addEventListener("change", handlePermissionChange);
    this.changeSubscriptions.push(
      new Subscription(() => {
        status.removeEventListener("change", handlePermissionChange);
      })
    );
  }
  unwatch() {
    this.changeSubscriptions.forEach((subscription) => subscription.unsubscribe());
  }
  async check() {
    if (!this.name) {
      this.error.next(new Error("Unknown media recording permission"));
      return;
    }
    let permissionState;
    try {
      const permissionStatus = await navigator.permissions.query({
        name: this.name
      });
      permissionState = permissionStatus.state;
      this.status.next(permissionStatus);
    } catch (e) {
      permissionState = "granted";
    }
    this.state.next(permissionState);
  }
};
var overrideUndefinedOnly = (object, source) => object ?? source;
var mergeDeepUndefined = (target, source) => (0, import_lodash6.default)(target, source, overrideUndefinedOnly);
var MAX_FREQUENCY_AMPLITUDE = 255;
var logError$1 = (e) => e && console.error("[AMPLITUDE RECORDER ERROR]", e);
var rootMeanSquare = (values2) => Math.sqrt(values2.reduce((acc, val) => acc + Math.pow(val, 2), 0) / values2.length);
var DEFAULT_AMPLITUDE_RECORDER_CONFIG = {
  analyserConfig: {
    fftSize: 32,
    maxDecibels: 0,
    minDecibels: -100
  },
  sampleCount: 100,
  samplingFrequencyMs: 60
};
var AmplitudeRecorder = class {
  constructor({ config, stream: stream2 }) {
    this.amplitudes = new BehaviorSubject([]);
    this.state = new BehaviorSubject(void 0);
    this.error = new Subject();
    this.start = () => {
      if (this.state.value === "closed") return;
      if (!this.stream) {
        throw new Error(
          "Missing MediaStream instance. Cannot to start amplitude recording"
        );
      }
      if (this.state.value === "recording") this.stop();
      if (!this.analyserNode) {
        if (!this.stream) return;
        this.init();
      }
      this.state.next(
        "recording"
        /* RECORDING */
      );
      this.amplitudeSamplingInterval = setInterval(() => {
        if (!(this.analyserNode && this.state.value === "recording"))
          return;
        const frequencyBins = new Uint8Array(this.analyserNode.frequencyBinCount);
        try {
          this.analyserNode.getByteFrequencyData(frequencyBins);
        } catch (e) {
          logError$1(e);
          this.error.next(e);
          return;
        }
        const normalizedSignalStrength = rootMeanSquare(frequencyBins) / MAX_FREQUENCY_AMPLITUDE;
        this.amplitudes.next([...this.amplitudes.value, normalizedSignalStrength]);
      }, this.config.samplingFrequencyMs);
    };
    this.config = mergeDeepUndefined({ ...config }, DEFAULT_AMPLITUDE_RECORDER_CONFIG);
    this.stream = stream2;
  }
  init() {
    this.audioContext = new AudioContext();
    this.analyserNode = this.audioContext.createAnalyser();
    const { analyserConfig } = this.config;
    this.analyserNode.fftSize = analyserConfig.fftSize;
    this.analyserNode.maxDecibels = analyserConfig.maxDecibels;
    this.analyserNode.minDecibels = analyserConfig.minDecibels;
    this.microphone = this.audioContext.createMediaStreamSource(this.stream);
    this.microphone.connect(this.analyserNode);
  }
  stop() {
    clearInterval(this.amplitudeSamplingInterval);
    this.amplitudeSamplingInterval = void 0;
    this.state.next(
      "stopped"
      /* STOPPED */
    );
  }
  close() {
    if (this.state.value !== "stopped") this.stop();
    this.state.next(
      "closed"
      /* CLOSED */
    );
    this.amplitudes.next([]);
    this.microphone?.disconnect();
    this.analyserNode?.disconnect();
    if (this.audioContext?.state !== "closed") this.audioContext?.close();
  }
};
var WAV_HEADER_LENGTH_BYTES = 44;
var BYTES_PER_SAMPLE = 2;
var RIFF_FILE_MAX_BYTES = 4294967295;
var HEADER = {
  AUDIO_FORMAT: { offset: 20, value: 1 },
  // PCM = 1
  BITS_PER_SAMPLE: { offset: 34, value: BYTES_PER_SAMPLE * 8 },
  // 16 bits encoding
  BLOCK_ALIGN: { offset: 32 },
  BYTE_RATE: { offset: 28 },
  CHANNEL_COUNT: { offset: 22 },
  // 1 - mono, 2 - stereo
  CHUNK_ID: { offset: 0, value: 1380533830 },
  // hex representation of string "RIFF" (Resource Interchange File Format) - identifies the file structure that defines a class of more specific file formats, e.g. WAVE
  CHUNK_SIZE: { offset: 4 },
  FILE_FORMAT: { offset: 8, value: 1463899717 },
  // hex representation of string "WAVE"
  SAMPLE_RATE: { offset: 24 },
  SUBCHUNK1_ID: { offset: 12, value: 1718449184 },
  // hex representation of string "fmt " - identifies the start of "format" section of the header
  SUBCHUNK1_SIZE: { offset: 16, value: 16 },
  // Subchunk1 Size without SUBCHUNK1_ID and SUBCHUNK1_SIZE fields
  SUBCHUNK2_ID: { offset: 36, value: 1684108385 },
  // hex representation of string "data" - identifies the start of actual audio data section
  SUBCHUNK2_SIZE: { offset: 40 }
  // actual audio data size
};
var fourCharsToInt = (chars2) => chars2.charCodeAt(0) << 24 | chars2.charCodeAt(1) << 16 | chars2.charCodeAt(2) << 8 | chars2.charCodeAt(3);
({
  data: fourCharsToInt("data"),
  fmt: fourCharsToInt("fmt "),
  RIFF: fourCharsToInt("RIFF"),
  WAVE: fourCharsToInt("WAVE")
});
var writeWavHeader = ({
  arrayBuffer,
  channelCount,
  sampleRate
}) => {
  const byteRate = sampleRate * channelCount * BYTES_PER_SAMPLE;
  const blockAlign = channelCount * BYTES_PER_SAMPLE;
  const dataView = new DataView(arrayBuffer);
  const dataChunkSize = Math.min(
    dataView.byteLength - WAV_HEADER_LENGTH_BYTES,
    RIFF_FILE_MAX_BYTES - WAV_HEADER_LENGTH_BYTES
  );
  dataView.setUint32(HEADER.CHUNK_ID.offset, HEADER.CHUNK_ID.value);
  dataView.setUint32(HEADER.CHUNK_SIZE.offset, arrayBuffer.byteLength - 8, true);
  dataView.setUint32(HEADER.FILE_FORMAT.offset, HEADER.FILE_FORMAT.value);
  dataView.setUint32(HEADER.SUBCHUNK1_ID.offset, HEADER.SUBCHUNK1_ID.value);
  dataView.setUint32(HEADER.SUBCHUNK1_SIZE.offset, HEADER.SUBCHUNK1_SIZE.value, true);
  dataView.setUint16(HEADER.AUDIO_FORMAT.offset, HEADER.AUDIO_FORMAT.value, true);
  dataView.setUint16(HEADER.CHANNEL_COUNT.offset, channelCount, true);
  dataView.setUint32(HEADER.SAMPLE_RATE.offset, sampleRate, true);
  dataView.setUint32(HEADER.BYTE_RATE.offset, byteRate, true);
  dataView.setUint16(HEADER.BLOCK_ALIGN.offset, blockAlign, true);
  dataView.setUint16(HEADER.BITS_PER_SAMPLE.offset, HEADER.BITS_PER_SAMPLE.value, true);
  dataView.setUint32(HEADER.SUBCHUNK2_ID.offset, HEADER.SUBCHUNK2_ID.value);
  dataView.setUint32(HEADER.SUBCHUNK2_SIZE.offset, dataChunkSize, true);
};
var splitDataByChannel = (audioBuffer) => Array.from(
  { length: audioBuffer.numberOfChannels },
  (_2, i2) => audioBuffer.getChannelData(i2)
);
var writeWavAudioData = ({ arrayBuffer, dataByChannel }) => {
  const dataView = new DataView(arrayBuffer);
  const channelCount = dataByChannel.length;
  dataByChannel.forEach((channelData, channelIndex) => {
    let writeOffset = WAV_HEADER_LENGTH_BYTES + channelCount * channelIndex;
    channelData.forEach((float32Value) => {
      dataView.setInt16(
        writeOffset,
        float32Value < 0 ? Math.max(-1, float32Value) * 32768 : Math.min(1, float32Value) * 32767,
        true
      );
      writeOffset += channelCount * BYTES_PER_SAMPLE;
    });
  });
};
var encodeToWaw = async (file, sampleRate) => {
  const audioBuffer = await renderAudio(await toAudioBuffer(file), sampleRate);
  const numberOfSamples = audioBuffer.duration * sampleRate;
  const fileSizeBytes = numberOfSamples * audioBuffer.numberOfChannels * BYTES_PER_SAMPLE + WAV_HEADER_LENGTH_BYTES;
  const arrayBuffer = new ArrayBuffer(fileSizeBytes);
  writeWavHeader({ arrayBuffer, channelCount: audioBuffer.numberOfChannels, sampleRate });
  writeWavAudioData({ arrayBuffer, dataByChannel: splitDataByChannel(audioBuffer) });
  return new Blob([arrayBuffer], { type: "audio/wav" });
};
var transcode = ({
  blob,
  encoder = encodeToWaw,
  sampleRate
}) => encoder(
  createFileFromBlobs({
    blobsArray: [blob],
    fileName: `audio_recording_${(/* @__PURE__ */ new Date()).toISOString()}.${getExtensionFromMimeType(
      blob.type
    )}`,
    mimeType: blob.type
  }),
  sampleRate
);
var RECORDED_MIME_TYPE_BY_BROWSER = {
  audio: {
    others: "audio/webm",
    safari: "audio/mp4;codecs=mp4a.40.2"
  }
};
var DEFAULT_AUDIO_TRANSCODER_CONFIG = {
  sampleRate: 16e3
};
var disposeOfMediaStream = (stream2) => {
  if (!stream2?.active) return;
  stream2.getTracks().forEach((track2) => {
    track2.stop();
    stream2.removeTrack(track2);
  });
};
var logError = (e) => e && console.error("[MEDIA RECORDER ERROR]", e);
var MediaRecordingState = ((MediaRecordingState2) => {
  MediaRecordingState2["PAUSED"] = "paused";
  MediaRecordingState2["RECORDING"] = "recording";
  MediaRecordingState2["STOPPED"] = "stopped";
  return MediaRecordingState2;
})(MediaRecordingState || {});
var RecordingAttachmentType = ((RecordingAttachmentType2) => {
  RecordingAttachmentType2["VOICE_RECORDING"] = "voiceRecording";
  return RecordingAttachmentType2;
})(RecordingAttachmentType || {});
var MediaRecorderController = class {
  constructor({ config, generateRecordingTitle, t: t2 } = {}) {
    this.recordedChunkDurations = [];
    this.recordedData = [];
    this.recordingState = new BehaviorSubject(void 0);
    this.recording = new BehaviorSubject(void 0);
    this.error = new Subject();
    this.notification = new Subject();
    this.generateRecordingTitle = (mimeType) => {
      if (this.customGenerateRecordingTitle) {
        return this.customGenerateRecordingTitle(mimeType);
      }
      return `${this.mediaType}_recording_${(/* @__PURE__ */ new Date()).toISOString()}.${getExtensionFromMimeType(
        mimeType
      )}`;
    };
    this.makeVoiceRecording = async () => {
      if (this.recordingUri) URL.revokeObjectURL(this.recordingUri);
      if (!this.recordedData.length) return;
      const { mimeType } = this.mediaRecorderConfig;
      let blob = new Blob(this.recordedData, { type: mimeType });
      if (mimeType.match("audio/webm")) {
        blob = await (0, import_fix_webm_duration.default)(blob, this.durationMs, {
          logger: () => null
          // prevents polluting the browser console
        });
      }
      if (!mimeType.match("audio/mp4")) {
        blob = await transcode({
          blob,
          ...this.transcoderConfig
        });
      }
      if (!blob) return;
      this.recordingUri = URL.createObjectURL(blob);
      const file = createFileFromBlobs({
        blobsArray: [blob],
        fileName: this.generateRecordingTitle(blob.type),
        mimeType: blob.type
      });
      return {
        asset_url: this.recordingUri,
        duration: this.durationMs / 1e3,
        file_size: blob.size,
        localMetadata: {
          file,
          id: nanoid()
        },
        mime_type: blob.type,
        title: file.name,
        type: "voiceRecording",
        waveform_data: resampleWaveformData(
          this.amplitudeRecorder?.amplitudes.value ?? [],
          this.amplitudeRecorderConfig.sampleCount
        )
      };
    };
    this.handleErrorEvent = (e) => {
      const { error: error2 } = e;
      logError(error2);
      this.error.next(error2);
      this.notification.next({
        text: this.t("An error has occurred during recording"),
        type: "error"
      });
    };
    this.handleDataavailableEvent = async (e) => {
      if (!e.data.size) return;
      if (this.mediaType !== "audio") return;
      try {
        this.recordedData.push(e.data);
        const recording = await this.makeVoiceRecording();
        if (!recording) return;
        this.signalRecordingReady?.(recording);
        this.recording.next(recording);
      } catch (e2) {
        logError(e2);
        this.error.next(e2);
        this.notification.next({
          text: this.t("An error has occurred during the recording processing"),
          type: "error"
        });
      }
    };
    this.resetRecordingState = () => {
      this.recordedData = [];
      this.recording.next(void 0);
      this.recordingState.next(void 0);
      this.recordedChunkDurations = [];
      this.startTime = void 0;
    };
    this.cleanUp = () => {
      this.resetRecordingState();
      if (this.recordingUri) URL.revokeObjectURL(this.recordingUri);
      this.amplitudeRecorder?.close();
      if (this.mediaRecorder) {
        disposeOfMediaStream(this.mediaRecorder.stream);
        this.mediaRecorder.removeEventListener(
          "dataavailable",
          this.handleDataavailableEvent
        );
        this.mediaRecorder.removeEventListener("error", this.handleErrorEvent);
      }
    };
    this.start = async () => {
      if ([
        "recording",
        "paused"
        /* PAUSED */
      ].includes(
        this.recordingState.value
      )) {
        const error2 = new Error("Cannot start recording. Recording already in progress");
        logError(error2);
        this.error.next(error2);
        return;
      }
      if (!navigator.mediaDevices) {
        const error2 = new Error("Media recording is not supported");
        logError(error2);
        this.error.next(error2);
        this.notification.next({ text: this.t("Error starting recording"), type: "error" });
        return;
      }
      if (this.mediaType === "video") {
        const error2 = new Error(
          `Video recording is not supported. Provided MIME type: ${this.mediaRecorderConfig.mimeType}`
        );
        logError(error2);
        this.error.next(error2);
        this.notification.next({ text: this.t("Error starting recording"), type: "error" });
        return;
      }
      if (!this.permission.state.value) {
        await this.permission.check();
      }
      if (this.permission.state.value === "denied") {
        logError(new Error("Permission denied"));
        return;
      }
      try {
        const stream2 = await navigator.mediaDevices.getUserMedia({ audio: true });
        this.mediaRecorder = new MediaRecorder(stream2, this.mediaRecorderConfig);
        this.mediaRecorder.addEventListener("dataavailable", this.handleDataavailableEvent);
        this.mediaRecorder.addEventListener("error", this.handleErrorEvent);
        this.startTime = (/* @__PURE__ */ new Date()).getTime();
        this.mediaRecorder.start();
        if (this.mediaType === "audio" && stream2) {
          this.amplitudeRecorder = new AmplitudeRecorder({
            config: this.amplitudeRecorderConfig,
            stream: stream2
          });
          this.amplitudeRecorder.start();
        }
        this.recordingState.next(
          "recording"
          /* RECORDING */
        );
      } catch (error2) {
        logError(error2);
        this.cancel();
        this.error.next(error2);
        this.notification.next({ text: this.t("Error starting recording"), type: "error" });
      }
    };
    this.pause = () => {
      if (this.recordingState.value !== "recording") return;
      if (this.startTime) {
        this.recordedChunkDurations.push((/* @__PURE__ */ new Date()).getTime() - this.startTime);
        this.startTime = void 0;
      }
      this.mediaRecorder?.pause();
      this.mediaRecorder?.requestData();
      this.amplitudeRecorder?.stop();
      this.recordingState.next(
        "paused"
        /* PAUSED */
      );
    };
    this.resume = () => {
      if (this.recordingState.value !== "paused") return;
      this.startTime = (/* @__PURE__ */ new Date()).getTime();
      this.mediaRecorder?.resume();
      this.amplitudeRecorder?.start();
      this.recordingState.next(
        "recording"
        /* RECORDING */
      );
    };
    this.stop = () => {
      const recording = this.recording.value;
      if (recording && this.mediaRecorder?.state === "inactive")
        return Promise.resolve(recording);
      if (![
        "paused",
        "recording"
        /* RECORDING */
      ].includes(
        this.mediaRecorder?.state || ""
      ))
        return Promise.resolve(void 0);
      if (this.startTime) {
        this.recordedChunkDurations.push((/* @__PURE__ */ new Date()).getTime() - this.startTime);
        this.startTime = void 0;
      }
      const result = new Promise((res) => {
        this.signalRecordingReady = res;
      });
      this.mediaRecorder?.stop();
      this.amplitudeRecorder?.stop();
      this.recordingState.next(
        "stopped"
        /* STOPPED */
      );
      return result;
    };
    this.cancel = () => {
      this.stop();
      this.cleanUp();
    };
    this.t = t2 || defaultTranslatorFunction;
    this.amplitudeRecorderConfig = mergeDeepUndefined(
      { ...config?.amplitudeRecorderConfig },
      DEFAULT_AMPLITUDE_RECORDER_CONFIG
    );
    this.mediaRecorderConfig = mergeDeepUndefined(
      { ...config?.mediaRecorderConfig },
      {
        mimeType: MediaRecorder.isTypeSupported("audio/webm") ? RECORDED_MIME_TYPE_BY_BROWSER.audio.others : RECORDED_MIME_TYPE_BY_BROWSER.audio.safari
      }
    );
    this.transcoderConfig = mergeDeepUndefined(
      { ...config?.transcoderConfig },
      DEFAULT_AUDIO_TRANSCODER_CONFIG
    );
    const mediaType = getRecordedMediaTypeFromMimeType(this.mediaRecorderConfig.mimeType);
    if (!mediaType) {
      throw new Error(
        `Unsupported media type (supported audio or video only). Provided mimeType: ${this.mediaRecorderConfig.mimeType}`
      );
    }
    this.mediaType = mediaType;
    this.permission = new BrowserPermission({ mediaType });
    this.customGenerateRecordingTitle = generateRecordingTitle;
  }
  get durationMs() {
    return this.recordedChunkDurations.reduce((acc, val) => acc + val, 0);
  }
};
var useMediaRecorder = ({
  asyncMessagesMultiSendEnabled,
  enabled,
  generateRecordingTitle,
  handleSubmit,
  recordingConfig
}) => {
  const { t: t2 } = useTranslationContext("useMediaRecorder");
  const messageComposer = useMessageComposerController();
  const [recording, setRecording] = (0, import_react29.useState)();
  const [recordingState, setRecordingState] = (0, import_react29.useState)();
  const [permissionState, setPermissionState] = (0, import_react29.useState)();
  const [isScheduledForSubmit, scheduleForSubmit] = (0, import_react29.useState)(false);
  const recorder = (0, import_react29.useMemo)(
    () => enabled ? new MediaRecorderController({
      config: recordingConfig ?? {},
      generateRecordingTitle,
      t: t2
    }) : void 0,
    [recordingConfig, enabled, generateRecordingTitle, t2]
  );
  const completeRecording = (0, import_react29.useCallback)(async () => {
    if (!recorder) return;
    const recording2 = await recorder.stop();
    if (!recording2) return;
    await messageComposer.attachmentManager.uploadAttachment(recording2);
    if (!asyncMessagesMultiSendEnabled) {
      scheduleForSubmit(true);
    }
    recorder.cleanUp();
  }, [asyncMessagesMultiSendEnabled, messageComposer, recorder]);
  (0, import_react29.useEffect)(() => {
    if (!isScheduledForSubmit) return;
    handleSubmit();
    scheduleForSubmit(false);
  }, [handleSubmit, isScheduledForSubmit]);
  (0, import_react29.useEffect)(() => {
    if (!recorder) return;
    recorder.permission.watch();
    const recordingSubscription = recorder.recording.subscribe(setRecording);
    const recordingStateSubscription = recorder.recordingState.subscribe(setRecordingState);
    const permissionStateSubscription = recorder.permission.state.subscribe(setPermissionState);
    return () => {
      recorder.cancel();
      recorder.permission.unwatch();
      recordingSubscription.unsubscribe();
      recordingStateSubscription.unsubscribe();
      permissionStateSubscription.unsubscribe();
    };
  }, [recorder]);
  return {
    completeRecording,
    permissionState,
    recorder,
    recording,
    recordingState
  };
};
var useMessageComposerBindings = (props) => {
  const { asyncMessagesMultiSendEnabled, audioRecordingConfig, audioRecordingEnabled } = props;
  const { textareaRef } = useTextareaRef(props);
  const { handleSubmit } = useSubmitHandler(props);
  const recordingController = useMediaRecorder({
    asyncMessagesMultiSendEnabled,
    enabled: !!audioRecordingEnabled,
    handleSubmit,
    recordingConfig: audioRecordingConfig
  });
  const { onPaste } = usePasteHandler();
  return {
    handleSubmit,
    onPaste,
    recordingController,
    textareaRef
  };
};
var editingAuditStateStateSelector$1 = (state) => state;
var useMessageComposerHasSendableData = () => {
  const messageComposer = useMessageComposerController();
  useStateStore(messageComposer.editingAuditState, editingAuditStateStateSelector$1);
  return messageComposer.hasSendableData;
};
var editingAuditStateStateSelector = (state) => state;
var useMessageContentIsEmpty = () => {
  const messageComposer = useMessageComposerController();
  useStateStore(messageComposer.editingAuditState, editingAuditStateStateSelector);
  return messageComposer.contentIsEmpty;
};
var RemoveAttachmentPreviewButton = ({
  className,
  uploadState,
  ...props
}) => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)(
    Button,
    {
      "aria-label": t2("aria/Remove attachment"),
      circular: true,
      className: clsx_default("str-chat__attachment-preview__remove-button", className),
      "data-testid": "preview-item-delete-button",
      disabled: uploadState === "uploading",
      ...props,
      children: (0, import_jsx_runtime4.jsx)(IconXmarkSmall, {})
    }
  );
};
var ImagePlaceholder = ({ className }) => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      "aria-label": t2("aria/Image failed to load"),
      className: clsx_default("str-chat__image-placeholder", className),
      "data-testid": "str-chat__base-image-placeholder",
      role: "img",
      children: (0, import_jsx_runtime4.jsx)(IconImage, {})
    }
  );
};
var BaseImage = (0, import_react29.forwardRef)(function BaseImage2({ src, ...props }, ref) {
  const {
    alt: propsAlt,
    className: propsClassName,
    onError: propsOnError,
    showDownloadButtonOnError = false,
    ...imgProps
  } = props;
  const [error2, setError] = (0, import_react29.useState)(false);
  const { ImagePlaceholder: ImagePlaceholderComponent = ImagePlaceholder } = useComponentContext();
  const sanitizedUrl = (0, import_react29.useMemo)(() => (0, import_sanitize_url.sanitizeUrl)(src), [src]);
  (0, import_react29.useEffect)(
    () => () => {
      setError(false);
    },
    [sanitizedUrl]
  );
  if (error2) {
    return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
      (0, import_jsx_runtime4.jsx)(
        ImagePlaceholderComponent,
        {
          className: clsx_default(propsClassName, "str-chat__base-image--load-failed")
        }
      ),
      showDownloadButtonOnError && (0, import_jsx_runtime4.jsx)(DownloadButton, { assetUrl: sanitizedUrl })
    ] });
  }
  return (0, import_jsx_runtime4.jsx)(
    "img",
    {
      "data-testid": "str-chat__base-image",
      ...imgProps,
      alt: propsAlt,
      className: clsx_default(propsClassName, "str-chat__base-image"),
      onError: (e) => {
        setError(true);
        propsOnError?.(e);
      },
      ref,
      src: sanitizedUrl
    }
  );
});
var toBaseImageDescriptors = (attachment, options = {}) => {
  if (isGiphyAttachment(attachment)) {
    const giphyVersion = options?.giphyVersionName && attachment.giphy ? attachment.giphy[options.giphyVersionName] : void 0;
    return {
      alt: giphyVersion?.url || attachment.thumb_url,
      dimensions: giphyVersion ? {
        height: giphyVersion.height,
        width: giphyVersion.width
      } : void 0,
      imageUrl: attachment.thumb_url,
      title: attachment.title || attachment.thumb_url
    };
  }
  if (isScrapedContent(attachment)) {
    const imageUrl = attachment.image_url || attachment.thumb_url;
    return {
      alt: attachment.title || imageUrl,
      imageUrl,
      title: attachment.title
    };
  }
  if (isLocalVideoAttachment(attachment)) {
    return {
      title: attachment.title,
      videoThumbnailUrl: attachment.thumb_url ?? attachment.localMetadata.previewUri,
      videoUrl: attachment.asset_url ?? attachment.localMetadata.previewUri
    };
  }
  if (isVideoAttachment(attachment)) {
    return {
      title: attachment.title,
      videoThumbnailUrl: attachment.thumb_url,
      videoUrl: attachment.asset_url
    };
  }
  if (isLocalImageAttachment(attachment)) {
    const imageUrl = attachment.image_url || attachment.localMetadata.previewUri;
    return {
      alt: attachment.title || imageUrl,
      imageUrl,
      title: attachment.title
    };
  }
  if (isImageAttachment(attachment)) {
    const imageUrl = attachment.image_url;
    return {
      alt: attachment.title || imageUrl,
      imageUrl,
      title: attachment.title
    };
  }
  return void 0;
};
var BASE_FILE_ICON_CLASSNAME = "str-chat__file-icon";
var FILE_ICON_GRAPHIC_CLASSNAME = "str-chat__file-icon__graphic";
var FILE_ICON_NO_LABEL_CLASSNAME = "str-chat__file-icon--no-label";
var FILE_ICON_SIZE_CONFIG = {
  lg: { height: 40, labelX: 16, labelY: 36, width: 32 },
  md: { height: 32, labelX: 16, labelY: 35, width: 26 },
  sm: { height: 24, labelX: 16, labelY: 31.5, width: 19 },
  xl: { height: 48, labelX: 16, labelY: 36, width: 40 }
};
var mergeFileIconSizeConfig = (overrides) => {
  if (!overrides) return FILE_ICON_SIZE_CONFIG;
  return ["sm", "md", "lg", "xl"].reduce(
    (acc, size4) => ({
      ...acc,
      [size4]: { ...FILE_ICON_SIZE_CONFIG[size4], ...overrides[size4] }
    }),
    {}
  );
};
var FILE_ICON_VIEWBOX = { height: 40, width: 32 };
var FILE_ICON_PAPER_PATH = "M0 4C0 1.79086 1.79086 0 4 0H22.4L32 10V36C32 38.2091 30.2091 40 28 40H4C1.79086 40 0 38.2091 0 36V4Z";
var FILE_ICON_FOLD_PATH = "M32 10H25.4C23.7431 10 22.4 8.65685 22.4 7V0L32 10Z";
var FILE_ICON_WITHOUT_CAPTION_SYMBOL_SCALE_X = 32 / 26;
var FILE_ICON_WITHOUT_CAPTION_SYMBOL_SCALE_Y = 40 / 32;
var Svg = ({ className, size: size4, sizeConfig, ...props }) => {
  const config = sizeConfig ?? FILE_ICON_SIZE_CONFIG;
  const dimensions = size4 ? config[size4] : void 0;
  const dimensionsStyle = dimensions ? {
    flexShrink: 0,
    height: `${dimensions.height}px`,
    width: `${dimensions.width}px`
  } : void 0;
  return (0, import_jsx_runtime4.jsx)(
    "svg",
    {
      height: dimensions?.height,
      viewBox: `0 0 ${FILE_ICON_VIEWBOX.width} ${FILE_ICON_VIEWBOX.height}`,
      width: dimensions?.width,
      xmlns: "http://www.w3.org/2000/svg",
      ...props,
      className: clsx_default(
        BASE_FILE_ICON_CLASSNAME,
        { [`${BASE_FILE_ICON_CLASSNAME}--size-${size4}`]: size4 },
        className
      ),
      style: { ...dimensionsStyle, ...props.style }
    }
  );
};
var FileIconLabel = ({ label, size: size4, sizeConfig }) => {
  const configMap = sizeConfig ?? FILE_ICON_SIZE_CONFIG;
  const config = size4 ? configMap[size4] : { labelX: 16, labelY: 33 };
  return (0, import_jsx_runtime4.jsx)("text", { className: "str-chat__file-icon__label", x: config.labelX, y: config.labelY, children: label });
};
var TEXT_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.6", rx: "0.8", width: "14.4", x: "8", y: "12.2" }),
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.6", rx: "0.8", width: "14.4", x: "8", y: "20.2" }),
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.6", rx: "0.8", width: "9.6", x: "8", y: "16.2" })
  ] }),
  withoutCaption: (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.4", rx: "0.7", width: "12.6", x: "6", y: "14.8" }),
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.4", rx: "0.7", width: "12.6", x: "6", y: "21.8" }),
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.4", rx: "0.7", width: "8.4", x: "6", y: "18.3" })
  ] })
};
var OTHER_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.6", rx: "0.8", width: "14.4", x: "8", y: "13.2" }),
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.6", rx: "0.8", width: "14.4", x: "8", y: "21.2" }),
    (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "1.6", rx: "0.8", width: "9.6", x: "8", y: "17.2" })
  ] }),
  withoutCaption: TEXT_SYMBOLS.withoutCaption
};
var CODE_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M14.5 22.5L17.5 11.5M20.1666 14.1667L21.9732 16.0862C22.4564 16.5996 22.4564 17.4004 21.9732 17.9138L20.1666 19.8333M11.8333 19.8333L10.0267 17.9138C9.54351 17.4004 9.54351 16.5996 10.0267 16.0862L11.8333 14.1667",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.2"
    }
  ),
  withoutCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M11.6876 23.8125L14.3126 14.1875M16.646 16.5208L18.2267 18.2004C18.6495 18.6496 18.6495 19.3503 18.2267 19.7996L16.646 21.4792M9.3543 21.4792L7.77352 19.7996C7.35072 19.3503 7.35072 18.6496 7.77352 18.2004L9.3543 16.5208",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.33333"
    }
  )
};
var AUDIO_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M20.5 15.5V18.5M22.5 14.5V19.5M13.5 19.5H10.5C10.3674 19.5 10.2402 19.4473 10.1464 19.3536C10.0527 19.2598 10 19.1326 10 19V15C10 14.8674 10.0527 14.7402 10.1464 14.6464C10.2402 14.5527 10.3674 14.5 10.5 14.5H13.5L18 11V23L13.5 19.5Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.2"
    }
  ),
  withoutCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M17.5 17.5V20.5M19.5 16.5V21.5M10.5 21.5H7.5C7.36739 21.5 7.24021 21.4473 7.14645 21.3536C7.05268 21.2598 7 21.1326 7 21V17C7 16.8674 7.05268 16.7402 7.14645 16.6464C7.24021 16.5527 7.36739 16.5 7.5 16.5H10.5L15 13V25L10.5 21.5Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.33333"
    }
  )
};
var PRESENTATION_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M12.5 15.5H19.5M16 17.5V22.5M14 22.5H18M9.49999 17.5C9.41472 17.5001 9.33085 17.4783 9.25635 17.4368C9.18185 17.3953 9.1192 17.3355 9.07436 17.263C9.02952 17.1904 9.00397 17.1076 9.00014 17.0224C8.99631 16.9373 9.01433 16.8525 9.05249 16.7762L11.5525 11.7763C11.5941 11.6932 11.6579 11.6233 11.737 11.5745C11.816 11.5257 11.9071 11.4999 12 11.5H20C20.0929 11.4999 20.184 11.5257 20.263 11.5745C20.342 11.6233 20.4059 11.6932 20.4475 11.7763L22.9475 16.7762C22.9857 16.8525 23.0037 16.9373 22.9998 17.0224C22.996 17.1076 22.9705 17.1904 22.9256 17.263C22.8808 17.3355 22.8181 17.3953 22.7436 17.4368C22.6691 17.4783 22.5853 17.5001 22.5 17.5H9.49999Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.2"
    }
  ),
  withoutCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M9.49999 17.5H16.5M13 19.5V24.5M11 24.5H15M6.49999 19.5C6.41472 19.5001 6.33085 19.4783 6.25635 19.4368C6.18185 19.3953 6.11921 19.3355 6.07436 19.263C6.02952 19.1904 6.00397 19.1076 6.00014 19.0224C5.99631 18.9373 6.01433 18.8525 6.05249 18.7763L8.55249 13.7763C8.59406 13.6932 8.65795 13.6233 8.73699 13.5745C8.81603 13.5257 8.9071 13.4999 8.99999 13.5H17C17.0929 13.4999 17.184 13.5257 17.263 13.5745C17.342 13.6233 17.4059 13.6932 17.4475 13.7763L19.9475 18.7763C19.9857 18.8525 20.0037 18.9373 19.9998 19.0224C19.996 19.1076 19.9705 19.1904 19.9256 19.263C19.8808 19.3355 19.8181 19.3953 19.7436 19.4368C19.6691 19.4783 19.5853 19.5001 19.5 19.5H6.49999Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.33333"
    }
  )
};
var SPREADSHEET_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M10 15.5H22M10 18.5H22M13.5 15.5V21.5M10 12.5H22V21C22 21.1326 21.9473 21.2598 21.8536 21.3536C21.7598 21.4473 21.6326 21.5 21.5 21.5H10.5C10.3674 21.5 10.2402 21.4473 10.1464 21.3536C10.0527 21.2598 10 21.1326 10 21V12.5Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.5"
    }
  ),
  withoutCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M7 17.5H19M7 20.5H19M10.5 17.5V23.5M7 14.5H19V23C19 23.1326 18.9473 23.2598 18.8536 23.3536C18.7598 23.4473 18.6326 23.5 18.5 23.5H7.5C7.36739 23.5 7.24021 23.4473 7.14645 23.3536C7.05268 23.2598 7 23.1326 7 23V14.5Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.2"
    }
  )
};
var COMPRESSION_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      clipRule: "evenodd",
      d: "M9.41177 0H7.52942V2H9.41177V4H7.52942V6H9.41177V8H7.52942V10H9.41177V12H7.52942V14H9.41177V12H11.2941V10H9.41177V8H11.2941V6H9.41177V4H11.2941V2H9.41177V0ZM7.52942 17C7.52942 16.4477 7.9508 16 8.4706 16H10.3529C10.8727 16 11.2941 16.4477 11.2941 17V23C11.2941 23.5523 10.8727 24 10.3529 24H8.4706C7.9508 24 7.52942 23.5523 7.52942 23V17ZM8.4706 23V20H10.3529V23H8.4706Z",
      fill: "white",
      fillRule: "evenodd"
    }
  ),
  withoutCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      clipRule: "evenodd",
      d: "M8.17031 0H6.11768V2.14737H8.17031V4.29474H6.11768V6.4421H8.17031V8.58947H6.11768V10.7368H8.17031V12.8842H6.11768V15.0316H8.17031V12.8842H10.2229V10.7368H8.17031V8.58947H10.2229V6.4421H8.17031V4.29474H10.2229V2.14737H8.17031V0ZM6.11768 18.2526C6.11768 17.6597 6.57717 17.1789 7.14399 17.1789H9.19662C9.76344 17.1789 10.2229 17.6597 10.2229 18.2526V24.6947C10.2229 25.2877 9.76344 25.7684 9.19662 25.7684H7.14399C6.57717 25.7684 6.11768 25.2877 6.11768 24.6947V18.2526ZM7.14399 24.6947V21.4737H9.19662V24.6947H7.14399Z",
      fill: "white",
      fillRule: "evenodd"
    }
  )
};
var VIDEO_SYMBOLS = {
  withCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M20.5 16L23.5 14V20L20.5 18M10 13H20C20.2761 13 20.5 13.2239 20.5 13.5V20.5C20.5 20.7761 20.2761 21 20 21H10C9.72386 21 9.5 20.7761 9.5 20.5V13.5C9.5 13.2239 9.72386 13 10 13Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.2"
    }
  ),
  withoutCaption: (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M17.5001 18L20.5001 16V22L17.5001 20M7.00012 15H17.0001C17.2763 15 17.5001 15.2239 17.5001 15.5V22.5C17.5001 22.7761 17.2763 23 17.0001 23H7.00012C6.72398 23 6.50012 22.7761 6.50012 22.5V15.5C6.50012 15.2239 6.72398 15 7.00012 15Z",
      stroke: "white",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.33333"
    }
  )
};
var StandardFileTypeIcon = ({
  className,
  color: color2,
  fileTypeClassName,
  label,
  size: size4,
  sizeConfig,
  symbols,
  ...props
}) => {
  const renderLabel = !!label;
  const resolvedLabel = renderLabel ? label : void 0;
  const symbolVariant = renderLabel ? "withCaption" : "withoutCaption";
  return (0, import_jsx_runtime4.jsxs)(
    Svg,
    {
      ...props,
      className: clsx_default(fileTypeClassName, className),
      size: size4,
      sizeConfig,
      children: [
        (0, import_jsx_runtime4.jsxs)("g", { className: FILE_ICON_GRAPHIC_CLASSNAME, children: [
          (0, import_jsx_runtime4.jsx)("path", { d: FILE_ICON_PAPER_PATH, fill: color2 }),
          renderLabel ? symbols[symbolVariant] : (0, import_jsx_runtime4.jsx)(
            "g",
            {
              transform: `scale(${FILE_ICON_WITHOUT_CAPTION_SYMBOL_SCALE_X} ${FILE_ICON_WITHOUT_CAPTION_SYMBOL_SCALE_Y})`,
              children: symbols[symbolVariant]
            }
          ),
          (0, import_jsx_runtime4.jsx)("path", { d: FILE_ICON_FOLD_PATH, fill: "white", opacity: "0.5" })
        ] }),
        resolvedLabel && (0, import_jsx_runtime4.jsx)(FileIconLabel, { label: resolvedLabel, size: size4, sizeConfig })
      ]
    }
  );
};
var PDF_SMALL_SYMBOL = (0, import_jsx_runtime4.jsx)(
  "path",
  {
    d: "M20.7533 19.5337C20.28 19.037 19.3093 18.7537 17.9373 18.7537C17.204 18.7537 16.3526 18.8244 15.43 18.9897C14.8647 18.4461 14.3499 17.8523 13.892 17.2157C13.5373 16.7424 13.2293 16.2224 12.9453 15.725C13.49 14.069 13.75 12.7204 13.75 11.7504C13.75 10.6624 13.348 9.52637 12.1886 9.52637C11.834 9.52637 11.4786 9.7397 11.2893 10.047C10.7693 10.9697 11.006 12.9804 11.9046 14.9677C11.5664 15.984 11.1876 16.9863 10.7693 17.9724C10.3906 18.8717 9.96465 19.7944 9.49131 20.6457C6.88931 21.687 5.20931 22.8937 5.01998 23.839C4.94931 24.1944 5.06731 24.5257 5.32798 24.7857C5.42265 24.857 5.75398 25.141 6.32131 25.141C8.04798 25.141 9.86998 22.349 10.7926 20.6697C11.5026 20.433 12.2126 20.1964 12.922 20.007C13.6704 19.8038 14.4284 19.638 15.1933 19.5104C17.0146 21.1424 18.6233 21.403 19.428 21.403C20.4213 21.403 20.7766 21.0004 20.8946 20.6697C21.108 20.243 20.966 19.7704 20.7533 19.5337ZM19.8066 20.2204C19.7353 20.575 19.38 20.8117 18.884 20.8117C18.742 20.8117 18.624 20.7877 18.4813 20.7644C17.5826 20.551 16.7306 20.1017 15.8793 19.3917C16.5357 19.2807 17.2003 19.2254 17.866 19.2264C18.3633 19.2264 18.7893 19.2497 19.0726 19.321C19.404 19.3917 19.9246 19.605 19.806 20.2197L19.8066 20.2204ZM14.7906 19.1084C14.1305 19.2321 13.4755 19.382 12.8273 19.5577C12.262 19.7047 11.7017 19.8703 11.1473 20.0544C11.4355 19.4962 11.7039 18.9281 11.952 18.351C12.236 17.6884 12.472 17.0024 12.7093 16.3637C12.9453 16.7657 13.206 17.1684 13.466 17.523C13.8911 18.065 14.3329 18.5937 14.7906 19.1084ZM11.692 10.307C11.7338 10.2232 11.7978 10.1525 11.877 10.1025C11.9563 10.0526 12.0477 10.0253 12.1413 10.0237C12.638 10.0237 12.7326 10.591 12.7326 11.041C12.7326 11.7977 12.496 12.957 12.094 14.2817C11.4073 12.4364 11.3606 10.899 11.692 10.307ZM9.08931 21.3317C7.88265 23.319 6.72331 24.549 6.01398 24.549C5.88599 24.5498 5.76132 24.5083 5.65931 24.431C5.51731 24.289 5.44598 24.1237 5.49331 23.9344C5.63531 23.2244 7.00731 22.231 9.08931 21.3317Z",
    fill: "white"
  }
);
var PDF_LEGACY_SYMBOL = (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
  (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M23.7533 19.2C23.28 18.7033 22.3093 18.42 20.9373 18.42C20.204 18.42 19.3526 18.4906 18.43 18.656C17.8647 18.1124 17.3499 17.5186 16.892 16.882C16.5373 16.4086 16.2293 15.8886 15.9453 15.3913C16.49 13.7353 16.75 12.3866 16.75 11.4166C16.75 10.3286 16.348 9.19263 15.1886 9.19263C14.834 9.19263 14.4786 9.40596 14.2893 9.71329C13.7693 10.636 14.006 12.6466 14.9046 14.634C14.5664 15.6502 14.1877 16.6526 13.7693 17.6386C13.3906 18.538 12.9646 19.4606 12.4913 20.312C9.88931 21.3533 8.20931 22.56 8.01998 23.5053C7.94931 23.8606 8.06731 24.192 8.32798 24.452C8.42265 24.5233 8.75398 24.8073 9.32131 24.8073C11.048 24.8073 12.87 22.0153 13.7926 20.336C14.5026 20.0993 15.2126 19.8626 15.922 19.6733C16.6704 19.4701 17.4284 19.3043 18.1933 19.1766C20.0146 20.8086 21.6233 21.0693 22.428 21.0693C23.4213 21.0693 23.7766 20.6666 23.8946 20.336C24.108 19.9093 23.966 19.4366 23.7533 19.2ZM22.8066 19.8866C22.7353 20.2413 22.38 20.478 21.884 20.478C21.742 20.478 21.624 20.454 21.4813 20.4306C20.5826 20.2173 19.7306 19.768 18.8793 19.058C19.5357 18.947 20.2003 18.8917 20.866 18.8926C21.3633 18.8926 21.7893 18.916 22.0726 18.9873C22.404 19.058 22.9246 19.2713 22.806 19.886L22.8066 19.8866ZM17.7906 18.7746C17.1305 18.8983 16.4755 19.0482 15.8273 19.224C15.262 19.3709 14.7017 19.5366 14.1473 19.7206C14.4355 19.1625 14.7039 18.5944 14.952 18.0173C15.236 17.3546 15.472 16.6686 15.7093 16.03C15.9453 16.432 16.206 16.8346 16.466 17.1893C16.8911 17.7313 17.3329 18.26 17.7906 18.7746ZM14.692 9.97329C14.7338 9.88949 14.7978 9.81875 14.877 9.7688C14.9563 9.71884 15.0477 9.69157 15.1413 9.68996C15.638 9.68996 15.7326 10.2573 15.7326 10.7073C15.7326 11.464 15.496 12.6233 15.094 13.948C14.4073 12.1026 14.3606 10.5653 14.692 9.97329ZM12.0893 20.998C10.8826 22.9853 9.72331 24.2153 9.01398 24.2153C8.88599 24.2161 8.76132 24.1746 8.65931 24.0973C8.51731 23.9553 8.44598 23.79 8.49331 23.6006C8.63531 22.8906 10.0073 21.8973 12.0893 20.998Z",
      fill: "white"
    }
  ),
  (0, import_jsx_runtime4.jsx)(
    "path",
    {
      d: "M9.74219 34.4258V28.6992H10.8828V29.3633H10.9531C11.0286 29.2096 11.1276 29.0781 11.25 28.9688C11.375 28.8594 11.5208 28.776 11.6875 28.7188C11.8542 28.6589 12.0391 28.6289 12.2422 28.6289C12.6016 28.6289 12.9115 28.7188 13.1719 28.8984C13.4323 29.0781 13.6328 29.3333 13.7734 29.6641C13.9167 29.9922 13.9883 30.3854 13.9883 30.8438V30.8516C13.9883 31.3125 13.918 31.7083 13.7773 32.0391C13.6367 32.3698 13.4362 32.6237 13.1758 32.8008C12.9154 32.9779 12.6042 33.0664 12.2422 33.0664C12.0443 33.0664 11.8607 33.0365 11.6914 32.9766C11.5247 32.9141 11.3776 32.8268 11.25 32.7148C11.125 32.6029 11.026 32.4688 10.9531 32.3125H10.8828V34.4258H9.74219ZM11.8516 32.1211C12.0547 32.1211 12.2279 32.0703 12.3711 31.9688C12.5169 31.8672 12.6289 31.7214 12.707 31.5312C12.7878 31.3411 12.8281 31.1146 12.8281 30.8516V30.8438C12.8281 30.5807 12.7878 30.3542 12.707 30.1641C12.6289 29.974 12.5169 29.8281 12.3711 29.7266C12.2279 29.625 12.0547 29.5742 11.8516 29.5742C11.651 29.5742 11.4766 29.625 11.3281 29.7266C11.1823 29.8281 11.069 29.974 10.9883 30.1641C10.9102 30.3516 10.8711 30.5781 10.8711 30.8438V30.8516C10.8711 31.112 10.9115 31.3385 10.9922 31.5312C11.0729 31.7214 11.1862 31.8672 11.332 31.9688C11.4805 32.0703 11.6536 32.1211 11.8516 32.1211ZM16.457 33.0664C16.1003 33.0664 15.7904 32.9779 15.5273 32.8008C15.2669 32.6211 15.0651 32.3659 14.9219 32.0352C14.7812 31.7044 14.7109 31.3099 14.7109 30.8516V30.8438C14.7109 30.3828 14.7799 29.987 14.918 29.6562C15.0586 29.3255 15.2591 29.0716 15.5195 28.8945C15.7799 28.7174 16.0924 28.6289 16.457 28.6289C16.6523 28.6289 16.8333 28.6602 17 28.7227C17.1693 28.7826 17.3177 28.8685 17.4453 28.9805C17.5729 29.0924 17.6719 29.2279 17.7422 29.3867H17.8125V27.0547H18.9531V33H17.8125V32.332H17.7422C17.6693 32.4857 17.5703 32.6172 17.4453 32.7266C17.3229 32.8359 17.1784 32.9206 17.0117 32.9805C16.8451 33.0378 16.6602 33.0664 16.457 33.0664ZM16.8438 32.1211C17.0469 32.1211 17.2214 32.0703 17.3672 31.9688C17.513 31.8672 17.625 31.7214 17.7031 31.5312C17.7839 31.3411 17.8242 31.1159 17.8242 30.8555V30.8477C17.8242 30.582 17.7839 30.3555 17.7031 30.168C17.625 29.9779 17.5117 29.832 17.3633 29.7305C17.2174 29.6263 17.0443 29.5742 16.8438 29.5742C16.6458 29.5742 16.4727 29.6263 16.3242 29.7305C16.1784 29.832 16.0664 29.9779 15.9883 30.168C15.9102 30.3555 15.8711 30.5807 15.8711 30.8438V30.8516C15.8711 31.1146 15.9102 31.3411 15.9883 31.5312C16.0664 31.7214 16.1784 31.8672 16.3242 31.9688C16.4701 32.0703 16.6432 32.1211 16.8438 32.1211ZM20.4648 33V29.5586H19.7695V28.6992H20.4648V28.2969C20.4648 28.0104 20.5156 27.7721 20.6172 27.582C20.7188 27.3919 20.8776 27.25 21.0938 27.1562C21.3125 27.0599 21.5951 27.0117 21.9414 27.0117C22.0586 27.0117 22.1641 27.0156 22.2578 27.0234C22.3542 27.0312 22.444 27.0417 22.5273 27.0547V27.8164C22.4909 27.8086 22.4427 27.8034 22.3828 27.8008C22.3255 27.7956 22.2604 27.793 22.1875 27.793C21.9661 27.793 21.8099 27.8438 21.7188 27.9453C21.6276 28.0443 21.582 28.1875 21.582 28.375V28.6992H22.4922V29.5586H21.6055V33H20.4648Z",
      fill: "white"
    }
  )
] });
var FilePdfIcon = ({
  className,
  label,
  size: size4,
  sizeConfig,
  ...props
}) => {
  const useLegacyPdfMarkup = !!label;
  return (0, import_jsx_runtime4.jsx)(
    Svg,
    {
      ...props,
      className: clsx_default(
        "str-chat__file-icon--pdf",
        useLegacyPdfMarkup && FILE_ICON_NO_LABEL_CLASSNAME,
        className
      ),
      size: size4,
      sizeConfig,
      children: (0, import_jsx_runtime4.jsxs)("g", { className: FILE_ICON_GRAPHIC_CLASSNAME, children: [
        (0, import_jsx_runtime4.jsx)("path", { d: FILE_ICON_PAPER_PATH, fill: "#E71A01" }),
        useLegacyPdfMarkup ? PDF_LEGACY_SYMBOL : (0, import_jsx_runtime4.jsx)(
          "g",
          {
            transform: `scale(${FILE_ICON_WITHOUT_CAPTION_SYMBOL_SCALE_X} ${FILE_ICON_WITHOUT_CAPTION_SYMBOL_SCALE_Y})`,
            children: PDF_SMALL_SYMBOL
          }
        ),
        (0, import_jsx_runtime4.jsx)("path", { d: FILE_ICON_FOLD_PATH, fill: "white", opacity: "0.5" })
      ] })
    }
  );
};
var FileWordIcon = ({ className, label, ...props }) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#3375E2",
    fileTypeClassName: "str-chat__file-icon--doc",
    label,
    symbols: TEXT_SYMBOLS
  }
);
var FilePowerPointIcon = ({ className, label, ...props }) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#D14423",
    fileTypeClassName: "str-chat__file-icon--ppt",
    label,
    symbols: PRESENTATION_SYMBOLS
  }
);
var FileExcelIcon = ({ className = "", label, ...props }) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#0C864B",
    fileTypeClassName: "str-chat__file-icon--xls",
    label,
    symbols: SPREADSHEET_SYMBOLS
  }
);
var FileArchiveIcon = ({
  className = "",
  label = "",
  ...props
}) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#E59E34",
    fileTypeClassName: "str-chat__file-icon--compressed",
    label,
    symbols: COMPRESSION_SYMBOLS
  }
);
var FileCodeIcon = ({ className = "", label, ...props }) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#00ACA1",
    fileTypeClassName: "str-chat__file-icon--code",
    label,
    symbols: CODE_SYMBOLS
  }
);
var FileAudioIcon = ({ className = "", label, ...props }) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#2727B0",
    fileTypeClassName: "str-chat__file-icon--audio",
    label,
    symbols: AUDIO_SYMBOLS
  }
);
var FileVideoIcon = ({ className = "", label, ...props }) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#A847B7",
    fileTypeClassName: "str-chat__file-icon--video",
    label,
    symbols: VIDEO_SYMBOLS
  }
);
var FileFallbackIcon = ({
  className = "",
  label = "",
  ...props
}) => (0, import_jsx_runtime4.jsx)(
  StandardFileTypeIcon,
  {
    ...props,
    className,
    color: "#888888",
    fileTypeClassName: "str-chat__file-icon--other",
    label,
    symbols: OTHER_SYMBOLS
  }
);
var wordMimeTypes = [
  // Microsoft Word
  // .doc .dot
  "application/msword",
  // .doc .dot
  "application/msword-template",
  // .docx
  "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
  // .dotx (no test)
  "application/vnd.openxmlformats-officedocument.wordprocessingml.template",
  // .docm
  "application/vnd.ms-word.document.macroEnabled.12",
  // .dotm (no test)
  "application/vnd.ms-word.template.macroEnabled.12",
  // LibreOffice/OpenOffice Writer
  // .odt
  "application/vnd.oasis.opendocument.text",
  // .ott
  "application/vnd.oasis.opendocument.text-template",
  // .fodt
  "application/vnd.oasis.opendocument.text-flat-xml"
  // .uot
  // NOTE: firefox doesn't know mimetype so maybe ignore
];
var excelMimeTypes = [
  // .csv
  "text/csv",
  // TODO: maybe more data files
  // Microsoft Excel
  // .xls .xlt .xla (no test for .xla)
  "application/vnd.ms-excel",
  // .xlsx
  "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
  // .xltx (no test)
  "application/vnd.openxmlformats-officedocument.spreadsheetml.template",
  // .xlsm
  "application/vnd.ms-excel.sheet.macroEnabled.12",
  // .xltm (no test)
  "application/vnd.ms-excel.template.macroEnabled.12",
  // .xlam (no test)
  "application/vnd.ms-excel.addin.macroEnabled.12",
  // .xlsb (no test)
  "application/vnd.ms-excel.addin.macroEnabled.12",
  // LibreOffice/OpenOffice Calc
  // .ods
  "application/vnd.oasis.opendocument.spreadsheet",
  // .ots
  "application/vnd.oasis.opendocument.spreadsheet-template",
  // .fods
  "application/vnd.oasis.opendocument.spreadsheet-flat-xml"
  // .uos
  // NOTE: firefox doesn't know mimetype so maybe ignore
];
var powerpointMimeTypes = [
  // Microsoft Word
  // .ppt .pot .pps .ppa (no test for .ppa)
  "application/vnd.ms-powerpoint",
  // .pptx
  "application/vnd.openxmlformats-officedocument.presentationml.presentation",
  // .potx (no test)
  "application/vnd.openxmlformats-officedocument.presentationml.template",
  // .ppsx
  "application/vnd.openxmlformats-officedocument.presentationml.slideshow",
  // .ppam
  "application/vnd.ms-powerpoint.addin.macroEnabled.12",
  // .pptm
  "application/vnd.ms-powerpoint.presentation.macroEnabled.12",
  // .potm
  "application/vnd.ms-powerpoint.template.macroEnabled.12",
  // .ppsm
  "application/vnd.ms-powerpoint.slideshow.macroEnabled.12",
  // LibreOffice/OpenOffice Writer
  // .odp
  "application/vnd.oasis.opendocument.presentation",
  // .otp
  "application/vnd.oasis.opendocument.presentation-template",
  // .fodp
  "application/vnd.oasis.opendocument.presentation-flat-xml"
  // .uop
  // NOTE: firefox doesn't know mimetype so maybe ignore
];
var archiveFileTypes = [
  // .zip
  "application/zip",
  // .z7
  "application/x-7z-compressed",
  // .ar
  "application/x-archive",
  // .tar
  "application/x-tar",
  // .tar.gz
  "application/gzip",
  // .tar.Z
  "application/x-compress",
  // .tar.bz2
  "application/x-bzip",
  // .tar.lz
  "application/x-lzip",
  // .tar.lz4
  "application/x-lz4",
  // .tar.lzma
  "application/x-lzma",
  // .tar.lzo (no test)
  "application/x-lzop",
  // .tar.xz
  "application/x-xz",
  // .war
  "application/x-webarchive",
  // .rar
  "application/vnd.rar"
];
var codeFileTypes = [
  // .html .htm
  "text/html",
  // .css
  "text/css",
  // .js
  "application/x-javascript",
  "text/javascript",
  // .json
  "application/json",
  // .py
  "text/x-python",
  // .go
  "text/x-go",
  // .c
  "text/x-csrc",
  // .cpp
  "text/x-c++src",
  // .rb
  "application/x-ruby",
  // .rust
  "text/rust",
  // .java
  "text/x-java",
  // .php
  "application/x-php",
  // .cs
  "text/x-csharp",
  // .scala
  "text/x-scala",
  // .erl
  "text/x-erlang",
  // .sh
  "application/x-shellscript"
];
var mimeTypeToExtensionMap = {
  // Application types (sorted alphabetically)
  "application/epub+zip": "epub",
  "application/gzip": "gz",
  "application/java-archive": "jar",
  "application/json": "json",
  "application/ld+json": "jsonld",
  "application/msword": "doc",
  "application/msword-template": "dot",
  "application/octet-stream": "bin",
  "application/ogg": "ogx",
  "application/pdf": "pdf",
  "application/postscript": "ps",
  "application/rtf": "rtf",
  "application/vnd.amazon.ebook": "azw",
  "application/vnd.apple.installer+xml": "mpkg",
  "application/vnd.mozilla.xul+xml": "xul",
  "application/vnd.ms-excel": "xls",
  "application/vnd.ms-excel.addin.macroEnabled.12": "xlam",
  "application/vnd.ms-excel.sheet.macroEnabled.12": "xlsm",
  "application/vnd.ms-excel.template.macroEnabled.12": "xltm",
  "application/vnd.ms-fontobject": "eot",
  "application/vnd.ms-powerpoint": "ppt",
  "application/vnd.ms-powerpoint.addin.macroEnabled.12": "ppam",
  "application/vnd.ms-powerpoint.presentation.macroEnabled.12": "pptm",
  "application/vnd.ms-powerpoint.slideshow.macroEnabled.12": "ppsm",
  "application/vnd.ms-powerpoint.template.macroEnabled.12": "potm",
  "application/vnd.ms-word.document.macroEnabled.12": "docm",
  "application/vnd.ms-word.template.macroEnabled.12": "dotm",
  "application/vnd.oasis.opendocument.presentation": "odp",
  "application/vnd.oasis.opendocument.presentation-flat-xml": "fodp",
  "application/vnd.oasis.opendocument.presentation-template": "otp",
  "application/vnd.oasis.opendocument.spreadsheet": "ods",
  "application/vnd.oasis.opendocument.spreadsheet-flat-xml": "fods",
  "application/vnd.oasis.opendocument.spreadsheet-template": "ots",
  "application/vnd.oasis.opendocument.text": "odt",
  "application/vnd.oasis.opendocument.text-flat-xml": "fodt",
  "application/vnd.oasis.opendocument.text-template": "ott",
  "application/vnd.openxmlformats-officedocument.presentationml.presentation": "pptx",
  "application/vnd.openxmlformats-officedocument.presentationml.slideshow": "ppsx",
  "application/vnd.openxmlformats-officedocument.presentationml.template": "potx",
  "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": "xlsx",
  "application/vnd.openxmlformats-officedocument.spreadsheetml.template": "xltx",
  "application/vnd.openxmlformats-officedocument.wordprocessingml.document": "docx",
  "application/vnd.openxmlformats-officedocument.wordprocessingml.template": "dotx",
  "application/vnd.rar": "rar",
  "application/vnd.visio": "vsd",
  "application/wasm": "wasm",
  "application/x-7z-compressed": "7z",
  "application/x-abiword": "abw",
  "application/x-archive": "ar",
  "application/x-bzip": "bz",
  "application/x-bzip2": "bz2",
  "application/x-cdf": "cda",
  "application/x-compress": "Z",
  "application/x-csh": "csh",
  "application/x-dosexec": "exe",
  "application/x-freearc": "arc",
  "application/x-httpd-php": "php",
  "application/x-iso9660-image": "iso",
  "application/x-javascript": "js",
  "application/x-lz4": "lz4",
  "application/x-lzip": "lz",
  "application/x-lzma": "lzma",
  "application/x-lzop": "lzo",
  "application/x-mobipocket-ebook": "mobi",
  "application/x-msdownload": "exe",
  "application/x-perl": "pl",
  "application/x-php": "php",
  "application/x-rar-compressed": "rar",
  "application/x-ruby": "rb",
  "application/x-sh": "sh",
  "application/x-shellscript": "sh",
  "application/x-shockwave-flash": "swf",
  "application/x-sql": "sql",
  "application/x-stuffit": "sit",
  "application/x-tar": "tar",
  "application/x-webarchive": "war",
  "application/x-xz": "xz",
  "application/x-yaml": "yaml",
  "application/xhtml+xml": "xhtml",
  "application/xml": "xml",
  "application/zip": "zip",
  // Audio types
  "audio/aac": "aac",
  "audio/flac": "flac",
  "audio/midi": "midi",
  "audio/mp4": "m4a",
  "audio/mpeg": "mp3",
  "audio/ogg": "oga",
  "audio/opus": "opus",
  "audio/wav": "wav",
  "audio/webm": "weba",
  "audio/x-aiff": "aiff",
  "audio/x-m4a": "m4a",
  "audio/x-midi": "midi",
  "audio/x-ms-wma": "wma",
  "audio/x-wav": "wav",
  // Font types
  "font/otf": "otf",
  "font/ttf": "ttf",
  "font/woff": "woff",
  "font/woff2": "woff2",
  // Image types
  "image/apng": "apng",
  "image/avif": "avif",
  "image/bmp": "bmp",
  "image/gif": "gif",
  "image/heic": "heic",
  "image/heif": "heif",
  "image/jpeg": "jpg",
  "image/png": "png",
  "image/svg+xml": "svg",
  "image/tiff": "tiff",
  "image/vnd.microsoft.icon": "ico",
  "image/webp": "webp",
  "image/x-icon": "ico",
  // Text types
  "text/calendar": "ics",
  "text/css": "css",
  "text/csv": "csv",
  "text/html": "html",
  "text/javascript": "js",
  "text/markdown": "md",
  "text/plain": "txt",
  "text/rtf": "rtf",
  "text/rust": "rs",
  "text/tab-separated-values": "tsv",
  "text/vcard": "vcf",
  "text/x-c": "c",
  "text/x-c++src": "cpp",
  "text/x-csharp": "cs",
  "text/x-csrc": "c",
  "text/x-diff": "diff",
  "text/x-erlang": "erl",
  "text/x-go": "go",
  "text/x-java": "java",
  "text/x-java-source": "java",
  "text/x-kotlin": "kt",
  "text/x-lua": "lua",
  "text/x-markdown": "md",
  "text/x-objectivec": "m",
  "text/x-pascal": "pas",
  "text/x-perl": "pl",
  "text/x-python": "py",
  "text/x-ruby": "rb",
  "text/x-rust": "rs",
  "text/x-scala": "scala",
  "text/x-sh": "sh",
  "text/x-shellscript": "sh",
  "text/x-sql": "sql",
  "text/x-swift": "swift",
  "text/x-typescript": "ts",
  "text/x-yaml": "yaml",
  "text/xml": "xml",
  "text/yaml": "yaml",
  // Video types
  "video/3gpp": "3gp",
  "video/3gpp2": "3g2",
  "video/mp2t": "ts",
  "video/mp4": "mp4",
  "video/mpeg": "mpeg",
  "video/ogg": "ogv",
  "video/quicktime": "mov",
  "video/webm": "webm",
  "video/x-flv": "flv",
  "video/x-m4v": "m4v",
  "video/x-matroska": "mkv",
  "video/x-ms-wmv": "wmv",
  "video/x-msvideo": "avi"
};
function generateMimeTypeToIconMap({
  FileArchiveIcon: FileArchiveIcon2,
  FileCodeIcon: FileCodeIcon2,
  FileExcelIcon: FileExcelIcon2,
  FilePdfIcon: FilePdfIcon2,
  FilePowerPointIcon: FilePowerPointIcon2,
  FileWordIcon: FileWordIcon2
}) {
  const mimeTypeToIconMap = {
    "application/pdf": FilePdfIcon2
  };
  for (const type of wordMimeTypes) {
    mimeTypeToIconMap[type] = FileWordIcon2;
  }
  for (const type of excelMimeTypes) {
    mimeTypeToIconMap[type] = FileExcelIcon2;
  }
  for (const type of powerpointMimeTypes) {
    mimeTypeToIconMap[type] = FilePowerPointIcon2;
  }
  for (const type of archiveFileTypes) {
    mimeTypeToIconMap[type] = FileArchiveIcon2;
  }
  for (const type of codeFileTypes) {
    mimeTypeToIconMap[type] = FileCodeIcon2;
  }
  return mimeTypeToIconMap;
}
function generateGeneralTypeToIconMap({
  FileAltIcon,
  FileAudioIcon: FileAudioIcon2,
  FileVideoIcon: FileVideoIcon2
}) {
  return {
    "audio/": FileAudioIcon2,
    "text/": FileAltIcon,
    "video/": FileVideoIcon2
  };
}
var iconMap = {
  standard: {
    ...generateMimeTypeToIconMap({
      FileArchiveIcon,
      FileCodeIcon,
      FileExcelIcon,
      FilePdfIcon,
      FilePowerPointIcon,
      FileWordIcon
    }),
    ...generateGeneralTypeToIconMap({
      FileAltIcon: FileFallbackIcon,
      FileAudioIcon,
      FileVideoIcon
    }),
    fallback: FileFallbackIcon
  }
};
function mimeTypeToIcon(mimeType) {
  const theMap = iconMap["standard"];
  if (!mimeType) return theMap.fallback;
  const icon = theMap[mimeType];
  if (icon) return icon;
  if (mimeType.startsWith("audio/")) return theMap["audio/"];
  if (mimeType.startsWith("video/")) return theMap["video/"];
  if (mimeType.startsWith("image/")) return theMap["image/"];
  if (mimeType.startsWith("text/")) return theMap["text/"];
  return theMap.fallback;
}
var labelFromMimeType = ({
  fileName,
  mimeType
}) => {
  let label;
  if (mimeType) {
    label = mimeTypeToExtensionMap[mimeType];
  }
  if (!label && fileName) {
    label = fileName.split(".").slice(-1)[0];
  }
  return label;
};
var FileIcon = (props) => {
  const {
    className,
    fileName,
    mimeType,
    size: size4 = "md",
    sizeConfig: sizeConfigOverride,
    ...rest
  } = props;
  const sizeConfig = (0, import_react29.useMemo)(
    () => mergeFileIconSizeConfig(sizeConfigOverride),
    [sizeConfigOverride]
  );
  const Icon = mimeTypeToIcon(mimeType);
  const label = fileName ? labelFromMimeType({ fileName, mimeType }) : void 0;
  return (0, import_jsx_runtime4.jsx)(
    Icon,
    {
      ...rest,
      className,
      label,
      size: size4,
      sizeConfig
    }
  );
};
var QuotedMessageIndicator = ({ isOwnMessage }) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    className: clsx_default("str-chat__quoted-message-indicator", {
      "str-chat__quoted-message-indicator--own-message": isOwnMessage
    })
  }
);
var messageComposerStateStoreSelector = (state) => ({
  quotedMessage: state.quotedMessage
});
var NullAttachmentIcon = () => null;
var QUOTED_GIPHY_PREVIEW_LABEL = "Giphy";
var isQuotedGiphyAttachment = (attachment) => isGiphyAttachment(attachment);
var getAttachmentType = (attachment) => {
  if (isQuotedGiphyAttachment(attachment)) {
    return "giphy";
  }
  if (isScrapedContent(attachment)) {
    return "link";
  } else if (isVideoAttachment(attachment, SUPPORTED_VIDEO_FORMATS$1)) {
    return "video";
  } else if (isImageAttachment(attachment)) {
    return "image";
  } else if (isAudioAttachment(attachment)) {
    return "audio";
  } else if (isVoiceRecordingAttachment(attachment)) {
    return "voiceRecording";
  } else if (isFileAttachment(attachment, SUPPORTED_VIDEO_FORMATS$1)) {
    return "file";
  }
  return "unsupported";
};
var getGroupedAttachments = (quotedMessage) => {
  const groupedAttachments = {
    documents: [],
    giphies: [],
    images: [],
    links: [],
    locations: [],
    polls: [],
    total: 0,
    videos: [],
    voiceRecordings: []
  };
  if (!quotedMessage || !quotedMessage.attachments) return groupedAttachments;
  const result = quotedMessage.attachments.reduce(
    (count2, attachment) => {
      switch (getAttachmentType(attachment)) {
        case "giphy":
          count2.giphies.push(attachment);
          count2.total += 1;
          break;
        case "link":
          count2.links.push(attachment);
          count2.total += 1;
          break;
        case "video":
          count2.videos.push(attachment);
          count2.total += 1;
          break;
        case "voiceRecording":
          count2.voiceRecordings.push(attachment);
          count2.total += 1;
          break;
        case "audio":
        case "file":
          count2.documents.push(attachment);
          count2.total += 1;
          break;
        default:
          if (isImageAttachment(attachment)) {
            count2.images.push(attachment);
            count2.total += 1;
          }
      }
      return count2;
    },
    groupedAttachments
  );
  if (quotedMessage.shared_location) {
    result.locations.push(quotedMessage.shared_location);
    result.total += 1;
  } else if (quotedMessage.poll) {
    result.polls.push(quotedMessage.poll);
    result.total += 1;
  }
  return result;
};
var getAttachmentIconWithType = (quotedMessage, giphyVersionName) => {
  const groupedAttachments = getGroupedAttachments(quotedMessage);
  const result = {
    groupedAttachments,
    Icon: NullAttachmentIcon,
    PreviewImage: null,
    previewType: null
  };
  if (!groupedAttachments.total) return result;
  if (groupedAttachments.polls.length > 0)
    return { ...result, Icon: IconPoll, previewType: "poll" };
  if (groupedAttachments.locations.length > 0)
    return { ...result, Icon: IconLocation, previewType: "location" };
  if (groupedAttachments.giphies.length > 0 && groupedAttachments.giphies.length === groupedAttachments.total) {
    const giphyAttachment = groupedAttachments.giphies[0];
    const giphyVersion = giphyAttachment.giphy?.[giphyVersionName];
    const src = giphyVersion?.url || giphyAttachment.thumb_url || giphyAttachment.image_url || "";
    return {
      ...result,
      Icon: IconFile,
      PreviewImage: (0, import_jsx_runtime4.jsx)(
        BaseImage,
        {
          alt: QUOTED_GIPHY_PREVIEW_LABEL,
          className: "str-chat__attachment-preview__thumbnail",
          src,
          title: QUOTED_GIPHY_PREVIEW_LABEL
        }
      ),
      previewType: "giphy"
    };
  }
  if (groupedAttachments.documents.length === groupedAttachments.total && groupedAttachments.documents.length === 1) {
    const fileAttachment = groupedAttachments.documents[0];
    return {
      ...result,
      Icon: IconFile,
      PreviewImage: (0, import_jsx_runtime4.jsx)(FileIcon, { fileName: fileAttachment.title, mimeType: fileAttachment.mime_type }),
      previewType: "file"
    };
  }
  if (groupedAttachments.links.length === groupedAttachments.total) {
    const linkAttachment = groupedAttachments.links[0];
    return {
      ...result,
      Icon: IconLink,
      PreviewImage: (0, import_jsx_runtime4.jsx)(
        BaseImage,
        {
          alt: linkAttachment.title,
          className: "str-chat__attachment-preview__thumbnail",
          src: linkAttachment.thumb_url || linkAttachment.image_url,
          title: linkAttachment.title
        }
      ),
      previewType: "link"
    };
  }
  if (groupedAttachments.videos.length === groupedAttachments.total) {
    const videoAttachment = groupedAttachments.videos[0];
    return {
      ...result,
      Icon: IconVideo,
      PreviewImage: (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)(
          BaseImage,
          {
            alt: videoAttachment.asset_url,
            className: "str-chat__attachment-preview__thumbnail",
            src: videoAttachment.thumb_url,
            title: videoAttachment.title
          }
        ),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment-preview__thumbnail__play-indicator", children: (0, import_jsx_runtime4.jsx)(IconPlayFill, {}) })
      ] }),
      previewType: "video"
    };
  }
  if (groupedAttachments.images.length === groupedAttachments.total) {
    const imageAttachment = groupedAttachments.images[0];
    return {
      ...result,
      Icon: IconCamera,
      PreviewImage: (0, import_jsx_runtime4.jsx)(
        BaseImage,
        {
          alt: imageAttachment.fallback,
          className: "str-chat__attachment-preview__thumbnail",
          src: imageAttachment.image_url,
          title: imageAttachment.title
        }
      ),
      previewType: "image"
    };
  }
  if (groupedAttachments.voiceRecordings.length === groupedAttachments.total)
    return { ...result, Icon: IconVoice, previewType: "voice" };
  return { ...result, Icon: IconFile, previewType: "mixed" };
};
var QuotedMessagePreview = ({
  getQuotedMessageAuthor,
  renderText: renderText2
}) => {
  const messageComposer = useMessageComposerController();
  const { quotedMessage } = useStateStore(
    messageComposer.state,
    messageComposerStateStoreSelector
  );
  return quotedMessage ? (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-composer__quoted-message-preview-slot", children: (0, import_jsx_runtime4.jsx)(
    QuotedMessagePreviewUI,
    {
      getQuotedMessageAuthor,
      onRemove: () => messageComposer.setQuotedMessage(null),
      quotedMessage,
      renderText: renderText2
    }
  ) }) : null;
};
var QuotedMessagePreviewUI = ({
  getQuotedMessageAuthor,
  onClick,
  onRemove,
  quotedMessage,
  renderText: renderText2
}) => {
  const { client } = useChatContext();
  const { t: t2, userLanguage } = useTranslationContext();
  const { giphyVersion: giphyVersionName = "fixed_height" } = useChannelStateContext("QuotedMessagePreview");
  const quotedMessageText = (0, import_react29.useMemo)(
    () => quotedMessage?.i18n?.[`${userLanguage}_text`] || quotedMessage?.text,
    [quotedMessage?.i18n, quotedMessage?.text, userLanguage]
  );
  const { AttachmentIcon, PreviewImage, renderedText } = (0, import_react29.useMemo)(() => {
    if (!quotedMessage) return { AttachmentIcon: NullAttachmentIcon, renderedText: null };
    const {
      groupedAttachments,
      Icon: AttachmentIcon2,
      PreviewImage: PreviewImage2,
      previewType
    } = getAttachmentIconWithType(quotedMessage, giphyVersionName);
    let renderedText2;
    if (!quotedMessageText) {
      if (previewType === "poll") {
        renderedText2 = quotedMessage.poll.name;
      } else if (previewType === "location") {
        renderedText2 = t2("Live location");
      } else if (previewType === "voice") {
        {
          const voiceRecording = groupedAttachments.voiceRecordings[0];
          renderedText2 = t2("Voice message {{ duration }}", {
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            duration: displayDuration(voiceRecording.duration)
          });
        }
      } else if (previewType === "giphy") {
        renderedText2 = QUOTED_GIPHY_PREVIEW_LABEL;
      } else if (previewType === "link") {
        renderedText2 = groupedAttachments.links[0].title;
      } else if (previewType === "mixed") {
        renderedText2 = t2("{{ count }} files", { count: groupedAttachments.total });
      } else if (previewType === "video") {
        renderedText2 = groupedAttachments.videos.length === 1 ? t2("Video") : t2("{{ count }} videos", {
          count: groupedAttachments.videos.length
        });
      } else if (previewType === "file") {
        renderedText2 = groupedAttachments.documents[0].title;
      } else if (previewType === "image") {
        renderedText2 = groupedAttachments.images.length === 1 ? t2("Photo") : t2("{{ count }} photos", {
          count: groupedAttachments.images.length
        });
      }
    } else if (renderText2) {
      renderedText2 = renderText2(quotedMessageText, quotedMessage?.mentioned_users);
    } else {
      renderedText2 = quotedMessageText;
    }
    return {
      AttachmentIcon: AttachmentIcon2,
      PreviewImage: PreviewImage2,
      renderedText: renderedText2
    };
  }, [giphyVersionName, quotedMessage, quotedMessageText, renderText2, t2]);
  const isOwnMessage = client.user?.id === quotedMessage?.user?.id;
  if (!renderedText && !AttachmentIcon && !PreviewImage) return null;
  const authorName = getQuotedMessageAuthor?.(quotedMessage) ?? quotedMessage.user?.name;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__quoted-message-preview", {
        "str-chat__quoted-message-preview--own": isOwnMessage
      }),
      "data-testid": "quoted-message-preview",
      onClick,
      tabIndex: onClick && 0,
      children: [
        (0, import_jsx_runtime4.jsx)(QuotedMessageIndicator, { isOwnMessage }),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__quoted-message-preview__content", children: [
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__quoted-message-preview__author", children: isOwnMessage ? t2("You") : authorName ? t2("Reply to {{ authorName }}", { authorName }) : t2("Reply") }),
          (0, import_jsx_runtime4.jsxs)(
            "div",
            {
              className: "str-chat__quoted-message-preview__message",
              "data-testid": "quoted-message-text",
              children: [
                (0, import_jsx_runtime4.jsx)(AttachmentIcon, {}),
                (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__quoted-message-preview__message-text", children: renderedText })
              ]
            }
          )
        ] }),
        PreviewImage && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__quoted-message-preview__image", children: PreviewImage }),
        onRemove && (0, import_jsx_runtime4.jsx)(
          RemoveAttachmentPreviewButton,
          {
            "aria-label": t2("aria/Cancel Reply"),
            "data-testid": "quoted-message-preview-dismiss-btn",
            onClick: onRemove
          }
        )
      ]
    }
  );
};
var QuotedMessage = ({ renderText: propsRenderText }) => {
  const { message, renderText: contextRenderText } = useMessageContext();
  const { jumpToMessage } = useChannelActionContext("QuotedMessage");
  const renderText2 = propsRenderText ?? contextRenderText;
  const { quoted_message } = message;
  return quoted_message ? (0, import_jsx_runtime4.jsx)(
    QuotedMessagePreviewUI,
    {
      onClick: (e) => {
        if (!quoted_message) return;
        e.stopPropagation();
        e.preventDefault();
        jumpToMessage(quoted_message.id);
      },
      quotedMessage: quoted_message,
      renderText: renderText2
    }
  ) : null;
};
var reminderStateSelector = (state) => ({
  timeLeftMs: state.timeLeftMs
});
function SavedForLaterContent() {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-saved-for-later", children: [
    (0, import_jsx_runtime4.jsx)(IconBookmark, {}),
    (0, import_jsx_runtime4.jsx)("span", { children: t2("Saved for later") })
  ] });
}
var THRESHOLD_RELATIVE_MINUTES = 59;
function RemindMeContent({ reminder }) {
  const { t: t2 } = useTranslationContext();
  const { timeLeftMs } = useStateStore(reminder?.state, reminderStateSelector) ?? {};
  const stopRefreshBoundaryMs = reminder?.timer.stopRefreshBoundaryMs;
  const stopRefreshTimeStamp = reminder?.remindAt && stopRefreshBoundaryMs ? reminder.remindAt.getTime() + stopRefreshBoundaryMs : void 0;
  const isBehindRefreshBoundary = !!stopRefreshTimeStamp && (/* @__PURE__ */ new Date()).getTime() > stopRefreshTimeStamp;
  if (timeLeftMs === null || !reminder.remindAt) return null;
  const nowMs = Date.now();
  const remindAtMs = reminder.remindAt.getTime();
  const diffMs = remindAtMs - nowMs;
  const diffMinutes = Math.abs(diffMs) / (60 * 1e3);
  const useAbsoluteFormat = diffMinutes > THRESHOLD_RELATIVE_MINUTES;
  const renderTime = () => {
    if (isBehindRefreshBoundary) {
      if (useAbsoluteFormat) {
        return t2("Due since {{ dueSince }}", {
          dueSince: t2("timestamp/ReminderNotification", {
            timestamp: reminder.remindAt
          })
        });
      }
      return t2("Due since {{ dueSince }}", {
        dueSince: t2("duration/Message reminder", {
          milliseconds: diffMs
        })
      });
    }
    if (useAbsoluteFormat) {
      return t2("timestamp/ReminderNotification", {
        timestamp: reminder.remindAt
      });
    }
    return t2("Due {{ timeLeft }}", {
      timeLeft: t2("duration/Message reminder", {
        milliseconds: timeLeftMs
      })
    });
  };
  return (0, import_jsx_runtime4.jsxs)("p", { className: "str-chat__message-reminder", children: [
    (0, import_jsx_runtime4.jsx)(IconBell, {}),
    (0, import_jsx_runtime4.jsx)("span", { children: t2("Reminder set") }),
    (0, import_jsx_runtime4.jsx)("span", { children: " · " }),
    (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__message-reminder__time-left", children: renderTime() })
  ] });
}
var ReminderNotification = ({ reminder }) => {
  if (!reminder) return null;
  if (!reminder.remindAt) {
    return (0, import_jsx_runtime4.jsx)(SavedForLaterContent, {});
  }
  return (0, import_jsx_runtime4.jsx)(RemindMeContent, { reminder });
};
var StreamedMessageText = (props) => {
  const {
    message: messageFromProps,
    renderingLetterCount,
    renderText: renderText2,
    streamingLetterIntervalMs
  } = props;
  const { message: messageFromContext } = useMessageContext();
  const { channel } = useChannelStateContext();
  const message = messageFromProps || messageFromContext;
  const { text: text22 = "" } = message;
  const { skipAnimation, streamedMessageText } = useMessageTextStreaming({
    renderingLetterCount,
    streamingLetterIntervalMs,
    text: text22
  });
  (0, import_react29.useEffect)(() => {
    channel?.on("ai_indicator.stop", () => {
      skipAnimation();
    });
  }, [channel, skipAnimation]);
  return (0, import_jsx_runtime4.jsx)(
    MessageText,
    {
      message: { ...message, text: streamedMessageText },
      renderText: renderText2
    }
  );
};
var DropdownContext = import_react29.default.createContext({
  close: () => null
});
var DropdownContextProvider = ({
  children,
  ...props
}) => (0, import_jsx_runtime4.jsx)(DropdownContext.Provider, { value: props, children });
var useDropdownContext = () => import_react29.default.useContext(DropdownContext);
var Dropdown = ({
  children,
  className,
  onClose,
  onOpen,
  placement = "bottom",
  referenceElement,
  TriggerComponent,
  triggerProps
}) => {
  const [mountedReferenceElement, setMountedReferenceElement] = (0, import_react29.useState)(null);
  const resolvedReferenceElement = TriggerComponent ? mountedReferenceElement : referenceElement ?? null;
  const [isOpen, setIsOpen] = (0, import_react29.useState)(!TriggerComponent);
  const [floatingElement, setFloatingElement] = (0, import_react29.useState)(null);
  const { refs, strategy, update, x: x2, y: y2 } = usePopoverPosition({
    placement
  });
  (0, import_react29.useEffect)(() => {
    if (!TriggerComponent) return;
    setIsOpen(false);
  }, [TriggerComponent]);
  const close = (0, import_react29.useCallback)(() => {
    setIsOpen(false);
    onClose?.();
  }, [onClose]);
  const open = (0, import_react29.useCallback)(() => {
    setIsOpen(true);
    onOpen?.();
  }, [onOpen]);
  const toggle = (0, import_react29.useCallback)(() => {
    if (isOpen) {
      close();
      return;
    }
    open();
  }, [close, isOpen, open]);
  (0, import_react29.useEffect)(() => {
    refs.setReference(resolvedReferenceElement);
  }, [refs, resolvedReferenceElement]);
  (0, import_react29.useEffect)(() => {
    refs.setFloating(floatingElement);
  }, [floatingElement, refs]);
  (0, import_react29.useEffect)(() => {
    if (!isOpen || !floatingElement || !resolvedReferenceElement) return;
    update?.();
  }, [floatingElement, isOpen, resolvedReferenceElement, update]);
  (0, import_react29.useEffect)(() => {
    if (!isOpen) return;
    const handleClickOutside = (event) => {
      if (!(event.target instanceof Node)) return;
      if (floatingElement?.contains(event.target)) return;
      if (resolvedReferenceElement?.contains(event.target)) return;
      close();
    };
    const handleEscape = (event) => {
      if (event.key !== "Escape") return;
      close();
    };
    document.addEventListener("mousedown", handleClickOutside);
    document.addEventListener("keyup", handleEscape);
    return () => {
      document.removeEventListener("mousedown", handleClickOutside);
      document.removeEventListener("keyup", handleEscape);
    };
  }, [close, floatingElement, isOpen, resolvedReferenceElement]);
  const DropdownTriggerComponent = TriggerComponent;
  const trigger = DropdownTriggerComponent ? (0, import_jsx_runtime4.jsx)(
    DropdownTriggerComponent,
    {
      "aria-expanded": isOpen,
      onClick: toggle,
      referenceRef: setMountedReferenceElement,
      ...triggerProps
    }
  ) : null;
  const content3 = isOpen && resolvedReferenceElement ? (0, import_jsx_runtime4.jsx)($535772f9d2c1f38d$export$20e40289641fbbb6, { autoFocus: true, restoreFocus: true, children: (0, import_jsx_runtime4.jsx)(DropdownContextProvider, { close, children: (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default("str-chat__dropdown__items", className),
      onClick: close,
      ref: setFloatingElement,
      style: {
        left: x2 ?? 0,
        position: strategy,
        top: y2 ?? 0
      },
      children
    }
  ) }) }) : null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__dropdown", children: [
    trigger,
    content3
  ] });
};
var SwitchField = ({
  children,
  description,
  fieldClassName,
  title,
  ...props
}) => {
  const inputRef = (0, import_react29.useRef)(null);
  const handleSwitchKeyDown = (e) => {
    if (![" ", "Enter"].includes(e.key) || !inputRef.current) return;
    e.preventDefault();
    inputRef.current.click();
  };
  const id3 = props.id;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default(
        "str-chat__form__switch-field",
        fieldClassName,
        props.disabled && "str-chat__form__switch-field--disabled"
      ),
      children: [
        (0, import_jsx_runtime4.jsx)(
          "input",
          {
            "aria-hidden": true,
            className: "str-chat__form__switch-field__input",
            id: id3,
            type: "checkbox",
            ...props,
            ref: inputRef
          }
        ),
        title ? (0, import_jsx_runtime4.jsx)(SwitchFieldLabel, { description, htmlFor: id3, title }) : children,
        (0, import_jsx_runtime4.jsx)(
          Switch,
          {
            on: props.checked,
            onClick: () => inputRef.current?.click(),
            onKeyDown: handleSwitchKeyDown
          }
        )
      ]
    }
  );
};
var Switch = ({ on: on2, ...props }) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    ...props,
    "aria-checked": on2,
    className: clsx_default("str-chat__form__switch-field__switch", {
      "str-chat__form__switch-field__switch--on": on2
    }),
    role: "button",
    tabIndex: 0,
    children: (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__form__switch-field__switch-handle" })
  }
);
var SwitchFieldLabel = ({
  asError,
  children,
  className,
  description,
  title,
  ...props
}) => (0, import_jsx_runtime4.jsx)(
  "label",
  {
    className: clsx_default(
      "str-chat__form__switch-field__label",
      { "str-chat__form__switch-field__label--as-error": asError },
      className
    ),
    ...props,
    children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__form__switch-field__label__content", children: title ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
      (0, import_jsx_runtime4.jsx)(SwitchFieldTitle, { children: title }),
      description != null && description !== "" && (0, import_jsx_runtime4.jsx)(SwitchFieldDescription, { children: description })
    ] }) : children })
  }
);
var SwitchFieldTitle = ({
  children,
  className,
  title,
  ...props
}) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    className: clsx_default("str-chat__form__switch-field__label__text", className),
    ...props,
    children: children ?? title
  }
);
var SwitchFieldDescription = ({
  children,
  className,
  description,
  ...props
}) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    className: clsx_default("str-chat__form__switch-field__label__description", className),
    ...props,
    children: children ?? description
  }
);
var MIN_LIVE_LOCATION_SHARE_DURATION = 60 * 1e3;
var DEFAULT_SHARE_LOCATION_DURATIONS = [
  15 * 60 * 1e3,
  // 15 minutes
  60 * 60 * 1e3,
  // 1 hour
  8 * 60 * 60 * 1e3
  // 8 hours
];
var DefaultGeolocationMap = () => null;
var LiveLocationDurationTrigger = ({
  children,
  onClick,
  referenceRef,
  ...props
}) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    ...props,
    className: "str-chat__live-location-sharing-duration-selector__button",
    onClick,
    ref: referenceRef,
    type: "button",
    children
  }
);
var ShareLocationDialog = ({
  close,
  GeolocationMap = DefaultGeolocationMap,
  shareDurations = DEFAULT_SHARE_LOCATION_DURATIONS
}) => {
  const { addNotification } = useNotificationApi();
  const { t: t2 } = useTranslationContext();
  const messageComposer = useMessageComposerController();
  const [durations, setDurations] = (0, import_react29.useState)([]);
  const [selectedDuration, setSelectedDuration] = (0, import_react29.useState)(void 0);
  const [geolocationPosition, setGeolocationPosition] = (0, import_react29.useState)(null);
  const [loadingLocation, setLoadingLocation] = (0, import_react29.useState)(false);
  const [geolocationPositionError, setGeolocationPositionError] = (0, import_react29.useState)(void 0);
  const validShareDurations = (0, import_react29.useMemo)(
    () => shareDurations.filter((d2) => d2 >= MIN_LIVE_LOCATION_SHARE_DURATION),
    [shareDurations]
  );
  const selectedDurationLabel = (0, import_react29.useMemo)(
    () => durations.length > 0 ? t2("duration/Share Location", {
      milliseconds: selectedDuration ?? durations[0]
    }) : void 0,
    [durations, selectedDuration, t2]
  );
  const durationTriggerProps = (0, import_react29.useMemo)(
    () => ({
      children: selectedDurationLabel ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)("span", { children: selectedDurationLabel }),
        (0, import_jsx_runtime4.jsx)(IconChevronDown, {})
      ] }) : null
    }),
    [selectedDurationLabel]
  );
  const getPosition = (0, import_react29.useCallback)(
    () => new Promise((resolve, reject) => {
      navigator.geolocation.getCurrentPosition(
        (position3) => {
          resolve(position3);
        },
        (positionError) => {
          console.warn(positionError);
          reject(positionError);
        },
        { timeout: 1e3 }
      );
    }),
    []
  );
  const setupPositionWatching = (0, import_react29.useCallback)(() => {
    setLoadingLocation(true);
    const watch = navigator.geolocation.watchPosition(
      (position3) => {
        setGeolocationPosition(position3);
        setLoadingLocation(false);
        setGeolocationPositionError(void 0);
      },
      (error2) => {
        setGeolocationPosition(null);
        setLoadingLocation(false);
        setGeolocationPositionError(error2);
      },
      { timeout: 1e3 }
    );
    return () => {
      navigator.geolocation.clearWatch(watch);
    };
  }, []);
  (0, import_react29.useEffect)(() => setupPositionWatching(), [setupPositionWatching]);
  const liveLocationSwitchEnabled = durations.length > 0;
  return (0, import_jsx_runtime4.jsxs)(
    Prompt.Root,
    {
      className: "str-chat__share-location-dialog",
      "data-testid": "share-location-dialog",
      children: [
        (0, import_jsx_runtime4.jsx)(Prompt.Header, { close, title: t2("Share Location") }),
        (0, import_jsx_runtime4.jsxs)(Prompt.Body, { children: [
          (0, import_jsx_runtime4.jsx)(
            GeolocationMap,
            {
              geolocationPositionError,
              latitude: geolocationPosition?.coords.latitude,
              loadingLocation,
              longitude: geolocationPosition?.coords.longitude,
              restartLocationWatching: setupPositionWatching
            }
          ),
          validShareDurations.length > 0 && (0, import_jsx_runtime4.jsxs)(
            "div",
            {
              className: clsx_default("str-chat__live-location-activation", {
                "str-chat__live-location-activation--expanded": liveLocationSwitchEnabled
              }),
              children: [
                (0, import_jsx_runtime4.jsx)(
                  SwitchField,
                  {
                    checked: liveLocationSwitchEnabled,
                    "data-testid": "share-location-dialog-live-location-switch",
                    disabled: !geolocationPosition,
                    onChange: (e) => {
                      e.stopPropagation();
                      if (liveLocationSwitchEnabled) {
                        setDurations([]);
                        setSelectedDuration(void 0);
                      } else {
                        setDurations(validShareDurations);
                        setSelectedDuration(validShareDurations[0]);
                      }
                    },
                    title: t2("Share live location for")
                  }
                ),
                liveLocationSwitchEnabled && selectedDurationLabel && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__live-location-sharing-duration-selector", children: (0, import_jsx_runtime4.jsx)(
                  Dropdown,
                  {
                    placement: "bottom-start",
                    TriggerComponent: LiveLocationDurationTrigger,
                    triggerProps: durationTriggerProps,
                    children: (0, import_jsx_runtime4.jsx)(
                      DurationDropdownItems,
                      {
                        durations,
                        selectDuration: setSelectedDuration
                      }
                    )
                  }
                ) })
              ]
            }
          )
        ] }),
        (0, import_jsx_runtime4.jsx)(Prompt.Footer, { children: (0, import_jsx_runtime4.jsxs)(Prompt.FooterControls, { children: [
          (0, import_jsx_runtime4.jsx)(
            Prompt.FooterControlsButtonSecondary,
            {
              className: "str-chat__prompt__footer__controls-button--cancel",
              onClick: () => {
                messageComposer.locationComposer.initState();
                close();
              },
              children: t2("Cancel")
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            Prompt.FooterControlsButtonSecondary,
            {
              className: "str-chat__prompt__footer__controls-button--submit",
              disabled: !geolocationPosition,
              onClick: async () => {
                let coords = geolocationPosition && {
                  latitude: geolocationPosition.coords.latitude,
                  longitude: geolocationPosition.coords.longitude
                };
                if (!coords) {
                  coords = (await getPosition()).coords;
                }
                messageComposer.locationComposer.setData({
                  ...coords,
                  durationMs: selectedDuration
                });
                close();
              },
              type: "submit",
              children: t2("Attach")
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            Prompt.FooterControlsButtonPrimary,
            {
              className: "str-chat__prompt__footer__controls-button--submit",
              disabled: !geolocationPosition,
              onClick: async () => {
                let coords = geolocationPosition && {
                  latitude: geolocationPosition.coords.latitude,
                  longitude: geolocationPosition.coords.longitude
                };
                if (!coords) {
                  try {
                    coords = (await getPosition()).coords;
                  } catch (e) {
                    addNotification({
                      emitter: "ShareLocationDialog",
                      error: e instanceof Error ? e : void 0,
                      message: t2("Failed to retrieve location"),
                      severity: "error",
                      type: "browser:location:get:failed"
                    });
                    return;
                  }
                }
                messageComposer.locationComposer.setData({
                  ...coords,
                  durationMs: selectedDuration
                });
                try {
                  await messageComposer.sendLocation();
                } catch (err) {
                  addNotification({
                    emitter: "ShareLocationDialog",
                    error: err instanceof Error ? err : void 0,
                    message: t2("Failed to share location"),
                    severity: "error",
                    type: "api:location:share:failed"
                  });
                  return;
                }
                close();
              },
              type: "submit",
              children: t2("Share")
            }
          )
        ] }) })
      ]
    }
  );
};
var DurationDropdownItems = ({
  durations,
  selectDuration
}) => {
  const { t: t2 } = useTranslationContext();
  const { close } = useDropdownContext();
  return (0, import_jsx_runtime4.jsx)(ContextMenuRoot, { children: (0, import_jsx_runtime4.jsx)(ContextMenuBody, { children: durations.map((duration2) => (0, import_jsx_runtime4.jsx)(
    ContextMenuButton,
    {
      className: "str-chat__live-location-sharing-duration-option",
      onClick: () => {
        selectDuration(duration2);
        close();
      },
      role: "option",
      children: t2("duration/Share Location", { milliseconds: duration2 })
    },
    `duration-${duration2}`
  )) }) });
};
var pollStateSelector$a = (nextValue) => ({
  enforce_unique_vote: nextValue.enforce_unique_vote,
  is_closed: nextValue.is_closed,
  max_votes_allowed: nextValue.max_votes_allowed,
  name: nextValue.name,
  options: nextValue.options
});
var PollHeader = () => {
  const { t: t2 } = useTranslationContext("PollHeader");
  const { poll } = usePollContext();
  const { enforce_unique_vote, is_closed, max_votes_allowed, name: name2, options } = useStateStore(poll.state, pollStateSelector$a);
  const selectionInstructions = (0, import_react29.useMemo)(() => {
    if (is_closed) return t2("Vote ended");
    if (enforce_unique_vote || options.length === 1) return t2("Select one");
    if (max_votes_allowed)
      return t2("Select up to {{count}}", {
        count: max_votes_allowed > options.length ? options.length : max_votes_allowed
      });
    if (options.length > 1) return t2("Select one or more");
    return "";
  }, [is_closed, enforce_unique_vote, max_votes_allowed, options.length, t2]);
  if (!name2) return;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-header", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-title", children: name2 }),
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-subtitle", children: selectionInstructions })
  ] });
};
var FieldError = ({ className, text: text22, ...props }) => (0, import_jsx_runtime4.jsx)("div", { ...props, className: clsx_default("str-chat__form-field-error", className), children: text22 });
var useStableId = (key) => {
  const id3 = (0, import_react29.useMemo)(() => nanoid(), [key]);
  return id3;
};
var parseNumeric = (s2) => {
  const trimmed = s2.trim();
  if (trimmed === "") return null;
  const n2 = Number(trimmed);
  return Number.isFinite(n2) ? n2 : null;
};
var clamp2 = (n2, min2, max2) => Math.min(Math.max(n2, min2), max2);
var NumericInput = (0, import_react29.forwardRef)(
  function NumericInput2({
    className,
    disabled = false,
    id: idProp,
    label,
    max: max2,
    min: min2,
    onChange,
    step = 1,
    value,
    ...inputProps
  }, ref) {
    const generatedId = useStableId();
    const id3 = idProp ?? generatedId;
    const num = parseNumeric(value);
    const minDef = min2 ?? -Infinity;
    const maxDef = max2 ?? Infinity;
    const atMin = num !== null && num <= minDef;
    const atMax = num !== null && num >= maxDef;
    const handleInputChange = (0, import_react29.useCallback)(
      (e) => {
        const next = e.target.value;
        if (next === "" || /^\d+$/.test(next)) {
          onChange(e);
        }
      },
      [onChange]
    );
    const createChangeEvent = (0, import_react29.useCallback)(
      (newValue) => ({
        currentTarget: { value: newValue },
        target: { value: newValue }
      }),
      []
    );
    const handleDecrement = (0, import_react29.useCallback)(() => {
      if (disabled || atMin) return;
      const next = num !== null ? clamp2(num - step, minDef, maxDef) : minDef;
      onChange(createChangeEvent(String(next)));
    }, [disabled, atMin, num, step, minDef, maxDef, onChange, createChangeEvent]);
    const handleIncrement = (0, import_react29.useCallback)(() => {
      if (disabled || atMax) return;
      const next = num !== null ? clamp2(num + step, minDef, maxDef) : minDef;
      onChange(createChangeEvent(String(next)));
    }, [disabled, atMax, num, step, minDef, maxDef, onChange, createChangeEvent]);
    const handleKeyDown = (0, import_react29.useCallback)(
      (e) => {
        if (e.key === "ArrowDown") {
          e.preventDefault();
          handleDecrement();
        } else if (e.key === "ArrowUp") {
          e.preventDefault();
          handleIncrement();
        }
      },
      [handleDecrement, handleIncrement]
    );
    return (0, import_jsx_runtime4.jsxs)(
      "div",
      {
        className: clsx_default(
          "str-chat__form-numeric-input",
          disabled && "str-chat__form-numeric-input--disabled",
          className
        ),
        children: [
          !!label && (0, import_jsx_runtime4.jsx)("label", { className: "str-chat__form-numeric-input__label", htmlFor: id3, children: label }),
          (0, import_jsx_runtime4.jsxs)("div", { className: clsx_default("str-chat__form-numeric-input__wrapper"), children: [
            (0, import_jsx_runtime4.jsx)(
              Button,
              {
                appearance: "outline",
                "aria-label": "Decrease value",
                circular: true,
                className: clsx_default(
                  "str-chat__form-numeric-input__stepper str-chat__form-numeric-input__stepper--decrement"
                ),
                disabled: disabled || atMin,
                onClick: handleDecrement,
                size: "xs",
                variant: "secondary",
                children: (0, import_jsx_runtime4.jsx)(IconMinus, { className: "str-chat__form-numeric-input__stepper-icon" })
              }
            ),
            (0, import_jsx_runtime4.jsx)(
              "input",
              {
                className: "str-chat__form-numeric-input__input",
                disabled,
                id: id3,
                inputMode: "numeric",
                onChange: handleInputChange,
                onKeyDown: handleKeyDown,
                ref,
                type: "text",
                value,
                ...inputProps
              }
            ),
            (0, import_jsx_runtime4.jsx)(
              Button,
              {
                appearance: "outline",
                "aria-label": "Increase value",
                circular: true,
                className: clsx_default(
                  "str-chat__form-numeric-input__stepper str-chat__form-numeric-input__stepper--increment"
                ),
                disabled: disabled || atMax,
                onClick: handleIncrement,
                size: "xs",
                variant: "secondary",
                children: (0, import_jsx_runtime4.jsx)(IconPlusSmall, { className: "str-chat__form-numeric-input__stepper-icon" })
              }
            )
          ] })
        ]
      }
    );
  }
);
var TextInputIconMessageLine = ({ icon, text: text22 }) => (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
  (0, import_jsx_runtime4.jsx)("span", { "aria-hidden": true, className: "str-chat__form-text-input__message-icon", children: icon }),
  (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__form-text-input__message-text", children: text22 })
] });
var TextInputFieldMessage = (props) => {
  if (props.kind === "neutral") {
    return (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: clsx_default(
          "str-chat__form-text-input__message",
          props.insidePlacement && "str-chat__form-text-input__message--field-message-inside"
        ),
        id: props.id,
        children: props.text
      }
    );
  } else if (props.kind === "success") {
    return (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: clsx_default(
          "str-chat__form-text-input__message",
          "str-chat__form-text-input__message--success",
          props.insidePlacement && "str-chat__form-text-input__message--field-message-inside"
        ),
        id: props.id,
        children: (0, import_jsx_runtime4.jsx)(
          TextInputIconMessageLine,
          {
            icon: props.successMessageIcon ?? (0, import_jsx_runtime4.jsx)(IconCheckmark, {}),
            text: props.text
          }
        )
      }
    );
  } else if (props.kind === "error") {
    return (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: clsx_default(
          "str-chat__form-text-input__message",
          "str-chat__form-field-error",
          props.insidePlacement && "str-chat__form-text-input__message--field-message-inside"
        ),
        id: props.id,
        role: "alert",
        children: (0, import_jsx_runtime4.jsx)(
          TextInputIconMessageLine,
          {
            icon: props.errorMessageIcon ?? (0, import_jsx_runtime4.jsx)(IconExclamationMark, {}),
            text: props.text
          }
        )
      }
    );
  }
  return null;
};
var TextInput = (0, import_react29.forwardRef)(function TextInput2({
  className,
  disabled,
  error: error2 = false,
  errorMessage,
  errorMessageIcon,
  fieldMessagePlacement = "outside",
  id: idProp,
  label,
  leading,
  message,
  successMessage,
  successMessageIcon,
  trailing,
  trailingText,
  variant = "outline",
  ...inputProps
}, ref) {
  const autoId = useStableId();
  const id3 = idProp ?? autoId;
  const hasError = error2 && (errorMessage != null || message != null);
  const showSuccess = !hasError && successMessage != null;
  const showNeutral = !hasError && !showSuccess && message != null;
  const hasFeedback = hasError || showSuccess || showNeutral;
  const messageInside = fieldMessagePlacement === "inside" && hasFeedback;
  const messageId = hasError ? `${id3}-field-error` : showSuccess || showNeutral ? `${id3}-message` : void 0;
  const describedBy = messageId;
  const fieldMessage = hasError ? (0, import_jsx_runtime4.jsx)(
    TextInputFieldMessage,
    {
      errorMessageIcon,
      id: messageId,
      insidePlacement: messageInside,
      kind: "error",
      text: errorMessage ?? message
    }
  ) : showSuccess ? (0, import_jsx_runtime4.jsx)(
    TextInputFieldMessage,
    {
      id: messageId,
      insidePlacement: messageInside,
      kind: "success",
      successMessageIcon,
      text: successMessage
    }
  ) : showNeutral ? (0, import_jsx_runtime4.jsx)(
    TextInputFieldMessage,
    {
      id: messageId,
      insidePlacement: messageInside,
      kind: "neutral",
      text: message
    }
  ) : null;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default(
        "str-chat__form-text-input",
        error2 && "str-chat__form-text-input--error",
        showSuccess && "str-chat__form-text-input--success",
        disabled && "str-chat__form-text-input--disabled",
        messageInside && "str-chat__form-text-input--field-message-inside",
        className
      ),
      children: [
        label ? (0, import_jsx_runtime4.jsx)("label", { className: "str-chat__form-text-input__label", htmlFor: id3, children: label }) : null,
        (0, import_jsx_runtime4.jsxs)(
          "div",
          {
            className: clsx_default(
              "str-chat__form-text-input__wrapper",
              `str-chat__form-text-input__wrapper--${variant}`,
              messageInside && "str-chat__form-text-input__wrapper--field-message-inside"
            ),
            children: [
              (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__form-text-input__control-row", children: [
                leading ? (0, import_jsx_runtime4.jsx)("span", { "aria-hidden": true, className: "str-chat__form-text-input__leading", children: leading }) : null,
                (0, import_jsx_runtime4.jsx)(
                  "input",
                  {
                    "aria-describedby": describedBy,
                    "aria-invalid": error2,
                    className: "str-chat__form-text-input__input",
                    disabled,
                    id: id3,
                    ref,
                    ...inputProps
                  }
                ),
                trailingText != null ? (0, import_jsx_runtime4.jsx)("span", { "aria-hidden": true, className: "str-chat__form-text-input__suffix", children: trailingText }) : null,
                trailing ? (0, import_jsx_runtime4.jsx)("span", { "aria-hidden": true, className: "str-chat__form-text-input__trailing", children: trailing }) : null
              ] }),
              messageInside ? fieldMessage : null
            ]
          }
        ),
        messageInside ? null : fieldMessage
      ]
    }
  );
});
var DragAndDropContainer = ({
  children,
  className,
  draggable,
  onSetNewOrder
}) => {
  const [order2, setOrder] = (0, import_react29.useState)([]);
  const [dragStartIndex, setDragStartIndex] = (0, import_react29.useState)(null);
  const [dragOverIndex, setDragOverIndex] = (0, import_react29.useState)(null);
  const [container, setContainer] = (0, import_react29.useState)(null);
  const moveDirection = dragStartIndex === null || dragOverIndex === null ? void 0 : dragStartIndex <= dragOverIndex ? "down" : "up";
  const childrenArray = import_react29.default.Children.toArray(children);
  (0, import_react29.useEffect)(() => {
    setOrder(import_react29.default.Children.map(children, (_2, index6) => index6) || []);
  }, [children]);
  (0, import_react29.useEffect)(() => {
    if (!container) return;
    const handleDragStart = (e) => {
      const target = e.target;
      const draggableItem = target.closest(".str-chat__drag-and-drop-container__item");
      if (e.dataTransfer) {
        e.dataTransfer.effectAllowed = "move";
      }
      if (draggableItem instanceof HTMLElement) {
        const index6 = Array.from(draggableItem.parentElement?.children || []).indexOf(
          draggableItem
        );
        setDragStartIndex(index6);
        e.dataTransfer?.setData("text/plain", index6.toString());
        draggableItem.style.opacity = "0.3";
      }
    };
    const handleDragOver = (e) => {
      e.preventDefault();
      const target = e.target;
      const draggableItem = target.closest(".str-chat__drag-and-drop-container__item");
      if (draggableItem instanceof HTMLElement) {
        const index6 = Array.from(draggableItem.parentElement?.children || []).indexOf(
          draggableItem
        );
        setDragOverIndex(index6);
      }
    };
    const handleDragLeave = () => {
      setDragOverIndex(null);
    };
    const handleDrop = (e) => {
      e.preventDefault();
      const draggedIndex = parseInt(e.dataTransfer?.getData("text/plain") || "-1", 10);
      const target = e.target;
      const draggableItem = target.closest(".str-chat__drag-and-drop-container__item");
      if (draggableItem instanceof HTMLElement) {
        const dropIndex = Array.from(draggableItem.parentElement?.children || []).indexOf(
          draggableItem
        );
        if (draggedIndex !== -1 && draggedIndex !== dropIndex) {
          setOrder((prevOrder) => {
            const newOrder = [...prevOrder];
            const [removed] = newOrder.splice(draggedIndex, 1);
            newOrder.splice(dropIndex, 0, removed);
            onSetNewOrder?.(newOrder);
            return newOrder;
          });
        }
      }
      setDragStartIndex(null);
      setDragOverIndex(null);
    };
    const handleDragEnd = (e) => {
      const target = e.target;
      if (target instanceof HTMLElement) {
        target.style.opacity = "";
      }
      setDragStartIndex(null);
      setDragOverIndex(null);
    };
    container.addEventListener("dragstart", handleDragStart);
    container.addEventListener("dragover", handleDragOver);
    container.addEventListener("dragleave", handleDragLeave);
    container.addEventListener("drop", handleDrop);
    container.addEventListener("dragend", handleDragEnd);
    return () => {
      container.removeEventListener("dragstart", handleDragStart);
      container.removeEventListener("dragover", handleDragOver);
      container.removeEventListener("dragleave", handleDragLeave);
      container.removeEventListener("drop", handleDrop);
      container.removeEventListener("dragend", handleDragEnd);
    };
  }, [container, onSetNewOrder]);
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default("str-chat__drag-and-drop-container", className),
      ref: setContainer,
      children: order2.map((originalIndex, currentIndex) => {
        const child = childrenArray[originalIndex];
        return (0, import_jsx_runtime4.jsx)(
          "div",
          {
            className: clsx_default("str-chat__drag-and-drop-container__item", {
              "str-chat__drag-and-drop-container__item--dragged-over-from-bottom": moveDirection === "up" && dragOverIndex === currentIndex,
              "str-chat__drag-and-drop-container__item--dragged-over-from-top": moveDirection === "down" && dragOverIndex === currentIndex
            }),
            draggable,
            children: child
          },
          import_react29.default.isValidElement(child) ? child.key : `draggable-item-${originalIndex}`
        );
      })
    }
  );
};
var TextInputFieldSet = ({
  children,
  className,
  draggable,
  label,
  onSetNewOrder
}) => (0, import_jsx_runtime4.jsxs)("fieldset", { className: clsx_default("str-chat__form__input-fieldset", className), children: [
  (0, import_jsx_runtime4.jsx)("legend", { className: "str-chat__form__input-fieldset__label", children: label }),
  (0, import_jsx_runtime4.jsx)(
    DragAndDropContainer,
    {
      className: "str-chat__form__input-fieldset__values",
      draggable,
      onSetNewOrder,
      children
    }
  )
] });
function useFormState(options) {
  const { initialValue, onSubmit, validators = {} } = options;
  const [value, setValue] = (0, import_react29.useState)(initialValue);
  const [fieldErrors, setFieldErrors] = (0, import_react29.useState)({});
  const setFieldValue = (0, import_react29.useCallback)(
    (field, fieldValue) => {
      setValue((prev) => ({ ...prev, [field]: fieldValue }));
      const validator = validators[field];
      if (validator) {
        const err = validator(fieldValue);
        setFieldErrors((prev) => {
          const next = { ...prev };
          if (err) next[field] = err;
          else delete next[field];
          return next;
        });
      }
    },
    [validators]
  );
  const handleSubmit = (0, import_react29.useCallback)(
    (e) => {
      e?.preventDefault();
      const errors = {};
      for (const key of Object.keys(value)) {
        const validator = validators[key];
        if (validator) {
          const err = validator(value[key]);
          if (err) errors[key] = err;
        }
      }
      if (Object.keys(errors).length > 0) {
        setFieldErrors(errors);
        return;
      }
      setFieldErrors({});
      void onSubmit(value);
    },
    [value, validators, onSubmit]
  );
  return {
    fieldErrors,
    handleSubmit,
    setFieldValue,
    setValue,
    value
  };
}
var pollStateSelector$9 = (nextValue) => ({
  ownAnswer: nextValue.ownAnswer
});
var AddCommentPrompt = ({ messageId }) => {
  const { t: t2 } = useTranslationContext();
  const { close } = useModalContext();
  const { poll } = usePollContext();
  const { ownAnswer } = useStateStore(poll.state, pollStateSelector$9);
  const [input, setInput] = (0, import_react29.useState)(null);
  const initialComment = ownAnswer?.answer_text ?? "";
  const initialValue = (0, import_react29.useMemo)(() => ({ comment: initialComment }), [initialComment]);
  const validators = (0, import_react29.useMemo)(
    () => ({
      comment: (v2) => {
        const trimmed = typeof v2 === "string" ? v2.trim() : "";
        if (!trimmed) {
          return new Error(t2("This field cannot be empty or contain only spaces"));
        }
        return void 0;
      }
    }),
    [t2]
  );
  const onSubmit = (0, import_react29.useCallback)(
    async (formValue) => {
      await poll.addAnswer(formValue.comment, messageId);
      close();
    },
    [poll, messageId, close]
  );
  const { fieldErrors, handleSubmit, setFieldValue, value } = useFormState({
    initialValue,
    onSubmit,
    validators
  });
  (0, import_react29.useEffect)(() => {
    input?.focus();
  }, [input]);
  const title = ownAnswer ? t2("Update your comment") : t2("Add a comment");
  const submitDisabled = !value.comment?.trim() || value.comment === ownAnswer?.answer_text;
  return (0, import_jsx_runtime4.jsxs)(Prompt.Root, { className: "str-chat__modal__poll-add-comment", children: [
    title && (0, import_jsx_runtime4.jsx)(Prompt.Header, { close, title }),
    (0, import_jsx_runtime4.jsxs)("form", { autoComplete: "off", onSubmit: handleSubmit, children: [
      (0, import_jsx_runtime4.jsx)(Prompt.Body, { children: (0, import_jsx_runtime4.jsx)(
        TextInput,
        {
          "aria-label": title,
          error: !!fieldErrors.comment,
          errorMessage: fieldErrors.comment?.message,
          id: "comment",
          name: "comment",
          onChange: (e) => setFieldValue("comment", e.target.value),
          placeholder: t2("placeholder/PollComment"),
          ref: setInput,
          required: true,
          type: "text",
          value: value.comment
        }
      ) }),
      (0, import_jsx_runtime4.jsx)(Prompt.Footer, { children: (0, import_jsx_runtime4.jsxs)(Prompt.FooterControls, { children: [
        (0, import_jsx_runtime4.jsx)(
          Prompt.FooterControlsButtonSecondary,
          {
            className: "str-chat__prompt__footer__controls-button--cancel",
            onClick: close,
            type: "button",
            children: t2("Cancel")
          }
        ),
        (0, import_jsx_runtime4.jsx)(
          Prompt.FooterControlsButtonPrimary,
          {
            className: "str-chat__prompt__footer__controls-button--submit",
            disabled: Object.keys(fieldErrors).length > 0 || submitDisabled,
            type: "submit",
            children: initialComment ? t2("Update") : t2("Send")
          }
        )
      ] }) })
    ] })
  ] });
};
var EndPollAlert = () => {
  const { addNotification } = useNotificationApi();
  const { t: t2 } = useTranslationContext();
  const { poll } = usePollContext();
  const { close } = useModalContext();
  return (0, import_jsx_runtime4.jsxs)(Alert.Root, { className: "str-chat__end-poll-alert", children: [
    (0, import_jsx_runtime4.jsx)(
      Alert.Header,
      {
        description: t2(
          "Do you want to end this poll now? Nobody will be able to vote in this poll anymore."
        ),
        title: t2("End this poll?")
      }
    ),
    (0, import_jsx_runtime4.jsxs)(Alert.Actions, { children: [
      (0, import_jsx_runtime4.jsx)(
        Button,
        {
          appearance: "outline",
          className: "str-chat__end-poll-alert__end-vote-button",
          "data-testid": "end-poll-alert-end-vote-button",
          onClick: async () => {
            try {
              await poll.close();
              close();
              addNotification({
                emitter: "EndPollAlert",
                message: t2("Poll ended"),
                severity: "success",
                type: "api:poll:end:success"
              });
            } catch (e) {
              addNotification({
                emitter: "EndPollAlert",
                error: e instanceof Error ? e : void 0,
                message: t2("Failed to end the poll"),
                severity: "error",
                type: "api:poll:end:failed"
              });
            }
          },
          size: "md",
          variant: "danger",
          children: t2("End poll")
        }
      ),
      (0, import_jsx_runtime4.jsx)(
        Button,
        {
          appearance: "outline",
          className: "str-chat__end-poll-alert__cancel-button",
          "data-testid": "end-poll-alert-cancel-button",
          onClick: close,
          size: "md",
          variant: "secondary",
          children: t2("Cancel")
        }
      )
    ] })
  ] });
};
var PollAction = ({
  buttonText,
  children,
  closeModal,
  isAdditionalAction,
  modalClassName,
  modalIsOpen,
  openModal
}) => {
  const { Modal = GlobalModal } = useComponentContext();
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "outline",
        className: clsx_default("str-chat__poll-action", {
          "str-chat__poll-action--additional": isAdditionalAction
        }),
        onClick: openModal,
        size: "md",
        variant: "secondary",
        children: buttonText
      }
    ),
    (0, import_jsx_runtime4.jsx)(Modal, { className: modalClassName, onClose: closeModal, open: modalIsOpen, children })
  ] });
};
var pollStateSelector$8 = (nextValue) => ({
  options: nextValue.options
});
var SuggestPollOptionPrompt = () => {
  const { client } = useChatContext();
  const { t: t2 } = useTranslationContext();
  const { poll } = usePollContext();
  const { close } = useModalContext();
  const { options } = useStateStore(poll.state, pollStateSelector$8);
  const [input, setInput] = (0, import_react29.useState)(null);
  const initialValue = (0, import_react29.useMemo)(() => ({ optionText: "" }), []);
  const validators = (0, import_react29.useMemo)(
    () => ({
      optionText: (v2) => {
        const trimmed = typeof v2 === "string" ? v2.trim() : "";
        if (!trimmed) {
          return new Error(t2("This field cannot be empty or contain only spaces"));
        }
        const existingOption = options.find((option) => option.text === trimmed);
        if (existingOption) {
          return new Error(t2("Option already exists"));
        }
        return void 0;
      }
    }),
    [t2, options]
  );
  const onSubmit = (0, import_react29.useCallback)(
    async (formValue) => {
      await client.createPollOption(poll.id, {
        text: formValue.optionText
      });
      close();
    },
    [client, poll, close]
  );
  const { fieldErrors, handleSubmit, setFieldValue, value } = useFormState({
    initialValue,
    onSubmit,
    validators
  });
  (0, import_react29.useEffect)(() => {
    input?.focus();
  }, [input]);
  const submitDisabled = !value.optionText?.trim();
  return (0, import_jsx_runtime4.jsxs)(Prompt.Root, { className: "str-chat__modal__suggest-poll-option-prompt", children: [
    (0, import_jsx_runtime4.jsx)(Prompt.Header, { close, title: t2("Suggest an option") }),
    (0, import_jsx_runtime4.jsxs)("form", { autoComplete: "off", onSubmit: handleSubmit, children: [
      (0, import_jsx_runtime4.jsx)(Prompt.Body, { children: (0, import_jsx_runtime4.jsx)(
        TextInput,
        {
          "aria-label": t2("Suggest an option"),
          error: !!fieldErrors.optionText,
          errorMessage: fieldErrors.optionText?.message,
          id: "optionText",
          name: "optionText",
          onChange: (e) => setFieldValue("optionText", e.target.value),
          placeholder: t2("placeholder/PollOptionSuggestion"),
          ref: setInput,
          required: true,
          type: "text",
          value: value.optionText
        }
      ) }),
      (0, import_jsx_runtime4.jsx)(Prompt.Footer, { children: (0, import_jsx_runtime4.jsxs)(Prompt.FooterControls, { children: [
        (0, import_jsx_runtime4.jsx)(
          Prompt.FooterControlsButtonSecondary,
          {
            className: "str-chat__prompt__footer__controls-button--cancel",
            onClick: close,
            children: t2("Cancel")
          }
        ),
        (0, import_jsx_runtime4.jsx)(
          Prompt.FooterControlsButtonPrimary,
          {
            className: "str-chat__prompt__footer__controls-button--submit",
            disabled: Object.keys(fieldErrors).length > 0 || submitDisabled,
            type: "submit",
            children: t2("Send")
          }
        )
      ] }) })
    ] })
  ] });
};
var PollVoteTimestamp = ({ timestamp }) => {
  const { t: t2 } = useTranslationContext();
  const { handleEnter, handleLeave, tooltipVisible } = useEnterLeaveHandlers();
  const [referenceElement, setReferenceElement] = (0, import_react29.useState)(null);
  const timestampDate = new Date(timestamp);
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__poll-vote__timestamp",
      onMouseEnter: handleEnter,
      onMouseLeave: handleLeave,
      ref: setReferenceElement,
      children: [
        t2("timestamp/PollVote", { timestamp: timestampDate }),
        (0, import_jsx_runtime4.jsx)(
          PopperTooltip,
          {
            offset: [0, 5],
            placement: "bottom",
            referenceElement,
            visible: tooltipVisible,
            children: t2("timestamp/PollVoteTooltip", { timestamp: timestampDate })
          }
        )
      ]
    }
  );
};
var PollVoteAuthor = ({ vote }) => {
  const { t: t2 } = useTranslationContext();
  const { client } = useChatContext();
  const displayName = client.user?.id && client.user.id === vote.user?.id ? t2("You") : vote.user?.name || vote.user?.id || t2("Anonymous");
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-vote__author", children: [
    vote.user && (0, import_jsx_runtime4.jsx)(
      Avatar,
      {
        className: "str-chat__avatar--poll-vote-author",
        imageUrl: vote.user.image,
        size: "md",
        userName: vote.user.name
      },
      `poll-vote-${vote.id}-avatar-${vote.user.id}`
    ),
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-vote__author__name", children: displayName })
  ] });
};
var PollVote = ({ vote }) => (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-vote", children: [
  (0, import_jsx_runtime4.jsx)(PollVoteAuthor, { vote }),
  (0, import_jsx_runtime4.jsx)(PollVoteTimestamp, { timestamp: vote.created_at })
] });
var PollVoteListing = ({ votes }) => (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-vote-listing", children: votes.map((vote) => (0, import_jsx_runtime4.jsx)(PollVote, { vote }, `poll-vote-${vote.id}`)) });
function useManagePollVotesRealtime(managedVoteType, cursorPaginatorState, optionId) {
  const { client } = useChatContext();
  const [votesInRealtime, setVotesInRealtime] = (0, import_react29.useState)(
    cursorPaginatorState?.getLatestValue().items ?? []
  );
  (0, import_react29.useEffect)(
    () => cursorPaginatorState?.subscribeWithSelector(
      (state) => [state.latestPageItems],
      ([latestPageItems]) => setVotesInRealtime((prev) => [...prev, ...latestPageItems])
    ),
    [cursorPaginatorState]
  );
  (0, import_react29.useEffect)(() => {
    const handleVoteEvent = (event) => {
      if (!event.poll_vote) return;
      const isAnswer = isVoteAnswer(event.poll_vote);
      if (managedVoteType === "answer" && !isAnswer || managedVoteType === "vote" && (isAnswer || event.poll_vote.option_id !== optionId))
        return;
      if (event.type === "poll.vote_removed") {
        setVotesInRealtime(
          (prev) => event.poll_vote ? prev.filter((vote) => vote.id !== event.poll_vote.id) : prev
        );
      }
      if (event.type === "poll.vote_changed") {
        setVotesInRealtime(
          (prev) => event.poll_vote ? prev.filter((vote) => vote.id !== event.poll_vote.id) : prev
        );
      }
      if (["poll.vote_casted", "poll.vote_changed"].includes(event.type)) {
        setVotesInRealtime(
          (prev) => event.poll_vote ? [event.poll_vote, ...prev] : prev
        );
      }
    };
    const voteCastedSubscription = client.on("poll.vote_casted", handleVoteEvent);
    const voteRemovedSubscription = client.on("poll.vote_removed", handleVoteEvent);
    const voteChangedSubscription = client.on("poll.vote_changed", handleVoteEvent);
    return () => {
      voteCastedSubscription.unsubscribe();
      voteRemovedSubscription.unsubscribe();
      voteChangedSubscription.unsubscribe();
    };
  }, [client, optionId, managedVoteType]);
  return votesInRealtime;
}
var useCursorPaginator = (paginationFn, loadFirstPage) => {
  const cursorPaginatorState = (0, import_react29.useMemo)(
    () => new StateStore({
      hasNextPage: true,
      items: [],
      latestPageItems: [],
      loading: false
    }),
    []
  );
  const loadMore = (0, import_react29.useCallback)(async () => {
    const { loading, next: currentNext } = cursorPaginatorState.getLatestValue();
    if (currentNext === null || loading) return;
    cursorPaginatorState.partialNext({ loading: true });
    try {
      const { items, next } = await paginationFn(currentNext);
      cursorPaginatorState.next((prev) => ({
        ...prev,
        hasNextPage: !!next,
        items: (0, import_lodash4.default)(prev.items.concat(items), "id"),
        latestPageItems: items,
        next: next || null
      }));
    } catch (error2) {
      cursorPaginatorState.partialNext({ error: error2 });
    }
    cursorPaginatorState.partialNext({ loading: false });
  }, [cursorPaginatorState, paginationFn]);
  (0, import_react29.useEffect)(() => {
    const { items } = cursorPaginatorState.getLatestValue();
    if (items.length) return;
    loadMore();
  }, [cursorPaginatorState, loadFirstPage, loadMore]);
  return {
    cursorPaginatorState,
    loadMore
  };
};
var paginationStateSelector$1 = (state) => [
  state.error,
  state.hasNextPage,
  state.loading
];
var usePollAnswerPagination = ({
  paginationParams
} = {}) => {
  const { poll } = usePollContext();
  const paginationFn = (0, import_react29.useCallback)(
    async (next) => {
      const { next: newNext, votes } = await poll.queryAnswers({
        filter: paginationParams?.filter,
        options: !next ? paginationParams?.options : { ...paginationParams?.options, next },
        sort: { created_at: -1, ...paginationParams?.sort }
      });
      return { items: votes, next: newNext };
    },
    [paginationParams, poll]
  );
  const { cursorPaginatorState, loadMore } = useCursorPaginator(paginationFn, true);
  const answers = useManagePollVotesRealtime("answer", cursorPaginatorState);
  const [error2, hasNextPage, loading] = useStateStore(
    cursorPaginatorState,
    paginationStateSelector$1
  );
  return {
    answers,
    error: error2,
    hasNextPage,
    loading,
    loadMore
  };
};
var paginationStateSelector = (state) => [
  state.error,
  state.hasNextPage,
  state.loading
];
var usePollOptionVotesPagination = ({
  paginationParams
}) => {
  const { poll } = usePollContext();
  const paginationFn = (0, import_react29.useCallback)(
    async (next) => {
      const { next: newNext, votes: votes2 } = await poll.queryOptionVotes({
        filter: paginationParams.filter,
        options: !next ? paginationParams?.options : { ...paginationParams?.options, next },
        sort: { created_at: -1, ...paginationParams?.sort }
      });
      return { items: votes2, next: newNext };
    },
    [paginationParams, poll]
  );
  const { cursorPaginatorState, loadMore } = useCursorPaginator(paginationFn, true);
  const votes = useManagePollVotesRealtime(
    "vote",
    cursorPaginatorState,
    paginationParams.filter.option_id
  );
  const [error2, hasNextPage, loading] = useStateStore(
    cursorPaginatorState,
    paginationStateSelector
  );
  return {
    error: error2,
    hasNextPage,
    loading,
    loadMore,
    votes
  };
};
var mousewheelListener$1 = (event) => {
  if (event instanceof WheelEvent && event.deltaY === 1) {
    event.preventDefault();
  }
};
var InfiniteScrollPaginator = (props) => {
  const {
    children,
    className,
    listenToScroll,
    loadNextDebounceMs = 500,
    loadNextOnScrollToBottom,
    loadNextOnScrollToTop,
    threshold = DEFAULT_LOAD_PAGE_SCROLL_THRESHOLD,
    useCapture = false,
    ...componentProps
  } = props;
  const rootRef = (0, import_react29.useRef)(null);
  const childRef = (0, import_react29.useRef)(null);
  const scrollListener = (0, import_react29.useMemo)(
    () => (0, import_lodash5.default)(() => {
      const root4 = rootRef.current;
      const child = childRef.current;
      if (!root4 || root4.offsetParent === null || !child) {
        return;
      }
      const distanceFromBottom = child.scrollHeight - root4.scrollTop - root4.clientHeight;
      const distanceFromTop = root4.scrollTop;
      if (listenToScroll) {
        listenToScroll(distanceFromBottom, distanceFromTop, threshold);
      }
      if (distanceFromTop < Number(threshold)) {
        loadNextOnScrollToTop?.();
      }
      if (distanceFromBottom < Number(threshold)) {
        loadNextOnScrollToBottom?.();
      }
    }, loadNextDebounceMs),
    [
      listenToScroll,
      loadNextDebounceMs,
      loadNextOnScrollToBottom,
      loadNextOnScrollToTop,
      threshold
    ]
  );
  (0, import_react29.useEffect)(() => {
    const scrollElement = rootRef.current;
    if (!scrollElement) return;
    scrollElement.addEventListener("scroll", scrollListener, useCapture);
    return () => {
      scrollElement.removeEventListener("scroll", scrollListener, useCapture);
    };
  }, [scrollListener, useCapture]);
  (0, import_react29.useEffect)(() => {
    const root4 = rootRef.current;
    if (!root4 || typeof ResizeObserver === "undefined" || !scrollListener) return;
    const observer = new ResizeObserver(scrollListener);
    observer.observe(root4);
    return () => {
      observer.disconnect();
    };
  }, [scrollListener]);
  (0, import_react29.useEffect)(() => {
    const root4 = rootRef.current;
    if (root4) {
      root4.addEventListener("wheel", mousewheelListener$1, { passive: false });
    }
    return () => {
      if (root4) {
        root4.removeEventListener("wheel", mousewheelListener$1, useCapture);
      }
    };
  }, [useCapture]);
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      ...componentProps,
      className: clsx_default("str-chat__infinite-scroll-paginator", className),
      ref: rootRef,
      children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__infinite-scroll-paginator__content", ref: childRef, children })
    }
  );
};
var pollStateSelector$7 = (nextValue) => ({
  is_closed: nextValue.is_closed,
  ownAnswer: nextValue.ownAnswer
});
var PollAnswerList = ({ onUpdateOwnAnswerClick }) => {
  const { client } = useChatContext();
  const { t: t2 } = useTranslationContext();
  const { poll } = usePollContext();
  const { close } = useModalContext();
  const { is_closed } = useStateStore(poll.state, pollStateSelector$7);
  const { answers, error: error2, hasNextPage, loading, loadMore } = usePollAnswerPagination();
  return (0, import_jsx_runtime4.jsxs)(Viewer.Root, { className: "str-chat__modal__poll-answer-list", children: [
    (0, import_jsx_runtime4.jsx)(Viewer.Header, { close, title: t2("Poll comments") }),
    (0, import_jsx_runtime4.jsxs)(Viewer.Body, { className: "str-chat__modal__poll-answer-list__body", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-answer-list", children: (0, import_jsx_runtime4.jsxs)(InfiniteScrollPaginator, { loadNextOnScrollToBottom: loadMore, threshold: 40, children: [
        answers.map((answer) => (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-answer", children: [
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-answer__data", children: [
            answer.answer_text && (0, import_jsx_runtime4.jsx)("p", { className: "str-chat__poll-answer__text", children: answer.answer_text }),
            (0, import_jsx_runtime4.jsx)(PollVote, { vote: answer }, `poll-vote-${answer.id}`)
          ] }),
          !is_closed && answer.user?.id === client.user?.id && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-vote__update-vote-button-container", children: (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "ghost",
              className: "str-chat__poll-vote__update-vote-button",
              onClick: onUpdateOwnAnswerClick,
              size: "md",
              variant: "secondary",
              children: t2("Update your comment")
            }
          ) })
        ] }, `comment-${answer.id}`)),
        hasNextPage && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__loading-indicator-placeholder", children: loading && (0, import_jsx_runtime4.jsx)(LoadingIndicator, {}) })
      ] }) }),
      error2?.message && (0, import_jsx_runtime4.jsx)("div", { children: error2?.message })
    ] })
  ] });
};
var pollStateSelector$6 = (nextValue) => ({
  maxVotedOptionIds: nextValue.maxVotedOptionIds,
  vote_counts_by_option: nextValue.vote_counts_by_option
});
var PollResultOptionVoteCounter = ({
  optionId
}) => {
  const { t: t2 } = useTranslationContext();
  const { poll } = usePollContext();
  const { maxVotedOptionIds, vote_counts_by_option } = useStateStore(
    poll.state,
    pollStateSelector$6
  );
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-result-option-vote-counter", children: [
    maxVotedOptionIds.length === 1 && maxVotedOptionIds[0] === optionId && (0, import_jsx_runtime4.jsx)(IconTrophy, {}),
    (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__poll-result-option-vote-count", children: t2("{{count}} votes", { count: vote_counts_by_option[optionId] ?? 0 }) })
  ] });
};
var PollOptionWithVotesHeader = ({
  option,
  optionOrderNumber
}) => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-option__header", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-option__header__label", children: t2("Question {{ optionOrderNumber}}", { optionOrderNumber }) }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-option__header__title", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-option__option-text", children: option.text }),
      (0, import_jsx_runtime4.jsx)(PollResultOptionVoteCounter, { optionId: option.id })
    ] })
  ] });
};
var pollStateSelector$5 = (nextValue) => ({
  latest_votes_by_option: nextValue.latest_votes_by_option
});
var PollOptionWithVotes = ({
  countVotesPreview,
  option,
  orderNumber,
  showAllVotes
}) => {
  const { t: t2 } = useTranslationContext();
  const { channelCapabilities = {} } = useChannelStateContext("PollOptionWithVotes");
  const { poll } = usePollContext();
  const { latest_votes_by_option } = useStateStore(poll.state, pollStateSelector$5);
  const votes = latest_votes_by_option && latest_votes_by_option[option.id];
  const voteCount = votes?.length ?? 0;
  const isVotesPreview = typeof countVotesPreview === "number";
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__poll-option", {
        "str-chat__poll-option--has-more-votes": isVotesPreview && voteCount > countVotesPreview,
        "str-chat__poll-option--has-votes": voteCount
      }),
      children: [
        (0, import_jsx_runtime4.jsx)(PollOptionWithVotesHeader, { option, optionOrderNumber: orderNumber }),
        !!votes && (0, import_jsx_runtime4.jsx)(PollVoteListing, { votes: votes.slice(0, countVotesPreview) }),
        channelCapabilities["query-poll-votes"] && showAllVotes && isVotesPreview && votes?.length > countVotesPreview && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-option__show-all-votes-button-container", children: (0, import_jsx_runtime4.jsx)(
          Button,
          {
            appearance: "ghost",
            className: "str-chat__poll-option__show-all-votes-button",
            onClick: showAllVotes,
            size: "md",
            variant: "secondary",
            children: t2("View all")
          }
        ) })
      ]
    }
  );
};
var MAX_POLL_OPTIONS = 100;
var MAX_OPTIONS_DISPLAYED = 5;
var COUNT_OPTION_VOTES_PREVIEW = 5;
var PollQuestion = ({ question }) => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__modal__poll-question", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__modal__poll-question__label", children: t2("Question") }),
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__modal__poll-question__text", children: question })
  ] });
};
var PollOptionWithVotesList = ({
  option,
  optionOrderNumber
}) => {
  const paginationParams = (0, import_react29.useMemo)(
    () => ({ filter: { option_id: option.id } }),
    [option.id]
  );
  const { hasNextPage, loading, loadMore, votes } = usePollOptionVotesPagination({
    paginationParams
  });
  return (0, import_jsx_runtime4.jsx)(InfiniteScrollPaginator, { loadNextOnScrollToBottom: loadMore, threshold: 40, children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat_poll-option-with-votes-list", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-option", children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-option__votes-paginated-list", children: [
    (0, import_jsx_runtime4.jsx)(
      PollOptionWithVotesHeader,
      {
        option,
        optionOrderNumber
      }
    ),
    (0, import_jsx_runtime4.jsx)(PollVoteListing, { votes }),
    hasNextPage && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__loading-indicator-placeholder", children: loading && (0, import_jsx_runtime4.jsx)(LoadingIndicator, {}) })
  ] }) }) }) });
};
var pollStateSelector$4 = ({
  name: name2,
  options,
  vote_count,
  vote_counts_by_option
}) => ({
  name: name2,
  options: [...options],
  vote_count,
  vote_counts_by_option
});
var PollResults = () => {
  const { t: t2 } = useTranslationContext();
  const { poll } = usePollContext();
  const { close } = useModalContext();
  const { name: name2, options, vote_count, vote_counts_by_option } = useStateStore(
    poll.state,
    pollStateSelector$4
  );
  const [optionToView, setOptionToView] = (0, import_react29.useState)(null);
  const goBack = (0, import_react29.useCallback)(() => setOptionToView(null), []);
  return (0, import_jsx_runtime4.jsx)(
    Viewer.Root,
    {
      className: clsx_default("str-chat__modal__poll-results", {
        "str-chat__modal__poll-results--option-detail": optionToView
      }),
      children: optionToView?.option ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)(Viewer.Header, { close, goBack, title: t2("Votes") }),
        (0, import_jsx_runtime4.jsx)(Viewer.Body, { className: "str-chat__modal__poll-results__body", children: (0, import_jsx_runtime4.jsx)(
          PollOptionWithVotesList,
          {
            option: optionToView?.option,
            optionOrderNumber: optionToView?.optionOrderNumber
          }
        ) })
      ] }) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)(Viewer.Header, { close, title: t2("Poll results") }),
        (0, import_jsx_runtime4.jsxs)(Viewer.Body, { className: "str-chat__modal__poll-results__body", children: [
          (0, import_jsx_runtime4.jsx)(PollQuestion, { question: name2 }),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__modal__poll-results__options", children: [
            (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__modal__poll-results__option-list", children: options.sort(
              (next, current) => (vote_counts_by_option[current.id] ?? 0) >= (vote_counts_by_option[next.id] ?? 0) ? 1 : -1
            ).map((option, i2) => {
              const optionOrderNumber = i2 + 1;
              return (0, import_jsx_runtime4.jsx)(
                PollOptionWithVotes,
                {
                  countVotesPreview: COUNT_OPTION_VOTES_PREVIEW,
                  option,
                  orderNumber: optionOrderNumber,
                  showAllVotes: () => setOptionToView({ option, optionOrderNumber })
                },
                `poll-option-${option.id}`
              );
            }) }),
            (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__modal__poll-results__options__footer", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__modal__poll-results__options-total-count", children: t2("totalVoteCount", { count: vote_count }) }) })
          ] })
        ] })
      ] })
    }
  );
};
var COMMON_MODAL_CLASS = "str-chat__poll-action-modal";
var pollStateSelector$3 = (nextValue) => ({
  allow_answers: nextValue.allow_answers,
  allow_user_suggested_options: nextValue.allow_user_suggested_options,
  answers_count: nextValue.answers_count,
  created_by_id: nextValue.created_by_id,
  is_closed: nextValue.is_closed,
  options: nextValue.options,
  ownAnswer: nextValue.ownAnswer,
  voteCount: nextValue.vote_count
});
var PollActions = ({
  AddCommentPrompt: AddCommentPrompt$1 = AddCommentPrompt,
  EndPollAlert: EndPollAlert$1 = EndPollAlert,
  PollAnswerList: PollAnswerList$1 = PollAnswerList,
  PollResults: PollResults$1 = PollResults,
  SuggestPollOptionForm = SuggestPollOptionPrompt
}) => {
  const { client } = useChatContext();
  const { t: t2 } = useTranslationContext("PollActions");
  const { channelCapabilities = {} } = useChannelStateContext("PollActions");
  const { message } = useMessageContext();
  const { poll } = usePollContext();
  const {
    allow_answers,
    allow_user_suggested_options,
    answers_count,
    created_by_id,
    is_closed,
    options,
    ownAnswer,
    voteCount
  } = useStateStore(poll.state, pollStateSelector$3);
  const [modalOpen, setModalOpen] = (0, import_react29.useState)();
  const canCastVote = channelCapabilities["cast-poll-vote"] && !is_closed;
  const closeModal = (0, import_react29.useCallback)(() => setModalOpen(void 0), []);
  const onUpdateAnswerClick = (0, import_react29.useCallback)(() => setModalOpen("add-comment"), []);
  const hasContents = !is_closed && created_by_id === client.user?.id || !!voteCount || canCastVote && allow_user_suggested_options && options.length < MAX_POLL_OPTIONS || !is_closed && allow_answers || answers_count > 0 && channelCapabilities["query-poll-votes"];
  if (!hasContents) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-actions", children: [
    !is_closed && created_by_id === client.user?.id && (0, import_jsx_runtime4.jsx)(
      PollAction,
      {
        buttonText: t2("End poll"),
        closeModal,
        modalClassName: clsx_default(COMMON_MODAL_CLASS, "str-chat__end-poll-modal"),
        modalIsOpen: modalOpen === "end-vote",
        openModal: () => setModalOpen("end-vote"),
        children: (0, import_jsx_runtime4.jsx)(EndPollAlert$1, {})
      }
    ),
    !!voteCount && (0, import_jsx_runtime4.jsx)(
      PollAction,
      {
        buttonText: t2("View results"),
        closeModal,
        modalClassName: clsx_default(COMMON_MODAL_CLASS, "str-chat__poll-results-modal"),
        modalIsOpen: modalOpen === "view-results",
        openModal: () => setModalOpen("view-results"),
        children: (0, import_jsx_runtime4.jsx)(PollResults$1, {})
      }
    ),
    canCastVote && allow_user_suggested_options && options.length < MAX_POLL_OPTIONS && (0, import_jsx_runtime4.jsx)(
      PollAction,
      {
        buttonText: t2("Suggest an option"),
        closeModal,
        isAdditionalAction: true,
        modalClassName: clsx_default(
          COMMON_MODAL_CLASS,
          "str-chat__suggest-poll-option-modal"
        ),
        modalIsOpen: modalOpen === "suggest-option",
        openModal: () => setModalOpen("suggest-option"),
        children: (0, import_jsx_runtime4.jsx)(SuggestPollOptionForm, {})
      }
    ),
    !is_closed && allow_answers && (0, import_jsx_runtime4.jsx)(
      PollAction,
      {
        buttonText: ownAnswer ? t2("Update your comment") : t2("Add a comment"),
        closeModal,
        isAdditionalAction: true,
        modalClassName: clsx_default(COMMON_MODAL_CLASS, "str-chat__add-poll-answer-modal"),
        modalIsOpen: modalOpen === "add-comment",
        openModal: () => setModalOpen("add-comment"),
        children: (0, import_jsx_runtime4.jsx)(AddCommentPrompt$1, { messageId: message.id })
      }
    ),
    answers_count > 0 && channelCapabilities["query-poll-votes"] && (0, import_jsx_runtime4.jsx)(
      PollAction,
      {
        buttonText: t2("View {{count}} comments", { count: answers_count }),
        closeModal,
        isAdditionalAction: true,
        modalClassName: clsx_default(COMMON_MODAL_CLASS, "str-chat__poll-answer-list-modal"),
        modalIsOpen: modalOpen === "view-comments",
        openModal: () => setModalOpen("view-comments"),
        children: (0, import_jsx_runtime4.jsx)(PollAnswerList$1, { onUpdateOwnAnswerClick: onUpdateAnswerClick })
      }
    )
  ] });
};
var AmountBar = ({ amount, className }) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    className: clsx_default("str-chat__amount-bar", className),
    "data-testid": "amount-bar",
    role: "progressbar",
    style: {
      "--str-chat__amount-bar-fulfillment": amount + "%"
    }
  }
);
var Checkmark = ({ checked }) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    className: clsx_default("str-chat__checkmark", { "str-chat__checkmark--checked": checked })
  }
);
var pollStateSelector$2 = (nextValue) => ({
  is_closed: nextValue.is_closed,
  latest_votes_by_option: nextValue.latest_votes_by_option,
  maxVotedOptionIds: nextValue.maxVotedOptionIds,
  ownVotesByOptionId: nextValue.ownVotesByOptionId,
  vote_counts_by_option: nextValue.vote_counts_by_option,
  voting_visibility: nextValue.voting_visibility
});
var PollOptionSelector = ({
  displayAvatarCount,
  option,
  voteCountVerbose
}) => {
  const { t: t2 } = useTranslationContext();
  const { channelCapabilities = {} } = useChannelStateContext("PollOptionsShortlist");
  const { message } = useMessageContext();
  const { AvatarStack: AvatarStack$1 = AvatarStack } = useComponentContext();
  const { poll } = usePollContext();
  const {
    is_closed,
    latest_votes_by_option,
    maxVotedOptionIds,
    ownVotesByOptionId,
    vote_counts_by_option,
    voting_visibility
  } = useStateStore(poll.state, pollStateSelector$2);
  const canCastVote = channelCapabilities["cast-poll-vote"] && !is_closed;
  const winningOptionCount = maxVotedOptionIds[0] ? vote_counts_by_option[maxVotedOptionIds[0]] : 0;
  const toggleVote = (0, import_react29.useMemo)(
    () => (0, import_lodash5.default)(() => {
      if (!canCastVote) return;
      const haveVotedForTheOption = !!ownVotesByOptionId[option.id];
      return haveVotedForTheOption ? poll.removeVote(ownVotesByOptionId[option.id].id, message.id) : poll.castVote(option.id, message.id);
    }, 100),
    [canCastVote, message.id, option.id, ownVotesByOptionId, poll]
  );
  const avatarDisplayInfo = (0, import_react29.useMemo)(
    () => latest_votes_by_option?.[option.id] && latest_votes_by_option[option.id].filter((vote) => !!vote.user && !isVoteAnswer(vote)).slice(0, displayAvatarCount).map(({ user }) => ({
      id: user.id,
      // eslint-disable-line @typescript-eslint/no-non-null-assertion
      imageUrl: user.image,
      // eslint-disable-line @typescript-eslint/no-non-null-assertion
      userName: user.name
      // eslint-disable-line @typescript-eslint/no-non-null-assertion
    })),
    [displayAvatarCount, latest_votes_by_option, option.id]
  );
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__poll-option", {
        "str-chat__poll-option--votable": canCastVote
      }),
      onClick: toggleVote,
      children: [
        canCastVote && (0, import_jsx_runtime4.jsx)(Checkmark, { checked: !!ownVotesByOptionId[option.id] }),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-option-data", children: [
          (0, import_jsx_runtime4.jsx)("p", { className: "str-chat__poll-option-text", children: option.text }),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-option-votes", children: [
            displayAvatarCount && voting_visibility === "public" && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-option-voters", children: (0, import_jsx_runtime4.jsx)(AvatarStack$1, { displayInfo: avatarDisplayInfo, size: "xs" }) }),
            (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__poll-option-vote-count", children: voteCountVerbose ? t2("{{count}} votes", {
              count: vote_counts_by_option[option.id] ?? 0
            }) : vote_counts_by_option[option.id] ?? 0 })
          ] })
        ] }),
        (0, import_jsx_runtime4.jsx)(
          AmountBar,
          {
            amount: (winningOptionCount && (vote_counts_by_option[option.id] ?? 0) / winningOptionCount) * 100,
            className: clsx_default("str-chat__poll-option__votes-bar", {
              "str-chat__poll-option__votes-bar--winner": is_closed && maxVotedOptionIds.length === 1 && maxVotedOptionIds[0] === option.id
            })
          }
        )
      ]
    },
    `base-poll-option-${option.id}`
  );
};
var pollStateSelector$1 = (nextValue) => ({
  options: nextValue.options
});
var PollOptionList = ({
  optionsDisplayCount,
  PollOptionsFullList: PollOptionsFullList$1 = PollOptionsFullList
}) => {
  const { PollOptionSelector: PollOptionSelector$1 = PollOptionSelector } = useComponentContext();
  const { t: t2 } = useTranslationContext("PollOptionList");
  const { poll } = usePollContext();
  const { options } = useStateStore(poll.state, pollStateSelector$1);
  const [viewAllOptionsOpen, setViewAllOptionsOpen] = (0, import_react29.useState)(false);
  const closeViewAllOptions = (0, import_react29.useCallback)(() => setViewAllOptionsOpen(false), []);
  const openViewAllOptions = (0, import_react29.useCallback)(() => setViewAllOptionsOpen(true), []);
  const showMoreOptionsButton = typeof optionsDisplayCount === "number" && options.length > optionsDisplayCount;
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: clsx_default("str-chat__poll-option-list", {
          "str-chat__poll-option-list--full": typeof optionsDisplayCount === "undefined"
        }),
        children: options.slice(0, optionsDisplayCount ?? options.length).map((option) => (0, import_jsx_runtime4.jsx)(
          PollOptionSelector$1,
          {
            displayAvatarCount: 3,
            option
          },
          `poll-option-${option.id}`
        ))
      }
    ),
    showMoreOptionsButton && (0, import_jsx_runtime4.jsx)(
      PollAction,
      {
        buttonText: t2("+{{count}} more options", {
          count: options.length
        }),
        closeModal: closeViewAllOptions,
        isAdditionalAction: true,
        modalClassName: "str-chat__poll-action-modal",
        modalIsOpen: viewAllOptionsOpen,
        openModal: openViewAllOptions,
        children: (0, import_jsx_runtime4.jsx)(PollOptionsFullList$1, {})
      }
    )
  ] });
};
var pollStateSelector = (nextValue) => ({
  name: nextValue.name
});
var PollOptionsFullList = () => {
  const { t: t2 } = useTranslationContext();
  const { poll } = usePollContext();
  const { close } = useModalContext();
  const { name: name2 } = useStateStore(poll.state, pollStateSelector);
  return (0, import_jsx_runtime4.jsxs)(Viewer.Root, { className: "str-chat__modal__poll-option-list", children: [
    (0, import_jsx_runtime4.jsx)(Viewer.Header, { close, title: t2("Poll options") }),
    (0, import_jsx_runtime4.jsxs)(Viewer.Body, { className: "str-chat__modal__poll-option-list__body", children: [
      (0, import_jsx_runtime4.jsx)(PollQuestion, { question: name2 }),
      (0, import_jsx_runtime4.jsx)(PollOptionList, {})
    ] })
  ] });
};
var pollStateSelectorPollContent = (nextValue) => ({ is_closed: nextValue.is_closed });
var PollContent = () => {
  const { PollActions: PollActions$1 = PollActions, PollHeader: PollHeader$1 = PollHeader } = useComponentContext();
  const { poll } = usePollContext();
  const { is_closed } = useStateStore(poll.state, pollStateSelectorPollContent);
  return (0, import_jsx_runtime4.jsxs)("div", { className: clsx_default("str-chat__poll", { "str-chat__poll--closed": is_closed }), children: [
    (0, import_jsx_runtime4.jsx)(PollHeader$1, {}),
    (0, import_jsx_runtime4.jsx)(PollOptionList, { optionsDisplayCount: MAX_OPTIONS_DISPLAYED }),
    (0, import_jsx_runtime4.jsx)(PollActions$1, {})
  ] });
};
var Poll = ({ poll }) => {
  const { PollContent: PollContent$1 = PollContent } = useComponentContext();
  if (!poll) return null;
  return (0, import_jsx_runtime4.jsx)(PollProvider, { poll, children: (0, import_jsx_runtime4.jsx)(PollContent$1, {}) });
};
var pollComposerStateSelector$3 = (state) => ({
  enforce_unique_vote: state.data.enforce_unique_vote,
  error: state.errors.max_votes_allowed,
  max_votes_allowed: state.data.max_votes_allowed
});
var MultipleAnswersField = () => {
  const { t: t2 } = useTranslationContext();
  const { pollComposer } = useMessageComposerController();
  const { enforce_unique_vote, error: error2, max_votes_allowed } = useStateStore(
    pollComposer.state,
    pollComposerStateSelector$3
  );
  const [voteLimitEnabled, setVoteLimitEnabled] = (0, import_react29.useState)(false);
  const knownValidationErrors = (0, import_react29.useMemo)(
    () => ({
      "Enforce unique vote is enabled": t2("Enforce unique vote is enabled"),
      "Type a number from 2 to 10": t2("Type a number from 2 to 10")
    }),
    [t2]
  );
  const multipleVotesEnabled = !enforce_unique_vote;
  const errorText = error2 && knownValidationErrors[error2];
  return (0, import_jsx_runtime4.jsxs)("div", { className: clsx_default("str-chat__form__switch-fieldset", {}), children: [
    (0, import_jsx_runtime4.jsx)(
      SwitchField,
      {
        checked: multipleVotesEnabled,
        description: t2("Select more than one option"),
        id: "enforce_unique_vote",
        onChange: (e) => {
          setVoteLimitEnabled(false);
          pollComposer.updateFields({ enforce_unique_vote: !e.target.checked });
        },
        title: t2("Multiple votes")
      }
    ),
    multipleVotesEnabled && (0, import_jsx_runtime4.jsx)(
      SwitchField,
      {
        checked: voteLimitEnabled,
        fieldClassName: "str-chat__multiple-answers-field__votes-limit-field",
        onChange: () => {
          setVoteLimitEnabled((prev) => !prev);
          pollComposer.updateFields({ max_votes_allowed: "2" });
        },
        children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__multiple-answers-field__votes-limit-field__numeric-field", children: [
          (0, import_jsx_runtime4.jsx)(
            SwitchFieldLabel,
            {
              asError: !!errorText,
              description: t2("Choose between 2 to 10 options"),
              htmlFor: "max_votes_allowed",
              title: t2("Limit votes per person")
            }
          ),
          voteLimitEnabled && (0, import_jsx_runtime4.jsx)(
            NumericInput,
            {
              id: "max_votes_allowed",
              max: 10,
              min: 2,
              onBlur: () => {
                pollComposer.handleFieldBlur("max_votes_allowed");
              },
              onChange: (e) => {
                const raw = e.target.value;
                const nativeFieldValidation = raw !== "" && !/^\d+$/.test(raw) ? { max_votes_allowed: t2("Only numbers are allowed") } : void 0;
                pollComposer.updateFields(
                  {
                    max_votes_allowed: nativeFieldValidation ? pollComposer.max_votes_allowed : raw
                  },
                  nativeFieldValidation
                );
              },
              value: max_votes_allowed ?? ""
            }
          )
        ] })
      }
    )
  ] });
};
var pollComposerStateSelector$2 = (state) => ({
  error: state.errors.name,
  name: state.data.name
});
var NameField = () => {
  const { t: t2 } = useTranslationContext();
  const { pollComposer } = useMessageComposerController();
  const { error: error2, name: name2 } = useStateStore(pollComposer.state, pollComposerStateSelector$2);
  const knownValidationErrors = (0, import_react29.useMemo)(
    () => ({
      "Question is required": t2("Question is required")
    }),
    [t2]
  );
  return (0, import_jsx_runtime4.jsx)(
    TextInput,
    {
      className: "str-chat__form__input-field__value",
      error: !!error2,
      errorMessage: error2 ? (0, import_jsx_runtime4.jsx)("span", { "data-testid": "poll-name-input-field-error", children: knownValidationErrors[error2] ?? t2("Error") }) : void 0,
      id: "name",
      label: t2("Question"),
      onBlur: () => {
        pollComposer.handleFieldBlur("name");
      },
      onChange: (e) => {
        pollComposer.updateFields({ name: e.target.value });
      },
      placeholder: t2("Ask a question"),
      type: "text",
      value: name2
    }
  );
};
var pollComposerStateSelector$1 = (state) => ({
  errors: state.errors.options,
  options: state.data.options
});
var OptionFieldSet = () => {
  const { pollComposer } = useMessageComposerController();
  const { errors, options } = useStateStore(
    pollComposer.state,
    pollComposerStateSelector$1
  );
  const { t: t2 } = useTranslationContext("OptionFieldSet");
  const knownValidationErrors = (0, import_react29.useMemo)(
    () => ({
      "Option already exists": t2("Option already exists"),
      "Option is empty": t2("Option is empty")
    }),
    [t2]
  );
  const onSetNewOrder = (0, import_react29.useCallback)(
    (newOrder) => {
      const prevOptions = pollComposer.options;
      pollComposer.updateFields({ options: newOrder.map((index6) => prevOptions[index6]) });
    },
    [pollComposer]
  );
  const clearOption = (0, import_react29.useCallback)(
    (removedOptionId) => {
      pollComposer.updateFields({
        options: pollComposer.options.filter((option) => option.id !== removedOptionId)
      });
    },
    [pollComposer]
  );
  const draggable = options.length > 1;
  return (0, import_jsx_runtime4.jsx)(
    TextInputFieldSet,
    {
      draggable,
      label: t2("Options"),
      onSetNewOrder,
      children: options.map((option, i2) => {
        const error2 = errors?.[option.id];
        return (0, import_jsx_runtime4.jsx)(
          "div",
          {
            className: clsx_default("str-chat__form__input-field", {
              "str-chat__form__input-field--draggable": draggable,
              "str-chat__form__input-field--has-error": error2
            }),
            children: (0, import_jsx_runtime4.jsx)(
              TextInput,
              {
                className: "str-chat__form__input-field__value",
                error: !!error2,
                fieldMessagePlacement: "inside",
                id: option.id,
                leading: draggable ? (0, import_jsx_runtime4.jsx)(IconReorder, { className: "str-chat__drag-handle" }) : void 0,
                message: error2 ? (0, import_jsx_runtime4.jsx)("span", { "data-testid": "poll-option-input-field-error", children: knownValidationErrors[error2] ?? t2("Error") }) : void 0,
                onBlur: () => {
                  pollComposer.handleFieldBlur("options");
                },
                onChange: (e) => {
                  pollComposer.updateFields({
                    options: { index: i2, text: e.target.value }
                  });
                },
                onKeyUp: (event) => {
                  const isFocusedLastOptionField = i2 === options.length - 1;
                  if (event.key === "Enter" && !isFocusedLastOptionField) {
                    const nextInputId = options[i2 + 1].id;
                    document.getElementById(nextInputId)?.focus();
                  }
                },
                placeholder: t2("Add an option"),
                trailing: option.text ? (0, import_jsx_runtime4.jsx)(RemoveOptionButton, { onClick: () => clearOption(option.id) }) : void 0,
                type: "text",
                value: option.text
              }
            )
          },
          `new-poll-option-${i2}`
        );
      })
    }
  );
};
var RemoveOptionButton = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    appearance: "ghost",
    circular: true,
    className: clsx_default("str-chat__form__remove-option-button", className),
    size: "xs",
    variant: "secondary",
    ...props,
    children: (0, import_jsx_runtime4.jsx)(IconMinusCircle, {})
  }
);
var PollCreationDialogControls = ({
  close
}) => {
  const { t: t2 } = useTranslationContext("PollCreationDialogControls");
  const { handleSubmit: handleSubmitMessage } = useMessageComposerContext();
  const messageComposer = useMessageComposerController();
  const canCreatePoll = useCanCreatePoll();
  return (0, import_jsx_runtime4.jsx)(Prompt.Footer, { children: (0, import_jsx_runtime4.jsxs)(Prompt.FooterControls, { children: [
    (0, import_jsx_runtime4.jsx)(
      Prompt.FooterControlsButtonSecondary,
      {
        className: clsx_default("str-chat__prompt__footer__controls-button--cancel"),
        onClick: () => {
          messageComposer.pollComposer.initState();
          close();
        },
        type: "button",
        children: t2("Cancel")
      }
    ),
    (0, import_jsx_runtime4.jsxs)(
      Prompt.FooterControlsButtonPrimary,
      {
        className: clsx_default("str-chat__prompt__footer__controls-button--submit"),
        disabled: !canCreatePoll,
        onClick: () => {
          messageComposer.createPoll().then(() => handleSubmitMessage()).then(() => {
            messageComposer.pollComposer.initState();
            close();
          }).catch(console.error);
        },
        type: "submit",
        children: [
          (0, import_jsx_runtime4.jsx)(IconSend, {}),
          t2("Send poll")
        ]
      }
    )
  ] }) });
};
var pollComposerStateSelector = (state) => ({
  allow_answers: state.data.allow_answers,
  allow_user_suggested_options: state.data.allow_user_suggested_options,
  voting_visibility: state.data.voting_visibility
});
var PollCreationDialog = ({ close }) => {
  const { t: t2 } = useTranslationContext();
  const { pollComposer } = useMessageComposerController();
  const { allow_answers, allow_user_suggested_options, voting_visibility } = useStateStore(pollComposer.state, pollComposerStateSelector);
  const onClose = (0, import_react29.useCallback)(() => {
    pollComposer.initState();
    close();
  }, [pollComposer, close]);
  return (0, import_jsx_runtime4.jsxs)(
    Prompt.Root,
    {
      className: "str-chat__poll-creation-dialog",
      "data-testid": "poll-creation-dialog",
      children: [
        (0, import_jsx_runtime4.jsx)(Prompt.Header, { close: onClose, title: t2("Create poll") }),
        (0, import_jsx_runtime4.jsx)(Prompt.Body, { children: (0, import_jsx_runtime4.jsxs)("form", { autoComplete: "off", children: [
          (0, import_jsx_runtime4.jsx)(NameField, {}),
          (0, import_jsx_runtime4.jsx)(OptionFieldSet, {}),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__poll-creation-dialog__features-selectors", children: [
            (0, import_jsx_runtime4.jsx)(MultipleAnswersField, {}),
            (0, import_jsx_runtime4.jsx)(
              SwitchField,
              {
                checked: voting_visibility === "anonymous",
                description: t2("Hide who voted"),
                id: "voting_visibility",
                onChange: (e) => pollComposer.updateFields({
                  voting_visibility: e.target.checked ? VotingVisibility.anonymous : VotingVisibility.public
                }),
                title: t2("Anonymous poll")
              }
            ),
            (0, import_jsx_runtime4.jsx)(
              SwitchField,
              {
                checked: allow_user_suggested_options,
                description: t2("Let others add options"),
                id: "allow_user_suggested_options",
                onChange: (e) => pollComposer.updateFields({
                  allow_user_suggested_options: e.target.checked
                }),
                title: t2("Suggest an option")
              }
            ),
            (0, import_jsx_runtime4.jsx)(
              SwitchField,
              {
                checked: allow_answers,
                description: t2("Allow others to add comments"),
                id: "allow_answers",
                onChange: (e) => pollComposer.updateFields({ allow_answers: e.target.checked }),
                title: t2("Add a comment")
              }
            )
          ] })
        ] }) }),
        (0, import_jsx_runtime4.jsx)(PollCreationDialogControls, { close })
      ]
    }
  );
};
var AttachmentSelectorContext = (0, import_react29.createContext)({
  fileInput: null
});
var AttachmentSelectorContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime4.jsx)(AttachmentSelectorContext.Provider, { value, children });
var useAttachmentSelectorContext = () => (0, import_react29.useContext)(AttachmentSelectorContext);
var icons = {
  ban: IconUserRemove,
  flag: IconFlag,
  giphy: IconGiphy,
  mute: IconMute,
  unban: IconUserAdd,
  unmute: IconAudio
};
var CommandsMenuClassName = "str-chat__context-menu--commands";
var CommandsSubmenuHeader = () => {
  const { t: t2 } = useTranslationContext();
  const { returnToParentMenu } = useContextMenuContext();
  return (0, import_jsx_runtime4.jsx)(ContextMenuHeader, { className: "str-chat__context-menu__header--commands str-chat__context-menu__header--submenu-commands", children: (0, import_jsx_runtime4.jsxs)(ContextMenuBackButton, { onClick: returnToParentMenu, children: [
    (0, import_jsx_runtime4.jsx)(IconChevronLeft, {}),
    (0, import_jsx_runtime4.jsx)("span", { children: t2("Instant commands") })
  ] }) });
};
var CommandsMenuHeader = () => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)(ContextMenuHeader, { className: "str-chat__context-menu__header--commands", children: (0, import_jsx_runtime4.jsx)("span", { children: t2("Instant commands") }) });
};
var CommandsMenu = () => {
  const { closeMenu } = useContextMenuContext();
  const messageComposer = useMessageComposerController();
  const { textareaRef } = useMessageComposerContext();
  const channelConfig = messageComposer.channel.getConfig();
  const commands = (0, import_react29.useMemo)(
    () => (channelConfig?.commands ?? []).filter(
      (command) => !!command.name
    ).sort((a2, b2) => (a2.name ?? "").localeCompare(b2.name ?? "")),
    [channelConfig]
  );
  return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: commands.map((command) => (0, import_jsx_runtime4.jsx)(
    CommandContextMenuItem,
    {
      command,
      onClick: () => {
        if (!command.name) return;
        messageComposer.textComposer.setCommand(command);
        closeMenu();
        requestAnimationFrame(() => textareaRef.current?.focus());
      }
    },
    command.name
  )) });
};
var useCommandTranslation = (command) => {
  const { t: t2 } = useTranslationContext();
  const knownArgsTranslations = (0, import_react29.useMemo)(
    () => ({
      ban: t2("ban-command-args"),
      giphy: t2("giphy-command-args"),
      mute: t2("mute-command-args"),
      unban: t2("unban-command-args"),
      unmute: t2("unmute-command-args")
    }),
    [t2]
  );
  const knownDescriptionTranslations = (0, import_react29.useMemo)(
    () => ({
      ban: t2("ban-command-description"),
      giphy: t2("giphy-command-description"),
      mute: t2("mute-command-description"),
      unban: t2("unban-command-description"),
      unmute: t2("unmute-command-description")
    }),
    [t2]
  );
  const args = command.args && (knownArgsTranslations[command.name ?? ""] ?? t2(command.args));
  const description = command.description && (knownDescriptionTranslations[command.name ?? ""] ?? t2(command.description));
  return { args, description };
};
var CommandContextMenuItem = ({
  className,
  command,
  ...props
}) => {
  const { args, description } = useCommandTranslation(command);
  const details = (0, import_react29.useMemo)(() => `/${command.name} ${args}`, [args, command.name]);
  return (0, import_react29.createElement)(
    ContextMenuButton,
    {
      ...props,
      className: clsx_default("str-chat__context-menu__button--command", className),
      details,
      Icon: icons[command.name],
      key: command.name,
      label: command.name,
      title: `${description} ${command.args}`
    }
  );
};
var AttachmentSelectorMenuInitButtonIcon = ({ className }) => {
  const { AttachmentSelectorInitiationButtonContents } = useComponentContext();
  if (AttachmentSelectorInitiationButtonContents) {
    return (0, import_jsx_runtime4.jsx)("span", { className, children: (0, import_jsx_runtime4.jsx)(AttachmentSelectorInitiationButtonContents, {}) });
  }
  return (0, import_jsx_runtime4.jsx)(
    IconPlus,
    {
      className: clsx_default("str-chat__attachment-selector__menu-button__icon", className)
    }
  );
};
var AttachmentSelectorButton = (0, import_react29.forwardRef)(function AttachmentSelectorButton2({ className, iconClassName, ...props }, ref) {
  return (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "outline",
      circular: true,
      className: clsx_default("str-chat__attachment-selector__menu-button", className),
      "data-testid": "invoke-attachment-selector-button",
      size: "lg",
      variant: "secondary",
      ...props,
      ref,
      children: (0, import_jsx_runtime4.jsx)(AttachmentSelectorMenuInitButtonIcon, { className: iconClassName })
    }
  );
});
var SimpleAttachmentSelector = () => {
  const { channelCapabilities } = useChannelStateContext();
  const inputRef = (0, import_react29.useRef)(null);
  const [buttonElement, setButtonElement] = (0, import_react29.useState)(null);
  const id3 = useStableId();
  const isCooldownActive = useIsCooldownActive();
  (0, import_react29.useEffect)(() => {
    if (!buttonElement) return;
    const handleKeyUp = (event) => {
      if (![" ", "Enter"].includes(event.key) || !inputRef.current) return;
      event.preventDefault();
      inputRef.current.click();
    };
    buttonElement.addEventListener("keyup", handleKeyUp);
    return () => {
      buttonElement.removeEventListener("keyup", handleKeyUp);
    };
  }, [buttonElement]);
  if (!channelCapabilities["upload-file"]) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-selector", children: [
    (0, import_jsx_runtime4.jsx)(
      AttachmentSelectorButton,
      {
        disabled: isCooldownActive,
        onClick: () => inputRef.current?.click(),
        ref: setButtonElement
      }
    ),
    (0, import_jsx_runtime4.jsx)(UploadFileInput, { id: id3, ref: inputRef })
  ] });
};
var DefaultAttachmentSelectorComponents = {
  Command({ submenuHeader, submenuItems }) {
    const { t: t2 } = useTranslationContext();
    const { openSubmenu } = useContextMenuContext();
    const hasSubmenu = !!submenuItems;
    return (0, import_jsx_runtime4.jsx)(
      ContextMenuButton,
      {
        className: "str-chat__attachment-selector-actions-menu__button str-chat__attachment-selector-actions-menu__create-poll-button",
        hasSubMenu: hasSubmenu,
        Icon: IconCommand,
        onClick: () => {
          if (!hasSubmenu) return;
          openSubmenu({
            Header: submenuHeader,
            menuClassName: CommandsMenuClassName,
            Submenu: submenuItems
          });
        },
        children: t2("Commands")
      }
    );
  },
  File() {
    const { t: t2 } = useTranslationContext();
    const { fileInput } = useAttachmentSelectorContext();
    const { closeMenu } = useContextMenuContext();
    return (0, import_jsx_runtime4.jsx)(
      ContextMenuButton,
      {
        className: "str-chat__attachment-selector-actions-menu__button str-chat__attachment-selector-actions-menu__upload-file-button",
        Icon: IconAttachment,
        onClick: () => {
          fileInput?.click();
          closeMenu();
        },
        children: t2("File")
      }
    );
  },
  Location({ openModalForAction }) {
    const { t: t2 } = useTranslationContext();
    const { closeMenu } = useContextMenuContext();
    return (0, import_jsx_runtime4.jsx)(
      ContextMenuButton,
      {
        className: "str-chat__attachment-selector-actions-menu__button str-chat__attachment-selector-actions-menu__add-location-button",
        Icon: IconLocation,
        onClick: () => {
          openModalForAction("addLocation");
          closeMenu();
        },
        children: t2("Location")
      }
    );
  },
  Poll({ openModalForAction }) {
    const { t: t2 } = useTranslationContext();
    const { closeMenu } = useContextMenuContext();
    return (0, import_jsx_runtime4.jsx)(
      ContextMenuButton,
      {
        className: "str-chat__attachment-selector-actions-menu__button str-chat__attachment-selector-actions-menu__create-poll-button",
        Icon: IconPoll,
        onClick: () => {
          openModalForAction("createPoll");
          closeMenu();
        },
        children: t2("Poll")
      }
    );
  }
};
var defaultAttachmentSelectorActionSet = [
  {
    ActionButton: DefaultAttachmentSelectorComponents.File,
    type: "uploadFile"
  },
  {
    ActionButton: DefaultAttachmentSelectorComponents.Poll,
    type: "createPoll"
  },
  {
    ActionButton: DefaultAttachmentSelectorComponents.Location,
    type: "addLocation"
  },
  {
    ActionButton: DefaultAttachmentSelectorComponents.Command,
    Header: CommandsSubmenuHeader,
    Submenu: CommandsMenu,
    type: "selectCommand"
  }
];
var useAttachmentSelectorActionsFiltered = (original) => {
  const {
    PollCreationDialog: PollCreationDialog$1 = PollCreationDialog,
    ShareLocationDialog: ShareLocationDialog$1 = ShareLocationDialog
  } = useComponentContext();
  const { channelCapabilities } = useChannelStateContext();
  const { isUploadEnabled } = useAttachmentManagerState();
  const messageComposer = useMessageComposerController();
  const channelConfig = messageComposer.channel.getConfig();
  return (0, import_react29.useMemo)(
    () => original.filter((action) => {
      if (action.type === "uploadFile")
        return channelCapabilities["upload-file"] && channelConfig?.uploads && isUploadEnabled;
      if (action.type === "createPoll")
        return channelCapabilities["send-poll"] && !messageComposer.threadId && channelConfig?.polls;
      if (action.type === "addLocation") {
        return channelConfig?.shared_locations && !messageComposer.threadId;
      }
      if (action.type === "selectCommand") {
        return !!channelConfig?.commands?.some((command) => !!command.name);
      }
      return true;
    }).map((action) => {
      if (action.type === "createPoll" && !action.ModalContent) {
        return { ...action, ModalContent: PollCreationDialog$1 };
      }
      if (action.type === "addLocation" && !action.ModalContent) {
        return { ...action, ModalContent: ShareLocationDialog$1 };
      }
      return action;
    }),
    [
      PollCreationDialog$1,
      ShareLocationDialog$1,
      channelCapabilities,
      channelConfig,
      isUploadEnabled,
      messageComposer.threadId,
      original
    ]
  );
};
var AttachmentSelector = ({
  attachmentSelectorActionSet = defaultAttachmentSelectorActionSet,
  getModalPortalDestination
}) => {
  const { t: t2 } = useTranslationContext();
  const { ContextMenu: ContextMenuComponent = ContextMenu, Modal = GlobalModal } = useComponentContext();
  const { channelCapabilities } = useChannelStateContext();
  const messageComposer = useMessageComposerController();
  const isCooldownActive = useIsCooldownActive();
  const actions = useAttachmentSelectorActionsFiltered(attachmentSelectorActionSet);
  const menuDialogId = `attachment-actions-menu${messageComposer.threadId ? "-thread" : ""}`;
  const { dialog: menuDialog, dialogManager } = useDialogOnNearestManager({
    id: menuDialogId
  });
  const menuDialogIsOpen = useDialogIsOpen(menuDialogId, dialogManager?.id);
  const [modalContentAction, setModalContentActionAction] = (0, import_react29.useState)();
  const openModalForAction = (0, import_react29.useCallback)(
    (actionType) => {
      const action = actions.find((a2) => a2.type === actionType);
      if (!action?.ModalContent) return;
      setModalContentActionAction(action);
    },
    [actions]
  );
  const closeModal = (0, import_react29.useCallback)(() => setModalContentActionAction(void 0), []);
  const [fileInput, setFileInput] = (0, import_react29.useState)(null);
  const menuButtonRef = (0, import_react29.useRef)(null);
  const contextMenuItems = (0, import_react29.useMemo)(
    () => actions.map((action) => (0, import_jsx_runtime4.jsx)(
      action.ActionButton,
      {
        openModalForAction,
        submenuHeader: action.Header,
        submenuItems: action.Submenu
      },
      action.type
    )),
    [actions, openModalForAction]
  );
  const getDefaultPortalDestination = (0, import_react29.useCallback)(
    () => document.getElementById(CHANNEL_CONTAINER_ID),
    []
  );
  if (actions.length === 0) return null;
  if (actions.length === 1 && actions[0].type === "uploadFile")
    return (0, import_jsx_runtime4.jsx)(SimpleAttachmentSelector, {});
  const ModalContent = modalContentAction?.ModalContent;
  const modalIsOpen = !!ModalContent;
  return (0, import_jsx_runtime4.jsx)(AttachmentSelectorContextProvider, { value: { fileInput }, children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-selector", children: [
    channelCapabilities["upload-file"] && (0, import_jsx_runtime4.jsx)(UploadFileInput, { ref: setFileInput }),
    (0, import_jsx_runtime4.jsx)(
      AttachmentSelectorButton,
      {
        "aria-expanded": menuDialogIsOpen,
        "aria-haspopup": "true",
        "aria-label": t2("aria/Open Attachment Selector"),
        disabled: isCooldownActive,
        iconClassName: clsx_default("str-chat__prepare-rotate45", {
          "str-chat__rotate45": menuDialogIsOpen
        }),
        onClick: () => menuDialog?.toggle(),
        ref: menuButtonRef
      }
    ),
    (0, import_jsx_runtime4.jsx)(
      ContextMenuComponent,
      {
        allowFlip: true,
        backLabel: t2("Back"),
        className: "str-chat__attachment-selector-actions-menu",
        "data-testid": "attachment-selector-actions-menu",
        dialogManagerId: dialogManager?.id,
        id: menuDialogId,
        onClose: menuDialog.close,
        placement: "top-start",
        referenceElement: menuButtonRef.current,
        tabIndex: -1,
        trapFocus: true,
        children: contextMenuItems
      }
    ),
    (0, import_jsx_runtime4.jsx)(
      Portal,
      {
        getPortalDestination: getModalPortalDestination ?? getDefaultPortalDestination,
        isOpen: modalIsOpen,
        children: (0, import_jsx_runtime4.jsx)(
          Modal,
          {
            className: clsx_default({
              "str-chat__create-poll-modal": modalContentAction?.type === "createPoll",
              "str-chat__share-location-modal": modalContentAction?.type === "addLocation"
            }),
            onClose: closeModal,
            open: modalIsOpen,
            children: ModalContent && (0, import_jsx_runtime4.jsx)(ModalContent, { close: closeModal })
          }
        )
      }
    )
  ] }) });
};
var UnsupportedAttachmentPreview = ({
  attachment,
  removeAttachments
}) => {
  const { t: t2 } = useTranslationContext();
  const { id: id3 } = attachment.localMetadata ?? {};
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__attachment-preview-unsupported",
      "data-testid": "attachment-preview-unsupported",
      children: [
        (0, import_jsx_runtime4.jsx)(IconUnsupportedAttachment, {}),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment-preview-unsupported__metadata", children: (0, import_jsx_runtime4.jsx)(
          "div",
          {
            className: "str-chat__attachment-preview-unsupported__title",
            "data-testid": "unsupported-attachment-preview-title",
            children: t2("Unsupported attachment")
          }
        ) }),
        (0, import_jsx_runtime4.jsx)(
          RemoveAttachmentPreviewButton,
          {
            "data-testid": "file-preview-item-delete-button",
            onClick: () => {
              if (id3) removeAttachments([id3]);
            }
          }
        )
      ]
    }
  );
};
function resolveAttachmentFullByteSize(attachment) {
  const fromFile = attachment.localMetadata?.file?.size;
  if (typeof fromFile === "number" && Number.isFinite(fromFile) && fromFile >= 0) {
    return fromFile;
  }
  const raw = attachment.file_size;
  if (typeof raw === "number" && Number.isFinite(raw) && raw >= 0) return raw;
  if (typeof raw === "string") {
    const n2 = parseFloat(raw);
    if (Number.isFinite(n2) && n2 >= 0) return n2;
  }
  return void 0;
}
var AttachmentUploadedSizeIndicator = ({
  attachment
}) => {
  const {
    FileSizeIndicator: FileSizeIndicator$1 = FileSizeIndicator,
    UploadedSizeIndicator: UploadedSizeIndicator$1 = UploadedSizeIndicator
  } = useComponentContext();
  const { uploadProgress, uploadState } = attachment.localMetadata ?? {};
  const fullBytes = resolveAttachmentFullByteSize(attachment);
  const uploaded = uploadProgress !== void 0 && fullBytes !== void 0 ? Math.round(uploadProgress / 100 * fullBytes) : void 0;
  if (uploadState === "uploading" && uploaded !== void 0 && fullBytes !== void 0) {
    return (0, import_jsx_runtime4.jsx)(UploadedSizeIndicator$1, { fullBytes, uploadedBytes: uploaded });
  }
  if (uploadState === "finished") {
    return (0, import_jsx_runtime4.jsx)(FileSizeIndicator$1, { fileSize: attachment.file_size });
  }
  return null;
};
var INTERACTIVE_SELECTOR = 'button, a, input, textarea, select, [role="button"], [role="link"], [data-interactive="true"]';
function hasInteractiveAncestorBeforeRoot(target, root4) {
  if (!(target instanceof Element) || !root4) return false;
  let el = target;
  while (el && el !== root4) {
    if (el.matches(INTERACTIVE_SELECTOR)) return true;
    el = el.parentElement;
  }
  return false;
}
var AttachmentPreviewRoot = ({
  attachment,
  onPressed,
  openPreview,
  tabIndex = 0,
  ...props
}) => {
  const { t: t2 } = useTranslationContext();
  const [root4, setRoot] = (0, import_react29.useState)(null);
  const url = attachment.asset_url || attachment.image_url || attachment.localMetadata.previewUri;
  const canDownloadAttachment = false;
  const canPreviewAttachment = !!openPreview && (!!url && isImageAttachment(attachment) || isVideoAttachment(attachment));
  const handlePressed = (e) => {
    if (e.defaultPrevented) return;
    if (hasInteractiveAncestorBeforeRoot(e.target, root4)) return;
    if (onPressed) {
      const shouldContinue = onPressed(e);
      if (!shouldContinue) return;
    }
    if (canPreviewAttachment) {
      openPreview();
      return;
    }
  };
  const isInteractive = canPreviewAttachment || canDownloadAttachment;
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      "aria-label": isInteractive ? t2(canPreviewAttachment ? "aria/Show preview" : "aria/Download attachment") : void 0,
      ...props,
      onClick: handlePressed,
      onKeyDown: isInteractive ? (e) => {
        if (e.key !== "Enter" && e.key !== " ") return;
        e.preventDefault();
        handlePressed(e);
      } : void 0,
      ref: setRoot,
      tabIndex: isInteractive ? tabIndex : -1,
      children: props.children
    }
  );
};
var FileAttachmentPreview = ({
  attachment,
  handleRetry,
  removeAttachments
}) => {
  const { t: t2 } = useTranslationContext("FilePreview");
  const { id: id3, uploadPermissionCheck, uploadProgress, uploadState } = attachment.localMetadata ?? {};
  const hasSizeLimitError = uploadPermissionCheck?.reason === "size_limit";
  const hasFatalError = uploadState === "blocked" || hasSizeLimitError;
  const hasRetriableError = uploadState === "failed" && !!handleRetry;
  return (0, import_jsx_runtime4.jsxs)(
    AttachmentPreviewRoot,
    {
      attachment,
      className: "str-chat__attachment-preview-file",
      "data-testid": "attachment-preview-file",
      children: [
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment-preview-file__icon", children: (0, import_jsx_runtime4.jsx)(FileIcon, { fileName: attachment.title, mimeType: attachment.mime_type }) }),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__info", children: [
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment-preview-file-name", title: attachment.title, children: attachment.title }),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__data", children: [
            uploadState === "uploading" && (0, import_jsx_runtime4.jsx)(UploadProgressIndicator, { uploadProgress }),
            (0, import_jsx_runtime4.jsx)(AttachmentUploadedSizeIndicator, { attachment }),
            hasFatalError && (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__fatal-error", children: [
              (0, import_jsx_runtime4.jsx)(IconExclamationMark, {}),
              (0, import_jsx_runtime4.jsx)("span", { children: hasSizeLimitError ? t2("File too large") : uploadState === "blocked" ? t2("Upload blocked") : t2("Upload failed") })
            ] }),
            hasRetriableError && (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__retriable-error", children: [
              (0, import_jsx_runtime4.jsx)(IconExclamationTriangleFill, {}),
              (0, import_jsx_runtime4.jsx)("span", { children: t2("Upload error") }),
              (0, import_jsx_runtime4.jsx)(
                "button",
                {
                  "aria-label": t2("aria/Retry upload"),
                  className: "str-chat__attachment-preview-file__retry-upload-button",
                  "data-testid": "file-preview-item-retry-button",
                  onClick: () => {
                    handleRetry(attachment);
                  },
                  type: "button",
                  children: t2("Retry upload")
                }
              )
            ] })
          ] })
        ] }),
        (0, import_jsx_runtime4.jsx)(
          RemoveAttachmentPreviewButton,
          {
            "data-testid": "file-preview-item-delete-button",
            onClick: () => {
              if (id3) removeAttachments([id3]);
            },
            uploadState
          }
        )
      ]
    }
  );
};
function formatTime(totalSeconds, rounding = "ceil") {
  if (totalSeconds == null || Number.isNaN(totalSeconds) || totalSeconds < 0) {
    return null;
  }
  const roundedSeconds = rounding === "floor" ? Math.floor(totalSeconds) : Math.ceil(totalSeconds);
  const hours = Math.floor(roundedSeconds / 3600);
  const minutes = Math.floor(roundedSeconds % 3600 / 60);
  const seconds = roundedSeconds % 60;
  const minSec = `${String(minutes).padStart(2, "0")}:${String(seconds).padStart(
    2,
    "0"
  )}`;
  return hours ? `${String(hours).padStart(2, "0")}:${minSec}` : minSec;
}
function DurationDisplay({
  className,
  duration: duration2,
  isPlaying,
  secondsElapsed,
  showRemaining = false
}) {
  const remainingSeconds = duration2 != null && secondsElapsed != null ? Math.max(0, duration2 - secondsElapsed) : void 0;
  const formattedDuration = formatTime(duration2);
  const formattedSecondsElapsed = formatTime(secondsElapsed);
  const formattedRemaining = formatTime(remainingSeconds);
  const shouldShowElapsed = !!secondsElapsed && secondsElapsed > 0 && secondsElapsed < (duration2 || 0);
  const canShowRemaining = showRemaining && duration2 != null && secondsElapsed != null;
  const primaryValue = showRemaining ? formattedRemaining : formattedSecondsElapsed;
  const showPrimary = (canShowRemaining || shouldShowElapsed) && !!primaryValue;
  const showDuration = !showPrimary && !!formattedDuration;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default(
        "str-chat__duration-display",
        {
          "str-chat__duration-display--hasProgress": !!secondsElapsed,
          "str-chat__duration-display--isPlaying": isPlaying
        },
        className
      ),
      children: [
        showPrimary && (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__duration-display__time-elapsed", children: primaryValue }),
        showDuration && (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__duration-display__duration", children: formattedDuration })
      ]
    }
  );
}
var PlaybackRateButton = ({
  children,
  className,
  ...rest
}) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    "data-testid": "playback-rate-button",
    ...rest,
    className: clsx_default("str-chat__playback-rate-button", className),
    children
  }
);
var getAvailableTrackWidth = (trackRoot) => {
  if (!trackRoot) return 0;
  const parent = trackRoot.parentElement;
  if (!parent) return trackRoot.getBoundingClientRect().width;
  const parentWidth = parent.getBoundingClientRect().width;
  const computedStyle = window.getComputedStyle(parent);
  const paddingLeft = parseFloat(computedStyle.paddingLeft) || 0;
  const paddingRight = parseFloat(computedStyle.paddingRight) || 0;
  const rawColumnGap = computedStyle.columnGap || computedStyle.gap;
  const parsedColumnGap = parseFloat(rawColumnGap);
  const columnGap = Number.isNaN(parsedColumnGap) ? 0 : parsedColumnGap;
  const gapCount = Math.max(0, parent.children.length - 1);
  const totalGapsWidth = columnGap * gapCount;
  const siblingsWidth = Array.from(parent.children).reduce((total, child) => {
    if (child === trackRoot) return total;
    return total + child.getBoundingClientRect().width;
  }, 0);
  return Math.max(
    0,
    parentWidth - paddingLeft - paddingRight - totalGapsWidth - siblingsWidth
  );
};
var useInteractiveProgressBar = ({
  progress = 0,
  seek
}) => {
  const isDragging = (0, import_react29.useRef)(false);
  const [availableTrackWidth, setAvailableTrackWidth] = (0, import_react29.useState)(0);
  const [root4, setRoot] = (0, import_react29.useState)(null);
  const [progressIndicator, setProgressIndicator] = (0, import_react29.useState)(null);
  const lastIndicatorWidth = (0, import_react29.useRef)(0);
  const handleDragStart = (e) => {
    e.preventDefault();
    if (!progressIndicator) return;
    isDragging.current = true;
    progressIndicator.style.cursor = "grabbing";
    root4?.classList.add("str-chat__wave-progress-bar__track--dragging");
  };
  const handleDrag = (e) => {
    if (!isDragging.current) return;
    seek({ ...e });
  };
  const handleDragStop = (0, import_react29.useCallback)(() => {
    if (!progressIndicator) return;
    isDragging.current = false;
    progressIndicator.style.removeProperty("cursor");
    root4?.classList.remove("str-chat__wave-progress-bar__track--dragging");
  }, [progressIndicator, root4]);
  (0, import_react29.useEffect)(() => {
    document.addEventListener("pointerup", handleDragStop);
    return () => {
      document.removeEventListener("pointerup", handleDragStop);
    };
  }, [handleDragStop]);
  (0, import_react29.useEffect)(() => {
    if (!root4 || typeof ResizeObserver === "undefined") return;
    const observer = new ResizeObserver(([entry]) => {
      const nextAvailableWidth = getAvailableTrackWidth(entry.target);
      setAvailableTrackWidth(nextAvailableWidth || entry.contentRect.width);
    });
    observer.observe(root4);
    return () => {
      observer.disconnect();
    };
  }, [root4]);
  (0, import_react29.useLayoutEffect)(() => {
    if (root4) {
      setAvailableTrackWidth(getAvailableTrackWidth(root4));
    }
    if (progressIndicator) {
      lastIndicatorWidth.current = progressIndicator.getBoundingClientRect().width;
    }
  }, [progressIndicator, root4]);
  const indicatorLeft = progress === 0 || !progressIndicator ? 0 : Math.max(0, availableTrackWidth - lastIndicatorWidth.current) * (progress / 100) + 1;
  return {
    availableTrackWidth,
    handleDrag,
    handleDragStart,
    handleDragStop,
    indicatorLeft,
    root: root4,
    setProgressIndicator,
    setRoot
  };
};
var ProgressBar = ({ className, progress, seek }) => {
  const {
    handleDrag,
    handleDragStart,
    handleDragStop,
    indicatorLeft,
    setProgressIndicator,
    setRoot
  } = useInteractiveProgressBar({ progress, seek });
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default(
        "str-chat__message-attachment-audio-widget--progress-track",
        className
      ),
      "data-progress": progress,
      "data-testid": "audio-progress",
      onClick: seek,
      onPointerDown: handleDragStart,
      onPointerMove: handleDrag,
      onPointerUp: handleDragStop,
      ref: setRoot,
      role: "progressbar",
      style: {
        "--str-chat__message-attachment-audio-widget-progress": progress + "%"
      },
      children: (0, import_jsx_runtime4.jsx)(
        "div",
        {
          className: "str-chat__message-attachment-audio-widget--progress-indicator",
          ref: setProgressIndicator,
          style: { insetInlineStart: `${indicatorLeft}px` }
        }
      )
    }
  );
};
var SUPPORTED_VIDEO_FORMATS$1 = [
  "video/mp4",
  "video/ogg",
  "video/webm",
  "video/quicktime"
];
var isGalleryAttachmentType = (attachment) => Array.isArray(attachment.items);
var isSvgAttachment = (attachment) => {
  const filename = attachment.fallback || "";
  return filename.toLowerCase().endsWith(".svg");
};
var divMod = (num, divisor) => [
  Math.floor(num / divisor),
  num % divisor
];
var displayDuration = (totalSeconds) => {
  if (!totalSeconds || totalSeconds < 0) return "00:00";
  const [hours, hoursLeftover] = divMod(totalSeconds, 3600);
  const [minutes, seconds] = divMod(hoursLeftover, 60);
  const roundedSeconds = Math.ceil(seconds);
  const prependHrsZero = String(hours).padStart(2, "0");
  const prependMinZero = String(minutes).padStart(2, "0");
  const prependSecZero = String(roundedSeconds).padStart(2, "0");
  const minSec = `${prependMinZero}:${prependSecZero}`;
  return hours ? `${prependHrsZero}:` + minSec : minSec;
};
function getCssDimensionsVariables(url) {
  const cssVars = {
    "--original-height": 1e6,
    "--original-width": 1e6
  };
  if (test(url, "url")) {
    const urlParams = new URL(url).searchParams;
    const oh = Number(urlParams.get("oh"));
    const ow = Number(urlParams.get("ow"));
    const originalHeight = oh > 1 ? oh : 1e6;
    const originalWidth = ow > 1 ? ow : 1e6;
    cssVars["--original-width"] = originalWidth;
    cssVars["--original-height"] = originalHeight;
  }
  return cssVars;
}
var resampleWaveformData = (waveformData, amplitudesCount) => waveformData.length === amplitudesCount ? waveformData : waveformData.length > amplitudesCount ? downSample(waveformData, amplitudesCount) : upSample(waveformData, amplitudesCount);
function downSample(data, targetOutputSize) {
  if (data.length <= targetOutputSize || targetOutputSize === 0) {
    return data;
  }
  if (targetOutputSize === 1) return [mean(data)];
  const result = [];
  const bucketSize = (data.length - 2) / (targetOutputSize - 2);
  let lastSelectedPointIndex = 0;
  result.push(data[lastSelectedPointIndex]);
  let maxAreaPoint, maxArea, triangleArea;
  for (let bucketIndex = 1; bucketIndex < targetOutputSize - 1; bucketIndex++) {
    const previousBucketRefPoint = data[lastSelectedPointIndex];
    const nextBucketMean = getNextBucketMean(data, bucketIndex, bucketSize);
    const currentBucketStartIndex = Math.floor((bucketIndex - 1) * bucketSize) + 1;
    const nextBucketStartIndex = Math.floor(bucketIndex * bucketSize) + 1;
    const countUnitsBetweenAtoC = 1 + nextBucketStartIndex - currentBucketStartIndex;
    maxArea = triangleArea = -1;
    for (let currentPointIndex = currentBucketStartIndex; currentPointIndex < nextBucketStartIndex; currentPointIndex++) {
      const countUnitsBetweenAtoB = Math.abs(currentPointIndex - currentBucketStartIndex) + 1;
      const countUnitsBetweenBtoC = countUnitsBetweenAtoC - countUnitsBetweenAtoB;
      const currentPointValue = data[currentPointIndex];
      triangleArea = triangleAreaHeron(
        triangleBase(
          Math.abs(previousBucketRefPoint - currentPointValue),
          countUnitsBetweenAtoB
        ),
        triangleBase(Math.abs(currentPointValue - nextBucketMean), countUnitsBetweenBtoC),
        triangleBase(
          Math.abs(previousBucketRefPoint - nextBucketMean),
          countUnitsBetweenAtoC
        )
      );
      if (triangleArea > maxArea) {
        maxArea = triangleArea;
        maxAreaPoint = data[currentPointIndex];
        lastSelectedPointIndex = currentPointIndex;
      }
    }
    if (typeof maxAreaPoint !== "undefined") result.push(maxAreaPoint);
  }
  result.push(data[data.length - 1]);
  return result;
}
var triangleAreaHeron = (a2, b2, c2) => {
  const s2 = (a2 + b2 + c2) / 2;
  return Math.sqrt(s2 * (s2 - a2) * (s2 - b2) * (s2 - c2));
};
var triangleBase = (a2, b2) => Math.sqrt(Math.pow(a2, 2) + Math.pow(b2, 2));
var mean = (values2) => values2.reduce((acc, value) => acc + value, 0) / values2.length;
var getNextBucketMean = (data, currentBucketIndex, bucketSize) => {
  const nextBucketStartIndex = Math.floor(currentBucketIndex * bucketSize) + 1;
  let nextNextBucketStartIndex = Math.floor((currentBucketIndex + 1) * bucketSize) + 1;
  nextNextBucketStartIndex = nextNextBucketStartIndex < data.length ? nextNextBucketStartIndex : data.length;
  return mean(data.slice(nextBucketStartIndex, nextNextBucketStartIndex));
};
var upSample = (values2, targetSize) => {
  if (!values2.length) {
    console.warn("Cannot extend empty array of amplitudes.");
    return values2;
  }
  if (values2.length > targetSize) {
    console.warn(
      "Requested to extend the waveformData that is longer than the target list size"
    );
    return values2;
  }
  if (targetSize === values2.length) return values2;
  let [bucketSize, remainder] = divMod(targetSize, values2.length);
  const result = [];
  for (let i2 = 0; i2 < values2.length; i2++) {
    const extra = remainder && remainder-- ? 1 : 0;
    result.push(...Array(bucketSize + extra).fill(values2[i2]));
  }
  return result;
};
var WaveProgressBar = ({
  amplitudeBarGapWidthPx,
  progress = 0,
  relativeAmplitudeBarWidth = 2,
  relativeAmplitudeGap = 1,
  seek,
  waveformData
}) => {
  const [trackAxisX, setTrackAxisX] = (0, import_react29.useState)();
  const lastAvailableTrackWidth = (0, import_react29.useRef)(0);
  const minAmplitudeBarWidthRef = (0, import_react29.useRef)(null);
  const lastMinAmplitudeBarWidthUsed = (0, import_react29.useRef)(null);
  const {
    availableTrackWidth,
    handleDrag,
    handleDragStart,
    handleDragStop,
    indicatorLeft,
    root: root4,
    setProgressIndicator,
    setRoot
  } = useInteractiveProgressBar({ progress, seek });
  const getTrackAxisX = (0, import_react29.useMemo)(
    () => (0, import_lodash3.default)((availableWidth) => {
      const minAmplitudeBarWidth = minAmplitudeBarWidthRef.current;
      const hasMinWidthChanged = minAmplitudeBarWidth !== lastMinAmplitudeBarWidthUsed.current;
      if (availableWidth === lastAvailableTrackWidth.current && !hasMinWidthChanged) {
        return;
      }
      lastAvailableTrackWidth.current = availableWidth;
      lastMinAmplitudeBarWidthUsed.current = minAmplitudeBarWidth;
      const possibleAmpCount = Math.floor(
        availableWidth / (relativeAmplitudeGap + relativeAmplitudeBarWidth)
      );
      const amplitudeBarWidthToGapRatio = relativeAmplitudeBarWidth / (relativeAmplitudeBarWidth + relativeAmplitudeGap);
      const maxCountByMinWidth = typeof minAmplitudeBarWidth === "number" && minAmplitudeBarWidth > 0 ? Math.floor(
        availableWidth * amplitudeBarWidthToGapRatio / minAmplitudeBarWidth
      ) : possibleAmpCount;
      const barCount = Math.max(0, Math.min(possibleAmpCount, maxCountByMinWidth));
      const barWidth = barCount && availableWidth / barCount * amplitudeBarWidthToGapRatio;
      setTrackAxisX({
        barCount,
        barWidth,
        gap: barWidth * (relativeAmplitudeGap / relativeAmplitudeBarWidth)
      });
    }, 1),
    [relativeAmplitudeBarWidth, relativeAmplitudeGap]
  );
  const resampledWaveformData = (0, import_react29.useMemo)(
    () => trackAxisX ? resampleWaveformData(waveformData, trackAxisX.barCount) : [],
    [trackAxisX, waveformData]
  );
  (0, import_react29.useLayoutEffect)(() => {
    if (availableTrackWidth > 0) {
      getTrackAxisX(availableTrackWidth);
    }
  }, [availableTrackWidth, getTrackAxisX]);
  (0, import_react29.useLayoutEffect)(() => {
    if (!root4 || typeof window === "undefined") return;
    const amplitudeBar = root4.querySelector(
      ".str-chat__wave-progress-bar__amplitude-bar"
    );
    if (!amplitudeBar) return;
    const computedStyle = window.getComputedStyle(amplitudeBar);
    const parsedMinWidth = parseFloat(computedStyle.minWidth);
    if (!Number.isNaN(parsedMinWidth) && parsedMinWidth > 0) {
      minAmplitudeBarWidthRef.current = parsedMinWidth;
    }
    if (availableTrackWidth > 0) {
      getTrackAxisX(availableTrackWidth);
    }
  }, [availableTrackWidth, getTrackAxisX, root4, trackAxisX?.barCount]);
  if (!waveformData.length || trackAxisX?.barCount === 0) return null;
  const amplitudeGapWidth = amplitudeBarGapWidthPx ?? trackAxisX?.gap;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__wave-progress-bar__track", {
        "str-chat__wave-progress-bar__track--playback-initiated": progress > 0
      }),
      "data-testid": "wave-progress-bar-track",
      onClick: seek,
      onPointerDown: handleDragStart,
      onPointerMove: handleDrag,
      onPointerUp: handleDragStop,
      ref: setRoot,
      role: "progressbar",
      style: {
        "--str-chat__voice-recording-amplitude-bar-gap-width": typeof amplitudeGapWidth === "number" ? `${amplitudeGapWidth}px` : void 0
      },
      children: [
        resampledWaveformData.map((amplitude, i2) => (0, import_jsx_runtime4.jsx)(
          "div",
          {
            className: clsx_default("str-chat__wave-progress-bar__amplitude-bar", {
              ["str-chat__wave-progress-bar__amplitude-bar--active"]: progress > i2 / resampledWaveformData.length * 100
            }),
            "data-testid": "amplitude-bar",
            style: {
              "--str-chat__voice-recording-amplitude-bar-width": trackAxisX?.barWidth + "px",
              "--str-chat__wave-progress-bar__amplitude-bar-height": amplitude ? amplitude * 100 + "%" : "0%"
            }
          },
          `amplitude-${i2}`
        )),
        (0, import_jsx_runtime4.jsx)(
          "div",
          {
            className: "str-chat__wave-progress-bar__progress-indicator",
            "data-testid": "wave-progress-bar-progress-indicator",
            ref: setProgressIndicator,
            style: {
              insetInlineStart: `${indicatorLeft}px`
            }
          }
        )
      ]
    }
  );
};
var audioPlayerStateSelector$3 = (state) => ({
  canPlayRecord: state.canPlayRecord,
  isPlaying: state.isPlaying,
  playbackRate: state.currentPlaybackRate,
  progressPercent: state.progressPercent,
  secondsElapsed: state.secondsElapsed
});
var AudioAttachmentPreview = ({
  attachment,
  handleRetry,
  removeAttachments
}) => {
  const { t: t2 } = useTranslationContext();
  const { id: id3, previewUri, uploadPermissionCheck, uploadProgress, uploadState } = attachment.localMetadata ?? {};
  const url = attachment.asset_url || previewUri;
  const audioPlayer = useAudioPlayer({
    fileSize: attachment.localMetadata.file?.size ?? attachment.file_size,
    mimeType: attachment.localMetadata.file?.type ?? attachment.mime_type,
    requester: attachment.localMetadata.id,
    src: url,
    title: attachment.title,
    waveformData: attachment.waveform_data
  });
  (0, import_react29.useEffect)(() => {
    audioPlayer?.cancelScheduledRemoval();
    return () => {
      audioPlayer?.scheduleRemoval();
    };
  }, [audioPlayer]);
  const { canPlayRecord, isPlaying, playbackRate, progressPercent, secondsElapsed } = useStateStore(audioPlayer?.state, audioPlayerStateSelector$3) ?? {};
  const resolvedDuration = audioPlayer?.durationSeconds ?? attachment.duration;
  const hasWaveform = !!audioPlayer?.waveformData?.length;
  const hasSizeLimitError = uploadPermissionCheck?.reason === "size_limit";
  const hasFatalError = uploadState === "blocked" || hasSizeLimitError;
  const hasRetriableError = uploadState === "failed" && !!handleRetry;
  const hasError = hasRetriableError || hasFatalError;
  const showProgressControls = !hasError || hasError && isPlaying;
  return (0, import_jsx_runtime4.jsxs)(
    AttachmentPreviewRoot,
    {
      attachment,
      className: clsx_default("str-chat__attachment-preview-audio"),
      "data-testid": "attachment-preview-audio",
      children: [
        (0, import_jsx_runtime4.jsx)(
          PlayButton,
          {
            isPlaying: Boolean(isPlaying),
            onClick: () => {
              audioPlayer?.togglePlay();
            }
          }
        ),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__info", children: [
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment-preview-file-name", title: attachment.title, children: isVoiceRecordingAttachment(attachment) ? t2("Voice message") : attachment.title }),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__data", children: [
            uploadState === "uploading" && (0, import_jsx_runtime4.jsx)(UploadProgressIndicator, { uploadProgress }),
            showProgressControls ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
              !resolvedDuration && !progressPercent && !isPlaying && (0, import_jsx_runtime4.jsx)(AttachmentUploadedSizeIndicator, { attachment }),
              hasWaveform ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
                (0, import_jsx_runtime4.jsx)(
                  DurationDisplay,
                  {
                    duration: resolvedDuration,
                    isPlaying: Boolean(isPlaying),
                    secondsElapsed,
                    showRemaining: true
                  }
                ),
                (0, import_jsx_runtime4.jsx)(
                  WaveProgressBar,
                  {
                    progress: progressPercent,
                    relativeAmplitudeBarWidth: 1,
                    relativeAmplitudeGap: 1,
                    seek: audioPlayer.seek,
                    waveformData: audioPlayer.waveformData
                  }
                )
              ] }) : (0, import_jsx_runtime4.jsx)(
                DurationDisplay,
                {
                  duration: resolvedDuration,
                  isPlaying: Boolean(isPlaying),
                  secondsElapsed
                }
              )
            ] }) : hasFatalError ? (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__fatal-error", children: [
              (0, import_jsx_runtime4.jsx)(IconExclamationMark, {}),
              (0, import_jsx_runtime4.jsx)("span", { children: hasSizeLimitError ? t2("File too large") : uploadState === "blocked" ? t2("Upload blocked") : t2("Upload failed") })
            ] }) : (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-file__retriable-error", children: [
              (0, import_jsx_runtime4.jsx)(IconExclamationTriangleFill, {}),
              (0, import_jsx_runtime4.jsx)("span", { children: t2("Upload error") }),
              (0, import_jsx_runtime4.jsx)(
                "button",
                {
                  "aria-label": t2("aria/Retry upload"),
                  className: "str-chat__attachment-preview-file__retry-upload-button",
                  "data-testid": "file-preview-item-retry-button",
                  onClick: () => {
                    handleRetry(attachment);
                  },
                  type: "button",
                  children: t2("Retry upload")
                }
              )
            ] })
          ] })
        ] }),
        audioPlayer && canPlayRecord && (0, import_jsx_runtime4.jsxs)(PlaybackRateButton, { onClick: audioPlayer.increasePlaybackRate, children: [
          "x",
          playbackRate?.toString()
        ] }),
        (0, import_jsx_runtime4.jsx)(
          RemoveAttachmentPreviewButton,
          {
            "data-testid": "audio-preview-item-delete-button",
            onClick: () => {
              if (id3) removeAttachments([id3]);
            },
            uploadState
          }
        )
      ]
    }
  );
};
var MediaBadgeVariantToIcon = {
  video: IconVideoFill,
  voiceRecording: IconMicrophoneSolid
};
var MediaBadge = ({ attachment, variant }) => {
  const Icon = MediaBadgeVariantToIcon[variant];
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__media-badge", {
        [`str-chat__media-badge--${variant}`]: variant
      }),
      children: [
        Icon && (0, import_jsx_runtime4.jsx)(Icon, {}),
        attachment.duration && (0, import_jsx_runtime4.jsx)("div", { children: attachment.duration })
      ]
    }
  );
};
var MediaAttachmentPreview = ({
  attachment,
  handleRetry,
  openPreview,
  removeAttachments
}) => {
  const { t: t2 } = useTranslationContext();
  const { BaseImage: BaseImage$1 = BaseImage } = useComponentContext();
  const [thumbnailPreviewError, setThumbnailPreviewError] = (0, import_react29.useState)(false);
  const { id: id3, uploadPermissionCheck, uploadProgress, uploadState } = attachment.localMetadata ?? {};
  const isUploading = uploadState === "uploading";
  const handleThumbnailLoadError = (0, import_react29.useCallback)(() => setThumbnailPreviewError(true), []);
  const hasSizeLimitError = uploadPermissionCheck?.reason === "size_limit";
  const hasFatalError = uploadState === "blocked" || hasSizeLimitError;
  const hasRetriableError = uploadState === "failed" && !!handleRetry;
  const hasUploadError = hasRetriableError || hasFatalError;
  const retry = (e) => {
    e.stopPropagation();
    handleRetry(attachment);
    return false;
  };
  const thumbnail = (0, import_react29.useMemo)(
    () => isVideoAttachment(attachment) ? {
      alt: attachment.title,
      title: attachment.title,
      url: attachment.thumb_url
    } : {
      alt: attachment.fallback,
      title: attachment.fallback,
      url: attachment.image_url || attachment.localMetadata.previewUri
    },
    [attachment]
  );
  return (0, import_jsx_runtime4.jsxs)(
    AttachmentPreviewRoot,
    {
      attachment,
      className: clsx_default("str-chat__attachment-preview-media", {
        "str-chat__attachment-preview-media--thumbnail-preview-error": thumbnailPreviewError,
        "str-chat__attachment-preview-media--upload-error": hasUploadError,
        "str-chat__attachment-preview-media--uploading": isUploading
      }),
      "data-testid": "attachment-preview-media",
      onPressed: hasRetriableError ? retry : void 0,
      openPreview: !isUploading && !hasUploadError ? openPreview : void 0,
      children: [
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment-preview-media__thumbnail-wrapper", children: [
          thumbnail.url && (0, import_jsx_runtime4.jsx)(
            BaseImage$1,
            {
              alt: thumbnail.alt,
              className: "str-chat__attachment-preview-media__thumbnail",
              onError: handleThumbnailLoadError,
              src: thumbnail.url,
              title: thumbnail.title
            }
          ),
          (0, import_jsx_runtime4.jsxs)("div", { className: clsx_default("str-chat__attachment-preview-media__overlay"), children: [
            isUploading && (0, import_jsx_runtime4.jsx)(UploadProgressIndicator, { uploadProgress }),
            isVideoAttachment(attachment) && !hasUploadError && uploadState !== "uploading" && (0, import_jsx_runtime4.jsx)(MediaBadge, { attachment, variant: "video" }),
            hasFatalError && (0, import_jsx_runtime4.jsx)(IconExclamationMark, {}),
            hasRetriableError && (0, import_jsx_runtime4.jsx)(
              Button,
              {
                appearance: "solid",
                "aria-label": t2("aria/Retry upload"),
                circular: true,
                className: "str-chat__attachment-preview-media__retry-upload-button",
                "data-testid": "video-preview-item-retry-button",
                onClick: retry,
                size: "sm",
                variant: "danger",
                children: (0, import_jsx_runtime4.jsx)(IconRetry, {})
              }
            )
          ] })
        ] }),
        (0, import_jsx_runtime4.jsx)(
          RemoveAttachmentPreviewButton,
          {
            "data-testid": "video-preview-item-delete-button",
            onClick: () => {
              if (id3) removeAttachments([id3]);
            },
            uploadState
          }
        )
      ]
    }
  );
};
var toGalleryItemDescriptors = (...args) => toBaseImageDescriptors(...args);
var GalleryContext = (0, import_react29.createContext)(void 0);
var useGalleryContext = () => {
  const contextValue = (0, import_react29.useContext)(GalleryContext);
  if (!contextValue) {
    console.warn(
      `The useGalleryContext hook was called outside of the GalleryContext provider. Make sure this hook is called within a child of the Gallery component.`
    );
    return {};
  }
  return contextValue;
};
var GalleryHeader = ({ currentItem }) => {
  const { t: t2 } = useTranslationContext();
  const { MessageTimestamp: MessageTimestamp$1 = MessageTimestamp } = useComponentContext();
  const { isMyMessage, message } = useMessageContext();
  const modalContext = (0, import_react29.useContext)(ModalContext);
  const headerTitle = isMyMessage?.() && t2("You") || message?.user?.name || message?.user?.id || currentItem.title || t2("User uploaded content");
  const downloadUrl = (0, import_react29.useMemo)(() => {
    const rawDownloadUrl = currentItem.videoUrl ?? currentItem.imageUrl;
    if (!rawDownloadUrl) return void 0;
    const sanitizedUrl = (0, import_sanitize_url.sanitizeUrl)(rawDownloadUrl);
    return sanitizedUrl === "about:blank" ? void 0 : sanitizedUrl;
  }, [currentItem.imageUrl, currentItem.videoUrl]);
  const downloadLabel = t2("aria/Download attachment");
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__gallery__header", children: [
    (0, import_jsx_runtime4.jsx)("div", { "aria-hidden": "true", className: "str-chat__gallery__header-spacer" }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__gallery__header-meta", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__gallery__title", children: headerTitle }),
      message?.created_at ? (0, import_jsx_runtime4.jsx)(MessageTimestamp$1, { customClass: "str-chat__gallery__timestamp" }) : null
    ] }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__gallery__header-actions", children: [
      downloadUrl ? (0, import_jsx_runtime4.jsx)(
        "a",
        {
          "aria-label": downloadLabel,
          className: "str-chat__gallery__action-button str-chat__gallery__action-button--download",
          download: true,
          href: downloadUrl,
          rel: "noreferrer",
          target: "_blank",
          title: downloadLabel,
          children: (0, import_jsx_runtime4.jsx)(IconArrowDownCircle, {})
        }
      ) : null,
      modalContext?.close ? (0, import_jsx_runtime4.jsx)(
        Button,
        {
          "aria-label": t2("Close"),
          className: "str-chat__gallery__action-button str-chat__gallery__action-button--close",
          onClick: modalContext.close,
          title: t2("Close"),
          children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
        }
      ) : null
    ] })
  ] });
};
var VideoPlayer = ({ isPlaying, thumbnailUrl, videoUrl }) => {
  const { VideoPlayer: VideoPlayerContext } = useComponentContext();
  return VideoPlayerContext ? (0, import_jsx_runtime4.jsx)(VideoPlayerContext, { thumbnailUrl, videoUrl }) : (0, import_jsx_runtime4.jsx)(
    import_react_player.default,
    {
      className: "react-player",
      config: { file: { attributes: { poster: thumbnailUrl } } },
      controls: true,
      height: "100%",
      playing: isPlaying,
      url: videoUrl,
      width: "100%"
    }
  );
};
var VideoThumbnail = ({
  className,
  onPlay,
  ...imageProps
}) => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment__video-thumbnail", children: [
    (0, import_jsx_runtime4.jsx)(
      BaseImage,
      {
        className: clsx_default("str-chat__message-attachment__video-thumbnail-image", className),
        ...imageProps
      }
    ),
    onPlay ? (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "solid",
        "aria-label": t2("Play video"),
        circular: true,
        className: clsx_default(
          "str-chat__message-attachment__video-thumbnail__play-indicator"
        ),
        onClick: onPlay,
        size: "lg",
        variant: "secondary",
        children: (0, import_jsx_runtime4.jsx)(IconPlayFill, {})
      }
    ) : (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__video-thumbnail__play-indicator", children: (0, import_jsx_runtime4.jsx)(IconPlayFill, {}) })
  ] });
};
var SWIPE_THRESHOLD = 50;
var TRANSITION_DURATION = 300;
var GalleryUI = () => {
  const { t: t2 } = useTranslationContext();
  const {
    closeOnBackgroundClick,
    currentIndex,
    currentItem,
    goToNext,
    goToPrevious,
    hasNext,
    hasPrevious,
    itemCount,
    onRequestClose
  } = useGalleryContext();
  const modalContext = (0, import_react29.useContext)(ModalContext);
  const [showVideo, setShowVideo] = (0, import_react29.useState)(false);
  const isTransitioningRef = (0, import_react29.useRef)(false);
  const [slideOffset, setSlideOffset] = (0, import_react29.useState)(0);
  const [isDragging, setIsDragging] = (0, import_react29.useState)(false);
  const [slideDirection, setSlideDirection] = (0, import_react29.useState)(
    null
  );
  const ignoreNextClickRef = (0, import_react29.useRef)(false);
  const touchStartRef = (0, import_react29.useRef)(null);
  const isVerticalSwipeRef = (0, import_react29.useRef)(false);
  const containerRef = (0, import_react29.useRef)(null);
  (0, import_react29.useEffect)(() => {
    setShowVideo(false);
  }, [currentIndex]);
  const prevIndexRef = (0, import_react29.useRef)(currentIndex);
  (0, import_react29.useEffect)(() => {
    if (prevIndexRef.current === currentIndex) return;
    const direction = currentIndex > prevIndexRef.current ? "forward" : "backward";
    setSlideDirection(direction);
    setSlideOffset(0);
    setIsDragging(false);
    isTransitioningRef.current = true;
    const timer = setTimeout(() => {
      setSlideDirection(null);
      isTransitioningRef.current = false;
    }, TRANSITION_DURATION);
    prevIndexRef.current = currentIndex;
    return () => clearTimeout(timer);
  }, [currentIndex]);
  const handleGoToNext = (0, import_react29.useCallback)(() => {
    if (isTransitioningRef.current) return;
    goToNext();
  }, [goToNext]);
  const handleGoToPrevious = (0, import_react29.useCallback)(() => {
    if (isTransitioningRef.current) return;
    goToPrevious();
  }, [goToPrevious]);
  const handleKeyDown = (0, import_react29.useCallback)(
    (event) => {
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        handleGoToPrevious();
      } else if (event.key === "ArrowRight") {
        event.preventDefault();
        handleGoToNext();
      }
    },
    [handleGoToNext, handleGoToPrevious]
  );
  (0, import_react29.useEffect)(() => {
    document.addEventListener("keydown", handleKeyDown);
    return () => document.removeEventListener("keydown", handleKeyDown);
  }, [handleKeyDown]);
  const handleTouchStart = (0, import_react29.useCallback)((event) => {
    if (isTransitioningRef.current) return;
    const touch = event.touches[0];
    ignoreNextClickRef.current = false;
    touchStartRef.current = { x: touch.clientX, y: touch.clientY };
    isVerticalSwipeRef.current = false;
  }, []);
  const handleTouchMove = (0, import_react29.useCallback)(
    (event) => {
      if (!touchStartRef.current || isTransitioningRef.current) return;
      const touch = event.touches[0];
      const deltaX = touch.clientX - touchStartRef.current.x;
      const deltaY = touch.clientY - touchStartRef.current.y;
      if (!isDragging && !isVerticalSwipeRef.current) {
        if (Math.abs(deltaY) > Math.abs(deltaX) && Math.abs(deltaY) > 10) {
          ignoreNextClickRef.current = true;
          isVerticalSwipeRef.current = true;
          return;
        }
        if (Math.abs(deltaX) > 10) {
          ignoreNextClickRef.current = true;
          setIsDragging(true);
        }
      }
      if (isVerticalSwipeRef.current) return;
      if (!hasNext && deltaX < 0 || !hasPrevious && deltaX > 0) {
        setSlideOffset(deltaX * 0.3);
      } else {
        setSlideOffset(deltaX);
      }
    },
    [isDragging, hasNext, hasPrevious]
  );
  const handleTouchEnd = (0, import_react29.useCallback)(() => {
    if (!touchStartRef.current || isVerticalSwipeRef.current) {
      if (isVerticalSwipeRef.current) ignoreNextClickRef.current = true;
      touchStartRef.current = null;
      return;
    }
    const offset4 = slideOffset;
    if (isDragging || Math.abs(offset4) > 10) {
      ignoreNextClickRef.current = true;
    }
    touchStartRef.current = null;
    if (Math.abs(offset4) >= SWIPE_THRESHOLD) {
      if (offset4 < 0 && hasNext) {
        goToNext();
      } else if (offset4 > 0 && hasPrevious) {
        goToPrevious();
      } else {
        setSlideOffset(0);
      }
    } else {
      setSlideOffset(0);
    }
    setIsDragging(false);
  }, [slideOffset, hasNext, hasPrevious, goToNext, goToPrevious, isDragging]);
  const requestClose = modalContext?.close ?? onRequestClose;
  const handleBackgroundClick = (0, import_react29.useCallback)(
    (event) => {
      if (event.target !== event.currentTarget) return;
      if (ignoreNextClickRef.current) {
        ignoreNextClickRef.current = false;
        return;
      }
      if (!closeOnBackgroundClick) return;
      requestClose?.();
    },
    [closeOnBackgroundClick, requestClose]
  );
  const mediaStyle = isDragging || slideOffset !== 0 && slideDirection === null ? { transform: `translateX(${slideOffset}px)` } : {};
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__gallery", children: [
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__gallery__main", children: [
      (0, import_jsx_runtime4.jsx)(GalleryHeader, { currentItem }),
      (0, import_jsx_runtime4.jsx)(
        NavButton,
        {
          "aria-label": t2("Previous image"),
          className: clsx_default(
            "str-chat__gallery__nav-button--prev",
            !hasPrevious && "str-chat__gallery__nav-button--hidden"
          ),
          disabled: !hasPrevious,
          onClick: handleGoToPrevious,
          children: (0, import_jsx_runtime4.jsx)(IconChevronLeft, {})
        }
      ),
      (0, import_jsx_runtime4.jsx)(
        "div",
        {
          className: "str-chat__gallery__slide-container",
          onClick: handleBackgroundClick,
          onTouchEnd: handleTouchEnd,
          onTouchMove: handleTouchMove,
          onTouchStart: handleTouchStart,
          ref: containerRef,
          children: (0, import_jsx_runtime4.jsx)(
            "div",
            {
              className: clsx_default("str-chat__gallery__media-container", {
                "str-chat__gallery__media--dragging": isDragging,
                "str-chat__gallery__media--slide-backward": !isDragging && slideDirection === "backward",
                "str-chat__gallery__media--slide-forward": !isDragging && slideDirection === "forward"
              }),
              style: mediaStyle,
              children: currentItem.videoUrl && currentItem.videoThumbnailUrl ? (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__gallery__media str-chat__gallery__media--video", children: showVideo ? (0, import_jsx_runtime4.jsx)(VideoPlayer, { isPlaying: true, videoUrl: currentItem.videoUrl }) : (0, import_jsx_runtime4.jsx)(
                VideoThumbnail,
                {
                  alt: currentItem.title ?? "",
                  onPlay: () => setShowVideo(true),
                  src: currentItem.videoThumbnailUrl
                }
              ) }) : (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__gallery__media str-chat__gallery__media--image", children: (0, import_jsx_runtime4.jsx)(BaseImage, { alt: currentItem.alt, src: currentItem.imageUrl }) })
            }
          )
        }
      ),
      (0, import_jsx_runtime4.jsx)(
        NavButton,
        {
          "aria-label": t2("Next image"),
          className: clsx_default(
            "str-chat__gallery__nav-button--next",
            !hasNext && "str-chat__gallery__nav-button--hidden"
          ),
          disabled: !hasNext,
          onClick: handleGoToNext,
          children: (0, import_jsx_runtime4.jsx)(IconChevronRight, {})
        }
      )
    ] }),
    itemCount > 1 && (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__gallery__position-indicator", children: [
      currentIndex + 1,
      " of ",
      itemCount
    ] })
  ] });
};
var NavButton = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)(Button, { ...props, className: clsx_default("str-chat__gallery__nav-button", className) });
var Gallery = ({
  closeOnBackgroundClick = true,
  GalleryUI: GalleryUI$1,
  initialIndex = 0,
  items,
  onIndexChange,
  onRequestClose
}) => {
  const { GalleryUI: ContextGalleryUI } = useComponentContext();
  const ResolvedGalleryUI = GalleryUI$1 ?? ContextGalleryUI ?? GalleryUI;
  const [currentIndex, setCurrentIndex] = (0, import_react29.useState)(initialIndex);
  const itemCount = items.length;
  const goToIndex = (0, import_react29.useCallback)(
    (index6) => {
      if (index6 >= 0 && index6 < itemCount) {
        setCurrentIndex(index6);
      }
    },
    [itemCount]
  );
  const goToNext = (0, import_react29.useCallback)(() => {
    setCurrentIndex((prev) => prev < itemCount - 1 ? prev + 1 : prev);
  }, [itemCount]);
  const goToPrevious = (0, import_react29.useCallback)(() => {
    setCurrentIndex((prev) => prev > 0 ? prev - 1 : prev);
  }, []);
  (0, import_react29.useEffect)(() => {
    onIndexChange?.(currentIndex);
  }, [currentIndex, onIndexChange]);
  const hasNext = currentIndex < itemCount - 1;
  const hasPrevious = currentIndex > 0;
  const currentItem = items[currentIndex];
  const contextValue = (0, import_react29.useMemo)(
    () => ({
      closeOnBackgroundClick,
      currentIndex,
      currentItem,
      goToIndex,
      goToNext,
      goToPrevious,
      hasNext,
      hasPrevious,
      itemCount,
      items,
      onRequestClose
    }),
    [
      closeOnBackgroundClick,
      currentIndex,
      currentItem,
      goToIndex,
      goToNext,
      goToPrevious,
      hasNext,
      hasPrevious,
      itemCount,
      items,
      onRequestClose
    ]
  );
  return (0, import_jsx_runtime4.jsx)(GalleryContext.Provider, { value: contextValue, children: (0, import_jsx_runtime4.jsx)(ResolvedGalleryUI, {}) });
};
var AttachmentPreviewList = ({
  AudioAttachmentPreview: AudioAttachmentPreview2 = FileAttachmentPreview,
  FileAttachmentPreview: FileAttachmentPreview$1 = FileAttachmentPreview,
  ImageAttachmentPreview = MediaAttachmentPreview,
  UnsupportedAttachmentPreview: UnsupportedAttachmentPreview$1 = UnsupportedAttachmentPreview,
  VideoAttachmentPreview = MediaAttachmentPreview
}) => {
  const messageComposer = useMessageComposerController();
  const { Modal = GlobalModal } = useComponentContext();
  const [showPreview, setShowPreview] = (0, import_react29.useState)(false);
  const initialIndexRef = (0, import_react29.useRef)(0);
  const { attachments } = useAttachmentsForPreview();
  const filteredAttachments = (0, import_react29.useMemo)(
    () => attachments.filter((a2) => !isVoiceRecordingAttachment(a2)),
    [attachments]
  );
  const { galleryItems, previewIndexById } = (0, import_react29.useMemo)(() => {
    const items = [];
    const indexById = {};
    for (const a2 of attachments) {
      if (isLocalImageAttachment(a2) || isLocalVideoAttachment(a2)) {
        const descriptor = toBaseImageDescriptors(a2);
        if (descriptor) {
          indexById[a2.localMetadata.id] = items.length;
          items.push(descriptor);
        }
      }
    }
    return { galleryItems: items, previewIndexById: indexById };
  }, [attachments]);
  const openPreviewAtIndex = (0, import_react29.useCallback)((index6) => {
    initialIndexRef.current = index6;
    setShowPreview(true);
  }, []);
  if (!filteredAttachments.length) return null;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__attachment-preview-list",
      "data-testid": "attachment-preview-list",
      children: [
        attachments.map((attachment) => {
          if (isScrapedContent(attachment)) return null;
          if (isLocalVoiceRecordingAttachment(attachment)) return null;
          if (isLocalAudioAttachment(attachment)) {
            return (0, import_jsx_runtime4.jsx)(
              AudioAttachmentPreview2,
              {
                attachment,
                handleRetry: messageComposer.attachmentManager.uploadAttachment,
                removeAttachments: messageComposer.attachmentManager.removeAttachments
              },
              attachment.localMetadata.id || attachment.asset_url
            );
          } else if (isLocalVideoAttachment(attachment)) {
            return (0, import_jsx_runtime4.jsx)(
              VideoAttachmentPreview,
              {
                attachment,
                handleRetry: messageComposer.attachmentManager.uploadAttachment,
                openPreview: () => openPreviewAtIndex(previewIndexById[attachment.localMetadata.id] ?? 0),
                removeAttachments: messageComposer.attachmentManager.removeAttachments
              },
              attachment.localMetadata.id || attachment.asset_url
            );
          } else if (isLocalImageAttachment(attachment)) {
            return (0, import_jsx_runtime4.jsx)(
              ImageAttachmentPreview,
              {
                attachment,
                handleRetry: messageComposer.attachmentManager.uploadAttachment,
                openPreview: () => openPreviewAtIndex(previewIndexById[attachment.localMetadata.id] ?? 0),
                removeAttachments: messageComposer.attachmentManager.removeAttachments
              },
              attachment.localMetadata.id || attachment.image_url
            );
          } else if (isLocalFileAttachment(attachment)) {
            return (0, import_jsx_runtime4.jsx)(
              FileAttachmentPreview$1,
              {
                attachment,
                handleRetry: messageComposer.attachmentManager.uploadAttachment,
                removeAttachments: messageComposer.attachmentManager.removeAttachments
              },
              attachment.localMetadata.id || attachment.asset_url
            );
          } else if (isLocalAttachment(attachment)) {
            return (0, import_jsx_runtime4.jsx)(
              UnsupportedAttachmentPreview$1,
              {
                attachment,
                handleRetry: messageComposer.attachmentManager.uploadAttachment,
                removeAttachments: messageComposer.attachmentManager.removeAttachments
              },
              attachment.localMetadata.id
            );
          }
          return null;
        }),
        galleryItems.length > 0 && (0, import_jsx_runtime4.jsx)(
          Modal,
          {
            className: "str-chat__gallery-modal",
            onClose: () => setShowPreview(false),
            open: showPreview,
            children: (0, import_jsx_runtime4.jsx)(Gallery, { initialIndex: initialIndexRef.current, items: galleryItems })
          }
        )
      ]
    }
  );
};
var VoiceRecordingPreviewSlot = ({
  VoiceRecordingPreview = AudioAttachmentPreview
}) => {
  const messageComposer = useMessageComposerController();
  const { attachments } = useAttachmentsForPreview();
  const voiceAttachments = attachments.filter(isLocalVoiceRecordingAttachment);
  const firstVoice = voiceAttachments[0];
  if (!firstVoice) return null;
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__message-composer-voice-preview-slot",
      "data-testid": "voice-preview-slot",
      children: (0, import_jsx_runtime4.jsx)(
        VoiceRecordingPreview,
        {
          attachment: firstVoice,
          handleRetry: messageComposer.attachmentManager.uploadAttachment,
          removeAttachments: messageComposer.attachmentManager.removeAttachments
        }
      )
    }
  );
};
var CommandChip = ({ command }) => {
  const { textComposer } = useMessageComposerController();
  const { textareaRef } = useMessageComposerContext();
  if (!command) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__command-chip", children: [
    (0, import_jsx_runtime4.jsx)(IconBolt, {}),
    (0, import_jsx_runtime4.jsx)("span", { children: command.name }),
    (0, import_jsx_runtime4.jsx)(
      "button",
      {
        className: "str-chat__command-chip__close-button",
        onClick: () => {
          textComposer.setCommand(null);
          textareaRef.current?.focus();
        },
        children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
      }
    )
  ] });
};
var CooldownTimer = () => {
  const secondsLeft = useCooldownRemaining();
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-composer-cooldown", "data-testid": "cooldown-timer", children: secondsLeft });
};
var UploadIcon = () => {
  const { t: t2 } = useTranslationContext("UploadIcon");
  return (0, import_jsx_runtime4.jsxs)(
    "svg",
    {
      "data-testid": "attach-icon",
      fill: "none",
      height: "24",
      viewBox: "0 0 24 24",
      width: "24",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        (0, import_jsx_runtime4.jsx)("title", { children: t2("Attach files") }),
        (0, import_jsx_runtime4.jsx)("g", { clipPath: "url(#clip0_10878_5)", children: (0, import_jsx_runtime4.jsx)(
          "path",
          {
            d: "M12.9997 6.99993L10.9997 6.99993L10.9997 10.9999L6.99972 10.9999L6.99972 12.9999L10.9997 12.9999L10.9997 16.9999L12.9997 16.9999L12.9997 12.9999L16.9997 12.9999L16.9997 10.9999L12.9997 10.9999L12.9997 6.99993ZM11.9997 1.99992C6.47972 1.99992 1.99972 6.47993 1.99972 11.9999C1.99972 17.5199 6.47972 21.9999 11.9997 21.9999C17.5197 21.9999 21.9997 17.5199 21.9997 11.9999C21.9997 6.47993 17.5197 1.99992 11.9997 1.99992ZM11.9997 19.9999C7.58972 19.9999 3.99972 16.4099 3.99972 11.9999C3.99972 7.58993 7.58972 3.99993 11.9997 3.99993C16.4097 3.99993 19.9997 7.58993 19.9997 11.9999C19.9997 16.4099 16.4097 19.9999 11.9997 19.9999Z",
            fill: "black"
          }
        ) }),
        (0, import_jsx_runtime4.jsx)("defs", { children: (0, import_jsx_runtime4.jsx)("clipPath", { id: "clip0_10878_5", children: (0, import_jsx_runtime4.jsx)("rect", { fill: "white", height: "24", width: "24" }) }) })
      ]
    }
  );
};
var BinIcon = () => (0, import_jsx_runtime4.jsx)("svg", { fill: "currentColor", viewBox: "0 0 32 32", xmlns: "http://www.w3.org/2000/svg", children: (0, import_jsx_runtime4.jsx)("path", { d: "M8.00033 25.3333C8.00033 26.8 9.20033 28 10.667 28H21.3337C22.8003 28 24.0003 26.8 24.0003 25.3333V12C24.0003 10.5333 22.8003 9.33333 21.3337 9.33333H10.667C9.20033 9.33333 8.00033 10.5333 8.00033 12V25.3333ZM24.0003 5.33333H20.667L19.7203 4.38667C19.4803 4.14667 19.1337 4 18.787 4H13.2137C12.867 4 12.5203 4.14667 12.2803 4.38667L11.3337 5.33333H8.00033C7.26699 5.33333 6.66699 5.93333 6.66699 6.66667C6.66699 7.4 7.26699 8 8.00033 8H24.0003C24.7337 8 25.3337 7.4 25.3337 6.66667C25.3337 5.93333 24.7337 5.33333 24.0003 5.33333Z" }) });
var PauseIcon = () => (0, import_jsx_runtime4.jsx)(
  "svg",
  {
    "data-testid": "str-chat__pause-icon",
    fill: "currentColor",
    viewBox: "0 0 16 20",
    xmlns: "http://www.w3.org/2000/svg",
    children: (0, import_jsx_runtime4.jsx)("path", { d: "M0 19.3333H5.33333V0.666626H0V19.3333ZM10.6667 0.666626V19.3333H16V0.666626H10.6667Z" })
  }
);
var PlayIcon = () => (0, import_jsx_runtime4.jsx)(
  "svg",
  {
    "data-testid": "str-chat__play-icon",
    fill: "currentColor",
    viewBox: "0 0 14 18",
    xmlns: "http://www.w3.org/2000/svg",
    children: (0, import_jsx_runtime4.jsx)("path", { d: "M0.236328 2.09338V15.9067C0.236328 16.9601 1.39633 17.6001 2.28966 17.0267L13.143 10.1201C13.9697 9.60005 13.9697 8.40005 13.143 7.86672L2.28966 0.973385C1.39633 0.400051 0.236328 1.04005 0.236328 2.09338Z" })
  }
);
var CheckSignIcon = () => (0, import_jsx_runtime4.jsx)("svg", { fill: "currentColor", viewBox: "0 0 18 14", xmlns: "http://www.w3.org/2000/svg", children: (0, import_jsx_runtime4.jsx)("path", { d: "M5.79457 10.875L2.32457 7.40502C1.93457 7.01502 1.30457 7.01502 0.91457 7.40502C0.52457 7.79502 0.52457 8.42502 0.91457 8.81502L5.09457 12.995C5.48457 13.385 6.11457 13.385 6.50457 12.995L17.0846 2.41502C17.4746 2.02502 17.4746 1.39502 17.0846 1.00502C16.6946 0.615024 16.0646 0.615024 15.6746 1.00502L5.79457 10.875Z" }) });
var linkPreviewsManagerStateSelector$1 = (state) => ({
  linkPreviews: Array.from(state.previews.values()).filter(
    (preview) => LinkPreviewsManager.previewIsLoaded(preview) || LinkPreviewsManager.previewIsLoading(preview)
  )
});
var LinkPreviewList = ({ displayLinkCount = 1 }) => {
  const messageComposer = useMessageComposerController();
  const { linkPreviewsManager } = messageComposer;
  const { linkPreviews } = useStateStore(
    linkPreviewsManager.state,
    linkPreviewsManagerStateSelector$1
  );
  if (linkPreviews.length === 0) return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__link-preview-list", children: linkPreviews.slice(0, displayLinkCount).map((linkPreview) => (0, import_jsx_runtime4.jsx)(LinkPreviewCard, { linkPreview }, linkPreview.og_scrape_url)) });
};
var LinkPreviewCard = ({ linkPreview }) => {
  const { linkPreviewsManager } = useMessageComposerController();
  const { handleEnter, handleLeave, tooltipVisible } = useEnterLeaveHandlers();
  const [referenceElement, setReferenceElement] = (0, import_react29.useState)(null);
  const { image_url, thumb_url, title, title_link } = linkPreview;
  if (!LinkPreviewsManager.previewIsLoaded(linkPreview) && !LinkPreviewsManager.previewIsLoading(linkPreview))
    return null;
  const previewImageSrc = title_link || image_url || thumb_url;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__link-preview-card", {
        "str-chat__link-preview-card--loading": LinkPreviewsManager.previewIsLoading(linkPreview)
      }),
      "data-testid": "link-preview-card",
      onMouseEnter: handleEnter,
      onMouseLeave: handleLeave,
      ref: setReferenceElement,
      children: [
        (0, import_jsx_runtime4.jsx)(
          PopperTooltip,
          {
            offset: [0, 5],
            referenceElement,
            visible: tooltipVisible,
            children: linkPreview.og_scrape_url
          }
        ),
        previewImageSrc && (0, import_jsx_runtime4.jsx)(
          BaseImage,
          {
            alt: title,
            className: "str-chat__attachment-preview__thumbnail",
            src: previewImageSrc,
            title
          }
        ),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__link-preview-card__content", children: [
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__link-preview-card__content-title", children: linkPreview.title }),
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__link-preview-card__content-description", children: linkPreview.text }),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__link-preview-card__content__url", children: [
            (0, import_jsx_runtime4.jsx)(IconLink, {}),
            (0, import_jsx_runtime4.jsx)("span", { children: linkPreview.og_scrape_url })
          ] })
        ] }),
        (0, import_jsx_runtime4.jsx)(
          RemoveAttachmentPreviewButton,
          {
            className: "str-chat__link-preview-card__dismiss-button",
            "data-testid": "link-preview-card-dismiss-btn",
            onClick: () => linkPreviewsManager.dismissPreview(linkPreview.og_scrape_url)
          }
        )
      ]
    }
  );
};
var RecordingPermissionDeniedNotification = ({
  permissionName
}) => {
  const { t: t2 } = useTranslationContext();
  const permissionTranslations = {
    body: {
      camera: t2("To start recording, allow the camera access in your browser"),
      microphone: t2("To start recording, allow the microphone access in your browser")
    },
    heading: {
      camera: t2("Allow access to camera"),
      microphone: t2("Allow access to microphone")
    }
  };
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__recording-permission-denied-notification", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__recording-permission-denied-notification__heading", children: permissionTranslations.heading[permissionName] }),
    (0, import_jsx_runtime4.jsx)("p", { className: "str-chat__recording-permission-denied-notification__message", children: permissionTranslations.body[permissionName] })
  ] });
};
var audioPlayerStateSelector$2 = (state) => ({
  isPlaying: state.isPlaying,
  progress: state.progressPercent,
  secondsElapsed: state.secondsElapsed
});
var AudioRecordingPlayback = ({
  durationSeconds,
  mimeType,
  src,
  waveformData
}) => {
  const audioPlayer = useAudioPlayer({
    durationSeconds,
    mimeType,
    src,
    waveformData
  });
  const { isPlaying, progress, secondsElapsed } = useStateStore(audioPlayer?.state, audioPlayerStateSelector$2) ?? {};
  const displayedDuration = secondsElapsed || durationSeconds;
  (0, import_react29.useEffect)(() => {
    audioPlayer?.cancelScheduledRemoval();
    return () => {
      audioPlayer?.scheduleRemoval();
    };
  }, [audioPlayer]);
  if (!audioPlayer) return;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__audio-recorder__recording-playback", {
        "str-chat__audio-recorder__recording-playback--isPlaying": isPlaying
      }),
      children: [
        (0, import_jsx_runtime4.jsx)(
          Button,
          {
            appearance: "ghost",
            circular: true,
            className: "str-chat__audio_recorder__toggle-playback-button",
            "data-testid": "audio-recording-preview-toggle-play-btn",
            onClick: audioPlayer.togglePlay,
            size: "sm",
            variant: "secondary",
            children: isPlaying ? (0, import_jsx_runtime4.jsx)(IconPauseFill, {}) : (0, import_jsx_runtime4.jsx)(IconPlayFill, {})
          }
        ),
        (0, import_jsx_runtime4.jsx)(
          DurationDisplay,
          {
            className: clsx_default("str-chat__recording-timer", {
              "str-chat__recording-timer--hours": displayedDuration >= 3600
            }),
            duration: durationSeconds,
            isPlaying: !!isPlaying,
            secondsElapsed
          }
        ),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__wave-progress-bar__track-container", children: (0, import_jsx_runtime4.jsx)(
          WaveProgressBar,
          {
            progress,
            seek: audioPlayer.seek,
            waveformData: waveformData || []
          }
        ) })
      ]
    }
  );
};
var useTimeElapsed = ({
  initialSeconds = 0,
  startOnMount
} = {}) => {
  const [secondsElapsed, setSecondsElapsed] = (0, import_react29.useState)(initialSeconds);
  const updateInterval = (0, import_react29.useRef)(void 0);
  const startCounter = (0, import_react29.useCallback)(() => {
    if (updateInterval.current) return;
    updateInterval.current = setInterval(() => {
      setSecondsElapsed((prev) => prev + 1);
    }, 1e3);
  }, []);
  const stopCounter = (0, import_react29.useCallback)(() => {
    clearInterval(updateInterval.current);
    updateInterval.current = void 0;
  }, []);
  (0, import_react29.useEffect)(() => {
    if (updateInterval.current) return;
    setSecondsElapsed(initialSeconds);
  }, [initialSeconds]);
  (0, import_react29.useEffect)(() => {
    if (!startOnMount) return;
    startCounter();
    return () => {
      stopCounter();
    };
  }, [startCounter, startOnMount, stopCounter]);
  return {
    secondsElapsed,
    startCounter,
    stopCounter
  };
};
var RecordingTimer = ({ durationSeconds }) => (0, import_jsx_runtime4.jsx)(
  "div",
  {
    className: clsx_default("str-chat__recording-timer", {
      "str-chat__recording-timer--hours": durationSeconds >= 3600
    }),
    children: displayDuration(durationSeconds)
  }
);
var AudioRecordingWaveform = ({ maxDataPointsDrawn = 200 }) => {
  const {
    recordingController: { recorder }
  } = useMessageComposerContext();
  const [amplitudes, setAmplitudes] = (0, import_react29.useState)([]);
  (0, import_react29.useEffect)(() => {
    if (!recorder?.amplitudeRecorder) return;
    const amplitudesSubscription = recorder.amplitudeRecorder.amplitudes.subscribe(setAmplitudes);
    return () => {
      amplitudesSubscription.unsubscribe();
    };
  }, [recorder]);
  if (!recorder) return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__waveform-box-container", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__wave-progress-bar__track", children: amplitudes.slice(-maxDataPointsDrawn).map((amplitude, i2) => (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__wave-progress-bar__amplitude-bar",
      style: {
        "--str-chat__wave-progress-bar__amplitude-bar-height": amplitude ? amplitude * 100 + "%" : "0%"
      }
    },
    `amplitude-${i2}-voice-recording`
  )) }) });
};
var AudioRecordingPreview = () => {
  const {
    recordingController: { recorder }
  } = useMessageComposerContext();
  const initialSeconds = recorder?.durationMs ? recorder.durationMs / 1e3 : 0;
  const { secondsElapsed, startCounter, stopCounter } = useTimeElapsed({
    initialSeconds
  });
  (0, import_react29.useEffect)(() => {
    if (!recorder?.mediaRecorder) return;
    const { mediaRecorder } = recorder;
    if (mediaRecorder.state === "recording") {
      startCounter();
    }
    mediaRecorder.addEventListener("start", startCounter);
    mediaRecorder.addEventListener("resume", startCounter);
    mediaRecorder.addEventListener("stop", stopCounter);
    mediaRecorder.addEventListener("pause", stopCounter);
    return () => {
      mediaRecorder.removeEventListener("start", startCounter);
      mediaRecorder.removeEventListener("resume", startCounter);
      mediaRecorder.removeEventListener("stop", stopCounter);
      mediaRecorder.removeEventListener("pause", stopCounter);
    };
  }, [recorder, startCounter, stopCounter]);
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__audio-recorder__recording-preview", children: [
    (0, import_jsx_runtime4.jsx)(IconVoice, {}),
    (0, import_jsx_runtime4.jsx)(RecordingTimer, { durationSeconds: secondsElapsed }),
    (0, import_jsx_runtime4.jsx)(AudioRecordingWaveform, {})
  ] });
};
var isStopped = (recordingState) => recordingState === MediaRecordingState.STOPPED;
var isRecording = (recordingState) => recordingState === MediaRecordingState.RECORDING;
var ToggleRecordingButton = () => {
  const {
    recordingController: { recorder, recordingState }
  } = useMessageComposerContext();
  return (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "outline",
      circular: true,
      className: "str-chat__audio_recorder__toggle-recording-button",
      onClick: () => isRecording(recordingState) ? recorder?.pause() : recorder?.resume(),
      size: "sm",
      variant: "secondary",
      children: isRecording(recordingState) ? (0, import_jsx_runtime4.jsx)(IconPauseFill, {}) : (0, import_jsx_runtime4.jsx)(IconVoice, {})
    }
  );
};
var AudioRecorderRecordingControls = () => {
  const { addNotification } = useNotificationApi();
  const { t: t2 } = useTranslationContext();
  const {
    recordingController: { completeRecording, recorder, recording, recordingState }
  } = useMessageComposerContext();
  const isUploadingFile = recording?.localMetadata?.uploadState === "uploading";
  const uploadProgress = recording?.localMetadata?.uploadProgress;
  if (!recorder) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__audio_recorder__recording-controls", children: [
    !isRecording(recordingState) && (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "ghost",
        circular: true,
        className: "str-chat__audio_recorder__cancel-button",
        "data-testid": "cancel-recording-audio-button",
        disabled: isUploadingFile,
        onClick: () => {
          recorder.cancel();
          addNotification({
            emitter: "AudioRecorder",
            message: t2("Voice message deleted"),
            severity: "info",
            type: "audioRecording:cancel:success"
          });
        },
        size: "sm",
        variant: "secondary",
        children: (0, import_jsx_runtime4.jsx)(IconDelete, {})
      }
    ),
    (0, import_jsx_runtime4.jsx)(ToggleRecordingButton, {}),
    (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "solid",
        circular: true,
        className: "str-chat__audio_recorder__stop-button",
        "data-testid": "audio-recorder-stop-button",
        onClick: completeRecording,
        size: "sm",
        variant: "primary",
        children: isUploadingFile ? (0, import_jsx_runtime4.jsx)(UploadProgressIndicator, { uploadProgress }) : (0, import_jsx_runtime4.jsx)(CheckSignIcon, {})
      }
    )
  ] });
};
var AudioRecorder = () => {
  const {
    recordingController: { recorder, recording, recordingState }
  } = useMessageComposerContext();
  const state = (0, import_react29.useMemo)(
    () => ({
      paused: recordingState === MediaRecordingState.PAUSED,
      recording: recordingState === MediaRecordingState.RECORDING,
      stopped: recordingState === MediaRecordingState.STOPPED
    }),
    [recordingState]
  );
  if (!recorder) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__audio_recorder", "data-testid": "audio-recorder", children: [
    (isStopped(recordingState) || state.paused) && recording?.asset_url ? (0, import_jsx_runtime4.jsx)(
      AudioRecordingPlayback,
      {
        durationSeconds: recording.duration ?? 0,
        mimeType: recording.mime_type,
        src: recording.asset_url,
        waveformData: recording.waveform_data
      }
    ) : state.recording ? (0, import_jsx_runtime4.jsx)(AudioRecordingPreview, {}) : null,
    (0, import_jsx_runtime4.jsx)(AudioRecorderRecordingControls, {})
  ] });
};
var dialogId = "recording-permission-denied-notification";
var AudioRecordingButtonWithNotification = () => {
  const {
    RecordingPermissionDeniedNotification: RecordingPermissionDeniedNotification$1 = RecordingPermissionDeniedNotification,
    StartRecordingAudioButton: StartRecordingAudioButton2 = DefaultStartRecordingAudioButton
  } = useComponentContext();
  const { asyncMessagesMultiSendEnabled, recordingController } = useMessageComposerContext();
  const { attachments } = useAttachmentManagerState();
  const { dialog, dialogManager } = useDialogOnNearestManager({ id: dialogId });
  const audioRecordingBtnRef = (0, import_react29.useRef)(null);
  const isRecording2 = !!recordingController.recordingState;
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      StartRecordingAudioButton2,
      {
        disabled: isRecording2 || !asyncMessagesMultiSendEnabled && attachments.some((a2) => a2.type === RecordingAttachmentType.VOICE_RECORDING),
        onClick: () => {
          recordingController.recorder?.start();
          const recordingEnabled = !!(recordingController.recorder && navigator.mediaDevices);
          const shouldShowNotification = recordingEnabled && recordingController.permissionState === "denied";
          if (shouldShowNotification) dialog.open();
        },
        ref: audioRecordingBtnRef
      }
    ),
    (0, import_jsx_runtime4.jsx)(
      Callout,
      {
        className: "str-chat__recording-permission-denied-notification",
        dialogManagerId: dialogManager?.id,
        id: dialogId,
        onClose: dialog.close,
        placement: "top-start",
        referenceElement: audioRecordingBtnRef.current,
        children: (0, import_jsx_runtime4.jsx)(RecordingPermissionDeniedNotification$1, { permissionName: RecordingPermission.MIC })
      }
    )
  ] });
};
var DefaultStartRecordingAudioButton = (0, import_react29.forwardRef)(function StartRecordingAudioButton(props, ref) {
  return (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "ghost",
      "aria-label": "Start recording audio",
      circular: true,
      className: "str-chat__start-recording-audio-button",
      "data-testid": "start-recording-audio-button",
      size: "sm",
      variant: "secondary",
      ...props,
      ref,
      children: (0, import_jsx_runtime4.jsx)(IconVoice, {})
    }
  );
});
var EditedMessagePreview = ({
  message,
  onCancel
}) => (0, import_jsx_runtime4.jsx)(QuotedMessagePreviewUI, { onRemove: onCancel, quotedMessage: message });
var stateSelector = (state) => ({
  showReplyInChannel: state.showReplyInChannel
});
var SendToChannelCheckbox = () => {
  const { t: t2 } = useTranslationContext();
  const messageComposer = useMessageComposerController();
  const { showReplyInChannel } = useStateStore(messageComposer.state, stateSelector);
  if (messageComposer.editedMessage || !messageComposer.threadId) return null;
  const labelText = Object.keys(messageComposer.channel.state.members).length === 2 ? t2("Also send as a direct message") : t2("Also send in channel");
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default("str-chat__send-to-channel-checkbox__container", {
        "str-chat__send-to-channel-checkbox__container--checked": showReplyInChannel
      }),
      "data-testid": "send-to-channel-checkbox",
      children: (0, import_jsx_runtime4.jsxs)(
        "label",
        {
          className: "str-chat__send-to-channel-checkbox__field",
          htmlFor: "send-to-channel-checkbox",
          children: [
            (0, import_jsx_runtime4.jsx)(
              "input",
              {
                "aria-checked": showReplyInChannel,
                checked: showReplyInChannel,
                className: "str-chat__send-to-channel-checkbox__input",
                id: "send-to-channel-checkbox",
                onChange: () => messageComposer.toggleShowReplyInChannel(),
                type: "checkbox"
              }
            ),
            (0, import_jsx_runtime4.jsx)("span", { "aria-hidden": true, className: "str-chat__send-to-channel-checkbox__visual", children: (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__send-to-channel-checkbox__checkmark", children: (0, import_jsx_runtime4.jsx)(IconCheckmark, {}) }) }),
            (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__send-to-channel-checkbox__label", children: labelText })
          ]
        }
      )
    }
  );
};
var CommandItem = (props) => {
  const { entity, focused: _2, ...buttonProps } = props;
  if (!entity.name) return null;
  return (0, import_jsx_runtime4.jsx)(
    CommandContextMenuItem,
    {
      ...buttonProps,
      command: entity
    }
  );
};
var EmoticonItem = (props) => {
  const { className, entity, focused: _2, ...buttonProps } = props;
  const hasEntity = Object.keys(entity).length;
  if (!hasEntity) return null;
  const { parts, token } = entity.tokenizedDisplayName ?? {};
  return (0, import_jsx_runtime4.jsx)(
    EmojiContextMenuButton,
    {
      ...buttonProps,
      className: clsx_default("str-chat__emoji-item", className),
      emoji: entity.native,
      children: parts?.map(
        (part, i2) => part.toLowerCase() === token ? (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__emoji-item--highlight", children: part }, `part-${i2}`) : (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__emoji-item--part", children: part }, `part-${i2}`)
      ) ?? null
    }
  );
};
var SuggestionListItem = ({
  className,
  component: Component2,
  focused,
  item,
  onClick,
  onKeyDown,
  onMouseEnter,
  ...restProps
}) => {
  const { textComposer } = useMessageComposerController();
  const { textareaRef } = useMessageComposerContext();
  const componentRef = (0, import_react29.useRef)(null);
  const handleSelect = (0, import_react29.useCallback)(() => {
    textComposer.handleSelect(item);
    textareaRef.current?.focus();
  }, [item, textareaRef, textComposer]);
  (0, import_react29.useLayoutEffect)(() => {
    if (!focused) return;
    componentRef.current?.scrollIntoView({ behavior: "instant", block: "nearest" });
  }, [focused]);
  return (0, import_jsx_runtime4.jsx)(
    Component2,
    {
      ...restProps,
      className: clsx_default("str-chat__suggestion-list-item", className, {
        "str-chat__suggestion-list-item--selected": focused
      }),
      entity: item,
      focused,
      onClick: (e) => {
        handleSelect();
        onClick?.(e);
      },
      onKeyDown: (event) => {
        if (event.key === "Enter") handleSelect();
        onKeyDown?.(event);
      },
      onMouseEnter,
      ref: componentRef
    }
  );
};
var UserItem = ({ entity, focused: _2, ...buttonProps }) => {
  const hasEntity = !!Object.keys(entity).length;
  if (!hasEntity) return null;
  const { parts, token } = entity.tokenizedDisplayName;
  const renderName = () => parts.map((part, i2) => {
    const matches2 = part.toLowerCase() === token;
    const partWithHTMLSpacesAround = part.replace(/^\s+|\s+$/g, " ");
    return (0, import_jsx_runtime4.jsx)(
      "span",
      {
        className: clsx_default({
          "str-chat__emoji-item-part": !matches2,
          "str-chat__suggestion-item-part--match": matches2
        }),
        children: partWithHTMLSpacesAround
      },
      `part-${i2}`
    );
  });
  return (0, import_jsx_runtime4.jsx)(
    UserContextMenuButton,
    {
      ...buttonProps,
      imageUrl: entity.image,
      title: entity.name || entity.id,
      userName: entity.name || entity.id,
      children: renderName()
    }
  );
};
var CARET_MIRROR_CLASS = "str-chat__textarea-caret-mirror";
var CARET_MARKER_CLASS = "str-chat__textarea-caret-marker";
var getTextareaCaretRect = (textarea, selectionEnd) => {
  if (!textarea || typeof window === "undefined") return null;
  const caretIndex = Math.max(0, selectionEnd ?? textarea.selectionEnd ?? 0);
  const value = textarea.value ?? "";
  const valueBeforeCaret = value.slice(0, caretIndex);
  const valueAfterCaret = value.slice(caretIndex);
  const computedStyle = window.getComputedStyle(textarea);
  const mirror = document.createElement("div");
  mirror.className = CARET_MIRROR_CLASS;
  mirror.style.position = "absolute";
  mirror.style.visibility = "hidden";
  mirror.style.top = "0";
  mirror.style.left = "-9999px";
  mirror.style.whiteSpace = "pre-wrap";
  mirror.style.wordWrap = "break-word";
  for (const property of computedStyle) {
    mirror.style.setProperty(property, computedStyle.getPropertyValue(property));
  }
  mirror.textContent = valueBeforeCaret;
  const caretMarker = document.createElement("span");
  caretMarker.className = CARET_MARKER_CLASS;
  caretMarker.textContent = valueAfterCaret.length ? valueAfterCaret : ".";
  mirror.appendChild(caretMarker);
  document.body.appendChild(mirror);
  mirror.scrollTop = textarea.scrollTop;
  mirror.scrollLeft = textarea.scrollLeft;
  const textareaRect = textarea.getBoundingClientRect();
  const mirrorRect = mirror.getBoundingClientRect();
  const caretRect = caretMarker.getBoundingClientRect();
  document.body.removeChild(mirror);
  const left = textareaRect.left + (caretRect.left - mirrorRect.left);
  const top = textareaRect.top + (caretRect.top - mirrorRect.top);
  const height = caretRect.height || parseFloat(computedStyle.lineHeight) || 0;
  return new DOMRect(left, top, 0, height);
};
var textComposerStateSelector$3 = ({ selection, suggestions }) => ({
  selection,
  suggestions
});
var searchSourceStateSelector$5 = (nextValue) => ({
  items: nextValue.items ?? []
});
var defaultComponents$1 = {
  "/": (props) => (0, import_jsx_runtime4.jsx)(CommandItem, { ...props, entity: props.entity }),
  ":": (props) => (0, import_jsx_runtime4.jsx)(EmoticonItem, { ...props, entity: props.entity }),
  "@": (props) => (0, import_jsx_runtime4.jsx)(UserItem, { ...props, entity: props.entity })
};
var SuggestionList = ({
  className,
  closeOnClickOutside = true,
  containerClassName,
  focusedItemIndex,
  setFocusedItemIndex,
  suggestionItemComponents = defaultComponents$1
}) => {
  const {
    AutocompleteSuggestionItem = SuggestionListItem,
    ContextMenu: ContextMenuComponent = ContextMenu
  } = useComponentContext();
  const { textareaRef } = useMessageComposerContext();
  const messageComposer = useMessageComposerController();
  const { textComposer } = messageComposer;
  const { selection, suggestions } = useStateStore(
    textComposer.state,
    textComposerStateSelector$3
  );
  const { items } = useStateStore(suggestions?.searchSource.state, searchSourceStateSelector$5) ?? {};
  const [container, setContainer] = (0, import_react29.useState)(null);
  const caretRectRef = (0, import_react29.useRef)(null);
  const virtualCaretReference = (0, import_react29.useMemo)(
    () => ({
      getBoundingClientRect: () => caretRectRef.current ?? new DOMRect()
    }),
    []
  );
  const { refs, strategy, update, x: x2, y: y2 } = usePopoverPosition({
    allowFlip: false,
    offset: 8,
    placement: "top-start",
    // For top placements, the cross-axis is X; we need this to allow flipping the list to the right when it overflows the right edge.
    shiftOptions: { crossAxis: true }
  });
  const component = suggestions?.trigger ? suggestionItemComponents[suggestions?.trigger] : void 0;
  const contextMenuItems = (0, import_react29.useMemo)(() => {
    if (!component) return [];
    const sortedItems = suggestions?.searchSource.type === "commands" ? [...items ?? []].sort(
      (a2, b2) => String(a2.name ?? "").localeCompare(
        String(b2.name ?? "")
      )
    ) : items ?? [];
    return sortedItems.map((item, i2) => {
      const Item2 = ({ ...props }) => (0, import_react29.createElement)(
        AutocompleteSuggestionItem,
        {
          ...props,
          component,
          focused: focusedItemIndex === i2,
          item,
          key: item.id.toString(),
          onMouseEnter: () => setFocusedItemIndex?.(i2)
        }
      );
      return Item2;
    });
  }, [
    items,
    component,
    focusedItemIndex,
    setFocusedItemIndex,
    AutocompleteSuggestionItem,
    suggestions?.searchSource.type
  ]);
  const ItemsWrapper = (0, import_react29.useCallback)(
    ({ children }) => (0, import_jsx_runtime4.jsx)(
      InfiniteScrollPaginator,
      {
        loadNextOnScrollToBottom: suggestions?.searchSource.search,
        threshold: 100,
        children
      }
    ),
    [suggestions?.searchSource.search]
  );
  (0, import_react29.useEffect)(() => {
    if (!closeOnClickOutside || !suggestions || !container) return;
    const handleClick = (event) => {
      if (container.contains(event.target)) return;
      textComposer.closeSuggestions();
    };
    document.addEventListener("click", handleClick);
    return () => {
      document.removeEventListener("click", handleClick);
    };
  }, [closeOnClickOutside, suggestions, container, textComposer]);
  (0, import_react29.useEffect)(() => {
    refs.setFloating(container);
  }, [container, refs]);
  (0, import_react29.useLayoutEffect)(() => {
    if (!suggestions || !update) return;
    const updatePosition = () => {
      const rect = getTextareaCaretRect(textareaRef.current ?? null, selection?.end);
      if (!rect) {
        caretRectRef.current = null;
        refs.setReference(null);
        return;
      }
      caretRectRef.current = rect;
      virtualCaretReference.contextElement = textareaRef.current ?? void 0;
      refs.setReference(virtualCaretReference);
      update();
    };
    updatePosition();
  }, [
    container,
    items?.length,
    refs,
    selection?.end,
    suggestions,
    textareaRef,
    update,
    virtualCaretReference
  ]);
  if (!suggestions || !items?.length || !component) return null;
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default("str-chat__suggestion-list-container", containerClassName),
      ref: setContainer,
      style: {
        left: x2 ?? 0,
        position: strategy,
        top: y2 ?? 0,
        visibility: x2 == null || y2 == null ? "hidden" : void 0,
        zIndex: 1e3
      },
      children: (0, import_jsx_runtime4.jsx)(
        ContextMenuComponent,
        {
          className: clsx_default("str-chat__suggestion-list", className),
          Header: suggestions.searchSource.type === "commands" ? CommandsMenuHeader : void 0,
          items: contextMenuItems,
          ItemsWrapper,
          menuClassName: suggestions.searchSource.type === "commands" ? CommandsMenuClassName : void 0
        }
      )
    }
  );
};
var textComposerStateSelector$2 = ({ command }) => ({ command });
var useTextareaPlaceholder = ({
  placeholder
} = {}) => {
  const { t: t2 } = useTranslationContext();
  const { additionalTextareaProps } = useMessageComposerContext();
  const cooldownRemaining = useCooldownRemaining();
  const messageComposer = useMessageComposerController();
  const { command } = useStateStore(
    messageComposer.textComposer.state,
    textComposerStateSelector$2
  );
  const knownArgsTranslations = (0, import_react29.useMemo)(
    () => ({
      ban: t2("ban-command-args"),
      giphy: t2("giphy-command-args"),
      mute: t2("mute-command-args"),
      unban: t2("unban-command-args"),
      unmute: t2("unmute-command-args")
    }),
    [t2]
  );
  const commandArgs = command?.args && (knownArgsTranslations[command.name ?? ""] ?? t2(command.args));
  const commandPlaceholder = command?.name === "giphy" ? t2("Search GIFs") : commandArgs ?? void 0;
  const defaultPlaceholder = placeholder ?? additionalTextareaProps?.placeholder ?? t2("Type your message");
  if (cooldownRemaining) {
    return t2("Slow mode, wait {{ seconds }}s...", { seconds: cooldownRemaining });
  }
  return commandPlaceholder ?? defaultPlaceholder;
};
var textComposerStateSelector$1 = (state) => ({
  selection: state.selection,
  suggestions: state.suggestions,
  text: state.text
});
var searchSourceStateSelector$4 = (state) => ({
  isLoadingItems: state.isLoading,
  items: state.items
});
var configStateSelector = (state) => ({
  enabled: state.text.enabled
});
var messageComposerStateSelector$2 = (state) => ({
  quotedMessage: state.quotedMessage
});
var attachmentManagerStateSelector$1 = (state) => ({
  attachments: state.attachments
});
var defaultShouldSubmit = (event) => event.key === "Enter" && !event.shiftKey && !event.nativeEvent.isComposing;
var TextareaComposer = ({
  className,
  closeSuggestionsOnClickOutside,
  containerClassName,
  listClassName,
  maxRows: maxRowsProp,
  minRows: minRowsProp,
  onBlur,
  onChange,
  onKeyDown,
  onScroll,
  onSelect,
  placeholder: placeholderProp,
  shouldSubmit: shouldSubmitProp,
  ...restTextareaProps
}) => {
  const { AutocompleteSuggestionList = SuggestionList } = useComponentContext();
  const {
    additionalTextareaProps,
    focus,
    handleSubmit,
    maxRows: maxRowsContext,
    minRows: minRowsContext,
    onPaste,
    shouldSubmit: shouldSubmitContext,
    textareaRef
  } = useMessageComposerContext();
  const cooldownRemaining = useCooldownRemaining();
  const placeholder = useTextareaPlaceholder({ placeholder: placeholderProp });
  const maxRows = maxRowsProp ?? maxRowsContext ?? 10;
  const minRows = minRowsProp ?? minRowsContext;
  const shouldSubmit = shouldSubmitProp ?? shouldSubmitContext ?? defaultShouldSubmit;
  const messageComposer = useMessageComposerController();
  const { textComposer } = messageComposer;
  const { selection, suggestions, text: text22 } = useStateStore(
    textComposer.state,
    textComposerStateSelector$1
  );
  const autosizeRows = !text22 && minRows == null ? 1 : void 0;
  const textareaStyle = text22 ? void 0 : {
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap"
  };
  const { enabled } = useStateStore(messageComposer.configState, configStateSelector);
  const { quotedMessage } = useStateStore(
    messageComposer.state,
    messageComposerStateSelector$2
  );
  const { attachments } = useStateStore(
    messageComposer.attachmentManager.state,
    attachmentManagerStateSelector$1
  );
  const { isLoadingItems } = useStateStore(suggestions?.searchSource.state, searchSourceStateSelector$4) ?? {};
  const containerRef = (0, import_react29.useRef)(null);
  const [focusedItemIndex, setFocusedItemIndex] = (0, import_react29.useState)(0);
  const [isComposing, setIsComposing] = (0, import_react29.useState)(false);
  const changeHandler = (0, import_react29.useCallback)(
    (e) => {
      if (onChange) {
        onChange(e);
        return;
      }
      if (!textareaRef.current) return;
      textComposer.handleChange({
        selection: {
          end: textareaRef.current.selectionEnd,
          start: textareaRef.current.selectionStart
        },
        text: e.target.value
      });
    },
    [onChange, textComposer, textareaRef]
  );
  const onCompositionEnd = (0, import_react29.useCallback)(() => {
    setIsComposing(false);
  }, []);
  const onCompositionStart = (0, import_react29.useCallback)(() => {
    setIsComposing(true);
  }, []);
  const keyDownHandler = (0, import_react29.useCallback)(
    (event) => {
      if (onKeyDown) {
        onKeyDown(event);
        return;
      }
      if (textComposer.suggestions && textComposer.suggestions.searchSource.items?.length) {
        if (event.key === "Escape") return textComposer.closeSuggestions();
        const loadedItems = textComposer.suggestions.searchSource.items;
        if (event.key === "Enter") {
          event.preventDefault();
          textComposer.handleSelect(loadedItems[focusedItemIndex]);
        }
        if (event.key === "ArrowUp") {
          event.preventDefault();
          setFocusedItemIndex((prev) => {
            let nextIndex = prev - 1;
            if (suggestions?.searchSource.hasNext) {
              nextIndex = prev;
            } else if (nextIndex < 0) {
              nextIndex = loadedItems.length - 1;
            }
            return nextIndex;
          });
        }
        if (event.key === "ArrowDown") {
          event.preventDefault();
          setFocusedItemIndex((prev) => {
            let nextIndex = prev + 1;
            if (suggestions?.searchSource.hasNext) {
              nextIndex = prev;
            } else if (nextIndex >= loadedItems.length) {
              nextIndex = 0;
            }
            return nextIndex;
          });
        }
      } else if (shouldSubmit(event) && textareaRef.current && messageComposer.hasSendableData) {
        if (event.key === "Enter") {
          event.preventDefault();
        }
        handleSubmit();
      }
    },
    [
      focusedItemIndex,
      handleSubmit,
      messageComposer,
      onKeyDown,
      shouldSubmit,
      suggestions,
      textComposer,
      textareaRef
    ]
  );
  const scrollHandler = (0, import_react29.useCallback)(
    (event) => {
      if (onScroll) {
        onScroll(event);
      } else {
        textComposer.closeSuggestions();
      }
    },
    [onScroll, textComposer]
  );
  const setSelection = (0, import_react29.useCallback)(
    (e) => {
      onSelect?.(e);
      textComposer.setSelection({
        end: e.target.selectionEnd,
        start: e.target.selectionStart
      });
    },
    [onSelect, textComposer]
  );
  (0, import_react29.useEffect)(() => {
    if (textComposer.suggestions) {
      setFocusedItemIndex(0);
    }
  }, [textComposer.suggestions]);
  (0, import_react29.useEffect)(() => {
    const textareaIsFocused = textareaRef.current?.matches(":focus");
    if (!textareaRef.current || textareaIsFocused || !focus) return;
    textareaRef.current.focus();
  }, [attachments, focus, quotedMessage, textareaRef]);
  (0, import_react29.useLayoutEffect)(() => {
    const textarea = textareaRef.current;
    if (!textarea || isComposing) return;
    if (textarea.value !== text22) {
      textarea.value = text22;
    }
    const length = textarea.value.length;
    const start2 = Math.max(0, Math.min(selection.start, length));
    const end = Math.max(start2, Math.min(selection.end, length));
    if (textarea.selectionStart === start2 && textarea.selectionEnd === end) return;
    textarea.setSelectionRange(start2, end, "forward");
  }, [text22, selection.start, selection.end, isComposing, textareaRef]);
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("rta", "str-chat__textarea", containerClassName, {
        ["rta--loading"]: isLoadingItems
      }),
      ref: containerRef,
      children: [
        (0, import_jsx_runtime4.jsx)(
          index5,
          {
            ...{ ...additionalTextareaProps, ...restTextareaProps },
            "aria-label": placeholder,
            className: clsx_default(
              "rta__textarea",
              "str-chat__textarea__textarea str-chat__message-textarea",
              className
            ),
            "data-testid": "message-input",
            disabled: !enabled || !!cooldownRemaining,
            maxRows: autosizeRows ?? maxRows,
            minRows: autosizeRows ?? minRows,
            onBlur,
            onChange: changeHandler,
            onCompositionEnd,
            onCompositionStart,
            onKeyDown: keyDownHandler,
            onPaste,
            onScroll: scrollHandler,
            onSelect: setSelection,
            placeholder,
            ref: (ref) => {
              textareaRef.current = ref;
            },
            style: textareaStyle
          }
        ),
        !isComposing && (0, import_jsx_runtime4.jsx)(
          AutocompleteSuggestionList,
          {
            className: listClassName,
            closeOnClickOutside: closeSuggestionsOnClickOutside,
            focusedItemIndex,
            setFocusedItemIndex
          }
        )
      ]
    }
  );
};
var DragAndDropUploadContext = import_react29.default.createContext({
  subscribeToDrop: null
});
var useDragAndDropUploadContext = () => (0, import_react29.useContext)(DragAndDropUploadContext);
var useRegisterDropHandlers = () => {
  const { subscribeToDrop } = useDragAndDropUploadContext();
  const messageComposer = useMessageComposerController();
  (0, import_react29.useEffect)(() => {
    const unsubscribe = subscribeToDrop?.(messageComposer.attachmentManager.uploadFiles);
    return unsubscribe;
  }, [subscribeToDrop, messageComposer]);
};
var attachmentManagerConfigStateSelector = (state) => ({
  acceptedFiles: state.attachments.acceptedFiles,
  multipleUploads: state.attachments.maxNumberOfFilesPerMessage > 1
});
var WithDragAndDropUpload = ({
  children,
  className,
  component: Component2 = "div",
  style
}) => {
  const dropHandlersRef = (0, import_react29.useRef)(/* @__PURE__ */ new Set());
  const messageComposerContext = useMessageComposerContext();
  const dragAndDropUploadContext = useDragAndDropUploadContext();
  const messageComposer = useMessageComposerController();
  const { isUploadEnabled } = useAttachmentManagerState();
  const { acceptedFiles, multipleUploads } = useStateStore(
    messageComposer.configState,
    attachmentManagerConfigStateSelector
  );
  const isCooldownActive = useIsCooldownActive();
  const isWithinMessageComposerContext = Object.keys(messageComposerContext).length > 0;
  const accept = (0, import_react29.useMemo)(
    () => acceptedFiles.reduce((mediaTypeMap, mediaType) => {
      mediaTypeMap[mediaType] ?? (mediaTypeMap[mediaType] = []);
      return mediaTypeMap;
    }, {}),
    [acceptedFiles]
  );
  const subscribeToDrop = (0, import_react29.useCallback)((fn) => {
    dropHandlersRef.current.add(fn);
    return () => {
      dropHandlersRef.current.delete(fn);
    };
  }, []);
  const handleDrop = (0, import_react29.useCallback)((files) => {
    dropHandlersRef.current.forEach((fn) => fn(files));
  }, []);
  const {
    getRootProps,
    isDragActive,
    isDragReject: isDragRejected
  } = useDropzone({
    accept,
    // apply `disabled` rules if available, otherwise allow anything and
    // let the `uploadNewFiles` handle the limitations internally
    disabled: isWithinMessageComposerContext ? !isUploadEnabled || isCooldownActive : false,
    multiple: multipleUploads,
    noClick: true,
    onDrop: isWithinMessageComposerContext ? messageComposer.attachmentManager.uploadFiles : handleDrop
  });
  if (dragAndDropUploadContext.subscribeToDrop !== null) {
    return (0, import_jsx_runtime4.jsx)(Component2, { className, children });
  }
  const rootClassName2 = clsx_default("str-chat__dropzone-root", className);
  return (0, import_jsx_runtime4.jsx)(DragAndDropUploadContext.Provider, { value: { subscribeToDrop }, children: (0, import_jsx_runtime4.jsxs)(Component2, { ...getRootProps({ className: rootClassName2, style }), children: [
    isDragActive && (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: clsx_default("str-chat__dropzone-container", {
          "str-chat__dropzone-container--not-accepted": isDragRejected
        }),
        role: "presentation",
        children: (0, import_jsx_runtime4.jsx)(FileDragAndDropContent, { isDragRejected })
      }
    ),
    children
  ] }) });
};
var FileDragAndDropContent = ({
  isDragRejected
}) => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__dropzone-container__content", children: isDragRejected ? (0, import_jsx_runtime4.jsx)("p", { children: t2("Some of the files will not be accepted") }) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(IconUpload, {}),
    (0, import_jsx_runtime4.jsx)("p", { children: t2("Drag your files here") })
  ] }) });
};
var StopAIGenerationButton = ({
  onClick,
  ...restProps
}) => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)(
    "button",
    {
      "aria-label": t2("aria/Stop AI Generation"),
      className: "str-chat__stop-ai-generation-button",
      "data-testid": "stop-ai-generation-button",
      onClick,
      ...restProps
    }
  );
};
var SendButton = ({ children, sendMessage, ...rest }) => {
  const { t: t2 } = useTranslationContext();
  const hasSendableData = useMessageComposerHasSendableData();
  return (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "solid",
      "aria-label": t2("aria/Send"),
      circular: true,
      className: "str-chat__send-button",
      "data-testid": "send-button",
      disabled: !hasSendableData,
      onClick: sendMessage,
      size: "sm",
      variant: "primary",
      ...rest,
      children: children ?? (0, import_jsx_runtime4.jsx)(IconSend, {})
    }
  );
};
var messageComposerStateSelector$1 = ({ editedMessage }) => ({
  editedMessage
});
var textComposerStateSelector = ({ command, text: text22 }) => ({
  command,
  text: text22
});
var MessageComposerActions = () => {
  const { channel } = useChannelStateContext();
  const { hideSendButton } = useMessageComposerContext();
  const messageComposer = useMessageComposerController();
  const {
    CooldownTimer: CooldownTimer$1 = CooldownTimer,
    SendButton: SendButton$1,
    StopAIGenerationButton: StopAIGenerationButtonOverride
  } = useComponentContext();
  const { editedMessage } = useStateStore(
    messageComposer.state,
    messageComposerStateSelector$1
  );
  const { command } = useStateStore(
    messageComposer.textComposer.state,
    textComposerStateSelector
  );
  const contentIsEmpty = useMessageContentIsEmpty();
  const StopAIGenerationButton$1 = StopAIGenerationButtonOverride === void 0 ? StopAIGenerationButton : StopAIGenerationButtonOverride;
  const { handleSubmit, recordingController } = useMessageComposerContext();
  const isCooldownActive = useIsCooldownActive();
  const { aiState } = useAIState(channel);
  const stopGenerating = (0, import_react29.useCallback)(() => channel?.stopAIResponse(), [channel]);
  const shouldDisplayStopAIGeneration = [AIStates.Thinking, AIStates.Generating].includes(aiState) && !!StopAIGenerationButton$1;
  const recordingEnabled = !!(recordingController.recorder && navigator.mediaDevices);
  let content3 = SendButton$1 ? (0, import_jsx_runtime4.jsx)(SendButton$1, { sendMessage: handleSubmit }) : (0, import_jsx_runtime4.jsx)(SendButton, { sendMessage: handleSubmit, children: editedMessage || command ? (0, import_jsx_runtime4.jsx)(IconCheckmark, {}) : (0, import_jsx_runtime4.jsx)(IconSend, {}) });
  if (shouldDisplayStopAIGeneration) {
    content3 = (0, import_jsx_runtime4.jsx)(StopAIGenerationButton$1, { onClick: stopGenerating });
  } else if (hideSendButton) return null;
  if (isCooldownActive) {
    content3 = (0, import_jsx_runtime4.jsx)(CooldownTimer$1, {});
  } else if (contentIsEmpty && !editedMessage && !command && recordingEnabled) {
    content3 = (0, import_jsx_runtime4.jsx)(AudioRecordingButtonWithNotification, {});
  }
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-composer__actions", children: content3 });
};
var AdditionalMessageComposerActions = () => {
  const { EmojiPicker } = useComponentContext();
  const isCooldownActive = useIsCooldownActive();
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-composer__additional-actions", children: !isCooldownActive && EmojiPicker ? (0, import_jsx_runtime4.jsx)(EmojiPicker, {}) : null });
};
var GeolocationPreviewImage = () => (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__location-preview-image", children: (0, import_jsx_runtime4.jsx)(IconLocation, {}) });
var GeolocationPreview = ({
  location: location2,
  PreviewImage = GeolocationPreviewImage,
  remove
}) => {
  const { t: t2 } = useTranslationContext();
  const shareDuration = location2.durationMs;
  const title = shareDuration ? t2("Live location") : t2("Current location");
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__location-preview", "data-testid": "location-preview", children: [
    (0, import_jsx_runtime4.jsx)(PreviewImage, { location: location2 }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__location-preview__data", children: [
      (0, import_jsx_runtime4.jsx)(
        "div",
        {
          className: "str-chat__location-preview__data__title",
          title: t2("Shared location"),
          children: title
        }
      ),
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__location-preview__data__subtitle", children: t2("Location: {{ coordinates }}", {
        coordinates: `${location2.latitude}, ${location2.longitude}`
      }) }),
      shareDuration && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__location-preview__data__sharing-duration", children: t2("Live for {{duration}}", {
        duration: t2("duration/Share Location", {
          milliseconds: shareDuration
        })
      }) })
    ] }),
    remove && (0, import_jsx_runtime4.jsx)(
      RemoveAttachmentPreviewButton,
      {
        "aria-label": t2("aria/Remove location attachment"),
        className: "str-chat__attachment-preview__remove-button",
        "data-testid": "location-preview-item-delete-button",
        onClick: remove
      }
    )
  ] });
};
var messageComposerStateSelector = ({
  editedMessage,
  quotedMessage
}) => ({
  editedMessage,
  quotedMessage
});
var attachmentManagerStateSelector = (state) => ({
  attachments: state.attachments
});
var linkPreviewsManagerStateSelector = (state) => ({
  linkPreviews: Array.from(state.previews.values()).filter(
    (preview) => LinkPreviewsManager.previewIsLoaded(preview) || LinkPreviewsManager.previewIsLoading(preview)
  )
});
var locationComposerStateSelector = (state) => ({
  location: state.location
});
var textComposerCommandSelector = ({ command }) => ({ command });
var MessageComposerPreviews = () => {
  const {
    AttachmentPreviewList: AttachmentPreviewList$1 = AttachmentPreviewList,
    EditedMessagePreview: EditedMessagePreview$1 = EditedMessagePreview,
    LinkPreviewList: LinkPreviewList$1 = LinkPreviewList,
    QuotedMessagePreview: QuotedMessagePreview$1 = QuotedMessagePreview,
    VoiceRecordingPreviewSlot: VoiceRecordingPreviewSlot$1 = VoiceRecordingPreviewSlot
  } = useComponentContext();
  const messageComposer = useMessageComposerController();
  const { editedMessage, quotedMessage } = useStateStore(
    messageComposer.state,
    messageComposerStateSelector
  );
  const { attachments } = useStateStore(
    messageComposer.attachmentManager.state,
    attachmentManagerStateSelector
  );
  const { location: location2 } = useStateStore(
    messageComposer.locationComposer.state,
    locationComposerStateSelector
  );
  const { linkPreviewsManager } = messageComposer;
  const { linkPreviews } = useStateStore(
    linkPreviewsManager.state,
    linkPreviewsManagerStateSelector
  );
  if (!quotedMessage && attachments.length === 0 && linkPreviews.length === 0 && !location2 && !editedMessage)
    return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-composer-previews", children: [
    editedMessage ? (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-composer-previews", children: (0, import_jsx_runtime4.jsx)(
      EditedMessagePreview$1,
      {
        message: editedMessage,
        onCancel: () => {
          restorePreEditSnapshot(messageComposer);
        }
      }
    ) }) : (0, import_jsx_runtime4.jsx)(QuotedMessagePreview$1, {}),
    (0, import_jsx_runtime4.jsx)(VoiceRecordingPreviewSlot$1, {}),
    (0, import_jsx_runtime4.jsx)(AttachmentPreviewList$1, {}),
    (0, import_jsx_runtime4.jsx)(LinkPreviewList$1, {}),
    location2 && (0, import_jsx_runtime4.jsx)(
      GeolocationPreview,
      {
        location: location2,
        remove: messageComposer.editedMessage ? void 0 : messageComposer.locationComposer.initState
      }
    )
  ] });
};
var MessageComposerUI = () => {
  const { message } = useMessageContext();
  const { recordingController } = useMessageComposerContext();
  const messageComposerController = useMessageComposerController();
  const { command } = useStateStore(
    messageComposerController.textComposer.state,
    textComposerCommandSelector
  );
  const {
    AdditionalMessageComposerActions: AdditionalMessageComposerActions$1 = AdditionalMessageComposerActions,
    AttachmentSelector: AttachmentSelector$1 = message ? SimpleAttachmentSelector : AttachmentSelector,
    AudioRecorder: AudioRecorder$1 = AudioRecorder,
    CommandChip: CommandChip$1 = CommandChip,
    SendToChannelCheckbox: SendToChannelCheckbox$1 = SendToChannelCheckbox,
    TextareaComposer: TextareaComposer$1 = TextareaComposer
  } = useComponentContext();
  return (0, import_jsx_runtime4.jsx)(
    WithDragAndDropUpload,
    {
      className: "str-chat__message-composer-container",
      component: "div",
      children: (0, import_jsx_runtime4.jsx)(
        "div",
        {
          className: clsx_default("str-chat__message-composer", {
            "str-chat__message-composer--command-active": !!command
          }),
          children: recordingController.recordingState ? (0, import_jsx_runtime4.jsx)(AudioRecorder$1, {}) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
            (0, import_jsx_runtime4.jsx)(AttachmentSelector$1, {}),
            (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-composer-compose-area", children: [
              (0, import_jsx_runtime4.jsx)(MessageComposerPreviews, {}),
              (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-composer-controls", children: [
                (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-composer-controls__text-composition-controls", children: [
                  (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-composer-controls__text-composition-controls__text", children: [
                    command && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-composer-controls__text-composition-controls__command-chip-container", children: (0, import_jsx_runtime4.jsx)(CommandChip$1, { command }) }),
                    (0, import_jsx_runtime4.jsx)(TextareaComposer$1, {})
                  ] }),
                  (0, import_jsx_runtime4.jsx)(SendToChannelCheckbox$1, {})
                ] }),
                (0, import_jsx_runtime4.jsx)(AdditionalMessageComposerActions$1, {}),
                (0, import_jsx_runtime4.jsx)(MessageComposerActions, {})
              ] })
            ] })
          ] })
        }
      )
    }
  );
};
var useCreateMessageComposerContext = (value) => {
  const {
    additionalTextareaProps,
    asyncMessagesMultiSendEnabled,
    audioRecordingEnabled,
    emojiSearchIndex,
    focus,
    handleSubmit,
    hideSendButton,
    maxRows,
    minRows,
    onPaste,
    parent,
    recordingController,
    shouldSubmit,
    textareaRef
  } = value;
  const parentId = parent?.id;
  const messageComposerContext = (0, import_react29.useMemo)(
    () => ({
      additionalTextareaProps,
      asyncMessagesMultiSendEnabled,
      audioRecordingEnabled,
      emojiSearchIndex,
      focus,
      handleSubmit,
      hideSendButton,
      maxRows,
      minRows,
      onPaste,
      parent,
      recordingController,
      shouldSubmit,
      textareaRef
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      asyncMessagesMultiSendEnabled,
      audioRecordingEnabled,
      emojiSearchIndex,
      handleSubmit,
      hideSendButton,
      minRows,
      parentId,
      recordingController
    ]
  );
  return messageComposerContext;
};
var MessageComposerProvider = (props) => {
  const messageComposerBindings = useMessageComposerBindings(props);
  const { emojiSearchIndex } = useComponentContext();
  const messageComposerContextValue = useCreateMessageComposerContext({
    ...messageComposerBindings,
    ...props,
    emojiSearchIndex: props.emojiSearchIndex ?? emojiSearchIndex
  });
  const messageComposer = useMessageComposerController();
  (0, import_react29.useEffect)(
    () => () => {
      messageComposer.createDraft().finally(() => messageComposer.clear());
    },
    [messageComposer]
  );
  (0, import_react29.useEffect)(() => {
    const threadId = messageComposer.threadId;
    if (!threadId || !messageComposer.channel || !messageComposer.contentIsEmpty || !messageComposer.config.drafts.enabled)
      return;
    messageComposer.channel.getDraft({ parent_id: threadId }).then(({ draft }) => {
      if (draft) {
        messageComposer.initState({ composition: draft });
      }
    }).catch(console.error);
  }, [messageComposer]);
  useRegisterDropHandlers();
  return (0, import_jsx_runtime4.jsx)(MessageComposerContextProvider, { value: messageComposerContextValue, children: props.children });
};
var UnMemoizedMessageComposer = (props) => {
  const { MessageComposerUI: MessageComposerUI$1 = MessageComposerUI } = useComponentContext();
  const messageComposer = useMessageComposerController();
  const id3 = useStableId();
  const dialogManagerId = messageComposer.threadId ? `message-input-dialog-manager-thread-${id3}` : `message-input-dialog-manager-${id3}`;
  return (0, import_jsx_runtime4.jsx)(DialogManagerProvider, { id: dialogManagerId, children: (0, import_jsx_runtime4.jsx)(MessageComposerProvider, { ...props, children: (0, import_jsx_runtime4.jsx)(MessageComposerUI$1, {}) }) });
};
var MessageComposer2 = import_react29.default.memo(
  UnMemoizedMessageComposer
);
var remarkPlugins = [
  htmlToTextPlugin,
  [remarkGfm, { singleTilde: false }],
  plusPlusToEmphasis,
  imageToLink
];
var renderPreviewText = (text22) => (0, import_jsx_runtime4.jsx)(Markdown, { remarkPlugins, skipHtml: true, children: text22 });
var getLatestPollVote = (latestVotesByOption) => {
  let latestVote;
  for (const optionVotes of Object.values(latestVotesByOption)) {
    optionVotes.forEach((vote) => {
      if (latestVote && new Date(latestVote.updated_at) >= new Date(vote.created_at))
        return;
      latestVote = vote;
    });
  }
  return latestVote;
};
var getLatestMessagePreview = (channel, t2, userLanguage = "en", isMessageAIGenerated) => {
  const latestMessage = channel.state.latestMessages[channel.state.latestMessages.length - 1];
  const previewTextToRender = getTranslatedMessageText({ language: userLanguage, message: latestMessage }) || latestMessage?.text;
  const poll = latestMessage?.poll;
  if (!latestMessage) {
    return t2("Nothing yet...");
  }
  if (isMessageDeleted(latestMessage)) {
    return t2("Message deleted");
  }
  if (poll) {
    if (!poll.vote_count) {
      const createdBy = poll.created_by?.id === channel.getClient().userID ? t2("You") : poll.created_by?.name ?? t2("Poll");
      return t2("📊 {{createdBy}} created: {{ pollName}}", {
        createdBy,
        pollName: poll.name
      });
    } else {
      const latestVote = getLatestPollVote(
        poll.latest_votes_by_option
      );
      const option = latestVote && poll.options.find((opt) => opt.id === latestVote.option_id);
      if (option && latestVote) {
        return t2("📊 {{votedBy}} voted: {{pollOptionText}}", {
          pollOptionText: option.text,
          votedBy: latestVote?.user?.id === channel.getClient().userID ? t2("You") : latestVote.user?.name ?? t2("Poll")
        });
      }
    }
  }
  if (previewTextToRender) {
    return isMessageAIGenerated?.(latestMessage) ? previewTextToRender : renderPreviewText(previewTextToRender);
  }
  if (latestMessage.command) {
    return `/${latestMessage.command}`;
  }
  if (latestMessage.attachments?.length) {
    return t2("🏙 Attachment...");
  }
  if (latestMessage.shared_location) {
    return t2("📍Shared location");
  }
  return t2("Empty message...");
};
var getChannelDisplayImage = (channel) => {
  const data = channel.data;
  if (data?.image && typeof data.image === "string") return data.image;
  const memberList = Object.values(channel.state.members);
  const currentUserId = channel.getClient().userID ?? void 0;
  if (memberList.length === 2) {
    const other = memberList.find((m2) => m2.user?.id !== currentUserId);
    const image3 = other?.user?.image;
    if (image3 && typeof image3 === "string") return image3;
  }
  return void 0;
};
var getGroupChannelDisplayInfo = (channel) => {
  const members = Object.values(channel.state.members);
  if (members.length <= 2) return;
  const memberList = [];
  for (const member of members) {
    const { user } = member;
    if (!user?.name && !user?.image) continue;
    memberList.push({ imageUrl: user.image, userName: user.name });
  }
  return {
    members: memberList
  };
};
function computeChannelDisplayName(channel, directMessageLabel) {
  const data = channel.data;
  if (data?.name && typeof data.name === "string") return data.name;
  const memberList = Object.values(channel.state.members);
  const currentUserId = channel.getClient().userID ?? void 0;
  const otherMembers = memberList.filter((m2) => m2.user?.id !== currentUserId);
  if (memberList.length === 2 && otherMembers.length === 1) {
    const name2 = otherMembers[0].user?.name;
    return name2 || directMessageLabel;
  }
  if (otherMembers.length >= 2) {
    const names = otherMembers.map((m2) => m2.user?.name).filter(Boolean).slice(0, 2);
    if (names.length > 0) return names.join(", ");
  }
  return void 0;
}
var useChannelDisplayName = (channel) => {
  const { client } = useChatContext("useChannelDisplayName");
  const { t: t2 } = useTranslationContext("useChannelDisplayName");
  const directMessageLabel = t2("Direct message");
  const [displayName, setDisplayName] = (0, import_react29.useState)(
    () => channel ? computeChannelDisplayName(channel, directMessageLabel) : void 0
  );
  (0, import_react29.useEffect)(() => {
    if (!channel) {
      setDisplayName(void 0);
      return;
    }
    const updateDisplayName = () => setDisplayName(computeChannelDisplayName(channel, directMessageLabel));
    updateDisplayName();
    client.on("user.updated", updateDisplayName);
    return () => {
      client.off("user.updated", updateDisplayName);
    };
  }, [channel, channel?.data, client, directMessageLabel]);
  return displayName;
};
var emptyGroupInfo = {
  members: [],
  overflowCount: void 0
};
var useChannelPreviewInfo = (props) => {
  const { channel, overrideImage, overrideTitle } = props;
  const { client } = useChatContext();
  const channelDisplayName = useChannelDisplayName(channel);
  const displayTitle = overrideTitle ?? channelDisplayName;
  const [displayImage, setDisplayImage] = (0, import_react29.useState)(
    () => channel ? overrideImage ?? getChannelDisplayImage(channel) : void 0
  );
  const [groupChannelDisplayInfo, setGroupChannelDisplayInfo] = (0, import_react29.useState)(
    () => channel ? getGroupChannelDisplayInfo(channel) ?? emptyGroupInfo : emptyGroupInfo
  );
  (0, import_react29.useEffect)(() => {
    if (!channel) return;
    if (overrideImage) return;
    const updateInfo = () => {
      setDisplayImage(getChannelDisplayImage(channel));
      setGroupChannelDisplayInfo(getGroupChannelDisplayInfo(channel) ?? emptyGroupInfo);
    };
    updateInfo();
    client.on("user.updated", updateInfo);
    return () => {
      client.off("user.updated", updateInfo);
    };
  }, [channel, channel?.data, client, overrideImage]);
  return (0, import_react29.useMemo)(
    () => ({
      displayImage: overrideImage ?? displayImage,
      displayTitle,
      groupChannelDisplayInfo
    }),
    [displayImage, displayTitle, groupChannelDisplayInfo, overrideImage]
  );
};
var TypingIndicatorDots = () => (0, import_jsx_runtime4.jsxs)(
  "svg",
  {
    "aria-hidden": true,
    fill: "none",
    height: "5",
    viewBox: "0 0 23 5",
    width: "23",
    xmlns: "http://www.w3.org/2000/svg",
    children: [
      (0, import_jsx_runtime4.jsx)("circle", { cx: "2.5", cy: "2.5", r: "2.5" }),
      (0, import_jsx_runtime4.jsx)("circle", { cx: "11.5", cy: "2.5", r: "2.5" }),
      (0, import_jsx_runtime4.jsx)("circle", { cx: "20.5", cy: "2.5", r: "2.5" })
    ]
  }
);
var DEFAULT_HIDE_DELAY_MS = 2e3;
var useStableTypingUsers = (typingUsers) => {
  const key = typingUsers.map((entry) => entry.user?.id ?? "").sort().join(",");
  return (0, import_react29.useMemo)(() => [...typingUsers], [key]);
};
function useDebouncedTypingActive(typingUsers, delayMs = DEFAULT_HIDE_DELAY_MS) {
  const stableTypingUsers = useStableTypingUsers(typingUsers);
  const [displayUsers, setDisplayUsers] = (0, import_react29.useState)([]);
  const timerRef = (0, import_react29.useRef)(null);
  const hadTypersRef = (0, import_react29.useRef)(false);
  (0, import_react29.useEffect)(() => {
    if (stableTypingUsers.length > 0) {
      if (timerRef.current) {
        clearTimeout(timerRef.current);
        timerRef.current = null;
      }
      hadTypersRef.current = true;
      setDisplayUsers(stableTypingUsers);
      return;
    }
    if (stableTypingUsers.length === 0 && hadTypersRef.current && !timerRef.current) {
      timerRef.current = setTimeout(() => {
        timerRef.current = null;
        hadTypersRef.current = false;
        setDisplayUsers([]);
      }, delayMs);
    }
  }, [stableTypingUsers, delayMs]);
  (0, import_react29.useEffect)(
    () => () => {
      if (timerRef.current) clearTimeout(timerRef.current);
    },
    []
  );
  return { displayUsers };
}
var TypingIndicatorHeader = (props) => {
  const { threadList = false } = props;
  const { t: t2 } = useTranslationContext();
  const { channelConfig, thread } = useChannelStateContext("TypingIndicatorHeader");
  const threadInstance = useThreadContext();
  const parentId = threadInstance?.id ?? thread?.id;
  const { client } = useChatContext("TypingIndicatorHeader");
  const { typing = {} } = useTypingContext("TypingIndicatorHeader");
  const typingInChannel = !threadList ? Object.values(typing).filter(
    ({ parent_id, user }) => user?.id !== client.user?.id && !parent_id
  ) : [];
  const typingInThread = threadList ? Object.values(typing).filter(
    ({ parent_id, user }) => user?.id !== client.user?.id && parent_id === parentId
  ) : [];
  const typingUsers = threadList ? typingInThread : typingInChannel;
  const { displayUsers } = useDebouncedTypingActive(typingUsers);
  if (channelConfig?.typing_events === false || displayUsers.length === 0) {
    return null;
  }
  const names = displayUsers.map(({ user }) => user?.name ?? "");
  let label;
  if (names.length === 1) {
    label = t2("{{ typing }} is typing", { typing: names[0] });
  } else if (names.length === 2) {
    label = t2("{{ typing }} are typing", {
      typing: `${names[0]} and ${names[1]}`
    });
  } else {
    label = t2("{{ count }} people are typing", { count: names.length });
  }
  return (0, import_jsx_runtime4.jsxs)(
    "span",
    {
      className: clsx_default("str-chat__typing-indicator-header", {
        "str-chat__typing-indicator-header--thread": threadList
      }),
      "data-testid": "typing-indicator-header",
      children: [
        (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__typing-indicator-header__text", children: label }),
        (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__typing-indicator__dots str-chat__typing-indicator-header__dots", children: (0, import_jsx_runtime4.jsx)(TypingIndicatorDots, {}) })
      ]
    }
  );
};
var threadStateSelector$1 = ({ replyCount }) => ({ replyCount });
var displayNameFromParentMessage = (message) => message.user?.name ?? void 0;
var ThreadHeaderSubtitle = ({
  replyCount,
  threadDisplayName,
  threadList
}) => {
  const { t: t2 } = useTranslationContext();
  const { channelConfig, thread } = useChannelStateContext("ThreadHeaderSubtitle");
  const threadInstance = useThreadContext();
  const parentId = threadInstance?.id ?? thread?.id;
  const { client } = useChatContext("ThreadHeaderSubtitle");
  const { typing = {} } = useTypingContext("ThreadHeaderSubtitle");
  const typingInThread = Object.values(typing).filter(
    ({ parent_id, user }) => user?.id !== client.user?.id && parent_id === parentId
  );
  const hasTyping = channelConfig?.typing_events !== false && typingInThread.length > 0;
  const replyCountText = t2("replyCount", { count: replyCount ?? 0 });
  const defaultSubtitle = threadDisplayName ? `${threadDisplayName} · ${replyCountText}` : replyCountText;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-header-subtitle", children: (0, import_jsx_runtime4.jsx)(
    "span",
    {
      className: "str-chat__subtitle-content-transition",
      children: hasTyping ? (0, import_jsx_runtime4.jsx)(TypingIndicatorHeader, { threadList }) : (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: defaultSubtitle })
    },
    hasTyping ? "typing" : "default"
  ) });
};
var ThreadHeader = (props) => {
  const { closeThread, overrideTitle, thread } = props;
  const { t: t2 } = useTranslationContext();
  const { channel } = useChannelStateContext();
  const { HeaderStartContent } = useComponentContext();
  const { activeChatView } = useChatViewContext();
  const { displayTitle: channelDisplayTitle } = useChannelPreviewInfo({ channel });
  const threadInstance = useThreadContext();
  const { replyCount: replyCountThreadInstance } = useStateStore(threadInstance?.state, threadStateSelector$1) ?? {};
  const replyCount = threadInstance ? replyCountThreadInstance : thread ? thread.reply_count ?? 0 : 0;
  const threadDisplayName = overrideTitle ?? channelDisplayTitle ?? displayNameFromParentMessage(thread) ?? void 0;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-header", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-header__start", children: activeChatView === "threads" && HeaderStartContent && (0, import_jsx_runtime4.jsx)(HeaderStartContent, {}) }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-header-details", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-header-title", children: t2("Thread") }),
      (0, import_jsx_runtime4.jsx)(
        ThreadHeaderSubtitle,
        {
          replyCount,
          threadDisplayName,
          threadList: true
        }
      )
    ] }),
    !threadInstance && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-header__end", children: (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "ghost",
        "aria-label": t2("aria/Close thread"),
        circular: true,
        className: "str-chat__close-thread-button",
        "data-testid": "close-thread-button",
        onClick: closeThread,
        size: "md",
        variant: "secondary",
        children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
      }
    ) })
  ] });
};
var threadStateSelector = ({ replyCount }) => ({
  replyCount
});
var ThreadStart = () => {
  const { thread } = useChannelStateContext("ThreadStart");
  const { t: t2 } = useTranslationContext("ThreadStart");
  const threadInstance = useThreadContext();
  const { replyCount: replyCountThreadInstance } = useStateStore(threadInstance?.state, threadStateSelector) ?? {};
  const replyCount = threadInstance ? replyCountThreadInstance : thread ? thread.reply_count ?? 0 : 0;
  if (!replyCount) return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-start", children: t2("replyCount", { count: replyCount }) });
};
var UnMemoizedDateSeparator = (props) => {
  const {
    calendar: calendar2,
    className,
    date: messageCreatedAt,
    floating,
    formatDate,
    ...restTimestampFormatterOptions
  } = props;
  const { t: t2, tDateTimeParser } = useTranslationContext("DateSeparator");
  const formattedDate = getDateString({
    calendar: calendar2,
    ...restTimestampFormatterOptions,
    formatDate,
    messageCreatedAt,
    t: t2,
    tDateTimeParser,
    timestampTranslationKey: "timestamp/DateSeparator"
  });
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default(
        "str-chat__date-separator",
        { "str-chat__date-separator--floating": floating },
        className
      ),
      "data-date": messageCreatedAt.toISOString(),
      "data-testid": floating ? "floating-date-separator" : "date-separator",
      children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__date-separator-date", children: formattedDate })
    }
  );
};
var DateSeparator = import_react29.default.memo(
  UnMemoizedDateSeparator
);
var ThreadHead = (props) => {
  const { ThreadStart: ThreadStart$1 = ThreadStart } = useComponentContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__parent-message-li", children: [
    (0, import_jsx_runtime4.jsx)(DateSeparator, { date: props.message.created_at }),
    (0, import_jsx_runtime4.jsx)(Message, { initialMessage: true, threadList: true, ...props }),
    (0, import_jsx_runtime4.jsx)(ThreadStart$1, {})
  ] });
};
var Thread = (props) => {
  const { channel, channelConfig, thread } = useChannelStateContext("Thread");
  const threadInstance = useThreadContext();
  if (!thread && !threadInstance) return null;
  if (channelConfig?.replies === false) return null;
  return (
    // FIXME: TS is having trouble here as at least one of the two would always be defined
    (0, import_react29.createElement)(
      ThreadInner,
      {
        ...props,
        key: `thread-${(thread ?? threadInstance)?.id}-${channel?.cid}`
      }
    )
  );
};
var selector$5 = (nextValue) => ({
  isLoadingNext: nextValue.pagination.isLoadingNext,
  isLoadingPrev: nextValue.pagination.isLoadingPrev,
  parentMessage: nextValue.parentMessage,
  replies: nextValue.replies
});
var ThreadInner = (props) => {
  const {
    additionalMessageComposerProps,
    additionalMessageListProps,
    additionalParentMessageProps,
    additionalVirtualizedMessageListProps,
    autoFocus = true,
    enableDateSeparator = false,
    Message: PropMessage,
    messageActions = Object.keys(MESSAGE_ACTIONS),
    virtualized
  } = props;
  const threadInstance = useThreadContext();
  const {
    thread,
    threadHasMore,
    threadLoadingMore,
    threadMessages = [],
    threadSuppressAutoscroll
  } = useChannelStateContext("Thread");
  const { closeThread, loadMoreThread } = useChannelActionContext("Thread");
  const { customClasses } = useChatContext("Thread");
  const {
    Message: ContextMessage,
    ThreadHead: ThreadHead$1 = ThreadHead,
    ThreadHeader: ThreadHeader$1 = ThreadHeader,
    VirtualMessage
  } = useComponentContext();
  const { isLoadingNext, isLoadingPrev, parentMessage, replies } = useStateStore(threadInstance?.state, selector$5) ?? {};
  const ThreadMessage = PropMessage || additionalMessageListProps?.Message;
  const FallbackMessage = virtualized && VirtualMessage ? VirtualMessage : ContextMessage;
  const MessageUIComponent = ThreadMessage || FallbackMessage;
  const ThreadMessageList = virtualized ? VirtualizedMessageList : MessageList;
  (0, import_react29.useEffect)(() => {
    if (threadInstance) return;
    if ((thread?.reply_count ?? 0) > 0) {
      loadMoreThread();
    }
  }, [thread, loadMoreThread, threadInstance]);
  const threadProps = threadInstance ? {
    loadingMore: isLoadingPrev,
    loadingMoreNewer: isLoadingNext,
    loadMore: threadInstance.loadPrevPage,
    loadMoreNewer: threadInstance.loadNextPage,
    messages: replies
  } : {
    hasMore: threadHasMore,
    loadingMore: threadLoadingMore,
    loadMore: loadMoreThread,
    messages: threadMessages
  };
  const messageAsThread = thread ?? parentMessage;
  if (!messageAsThread) return null;
  const threadClass = customClasses?.thread || clsx_default("str-chat__thread-container str-chat__thread", {
    "str-chat__thread--virtualized": virtualized
  });
  const head = (0, import_jsx_runtime4.jsx)(
    ThreadHead$1,
    {
      message: messageAsThread,
      Message: MessageUIComponent,
      ...additionalParentMessageProps
    },
    messageAsThread.id
  );
  return (
    // Thread component needs a context which we can use for message composer
    (0, import_jsx_runtime4.jsx)(
      LegacyThreadContext.Provider,
      {
        value: {
          legacyThread: thread ?? void 0
        },
        children: (0, import_jsx_runtime4.jsxs)("div", { className: threadClass, children: [
          (0, import_jsx_runtime4.jsx)(ThreadHeader$1, { closeThread, thread: messageAsThread }),
          (0, import_jsx_runtime4.jsx)(
            ThreadMessageList,
            {
              disableDateSeparator: !enableDateSeparator,
              head,
              Message: MessageUIComponent,
              messageActions,
              suppressAutoscroll: threadSuppressAutoscroll,
              threadList: true,
              ...threadProps,
              ...virtualized ? additionalVirtualizedMessageListProps : additionalMessageListProps
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            MessageComposer2,
            {
              focus: autoFocus,
              parent: thread ?? parentMessage,
              ...additionalMessageComposerProps
            }
          )
        ] })
      }
    )
  );
};
var mapEmojiMartData = (emojiMartData) => {
  if (!emojiMartData || !emojiMartData.emojis) {
    return {};
  }
  const newMap = {};
  for (const emojiId in emojiMartData.emojis) {
    const emojiData = emojiMartData.emojis[emojiId];
    const [firstEmoji] = emojiData.skins;
    if (!firstEmoji || !firstEmoji.native) continue;
    const nativeEmoji = firstEmoji.native;
    const unicode = emojiToUnicode(nativeEmoji);
    newMap[unicode] = {
      Component: () => (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: nativeEmoji }),
      name: emojiData.name
    };
  }
  return newMap;
};
var emojiToUnicode = (emoji) => {
  const unicodeStrings = [];
  for (const c2 of emoji) {
    const codePoint = c2.codePointAt(0);
    unicodeStrings.push(`U+${codePoint.toString(16).toUpperCase().padStart(4, "0")}`);
  }
  return unicodeStrings.join("-");
};
var unicodeToEmoji = (unicode) => unicode.split("-").map((code4) => String.fromCodePoint(parseInt(code4.replace("U+", ""), 16))).join("");
var defaultReactionOptions = {
  quick: {
    haha: {
      Component: () => (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: "😂" }),
      name: "Joy",
      unicode: emojiToUnicode("😂")
    },
    like: {
      Component: () => (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: "👍" }),
      name: "Thumbs up",
      unicode: emojiToUnicode("👍")
    },
    love: {
      Component: () => (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: "❤️" }),
      name: "Heart",
      unicode: emojiToUnicode("❤️")
    },
    sad: { Component: () => (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: "😔" }), name: "Sad", unicode: emojiToUnicode("😔") },
    wow: {
      Component: () => (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: "😮" }),
      name: "Astonished",
      unicode: emojiToUnicode("😮")
    },
    fire: {
      Component: () => (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: "🔥" }),
      name: "Fire",
      unicode: emojiToUnicode("🔥")
    }
  }
};
var stableOwnReactions = [];
var ReactionSelector = (props) => {
  const {
    dialogId: propDialogId,
    handleReaction: propHandleReaction,
    own_reactions: propOwnReactions
  } = props;
  const [extendedListOpen, setExtendedListOpen] = (0, import_react29.useState)(false);
  const {
    reactionOptions = defaultReactionOptions,
    ReactionSelectorExtendedList: ReactionSelectorExtendedList2 = ReactionSelector.ExtendedList
  } = useComponentContext();
  const {
    closeReactionSelectorOnClick,
    handleReaction: contextHandleReaction,
    message,
    threadList
  } = useMessageContext();
  const dialogId2 = propDialogId ?? ReactionSelector.getDialogId({
    messageId: message.id,
    threadList
  });
  const { dialog } = useDialogOnNearestManager({ id: dialogId2 });
  const handleReaction = propHandleReaction ?? contextHandleReaction;
  const ownReactions = propOwnReactions ?? message?.own_reactions ?? stableOwnReactions;
  const ownReactionByType = (0, import_react29.useMemo)(() => {
    var _a;
    const map4 = {};
    for (const reaction of ownReactions) {
      map4[_a = reaction.type] ?? (map4[_a] = reaction);
    }
    return map4;
  }, [ownReactions]);
  const adjustedQuickReactionOptions = (0, import_react29.useMemo)(() => {
    if (Array.isArray(reactionOptions)) return reactionOptions;
    return Object.entries(reactionOptions.quick).map(
      ([type, { Component: Component2, name: name2, unicode }]) => ({
        Component: Component2,
        name: name2,
        type,
        unicode
      })
    );
  }, [reactionOptions]);
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__reaction-selector", "data-testid": "reaction-selector", children: !extendedListOpen ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      "ul",
      {
        className: "str-chat__reaction-selector-list",
        "data-testid": "reaction-selector-list",
        children: adjustedQuickReactionOptions.map(
          ({ Component: Component2, name: reactionName, type: reactionType }) => (0, import_jsx_runtime4.jsx)("li", { className: "str-chat__reaction-list-selector__item", children: (0, import_jsx_runtime4.jsx)(
            "button",
            {
              "aria-label": `Select Reaction: ${reactionName || reactionType}`,
              "aria-pressed": typeof ownReactionByType[reactionType] !== "undefined",
              className: clsx_default("str-chat__reaction-selector-list__item-button"),
              "data-testid": "select-reaction-button",
              "data-text": reactionType,
              onClick: (event) => {
                handleReaction(reactionType, event);
                if (closeReactionSelectorOnClick) {
                  dialog.close();
                }
              },
              children: (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__reaction-icon", children: (0, import_jsx_runtime4.jsx)(Component2, {}) })
            }
          ) }, reactionType)
        )
      }
    ),
    (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "outline",
        circular: true,
        className: "str-chat__reaction-selector__add-button",
        "data-testid": "reaction-selector-add-button",
        onClick: () => {
          setExtendedListOpen(true);
        },
        size: "sm",
        variant: "secondary",
        children: (0, import_jsx_runtime4.jsx)(IconPlus, {})
      }
    )
  ] }) : (0, import_jsx_runtime4.jsx)(
    ReactionSelectorExtendedList2,
    {
      ...props,
      dialogId: dialogId2,
      handleReaction: async (reactionType, event) => {
        await handleReaction(reactionType, event);
        if (closeReactionSelectorOnClick) {
          dialog.close();
        }
      }
    }
  ) });
};
ReactionSelector.getDialogId = ({ messageId, threadList }) => {
  const dialogIdNamespace = threadList ? "-thread" : "";
  return `reaction-selector${dialogIdNamespace}-${messageId}`;
};
ReactionSelector.displayName = "ReactionSelector";
ReactionSelector.ExtendedList = function ReactionSelectorExtendedList({
  dialogId: dialogId2,
  handleReaction: propHandleReaction,
  own_reactions: propOwnReactions
}) {
  const { reactionOptions = defaultReactionOptions } = useComponentContext();
  const {
    closeReactionSelectorOnClick,
    handleReaction: contextHandleReaction,
    message
  } = useMessageContext();
  const handleReaction = propHandleReaction ?? contextHandleReaction;
  const ownReactions = propOwnReactions ?? message?.own_reactions ?? stableOwnReactions;
  const { dialog } = useDialogOnNearestManager({ id: dialogId2 || "" });
  const ownReactionByType = (0, import_react29.useMemo)(() => {
    var _a;
    const map4 = {};
    for (const reaction of ownReactions) {
      map4[_a = reaction.type] ?? (map4[_a] = reaction);
    }
    return map4;
  }, [ownReactions]);
  if (Array.isArray(reactionOptions) || !reactionOptions.extended) {
    return null;
  }
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__reaction-selector-extended-list",
      "data-testid": "reaction-selector-extended-list",
      children: Object.entries(reactionOptions.extended).map(
        ([reactionType, { Component: Component2, name: reactionName }]) => (0, import_jsx_runtime4.jsx)(
          "button",
          {
            "aria-label": `Select Reaction: ${reactionName || reactionType}`,
            "aria-pressed": typeof ownReactionByType[reactionType] !== "undefined",
            className: "str-chat__reaction-selector-extended-list__button str-chat__button str-chat__button--ghost str-chat__button--size-sm str-chat__button--circular",
            "data-testid": "select-reaction-button",
            "data-text": reactionType,
            onClick: (event) => {
              handleReaction(reactionType, event);
              if (closeReactionSelectorOnClick) {
                dialog.close();
              }
            },
            children: (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__reaction-icon", children: (0, import_jsx_runtime4.jsx)(Component2, {}) })
          },
          reactionType
        )
      )
    }
  );
};
function useFetchReactions(options) {
  const { addNotification } = useNotificationApi();
  const { handleFetchReactions: contextHandleFetchReactions } = useMessageContext();
  const { t: t2 } = useTranslationContext("useFetchReactions");
  const [reactions, setReactions] = (0, import_react29.useState)([]);
  const {
    handleFetchReactions: propHandleFetchReactions,
    reactionType,
    shouldFetch,
    sort
  } = options;
  const [isLoading, setIsLoading] = (0, import_react29.useState)(shouldFetch);
  const handleFetchReactions = propHandleFetchReactions ?? contextHandleFetchReactions;
  const [refetchNonce, setRefetchNonce] = (0, import_react29.useState)(null);
  (0, import_react29.useEffect)(() => {
    if (!shouldFetch) {
      return;
    }
    let cancel = false;
    (async () => {
      try {
        setIsLoading(true);
        const reactions2 = await handleFetchReactions(reactionType ?? void 0, sort);
        if (!cancel) {
          setReactions(reactions2);
        }
      } catch (e) {
        if (!cancel) {
          setReactions([]);
          addNotification({
            emitter: "Reactions",
            error: e instanceof Error ? e : void 0,
            message: t2("Error fetching reactions"),
            severity: "error",
            type: "api:message:reactions:fetch:failed"
          });
        }
      } finally {
        if (!cancel) {
          setIsLoading(false);
        }
      }
    })();
    return () => {
      cancel = true;
    };
  }, [
    addNotification,
    handleFetchReactions,
    reactionType,
    refetchNonce,
    shouldFetch,
    sort,
    t2
  ]);
  const refetch = (0, import_react29.useCallback)(() => {
    setRefetchNonce(Math.random());
  }, []);
  return { isLoading, reactions, refetch };
}
var defaultReactionDetailsSort = { created_at: -1 };
var MessageReactionsDetailLoadingIndicator = () => {
  const elements = (0, import_react29.useMemo)(
    () => Array.from({ length: 3 }, (_2, index6) => (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-reactions-detail__skeleton-item", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-reactions-detail__skeleton-avatar" }),
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-reactions-detail__skeleton-line" })
    ] }, index6)),
    []
  );
  return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: elements });
};
var MessageReactionsDetail = ({
  handleFetchReactions,
  handleReaction,
  onSelectedReactionTypeChange,
  own_reactions,
  reactionDetailsSort: propReactionDetailsSort,
  reactionGroups,
  reactions,
  selectedReactionType,
  totalReactionCount
}) => {
  const [extendedReactionListOpen, setExtendedReactionListOpen] = (0, import_react29.useState)(false);
  const { client } = useChatContext();
  const {
    Avatar: Avatar$1 = Avatar,
    LoadingIndicator: LoadingIndicator2 = MessageReactionsDetailLoadingIndicator,
    reactionOptions = defaultReactionOptions,
    ReactionSelectorExtendedList: ReactionSelectorExtendedList2 = ReactionSelector.ExtendedList
  } = useComponentContext(MessageReactionsDetail.name);
  const { t: t2 } = useTranslationContext();
  const {
    handleReaction: contextHandleReaction,
    message,
    reactionDetailsSort: contextReactionDetailsSort
  } = useMessageContext(MessageReactionsDetail.name);
  const reactionDetailsSort = propReactionDetailsSort ?? contextReactionDetailsSort ?? defaultReactionDetailsSort;
  const {
    isLoading: areReactionsLoading,
    reactions: reactionDetails,
    refetch
  } = useFetchReactions({
    handleFetchReactions,
    reactionType: selectedReactionType,
    shouldFetch: true,
    sort: reactionDetailsSort
  });
  if (extendedReactionListOpen) {
    return (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: "str-chat__message-reactions-detail",
        "data-testid": "message-reactions-detail",
        children: (0, import_jsx_runtime4.jsx)(
          ReactionSelectorExtendedList2,
          {
            dialogId: MessageReactionsDetail.getDialogId({ messageId: message.id }),
            handleReaction,
            own_reactions
          }
        )
      }
    );
  }
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__message-reactions-detail",
      "data-testid": "message-reactions-detail",
      children: [
        typeof totalReactionCount === "number" && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-reactions-detail__total-count", children: t2("{{ count }} reactions", { count: totalReactionCount }) }),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-reactions-detail__reaction-type-list-container", children: (0, import_jsx_runtime4.jsxs)(
          "ul",
          {
            className: "str-chat__message-reactions-detail__reaction-type-list",
            "data-testid": "reaction-type-list",
            children: [
              (0, import_jsx_runtime4.jsx)("li", { className: "str-chat__message-reactions-detail__reaction-type-list-item", children: (0, import_jsx_runtime4.jsx)(
                "button",
                {
                  "aria-label": t2("Add reaction"),
                  className: "str-chat__message-reactions-detail__reaction-type-list-item-button",
                  "data-testid": "add-reaction-button",
                  onClick: () => setExtendedReactionListOpen(true),
                  children: (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__message-reactions-detail__reaction-type-list-item-icon", children: (0, import_jsx_runtime4.jsx)(IconEmojiAdd, {}) })
                }
              ) }),
              reactions.map(
                ({ EmojiComponent, reactionCount, reactionType }) => EmojiComponent && (0, import_jsx_runtime4.jsx)(
                  "li",
                  {
                    className: "str-chat__message-reactions-detail__reaction-type-list-item",
                    children: (0, import_jsx_runtime4.jsxs)(
                      "button",
                      {
                        "aria-pressed": reactionType === selectedReactionType,
                        className: "str-chat__message-reactions-detail__reaction-type-list-item-button",
                        onClick: () => onSelectedReactionTypeChange?.(
                          selectedReactionType === reactionType ? null : reactionType
                        ),
                        children: [
                          (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__message-reactions-detail__reaction-type-list-item-icon", children: (0, import_jsx_runtime4.jsx)(EmojiComponent, {}) }),
                          (0, import_jsx_runtime4.jsx)(
                            "span",
                            {
                              className: "str-chat__message-reactions-detail__reaction-type-list-item-count",
                              "data-testid": "reaction-type-count",
                              children: reactionCount
                            }
                          )
                        ]
                      }
                    )
                  },
                  reactionType
                )
              )
            ]
          }
        ) }),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-reactions-detail__user-list-container", children: (0, import_jsx_runtime4.jsxs)(
          "div",
          {
            className: "str-chat__message-reactions-detail__user-list",
            "data-testid": "all-reacting-users",
            children: [
              areReactionsLoading && (0, import_jsx_runtime4.jsx)(LoadingIndicator2, {}),
              !areReactionsLoading && (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: reactionDetails.map(({ type, user }) => {
                const belongsToCurrentUser = client.user?.id === user?.id;
                const EmojiComponent = Array.isArray(reactionOptions) ? void 0 : reactionOptions.quick[type]?.Component ?? reactionOptions.extended?.[type]?.Component;
                return (0, import_jsx_runtime4.jsxs)(
                  "div",
                  {
                    className: "str-chat__message-reactions-detail__user-list-item",
                    children: [
                      (0, import_jsx_runtime4.jsx)(
                        Avatar$1,
                        {
                          className: "str-chat__avatar--with-border",
                          "data-testid": "avatar",
                          imageUrl: user?.image,
                          size: "md",
                          userName: user?.name || user?.id
                        }
                      ),
                      (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-reactions-detail__user-list-item-info", children: [
                        (0, import_jsx_runtime4.jsx)(
                          "span",
                          {
                            className: "str-chat__message-reactions-detail__user-list-item-username",
                            "data-testid": "reaction-user-username",
                            children: belongsToCurrentUser ? t2("You") : user?.name || user?.id
                          }
                        ),
                        belongsToCurrentUser && (0, import_jsx_runtime4.jsx)(
                          "button",
                          {
                            className: "str-chat__message-reactions-detail__user-list-item-button",
                            "data-testid": "remove-reaction-button",
                            onClick: async (e) => {
                              const reactionCountBeforeRemoval = reactionGroups?.[type]?.count ?? 0;
                              await contextHandleReaction(type, e);
                              if (selectedReactionType !== null && reactionCountBeforeRemoval <= 1) {
                                onSelectedReactionTypeChange?.(null);
                              } else {
                                refetch();
                              }
                            },
                            children: t2("Tap to remove")
                          }
                        )
                      ] }),
                      (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__message-reactions-detail__user-list-item-icon", children: !selectedReactionType && EmojiComponent && (0, import_jsx_runtime4.jsx)(EmojiComponent, {}) })
                    ]
                  },
                  `${user?.id}-${type}`
                );
              }) })
            ]
          }
        ) })
      ]
    }
  );
};
MessageReactionsDetail.displayName = "MessageReactionsDetail";
MessageReactionsDetail.getDialogId = ({ messageId }) => `message-reactions-detail-${messageId}`;
var defaultReactionsSort = (a2, b2) => {
  if (a2.firstReactionAt && b2.firstReactionAt) {
    return +a2.firstReactionAt - +b2.firstReactionAt;
  }
  return a2.reactionType.localeCompare(b2.reactionType, "en");
};
var useProcessReactions = (params) => {
  const {
    own_reactions: propOwnReactions,
    reaction_groups: propReactionGroups,
    reactions: propReactions,
    sortReactions: propSortReactions
  } = params;
  const { message, sortReactions: contextSortReactions } = useMessageContext();
  const { reactionOptions = defaultReactionOptions } = useComponentContext();
  const sortReactions = propSortReactions ?? contextSortReactions ?? defaultReactionsSort;
  const latestReactions = propReactions ?? message.latest_reactions;
  const ownReactions = propOwnReactions ?? message?.own_reactions;
  const reactionGroups = propReactionGroups ?? message?.reaction_groups ?? void 0;
  const isOwnReaction = (0, import_react29.useCallback)(
    (reactionType) => ownReactions?.some((reaction) => reaction.type === reactionType) ?? false,
    [ownReactions]
  );
  const getEmojiByReactionType = (0, import_react29.useCallback)(
    (reactionType) => {
      if (Array.isArray(reactionOptions)) {
        return reactionOptions.find(({ type }) => type === reactionType)?.Component ?? null;
      }
      return reactionOptions.quick[reactionType]?.Component ?? reactionOptions.extended?.[reactionType]?.Component ?? null;
    },
    [reactionOptions]
  );
  const isSupportedReaction = (0, import_react29.useCallback)(
    (reactionType) => {
      if (Array.isArray(reactionOptions)) {
        return reactionOptions.some(
          (reactionOption) => reactionOption.type === reactionType
        );
      }
      return typeof reactionOptions.quick[reactionType] !== "undefined" || typeof reactionOptions.extended?.[reactionType] !== "undefined";
    },
    [reactionOptions]
  );
  const uniqueReactionTypeCount = (0, import_react29.useMemo)(() => {
    if (!reactionGroups) {
      return 0;
    }
    return Object.keys(reactionGroups).filter(
      (reactionType) => isSupportedReaction(reactionType)
    ).length;
  }, [isSupportedReaction, reactionGroups]);
  const getLatestReactedUserNames = (0, import_react29.useCallback)(
    (reactionType) => latestReactions?.flatMap((reaction) => {
      if (reactionType && reactionType === reaction.type) {
        const username = reaction.user?.name || reaction.user?.id;
        return username ? [username] : [];
      }
      return [];
    }) ?? [],
    [latestReactions]
  );
  const existingReactions = (0, import_react29.useMemo)(() => {
    if (!reactionGroups) {
      return [];
    }
    const unsortedReactions = Object.entries(reactionGroups).flatMap(
      ([reactionType, { count: count2, first_reaction_at, last_reaction_at }]) => {
        if (count2 === 0 || !isSupportedReaction(reactionType)) {
          return [];
        }
        const latestReactedUserNames = getLatestReactedUserNames(reactionType);
        return [
          {
            EmojiComponent: getEmojiByReactionType(reactionType),
            firstReactionAt: first_reaction_at ? new Date(first_reaction_at) : null,
            isOwnReaction: isOwnReaction(reactionType),
            lastReactionAt: last_reaction_at ? new Date(last_reaction_at) : null,
            latestReactedUserNames,
            reactionCount: count2,
            reactionType,
            unlistedReactedUserCount: count2 - latestReactedUserNames.length
          }
        ];
      }
    );
    return unsortedReactions.sort(sortReactions);
  }, [
    getEmojiByReactionType,
    getLatestReactedUserNames,
    isOwnReaction,
    isSupportedReaction,
    reactionGroups,
    sortReactions
  ]);
  const hasReactions = existingReactions.length > 0;
  const totalReactionCount = (0, import_react29.useMemo)(
    () => Object.values(reactionGroups ?? {}).reduce((total, { count: count2 }) => total + count2, 0),
    [reactionGroups]
  );
  return {
    existingReactions,
    hasReactions,
    reactionGroups,
    totalReactionCount,
    uniqueReactionTypeCount
  };
};
var FragmentOrButton = ({
  buttonIf: renderButton = false,
  children,
  ...props
}) => {
  if (renderButton) {
    return (0, import_jsx_runtime4.jsx)("button", { ...props, children });
  }
  return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children });
};
var UnMemoizedMessageReactions = (props) => {
  const {
    capLimit: { clustered: capLimitClustered = 5, segmented: capLimitSegmented = 4 } = {},
    flipHorizontalPosition = false,
    handleFetchReactions,
    reactionDetailsSort,
    verticalPosition = "top",
    visualStyle = "clustered",
    ...rest
  } = props;
  const {
    existingReactions,
    hasReactions,
    reactionGroups,
    totalReactionCount,
    uniqueReactionTypeCount
  } = useProcessReactions(rest);
  const [selectedReactionType, setSelectedReactionType] = (0, import_react29.useState)(
    null
  );
  const { t: t2 } = useTranslationContext("MessageReactions");
  const { MessageReactionsDetail: MessageReactionsDetail$1 = MessageReactionsDetail } = useComponentContext();
  const { isMyMessage, message } = useMessageContext();
  const divRef = (0, import_react29.useRef)(null);
  const dialogId2 = MessageReactionsDetail.getDialogId({
    messageId: message.id
  });
  const { dialog, dialogManager } = useDialogOnNearestManager({ id: dialogId2 });
  const isDialogOpen = useDialogIsOpen(dialogId2, dialogManager?.id);
  const handleReactionButtonClick = (reactionType) => {
    if (totalReactionCount > MAX_MESSAGE_REACTIONS_TO_FETCH) {
      return;
    }
    setSelectedReactionType(reactionType);
    dialog.open();
  };
  const cappedExistingReactions = (0, import_react29.useMemo)(() => {
    if (visualStyle === "segmented" && verticalPosition !== "top") return null;
    const capLimit = visualStyle === "segmented" ? capLimitSegmented : capLimitClustered;
    const sliced = existingReactions.slice(0, capLimit);
    return {
      /**
       * Accumulated reaction count of capped reaction types, first four in case of
       * segmented(top) and first five in case of clustered(top/bottom) variations.
       */
      reactionCountToDisplay: sliced.reduce(
        (accumulatedCount, { reactionCount }) => accumulatedCount + reactionCount,
        0
      ),
      reactionsToDisplay: sliced
    };
  }, [
    capLimitClustered,
    capLimitSegmented,
    existingReactions,
    verticalPosition,
    visualStyle
  ]);
  if (!hasReactions) return null;
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      "div",
      {
        "aria-label": t2("aria/Reaction list"),
        className: clsx_default("str-chat__message-reactions", {
          [`str-chat__message-reactions--flipped-horizontally`]: flipHorizontalPosition,
          [`str-chat__message-reactions--${verticalPosition}`]: typeof verticalPosition === "string",
          [`str-chat__message-reactions--${visualStyle}`]: typeof visualStyle === "string"
        }),
        ref: divRef,
        role: "figure",
        children: (0, import_jsx_runtime4.jsxs)(
          FragmentOrButton,
          {
            "aria-expanded": isDialogOpen,
            "aria-pressed": isDialogOpen,
            buttonIf: visualStyle === "clustered",
            className: "str-chat__message-reactions__list-button",
            "data-testid": "message-reactions-list-button",
            onClick: () => handleReactionButtonClick(null),
            children: [
              (0, import_jsx_runtime4.jsxs)("ul", { className: "str-chat__message-reactions__list", children: [
                (cappedExistingReactions?.reactionsToDisplay ?? existingReactions).map(
                  ({ EmojiComponent, reactionCount, reactionType }) => EmojiComponent && (0, import_jsx_runtime4.jsx)(
                    "li",
                    {
                      className: "str-chat__message-reactions__list-item",
                      "data-testid": "message-reactions-list-item",
                      children: (0, import_jsx_runtime4.jsxs)(
                        FragmentOrButton,
                        {
                          buttonIf: visualStyle === "segmented",
                          className: "str-chat__message-reactions__list-item-button",
                          onClick: () => handleReactionButtonClick(reactionType),
                          children: [
                            (0, import_jsx_runtime4.jsx)(
                              "span",
                              {
                                className: "str-chat__message-reactions__list-item-icon",
                                "data-testid": "message-reactions-list-item-icon",
                                children: (0, import_jsx_runtime4.jsx)(EmojiComponent, {})
                              }
                            ),
                            visualStyle === "segmented" && reactionCount > 1 && (0, import_jsx_runtime4.jsx)(
                              "span",
                              {
                                className: "str-chat__message-reactions__list-item-count",
                                "data-testclass": "message-reactions-item-count",
                                children: reactionCount
                              }
                            )
                          ]
                        }
                      )
                    },
                    reactionType
                  )
                ),
                uniqueReactionTypeCount > 4 && cappedExistingReactions && visualStyle === "segmented" && (0, import_jsx_runtime4.jsx)("li", { className: "str-chat__message-reactions__list-item str-chat__message-reactions__list-item--more", children: (0, import_jsx_runtime4.jsx)(
                  "button",
                  {
                    className: "str-chat__message-reactions__list-item-button",
                    onClick: () => handleReactionButtonClick(null),
                    children: (0, import_jsx_runtime4.jsxs)("span", { className: "str-chat__message-reactions__overflow-count", children: [
                      "+",
                      totalReactionCount - cappedExistingReactions.reactionCountToDisplay
                    ] })
                  }
                ) })
              ] }),
              visualStyle === "clustered" && (0, import_jsx_runtime4.jsx)(
                "span",
                {
                  className: "str-chat__message-reactions__total-count",
                  "data-testid": "message-reactions-total-count",
                  children: totalReactionCount
                }
              )
            ]
          }
        )
      }
    ),
    (0, import_jsx_runtime4.jsx)(
      DialogAnchor,
      {
        dialogManagerId: dialogManager?.id,
        id: dialogId2,
        offset: 8,
        placement: isMyMessage() ? "bottom-end" : "bottom-start",
        referenceElement: divRef.current,
        trapFocus: true,
        updatePositionOnContentResize: true,
        children: (0, import_jsx_runtime4.jsx)(
          MessageReactionsDetail$1,
          {
            handleFetchReactions,
            onSelectedReactionTypeChange: setSelectedReactionType,
            reactionDetailsSort,
            reactionGroups,
            reactions: existingReactions,
            selectedReactionType,
            totalReactionCount
          }
        )
      }
    )
  ] });
};
var MessageReactions = import_react29.default.memo(
  UnMemoizedMessageReactions
);
var getImageDimensions = (source) => new Promise((resolve, reject) => {
  const image3 = new Image();
  image3.addEventListener(
    "load",
    () => {
      resolve([image3.width, image3.height]);
    },
    { once: true }
  );
  image3.addEventListener("error", () => reject(`Couldn't load image from ${source}`), {
    once: true
  });
  image3.src = source;
});
var SpriteImage = ({
  columns,
  fallback,
  height,
  position: position3,
  rows,
  spriteUrl,
  style,
  width
}) => {
  const [[spriteWidth, spriteHeight], setSpriteDimensions] = (0, import_react29.useState)([0, 0]);
  (0, import_react29.useEffect)(() => {
    getImageDimensions(spriteUrl).then(setSpriteDimensions).catch(console.error);
  }, [spriteUrl]);
  const [x2, y2] = position3;
  if (!spriteHeight || !spriteWidth) return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: fallback });
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      "data-testid": "sprite-image",
      style: {
        ...style,
        "--str-chat__sprite-image-resize-ratio": "var(--str-chat__sprite-image-resize-ratio-x, var(--str-chat__sprite-image-resize-ratio-y, 1))",
        "--str-chat__sprite-image-resize-ratio-x": "calc(var(--str-chat__sprite-image-width) / var(--str-chat__sprite-item-width))",
        "--str-chat__sprite-image-resize-ratio-y": "calc(var(--str-chat__sprite-image-height) / var(--str-chat__sprite-item-height))",
        "--str-chat__sprite-item-height": `${spriteHeight / rows}`,
        "--str-chat__sprite-item-width": `${spriteWidth / columns}`,
        ...Number.isFinite(height) ? { "--str-chat__sprite-image-height": `${height}px` } : {},
        ...Number.isFinite(width) ? { "--str-chat__sprite-image-width": `${width}px` } : {},
        backgroundImage: `url('${spriteUrl}')`,
        backgroundPosition: `${x2 * (100 / (columns - 1))}% ${y2 * (100 / (rows - 1))}%`,
        backgroundSize: `${columns * 100}% ${rows * 100}%`,
        height: "var(--str-chat__sprite-image-height, calc(var(--str-chat__sprite-item-height) * var(--str-chat__sprite-image-resize-ratio)))",
        width: "var(--str-chat__sprite-image-width, calc(var(--str-chat__sprite-item-width) * var(--str-chat__sprite-image-resize-ratio)))"
      }
    }
  );
};
var ReactionSelectorWithButton = ({
  ReactionIcon
}) => {
  const { t: t2 } = useTranslationContext("ReactionSelectorWithButton");
  const { isMyMessage, message, threadList } = useMessageContext();
  const { ReactionSelector: ReactionSelector$1 = ReactionSelector } = useComponentContext();
  const buttonRef = (0, import_react29.useRef)(null);
  const dialogId2 = ReactionSelector.getDialogId({
    messageId: message.id,
    threadList
  });
  const { dialog, dialogManager } = useDialogOnNearestManager({ id: dialogId2 });
  const dialogIsOpen = useDialogIsOpen(dialogId2, dialogManager?.id);
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      DialogAnchor,
      {
        dialogManagerId: dialogManager?.id,
        id: dialogId2,
        offset: 8,
        placement: isMyMessage() ? "top-end" : "top-start",
        referenceElement: buttonRef.current,
        trapFocus: true,
        updatePositionOnContentResize: true,
        children: (0, import_jsx_runtime4.jsx)(ReactionSelector$1, {})
      }
    ),
    (0, import_jsx_runtime4.jsx)(
      QuickMessageActionsButton,
      {
        "aria-expanded": dialogIsOpen,
        "aria-label": t2("aria/Open Reaction Selector"),
        className: "str-chat__message-reactions-button",
        "data-testid": "message-reaction-action",
        onClick: () => dialog?.toggle(),
        ref: buttonRef,
        children: (0, import_jsx_runtime4.jsx)(ReactionIcon, { className: "str-chat__message-action-icon" })
      }
    )
  ] });
};
var getErrorMessage$1 = (error2, fallback) => error2 instanceof Error && error2.message ? error2.message : fallback;
var getNotificationError$1 = (error2) => {
  if (error2 instanceof Error) return error2;
  if (typeof error2 === "string") return new Error(error2);
  if (error2 && typeof error2 === "object" && "message" in error2) {
    const message = error2.message;
    if (typeof message === "string") return new Error(message);
  }
  return void 0;
};
var RemindMeSubmenuHeader = () => {
  const { t: t2 } = useTranslationContext();
  const { returnToParentMenu } = useContextMenuContext();
  return (0, import_jsx_runtime4.jsx)(ContextMenuHeader, { children: (0, import_jsx_runtime4.jsxs)(ContextMenuBackButton, { onClick: returnToParentMenu, children: [
    (0, import_jsx_runtime4.jsx)(IconChevronLeft, {}),
    (0, import_jsx_runtime4.jsx)("span", { children: t2("Remind Me") })
  ] }) });
};
var RemindMeSubmenu = () => {
  const { t: t2 } = useTranslationContext();
  const { client } = useChatContext();
  const { message } = useMessageContext();
  const { closeMenu } = useContextMenuContext();
  const { addNotification } = useNotificationApi();
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      "aria-label": t2("aria/Remind Me Options"),
      className: "str-chat__message-actions-box__submenu",
      role: "listbox",
      children: client.reminders.scheduledOffsetsMs.map((offsetMs) => (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          className: "str-chat__message-actions-list-item-button",
          onClick: async () => {
            try {
              await client.reminders.upsertReminder({
                messageId: message.id,
                remind_at: new Date((/* @__PURE__ */ new Date()).getTime() + offsetMs).toISOString()
              });
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                message: t2("Reminder set"),
                severity: "success",
                type: "api:message:reminder:set:success"
              });
            } catch (error2) {
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                error: getNotificationError$1(error2),
                message: getErrorMessage$1(error2, "Error setting reminder"),
                severity: "error",
                type: "api:message:reminder:set:failed"
              });
            } finally {
              closeMenu();
            }
          },
          children: t2("duration/Remind Me", { milliseconds: offsetMs })
        },
        `reminder-offset-option--${offsetMs}`
      ))
    }
  );
};
var QuickMessageActionsButton = ({ className, ...props }) => (0, import_jsx_runtime4.jsx)(
  Button,
  {
    appearance: "ghost",
    circular: true,
    className: clsx_default("str-chat__message-actions-box-button", className),
    size: "sm",
    variant: "secondary",
    ...props
  }
);
var DeleteMessageAlert = ({ onCancel, onDelete }) => {
  const { t: t2 } = useTranslationContext();
  const { close } = useModalContext();
  return (0, import_jsx_runtime4.jsxs)(
    Alert.Root,
    {
      className: "str-chat__delete-message-alert",
      "data-testid": "message-delete-alert",
      children: [
        (0, import_jsx_runtime4.jsx)(
          Alert.Header,
          {
            description: t2("Are you sure you want to delete this message?"),
            title: t2("Delete message")
          }
        ),
        (0, import_jsx_runtime4.jsxs)(Alert.Actions, { children: [
          (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "outline",
              className: "str-chat__delete-message-alert__delete-button",
              "data-testid": "delete-message-alert-delete-button",
              onClick: onDelete,
              size: "md",
              variant: "danger",
              children: t2("Delete message")
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "outline",
              className: "str-chat__delete-message-alert__cancel-button",
              "data-testid": "delete-message-alert-cancel-button",
              onClick: () => {
                onCancel();
                close();
              },
              size: "md",
              variant: "secondary",
              children: t2("Cancel")
            }
          )
        ] })
      ]
    }
  );
};
var isDownloadableAttachment = (attachment) => {
  if (!attachment || typeof attachment !== "object") return false;
  const typedAttachment = attachment;
  return isFileAttachment(typedAttachment) || isImageAttachment(typedAttachment) || isVideoAttachment(typedAttachment) || isAudioAttachment(typedAttachment) || isVoiceRecordingAttachment(typedAttachment);
};
var triggerUrlDownload = ({
  filename,
  openInNewTab,
  revokeObjectUrl,
  url
}) => {
  const anchor = document.createElement("a");
  anchor.download = filename;
  anchor.href = url;
  anchor.rel = "noopener noreferrer";
  if (openInNewTab && !url.startsWith("blob:")) {
    anchor.target = "_blank";
  }
  document.body.append(anchor);
  anchor.click();
  anchor.remove();
  const shouldRevokeObjectUrl = revokeObjectUrl ?? url.startsWith("blob:");
  if (shouldRevokeObjectUrl && url.startsWith("blob:")) {
    setTimeout(() => URL.revokeObjectURL(url), 0);
  }
};
var fetchRemoteAttachmentAsObjectUrl = async (url) => {
  if (typeof fetch !== "function") return null;
  const response = await fetch(url);
  if (!response.ok) {
    throw new Error(`Failed to fetch attachment, status ${response.status}`);
  }
  const blob = await response.blob();
  return URL.createObjectURL(blob);
};
var downloadAttachment = async (attachment, options = {}) => {
  const { openRemoteInNewTab = true, preferRemoteFetch = true } = options;
  const titleFallback = attachment.title ?? "downloaded-attachment";
  const rawFile = attachment.localMetadata?.file;
  if (rawFile instanceof Blob) {
    const filename2 = rawFile instanceof File && rawFile.name ? rawFile.name : titleFallback;
    triggerUrlDownload({
      filename: filename2,
      url: URL.createObjectURL(rawFile)
    });
    return;
  }
  const filename = rawFile && typeof rawFile === "object" && "name" in rawFile && typeof rawFile.name === "string" ? rawFile.name : titleFallback;
  const url = attachment.asset_url ?? attachment.image_url;
  if (!url) return;
  if (preferRemoteFetch) {
    try {
      const objectUrl = await fetchRemoteAttachmentAsObjectUrl(url);
      if (objectUrl) {
        triggerUrlDownload({
          filename,
          url: objectUrl
        });
        return;
      }
    } catch {
    }
  }
  triggerUrlDownload({ filename, openInNewTab: openRemoteInNewTab, url });
};
var downloadAllAttachments = async (attachments) => {
  for (const attachment of attachments) {
    await downloadAttachment(attachment, {
      openRemoteInNewTab: true,
      preferRemoteFetch: true
    });
  }
};
var msgActionsBoxButtonClassName = "str-chat__message-actions-list-item-button";
var getErrorMessage = (error2, fallback) => error2 instanceof Error && error2.message ? error2.message : fallback;
var getNotificationError = (error2) => {
  if (error2 instanceof Error) return error2;
  if (typeof error2 === "string") return new Error(error2);
  if (error2 && typeof error2 === "object" && "message" in error2) {
    const message = error2.message;
    if (typeof message === "string") return new Error(message);
  }
  return void 0;
};
var DefaultMessageActionComponents = {
  dropdown: {
    React() {
      const { ReactionSelector: ReactionSelector$1 = ReactionSelector } = useComponentContext();
      const { anchorReferenceElement } = useContextMenuContext();
      const { isMyMessage, message, threadList } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      const [referenceElement, setReferenceElement] = (0, import_react29.useState)(null);
      const dialogId2 = `${ReactionSelector.getDialogId({
        messageId: message.id,
        threadList
      })}-dropdown`;
      const { dialog, dialogManager } = useDialogOnNearestManager({
        id: dialogId2
      });
      const dialogIsOpen = useDialogIsOpen(dialogId2, dialogManager?.id);
      return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)(
          DialogAnchor,
          {
            dialogManagerId: dialogManager?.id,
            id: dialogId2,
            offset: 8,
            placement: isMyMessage() ? "top-end" : "top-start",
            referenceElement,
            trapFocus: true,
            updatePositionOnContentResize: true,
            children: (0, import_jsx_runtime4.jsx)(ReactionSelector$1, { dialogId: dialogId2 })
          }
        ),
        (0, import_jsx_runtime4.jsx)(
          ContextMenuButton,
          {
            "aria-expanded": dialogIsOpen,
            "aria-label": t2("aria/Open Reaction Selector"),
            className: clsx_default(
              msgActionsBoxButtonClassName,
              "str-chat__message-actions-list-item-button--react"
            ),
            "data-testid": "dropdown-react-action",
            Icon: IconEmoji,
            onClick: (event) => {
              if (dialogIsOpen) {
                dialog.close();
                return;
              }
              setReferenceElement(
                anchorReferenceElement instanceof HTMLElement ? anchorReferenceElement : event.currentTarget
              );
              dialog.open();
            },
            children: t2("Add reaction")
          }
        )
      ] });
    },
    ThreadReply() {
      const { closeMenu } = useContextMenuContext();
      const { handleOpenThread } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Open Thread"),
          className: msgActionsBoxButtonClassName,
          "data-testid": "thread-action",
          Icon: IconThread,
          onClick: (e) => {
            handleOpenThread(e);
            closeMenu();
          },
          children: t2("Thread Reply")
        }
      );
    },
    Quote() {
      const { closeMenu } = useContextMenuContext();
      const { message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      const messageComposer = useMessageComposerController();
      const handleQuote = () => {
        messageComposer.setQuotedMessage(message);
        const elements = message.parent_id ? document.querySelectorAll(".str-chat__thread .str-chat__textarea__textarea") : document.getElementsByClassName("str-chat__textarea__textarea");
        const textarea = elements.item(0);
        if (textarea instanceof HTMLTextAreaElement) {
          textarea.focus();
        }
      };
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Quote Message"),
          className: msgActionsBoxButtonClassName,
          Icon: IconQuote,
          onClick: () => {
            handleQuote();
            closeMenu();
          },
          children: t2("Quote Reply")
        }
      );
    },
    Download() {
      const { closeMenu, openSubmenu } = useContextMenuContext();
      const { message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      const downloadableAttachments = (message.attachments ?? []).filter(
        isDownloadableAttachment
      );
      if (!downloadableAttachments.length) return null;
      const DownloadSubmenuHeader = () => {
        const { returnToParentMenu: goBack } = useContextMenuContext();
        const { t: translate } = useTranslationContext();
        return (0, import_jsx_runtime4.jsx)(ContextMenuHeader, { children: (0, import_jsx_runtime4.jsxs)(ContextMenuBackButton, { onClick: goBack, children: [
          (0, import_jsx_runtime4.jsx)(IconChevronLeft, {}),
          (0, import_jsx_runtime4.jsx)("span", { children: translate("Download Attachment") })
        ] }) });
      };
      const DownloadSubmenu = () => (0, import_jsx_runtime4.jsxs)(
        "div",
        {
          "aria-label": t2("aria/Download attachment"),
          className: "str-chat__message-actions-box__submenu str-chat__message-actions-box__submenu--download-attachments",
          role: "listbox",
          children: [
            downloadableAttachments.map((attachment, index6) => {
              const fileName = attachment.localMetadata?.file?.name ?? attachment.title ?? t2("Download Attachment");
              return (0, import_jsx_runtime4.jsx)(
                ContextMenuButton,
                {
                  className: msgActionsBoxButtonClassName,
                  Icon: IconDownload,
                  onClick: () => {
                    void downloadAttachment(attachment);
                    closeMenu();
                  },
                  children: `Download ${fileName}`
                },
                attachment.localMetadata?.id ?? attachment.asset_url ?? attachment.image_url ?? `${fileName}-${index6}`
              );
            }),
            (0, import_jsx_runtime4.jsx)(
              ContextMenuButton,
              {
                className: msgActionsBoxButtonClassName,
                Icon: IconDownload,
                onClick: () => {
                  void downloadAllAttachments(downloadableAttachments);
                  closeMenu();
                },
                children: t2("Download All")
              }
            )
          ]
        }
      );
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Download attachment"),
          className: msgActionsBoxButtonClassName,
          hasSubMenu: downloadableAttachments.length > 1,
          Icon: IconDownload,
          onClick: () => {
            if (downloadableAttachments.length > 1) {
              openSubmenu({
                Header: DownloadSubmenuHeader,
                Submenu: DownloadSubmenu
              });
              return;
            }
            void downloadAttachment(downloadableAttachments[0]);
            closeMenu();
          },
          children: t2("Download Attachment")
        }
      );
    },
    Pin() {
      const { closeMenu } = useContextMenuContext();
      const { handlePin, message } = useMessageContext();
      const { addNotification } = useNotificationApi();
      const { t: t2 } = useTranslationContext();
      const isPinned = !!message.pinned;
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": isPinned ? t2("aria/Unpin Message") : t2("aria/Pin Message"),
          className: msgActionsBoxButtonClassName,
          Icon: isPinned ? IconUnpin : IconPin,
          onClick: async (event) => {
            try {
              await handlePin(event);
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                message: isPinned ? t2("Message unpinned") : t2("Message pinned"),
                severity: "success",
                type: isPinned ? "api:message:unpin:success" : "api:message:pin:success"
              });
            } catch (error2) {
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                error: getNotificationError(error2),
                message: getErrorMessage(
                  error2,
                  isPinned ? t2("Error removing message pin") : t2("Error pinning message")
                ),
                severity: "error",
                type: isPinned ? "api:message:unpin:failed" : "api:message:pin:failed"
              });
            }
            closeMenu();
          },
          children: isPinned ? t2("Unpin") : t2("Pin")
        }
      );
    },
    CopyMessageText() {
      const { closeMenu } = useContextMenuContext();
      const { message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Copy Message Text"),
          className: msgActionsBoxButtonClassName,
          Icon: IconCopy,
          onClick: () => {
            if (message.text) navigator.clipboard.writeText(message.text);
            closeMenu();
          },
          children: t2("Copy Message")
        }
      );
    },
    Resend() {
      const { closeMenu } = useContextMenuContext();
      const { handleRetry, message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Resend Message"),
          className: msgActionsBoxButtonClassName,
          Icon: IconRetry,
          onClick: () => {
            handleRetry(message);
            closeMenu();
          },
          children: t2("Resend")
        }
      );
    },
    Edit() {
      const messageComposer = useMessageComposerController();
      const { message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      const { closeMenu } = useContextMenuContext();
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Edit Message"),
          className: msgActionsBoxButtonClassName,
          Icon: IconEdit,
          onClick: () => {
            savePreEditSnapshot(messageComposer);
            messageComposer.initState({ composition: message });
            closeMenu();
          },
          children: t2("Edit Message")
        }
      );
    },
    MarkUnread() {
      const { closeMenu } = useContextMenuContext();
      const { handleMarkUnread, message } = useMessageContext();
      const { addNotification } = useNotificationApi();
      const { t: t2 } = useTranslationContext();
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Mark Message Unread"),
          className: msgActionsBoxButtonClassName,
          Icon: IconNotification,
          onClick: async (event) => {
            try {
              await handleMarkUnread(event);
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                message: t2("Message marked as unread"),
                severity: "success",
                type: "api:message:markUnread:success"
              });
            } catch (error2) {
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                error: getNotificationError(error2),
                message: getErrorMessage(
                  error2,
                  t2(
                    "Error marking message unread. Cannot mark unread messages older than the newest 100 channel messages."
                  )
                ),
                severity: "error",
                type: "api:message:markUnread:failed"
              });
            }
            closeMenu();
          },
          children: t2("Mark as unread")
        }
      );
    },
    RemindMe() {
      const { closeMenu, openSubmenu } = useContextMenuContext();
      const { client } = useChatContext();
      const { addNotification } = useNotificationApi();
      const { t: t2 } = useTranslationContext();
      const { message } = useMessageContext();
      const reminder = useMessageReminder(message.id);
      const messageAlreadyBookmarked = reminder && !reminder?.remindAt;
      if (messageAlreadyBookmarked) return null;
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": reminder ? t2("aria/Remind Me Message") : t2("aria/Remove Reminder"),
          className: msgActionsBoxButtonClassName,
          hasSubMenu: !reminder,
          Icon: reminder ? IconBellOff : IconBell,
          onClick: async () => {
            if (reminder) {
              try {
                await client.reminders.deleteReminder(reminder.id);
                addNotification({
                  context: {
                    message
                  },
                  emitter: "MessageActions",
                  message: t2("Remove reminder"),
                  severity: "success",
                  type: "api:message:reminder:delete:success"
                });
              } catch (error2) {
                addNotification({
                  context: {
                    message
                  },
                  emitter: "MessageActions",
                  error: getNotificationError(error2),
                  message: getErrorMessage(error2, "Error removing reminder"),
                  severity: "error",
                  type: "api:message:reminder:delete:failed"
                });
              } finally {
                closeMenu();
              }
            } else {
              openSubmenu({
                Header: RemindMeSubmenuHeader,
                Submenu: RemindMeSubmenu
              });
            }
          },
          children: reminder ? t2("Remove reminder") : t2("Remind me")
        }
      );
    },
    SaveForLater() {
      const { closeMenu } = useContextMenuContext();
      const { client } = useChatContext();
      const { addNotification } = useNotificationApi();
      const { message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      const reminder = useMessageReminder(message.id);
      const messageAlreadyHasReminderScheduled = Boolean(reminder && reminder?.remindAt);
      if (messageAlreadyHasReminderScheduled) return null;
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": reminder ? t2("aria/Remove Save For Later") : t2("aria/Bookmark Message"),
          className: msgActionsBoxButtonClassName,
          Icon: reminder ? IconBookmarkRemove : IconBookmark,
          onClick: async () => {
            try {
              if (reminder) {
                await client.reminders.deleteReminder(reminder.id);
                addNotification({
                  context: {
                    message
                  },
                  emitter: "MessageActions",
                  message: t2("Remove save for later"),
                  severity: "success",
                  type: "api:message:saveForLater:delete:success"
                });
              } else {
                await client.reminders.createReminder({ messageId: message.id });
                addNotification({
                  context: {
                    message
                  },
                  emitter: "MessageActions",
                  message: t2("Saved for later"),
                  severity: "success",
                  type: "api:message:saveForLater:create:success"
                });
              }
            } catch (error2) {
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                error: getNotificationError(error2),
                message: getErrorMessage(
                  error2,
                  reminder ? "Error removing message from saved for later" : "Error saving message for later"
                ),
                severity: "error",
                type: reminder ? "api:message:saveForLater:delete:failed" : "api:message:saveForLater:create:failed"
              });
            } finally {
              closeMenu();
            }
          },
          children: reminder ? t2("Remove save for later") : t2("Save for later")
        }
      );
    },
    Flag() {
      const { closeMenu } = useContextMenuContext();
      const { handleFlag, message } = useMessageContext();
      const { addNotification } = useNotificationApi();
      const { t: t2 } = useTranslationContext();
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": t2("aria/Flag Message"),
          className: msgActionsBoxButtonClassName,
          Icon: IconFlag,
          onClick: async (event) => {
            try {
              await handleFlag(event);
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                message: t2("Message has been successfully flagged"),
                severity: "success",
                type: "api:message:flag:success"
              });
            } catch (error2) {
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                error: getNotificationError(error2),
                message: getErrorMessage(error2, t2("Error adding flag")),
                severity: "error",
                type: "api:message:flag:failed"
              });
            }
            closeMenu();
          },
          children: t2("Flag")
        }
      );
    },
    Mute() {
      const { closeMenu } = useContextMenuContext();
      const { handleMute, message } = useMessageContext();
      const { addNotification } = useNotificationApi();
      const { mutes } = useChatContext();
      const { t: t2 } = useTranslationContext();
      const isMuted = isUserMuted(message, mutes);
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": isMuted ? t2("aria/Unmute User") : t2("aria/Mute User"),
          className: msgActionsBoxButtonClassName,
          Icon: isMuted ? IconAudio : IconMute,
          onClick: async (event) => {
            try {
              await handleMute(event);
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                message: isMuted ? t2("{{ user }} has been unmuted", {
                  user: message.user?.name || message.user?.id
                }) : t2("{{ user }} has been muted", {
                  user: message.user?.name || message.user?.id
                }),
                severity: "success",
                type: isMuted ? "api:user:unmute:success" : "api:user:mute:success"
              });
            } catch (error2) {
              addNotification({
                context: {
                  message
                },
                emitter: "MessageActions",
                error: getNotificationError(error2),
                message: getErrorMessage(
                  error2,
                  isMuted ? t2("Error unmuting a user ...") : t2("Error muting a user ...")
                ),
                severity: "error",
                type: isMuted ? "api:user:unmute:failed" : "api:user:mute:failed"
              });
            }
            closeMenu();
          },
          children: isMuted ? t2("Unmute") : t2("Mute")
        }
      );
    },
    Delete() {
      const { closeMenu } = useContextMenuContext();
      const { addNotification } = useNotificationApi();
      const { Modal = GlobalModal } = useComponentContext();
      const { handleDelete: handleDelete2, message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      const [openModal, setOpenModal] = (0, import_react29.useState)(false);
      if (isMessageDeleted(message)) return null;
      return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)(
          ContextMenuButton,
          {
            "aria-label": t2("aria/Delete Message"),
            className: msgActionsBoxButtonClassName,
            Icon: IconDelete,
            onClick: () => {
              setOpenModal(true);
            },
            variant: "destructive",
            children: t2("Delete message")
          }
        ),
        (0, import_jsx_runtime4.jsx)(Modal, { open: openModal, children: (0, import_jsx_runtime4.jsx)(
          DeleteMessageAlert,
          {
            onCancel: () => {
              setOpenModal(false);
              closeMenu();
            },
            onDelete: async () => {
              try {
                await handleDelete2();
                addNotification({
                  context: {
                    message
                  },
                  emitter: "MessageActions",
                  message: t2("Message deleted"),
                  severity: "success",
                  type: "api:message:delete:success"
                });
              } catch (error2) {
                addNotification({
                  context: {
                    message
                  },
                  emitter: "MessageActions",
                  error: getNotificationError(error2),
                  message: getErrorMessage(error2, t2("Error deleting message")),
                  severity: "error",
                  type: "api:message:delete:failed"
                });
              } finally {
                setOpenModal(false);
                closeMenu();
              }
            }
          }
        ) })
      ] });
    },
    BlockUser() {
      const { closeMenu } = useContextMenuContext();
      const { client } = useChatContext();
      const { message } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      const isBlocked = !message.user?.id || new Set(client.blockedUsers.getLatestValue().userIds).has(message.user?.id);
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": isBlocked ? t2("aria/Unblock User") : t2("aria/Block User"),
          className: clsx_default(msgActionsBoxButtonClassName),
          Icon: isBlocked ? IconUserCheck : IconNoSign,
          onClick: () => {
            const targetId = message.user?.id;
            if (targetId) {
              if (isBlocked) client.unBlockUser(targetId);
              else client.blockUser(targetId);
            }
            closeMenu();
          },
          children: isBlocked ? t2("Unblock User") : t2("Block User")
        }
      );
    }
  },
  quick: {
    // eslint-disable-next-line react/display-name
    DropdownToggle: (0, import_react29.forwardRef)((_2, ref) => {
      const { t: t2 } = useTranslationContext();
      const { message, threadList } = useMessageContext();
      const dropdownDialogIsOpen = useDialogIsOpen(
        MessageActions.getDialogId({ messageId: message.id })
      );
      const { dialog } = useDialogOnNearestManager({
        id: MessageActions.getDialogId({ messageId: message.id })
      });
      const reactionSelectorDialogId = ReactionSelector.getDialogId({
        messageId: message.id,
        threadList
      });
      const { dialog: dropdownReactionSelectorDialog } = useDialogOnNearestManager({
        id: `${reactionSelectorDialogId}-dropdown`
      });
      return (0, import_jsx_runtime4.jsx)(
        QuickMessageActionsButton,
        {
          "aria-expanded": dropdownDialogIsOpen,
          "aria-haspopup": "true",
          "aria-label": t2("aria/Open Message Actions Menu"),
          className: "str-chat__message-actions-box-button",
          "data-testid": "message-actions-toggle-button",
          onClick: () => {
            dropdownReactionSelectorDialog?.close();
            dialog?.toggle();
          },
          ref,
          children: (0, import_jsx_runtime4.jsx)(IconMore, { className: "str-chat__message-action-icon" })
        }
      );
    }),
    React() {
      return (0, import_jsx_runtime4.jsx)(ReactionSelectorWithButton, { ReactionIcon: IconEmoji });
    },
    Reply() {
      const { handleOpenThread } = useMessageContext();
      const { t: t2 } = useTranslationContext();
      return (0, import_jsx_runtime4.jsx)(
        QuickMessageActionsButton,
        {
          "aria-label": t2("aria/Open Thread"),
          className: "str-chat__message-reply-in-thread-button",
          "data-testid": "thread-action",
          onClick: handleOpenThread,
          children: (0, import_jsx_runtime4.jsx)(IconReply, { className: "str-chat__message-action-icon" })
        }
      );
    }
  }
};
var defaultMessageActionSet = [
  {
    Component: DefaultMessageActionComponents.quick.DropdownToggle,
    placement: "quick-dropdown-toggle"
  },
  {
    Component: DefaultMessageActionComponents.quick.Reply,
    placement: "quick",
    type: "reply"
  },
  {
    Component: DefaultMessageActionComponents.quick.React,
    placement: "quick",
    type: "react"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.React,
    placement: "dropdown",
    type: "react"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.ThreadReply,
    placement: "dropdown",
    type: "reply"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Quote,
    placement: "dropdown",
    type: "quote"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Download,
    placement: "dropdown",
    type: "download"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Pin,
    placement: "dropdown",
    type: "pin"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.CopyMessageText,
    placement: "dropdown",
    type: "copyMessageText"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Resend,
    placement: "dropdown",
    type: "resendMessage"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Edit,
    placement: "dropdown",
    type: "edit"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.MarkUnread,
    placement: "dropdown",
    type: "markUnread"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.RemindMe,
    placement: "dropdown",
    type: "remindMe"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.SaveForLater,
    placement: "dropdown",
    type: "saveForLater"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Flag,
    placement: "dropdown",
    type: "flag"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Mute,
    placement: "dropdown",
    type: "mute"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.Delete,
    placement: "dropdown",
    type: "delete"
  },
  {
    Component: DefaultMessageActionComponents.dropdown.BlockUser,
    placement: "dropdown",
    type: "blockUser"
  }
];
var useSplitActionSet = (actionSet) => (0, import_react29.useMemo)(() => {
  const quickActionSet = [];
  const dropdownActionSet = [];
  let quickDropdownToggleAction;
  for (const action of actionSet) {
    if (action.placement === "quick")
      quickActionSet.push(action);
    if (action.placement === "dropdown")
      dropdownActionSet.push(action);
    if (action.placement === "quick-dropdown-toggle") {
      quickDropdownToggleAction ?? (quickDropdownToggleAction = action);
    }
  }
  return { dropdownActionSet, quickActionSet, quickDropdownToggleAction };
}, [actionSet]);
var MessageActions = ({
  disableBaseMessageActionSetFilter = false,
  messageActionSet = defaultMessageActionSet
}) => {
  const { isMyMessage, message, threadList } = useMessageContext();
  const { ContextMenu: ContextMenuComponent = ContextMenu } = useComponentContext();
  const { t: t2 } = useTranslationContext();
  const [actionsBoxButtonElement, setActionsBoxButtonElement] = (0, import_react29.useState)(null);
  const filteredMessageActionSet = useBaseMessageActionSetFilter(
    messageActionSet,
    disableBaseMessageActionSetFilter
  );
  const { dropdownActionSet, quickActionSet, quickDropdownToggleAction } = useSplitActionSet(filteredMessageActionSet);
  const messageActionsDialogId = MessageActions.getDialogId({ messageId: message.id });
  const reactionSelectorDialogId = ReactionSelector.getDialogId({
    messageId: message.id,
    threadList
  });
  const dropdownReactionSelectorDialogId = `${reactionSelectorDialogId}-dropdown`;
  const { dialog, dialogManager } = useDialogOnNearestManager({
    id: messageActionsDialogId
  });
  const messageActionsDialogIsOpen = useDialogIsOpen(
    messageActionsDialogId,
    dialogManager?.id
  );
  const reactionSelectorDialogIsOpen = useDialogIsOpen(
    reactionSelectorDialogId,
    dialogManager?.id
  );
  const dropdownReactionSelectorDialogIsOpen = useDialogIsOpen(
    dropdownReactionSelectorDialogId,
    dialogManager?.id
  );
  if (dropdownActionSet.length + quickActionSet.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__message-options", {
        "str-chat__message-options--active": messageActionsDialogIsOpen || reactionSelectorDialogIsOpen || dropdownReactionSelectorDialogIsOpen
      }),
      children: [
        quickDropdownToggleAction && dropdownActionSet.length > 0 && (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          (0, import_jsx_runtime4.jsx)(quickDropdownToggleAction.Component, { ref: setActionsBoxButtonElement }),
          (0, import_jsx_runtime4.jsx)(
            ContextMenuComponent,
            {
              backLabel: t2("Back"),
              className: clsx_default("str-chat__message-actions-box", {
                "str-chat__message-actions-box--hidden": dropdownReactionSelectorDialogIsOpen,
                "str-chat__message-actions-box--open": messageActionsDialogIsOpen
              }),
              dialogManagerId: dialogManager?.id,
              id: messageActionsDialogId,
              onClose: dialog?.close,
              placement: isMyMessage() ? "top-end" : "top-start",
              referenceElement: actionsBoxButtonElement,
              tabIndex: -1,
              trapFocus: true,
              children: dropdownActionSet.map(({ Component: Component2, type }) => (0, import_jsx_runtime4.jsx)(Component2, {}, type))
            }
          )
        ] }),
        quickActionSet.map(({ Component: QuickActionComponent, type }) => (0, import_jsx_runtime4.jsx)(QuickActionComponent, {}, type))
      ]
    }
  );
};
MessageActions.getDialogId = ({ messageId }) => `message-actions-${messageId}`;
MessageActions.displayName = "MessageActions";
var MessageUIWithContext = ({
  endOfGroup,
  firstOfGroup,
  groupedByUser,
  handleAction,
  handleOpenThread,
  highlighted,
  isMessageAIGenerated,
  isMyMessage,
  message,
  onUserClick,
  onUserHover,
  renderText: renderText2,
  showAvatar = "incoming",
  threadList
}) => {
  const { channel } = useChannelStateContext();
  const { client } = useChatContext();
  const [isBounceDialogOpen, setIsBounceDialogOpen] = (0, import_react29.useState)(false);
  const reminder = useMessageReminder(message.id);
  const {
    Attachment: Attachment$12 = Attachment,
    Avatar: Avatar$1 = Avatar,
    MessageActions: MessageActions$1 = MessageActions,
    MessageAlsoSentInChannelIndicator: MessageAlsoSentInChannelIndicator$1 = MessageAlsoSentInChannelIndicator,
    MessageBlocked: MessageBlocked$1 = MessageBlocked,
    MessageBouncePrompt: MessageBouncePrompt$1 = MessageBouncePrompt,
    MessageDeleted,
    MessageDeletedBubble: MessageDeletedBubble$1 = MessageDeletedBubble,
    MessageEditedIndicator: MessageEditedIndicator$1 = MessageEditedIndicator,
    MessageReactions: MessageReactions$1 = MessageReactions,
    MessageRepliesCountButton: MessageRepliesCountButton$1 = MessageRepliesCountButton,
    MessageStatus: MessageStatus$1 = MessageStatus,
    MessageTimestamp: MessageTimestamp$1 = MessageTimestamp,
    MessageTranslationIndicator: MessageTranslationIndicator$1 = MessageTranslationIndicator,
    PinIndicator: PinIndicator$1 = PinIndicator,
    QuotedMessage: QuotedMessage$1 = QuotedMessage,
    ReminderNotification: ReminderNotification$1 = ReminderNotification,
    StreamedMessageText: StreamedMessageText$1 = StreamedMessageText
  } = useComponentContext();
  const isAIGenerated = (0, import_react29.useMemo)(
    () => isMessageAIGenerated?.(message),
    [isMessageAIGenerated, message]
  );
  const isDeleted = isMessageDeleted(message);
  const finalAttachments = (0, import_react29.useMemo)(
    () => !message.shared_location && !message.attachments ? [] : !message.shared_location ? message.attachments : [message.shared_location, ...message.attachments ?? []],
    [message]
  );
  if (isDateSeparatorMessage(message)) {
    return null;
  }
  if (MessageDeleted && isDeleted) {
    return (0, import_jsx_runtime4.jsx)(MessageDeleted, { message });
  }
  if (isMessageBlocked(message)) {
    return (0, import_jsx_runtime4.jsx)(MessageBlocked$1, {});
  }
  const poll = message.poll_id && client.polls.fromState(message.poll_id);
  const memberCount = Object.keys(channel?.state?.members ?? {}).length;
  const hasAttachment = !isDeleted && messageHasAttachments(message);
  const hasSingleAttachment = !isDeleted && messageHasSingleAttachment(message);
  const hasGiphyAttachment = !isDeleted && messageHasGiphyAttachment(message);
  const hasReactions = !isDeleted && messageHasReactions(message);
  const hasQuotedMessage = !isDeleted && messageHasQuotedMessage(message);
  const textHasEmojisOnly = !isDeleted && messageTextHasEmojisOnly(message);
  const allowRetry = isMessageErrorRetryable(message);
  const isBounced = isMessageBounced(message);
  const isEdited = isMessageEdited(message) && !isAIGenerated;
  const showMetadata = !groupedByUser || endOfGroup;
  const showReplyCountButton = !threadList && !!message.reply_count;
  const rootClassName2 = clsx_default(
    "str-chat__message",
    `str-chat__message--${message.type}`,
    `str-chat__message--${message.status}`,
    {
      "str-chat__message--has-attachment": hasAttachment,
      "str-chat__message--has-giphy-attachment": hasGiphyAttachment,
      "str-chat__message--has-no-text": !message.text,
      "str-chat__message--has-quoted-message": hasQuotedMessage,
      "str-chat__message--has-single-attachment": hasSingleAttachment,
      "str-chat__message--has-text": !!message.text,
      "str-chat__message--highlighted": highlighted,
      "str-chat__message--is-emoji-only": textHasEmojisOnly,
      [`str-chat__message--is-emoji-only-count-${countEmojis(message.text)}`]: textHasEmojisOnly,
      "str-chat__message--me": isMyMessage(),
      "str-chat__message--other": !isMyMessage(),
      "str-chat__message--pinned": message.pinned,
      "str-chat__message--with-avatar": (() => {
        if (!message.user) return false;
        if (showAvatar === "incoming") return !isMyMessage();
        if (showAvatar === "outgoing") return isMyMessage();
        return showAvatar;
      })(),
      "str-chat__message--with-reactions": hasReactions,
      "str-chat__message-send-can-be-retried": message?.status === "failed" && message?.error?.status !== 403,
      "str-chat__message-with-thread-link": showReplyCountButton,
      "str-chat__virtual-message__wrapper--end": endOfGroup,
      "str-chat__virtual-message__wrapper--first": firstOfGroup,
      "str-chat__virtual-message__wrapper--group": groupedByUser
    }
  );
  let handleClick = void 0;
  if (isBounced) {
    handleClick = () => setIsBounceDialogOpen(true);
  }
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    isBounceDialogOpen && (0, import_jsx_runtime4.jsx)(
      MessageBounceModal,
      {
        MessageBouncePrompt: MessageBouncePrompt$1,
        onClose: () => setIsBounceDialogOpen(false),
        open: isBounceDialogOpen
      }
    ),
    (0, import_jsx_runtime4.jsxs)("div", { className: rootClassName2, children: [
      message.pinned && (0, import_jsx_runtime4.jsx)(PinIndicator$1, { message }),
      message.show_in_channel && (0, import_jsx_runtime4.jsx)(MessageAlsoSentInChannelIndicator$1, {}),
      !!reminder && (0, import_jsx_runtime4.jsx)(ReminderNotification$1, { reminder }),
      (0, import_jsx_runtime4.jsx)(MessageTranslationIndicator$1, { message }),
      message.user && (0, import_jsx_runtime4.jsx)(
        Avatar$1,
        {
          className: "str-chat__avatar--with-border",
          imageUrl: message.user.image,
          onClick: onUserClick,
          onMouseOver: onUserHover,
          size: "md",
          userName: message.user.name || message.user.id
        }
      ),
      (0, import_jsx_runtime4.jsxs)(
        "div",
        {
          className: clsx_default("str-chat__message-inner", {
            "str-chat__message-inner--error": allowRetry || isBounced
          }),
          "data-testid": "message-inner",
          onClick: handleClick,
          onKeyUp: handleClick,
          children: [
            !isDeleted && (0, import_jsx_runtime4.jsx)(MessageActions$1, {}),
            showReplyCountButton && (0, import_jsx_runtime4.jsx)(
              MessageRepliesCountButton$1,
              {
                onClick: handleOpenThread,
                reply_count: message.reply_count,
                thread_participants: message.thread_participants
              }
            ),
            isDeleted ? (0, import_jsx_runtime4.jsx)(MessageDeletedBubble$1, {}) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
              (0, import_jsx_runtime4.jsxs)(MessageBubble, { children: [
                poll && (0, import_jsx_runtime4.jsx)(Poll, { poll }),
                message.quoted_message && (0, import_jsx_runtime4.jsx)(QuotedMessage$1, {}),
                finalAttachments?.length ? (0, import_jsx_runtime4.jsx)(
                  Attachment$12,
                  {
                    actionHandler: handleAction,
                    attachments: finalAttachments
                  }
                ) : null,
                isAIGenerated ? (0, import_jsx_runtime4.jsx)(StreamedMessageText$1, { message, renderText: renderText2 }) : (0, import_jsx_runtime4.jsx)(MessageText, { message, renderText: renderText2 })
              ] }),
              (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-reactions-host", children: hasReactions && (0, import_jsx_runtime4.jsx)(MessageReactions$1, { reverse: true }) }),
              (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-error-indicator", children: (0, import_jsx_runtime4.jsx)(ErrorBadge, {}) })
            ] })
          ]
        }
      ),
      showMetadata && (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-metadata", children: [
        (0, import_jsx_runtime4.jsx)(MessageStatus$1, {}),
        !isMyMessage() && !!message.user && memberCount > 2 && (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__message-metadata__name", children: message.user.name || message.user.id }),
        (0, import_jsx_runtime4.jsx)(MessageTimestamp$1, { customClass: "str-chat__message-metadata__timestamp" }),
        !isDeleted && isEdited && (0, import_jsx_runtime4.jsx)(MessageEditedIndicator$1, {})
      ] })
    ] }, message.id)
  ] });
};
var MemoizedMessageUI = import_react29.default.memo(
  MessageUIWithContext,
  areMessageUIPropsEqual
);
var MessageUI = (props) => {
  const messageContext = useMessageContext();
  return (0, import_jsx_runtime4.jsx)(MemoizedMessageUI, { ...messageContext, ...props });
};
var MessageWithContext = (props) => {
  const {
    canPin,
    Message: propMessage,
    message,
    messageActions = Object.keys(MESSAGE_ACTIONS),
    onUserClick: propOnUserClick,
    onUserHover: propOnUserHover,
    userRoles
  } = props;
  const { client, isMessageAIGenerated } = useChatContext("Message");
  const { channelConfig, read } = useChannelStateContext("Message");
  const { Message: contextMessage } = useComponentContext();
  const { getTranslationView, setTranslationView: setTranslationViewInContext } = useMessageTranslationViewContext();
  const translationView = getTranslationView(message.id, !!message.i18n);
  const setTranslationView = (0, import_react29.useCallback)(
    (view) => setTranslationViewInContext(message.id, view),
    [message.id, setTranslationViewInContext]
  );
  const actionsEnabled = message.type === "regular" && message.status === "received";
  const MessageUIComponent = propMessage ?? contextMessage ?? MessageUI;
  const { onUserClick, onUserHover } = useUserHandler(message, {
    onUserClickHandler: propOnUserClick,
    onUserHoverHandler: propOnUserHover
  });
  const {
    canDelete,
    canEdit,
    canFlag,
    canMarkUnread,
    canMute,
    canQuote,
    canReact,
    canReply,
    isMyMessage
  } = userRoles;
  const messageIsUnread = (0, import_react29.useMemo)(
    () => !!(!isMyMessage && client.user?.id && read && (!read[client.user.id] || message?.created_at && new Date(message.created_at).getTime() > read[client.user.id].last_read.getTime())),
    [client, isMyMessage, message.created_at, read]
  );
  const messageActionsHandler = (0, import_react29.useCallback)(
    () => getMessageActions(
      messageActions,
      {
        canDelete,
        canEdit,
        canFlag,
        canMarkUnread,
        canMute,
        canPin,
        canQuote,
        canReact,
        canReply
      },
      channelConfig
    ),
    [
      messageActions,
      canDelete,
      canEdit,
      canFlag,
      canMarkUnread,
      canMute,
      canPin,
      canQuote,
      canReact,
      canReply,
      channelConfig
    ]
  );
  const {
    canPin: canPinPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    messageActions: messageActionsPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    onUserClick: onUserClickPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    onUserHover: onUserHoverPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    userRoles: userRolesPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    ...rest
  } = props;
  const messageContextValue = {
    ...rest,
    actionsEnabled,
    getMessageActions: messageActionsHandler,
    isMessageAIGenerated,
    isMyMessage: () => isMyMessage,
    messageIsUnread,
    onUserClick,
    onUserHover,
    setTranslationView,
    translationView
  };
  return (0, import_jsx_runtime4.jsx)(MessageProvider, { value: messageContextValue, children: (0, import_jsx_runtime4.jsx)(MessageUIComponent, {}) });
};
var MemoizedMessage = import_react29.default.memo(
  MessageWithContext,
  areMessagePropsEqual
);
var Message = (props) => {
  const {
    closeReactionSelectorOnClick,
    disableQuotedMessages,
    message,
    onMentionsClick: propOnMentionsClick,
    onMentionsHover: propOnMentionsHover,
    openThread: propOpenThread,
    reactionDetailsSort,
    retrySendMessage: propRetrySendMessage,
    sortReactions
  } = props;
  const { highlightedMessageId, mutes } = useChannelStateContext("Message");
  const handleAction = useActionHandler(message);
  const handleOpenThread = useOpenThreadHandler(message, propOpenThread);
  const handleReaction = useReactionHandler(message);
  const handleRetry = useRetryHandler(propRetrySendMessage);
  const userRoles = useUserRole(message, disableQuotedMessages);
  const handleFetchReactions = useReactionsFetcher(message);
  const handleDelete2 = useDeleteHandler(message);
  const handleFlag = useFlagHandler(message);
  const handleMarkUnread = useMarkUnreadHandler(message);
  const handleMute = useMuteHandler(message);
  const { onMentionsClick, onMentionsHover } = useMentionsHandler(message, {
    onMentionsClick: propOnMentionsClick,
    onMentionsHover: propOnMentionsHover
  });
  const { canPin, handlePin } = usePinHandler(message);
  const highlighted = highlightedMessageId === message.id;
  return (0, import_jsx_runtime4.jsx)(
    MemoizedMessage,
    {
      additionalMessageComposerProps: props.additionalMessageComposerProps,
      autoscrollToBottom: props.autoscrollToBottom,
      canPin,
      closeReactionSelectorOnClick,
      deliveredTo: props.deliveredTo,
      disableQuotedMessages: props.disableQuotedMessages,
      formatDate: props.formatDate,
      groupStyles: props.groupStyles,
      handleAction,
      handleDelete: handleDelete2,
      handleFetchReactions,
      handleFlag,
      handleMarkUnread,
      handleMute,
      handleOpenThread,
      handlePin,
      handleReaction,
      handleRetry,
      highlighted,
      initialMessage: props.initialMessage,
      lastOwnMessage: props.lastOwnMessage,
      lastReceivedId: props.lastReceivedId,
      message,
      Message: props.Message,
      messageActions: props.messageActions,
      messageListRect: props.messageListRect,
      mutes,
      onMentionsClickMessage: onMentionsClick,
      onMentionsHoverMessage: onMentionsHover,
      onUserClick: props.onUserClick,
      onUserHover: props.onUserHover,
      reactionDetailsSort,
      readBy: props.readBy,
      renderText: props.renderText,
      returnAllReadData: props.returnAllReadData,
      sortReactions,
      threadList: props.threadList,
      unsafeHTML: props.unsafeHTML,
      userRoles
    }
  );
};
var GiphyPreviewMessage = (props) => {
  const { message } = props;
  return (0, import_jsx_runtime4.jsx)("div", { className: "giphy-preview-message", children: (0, import_jsx_runtime4.jsx)(Message, { message }) });
};
var useEnrichedMessages = (args) => {
  const {
    channel,
    disableDateSeparator,
    groupStyles,
    headerPosition,
    hideDeletedMessages,
    hideNewMessageSeparator,
    maxTimeBetweenGroupedMessages,
    messages,
    noGroupByUser,
    reviewProcessedMessage
  } = args;
  const { client } = useChatContext("useEnrichedMessages");
  const { HeaderComponent } = useComponentContext();
  const lastRead = (0, import_react29.useMemo)(() => channel.lastRead?.(), [channel]);
  const enableDateSeparator = !disableDateSeparator;
  let messagesWithDates = !enableDateSeparator && !hideDeletedMessages && hideNewMessageSeparator ? messages : processMessages({
    enableDateSeparator,
    hideDeletedMessages,
    hideNewMessageSeparator,
    lastRead,
    messages,
    reviewProcessedMessage,
    userId: client.userID || ""
  });
  if (HeaderComponent) {
    messagesWithDates = insertIntro(messagesWithDates, headerPosition);
  }
  const groupStylesFn = groupStyles || getGroupStyles;
  const messageGroupStyles = (0, import_react29.useMemo)(
    () => messagesWithDates.reduce((acc, message, i2) => {
      const style = groupStylesFn(
        message,
        messagesWithDates[i2 - 1],
        messagesWithDates[i2 + 1],
        noGroupByUser,
        maxTimeBetweenGroupedMessages
      );
      if (style) acc[message.id] = style;
      return acc;
    }, {}),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [maxTimeBetweenGroupedMessages, messagesWithDates, noGroupByUser]
  );
  return { messageGroupStyles, messages: messagesWithDates };
};
var DATE_SEPARATOR_SELECTOR = ".str-chat__date-separator:not(.str-chat__date-separator--floating)";
var THROTTLE_MS = 100;
var useFloatingDateSeparatorMessageList = ({
  disableDateSeparator,
  listElement,
  processedMessages
}) => {
  const [state, setState] = (0, import_react29.useState)({
    date: null,
    visible: false
  });
  const update = (0, import_react29.useCallback)(() => {
    if (disableDateSeparator || !listElement || processedMessages.length === 0) {
      setState({ date: null, visible: false });
      return;
    }
    const separators = listElement.querySelectorAll(DATE_SEPARATOR_SELECTOR);
    if (separators.length === 0) {
      setState({ date: null, visible: false });
      return;
    }
    const containerRect = listElement.getBoundingClientRect();
    let bestDate = null;
    let bestTop = -Infinity;
    for (const el of separators) {
      const rect = el.getBoundingClientRect();
      const dataDate = el.getAttribute("data-date");
      if (!dataDate) continue;
      const isAtOrAboveTopBoundary = rect.top <= containerRect.top;
      if (isAtOrAboveTopBoundary && rect.top > bestTop) {
        bestTop = rect.top;
        const d2 = new Date(dataDate);
        if (!isNaN(d2.getTime())) bestDate = d2;
      }
    }
    setState({
      date: bestDate,
      visible: bestDate !== null
    });
  }, [disableDateSeparator, listElement, processedMessages]);
  (0, import_react29.useEffect)(() => {
    if (!listElement) return;
    const throttled = (0, import_lodash3.default)(update, THROTTLE_MS);
    throttled();
    listElement.addEventListener("scroll", throttled);
    if (typeof ResizeObserver === "undefined") {
      return () => {
        listElement.removeEventListener("scroll", throttled);
        throttled.cancel();
      };
    }
    const resizeObserver = new ResizeObserver(throttled);
    resizeObserver.observe(listElement);
    return () => {
      listElement.removeEventListener("scroll", throttled);
      resizeObserver.disconnect();
      throttled.cancel();
    };
  }, [listElement, update]);
  return {
    floatingDate: state.date,
    showFloatingDate: state.visible
  };
};
var useLastReadData = (props) => {
  const { channel, lastOwnMessage, messages, returnAllReadData } = props;
  return (0, import_react29.useMemo)(() => {
    if (returnAllReadData) {
      return messages.reduce(
        (acc, msg) => {
          acc[msg.id] = channel.messageReceiptsTracker.readersForMessage({
            msgId: msg.id,
            timestampMs: msg.created_at.getTime()
          });
          return acc;
        },
        {}
      );
    }
    if (!lastOwnMessage) return {};
    return {
      [lastOwnMessage.id]: channel.messageReceiptsTracker.readersForMessage({
        msgId: lastOwnMessage.id,
        timestampMs: lastOwnMessage.created_at.getTime()
      })
    };
  }, [channel, lastOwnMessage, messages, returnAllReadData]);
};
var useLastDeliveredData = (props) => {
  const { channel, lastOwnMessage, messages, returnAllReadData } = props;
  const calculateForAll = (0, import_react29.useCallback)(
    () => messages.reduce(
      (acc, msg) => {
        acc[msg.id] = channel.messageReceiptsTracker.deliveredForMessage({
          msgId: msg.id,
          timestampMs: msg.created_at.getTime()
        });
        return acc;
      },
      {}
    ),
    [channel, messages]
  );
  const calculateForLastOwn = (0, import_react29.useCallback)(() => {
    if (!lastOwnMessage) return {};
    return {
      [lastOwnMessage.id]: channel.messageReceiptsTracker.deliveredForMessage({
        msgId: lastOwnMessage.id,
        timestampMs: lastOwnMessage.created_at.getTime()
      })
    };
  }, [channel, lastOwnMessage]);
  const [deliveredTo, setDeliveredTo] = (0, import_react29.useState)(
    returnAllReadData ? calculateForAll : calculateForLastOwn
  );
  (0, import_react29.useEffect)(() => {
    if (!returnAllReadData) return;
    setDeliveredTo(calculateForAll);
    return channel.on("message.delivered", () => setDeliveredTo(calculateForAll)).unsubscribe;
  }, [channel, calculateForAll, returnAllReadData]);
  (0, import_react29.useEffect)(() => {
    if (returnAllReadData) return;
    else setDeliveredTo(calculateForLastOwn);
    return channel.on("message.delivered", () => setDeliveredTo(calculateForLastOwn)).unsubscribe;
  }, [channel, calculateForLastOwn, returnAllReadData]);
  return deliveredTo;
};
var useMessageListElements = (props) => {
  const {
    channelUnreadUiState,
    enrichedMessages,
    internalMessageProps,
    lastOwnMessage,
    messageGroupStyles,
    messages,
    renderMessages,
    returnAllReadData,
    threadList
  } = props;
  const { customClasses } = useChatContext("useMessageListElements");
  const { channel } = useChannelStateContext();
  const components = useComponentContext();
  const readData = useLastReadData({
    channel,
    lastOwnMessage,
    messages,
    returnAllReadData
  });
  const ownMessagesDeliveredToOthers = useLastDeliveredData({
    channel,
    lastOwnMessage,
    messages,
    returnAllReadData
  });
  const lastReceivedMessageId = (0, import_react29.useMemo)(
    () => getLastReceived(enrichedMessages),
    [enrichedMessages]
  );
  const elements = (0, import_react29.useMemo)(
    () => renderMessages({
      channelUnreadUiState,
      components,
      customClasses,
      lastOwnMessage,
      lastReceivedMessageId,
      messageGroupStyles,
      messages: enrichedMessages,
      ownMessagesDeliveredToOthers,
      readData,
      sharedMessageProps: { ...internalMessageProps, returnAllReadData, threadList }
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      enrichedMessages,
      internalMessageProps,
      lastOwnMessage,
      lastReceivedMessageId,
      messageGroupStyles,
      channelUnreadUiState,
      readData,
      renderMessages,
      returnAllReadData,
      threadList
    ]
  );
  return elements;
};
var messageIdsMatchAsPrefix = (prevMessages, newMessages) => prevMessages.every((message, index6) => message.id === newMessages[index6]?.id);
var messageIdsMatchAsSuffix = (prevMessages, newMessages) => prevMessages.every(
  (message, index6) => message.id === newMessages[newMessages.length - prevMessages.length + index6]?.id
);
function useMessageListScrollManager(params) {
  const {
    captureAnchor,
    disableScrollManagement = false,
    justReachedLatestMessageSet = false,
    loadingMore = false,
    loadMoreScrollThreshold,
    onScrollBy,
    onScrollToTop,
    restoreAnchor,
    scrollContainerMeasures,
    scrolledUpThreshold,
    scrollToBottom,
    showNewMessages
  } = params;
  const { client } = useChatContext("useMessageListScrollManager");
  const measures = (0, import_react29.useRef)({
    offsetHeight: 0,
    scrollHeight: 0
  });
  const messages = (0, import_react29.useRef)(void 0);
  const olderPaginationState = (0, import_react29.useRef)({
    anchor: null,
    mode: "idle"
  });
  const previousLoadingMoreRef = (0, import_react29.useRef)(loadingMore);
  const scrollTop = (0, import_react29.useRef)(0);
  (0, import_react29.useLayoutEffect)(() => {
    if (disableScrollManagement) {
      messages.current = params.messages;
      measures.current = scrollContainerMeasures();
      previousLoadingMoreRef.current = loadingMore;
      return;
    }
    const prevMeasures = measures.current;
    const prevMessages = messages.current;
    const newMessages = params.messages;
    const lastNewMessage = newMessages[newMessages.length - 1] || {};
    const lastPrevMessage = prevMessages?.[prevMessages.length - 1];
    const newMeasures = scrollContainerMeasures();
    const startedLoadingOlder = loadingMore && !previousLoadingMoreRef.current;
    const finishedLoadingOlder = !loadingMore && previousLoadingMoreRef.current;
    if (startedLoadingOlder) {
      if (scrollTop.current <= 1) {
        olderPaginationState.current = {
          anchor: null,
          mode: "stick-to-top"
        };
      } else if (scrollTop.current < loadMoreScrollThreshold) {
        const capturedAnchor = captureAnchor();
        if (capturedAnchor) {
          olderPaginationState.current = {
            anchor: capturedAnchor,
            mode: "preserve-anchor"
          };
        } else {
          olderPaginationState.current = {
            anchor: null,
            mode: "idle"
          };
        }
      } else {
        olderPaginationState.current = {
          anchor: null,
          mode: "idle"
        };
      }
    }
    const wasAtBottom = prevMeasures.scrollHeight - prevMeasures.offsetHeight - scrollTop.current < scrolledUpThreshold;
    if (typeof prevMessages !== "undefined") {
      if (prevMessages.length < newMessages.length) {
        const messagesAddedToTop = messageIdsMatchAsSuffix(prevMessages, newMessages);
        const messagesAddedToBottom = messageIdsMatchAsPrefix(prevMessages, newMessages);
        if (messagesAddedToTop) {
          const preservedAnchor = olderPaginationState.current.mode === "preserve-anchor" && olderPaginationState.current.anchor && (finishedLoadingOlder || loadingMore) ? olderPaginationState.current.anchor : null;
          if (olderPaginationState.current.mode === "stick-to-top") {
            onScrollToTop();
          } else if (preservedAnchor) {
            restoreAnchor(preservedAnchor);
          } else if (scrollTop.current < loadMoreScrollThreshold) {
            const listHeightDelta = newMeasures.scrollHeight - prevMeasures.scrollHeight;
            onScrollBy(listHeightDelta);
          }
          olderPaginationState.current = {
            anchor: null,
            mode: "idle"
          };
        } else if (messagesAddedToBottom) {
          if (justReachedLatestMessageSet) {
            messages.current = newMessages;
            measures.current = newMeasures;
            previousLoadingMoreRef.current = loadingMore;
            return;
          }
          const lastMessageIsFromCurrentUser = lastNewMessage.user?.id === client.userID;
          if (lastMessageIsFromCurrentUser || wasAtBottom) {
            scrollToBottom();
          } else {
            showNewMessages();
          }
        } else {
          olderPaginationState.current = {
            anchor: null,
            mode: "idle"
          };
        }
      } else {
        const hasNewReactions = lastPrevMessage?.latest_reactions?.length !== lastNewMessage.latest_reactions?.length;
        const hasNewReplies = lastPrevMessage?.reply_count !== lastNewMessage.reply_count;
        if ((hasNewReactions || hasNewReplies) && wasAtBottom) {
          scrollToBottom();
        }
        if (finishedLoadingOlder) {
          olderPaginationState.current = {
            anchor: null,
            mode: "idle"
          };
        }
      }
    }
    messages.current = newMessages;
    measures.current = newMeasures;
    previousLoadingMoreRef.current = loadingMore;
  }, [
    captureAnchor,
    disableScrollManagement,
    justReachedLatestMessageSet,
    loadingMore,
    measures,
    messages,
    params.messages,
    restoreAnchor,
    scrollContainerMeasures
  ]);
  return (scrollTopValue, getLatestAnchor = null) => {
    scrollTop.current = scrollTopValue;
    if (loadingMore && getLatestAnchor && olderPaginationState.current.mode === "preserve-anchor") {
      const latestAnchor = getLatestAnchor();
      if (!latestAnchor) return;
      olderPaginationState.current = {
        anchor: latestAnchor,
        mode: "preserve-anchor"
      };
    }
  };
}
var useScrollLocationLogic = (params) => {
  const {
    disableAutoScrollToBottom = false,
    disableScrollManagement = false,
    hasMoreNewer,
    listElement,
    loadingMore = false,
    loadMoreScrollThreshold,
    messages = [],
    scrolledUpThreshold = 200,
    suppressAutoscroll
  } = params;
  const [hasNewMessages, setHasNewMessages] = (0, import_react29.useState)(false);
  const [wrapperRect, setWrapperRect] = (0, import_react29.useState)();
  const previousHasMoreNewerRef = (0, import_react29.useRef)(hasMoreNewer);
  const justReachedLatestMessageSet = previousHasMoreNewerRef.current && !hasMoreNewer;
  const skipNextAutoScrollRef = (0, import_react29.useRef)(false);
  const isRestoringOlderAnchorRef = (0, import_react29.useRef)(false);
  const [isMessageListScrolledToBottom, setIsMessageListScrolledToBottom] = (0, import_react29.useState)(true);
  const closeToBottom = (0, import_react29.useRef)(false);
  const closeToTop = (0, import_react29.useRef)(false);
  const previousScrollTopRef = (0, import_react29.useRef)(0);
  const previousMessagesLengthRef = (0, import_react29.useRef)(messages.length);
  const previousDisableAutoScrollToBottomRef = (0, import_react29.useRef)(disableAutoScrollToBottom);
  const previousDisableAutoScrollSettleRef = (0, import_react29.useRef)(disableAutoScrollToBottom);
  const anchorRestoreCleanupRef = (0, import_react29.useRef)(null);
  const captureAnchor = (0, import_react29.useCallback)(() => {
    if (!listElement) return null;
    const listRect = listElement.getBoundingClientRect();
    const listTop = listRect.top;
    const listBottom = listRect.bottom;
    const listCenter = listTop + listRect.height / 2;
    const preferTopEdgeAnchor = loadingMore || listElement.scrollTop < loadMoreScrollThreshold;
    const messageElements = Array.from(
      listElement.querySelectorAll("[data-message-id]")
    );
    const messageAnchors = messageElements.map((element4) => {
      const rect = element4.getBoundingClientRect();
      return {
        center: rect.top + rect.height / 2,
        element: element4,
        offsetTop: rect.top - listTop,
        rect
      };
    });
    const visibleMessageAnchors = messageAnchors.filter(
      ({ rect }) => rect.bottom > listTop && rect.top < listBottom
    );
    const topEdgeAnchor = visibleMessageAnchors.reduce((closest, candidate) => {
      if (!closest || candidate.rect.top < closest.rect.top) {
        return candidate;
      }
      return closest;
    }, null);
    const centerAnchor = visibleMessageAnchors.find(
      ({ rect }) => rect.top <= listCenter && rect.bottom >= listCenter
    ) ?? visibleMessageAnchors.reduce(
      (closest, candidate) => {
        if (!closest) return candidate;
        const distance = Math.abs(candidate.center - listCenter);
        const closestDistance = Math.abs(closest.center - listCenter);
        return distance < closestDistance ? candidate : closest;
      },
      null
    );
    const anchor = (preferTopEdgeAnchor ? topEdgeAnchor : centerAnchor) ?? messageAnchors[0];
    if (!anchor?.element.dataset.messageId) return null;
    return {
      id: anchor.element.dataset.messageId,
      offsetTop: anchor.offsetTop
    };
  }, [listElement, loadMoreScrollThreshold, loadingMore]);
  const restoreAnchor = (0, import_react29.useCallback)(
    (anchor) => {
      if (!listElement) return;
      anchorRestoreCleanupRef.current?.();
      let cancelled = false;
      let stableFrameCount = 0;
      let frameQueued = false;
      let resizeObserver;
      let settleTimeoutId;
      let animationFrameId;
      isRestoringOlderAnchorRef.current = true;
      const applyAnchor = () => {
        if (cancelled) return true;
        const anchorElement = listElement.querySelector(
          `[data-message-id='${anchor.id}']`
        );
        if (!anchorElement) return true;
        const listTop = listElement.getBoundingClientRect().top;
        const nextOffsetTop = anchorElement.getBoundingClientRect().top - listTop;
        const offsetDelta = nextOffsetTop - anchor.offsetTop;
        if (Math.abs(offsetDelta) > 1) {
          listElement.scrollBy({ top: offsetDelta });
          return false;
        }
        return true;
      };
      const cleanup2 = () => {
        cancelled = true;
        frameQueued = false;
        isRestoringOlderAnchorRef.current = false;
        if (typeof animationFrameId !== "undefined") {
          window.cancelAnimationFrame(animationFrameId);
        }
        if (settleTimeoutId) {
          clearTimeout(settleTimeoutId);
        }
        resizeObserver?.disconnect();
      };
      const queueNextFrame = () => {
        if (cancelled || frameQueued) return;
        frameQueued = true;
        animationFrameId = window.requestAnimationFrame(() => {
          frameQueued = false;
          const isStable = applyAnchor();
          stableFrameCount = isStable ? stableFrameCount + 1 : 0;
          if (stableFrameCount >= 2) {
            cleanup2();
            return;
          }
          queueNextFrame();
        });
      };
      stableFrameCount = applyAnchor() ? 1 : 0;
      queueNextFrame();
      if (typeof ResizeObserver !== "undefined") {
        resizeObserver = new ResizeObserver(() => {
          stableFrameCount = 0;
          queueNextFrame();
        });
        resizeObserver.observe(listElement);
      }
      settleTimeoutId = setTimeout(() => {
        cleanup2();
      }, 1200);
      anchorRestoreCleanupRef.current = cleanup2;
    },
    [listElement]
  );
  (0, import_react29.useLayoutEffect)(
    () => () => {
      anchorRestoreCleanupRef.current?.();
    },
    []
  );
  const scrollToBottom = (0, import_react29.useCallback)(
    (options) => {
      if (!listElement?.scrollTo || hasMoreNewer || isRestoringOlderAnchorRef.current || justReachedLatestMessageSet || suppressAutoscroll) {
        return;
      }
      listElement.scrollTo({
        behavior: options?.behavior,
        top: listElement.scrollHeight
      });
      setHasNewMessages(false);
    },
    [hasMoreNewer, justReachedLatestMessageSet, listElement, suppressAutoscroll]
  );
  (0, import_react29.useLayoutEffect)(() => {
    const disableAutoScrollJustReleased = previousDisableAutoScrollToBottomRef.current && !disableAutoScrollToBottom;
    previousDisableAutoScrollToBottomRef.current = disableAutoScrollToBottom;
    if (disableAutoScrollJustReleased) {
      return;
    }
    if (listElement) {
      setWrapperRect(listElement.getBoundingClientRect());
    }
    if (listElement && justReachedLatestMessageSet) {
      listElement.scrollTop = previousScrollTopRef.current;
      skipNextAutoScrollRef.current = true;
      return;
    }
    if (skipNextAutoScrollRef.current) {
      skipNextAutoScrollRef.current = false;
      return;
    }
    if (listElement && !disableAutoScrollToBottom && !isRestoringOlderAnchorRef.current) {
      scrollToBottom();
    }
  }, [disableAutoScrollToBottom, justReachedLatestMessageSet, listElement, hasMoreNewer]);
  (0, import_react29.useLayoutEffect)(() => {
    const disableAutoScrollJustReleased = previousDisableAutoScrollSettleRef.current && !disableAutoScrollToBottom;
    previousDisableAutoScrollSettleRef.current = disableAutoScrollToBottom;
    if (disableAutoScrollJustReleased) {
      return;
    }
    if (!listElement || disableAutoScrollToBottom || hasMoreNewer || suppressAutoscroll || justReachedLatestMessageSet || isRestoringOlderAnchorRef.current) {
      return;
    }
    const initialDistanceToBottom = listElement.scrollHeight - (listElement.scrollTop + listElement.clientHeight);
    const messagesHydrated = previousMessagesLengthRef.current === 0 && messages.length > 0;
    if (initialDistanceToBottom > scrolledUpThreshold && !messagesHydrated) {
      return;
    }
    let keepPinnedToBottom = true;
    const maybeScrollToBottom = () => {
      if (keepPinnedToBottom) {
        scrollToBottom();
      }
    };
    maybeScrollToBottom();
    const settleDelays = [80, messagesHydrated ? 260 : 420, 900, 1700];
    const settleTimeoutIds = settleDelays.map(
      (delay) => setTimeout(maybeScrollToBottom, delay)
    );
    const stopKeepingPinnedToBottom = () => {
      keepPinnedToBottom = false;
    };
    listElement.addEventListener("pointerdown", stopKeepingPinnedToBottom, {
      passive: true
    });
    listElement.addEventListener("touchstart", stopKeepingPinnedToBottom, {
      passive: true
    });
    listElement.addEventListener("wheel", stopKeepingPinnedToBottom, {
      passive: true
    });
    listElement.addEventListener("keydown", stopKeepingPinnedToBottom);
    const pinWindowTimeoutId = setTimeout(() => {
      stopKeepingPinnedToBottom();
    }, 2200);
    return () => {
      settleTimeoutIds.forEach(clearTimeout);
      clearTimeout(pinWindowTimeoutId);
      listElement.removeEventListener("pointerdown", stopKeepingPinnedToBottom);
      listElement.removeEventListener("touchstart", stopKeepingPinnedToBottom);
      listElement.removeEventListener("wheel", stopKeepingPinnedToBottom);
      listElement.removeEventListener("keydown", stopKeepingPinnedToBottom);
    };
  }, [
    disableAutoScrollToBottom,
    hasMoreNewer,
    justReachedLatestMessageSet,
    listElement,
    messages.length,
    scrollToBottom,
    scrolledUpThreshold,
    suppressAutoscroll
  ]);
  const updateScrollTop = useMessageListScrollManager({
    captureAnchor,
    disableScrollManagement: disableScrollManagement || isRestoringOlderAnchorRef.current,
    justReachedLatestMessageSet,
    loadingMore,
    loadMoreScrollThreshold,
    messages,
    onScrollBy: (scrollBy) => {
      listElement?.scrollBy({ top: scrollBy });
    },
    onScrollToTop: () => {
      if (!listElement) return;
      listElement.scrollTop = 0;
    },
    restoreAnchor,
    scrollContainerMeasures: () => ({
      offsetHeight: listElement?.offsetHeight || 0,
      scrollHeight: listElement?.scrollHeight || 0
    }),
    scrolledUpThreshold,
    scrollToBottom,
    showNewMessages: () => setHasNewMessages(true)
  });
  (0, import_react29.useLayoutEffect)(() => {
    previousHasMoreNewerRef.current = hasMoreNewer;
  }, [hasMoreNewer]);
  (0, import_react29.useLayoutEffect)(() => {
    previousMessagesLengthRef.current = messages.length;
  }, [messages.length]);
  const onScroll = (0, import_react29.useCallback)(
    (event) => {
      const element4 = event.target;
      const scrollTop = element4.scrollTop;
      previousScrollTopRef.current = scrollTop;
      updateScrollTop(scrollTop, captureAnchor);
      const offsetHeight = element4.offsetHeight;
      const scrollHeight = element4.scrollHeight;
      const distanceToBottom = scrollHeight - (scrollTop + offsetHeight);
      const bottomEnterThreshold = Math.max(Math.floor(scrolledUpThreshold * 0.6), 24);
      const prevCloseToBottom = closeToBottom.current;
      closeToBottom.current = prevCloseToBottom ? distanceToBottom < scrolledUpThreshold : distanceToBottom < bottomEnterThreshold;
      closeToTop.current = scrollTop < scrolledUpThreshold;
      if (closeToBottom.current) {
        setHasNewMessages(false);
      }
      if (prevCloseToBottom && !closeToBottom.current) {
        setIsMessageListScrolledToBottom(false);
      } else if (!prevCloseToBottom && closeToBottom.current) {
        setIsMessageListScrolledToBottom(true);
      }
    },
    [captureAnchor, updateScrollTop, closeToTop, closeToBottom, scrolledUpThreshold]
  );
  return {
    hasNewMessages,
    isMessageListScrolledToBottom,
    onScroll,
    scrollToBottom,
    wrapperRect
  };
};
var MESSAGE_LIST_MAIN_PANEL_CLASS = "str-chat__main-panel-inner str-chat__message-list-main-panel";
var MessageListMainPanel = ({ children }) => (0, import_jsx_runtime4.jsx)("div", { className: MESSAGE_LIST_MAIN_PANEL_CLASS, children });
var UNREAD_MESSAGE_SEPARATOR_CLASS = "str-chat__unread-messages-separator";
var UnreadMessagesSeparator = ({
  showCount = true,
  unreadCount
}) => {
  const { t: t2 } = useTranslationContext("UnreadMessagesSeparator");
  const { markRead } = useChannelActionContext();
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: UNREAD_MESSAGE_SEPARATOR_CLASS,
      "data-testid": "unread-messages-separator",
      children: [
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__unread-messages-separator__text", children: unreadCount && showCount ? t2("{{count}} unread", { count: unreadCount }) : t2("Unread messages") }),
        (0, import_jsx_runtime4.jsx)(
          Button,
          {
            appearance: "ghost",
            circular: true,
            onClick: () => markRead(),
            size: "sm",
            variant: "secondary",
            children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
          }
        )
      ]
    }
  );
};
var targetScrolledAboveVisibleContainerArea = (element4, container) => {
  const { bottom: targetBottom } = element4.getBoundingClientRect();
  const containerTop = container?.getBoundingClientRect().top ?? 0;
  return targetBottom < containerTop;
};
var targetScrolledBelowVisibleContainerArea = (element4, container) => {
  const { top: targetTop } = element4.getBoundingClientRect();
  const { bottom: containerBottom } = container.getBoundingClientRect();
  return targetTop > containerBottom;
};
var useUnreadMessagesNotification = ({
  isMessageListScrolledToBottom,
  listElement,
  showAlways,
  unreadCount
}) => {
  const { messages } = useChannelStateContext("UnreadMessagesNotification");
  const [show, setShow] = (0, import_react29.useState)(false);
  const isScrolledAboveTargetTop = (0, import_react29.useRef)(false);
  const intersectionObserverIsSupported = typeof IntersectionObserver !== "undefined";
  (0, import_react29.useEffect)(() => {
    if (!(unreadCount && intersectionObserverIsSupported)) {
      setShow(false);
      return;
    }
    const scrollRoot = listElement ?? null;
    if (!scrollRoot) {
      const [msgListPanel] = document.getElementsByClassName(
        MESSAGE_LIST_MAIN_PANEL_CLASS
      );
      if (!msgListPanel) return;
      const [observedTarget2] = document.getElementsByClassName(
        UNREAD_MESSAGE_SEPARATOR_CLASS
      );
      if (!observedTarget2) {
        setShow(true);
      }
      return;
    }
    const [observedTarget] = document.getElementsByClassName(
      UNREAD_MESSAGE_SEPARATOR_CLASS
    );
    if (!observedTarget) {
      setShow(true);
      return;
    }
    const scrolledBelowSeparator = targetScrolledAboveVisibleContainerArea(
      observedTarget,
      scrollRoot
    );
    const scrolledAboveSeparator = targetScrolledBelowVisibleContainerArea(
      observedTarget,
      scrollRoot
    );
    setShow(
      showAlways ? scrolledBelowSeparator || scrolledAboveSeparator : scrolledBelowSeparator
    );
    const observer = new IntersectionObserver(
      (elements) => {
        if (!elements.length) return;
        const entry = elements[0];
        const { boundingClientRect, isIntersecting, rootBounds } = entry;
        if (isIntersecting) {
          setShow(false);
          return;
        }
        const rootTop = rootBounds?.top ?? 0;
        const separatorIsAboveContainerTop = boundingClientRect.bottom < rootTop;
        setShow(showAlways || separatorIsAboveContainerTop);
        isScrolledAboveTargetTop.current = separatorIsAboveContainerTop;
      },
      { root: scrollRoot }
    );
    observer.observe(observedTarget);
    return () => {
      observer.disconnect();
    };
  }, [
    intersectionObserverIsSupported,
    listElement,
    isMessageListScrolledToBottom,
    messages,
    showAlways,
    unreadCount
  ]);
  (0, import_react29.useEffect)(() => {
    if (unreadCount && isMessageListScrolledToBottom && isScrolledAboveTargetTop.current) {
      setShow(true);
      isScrolledAboveTargetTop.current = false;
    }
  }, [isMessageListScrolledToBottom, unreadCount]);
  return { show: show && intersectionObserverIsSupported };
};
var hasReadLastMessage = (channel, userId) => {
  const latestMessageIdInChannel = channel.state.latestMessages.slice(-1)[0]?.id;
  const lastReadMessageIdServer = channel.state.read[userId]?.last_read_message_id;
  return latestMessageIdInChannel === lastReadMessageIdServer;
};
var useMarkRead = ({
  isMessageListScrolledToBottom,
  messageListIsThread,
  wasMarkedUnread
}) => {
  const { client } = useChatContext("useMarkRead");
  const { markRead, setChannelUnreadUiState } = useChannelActionContext("useMarkRead");
  const { channel } = useChannelStateContext("useMarkRead");
  (0, import_react29.useEffect)(() => {
    if (!channel.getConfig()?.read_events) return;
    const shouldMarkRead = () => !document.hidden && !wasMarkedUnread && !messageListIsThread && isMessageListScrolledToBottom && client.user?.id && !hasReadLastMessage(channel, client.user.id);
    const onVisibilityChange = () => {
      if (shouldMarkRead()) markRead();
    };
    const handleMessageNew = (event) => {
      const mainChannelUpdated = !event.message?.parent_id || event.message?.show_in_channel;
      if (!isMessageListScrolledToBottom || wasMarkedUnread || document.hidden) {
        setChannelUnreadUiState((prev) => {
          const previousUnreadCount = prev?.unread_messages ?? 0;
          const previousLastMessage = getPreviousLastMessage(
            channel.state.messages,
            event.message
          );
          return {
            ...prev || {},
            last_read: prev?.last_read ?? (previousUnreadCount === 0 && previousLastMessage?.created_at ? new Date(previousLastMessage.created_at) : /* @__PURE__ */ new Date(0)),
            // not having information about the last read message means the whole channel is unread,
            unread_messages: previousUnreadCount + 1
          };
        });
      } else if (mainChannelUpdated && shouldMarkRead()) {
        markRead();
      }
    };
    channel.on("message.new", handleMessageNew);
    document.addEventListener("visibilitychange", onVisibilityChange);
    if (shouldMarkRead()) {
      markRead();
    }
    return () => {
      channel.off("message.new", handleMessageNew);
      document.removeEventListener("visibilitychange", onVisibilityChange);
    };
  }, [
    channel,
    client,
    isMessageListScrolledToBottom,
    markRead,
    messageListIsThread,
    setChannelUnreadUiState,
    wasMarkedUnread
  ]);
};
function getPreviousLastMessage(messages, newMessage) {
  if (!newMessage) return;
  let previousLastMessage;
  for (let i2 = messages.length - 1; i2 >= 0; i2--) {
    const msg = messages[i2];
    if (!msg?.id) break;
    if (msg.id !== newMessage.id) {
      previousLastMessage = msg;
      break;
    }
  }
  return previousLastMessage;
}
var UnMemoizedNewMessageNotification = (props) => {
  const { newMessageCount = 0, showNotification } = props;
  const { t: t2 } = useTranslationContext();
  const { NewMessageNotification: CustomNewMessageNotification } = useComponentContext();
  if (!showNotification) return null;
  const label = newMessageCount > 0 ? t2("{{count}} new messages", { count: newMessageCount }) : t2("New Messages!");
  if (CustomNewMessageNotification) {
    return (0, import_jsx_runtime4.jsx)(CustomNewMessageNotification, { ...props, label });
  }
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__new-message-notification", children: (0, import_jsx_runtime4.jsx)(
    "div",
    {
      "aria-live": "polite",
      className: "str-chat__message-notification__label",
      "data-testid": "message-notification",
      children: label
    }
  ) });
};
var NewMessageNotification = import_react29.default.memo(
  UnMemoizedNewMessageNotification
);
var UnreadMessagesNotification = ({
  queryMessageLimit,
  showCount = true,
  unreadCount
}) => {
  const { jumpToFirstUnreadMessage, markRead } = useChannelActionContext();
  const { t: t2 } = useTranslationContext("UnreadMessagesNotification");
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__unread-messages-notification", {
        "str-chat__unread-messages-notification--with-count": unreadCount && showCount
      }),
      "data-testid": "unread-messages-notification",
      children: [
        (0, import_jsx_runtime4.jsxs)(
          Button,
          {
            appearance: "outline",
            onClick: () => jumpToFirstUnreadMessage(queryMessageLimit),
            variant: "secondary",
            children: [
              (0, import_jsx_runtime4.jsx)(IconArrowUp, {}),
              unreadCount && showCount ? t2("{{count}} unread", { count: unreadCount }) : t2("Unread messages")
            ]
          }
        ),
        (0, import_jsx_runtime4.jsx)(Button, { appearance: "outline", onClick: () => markRead(), variant: "secondary", children: (0, import_jsx_runtime4.jsx)(IconXmark, {}) })
      ]
    }
  );
};
var mousewheelListener = (event) => {
  if (event instanceof WheelEvent && event.deltaY === 1) {
    event.preventDefault();
  }
};
var InfiniteScroll = (props) => {
  const {
    children,
    element: Component2 = "div",
    hasNextPage,
    hasPreviousPage,
    head,
    initialLoad = true,
    isLoading,
    listenToScroll,
    loader,
    loadNextPage,
    loadPreviousPage,
    threshold = DEFAULT_LOAD_PAGE_SCROLL_THRESHOLD,
    useCapture = false,
    ...elementProps
  } = props;
  const [scrollComponent, setScrollComponent] = (0, import_react29.useState)(null);
  const previousOffset = (0, import_react29.useRef)(void 0);
  const previousReverseOffset = (0, import_react29.useRef)(void 0);
  const scrollListenerRef = (0, import_react29.useRef)(void 0);
  scrollListenerRef.current = () => {
    const element4 = scrollComponent;
    if (!element4 || element4.offsetParent === null) {
      return;
    }
    const parentElement = element4.parentElement;
    const offset4 = element4.scrollHeight - parentElement.scrollTop - parentElement.clientHeight;
    const reverseOffset = parentElement.scrollTop;
    if (listenToScroll) {
      listenToScroll(offset4, reverseOffset, threshold);
    }
    if (isLoading) return;
    if (previousOffset.current === offset4 && previousReverseOffset.current === reverseOffset)
      return;
    previousOffset.current = offset4;
    previousReverseOffset.current = reverseOffset;
    if (reverseOffset < Number(threshold) && typeof loadPreviousPage === "function" && hasPreviousPage) {
      loadPreviousPage();
    }
    if (offset4 < Number(threshold) && typeof loadNextPage === "function" && hasNextPage) {
      loadNextPage();
    }
  };
  (0, import_react29.useEffect)(() => {
    const scrollElement = scrollComponent?.parentNode;
    if (!scrollElement) return;
    const scrollListener = () => scrollListenerRef.current?.();
    scrollElement.addEventListener("scroll", scrollListener, useCapture);
    scrollElement.addEventListener("resize", scrollListener, useCapture);
    scrollListener();
    return () => {
      scrollElement.removeEventListener("scroll", scrollListener, useCapture);
      scrollElement.removeEventListener("resize", scrollListener, useCapture);
    };
  }, [initialLoad, scrollComponent, useCapture]);
  (0, import_react29.useEffect)(() => {
    const scrollElement = scrollComponent?.parentNode;
    if (!scrollElement) return;
    scrollElement.addEventListener("wheel", mousewheelListener, { passive: false });
    return () => {
      scrollElement.removeEventListener("wheel", mousewheelListener, useCapture);
    };
  }, [scrollComponent, useCapture]);
  return (0, import_jsx_runtime4.jsxs)(Component2, { ...elementProps, ref: setScrollComponent, children: [
    head,
    loader,
    children
  ] });
};
var UnMemoizedTypingIndicator = (props) => {
  const { isMessageListScrolledToBottom = true, scrollToBottom, threadList } = props;
  const { channelConfig, thread } = useChannelStateContext("TypingIndicator");
  const threadInstance = useThreadContext();
  const parentId = threadInstance?.id ?? thread?.id;
  const { client } = useChatContext("TypingIndicator");
  const { typing = {} } = useTypingContext("TypingIndicator");
  const typingInChannel = !threadList ? Object.values(typing).filter(
    ({ parent_id, user }) => user?.id !== client.user?.id && !parent_id
  ) : [];
  const typingInThread = threadList ? Object.values(typing).filter(
    ({ parent_id, user }) => user?.id !== client.user?.id && parent_id === parentId
  ) : [];
  const typingUsers = threadList ? typingInThread : typingInChannel;
  const { displayUsers } = useDebouncedTypingActive(typingUsers);
  const showIndicator = displayUsers.length > 0;
  const displayInfo = (0, import_react29.useMemo)(
    () => displayUsers.map(
      ({ user }) => ({
        id: user?.id,
        imageUrl: user?.image,
        userName: user?.name
      })
    ),
    [displayUsers]
  );
  (0, import_react29.useEffect)(() => {
    if (showIndicator && isMessageListScrolledToBottom) scrollToBottom();
  }, [scrollToBottom, isMessageListScrolledToBottom, showIndicator]);
  if (channelConfig?.typing_events === false) {
    return null;
  }
  if (!showIndicator || !isMessageListScrolledToBottom) {
    return null;
  }
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default(
        "str-chat__typing-indicator",
        "str-chat__typing-indicator--with-transition",
        {
          "str-chat__typing-indicator--typing": showIndicator
        }
      ),
      "data-testid": "typing-indicator",
      children: [
        displayInfo.length > 0 && (0, import_jsx_runtime4.jsx)(AvatarStack, { badgeSize: "md", displayInfo, size: "md" }),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__typing-indicator__bubble", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__typing-indicator__dots", children: (0, import_jsx_runtime4.jsx)(TypingIndicatorDots, {}) }) })
      ]
    }
  );
};
var TypingIndicator = import_react29.default.memo(
  UnMemoizedTypingIndicator
);
function getFloatingDateForFirstMessage(firstMessage, processedMessages, firstMessageIndex) {
  if (isIntroMessage(firstMessage)) return null;
  for (let i2 = firstMessageIndex - 1; i2 >= 0; i2 -= 1) {
    const item = processedMessages[i2];
    if (isDateSeparatorMessage(item)) {
      return item.date;
    }
  }
  const msg = firstMessage;
  const created = msg.created_at;
  if (created) {
    const d2 = new Date(created);
    return isNaN(d2.getTime()) ? null : d2;
  }
  return null;
}
function getFloatingDateForFirstItem(firstItem, processedMessages, firstItemIndex) {
  if (isDateSeparatorMessage(firstItem)) return firstItem.date;
  return getFloatingDateForFirstMessage(firstItem, processedMessages, firstItemIndex);
}
var HIDDEN_STATE = { date: null, visible: false };
var useFloatingDateSeparator = ({
  disableDateSeparator,
  processedMessages
}) => {
  const [state, setState] = (0, import_react29.useState)(HIDDEN_STATE);
  const onItemsRendered = (0, import_react29.useCallback)(
    (rendered) => {
      if (disableDateSeparator || processedMessages.length === 0) {
        setState(HIDDEN_STATE);
        return;
      }
      const valid2 = rendered.filter((m2) => m2 != null);
      if (valid2.length === 0) {
        setState(HIDDEN_STATE);
        return;
      }
      const first = valid2[0];
      const firstIndex = processedMessages.findIndex((m2) => m2.id === first.id);
      const date = firstIndex >= 0 ? getFloatingDateForFirstItem(first, processedMessages, firstIndex) : null;
      const visible = date !== null;
      setState((prev) => {
        const prevTime = prev.date?.getTime() ?? null;
        const nextTime = date?.getTime() ?? null;
        if (prev.visible === visible && prevTime === nextTime) return prev;
        return { date, visible };
      });
    },
    [disableDateSeparator, processedMessages]
  );
  return {
    floatingDate: state.date,
    onItemsRendered,
    showFloatingDate: !!state.date && state.visible
  };
};
var useGiphyPreview = (separateGiphyPreview) => {
  const [giphyPreviewMessage, setGiphyPreviewMessage] = (0, import_react29.useState)();
  const { client } = useChatContext("useGiphyPreview");
  (0, import_react29.useEffect)(() => {
    if (!separateGiphyPreview) return;
    const handleEvent = (event) => {
      const { message, user } = event;
      if (message?.command === "giphy" && user?.id === client.userID) {
        setGiphyPreviewMessage(void 0);
      }
    };
    client.on("message.new", handleEvent);
    return () => client.off("message.new", handleEvent);
  }, [client, separateGiphyPreview]);
  return {
    giphyPreviewMessage,
    setGiphyPreviewMessage: separateGiphyPreview ? setGiphyPreviewMessage : void 0
  };
};
var useMessageSetKey = ({ messages }) => {
  const [messageSetKey, setMessageSetKey] = (0, import_react29.useState)(+/* @__PURE__ */ new Date());
  const firstMessageId = (0, import_react29.useRef)(void 0);
  (0, import_react29.useEffect)(() => {
    const continuousSet = messages?.find(
      (message) => message.id === firstMessageId.current
    );
    if (!continuousSet) {
      setMessageSetKey(+/* @__PURE__ */ new Date());
    }
    firstMessageId.current = messages?.[0]?.id;
  }, [messages]);
  return {
    messageSetKey
  };
};
function useNewMessageNotification(messages, currentUserId, hasMoreNewer) {
  const [newMessagesNotification, setNewMessagesNotification] = (0, import_react29.useState)(false);
  const [isMessageListScrolledToBottom, setIsMessageListScrolledToBottom] = (0, import_react29.useState)(true);
  const didMount = (0, import_react29.useRef)(false);
  const lastMessageId = (0, import_react29.useRef)("");
  const atBottom = (0, import_react29.useRef)(false);
  (0, import_react29.useEffect)(() => {
    if (hasMoreNewer) {
      setNewMessagesNotification(true);
      return;
    }
    if (!messages?.length) return;
    const lastMessage = messages[messages.length - 1];
    const prevMessageId = lastMessageId.current;
    lastMessageId.current = lastMessage.id || "";
    if (lastMessage.id === prevMessageId) return;
    if (atBottom.current) return;
    if (lastMessage.user?.id !== currentUserId && didMount.current) {
      setNewMessagesNotification(true);
    }
    didMount.current = true;
  }, [currentUserId, messages, hasMoreNewer]);
  return {
    atBottom,
    isMessageListScrolledToBottom,
    newMessagesNotification,
    setIsMessageListScrolledToBottom,
    setNewMessagesNotification
  };
}
var STATUSES_EXCLUDED_FROM_PREPEND = {
  failed: true,
  sending: true
};
function usePrependedMessagesCount(messages, hasDateSeparator) {
  const firstRealMessageIndex = hasDateSeparator ? 1 : 0;
  const firstMessageOnFirstLoadedPage = (0, import_react29.useRef)(void 0);
  const previousFirstMessageOnFirstLoadedPage = (0, import_react29.useRef)(void 0);
  const previousNumItemsPrepended = (0, import_react29.useRef)(0);
  const numItemsPrepended = (0, import_react29.useMemo)(() => {
    if (!messages || !messages.length) {
      previousNumItemsPrepended.current = 0;
      return 0;
    }
    const currentFirstMessage = messages?.[firstRealMessageIndex];
    const noNewMessages = currentFirstMessage?.id === previousFirstMessageOnFirstLoadedPage.current?.id;
    const currentFirstMessageStatus = isLocalMessage(currentFirstMessage) ? currentFirstMessage.status : void 0;
    const firstMsgMovedAfterMessagesInExcludedStatus = !!(currentFirstMessageStatus && STATUSES_EXCLUDED_FROM_PREPEND[currentFirstMessageStatus]);
    if (noNewMessages || firstMsgMovedAfterMessagesInExcludedStatus) {
      return previousNumItemsPrepended.current;
    }
    if (!firstMessageOnFirstLoadedPage.current) {
      firstMessageOnFirstLoadedPage.current = currentFirstMessage;
    }
    previousFirstMessageOnFirstLoadedPage.current = currentFirstMessage;
    for (let prependedMessageCount = previousNumItemsPrepended.current; prependedMessageCount < messages.length; prependedMessageCount += 1) {
      const messageIsFirstOnFirstLoadedPage = messages[prependedMessageCount].id === firstMessageOnFirstLoadedPage.current?.id;
      if (messageIsFirstOnFirstLoadedPage) {
        previousNumItemsPrepended.current = prependedMessageCount - firstRealMessageIndex;
        return previousNumItemsPrepended.current;
      }
    }
    firstMessageOnFirstLoadedPage.current = currentFirstMessage;
    previousNumItemsPrepended.current = 0;
    return 0;
  }, [firstRealMessageIndex, messages, messages?.length]);
  return numItemsPrepended;
}
var useScrollToBottomOnNewMessage = ({
  messages,
  scrollToBottom,
  scrollToLatestMessageOnFocus
}) => {
  const [newMessagesReceivedInBackground, setNewMessagesReceivedInBackground] = (0, import_react29.useState)(false);
  const scrollToBottomIfConfigured = (0, import_react29.useRef)(void 0);
  scrollToBottomIfConfigured.current = (event) => {
    if (!scrollToLatestMessageOnFocus || !newMessagesReceivedInBackground || event.target !== window) {
      return;
    }
    setTimeout(scrollToBottom, 100);
  };
  (0, import_react29.useEffect)(() => {
    setNewMessagesReceivedInBackground(true);
  }, [messages]);
  (0, import_react29.useEffect)(() => {
    const handleFocus = (event) => {
      scrollToBottomIfConfigured.current?.(event);
    };
    const handleBlur = () => {
      setNewMessagesReceivedInBackground(false);
    };
    if (typeof window !== "undefined") {
      window.addEventListener("focus", handleFocus);
      window.addEventListener("blur", handleBlur);
    }
    return () => {
      window.removeEventListener("focus", handleFocus);
      window.removeEventListener("blur", handleBlur);
    };
  }, []);
};
function useShouldForceScrollToBottom(messages, currentUserId) {
  const lastFocusedOwnMessage = (0, import_react29.useRef)("");
  const initialFocusRegistered = (0, import_react29.useRef)(false);
  function recheckForNewOwnMessage() {
    if (messages && messages.length > 0) {
      const lastMessage = messages[messages.length - 1];
      if (lastMessage.user?.id === currentUserId && lastFocusedOwnMessage.current !== lastMessage.id) {
        lastFocusedOwnMessage.current = lastMessage.id;
        return true;
      }
    }
    return false;
  }
  (0, import_react29.useEffect)(() => {
    if (messages && messages.length && !initialFocusRegistered.current) {
      initialFocusRegistered.current = true;
      recheckForNewOwnMessage();
    }
  }, [messages, messages?.length]);
  return recheckForNewOwnMessage;
}
var useUnreadMessagesNotificationVirtualized = ({
  lastRead,
  showAlways,
  unreadCount
}) => {
  const [show, setShow] = (0, import_react29.useState)(false);
  const toggleShowUnreadMessagesNotification = (0, import_react29.useCallback)(
    (renderedMessages) => {
      if (!unreadCount) return;
      const firstRenderedMessage = renderedMessages[0];
      const lastRenderedMessage = renderedMessages.slice(-1)[0];
      if (!(firstRenderedMessage && lastRenderedMessage)) return;
      const firstRenderedMessageTime = new Date(
        firstRenderedMessage.created_at ?? 0
      ).getTime();
      const lastRenderedMessageTime = new Date(
        lastRenderedMessage.created_at ?? 0
      ).getTime();
      const lastReadTime = new Date(lastRead ?? 0).getTime();
      const scrolledBelowSeparator = !!lastReadTime && firstRenderedMessageTime > lastReadTime;
      const scrolledAboveSeparator = !!lastReadTime && lastRenderedMessageTime < lastReadTime;
      setShow(
        showAlways ? scrolledBelowSeparator || scrolledAboveSeparator : scrolledBelowSeparator
      );
    },
    [lastRead, showAlways, unreadCount]
  );
  (0, import_react29.useEffect)(() => {
    if (!unreadCount) setShow(false);
  }, [unreadCount]);
  return { show, toggleShowUnreadMessagesNotification };
};
var FloatingDateSeparator = (props) => {
  const { DateSeparator: DateSeparator$1 = DateSeparator } = useComponentContext();
  const { disableDateSeparator, processedMessages } = props;
  const listElement = "listElement" in props ? props.listElement : null;
  const useDomMode = listElement != null;
  const virtuosoResult = useFloatingDateSeparator({
    disableDateSeparator,
    processedMessages
  });
  const domResult = useFloatingDateSeparatorMessageList({
    disableDateSeparator,
    listElement,
    processedMessages
  });
  const floatingDate = useDomMode ? domResult.floatingDate : virtuosoResult.floatingDate;
  const showFloatingDate = useDomMode ? domResult.showFloatingDate : virtuosoResult.showFloatingDate;
  const itemsRenderedRef = "itemsRenderedRef" in props ? props.itemsRenderedRef : null;
  (0, import_react29.useLayoutEffect)(() => {
    if (!itemsRenderedRef) return;
    itemsRenderedRef.current = virtuosoResult.onItemsRendered;
    return () => {
      itemsRenderedRef.current = null;
    };
  }, [itemsRenderedRef, virtuosoResult.onItemsRendered]);
  if (!showFloatingDate || !floatingDate || !DateSeparator$1) return null;
  return (0, import_jsx_runtime4.jsx)(DateSeparator$1, { date: floatingDate, floating: true });
};
var UnMemoizedEventComponent = (props) => {
  const { message, unsafeHTML = false } = props;
  const { type } = message;
  if (type !== "system") return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message--system", "data-testid": "message-system", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message--system__text", children: unsafeHTML ? (0, import_jsx_runtime4.jsx)(
    "div",
    {
      dangerouslySetInnerHTML: { __html: message.html || "" },
      "data-unsafe-inner-html": true
    }
  ) : (0, import_jsx_runtime4.jsx)("span", { children: message.text }) }) });
};
var EventComponent = import_react29.default.memo(
  UnMemoizedEventComponent
);
function defaultRenderMessages({
  channelUnreadUiState,
  components,
  customClasses,
  lastOwnMessage,
  lastReceivedMessageId: lastReceivedId,
  messageGroupStyles,
  messages,
  ownMessagesDeliveredToOthers,
  readData,
  sharedMessageProps: messageProps
}) {
  const {
    DateSeparator: DateSeparator$1 = DateSeparator,
    HeaderComponent,
    MessageListItem = "li",
    MessageSystem = EventComponent,
    UnreadMessagesSeparator: UnreadMessagesSeparator$1 = UnreadMessagesSeparator
  } = components;
  const renderedMessages = [];
  let firstMessage;
  let previousMessage = void 0;
  for (let index6 = 0; index6 < messages.length; index6++) {
    const message = messages[index6];
    if (isDateSeparatorMessage(message)) {
      renderedMessages.push(
        (0, import_jsx_runtime4.jsx)(MessageListItem, { "data-index": index6, children: (0, import_jsx_runtime4.jsx)(
          DateSeparator$1,
          {
            date: message.date,
            formatDate: messageProps.formatDate,
            unread: message.unread
          }
        ) }, `${message.date.toISOString()}-i`)
      );
    } else if (isIntroMessage(message)) {
      if (HeaderComponent) {
        renderedMessages.push(
          (0, import_jsx_runtime4.jsx)(MessageListItem, { "data-index": index6, children: (0, import_jsx_runtime4.jsx)(HeaderComponent, {}) }, "intro")
        );
      }
    } else if (message.type === "system") {
      renderedMessages.push(
        (0, import_jsx_runtime4.jsx)(
          MessageListItem,
          {
            "data-index": index6,
            "data-message-id": message.id,
            children: (0, import_jsx_runtime4.jsx)(MessageSystem, { message, unsafeHTML: messageProps.unsafeHTML })
          },
          message.id || message.created_at.toISOString()
        )
      );
    } else {
      if (!firstMessage) {
        firstMessage = message;
      }
      const groupStyles = messageGroupStyles[message.id] || "";
      const messageClass = customClasses?.message || `str-chat__li str-chat__li--${groupStyles}`;
      const isFirstUnreadMessage = getIsFirstUnreadMessage({
        firstUnreadMessageId: channelUnreadUiState?.first_unread_message_id,
        isFirstMessage: !!firstMessage?.id && firstMessage.id === message.id,
        lastReadDate: channelUnreadUiState?.last_read,
        lastReadMessageId: channelUnreadUiState?.last_read_message_id,
        message,
        previousMessage,
        unreadMessageCount: channelUnreadUiState?.unread_messages
      });
      renderedMessages.push(
        (0, import_jsx_runtime4.jsxs)(import_react29.Fragment, { children: [
          isFirstUnreadMessage && UnreadMessagesSeparator$1 && (0, import_jsx_runtime4.jsx)(MessageListItem, { className: "str-chat__li str-chat__unread-messages-separator-wrapper", children: (0, import_jsx_runtime4.jsx)(
            UnreadMessagesSeparator$1,
            {
              unreadCount: channelUnreadUiState?.unread_messages
            }
          ) }),
          (0, import_jsx_runtime4.jsx)(
            MessageListItem,
            {
              className: messageClass,
              "data-index": index6,
              "data-message-id": message.id,
              "data-testid": messageClass,
              children: (0, import_jsx_runtime4.jsx)(
                Message,
                {
                  deliveredTo: ownMessagesDeliveredToOthers[message.id] || [],
                  groupStyles: [groupStyles],
                  lastOwnMessage,
                  lastReceivedId,
                  message,
                  readBy: readData[message.id] || [],
                  ...messageProps
                }
              )
            }
          )
        ] }, message.id || message.created_at.toISOString())
      );
      previousMessage = message;
    }
  }
  return renderedMessages;
}
var findReverse = (items, matches2) => {
  for (let i2 = items.length - 1; i2 >= 0; i2 -= 1) {
    if (matches2(items[i2])) {
      return items[i2];
    }
  }
  return void 0;
};
var useLastOwnMessage = ({
  messages,
  ownUserId
}) => (0, import_react29.useMemo)(
  () => messages && findReverse(messages, (msg) => (msg.user && msg.user.id) === ownUserId),
  [messages, ownUserId]
);
var UnMemoizedScrollToLatestMessageButton = (props) => {
  const {
    isMessageListScrolledToBottom,
    isNotAtLatestMessageSet = false,
    onClick,
    threadList
  } = props;
  const { channel: activeChannel, client } = useChatContext();
  const { thread } = useChannelStateContext();
  const [countUnread, setCountUnread] = (0, import_react29.useState)(activeChannel?.countUnread() || 0);
  const [replyCount, setReplyCount] = (0, import_react29.useState)(thread?.reply_count || 0);
  const observedEvent = threadList ? "message.updated" : "message.new";
  (0, import_react29.useEffect)(() => {
    const handleEvent = (event) => {
      const newMessageInAnotherChannel = event.cid !== activeChannel?.cid;
      const newMessageIsMine = event.user?.id === client.user?.id;
      const isThreadOpen = !!thread;
      const newMessageIsReply = !!event.message?.parent_id;
      const dontIncreaseMainListCounterOnNewReply = isThreadOpen && !threadList && newMessageIsReply;
      if (isMessageListScrolledToBottom || newMessageInAnotherChannel || newMessageIsMine || dontIncreaseMainListCounterOnNewReply) {
        return;
      }
      if (event.type === "message.new") {
        setCountUnread((prev) => prev + 1);
      } else if (event.message?.id === thread?.id) {
        const newReplyCount = event.message?.reply_count || 0;
        setCountUnread(() => newReplyCount - replyCount);
      }
    };
    client.on(observedEvent, handleEvent);
    return () => {
      client.off(observedEvent, handleEvent);
    };
  }, [
    activeChannel,
    client,
    isMessageListScrolledToBottom,
    observedEvent,
    replyCount,
    thread,
    threadList
  ]);
  (0, import_react29.useEffect)(() => {
    if (isMessageListScrolledToBottom) {
      setCountUnread(0);
      setReplyCount(thread?.reply_count || 0);
    }
  }, [isMessageListScrolledToBottom, thread]);
  if (isMessageListScrolledToBottom && !isNotAtLatestMessageSet) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__jump-to-latest-message", children: [
    (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "outline",
        "aria-live": "polite",
        circular: true,
        className: "str-chat__jump-to-latest-message__button",
        "data-testid": "scroll-to-latest-message-button",
        onClick,
        size: "md",
        variant: "secondary",
        children: (0, import_jsx_runtime4.jsx)(IconArrowDown, {})
      }
    ),
    countUnread > 0 && (0, import_jsx_runtime4.jsx)(
      Badge,
      {
        className: "str-chat__jump-to-latest__unread-count",
        "data-testid": "unread-message-notification-counter",
        size: "md",
        variant: "primary",
        children: countUnread
      }
    )
  ] });
};
var ScrollToLatestMessageButton = import_react29.default.memo(
  UnMemoizedScrollToLatestMessageButton
);
var getMessageSetSignature = (messages) => `${messages.length}:${messages[0]?.id || ""}:${messages[messages.length - 1]?.id || ""}`;
var getMessageTimestamp = (message) => message?.created_at?.getTime?.() ?? null;
var MessageListWithContext = (props) => {
  const {
    channel,
    channelUnreadUiState,
    disableDateSeparator = false,
    groupStyles,
    hasMoreNewer = false,
    headerPosition,
    hideDeletedMessages = false,
    hideNewMessageSeparator = false,
    highlightedMessageId,
    internalInfiniteScrollProps: {
      threshold: loadMoreScrollThreshold = DEFAULT_LOAD_PAGE_SCROLL_THRESHOLD,
      ...restInternalInfiniteScrollProps
    } = {},
    jumpToLatestMessage = () => Promise.resolve(),
    loadMore: loadMoreCallback,
    loadMoreNewer: loadMoreNewerCallback,
    maxTimeBetweenGroupedMessages,
    messageActions = Object.keys(MESSAGE_ACTIONS),
    messageLimit = DEFAULT_NEXT_CHANNEL_PAGE_SIZE,
    messages = [],
    noGroupByUser = false,
    reactionDetailsSort,
    renderMessages = defaultRenderMessages,
    returnAllReadData = false,
    reviewProcessedMessage,
    showUnreadNotificationAlways,
    sortReactions,
    suppressAutoscroll,
    threadList = false,
    unsafeHTML = false
  } = props;
  const [listElement, setListElement] = import_react29.default.useState(null);
  const [jumpToLatestPhase, setJumpToLatestPhase] = import_react29.default.useState("idle");
  const [highlightedJumpPhase, setHighlightedJumpPhase] = import_react29.default.useState("idle");
  const [jumpSourceMessageSetSignature, setJumpSourceMessageSetSignature] = import_react29.default.useState(null);
  const previousHasMoreNewerRef = import_react29.default.useRef(hasMoreNewer);
  const previousMessageSetSignatureRef = import_react29.default.useRef("");
  const previousMessageSetBoundsRef = import_react29.default.useRef({
    firstTimestamp: null,
    lastTimestamp: null
  });
  const { customClasses } = useChatContext("MessageList");
  const {
    EmptyStateIndicator: EmptyStateIndicator$1 = EmptyStateIndicator,
    LoadingIndicator: LoadingIndicator$1 = LoadingIndicator,
    MessageListMainPanel: MessageListMainPanel$1 = MessageListMainPanel,
    MessageListWrapper = "ul",
    NewMessageNotification: NewMessageNotification$1 = NewMessageNotification,
    NotificationList: NotificationList$1 = NotificationList,
    TypingIndicator: TypingIndicator$1 = TypingIndicator,
    UnreadMessagesNotification: UnreadMessagesNotification$1 = UnreadMessagesNotification
  } = useComponentContext();
  const notificationTarget = useNotificationTarget();
  const messageSetSignature = import_react29.default.useMemo(
    () => getMessageSetSignature(messages),
    [messages]
  );
  const messageSetBounds = import_react29.default.useMemo(
    () => ({
      firstTimestamp: getMessageTimestamp(messages[0]),
      lastTimestamp: getMessageTimestamp(messages[messages.length - 1])
    }),
    [messages]
  );
  const isJumpingToLatest = jumpToLatestPhase !== "idle";
  const isHighlightedJumpRequested = !!highlightedMessageId;
  const isJumpingToHighlightedMessage = highlightedJumpPhase !== "idle";
  const justReachedLatestMergedSet = previousHasMoreNewerRef.current && !hasMoreNewer;
  const {
    hasNewMessages,
    isMessageListScrolledToBottom,
    onScroll,
    scrollToBottom,
    wrapperRect
  } = useScrollLocationLogic({
    disableAutoScrollToBottom: isJumpingToLatest || isHighlightedJumpRequested || justReachedLatestMergedSet,
    disableScrollManagement: isJumpingToLatest || isJumpingToHighlightedMessage,
    hasMoreNewer,
    listElement,
    loadingMore: props.loadingMore,
    loadMoreScrollThreshold,
    messages,
    // todo: is it correct to base the scroll logic on an array that does not contain date separators or intro?
    scrolledUpThreshold: props.scrolledUpThreshold,
    suppressAutoscroll
  });
  const isTypingIndicatorScrolledToBottom = isMessageListScrolledToBottom && !isJumpingToLatest && !isJumpingToHighlightedMessage && !justReachedLatestMergedSet;
  const { show: showUnreadMessagesNotification } = useUnreadMessagesNotification({
    isMessageListScrolledToBottom,
    listElement,
    showAlways: !!showUnreadNotificationAlways,
    unreadCount: channelUnreadUiState?.unread_messages
  });
  useMarkRead({
    isMessageListScrolledToBottom: isTypingIndicatorScrolledToBottom,
    messageListIsThread: threadList,
    wasMarkedUnread: !!channelUnreadUiState?.first_unread_message_id
  });
  const { messageGroupStyles, messages: enrichedMessages } = useEnrichedMessages({
    channel,
    disableDateSeparator,
    groupStyles,
    headerPosition,
    hideDeletedMessages,
    hideNewMessageSeparator,
    maxTimeBetweenGroupedMessages,
    messages,
    noGroupByUser,
    reviewProcessedMessage
  });
  const lastOwnMessage = useLastOwnMessage({
    messages,
    ownUserId: channel.getClient().user?.id
  });
  const elements = useMessageListElements({
    channelUnreadUiState,
    enrichedMessages,
    internalMessageProps: {
      additionalMessageComposerProps: props.additionalMessageComposerProps,
      closeReactionSelectorOnClick: props.closeReactionSelectorOnClick,
      disableQuotedMessages: props.disableQuotedMessages,
      formatDate: props.formatDate,
      Message: props.Message,
      messageActions,
      messageListRect: wrapperRect,
      onMentionsClick: props.onMentionsClick,
      onMentionsHover: props.onMentionsHover,
      onUserClick: props.onUserClick,
      onUserHover: props.onUserHover,
      openThread: props.openThread,
      reactionDetailsSort,
      renderText: props.renderText,
      retrySendMessage: props.retrySendMessage,
      showAvatar: props.showAvatar,
      sortReactions,
      unsafeHTML
    },
    lastOwnMessage,
    messageGroupStyles,
    messages,
    renderMessages,
    returnAllReadData,
    threadList
  });
  const messageListClass = customClasses?.messageList || "str-chat__message-list";
  const loadMore = import_react29.default.useCallback(() => {
    if (loadMoreCallback) {
      loadMoreCallback(messageLimit);
    }
  }, [loadMoreCallback, messageLimit]);
  const loadMoreNewer = import_react29.default.useCallback(() => {
    if (loadMoreNewerCallback) {
      loadMoreNewerCallback(messageLimit);
    }
  }, [loadMoreNewerCallback, messageLimit]);
  const scrollToBottomFromNotification = import_react29.default.useCallback(async () => {
    if (hasMoreNewer) {
      setJumpSourceMessageSetSignature(messageSetSignature);
      setJumpToLatestPhase("waiting-for-render");
      try {
        await jumpToLatestMessage();
      } catch (error2) {
        setJumpSourceMessageSetSignature(null);
        setJumpToLatestPhase("idle");
        throw error2;
      }
      return;
    }
    scrollToBottom({ behavior: "smooth" });
  }, [hasMoreNewer, jumpToLatestMessage, messageSetSignature, scrollToBottom]);
  import_react29.default.useLayoutEffect(() => {
    if (jumpToLatestPhase !== "waiting-for-render" || hasMoreNewer || !listElement?.scrollTo || messageSetSignature === jumpSourceMessageSetSignature) {
      return;
    }
    listElement.scrollTo({ top: 0 });
    const animationFrameId = requestAnimationFrame(() => {
      setJumpToLatestPhase("animating");
      listElement.scrollTo({
        behavior: "smooth",
        top: listElement.scrollHeight
      });
    });
    return () => cancelAnimationFrame(animationFrameId);
  }, [
    hasMoreNewer,
    jumpSourceMessageSetSignature,
    jumpToLatestPhase,
    listElement,
    messageSetSignature
  ]);
  import_react29.default.useLayoutEffect(() => {
    if (jumpToLatestPhase !== "animating" || !listElement?.scrollTo) {
      return;
    }
    const finalize = () => {
      listElement.scrollTo({ top: listElement.scrollHeight });
      setJumpSourceMessageSetSignature(null);
      setJumpToLatestPhase("idle");
    };
    const settleTimeoutId = setTimeout(finalize, 500);
    return () => {
      clearTimeout(settleTimeoutId);
    };
  }, [jumpToLatestPhase, listElement]);
  import_react29.default.useLayoutEffect(() => {
    if (!highlightedMessageId) {
      setHighlightedJumpPhase("idle");
      return;
    }
    const element4 = listElement?.querySelector(
      `[data-message-id='${highlightedMessageId}']`
    );
    if (!element4) {
      setHighlightedJumpPhase("waiting-for-render");
      return;
    }
    const messageSetChanged = previousMessageSetSignatureRef.current !== messageSetSignature;
    setHighlightedJumpPhase(messageSetChanged ? "animating" : "idle");
    let settleTimeoutId;
    const animationFrameId = requestAnimationFrame(() => {
      element4.scrollIntoView({
        behavior: "smooth",
        block: "center"
      });
      if (!messageSetChanged || !listElement?.scrollTo) {
        setHighlightedJumpPhase("idle");
        return;
      }
      settleTimeoutId = setTimeout(() => {
        const elementRect = element4.getBoundingClientRect();
        const listRect = listElement.getBoundingClientRect();
        const targetTop = listElement.scrollTop + (elementRect.top - listRect.top) - (listElement.clientHeight - elementRect.height) / 2;
        listElement.scrollTo({ top: Math.max(targetTop, 0) });
        setHighlightedJumpPhase("idle");
      }, 500);
    });
    return () => {
      cancelAnimationFrame(animationFrameId);
      if (settleTimeoutId) {
        clearTimeout(settleTimeoutId);
      }
    };
  }, [highlightedMessageId, messageSetSignature]);
  import_react29.default.useEffect(() => {
    previousHasMoreNewerRef.current = hasMoreNewer;
  }, [hasMoreNewer]);
  import_react29.default.useEffect(() => {
    previousMessageSetSignatureRef.current = messageSetSignature;
    previousMessageSetBoundsRef.current = messageSetBounds;
  }, [messageSetBounds, messageSetSignature]);
  const id3 = useStableId();
  const showEmptyStateIndicator = elements.length === 0 && !threadList;
  const dialogManagerId = threadList ? `message-list-dialog-manager-thread-${id3}` : `message-list-dialog-manager-${id3}`;
  return (0, import_jsx_runtime4.jsx)(
    MessageListContextProvider,
    {
      value: {
        listElement,
        processedMessages: enrichedMessages,
        scrollToBottom
      },
      children: (0, import_jsx_runtime4.jsx)(MessageTranslationViewProvider, { children: (0, import_jsx_runtime4.jsxs)(MessageListMainPanel$1, { children: [
        (0, import_jsx_runtime4.jsxs)(DialogManagerProvider, { id: dialogManagerId, children: [
          !threadList && showUnreadMessagesNotification && (0, import_jsx_runtime4.jsx)(
            UnreadMessagesNotification$1,
            {
              unreadCount: channelUnreadUiState?.unread_messages
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            FloatingDateSeparator,
            {
              disableDateSeparator,
              listElement,
              processedMessages: enrichedMessages
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            "div",
            {
              className: clsx_default(messageListClass, customClasses?.threadList),
              onScroll,
              ref: setListElement,
              tabIndex: 0,
              children: showEmptyStateIndicator ? (0, import_jsx_runtime4.jsx)(EmptyStateIndicator$1, { listType: threadList ? "thread" : "message" }) : (0, import_jsx_runtime4.jsxs)(
                InfiniteScroll,
                {
                  className: "str-chat__message-list-scroll",
                  "data-testid": "reverse-infinite-scroll",
                  hasNextPage: props.hasMoreNewer,
                  hasPreviousPage: isJumpingToHighlightedMessage ? false : props.hasMore,
                  head: props.head,
                  isLoading: Boolean(
                    props.loadingMore || props.loadingMoreForJumpToChannelMessage
                  ),
                  loader: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__list__loading", children: (props.loadingMore || props.loadingMoreForJumpToChannelMessage) && (0, import_jsx_runtime4.jsx)(LoadingIndicator$1, {}) }, "loading-indicator"),
                  loadNextPage: loadMoreNewer,
                  loadPreviousPage: isJumpingToHighlightedMessage ? () => void 0 : loadMore,
                  threshold: loadMoreScrollThreshold,
                  ...restInternalInfiniteScrollProps,
                  children: [
                    (0, import_jsx_runtime4.jsx)(MessageListWrapper, { className: "str-chat__ul", children: elements }),
                    (0, import_jsx_runtime4.jsx)(
                      TypingIndicator$1,
                      {
                        isMessageListScrolledToBottom: isTypingIndicatorScrolledToBottom,
                        scrollToBottom,
                        threadList
                      }
                    ),
                    (0, import_jsx_runtime4.jsx)("div", {}, "bottom")
                  ]
                }
              )
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            NewMessageNotification$1,
            {
              newMessageCount: channelUnreadUiState?.unread_messages,
              showNotification: hasNewMessages || hasMoreNewer
            }
          ),
          (0, import_jsx_runtime4.jsx)(
            ScrollToLatestMessageButton,
            {
              isMessageListScrolledToBottom,
              isNotAtLatestMessageSet: hasMoreNewer,
              onClick: scrollToBottomFromNotification,
              threadList
            }
          )
        ] }),
        (0, import_jsx_runtime4.jsx)(NotificationList$1, { panel: notificationTarget })
      ] }) })
    }
  );
};
var MessageList = (props) => {
  const { jumpToLatestMessage, loadMore, loadMoreNewer } = useChannelActionContext("MessageList");
  const {
    members: membersPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    mutes: mutesPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    watchers: watchersPropToNotPass,
    // eslint-disable-line @typescript-eslint/no-unused-vars
    ...restChannelStateContext
  } = useChannelStateContext("MessageList");
  return (0, import_jsx_runtime4.jsx)(
    MessageListWithContext,
    {
      jumpToLatestMessage,
      loadMore,
      loadMoreNewer,
      ...restChannelStateContext,
      ...props
    }
  );
};
var PREPEND_OFFSET = 10 ** 7;
function calculateItemIndex(virtuosoIndex, numItemsPrepended) {
  return virtuosoIndex + numItemsPrepended - PREPEND_OFFSET;
}
function calculateFirstItemIndex(numItemsPrepended) {
  return PREPEND_OFFSET - numItemsPrepended;
}
var makeItemsRenderedHandler = (renderedItemsActions, processedMessages) => (0, import_lodash3.default)((items) => {
  const renderedMessages = items.map((item) => {
    if (!item.originalIndex) return void 0;
    return processedMessages[calculateItemIndex(item.originalIndex, PREPEND_OFFSET)];
  }).filter((msg) => !!msg);
  renderedItemsActions.forEach(
    (action) => action(renderedMessages)
  );
}, 200);
var Item = ({ context, ...props }) => {
  if (!context) return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, {});
  const message = context.processedMessages[calculateItemIndex(props["data-item-index"], context.numItemsPrepended)];
  const groupStyles = context.messageGroupStyles[message.id];
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      ...props,
      className: context?.customClasses?.virtualMessage || clsx_default("str-chat__virtual-list-message-wrapper str-chat__li", {
        [`str-chat__li--${groupStyles}`]: groupStyles
      })
    }
  );
};
var Header = ({ context }) => {
  const { LoadingIndicator: LoadingIndicator$1 = LoadingIndicator } = useComponentContext();
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    context?.head,
    context?.loadingMore && LoadingIndicator$1 && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__virtual-list__loading", children: (0, import_jsx_runtime4.jsx)(LoadingIndicator$1, {}) })
  ] });
};
var EmptyPlaceholder = ({ context }) => {
  const { EmptyStateIndicator: EmptyStateIndicator$1 = EmptyStateIndicator } = useComponentContext();
  if (typeof context?.processedMessages !== "undefined" && context.processedMessages.length > 0)
    return null;
  return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: EmptyStateIndicator$1 && (0, import_jsx_runtime4.jsx)(EmptyStateIndicator$1, { listType: context?.threadList ? "thread" : "message" }) });
};
var messageRenderer = (virtuosoIndex, _data, virtuosoContext) => {
  const {
    additionalMessageComposerProps,
    closeReactionSelectorOnClick,
    customMessageRenderer,
    DateSeparator: DateSeparator2,
    firstUnreadMessageId,
    formatDate,
    lastOwnMessage,
    lastReadDate,
    lastReadMessageId,
    lastReceivedMessageId,
    Message: MessageUIComponent,
    messageActions,
    messageGroupStyles,
    MessageSystem,
    numItemsPrepended,
    openThread,
    ownMessagesDeliveredToOthers,
    ownMessagesReadByOthers,
    processedMessages: messageList,
    reactionDetailsSort,
    renderText: renderText2,
    returnAllReadData,
    showAvatar,
    sortReactions,
    threadList,
    unreadMessageCount = 0,
    UnreadMessagesSeparator: UnreadMessagesSeparator2,
    virtuosoRef
  } = virtuosoContext;
  const streamMessageIndex = calculateItemIndex(virtuosoIndex, numItemsPrepended);
  if (customMessageRenderer) {
    return customMessageRenderer(messageList, streamMessageIndex);
  }
  const message = messageList[streamMessageIndex];
  if (!message || isIntroMessage(message)) return (0, import_jsx_runtime4.jsx)("div", { style: { height: "1px" } });
  if (isDateSeparatorMessage(message)) {
    return DateSeparator2 ? (0, import_jsx_runtime4.jsx)(DateSeparator2, { date: message.date, unread: message.unread }) : null;
  }
  if (message.type === "system") {
    return MessageSystem ? (0, import_jsx_runtime4.jsx)(MessageSystem, { message }) : null;
  }
  const isFirstUnreadMessage = getIsFirstUnreadMessage({
    firstUnreadMessageId,
    isFirstMessage: streamMessageIndex === 0,
    lastReadDate,
    lastReadMessageId,
    message,
    previousMessage: streamMessageIndex ? messageList[streamMessageIndex - 1] : void 0,
    unreadMessageCount
  });
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    isFirstUnreadMessage && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__unread-messages-separator-wrapper", children: (0, import_jsx_runtime4.jsx)(UnreadMessagesSeparator2, { unreadCount: unreadMessageCount }) }),
    (0, import_jsx_runtime4.jsx)(
      Message,
      {
        additionalMessageComposerProps,
        autoscrollToBottom: virtuosoRef.current?.autoscrollToBottom,
        closeReactionSelectorOnClick,
        deliveredTo: ownMessagesDeliveredToOthers[message.id] || [],
        formatDate,
        groupStyles: [messageGroupStyles[message.id] ?? ""],
        lastOwnMessage,
        lastReceivedId: lastReceivedMessageId,
        message,
        Message: MessageUIComponent,
        messageActions,
        openThread,
        reactionDetailsSort,
        readBy: ownMessagesReadByOthers[message.id] || [],
        renderText: renderText2,
        returnAllReadData,
        showAvatar,
        sortReactions,
        threadList
      }
    )
  ] });
};
var VirtualizedMessageListContext = (0, import_react29.createContext)(void 0);
var VirtualizedMessageListContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime4.jsx)(
  VirtualizedMessageListContext.Provider,
  {
    value,
    children
  }
);
function captureResizeObserverExceededError(e) {
  if (e.message === "ResizeObserver loop completed with undelivered notifications." || e.message === "ResizeObserver loop limit exceeded") {
    e.stopImmediatePropagation();
  }
}
function useCaptureResizeObserverExceededError() {
  (0, import_react29.useEffect)(() => {
    window.addEventListener("error", captureResizeObserverExceededError);
    return () => {
      window.removeEventListener("error", captureResizeObserverExceededError);
    };
  }, []);
}
function fractionalItemSize(element4) {
  return element4.getBoundingClientRect().height;
}
function findMessageIndex(messages, id3) {
  return messages.findIndex((message) => message.id === id3);
}
function calculateInitialTopMostItemIndex(messages, highlightedMessageId) {
  if (highlightedMessageId) {
    const index6 = findMessageIndex(messages, highlightedMessageId);
    if (index6 !== -1) {
      return { align: "center", index: index6 };
    }
  }
  return messages.length - 1;
}
var VirtualizedMessageListWithContext = (props) => {
  const {
    additionalMessageComposerProps,
    additionalVirtuosoProps = {},
    channel,
    channelUnreadUiState,
    closeReactionSelectorOnClick,
    customMessageRenderer,
    defaultItemHeight,
    disableDateSeparator = true,
    formatDate,
    groupStyles,
    hasMoreNewer,
    head,
    hideDeletedMessages = false,
    hideNewMessageSeparator = false,
    highlightedMessageId,
    jumpToLatestMessage,
    loadingMore,
    loadMore,
    loadMoreNewer,
    maxTimeBetweenGroupedMessages,
    Message: MessageUIComponentFromProps,
    messageActions,
    messageLimit = DEFAULT_NEXT_CHANNEL_PAGE_SIZE,
    messages,
    openThread,
    // TODO: refactor to scrollSeekPlaceHolderConfiguration and components.ScrollSeekPlaceholder, like the Virtuoso Component
    overscan = 0,
    reactionDetailsSort,
    renderText: renderText2,
    returnAllReadData = false,
    reviewProcessedMessage,
    scrollSeekPlaceHolder,
    scrollToLatestMessageOnFocus = false,
    separateGiphyPreview = false,
    shouldGroupByUser = false,
    showAvatar,
    showUnreadNotificationAlways,
    sortReactions,
    stickToBottomScrollBehavior = "smooth",
    suppressAutoscroll,
    threadList
  } = props;
  const { components: virtuosoComponentsFromProps, ...overridingVirtuosoProps } = additionalVirtuosoProps;
  useCaptureResizeObserverExceededError();
  const {
    DateSeparator: DateSeparator$1 = DateSeparator,
    GiphyPreviewMessage: GiphyPreviewMessage$1 = GiphyPreviewMessage,
    MessageListMainPanel: MessageListMainPanel$1 = MessageListMainPanel,
    MessageSystem = EventComponent,
    NewMessageNotification: NewMessageNotification$1 = NewMessageNotification,
    NotificationList: NotificationList$1 = NotificationList,
    TypingIndicator: TypingIndicator2,
    UnreadMessagesNotification: UnreadMessagesNotification$1 = UnreadMessagesNotification,
    UnreadMessagesSeparator: UnreadMessagesSeparator$1 = UnreadMessagesSeparator,
    VirtualMessage: MessageUIComponentFromContext = MessageUI
  } = useComponentContext();
  const MessageUIComponent = MessageUIComponentFromProps || MessageUIComponentFromContext;
  const { client, customClasses } = useChatContext("VirtualizedMessageList");
  const notificationTarget = useNotificationTarget();
  const virtuoso = (0, import_react29.useRef)(null);
  const lastRead = (0, import_react29.useMemo)(() => channel.lastRead?.(), [channel]);
  const { show: showUnreadMessagesNotification, toggleShowUnreadMessagesNotification } = useUnreadMessagesNotificationVirtualized({
    lastRead: channelUnreadUiState?.last_read,
    showAlways: !!showUnreadNotificationAlways,
    unreadCount: channelUnreadUiState?.unread_messages ?? 0
  });
  const { giphyPreviewMessage, setGiphyPreviewMessage } = useGiphyPreview(separateGiphyPreview);
  const processedMessages = (0, import_react29.useMemo)(() => {
    if (typeof messages === "undefined") {
      return [];
    }
    if (disableDateSeparator && !hideDeletedMessages && hideNewMessageSeparator && !separateGiphyPreview) {
      return messages;
    }
    return processMessages({
      enableDateSeparator: !disableDateSeparator,
      hideDeletedMessages,
      hideNewMessageSeparator,
      lastRead,
      messages,
      reviewProcessedMessage,
      setGiphyPreviewMessage,
      userId: client.userID || ""
    });
  }, [
    disableDateSeparator,
    hideDeletedMessages,
    hideNewMessageSeparator,
    lastRead,
    messages,
    messages?.length,
    client.userID
  ]);
  const floatingDateItemsRenderedRef = (0, import_react29.useRef)(null);
  const lastOwnMessage = useLastOwnMessage({ messages, ownUserId: client.user?.id });
  const ownMessagesReadByOthers = useLastReadData({
    channel,
    lastOwnMessage,
    messages: messages || [],
    returnAllReadData
  });
  const ownMessagesDeliveredToOthers = useLastDeliveredData({
    channel,
    lastOwnMessage,
    messages: messages || [],
    returnAllReadData
  });
  const lastReceivedMessageId = (0, import_react29.useMemo)(
    () => getLastReceived(processedMessages),
    [processedMessages]
  );
  const groupStylesFn = groupStyles || getGroupStyles;
  const messageGroupStyles = (0, import_react29.useMemo)(
    () => processedMessages.reduce((acc, message, i2) => {
      const style = groupStylesFn(
        message,
        processedMessages[i2 - 1],
        processedMessages[i2 + 1],
        !shouldGroupByUser,
        maxTimeBetweenGroupedMessages
      );
      if (style && message.id) acc[message.id] = style;
      return acc;
    }, {}),
    // processedMessages were incorrectly rebuilt with a new object identity at some point, hence the .length usage
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      maxTimeBetweenGroupedMessages,
      processedMessages.length,
      shouldGroupByUser,
      groupStylesFn
    ]
  );
  const {
    atBottom,
    isMessageListScrolledToBottom,
    newMessagesNotification,
    setIsMessageListScrolledToBottom,
    setNewMessagesNotification
  } = useNewMessageNotification(processedMessages, client.userID, hasMoreNewer);
  useMarkRead({
    isMessageListScrolledToBottom,
    messageListIsThread: !!threadList,
    wasMarkedUnread: !!channelUnreadUiState?.first_unread_message_id
  });
  const scrollToBottom = (0, import_react29.useCallback)(async () => {
    if (hasMoreNewer) {
      await jumpToLatestMessage();
      return;
    }
    if (virtuoso.current) {
      virtuoso.current.scrollToIndex(processedMessages.length - 1);
    }
    setNewMessagesNotification(false);
  }, [
    virtuoso,
    processedMessages,
    setNewMessagesNotification,
    // processedMessages were incorrectly rebuilt with a new object identity at some point, hence the .length usage
    processedMessages.length,
    hasMoreNewer,
    jumpToLatestMessage
  ]);
  useScrollToBottomOnNewMessage({
    messages,
    scrollToBottom,
    scrollToLatestMessageOnFocus
  });
  const numItemsPrepended = usePrependedMessagesCount(
    processedMessages,
    !disableDateSeparator
  );
  const { messageSetKey } = useMessageSetKey({ messages });
  const shouldForceScrollToBottom = useShouldForceScrollToBottom(
    processedMessages,
    client.userID
  );
  const handleItemsRendered = (0, import_react29.useMemo)(
    () => makeItemsRenderedHandler(
      [
        toggleShowUnreadMessagesNotification,
        (rendered) => floatingDateItemsRenderedRef.current?.(rendered)
      ],
      processedMessages
    ),
    [processedMessages, toggleShowUnreadMessagesNotification]
  );
  const followOutput = (isAtBottom) => {
    if (hasMoreNewer || suppressAutoscroll) {
      return false;
    }
    if (shouldForceScrollToBottom()) {
      return isAtBottom ? stickToBottomScrollBehavior : "auto";
    }
    return isAtBottom ? stickToBottomScrollBehavior : false;
  };
  const computeItemKey2 = (0, import_react29.useCallback)(
    (index6, _2, { numItemsPrepended: numItemsPrepended2, processedMessages: processedMessages2 }) => processedMessages2[calculateItemIndex(index6, numItemsPrepended2)].id,
    []
  );
  const atBottomStateChange = (isAtBottom) => {
    atBottom.current = isAtBottom;
    setIsMessageListScrolledToBottom(isAtBottom);
    if (isAtBottom) {
      loadMoreNewer?.(messageLimit);
      setNewMessagesNotification?.(false);
    }
  };
  const atTopStateChange = (isAtTop) => {
    if (isAtTop) {
      loadMore?.(messageLimit);
    }
  };
  (0, import_react29.useEffect)(() => {
    let scrollTimeout;
    if (highlightedMessageId) {
      const index6 = findMessageIndex(processedMessages, highlightedMessageId);
      if (index6 !== -1) {
        scrollTimeout = setTimeout(() => {
          virtuoso.current?.scrollToIndex({ align: "center", index: index6 });
        }, 0);
      }
    }
    return () => {
      clearTimeout(scrollTimeout);
    };
  }, [highlightedMessageId, processedMessages]);
  const id3 = useStableId();
  if (!processedMessages) return null;
  const dialogManagerId = threadList ? `virtualized-message-list-dialog-manager-thread-${id3}` : `virtualized-message-list-dialog-manager-${id3}`;
  return (0, import_jsx_runtime4.jsx)(VirtualizedMessageListContextProvider, { value: { scrollToBottom }, children: (0, import_jsx_runtime4.jsxs)(MessageTranslationViewProvider, { children: [
    (0, import_jsx_runtime4.jsxs)(MessageListMainPanel$1, { children: [
      (0, import_jsx_runtime4.jsxs)(DialogManagerProvider, { id: dialogManagerId, children: [
        !threadList && showUnreadMessagesNotification && (0, import_jsx_runtime4.jsx)(
          UnreadMessagesNotification$1,
          {
            unreadCount: channelUnreadUiState?.unread_messages
          }
        ),
        (0, import_jsx_runtime4.jsxs)(
          "div",
          {
            className: customClasses?.virtualizedMessageList || "str-chat__virtual-list",
            children: [
              (0, import_jsx_runtime4.jsx)(
                FloatingDateSeparator,
                {
                  disableDateSeparator,
                  itemsRenderedRef: floatingDateItemsRenderedRef,
                  processedMessages
                }
              ),
              (0, import_jsx_runtime4.jsx)(
                ln,
                {
                  atBottomStateChange,
                  atBottomThreshold: 100,
                  atTopStateChange,
                  atTopThreshold: 100,
                  className: "str-chat__message-list-scroll",
                  components: {
                    EmptyPlaceholder,
                    Header,
                    Item,
                    ...TypingIndicator2 && {
                      Footer: () => isMessageListScrolledToBottom ? (0, import_jsx_runtime4.jsx)(
                        TypingIndicator2,
                        {
                          isMessageListScrolledToBottom,
                          scrollToBottom,
                          threadList
                        }
                      ) : null
                    },
                    ...virtuosoComponentsFromProps
                  },
                  computeItemKey: computeItemKey2,
                  context: {
                    additionalMessageComposerProps,
                    closeReactionSelectorOnClick,
                    customClasses,
                    customMessageRenderer,
                    DateSeparator: DateSeparator$1,
                    firstUnreadMessageId: channelUnreadUiState?.first_unread_message_id,
                    formatDate,
                    head,
                    lastOwnMessage,
                    lastReadDate: channelUnreadUiState?.last_read,
                    lastReadMessageId: channelUnreadUiState?.last_read_message_id,
                    lastReceivedMessageId,
                    loadingMore,
                    Message: MessageUIComponent,
                    messageActions,
                    messageGroupStyles,
                    MessageSystem,
                    numItemsPrepended,
                    openThread,
                    ownMessagesDeliveredToOthers,
                    ownMessagesReadByOthers,
                    processedMessages,
                    reactionDetailsSort,
                    renderText: renderText2,
                    returnAllReadData,
                    shouldGroupByUser,
                    showAvatar,
                    sortReactions,
                    threadList,
                    unreadMessageCount: channelUnreadUiState?.unread_messages,
                    UnreadMessagesSeparator: UnreadMessagesSeparator$1,
                    virtuosoRef: virtuoso
                  },
                  firstItemIndex: calculateFirstItemIndex(numItemsPrepended),
                  followOutput,
                  increaseViewportBy: { bottom: 200, top: 0 },
                  initialTopMostItemIndex: calculateInitialTopMostItemIndex(
                    processedMessages,
                    highlightedMessageId
                  ),
                  itemContent: messageRenderer,
                  itemSize: fractionalItemSize,
                  itemsRendered: handleItemsRendered,
                  overscan,
                  ref: virtuoso,
                  style: { overflowX: "hidden" },
                  totalCount: processedMessages.length,
                  ...overridingVirtuosoProps,
                  ...scrollSeekPlaceHolder ? { scrollSeek: scrollSeekPlaceHolder } : {},
                  ...defaultItemHeight ? { defaultItemHeight } : {}
                },
                messageSetKey
              ),
              (0, import_jsx_runtime4.jsx)(
                NewMessageNotification$1,
                {
                  newMessageCount: channelUnreadUiState?.unread_messages,
                  showNotification: newMessagesNotification || hasMoreNewer
                }
              ),
              (0, import_jsx_runtime4.jsx)(
                ScrollToLatestMessageButton,
                {
                  isMessageListScrolledToBottom,
                  isNotAtLatestMessageSet: hasMoreNewer,
                  onClick: scrollToBottom,
                  threadList
                }
              )
            ]
          }
        )
      ] }),
      (0, import_jsx_runtime4.jsx)(NotificationList$1, { panel: notificationTarget })
    ] }),
    giphyPreviewMessage && (0, import_jsx_runtime4.jsx)(GiphyPreviewMessage$1, { message: giphyPreviewMessage })
  ] }) });
};
function VirtualizedMessageList(props) {
  const { jumpToLatestMessage, loadMore, loadMoreNewer } = useChannelActionContext(
    "VirtualizedMessageList"
  );
  const {
    channel,
    channelUnreadUiState,
    hasMore,
    hasMoreNewer,
    highlightedMessageId,
    loadingMore,
    loadingMoreNewer,
    messages: contextMessages,
    notifications,
    read,
    suppressAutoscroll
  } = useChannelStateContext("VirtualizedMessageList");
  const messages = props.messages || contextMessages;
  return (0, import_jsx_runtime4.jsx)(
    VirtualizedMessageListWithContext,
    {
      channel,
      channelUnreadUiState: props.channelUnreadUiState ?? channelUnreadUiState,
      hasMore: !!hasMore,
      hasMoreNewer: !!hasMoreNewer,
      highlightedMessageId,
      jumpToLatestMessage,
      loadingMore: !!loadingMore,
      loadingMoreNewer: !!loadingMoreNewer,
      loadMore,
      loadMoreNewer,
      messages,
      notifications,
      read,
      suppressAutoscroll,
      ...props
    }
  );
}
var getWholeChar = (str, i2) => {
  const code4 = str.charCodeAt(i2);
  if (Number.isNaN(code4)) return "";
  if (code4 < 55296 || code4 > 57343) return str.charAt(i2);
  if (55296 <= code4 && code4 <= 56319) {
    if (str.length <= i2 + 1) {
      throw "High surrogate without following low surrogate";
    }
    const next = str.charCodeAt(i2 + 1);
    if (56320 > next || next > 57343) {
      throw "High surrogate without following low surrogate";
    }
    return str.charAt(i2) + str.charAt(i2 + 1);
  }
  if (i2 === 0) {
    throw "Low surrogate without preceding high surrogate";
  }
  const prev = str.charCodeAt(i2 - 1);
  if (55296 > prev || prev > 56319) {
    throw "Low surrogate without preceding high surrogate";
  }
  return "";
};
var searchControllerStateSelector$4 = (value) => ({
  sources: value.sources
});
var useSearchQueriesInProgress = (searchController) => {
  const [queriesInProgress, setQueriesInProgress] = (0, import_react29.useState)([]);
  const { sources } = useStateStore(
    searchController.state,
    searchControllerStateSelector$4
  );
  (0, import_react29.useEffect)(() => {
    const subscriptions = sources.map(
      (source) => source.state.subscribeWithSelector(
        (value) => ({ isLoading: value.isLoading }),
        ({ isLoading }) => {
          setQueriesInProgress((prev) => {
            if (isLoading) return prev.concat(source.type);
            return prev.filter((type) => type !== source.type);
          });
        }
      )
    );
    return () => {
      subscriptions.forEach((unsubscribe) => unsubscribe());
    };
  }, [sources]);
  return queriesInProgress;
};
var useConnectionRecoveredListener = (forceUpdate) => {
  const { client } = useChatContext("useConnectionRecoveredListener");
  (0, import_react29.useEffect)(() => {
    const handleEvent = () => {
      if (forceUpdate) {
        forceUpdate();
      }
    };
    client.on("connection.recovered", handleEvent);
    return () => {
      client.off("connection.recovered", handleEvent);
    };
  }, [client, forceUpdate]);
};
var RECOVER_LOADED_CHANNELS_THROTTLE_INTERVAL_IN_MS = 5e3;
var MIN_RECOVER_LOADED_CHANNELS_THROTTLE_INTERVAL_IN_MS = 2e3;
var usePaginatedChannels = (client, filters, sort, options, activeChannelHandler, recoveryThrottleIntervalMs = RECOVER_LOADED_CHANNELS_THROTTLE_INTERVAL_IN_MS, customQueryChannels) => {
  const {
    channelsQueryState: { error: error2, setError, setQueryInProgress }
  } = useChatContext("usePaginatedChannels");
  const [channels, setChannels] = (0, import_react29.useState)([]);
  const [hasNextPage, setHasNextPage] = (0, import_react29.useState)(true);
  const lastRecoveryTimestamp = (0, import_react29.useRef)(void 0);
  const recoveryThrottleInterval = recoveryThrottleIntervalMs < MIN_RECOVER_LOADED_CHANNELS_THROTTLE_INTERVAL_IN_MS ? MIN_RECOVER_LOADED_CHANNELS_THROTTLE_INTERVAL_IN_MS : recoveryThrottleIntervalMs ?? RECOVER_LOADED_CHANNELS_THROTTLE_INTERVAL_IN_MS;
  const filterString = (0, import_react29.useMemo)(() => JSON.stringify(filters), [filters]);
  const sortString = (0, import_react29.useMemo)(() => JSON.stringify(sort), [sort]);
  const queryChannels = async (queryType = "load-more") => {
    setError(null);
    if (queryType === "reload") {
      setChannels([]);
    }
    setQueryInProgress(queryType);
    try {
      if (customQueryChannels) {
        await customQueryChannels({
          currentChannels: channels,
          queryType,
          setChannels,
          setHasNextPage
        });
      } else {
        const offset4 = queryType === "reload" ? 0 : channels.length;
        const newOptions = {
          offset: offset4,
          ...options
        };
        const channelQueryResponse = await client.queryChannels(
          filters,
          sort || {},
          newOptions
        );
        const newChannels = queryType === "reload" ? channelQueryResponse : (0, import_lodash4.default)([...channels, ...channelQueryResponse], "cid");
        setChannels(newChannels);
        setHasNextPage(channelQueryResponse.length >= (newOptions.limit ?? 1));
        if (!offset4 && activeChannelHandler) {
          activeChannelHandler(newChannels, setChannels);
        }
      }
    } catch (error22) {
      console.warn(error22);
      setError(error22);
    }
    setQueryInProgress(null);
  };
  const throttleRecover = (0, import_react29.useCallback)(() => {
    const now = Date.now();
    const isFirstRecovery = !lastRecoveryTimestamp.current;
    const timeElapsedSinceLastRecoveryMs = lastRecoveryTimestamp.current ? now - lastRecoveryTimestamp.current : 0;
    if (!isFirstRecovery && timeElapsedSinceLastRecoveryMs < recoveryThrottleInterval && !error2) {
      return;
    }
    lastRecoveryTimestamp.current = now;
    queryChannels("reload");
  }, [error2, queryChannels, recoveryThrottleInterval]);
  const loadNextPage = () => queryChannels();
  (0, import_react29.useEffect)(() => {
    if (client.recoverStateOnReconnect) return;
    const { unsubscribe } = client.on("connection.recovered", throttleRecover);
    return () => {
      unsubscribe();
    };
  }, [client, throttleRecover]);
  (0, import_react29.useEffect)(() => {
    queryChannels("reload");
  }, [filterString, sortString]);
  return {
    channels,
    hasNextPage,
    loadNextPage,
    setChannels
  };
};
function findLastPinnedChannelIndex({ channels }) {
  let lastPinnedChannelIndex = null;
  for (const channel of channels) {
    if (!isChannelPinned(channel)) break;
    if (typeof lastPinnedChannelIndex === "number") {
      lastPinnedChannelIndex++;
    } else {
      lastPinnedChannelIndex = 0;
    }
  }
  return lastPinnedChannelIndex;
}
var moveChannelUpwards = ({
  channels,
  channelToMove,
  channelToMoveIndexWithinChannels,
  sort
}) => {
  const targetChannelIndex = channelToMoveIndexWithinChannels ?? channels.findIndex((channel) => channel.cid === channelToMove.cid);
  const targetChannelExistsWithinList = targetChannelIndex >= 0;
  const targetChannelAlreadyAtTheTop = targetChannelIndex === 0;
  const considerPinnedChannels = shouldConsiderPinnedChannels(sort);
  const isTargetChannelPinned = isChannelPinned(channelToMove);
  if (targetChannelAlreadyAtTheTop || considerPinnedChannels && isTargetChannelPinned) {
    return channels;
  }
  const newChannels = [...channels];
  if (targetChannelExistsWithinList) {
    newChannels.splice(targetChannelIndex, 1);
  }
  let lastPinnedChannelIndex = null;
  if (considerPinnedChannels) {
    lastPinnedChannelIndex = findLastPinnedChannelIndex({ channels: newChannels });
  }
  newChannels.splice(
    typeof lastPinnedChannelIndex === "number" ? lastPinnedChannelIndex + 1 : 0,
    0,
    channelToMove
  );
  return newChannels;
};
var shouldConsiderPinnedChannels = (sort) => {
  const value = extractSortValue({ atIndex: 0, sort, targetKey: "pinned_at" });
  if (typeof value !== "number") return false;
  return Math.abs(value) === 1;
};
var extractSortValue = ({
  atIndex,
  sort,
  targetKey
}) => {
  if (!sort) return null;
  let option = null;
  if (Array.isArray(sort)) {
    option = sort[atIndex] ?? null;
  } else {
    let index6 = 0;
    for (const key in sort) {
      if (index6 !== atIndex) {
        index6++;
        continue;
      }
      if (key !== targetKey) {
        return null;
      }
      option = sort;
      break;
    }
  }
  return option?.[targetKey] ?? null;
};
var shouldConsiderArchivedChannels = (filters) => {
  if (!filters) return false;
  return typeof filters.archived === "boolean";
};
var isChannelPinned = (channel) => {
  if (!channel) return false;
  const membership = channel.state.membership;
  return typeof membership.pinned_at === "string";
};
var isChannelArchived = (channel) => {
  if (!channel) return false;
  const membership = channel.state.membership;
  return typeof membership.archived_at === "string";
};
var shared = ({
  customHandler,
  event,
  setChannels
}) => {
  if (typeof customHandler === "function") {
    return customHandler(setChannels, event);
  }
  setChannels((channels) => {
    const channelIndex = channels.findIndex((channel) => channel.cid === event.cid);
    if (channelIndex < 0) return channels;
    channels.splice(channelIndex, 1);
    return [...channels];
  });
};
var useChannelListShapeDefaults = () => {
  const { client } = useChatContext();
  const handleMessageNew = (0, import_react29.useCallback)(
    ({
      allowNewMessagesFromUnfilteredChannels,
      customHandler,
      event,
      filters,
      lockChannelOrder,
      setChannels,
      sort
    }) => {
      if (typeof customHandler === "function") {
        return customHandler(setChannels, event);
      }
      const channelType = event.channel_type;
      const channelId = event.channel_id;
      if (!channelType || !channelId) return;
      setChannels((currentChannels) => {
        const targetChannel = client.channel(channelType, channelId);
        const targetChannelIndex = currentChannels.indexOf(targetChannel);
        const targetChannelExistsWithinList = targetChannelIndex >= 0;
        const isTargetChannelPinned = isChannelPinned(targetChannel);
        const isTargetChannelArchived = isChannelArchived(targetChannel);
        const considerArchivedChannels = shouldConsiderArchivedChannels(filters);
        const considerPinnedChannels = shouldConsiderPinnedChannels(sort);
        if (
          // filter is defined, target channel is archived and filter option is set to false
          considerArchivedChannels && isTargetChannelArchived && !filters.archived || // filter is defined, target channel isn't archived and filter option is set to true
          considerArchivedChannels && !isTargetChannelArchived && filters.archived || // sort option is defined, target channel is pinned
          considerPinnedChannels && isTargetChannelPinned || // list order is locked
          lockChannelOrder || // target channel is not within the loaded list and loading from cache is disallowed
          !targetChannelExistsWithinList && !allowNewMessagesFromUnfilteredChannels
        ) {
          return currentChannels;
        }
        return moveChannelUpwards({
          channels: currentChannels,
          channelToMove: targetChannel,
          channelToMoveIndexWithinChannels: targetChannelIndex,
          sort
        });
      });
    },
    [client]
  );
  const handleNotificationMessageNew = (0, import_react29.useCallback)(
    async ({
      allowNewMessagesFromUnfilteredChannels,
      customHandler,
      event,
      filters,
      setChannels,
      sort
    }) => {
      if (typeof customHandler === "function") {
        return customHandler(setChannels, event);
      }
      if (!event.channel) {
        return;
      }
      const channel = await getChannel({
        client,
        id: event.channel.id,
        type: event.channel.type
      });
      const considerArchivedChannels = shouldConsiderArchivedChannels(filters);
      if (isChannelArchived(channel) && considerArchivedChannels && !filters.archived) {
        return;
      }
      if (!allowNewMessagesFromUnfilteredChannels) {
        return;
      }
      setChannels(
        (channels) => moveChannelUpwards({
          channels,
          channelToMove: channel,
          sort
        })
      );
    },
    [client]
  );
  const handleNotificationAddedToChannel = (0, import_react29.useCallback)(
    async ({
      allowNewMessagesFromUnfilteredChannels,
      customHandler,
      event,
      setChannels,
      sort
    }) => {
      if (typeof customHandler === "function") {
        return customHandler(setChannels, event);
      }
      if (!event.channel || !allowNewMessagesFromUnfilteredChannels) {
        return;
      }
      const channel = await getChannel({
        client,
        id: event.channel.id,
        members: event.channel.members?.reduce(
          (newMembers, { user, user_id }) => {
            const userId = user_id || user?.id;
            if (userId) newMembers.push(userId);
            return newMembers;
          },
          []
        ),
        type: event.channel.type
      });
      setChannels(
        (channels) => moveChannelUpwards({
          channels,
          channelToMove: channel,
          sort
        })
      );
    },
    [client]
  );
  const handleNotificationRemovedFromChannel = (0, import_react29.useCallback)(
    ({
      customHandler,
      event,
      setChannels
    }) => {
      if (typeof customHandler === "function") {
        return customHandler(setChannels, event);
      }
      setChannels(
        (channels) => channels.filter((channel) => channel.cid !== event.channel?.cid)
      );
    },
    []
  );
  const handleMemberUpdated = (0, import_react29.useCallback)(
    ({
      event,
      filters,
      lockChannelOrder,
      setChannels,
      sort
    }) => {
      if (!event.member?.user || event.member.user.id !== client.userID || !event.channel_type) {
        return;
      }
      const channelType = event.channel_type;
      const channelId = event.channel_id;
      const considerPinnedChannels = shouldConsiderPinnedChannels(sort);
      const considerArchivedChannels = shouldConsiderArchivedChannels(filters);
      if (!considerPinnedChannels && !considerArchivedChannels || lockChannelOrder) {
        return;
      }
      const pinnedAtSort = extractSortValue({ atIndex: 0, sort, targetKey: "pinned_at" });
      setChannels((currentChannels) => {
        const targetChannel = client.channel(channelType, channelId);
        const targetChannelIndex = currentChannels.indexOf(targetChannel);
        const targetChannelExistsWithinList = targetChannelIndex >= 0;
        const isTargetChannelArchived = isChannelArchived(targetChannel);
        const isTargetChannelPinned = isChannelPinned(targetChannel);
        const newChannels = [...currentChannels];
        if (targetChannelExistsWithinList) {
          newChannels.splice(targetChannelIndex, 1);
        }
        if (considerArchivedChannels && isTargetChannelArchived && !filters.archived || considerArchivedChannels && !isTargetChannelArchived && filters.archived) {
          return newChannels;
        }
        let lastPinnedChannelIndex = null;
        if (pinnedAtSort === 1 || pinnedAtSort === -1 && !isTargetChannelPinned) {
          lastPinnedChannelIndex = findLastPinnedChannelIndex({ channels: newChannels });
        }
        const newTargetChannelIndex = typeof lastPinnedChannelIndex === "number" ? lastPinnedChannelIndex + 1 : 0;
        newChannels.splice(newTargetChannelIndex, 0, targetChannel);
        return newChannels;
      });
    },
    [client]
  );
  const handleChannelDeleted = (0, import_react29.useCallback)(
    (p2) => shared(p2),
    []
  );
  const handleChannelHidden = (0, import_react29.useCallback)(
    (p2) => shared(p2),
    []
  );
  const handleChannelVisible = (0, import_react29.useCallback)(
    async ({
      customHandler,
      event,
      filters,
      setChannels,
      sort
    }) => {
      if (typeof customHandler === "function") {
        return customHandler(setChannels, event);
      }
      if (!event.channel_id && !event.channel_type) {
        return;
      }
      const channel = await getChannel({
        client,
        id: event.channel_id,
        type: event.channel_type
      });
      const considerArchivedChannels = shouldConsiderArchivedChannels(filters);
      if (isChannelArchived(channel) && considerArchivedChannels && !filters.archived) {
        return;
      }
      setChannels(
        (channels) => moveChannelUpwards({
          channels,
          channelToMove: channel,
          sort
        })
      );
    },
    [client]
  );
  const handleChannelTruncated = (0, import_react29.useCallback)(
    ({ customHandler, event, setChannels }) => {
      if (typeof customHandler === "function") {
        return customHandler(setChannels, event);
      }
      setChannels((channels) => [...channels]);
    },
    []
  );
  const handleChannelUpdated = (0, import_react29.useCallback)(
    ({ customHandler, event, setChannels }) => {
      if (typeof customHandler === "function") {
        return customHandler(setChannels, event);
      }
      setChannels((channels) => {
        const channelIndex = channels.findIndex(
          (channel) => channel.cid === event.channel?.cid
        );
        if (channelIndex > -1 && event.channel) {
          const newChannels = channels;
          newChannels[channelIndex].data = {
            ...event.channel,
            hidden: event.channel?.hidden ?? newChannels[channelIndex].data?.hidden,
            own_capabilities: event.channel?.own_capabilities ?? newChannels[channelIndex].data?.own_capabilities
          };
          return [...newChannels];
        }
        return channels;
      });
    },
    []
  );
  const handleUserPresenceChanged = (0, import_react29.useCallback)(
    ({ event, setChannels }) => {
      setChannels((channels) => {
        const newChannels = channels.map((channel) => {
          if (!event.user?.id || !channel.state.members[event.user.id]) {
            return channel;
          }
          const newChannel = channel;
          newChannel.state.members[event.user.id].user = event.user;
          return newChannel;
        });
        return newChannels;
      });
    },
    []
  );
  return (0, import_react29.useMemo)(
    () => ({
      handleChannelDeleted,
      handleChannelHidden,
      handleChannelTruncated,
      handleChannelUpdated,
      handleChannelVisible,
      handleMemberUpdated,
      handleMessageNew,
      handleNotificationAddedToChannel,
      handleNotificationMessageNew,
      handleNotificationRemovedFromChannel,
      handleUserPresenceChanged
    }),
    [
      handleChannelDeleted,
      handleChannelHidden,
      handleChannelTruncated,
      handleChannelUpdated,
      handleChannelVisible,
      handleMemberUpdated,
      handleMessageNew,
      handleNotificationAddedToChannel,
      handleNotificationMessageNew,
      handleNotificationRemovedFromChannel,
      handleUserPresenceChanged
    ]
  );
};
var usePrepareShapeHandlers = ({
  allowNewMessagesFromUnfilteredChannels,
  customHandleChannelListShape,
  filters,
  lockChannelOrder,
  onAddedToChannel,
  onChannelDeleted,
  onChannelHidden,
  onChannelTruncated,
  onChannelUpdated,
  onChannelVisible,
  onMessageNew,
  onMessageNewHandler,
  onRemovedFromChannel,
  setChannels,
  sort
}) => {
  const defaults = useChannelListShapeDefaults();
  const defaultHandleChannelListShapeRef = (0, import_react29.useRef)(void 0);
  const customHandleChannelListShapeRef = (0, import_react29.useRef)(void 0);
  customHandleChannelListShapeRef.current = (event) => {
    customHandleChannelListShape?.({ defaults, event, setChannels });
  };
  defaultHandleChannelListShapeRef.current = (event) => {
    switch (event.type) {
      case "message.new":
        defaults.handleMessageNew({
          allowNewMessagesFromUnfilteredChannels,
          customHandler: onMessageNewHandler,
          event,
          filters,
          lockChannelOrder,
          setChannels,
          sort
        });
        break;
      case "notification.message_new":
        defaults.handleNotificationMessageNew({
          allowNewMessagesFromUnfilteredChannels,
          customHandler: onMessageNew,
          event,
          filters,
          lockChannelOrder,
          setChannels,
          sort
        });
        break;
      case "notification.added_to_channel":
        defaults.handleNotificationAddedToChannel({
          allowNewMessagesFromUnfilteredChannels,
          customHandler: onAddedToChannel,
          event,
          lockChannelOrder,
          setChannels,
          sort
        });
        break;
      case "notification.removed_from_channel":
        defaults.handleNotificationRemovedFromChannel({
          customHandler: onRemovedFromChannel,
          event,
          setChannels
        });
        break;
      case "channel.deleted":
        defaults.handleChannelDeleted({
          customHandler: onChannelDeleted,
          event,
          setChannels
        });
        break;
      case "channel.hidden":
        defaults.handleChannelHidden({
          customHandler: onChannelHidden,
          event,
          setChannels
        });
        break;
      case "channel.visible":
        defaults.handleChannelVisible({
          customHandler: onChannelVisible,
          event,
          filters,
          setChannels,
          sort
        });
        break;
      case "channel.truncated":
        defaults.handleChannelTruncated({
          customHandler: onChannelTruncated,
          event,
          setChannels
        });
        break;
      case "channel.updated":
        defaults.handleChannelUpdated({
          customHandler: onChannelUpdated,
          event,
          setChannels
        });
        break;
      case "user.presence.changed":
        defaults.handleUserPresenceChanged({ event, setChannels });
        break;
      case "member.updated":
        defaults.handleMemberUpdated({
          event,
          filters,
          lockChannelOrder,
          setChannels,
          sort
        });
        break;
    }
  };
  const defaultFn = (0, import_react29.useCallback)((e) => {
    defaultHandleChannelListShapeRef.current?.(e);
  }, []);
  const customFn = (0, import_react29.useMemo)(() => {
    if (!customHandleChannelListShape) return null;
    return (e) => {
      customHandleChannelListShapeRef.current?.(e);
    };
  }, [customHandleChannelListShape]);
  return {
    customHandler: customFn,
    defaultHandler: defaultFn
  };
};
var useChannelListShape = (handler) => {
  const { client } = useChatContext();
  (0, import_react29.useEffect)(() => {
    const subscription = client.on("all", handler);
    return subscription.unsubscribe;
  }, [client, handler]);
};
var ChannelListUI = (props) => {
  const { children, error: error2 = null, loading = false } = props;
  const { LoadingErrorIndicator: LoadingErrorIndicator2 = NullComponent, LoadingIndicator: LoadingIndicator2 = LoadingChannels } = useComponentContext();
  const { t: t2 } = useTranslationContext("ChannelListUI");
  if (error2) {
    return (0, import_jsx_runtime4.jsx)(LoadingErrorIndicator2, { error: error2 });
  }
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__channel-list-inner", children: (0, import_jsx_runtime4.jsx)(
    "div",
    {
      "aria-label": t2("aria/Channel list"),
      className: "str-chat__channel-list-inner__main",
      role: "listbox",
      children: loading ? (0, import_jsx_runtime4.jsx)(LoadingIndicator2, {}) : children
    }
  ) });
};
var SearchContext = (0, import_react29.createContext)(void 0);
var SearchContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime4.jsx)(SearchContext.Provider, { value, children });
var useSearchContext = () => {
  const contextValue = (0, import_react29.useContext)(SearchContext);
  return contextValue;
};
var searchControllerStateSelector$3 = (nextValue) => ({
  isActive: nextValue.isActive,
  searchQuery: nextValue.searchQuery
});
var SearchBar = () => {
  const { t: t2 } = useTranslationContext();
  const {
    disabled,
    exitSearchOnInputBlur,
    filterButtonsContainerRef,
    placeholder,
    searchController
  } = useSearchContext();
  const queriesInProgress = useSearchQueriesInProgress(searchController);
  const clearButtonRef = import_react29.default.useRef(null);
  const [input, setInput] = (0, import_react29.useState)(null);
  const { isActive, searchQuery } = useStateStore(
    searchController.state,
    searchControllerStateSelector$3
  );
  (0, import_react29.useEffect)(() => {
    if (!input) return;
    const handleKeyDown = (event) => {
      if (event.key === "Escape") {
        input.blur();
        searchController.exit();
      }
    };
    document.addEventListener("keydown", handleKeyDown);
    return () => {
      document.removeEventListener("keydown", handleKeyDown);
    };
  }, [searchController, input]);
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__search-bar", "data-testid": "search-bar", children: [
    (0, import_jsx_runtime4.jsxs)(
      "div",
      {
        className: clsx_default("str-chat__search-bar__input-wrapper", {
          "str-chat__search-bar__input-wrapper--active": isActive
        }),
        children: [
          (0, import_jsx_runtime4.jsx)(IconSearch, {}),
          (0, import_jsx_runtime4.jsx)(
            "input",
            {
              className: "str-chat__search-bar__input",
              "data-testid": "search-input",
              disabled,
              onBlur: ({ currentTarget, relatedTarget }) => {
                if (exitSearchOnInputBlur && // input is empty
                !currentTarget.value && // clicking on filter buttons or clear button shouldn't trigger exit search on blur
                !filterButtonsContainerRef.current?.contains(relatedTarget) && // clicking clear button shouldn't trigger exit search on blur
                (!clearButtonRef.current || relatedTarget !== clearButtonRef.current)) {
                  searchController.exit();
                }
              },
              onChange: (event) => {
                if (event.target.value) {
                  searchController.search(event.target.value);
                } else if (!event.target.value) {
                  searchController.clear();
                }
              },
              onFocus: searchController.activate,
              placeholder: placeholder ?? t2("Search"),
              ref: setInput,
              type: "text",
              value: searchQuery
            }
          ),
          searchQuery && (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "ghost",
              circular: true,
              className: "str-chat__search-bar__clear-button",
              "data-testid": "clear-input-button",
              disabled: queriesInProgress.length > 0,
              onClick: () => {
                searchController.clear();
                input?.focus();
              },
              ref: clearButtonRef,
              size: "xs",
              variant: "secondary",
              children: (0, import_jsx_runtime4.jsx)(IconXCircle, {})
            }
          )
        ]
      }
    ),
    isActive && (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "ghost",
        className: "str-chat__search-bar__exit-search-button",
        "data-testid": "search-bar-button",
        onClick: () => {
          input?.blur();
          searchController.exit();
        },
        size: "sm",
        variant: "secondary",
        children: t2("Cancel")
      }
    )
  ] });
};
var ChannelSearchResultItem = ({ item }) => {
  const { setActiveChannel } = useChatContext();
  const { setChannels } = useChannelListContext();
  const onSelect = (0, import_react29.useCallback)(() => {
    setActiveChannel(item);
    setChannels?.((channels) => (0, import_lodash4.default)([item, ...channels], "cid"));
  }, [item, setActiveChannel, setChannels]);
  return (0, import_jsx_runtime4.jsx)(
    ChannelListItem,
    {
      channel: item,
      className: "str-chat__search-result",
      onSelect
    }
  );
};
var MessageSearchResultItem = ({
  item
}) => {
  const {
    channel: activeChannel,
    client,
    searchController,
    setActiveChannel
  } = useChatContext();
  const { setChannels } = useChannelListContext();
  const channel = (0, import_react29.useMemo)(() => {
    const { channel: channelData } = item;
    const type = channelData?.type ?? "unknown";
    const id3 = channelData?.id ?? "unknown";
    return client.channel(type, id3);
  }, [client, item]);
  const onSelect = (0, import_react29.useCallback)(async () => {
    if (!channel) return;
    await channel.state.loadMessageIntoState(
      item.id,
      void 0,
      DEFAULT_JUMP_TO_PAGE_SIZE
    );
    searchController._internalState.partialNext({ focusedMessage: item });
    setActiveChannel(channel);
    setChannels?.((channels) => (0, import_lodash4.default)([channel, ...channels], "cid"));
  }, [channel, item, searchController, setActiveChannel, setChannels]);
  const getLatestMessagePreview2 = (0, import_react29.useCallback)(() => item.text, [item]);
  if (!channel) return;
  return (0, import_jsx_runtime4.jsx)(
    ChannelListItem,
    {
      active: channel.cid === activeChannel?.cid && item.id === searchController._internalState.getLatestValue().focusedMessage?.id,
      channel,
      className: "str-chat__search-result",
      getLatestMessagePreview: getLatestMessagePreview2,
      onSelect
    }
  );
};
var UserSearchResultItem = ({ item }) => {
  const { client, setActiveChannel } = useChatContext();
  const { setChannels } = useChannelListContext();
  const { directMessagingChannelType } = useSearchContext();
  const onClick = (0, import_react29.useCallback)(() => {
    const newChannel = client.channel(directMessagingChannelType, {
      members: [client.userID, item.id]
    });
    newChannel.watch();
    setActiveChannel(newChannel);
    setChannels?.((channels) => (0, import_lodash4.default)([newChannel, ...channels], "cid"));
  }, [client, item, setActiveChannel, setChannels, directMessagingChannelType]);
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__search-result-container", children: (0, import_jsx_runtime4.jsxs)(
    "button",
    {
      "aria-label": `Select User Channel: ${item.name || ""}`,
      className: "str-chat__search-result str-chat__search-result--user",
      "data-testid": "search-result-user",
      onClick,
      role: "option",
      children: [
        (0, import_jsx_runtime4.jsx)(
          Avatar,
          {
            imageUrl: item.image,
            isOnline: item.online,
            size: "xl",
            userName: item.name || item.id
          }
        ),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__search-result-data", children: [
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__search-result__display-name", children: item.name || item.username || item.id }),
          (0, import_jsx_runtime4.jsx)(
            Timestamp,
            {
              customClass: "str-chat__search-result__last-active-timestamp",
              timestamp: item.last_active
            }
          )
        ] })
      ]
    }
  ) });
};
var DefaultSearchResultItems = {
  channels: ChannelSearchResultItem,
  messages: MessageSearchResultItem,
  users: UserSearchResultItem
};
var SearchSourceResultsContext = (0, import_react29.createContext)(void 0);
var SearchSourceResultsContextProvider = ({
  children,
  value
}) => (0, import_jsx_runtime4.jsx)(
  SearchSourceResultsContext.Provider,
  {
    value,
    children
  }
);
var useSearchSourceResultsContext = () => {
  const contextValue = (0, import_react29.useContext)(SearchSourceResultsContext);
  return contextValue;
};
var SearchSourceResultsLoadingIndicator = () => {
  const { t: t2 } = useTranslationContext();
  const { searchSource } = useSearchSourceResultsContext();
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__search-source-results__loading-indicator",
      "data-testid": "search-loading-indicator",
      children: t2("Searching for {{ searchSourceType }}...", {
        searchSourceType: searchSource.type
      })
    }
  );
};
var searchSourceStateSelector$3 = (value) => ({
  hasNext: value.hasNext,
  isLoading: value.isLoading
});
var SearchSourceResultListFooter = () => {
  const { t: t2 } = useTranslationContext();
  const {
    SearchSourceResultsLoadingIndicator: SearchSourceResultsLoadingIndicator$1 = SearchSourceResultsLoadingIndicator
  } = useComponentContext();
  const { searchSource } = useSearchSourceResultsContext();
  const { hasNext, isLoading } = useStateStore(
    searchSource.state,
    searchSourceStateSelector$3
  );
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__search-source-result-list__footer",
      "data-testid": "search-footer",
      children: isLoading ? (0, import_jsx_runtime4.jsx)(SearchSourceResultsLoadingIndicator$1, {}) : !hasNext ? (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__search-source-results---empty", children: t2("All results loaded") }) : null
    }
  );
};
var searchSourceStateSelector$2 = (nextValue) => ({
  items: nextValue.items
});
var SearchSourceResultList = ({
  loadMoreDebounceMs = 100,
  loadMoreThresholdPx = 80,
  SearchResultItems = DefaultSearchResultItems
}) => {
  const { SearchSourceResultListFooter: SearchSourceResultListFooter$1 = SearchSourceResultListFooter } = useComponentContext();
  const { searchSource } = useSearchSourceResultsContext();
  const { items } = useStateStore(searchSource.state, searchSourceStateSelector$2);
  const SearchResultItem = SearchResultItems[searchSource.type];
  if (!SearchResultItem) return null;
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__search-source-result-list",
      "data-testid": "search-source-result-list",
      children: (0, import_jsx_runtime4.jsxs)(
        InfiniteScrollPaginator,
        {
          loadNextDebounceMs: loadMoreDebounceMs,
          loadNextOnScrollToBottom: searchSource.search,
          threshold: loadMoreThresholdPx,
          children: [
            items?.map((item, i2) => (0, import_jsx_runtime4.jsx)(
              SearchResultItem,
              {
                item
              },
              `source-search-result-${searchSource.type}-${i2}`
            )),
            (0, import_jsx_runtime4.jsx)(SearchSourceResultListFooter$1, {})
          ]
        }
      )
    }
  );
};
var SearchSourceResultsEmpty = () => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__search-source-results-empty", children: t2("No results found") });
};
var SearchSourceResultsHeader = () => null;
var searchSourceStateSelector$1 = (nextValue) => ({
  isLoading: nextValue.isLoading,
  items: nextValue.items
});
var SearchSourceResults = ({ searchSource }) => {
  const {
    SearchSourceResultList: SearchSourceResultList$1 = SearchSourceResultList,
    SearchSourceResultsEmpty: SearchSourceResultsEmpty$1 = SearchSourceResultsEmpty,
    SearchSourceResultsHeader: SearchSourceResultsHeader$1 = SearchSourceResultsHeader
  } = useComponentContext();
  const { isLoading, items } = useStateStore(
    searchSource.state,
    searchSourceStateSelector$1
  );
  if (!items && !isLoading) return null;
  return (0, import_jsx_runtime4.jsx)(SearchSourceResultsContextProvider, { value: { searchSource }, children: (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__search-source-results",
      "data-testid": "search-source-results",
      children: [
        (0, import_jsx_runtime4.jsx)(SearchSourceResultsHeader$1, {}),
        items?.length || isLoading ? (0, import_jsx_runtime4.jsx)(SearchSourceResultList$1, {}) : (0, import_jsx_runtime4.jsx)(SearchSourceResultsEmpty$1, {})
      ]
    }
  ) });
};
var searchSourceStateSelector = (nextValue) => ({
  isActive: nextValue.isActive
});
var SearchSourceFilterButton = ({ source }) => {
  const { t: t2 } = useTranslationContext();
  const { searchController } = useSearchContext();
  const { isActive } = useStateStore(source.state, searchSourceStateSelector);
  const label = `search-results-header-filter-source-button-label--${source.type}`;
  const knownLabels = (0, import_react29.useMemo)(
    () => ({
      "search-results-header-filter-source-button-label--channels": t2(
        "search-results-header-filter-source-button-label--channels"
      ),
      "search-results-header-filter-source-button-label--messages": t2(
        "search-results-header-filter-source-button-label--messages"
      ),
      "search-results-header-filter-source-button-label--users": t2(
        "search-results-header-filter-source-button-label--users"
      )
    }),
    [t2]
  );
  return (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "outline",
      "aria-label": t2("aria/Search results header filter button"),
      "aria-pressed": isActive,
      className: clsx_default("str-chat__search-results-header__filter-source-button", {
        "str-chat__search-results-header__filter-source-button--active": isActive
      }),
      onClick: () => {
        if (source.isActive) {
          searchController.deactivateSource(source.type);
        } else {
          searchController.activateSource(source.type);
          if (searchController.searchQuery && !source.items?.length)
            source.search(searchController.searchQuery);
        }
      },
      size: "xs",
      variant: "secondary",
      children: knownLabels[label] ?? t2(label)
    },
    label
  );
};
var SearchResultsHeader = () => {
  const { filterButtonsContainerRef, searchController } = useSearchContext();
  if (searchController.sources.length < 2) return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__search-results-header", "data-testid": "search-results-header", children: (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__search-results-header__filter-source-buttons",
      "data-testid": "filter-source-buttons",
      ref: filterButtonsContainerRef,
      children: searchController.sources.map((source) => (0, import_jsx_runtime4.jsx)(SearchSourceFilterButton, { source }, source.type))
    }
  ) });
};
var SearchResultsPresearch = () => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__search-results-presearch", children: t2("Start typing to search") });
};
var searchControllerStateSelector$2 = (nextValue) => ({
  activeSources: nextValue.sources.filter((s2) => s2.isActive),
  isActive: nextValue.isActive,
  searchQuery: nextValue.searchQuery
});
var SearchResults = () => {
  const { t: t2 } = useTranslationContext("ResultsContainer");
  const {
    SearchResultsHeader: SearchResultsHeader$1 = SearchResultsHeader,
    SearchResultsPresearch: SearchResultsPresearch$1 = SearchResultsPresearch,
    SearchSourceResults: SearchSourceResults$1 = SearchSourceResults
  } = useComponentContext();
  const { searchController } = useSearchContext();
  const { activeSources, isActive, searchQuery } = useStateStore(
    searchController.state,
    searchControllerStateSelector$2
  );
  return !isActive ? null : (0, import_jsx_runtime4.jsxs)("div", { "aria-label": t2("aria/Search results"), className: "str-chat__search-results", children: [
    (0, import_jsx_runtime4.jsx)(SearchResultsHeader$1, {}),
    !searchQuery ? (0, import_jsx_runtime4.jsx)(SearchResultsPresearch$1, { activeSources }) : activeSources.map((source) => (0, import_jsx_runtime4.jsx)(SearchSourceResults$1, { searchSource: source }, source.type))
  ] });
};
var searchControllerStateSelector$1 = (nextValue) => ({ isActive: nextValue.isActive });
var Search = ({
  directMessagingChannelType = "messaging",
  disabled,
  exitSearchOnInputBlur = false,
  placeholder
}) => {
  const { SearchBar: SearchBar$1 = SearchBar, SearchResults: SearchResults$1 = SearchResults } = useComponentContext();
  const containerRef = (0, import_react29.useRef)(null);
  const filterButtonsContainerRef = (0, import_react29.useRef)(null);
  const { searchController } = useChatContext();
  const { isActive } = useStateStore(searchController.state, searchControllerStateSelector$1);
  return (0, import_jsx_runtime4.jsx)(
    SearchContextProvider,
    {
      value: {
        containerRef,
        directMessagingChannelType,
        disabled,
        exitSearchOnInputBlur,
        filterButtonsContainerRef,
        placeholder,
        searchController
      },
      children: (0, import_jsx_runtime4.jsxs)(
        "div",
        {
          className: clsx_default("str-chat__search", {
            "str-chat__search--active": isActive
          }),
          "data-testid": "search",
          ref: containerRef,
          children: [
            (0, import_jsx_runtime4.jsx)(SearchBar$1, {}),
            (0, import_jsx_runtime4.jsx)(SearchResults$1, {})
          ]
        }
      )
    }
  );
};
var UnMemoizedLoadMoreButton = ({
  children,
  isLoading,
  onClick
}) => {
  const { t: t2 } = useTranslationContext("UnMemoizedLoadMoreButton");
  const childrenOrDefaultString = children ?? t2("Load more");
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__load-more-button", children: (0, import_jsx_runtime4.jsx)(
    Button,
    {
      appearance: "ghost",
      "aria-label": t2("aria/Load More Channels"),
      "data-testid": "load-more-button",
      disabled: isLoading,
      onClick,
      size: "sm",
      variant: "secondary",
      children: isLoading ? (0, import_jsx_runtime4.jsx)(LoadingIndicator, {}) : childrenOrDefaultString
    }
  ) });
};
var LoadMoreButton = import_react29.default.memo(
  UnMemoizedLoadMoreButton
);
var UnMemoizedLoadMorePaginator = (props) => {
  const {
    children,
    hasNextPage,
    isLoading,
    LoadMoreButton: LoadMoreButton$1 = LoadMoreButton,
    loadNextPage,
    reverse
  } = props;
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    !reverse && children,
    hasNextPage && (0, import_jsx_runtime4.jsx)(LoadMoreButton$1, { isLoading, onClick: loadNextPage }),
    reverse && children
  ] });
};
var LoadMorePaginator = import_react29.default.memo(
  UnMemoizedLoadMorePaginator
);
var ChannelListHeader = () => {
  const { t: t2 } = useTranslationContext();
  const { channel } = useChatContext();
  const { HeaderEndContent } = useComponentContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-list__header", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__channel-list__header__title", children: t2("Chats") }),
    channel && HeaderEndContent && (0, import_jsx_runtime4.jsx)(HeaderEndContent, {})
  ] });
};
var DEFAULT_FILTERS = {};
var DEFAULT_OPTIONS = {};
var DEFAULT_SORT = {};
var searchControllerStateSelector2 = (nextValue) => ({
  searchIsActive: nextValue.isActive
});
var UnMemoizedChannelList = (props) => {
  const {
    allowNewMessagesFromUnfilteredChannels = true,
    channelRenderFilterFn,
    customActiveChannel,
    customQueryChannels,
    EmptyStateIndicator: EmptyStateIndicator$1 = EmptyStateIndicator,
    filters = {},
    getLatestMessagePreview: getLatestMessagePreview2,
    lockChannelOrder = false,
    onAddedToChannel,
    onChannelDeleted,
    onChannelHidden,
    onChannelTruncated,
    onChannelUpdated,
    onChannelVisible,
    onMessageNew,
    onMessageNewHandler,
    onRemovedFromChannel,
    options,
    Paginator = LoadMorePaginator,
    recoveryThrottleIntervalMs,
    renderChannels,
    sendChannelsToList = false,
    setActiveChannelOnMount = true,
    showChannelSearch = false,
    sort = DEFAULT_SORT,
    watchers = {}
  } = props;
  const stableId = useStableId();
  const {
    channel,
    channelsQueryState,
    client,
    customClasses,
    searchController,
    setActiveChannel,
    theme,
    useImageFlagEmojisOnWindows
  } = useChatContext("ChannelList");
  const {
    ChannelListUI: ChannelListUI$1 = ChannelListUI,
    NotificationList: NotificationList$1 = NotificationList,
    Search: Search$12 = Search
  } = useComponentContext();
  const channelListRef = (0, import_react29.useRef)(null);
  const [channelUpdateCount, setChannelUpdateCount] = (0, import_react29.useState)(0);
  const { searchIsActive } = useStateStore(
    searchController.state,
    searchControllerStateSelector2
  );
  const activeChannelHandler = async (channels2, setChannels2) => {
    if (!channels2.length) {
      return;
    }
    if (customActiveChannel) {
      let customActiveChannelObject = channels2.find(
        (chan) => chan.id === customActiveChannel
      );
      if (!customActiveChannelObject) {
        [customActiveChannelObject] = await client.queryChannels({
          id: customActiveChannel
        });
      }
      if (customActiveChannelObject) {
        setActiveChannel(customActiveChannelObject, watchers);
        const newChannels = moveChannelUpwards({
          channels: channels2,
          channelToMove: customActiveChannelObject,
          sort
        });
        setChannels2(newChannels);
        return;
      }
    }
    if (setActiveChannelOnMount) {
      setActiveChannel(channels2[0], watchers);
    }
  };
  const forceUpdate = (0, import_react29.useCallback)(() => setChannelUpdateCount((count2) => count2 + 1), []);
  const { channels, hasNextPage, loadNextPage, setChannels } = usePaginatedChannels(
    client,
    filters || DEFAULT_FILTERS,
    sort || DEFAULT_SORT,
    options || DEFAULT_OPTIONS,
    activeChannelHandler,
    recoveryThrottleIntervalMs,
    customQueryChannels
  );
  const loadedChannels = channelRenderFilterFn ? channelRenderFilterFn(channels) : channels;
  const { customHandler, defaultHandler } = usePrepareShapeHandlers({
    allowNewMessagesFromUnfilteredChannels,
    filters,
    lockChannelOrder,
    onAddedToChannel,
    onChannelDeleted,
    onChannelHidden,
    onChannelTruncated,
    onChannelUpdated,
    onChannelVisible,
    onMessageNew,
    onMessageNewHandler,
    onRemovedFromChannel,
    setChannels,
    sort
    // TODO: implement
    // customHandleChannelListShape
  });
  useChannelListShape(customHandler ?? defaultHandler);
  useConnectionRecoveredListener(forceUpdate);
  (0, import_react29.useEffect)(() => {
    const handleEvent = (event) => {
      if (event.cid === channel?.cid) {
        setActiveChannel();
      }
    };
    client.on("channel.deleted", handleEvent);
    client.on("channel.hidden", handleEvent);
    return () => {
      client.off("channel.deleted", handleEvent);
      client.off("channel.hidden", handleEvent);
    };
  }, [channel?.cid]);
  const renderChannel = (item) => {
    const previewProps = {
      activeChannel: channel,
      channel: item,
      // forces the update of preview component on channel update
      channelUpdateCount,
      getLatestMessagePreview: getLatestMessagePreview2,
      setActiveChannel,
      watchers
    };
    return (0, import_jsx_runtime4.jsx)(ChannelListItem, { ...previewProps }, item.cid);
  };
  const baseClass = "str-chat__channel-list";
  const className = clsx_default(
    customClasses?.chat ?? "str-chat",
    theme,
    customClasses?.channelList ?? `${baseClass}`,
    {
      "str-chat--windows-flags": useImageFlagEmojisOnWindows && navigator.userAgent.match(/Win/)
    }
  );
  const showChannelList = !searchIsActive;
  return (0, import_jsx_runtime4.jsx)(
    ChannelListContextProvider,
    {
      value: { channels, hasNextPage, loadNextPage, setChannels },
      children: (0, import_jsx_runtime4.jsx)("div", { className, ref: channelListRef, children: (0, import_jsx_runtime4.jsxs)(DialogManagerProvider, { id: `channel-list-dialog-manager-${stableId}`, children: [
        (0, import_jsx_runtime4.jsx)(ChannelListHeader, {}),
        showChannelSearch && (0, import_jsx_runtime4.jsx)(Search$12, {}),
        showChannelList && (0, import_jsx_runtime4.jsx)(
          ChannelListUI$1,
          {
            error: channelsQueryState.error,
            loadedChannels: sendChannelsToList ? loadedChannels : void 0,
            loading: !!channelsQueryState.queryInProgress && ["reload", "uninitialized"].includes(channelsQueryState.queryInProgress),
            setChannels,
            children: !loadedChannels?.length ? (0, import_jsx_runtime4.jsx)(EmptyStateIndicator$1, { listType: "channel" }) : (0, import_jsx_runtime4.jsx)(
              Paginator,
              {
                hasNextPage,
                isLoading: channelsQueryState.queryInProgress === "load-more",
                loadNextPage,
                children: renderChannels ? renderChannels(loadedChannels, renderChannel) : loadedChannels.map((channel2) => renderChannel(channel2))
              }
            )
          }
        ),
        (0, import_jsx_runtime4.jsx)(NotificationList$1, { panel: "channel-list" })
      ] }) })
    }
  );
};
var ChannelList = import_react29.default.memo(
  UnMemoizedChannelList
);
var noop11 = () => {
};
function useSelectedChannelState({
  channel,
  selector: selector2,
  stateChangeEventKeys = ["all"]
}) {
  const subscribe2 = (0, import_react29.useCallback)(
    (onStoreChange) => {
      if (!channel) return noop11;
      const subscriptions = stateChangeEventKeys.map(
        (et2) => channel.on(et2, () => {
          onStoreChange(selector2(channel));
        })
      );
      return () => subscriptions.forEach((subscription) => subscription.unsubscribe());
    },
    [channel, selector2, stateChangeEventKeys]
  );
  const getSnapshot = (0, import_react29.useCallback)(() => {
    if (!channel) return void 0;
    return selector2(channel);
  }, [channel, selector2]);
  return (0, import_shim2.useSyncExternalStore)(subscribe2, getSnapshot);
}
var selector$4 = (c2) => c2.state.membership;
var keys$1 = ["member.updated"];
function useChannelMembershipState(channel) {
  return useSelectedChannelState({ channel, selector: selector$4, stateChangeEventKeys: keys$1 });
}
var selector$3 = (c2) => c2.state.members;
var keys2 = [
  "member.updated",
  "member.added",
  "member.removed",
  "user.banned",
  "user.unbanned",
  "user.deleted",
  "user.presence.changed"
];
function useChannelMembersState(channel) {
  return useSelectedChannelState({ channel, selector: selector$3, stateChangeEventKeys: keys2 });
}
var useIsChannelMuted = (channel) => {
  const { client } = useChatContext("useIsChannelMuted");
  const [muted, setMuted] = (0, import_react29.useState)(channel.muteStatus());
  (0, import_react29.useEffect)(() => {
    const handleEvent = () => setMuted(channel.muteStatus());
    client.on("notification.channel_mutes_updated", handleEvent);
    return () => client.off("notification.channel_mutes_updated", handleEvent);
  }, [muted]);
  return muted;
};
var useMuteActionButtonBehavior = () => {
  const { addNotification } = useNotificationApi();
  const { channel } = useChannelListItemContext();
  const { t: t2 } = useTranslationContext();
  const { muted: isMuted } = useIsChannelMuted(channel);
  const [inProgress, setInProgress] = (0, import_react29.useState)(false);
  return {
    "aria-pressed": isMuted,
    disabled: inProgress,
    onClick: async (e) => {
      e.stopPropagation();
      try {
        setInProgress(true);
        if (isMuted) {
          await channel.unmute();
          addNotification({
            context: {
              channel
            },
            emitter: ChannelListItemActionButtons.name,
            message: t2("Channel unmuted"),
            severity: "success",
            type: "api:channel:unmute:success"
          });
        } else {
          await channel.mute();
          addNotification({
            context: {
              channel
            },
            emitter: ChannelListItemActionButtons.name,
            message: t2("Channel muted"),
            severity: "success",
            type: "api:channel:mute:success"
          });
        }
      } catch (error2) {
        addNotification({
          context: {
            channel
          },
          emitter: ChannelListItemActionButtons.name,
          error: error2 instanceof Error ? error2 : new Error("An unknown error occurred"),
          message: t2("Failed to update channel mute status"),
          severity: "error",
          type: "api:channel:mute:failed"
        });
      } finally {
        setInProgress(false);
      }
    },
    title: isMuted ? t2("Unmute") : t2("Mute")
  };
};
var useArchiveActionButtonBehavior = () => {
  const { channel } = useChannelListItemContext();
  const { addNotification } = useNotificationApi();
  const membership = useChannelMembershipState(channel);
  const { t: t2 } = useTranslationContext();
  const [inProgress, setInProgress] = (0, import_react29.useState)(false);
  return {
    "aria-pressed": typeof membership.archived_at === "string",
    disabled: inProgress,
    onClick: async (e) => {
      e.stopPropagation();
      try {
        setInProgress(true);
        if (membership.archived_at) {
          await channel.unarchive();
          addNotification({
            context: {
              channel
            },
            emitter: ChannelListItemActionButtons.name,
            message: t2("Channel unarchived"),
            severity: "success",
            type: "api:channel:unarchive:success"
          });
        } else {
          await channel.archive();
          addNotification({
            context: {
              channel
            },
            emitter: ChannelListItemActionButtons.name,
            message: t2("Channel archived"),
            severity: "success",
            type: "api:channel:archive:success"
          });
        }
      } catch (error2) {
        addNotification({
          context: {
            channel
          },
          emitter: ChannelListItemActionButtons.name,
          error: error2 instanceof Error ? error2 : new Error("An unknown error occurred"),
          message: t2("Failed to update channel archive status"),
          severity: "error",
          type: "api:channel:archive:failed"
        });
      } finally {
        setInProgress(false);
      }
    },
    title: membership.archived_at ? t2("Unarchive") : t2("Archive")
  };
};
var defaultComponents = {
  dropdown: {
    Archive() {
      const behaviorProps = useArchiveActionButtonBehavior();
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": behaviorProps.title,
          Icon: IconArchive,
          ...behaviorProps,
          children: behaviorProps.title
        }
      );
    },
    Ban() {
      const { client } = useChatContext();
      const { addNotification } = useNotificationApi();
      const { t: t2 } = useTranslationContext();
      const { channel } = useChannelListItemContext();
      const [inProgress, setInProgress] = (0, import_react29.useState)(false);
      const members = useChannelMembersState(channel);
      const isUserBanned = Object.values(members || {}).some(
        (member) => member.user?.id !== client.userID && member.banned
      );
      const title = isUserBanned ? t2("Unblock User") : t2("Block User");
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": title,
          disabled: inProgress,
          Icon: IconNoSign,
          onClick: async () => {
            try {
              setInProgress(true);
              const otherUserId = Object.keys(channel.state.members).find(
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                (userId) => userId !== client.userID
              );
              if (!otherUserId) return;
              if (isUserBanned) {
                await channel.unbanUser(otherUserId);
                addNotification({
                  context: {
                    channel
                  },
                  emitter: ChannelListItemActionButtons.name,
                  message: t2("User unblocked"),
                  severity: "success",
                  type: "api:user:unban:success"
                });
              } else {
                await channel.banUser(otherUserId, {});
                addNotification({
                  context: {
                    channel
                  },
                  emitter: ChannelListItemActionButtons.name,
                  message: t2("User blocked"),
                  severity: "success",
                  type: "api:user:ban:success"
                });
              }
            } catch (error2) {
              addNotification({
                context: {
                  channel
                },
                emitter: ChannelListItemActionButtons.name,
                error: error2 instanceof Error ? error2 : new Error("An unknown error occurred"),
                message: t2("Failed to block user"),
                severity: "error",
                type: "api:user:ban:failed"
              });
            } finally {
              setInProgress(false);
            }
          },
          children: title
        }
      );
    },
    Leave() {
      const { t: t2 } = useTranslationContext();
      const { channel } = useChannelListItemContext();
      const { client } = useChatContext();
      const { addNotification } = useNotificationApi();
      const [inProgress, setInProgress] = (0, import_react29.useState)(false);
      const title = t2("Leave Channel");
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": title,
          disabled: inProgress,
          Icon: IconLeave,
          onClick: async (e) => {
            e.stopPropagation();
            try {
              setInProgress(true);
              await channel.removeMembers([client.userID]);
              addNotification({
                context: {
                  channel
                },
                emitter: ChannelListItemActionButtons.name,
                message: t2("Left channel"),
                severity: "success",
                type: "api:channel:leave:success"
              });
            } catch (error2) {
              addNotification({
                context: {
                  channel
                },
                emitter: ChannelListItemActionButtons.name,
                error: error2 instanceof Error ? error2 : new Error("An unknown error occurred"),
                message: t2("Failed to leave channel"),
                severity: "error",
                type: "api:channel:leave:failed"
              });
            } finally {
              setInProgress(false);
            }
          },
          title,
          variant: "destructive",
          children: title
        }
      );
    },
    Pin() {
      const { t: t2 } = useTranslationContext();
      const { addNotification } = useNotificationApi();
      const { channel } = useChannelListItemContext();
      const membership = useChannelMembershipState(channel);
      const dialogId2 = ChannelListItemActionButtons.getDialogId(
        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
        { channelId: channel.id }
      );
      const { dialog } = useDialogOnNearestManager({ id: dialogId2 });
      const [inProgress, setInProgress] = (0, import_react29.useState)(false);
      const title = membership.pinned_at ? t2("Unpin") : t2("Pin");
      return (0, import_jsx_runtime4.jsx)(
        ContextMenuButton,
        {
          "aria-label": title,
          disabled: inProgress,
          Icon: IconPin,
          onClick: async (e) => {
            e.stopPropagation();
            let error2 = null;
            try {
              setInProgress(true);
              if (membership.pinned_at) {
                await channel.unpin();
                addNotification({
                  context: {
                    channel
                  },
                  emitter: ChannelListItemActionButtons.name,
                  message: t2("Channel unpinned"),
                  severity: "success",
                  type: "api:channel:unpin:success"
                });
              } else {
                await channel.pin();
                addNotification({
                  context: {
                    channel
                  },
                  emitter: ChannelListItemActionButtons.name,
                  message: t2("Channel pinned"),
                  severity: "success",
                  type: "api:channel:pin:success"
                });
              }
            } catch (e2) {
              error2 = e2 instanceof Error ? e2 : new Error("An unknown error occurred");
              addNotification({
                context: {
                  channel
                },
                emitter: ChannelListItemActionButtons.name,
                error: error2,
                message: t2("Failed to update channel pinned status"),
                severity: "error",
                type: "api:channel:pin:failed"
              });
            } finally {
              if (!error2) dialog?.close();
              setInProgress(false);
            }
          },
          title,
          children: title
        }
      );
    }
  },
  quick: {
    Mute() {
      const behaviorProps = useMuteActionButtonBehavior();
      return (0, import_jsx_runtime4.jsx)(
        Button,
        {
          appearance: "ghost",
          "aria-label": behaviorProps.title,
          circular: true,
          size: "sm",
          variant: "secondary",
          ...behaviorProps,
          children: (0, import_jsx_runtime4.jsx)(IconMute, {})
        }
      );
    }
  },
  QuickDropdownToggle: (0, import_react29.forwardRef)((_2, ref) => {
    const { channel } = useChannelListItemContext();
    const dialogId2 = ChannelListItemActionButtons.getDialogId({
      // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
      channelId: channel.id
    });
    const { dialog, dialogManager } = useDialogOnNearestManager({ id: dialogId2 });
    const dialogIsOpen = useDialogIsOpen(dialogId2, dialogManager?.id);
    return (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "ghost",
        "aria-expanded": dialogIsOpen,
        "aria-pressed": dialogIsOpen,
        circular: true,
        onClick: (e) => {
          e.stopPropagation();
          dialog.toggle();
        },
        ref,
        size: "sm",
        variant: "secondary",
        children: (0, import_jsx_runtime4.jsx)(IconMore, {})
      }
    );
  })
};
defaultComponents.QuickDropdownToggle.displayName = "QuickDropdownToggle";
var defaultChannelActionSet = [
  {
    Component: defaultComponents.QuickDropdownToggle,
    placement: "quick-dropdown-toggle"
  },
  {
    Component: defaultComponents.quick.Mute,
    placement: "quick",
    type: "mute"
  },
  {
    Component: defaultComponents.dropdown.Archive,
    placement: "dropdown",
    type: "archive"
  },
  {
    Component: defaultComponents.dropdown.Ban,
    placement: "dropdown",
    type: "ban"
  },
  {
    Component: defaultComponents.dropdown.Pin,
    placement: "dropdown",
    type: "pin"
  },
  {
    Component: defaultComponents.dropdown.Leave,
    placement: "dropdown",
    type: "leave"
  }
];
var useBaseChannelActionSetFilter = (channelActionSet) => {
  const { channel } = useChannelListItemContext();
  const membership = useChannelMembershipState(channel);
  const memberCount = channel.data?.member_count ?? 0;
  const connectedUserIsMember = typeof membership.user !== "undefined";
  const ownCapabilities = channel.data?.own_capabilities;
  return (0, import_react29.useMemo)(() => {
    const filtered = channelActionSet.filter((action) => {
      if (action.placement === "quick-dropdown-toggle") return true;
      switch (action.type) {
        case "archive":
          return connectedUserIsMember;
        case "mute":
          return ownCapabilities?.includes("mute-channel");
        case "ban":
          return memberCount > 0 && memberCount <= 2 && ownCapabilities?.includes("ban-channel-members");
        case "leave":
          return ownCapabilities?.includes("leave-channel");
        case "pin":
          return connectedUserIsMember;
        default:
          return true;
      }
    });
    return filtered;
  }, [channelActionSet, memberCount, ownCapabilities, connectedUserIsMember]);
};
var ChannelListItemActionButtons = () => {
  const { ContextMenu: ContextMenuComponent = ContextMenu } = useComponentContext();
  const { channel } = useChannelListItemContext();
  const [referenceElement, setReferenceElement] = import_react29.default.useState(null);
  const dialogId2 = ChannelListItemActionButtons.getDialogId({
    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
    channelId: channel.id
  });
  const { dialog, dialogManager } = useDialogOnNearestManager({ id: dialogId2 });
  const dialogIsOpen = useDialogIsOpen(dialogId2, dialogManager?.id);
  const filteredActionSet = useBaseChannelActionSetFilter(defaultChannelActionSet);
  const { dropdownActionSet, quickActionSet, quickDropdownToggleAction } = useSplitActionSet(filteredActionSet);
  if (quickActionSet.length + dropdownActionSet.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__channel-list-item__action-buttons", {
        "str-chat__channel-list-item__action-buttons--active": dialogIsOpen
      }),
      children: [
        quickDropdownToggleAction && dropdownActionSet.length > 0 && (0, import_jsx_runtime4.jsx)(quickDropdownToggleAction.Component, { ref: setReferenceElement }),
        quickActionSet.map(({ Component: Component2, type }) => (0, import_jsx_runtime4.jsx)(Component2, {}, type)),
        (0, import_jsx_runtime4.jsx)(
          ContextMenuComponent,
          {
            className: "str-chat__channel-list-item__action-buttons-context-menu",
            dialogManagerId: dialogManager?.id,
            id: dialog.id,
            onClose: dialog?.close,
            placement: "bottom-start",
            referenceElement,
            tabIndex: -1,
            trapFocus: true,
            children: dropdownActionSet.map(({ Component: Component2, type }) => (0, import_jsx_runtime4.jsx)(Component2, {}, type))
          }
        )
      ]
    }
  );
};
ChannelListItemActionButtons.getDialogId = ({ channelId }) => `channel-action-buttons-${channelId}`;
ChannelListItemActionButtons.displayName = "ChannelListItemActionButtons";
function ChannelListItemTimestamp({ lastMessage }) {
  const { t: t2, tDateTimeParser } = useTranslationContext("ChannelListItemTimestamp");
  const timestamp = lastMessage?.created_at;
  const normalizedTimestamp = timestamp && isDate(timestamp) ? timestamp.toISOString() : void 0;
  const when = (0, import_react29.useMemo)(
    () => getDateString({
      messageCreatedAt: normalizedTimestamp,
      t: t2,
      tDateTimeParser,
      timestampTranslationKey: "timestamp/ChannelPreviewTimestamp"
    }),
    [normalizedTimestamp, t2, tDateTimeParser]
  );
  if (!when) return null;
  return (0, import_jsx_runtime4.jsx)(
    "time",
    {
      className: "str-chat__channel-list-item-timestamp",
      dateTime: normalizedTimestamp,
      children: when
    }
  );
}
var SUPPORTED_VIDEO_FORMATS = [
  "video/mp4",
  "video/ogg",
  "video/webm",
  "video/quicktime"
];
function getAttachmentContentType(attachment) {
  if (!attachment) return "text";
  if (attachment.type === "giphy") return "giphy";
  if (isScrapedContent(attachment)) return "link";
  if (isImageAttachment(attachment)) return "image";
  if (isVideoAttachment(attachment, SUPPORTED_VIDEO_FORMATS)) return "video";
  if (isVoiceRecordingAttachment(attachment)) return "voice";
  if (isAudioAttachment(attachment) || isFileAttachment(attachment, SUPPORTED_VIDEO_FORMATS)) {
    return "file";
  }
  return "unsupported";
}
function getAttachmentFallbackText(type, count2, t2) {
  switch (type) {
    case "image":
      return t2("imageCount", { count: count2 });
    case "video":
      return t2("videoCount", { count: count2 });
    case "voice":
      return t2("voiceMessageCount", { count: count2 });
    case "link":
      return t2("linkCount", { count: count2 });
    case "file":
      return t2("fileCount", { count: count2 });
    case "unsupported":
      return t2("Unsupported attachment");
    default:
      return t2("fileCount", { count: count2 });
  }
}
var useLatestMessagePreview = ({
  latestMessage,
  messageDeliveryStatus,
  participantCount
}) => {
  const { client } = useChatContext("useLatestMessagePreview");
  const { t: t2, userLanguage } = useTranslationContext("useLatestMessagePreview");
  return (0, import_react29.useMemo)(() => {
    if (!latestMessage) {
      return { text: t2("Nothing yet..."), type: "empty" };
    }
    if (latestMessage.status === "failed" || latestMessage.type === "error") {
      return { text: t2("Message failed to send"), type: "error" };
    }
    const isOwnMessage = latestMessage.user?.id === client.user?.id;
    let deliveryStatus;
    if (isOwnMessage) {
      deliveryStatus = messageDeliveryStatus ?? latestMessage.status;
    }
    let senderName;
    if (isOwnMessage) {
      senderName = t2("You");
    } else if (!isOwnMessage && participantCount !== void 0 && participantCount > 2) {
      senderName = latestMessage.user?.name || latestMessage.user?.id;
    }
    if (isMessageDeleted(latestMessage)) {
      return {
        deliveryStatus,
        senderName,
        text: t2("Message deleted"),
        type: "deleted"
      };
    }
    if (latestMessage.poll) {
      return {
        deliveryStatus,
        senderName,
        text: t2("Poll"),
        type: "poll"
      };
    }
    const textContent = getTranslatedMessageText({ language: userLanguage, message: latestMessage }) || latestMessage.text;
    if (latestMessage.shared_location) {
      return {
        deliveryStatus,
        senderName,
        text: textContent || t2("Location"),
        type: "location"
      };
    }
    if (latestMessage.attachments && latestMessage.attachments.length) {
      const attachments = latestMessage.attachments;
      let contentType;
      const [firstAttachment] = attachments;
      const firstAttachmentType = getAttachmentContentType(firstAttachment);
      if (attachments.every(
        (attachment) => getAttachmentContentType(attachment) === firstAttachmentType
      )) {
        contentType = firstAttachmentType;
      } else {
        contentType = "file";
      }
      let text22 = contentType === "giphy" ? `GIPHY ${firstAttachment.title ?? ""}`.trim() : (
        // prioritize message text content if available
        textContent || // then fallback text of the single attachment if only one attachment is present and it's not a voice recording (fallback text is generic for voice recordings, so not useful in the preview)
        (attachments.length === 1 && contentType !== "voice" && contentType !== "unsupported" ? firstAttachment.fallback || firstAttachment.title : "") || // then generic fallback text based on attachment type and count
        getAttachmentFallbackText(contentType, attachments.length, t2)
      );
      if (attachments.length === 1 && typeof firstAttachment.duration === "number") {
        const minutes = Math.floor(firstAttachment.duration / 60);
        const seconds = Math.ceil(firstAttachment.duration) % 60;
        const durationString = `${minutes}:${seconds.toString().padStart(2, "0")}`;
        text22 += ` (${durationString})`;
      }
      return {
        deliveryStatus,
        senderName,
        text: text22,
        type: contentType
      };
    }
    if (textContent) {
      return {
        deliveryStatus,
        senderName,
        text: textContent,
        type: "text"
      };
    }
    return { text: t2("Empty message..."), type: "empty" };
  }, [
    client.user?.id,
    latestMessage,
    messageDeliveryStatus,
    participantCount,
    t2,
    userLanguage
  ]);
};
var deliveryStatusIconMap = {
  delivered: IconChecks,
  read: IconChecks,
  sending: IconClock,
  sent: IconCheckmark1Small
};
var contentTypeIconMap = {
  deleted: IconNoSign,
  error: IconExclamationCircleFill,
  file: IconFile,
  giphy: IconGiphy,
  image: IconCamera,
  link: IconLink,
  location: IconLocation,
  unsupported: IconUnsupportedAttachment,
  video: IconVideo,
  voice: IconVoice
};
var SummarizedMessagePreview = ({
  latestMessage,
  messageDeliveryStatus,
  participantCount
}) => {
  const { deliveryStatus, senderName, text: text22, type } = useLatestMessagePreview({
    latestMessage,
    messageDeliveryStatus,
    participantCount
  });
  const DeliveryStatusIcon = deliveryStatus ? deliveryStatusIconMap[deliveryStatus] : void 0;
  const ContentTypeIcon = contentTypeIconMap[type];
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: clsx_default("str-chat__summarized-message-preview", {
        [`str-chat__summarized-message-preview--${type}`]: type
      }),
      children: [
        type !== "error" && (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          DeliveryStatusIcon && (0, import_jsx_runtime4.jsx)(
            "span",
            {
              className: clsx_default("str-chat__summarized-message-preview__delivery-status", {
                [`str-chat__summarized-message-preview__delivery-status--${deliveryStatus}`]: deliveryStatus
              }),
              children: (0, import_jsx_runtime4.jsx)(DeliveryStatusIcon, {})
            }
          ),
          senderName && (0, import_jsx_runtime4.jsxs)("span", { className: "str-chat__summarized-message-preview__sender", children: [
            senderName,
            ":"
          ] })
        ] }),
        ContentTypeIcon && (0, import_jsx_runtime4.jsx)(ContentTypeIcon, {}),
        (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__summarized-message-preview__text", children: text22 })
      ]
    }
  );
};
var UnMemoizedChannelListItemUI = (props) => {
  const {
    active,
    channel,
    className: customClassName = "",
    displayImage,
    displayTitle,
    groupChannelDisplayInfo,
    lastMessage,
    messageDeliveryStatus,
    muted,
    onSelect: customOnSelectChannel,
    setActiveChannel,
    unread,
    watchers
  } = props;
  const {
    Avatar: Avatar2 = ChannelAvatar,
    ChannelListItemActionButtons: ChannelListItemActionButtons$1 = ChannelListItemActionButtons
  } = useComponentContext();
  const channelPreviewButton = (0, import_react29.useRef)(null);
  const avatarName = displayTitle || channel.state.messages[channel.state.messages.length - 1]?.user?.id;
  const onSelectChannel = (e) => {
    if (customOnSelectChannel) {
      customOnSelectChannel(e);
    } else if (setActiveChannel) {
      setActiveChannel(channel, watchers);
    }
    if (channelPreviewButton?.current) {
      channelPreviewButton.current.blur();
    }
  };
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-list-item-container", children: [
    (0, import_jsx_runtime4.jsx)(ChannelListItemActionButtons$1, {}),
    (0, import_jsx_runtime4.jsxs)(
      "button",
      {
        "aria-label": `Select Channel: ${displayTitle || ""}`,
        "aria-pressed": active,
        className: clsx_default(
          "str-chat__channel-list-item",
          typeof unread === "number" && unread > 0 && "str-chat__channel-list-item--unread",
          muted && "str-chat__channel-list-item--muted",
          customClassName
        ),
        "data-testid": "channel-list-item-button",
        onClick: onSelectChannel,
        ref: channelPreviewButton,
        role: "option",
        children: [
          (0, import_jsx_runtime4.jsx)(
            Avatar2,
            {
              displayMembers: groupChannelDisplayInfo?.members,
              imageUrl: displayImage,
              size: "xl",
              userName: avatarName
            }
          ),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-list-item-data", children: [
            (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-list-item-data__first-row", children: [
              (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-list-item-data__title", children: [
                (0, import_jsx_runtime4.jsx)("span", { children: displayTitle || "N/A" }),
                muted && (0, import_jsx_runtime4.jsx)(IconMute, {})
              ] }),
              (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-list-item-data__timestamp-and-badge", children: [
                (0, import_jsx_runtime4.jsx)(ChannelListItemTimestamp, { lastMessage }),
                typeof unread === "number" && unread > 0 && (0, import_jsx_runtime4.jsx)(Badge, { "data-testid": "unread-badge", size: "md", variant: "primary", children: unread })
              ] })
            ] }),
            (0, import_jsx_runtime4.jsx)(
              SummarizedMessagePreview,
              {
                latestMessage: lastMessage,
                messageDeliveryStatus,
                participantCount: channel.data?.member_count
              }
            )
          ] })
        ]
      }
    )
  ] });
};
var ChannelListItemUI = import_react29.default.memo(
  UnMemoizedChannelListItemUI
);
var MessageDeliveryStatus = ((MessageDeliveryStatus2) => {
  MessageDeliveryStatus2["SENT"] = "sent";
  MessageDeliveryStatus2["DELIVERED"] = "delivered";
  MessageDeliveryStatus2["READ"] = "read";
  return MessageDeliveryStatus2;
})(MessageDeliveryStatus || {});
var useMessageDeliveryStatus = ({
  channel,
  lastMessage
}) => {
  const { client } = useChatContext();
  const [messageDeliveryStatus, setMessageDeliveryStatus] = (0, import_react29.useState)();
  const isOwnMessage = (0, import_react29.useCallback)(
    (message) => client.user && message && message.user?.id === client.user.id,
    [client]
  );
  (0, import_react29.useEffect)(() => {
    if (!lastMessage) {
      setMessageDeliveryStatus(void 0);
    }
    const lastMessageIsOwn = isOwnMessage(lastMessage);
    if (!lastMessage?.created_at || !lastMessageIsOwn) return;
    const msgRef = {
      msgId: lastMessage.id,
      timestampMs: lastMessage.created_at.getTime()
    };
    const readersForMessage = channel.messageReceiptsTracker.readersForMessage(msgRef);
    const deliveredForMessage = channel.messageReceiptsTracker.deliveredForMessage(msgRef);
    setMessageDeliveryStatus(
      readersForMessage.length > 1 || readersForMessage.length === 1 && readersForMessage[0].id !== client.user?.id ? "read" : deliveredForMessage.length > 1 || deliveredForMessage.length === 1 && deliveredForMessage[0].id !== client.user?.id ? "delivered" : "sent"
      /* SENT */
    );
  }, [channel, client, isOwnMessage, lastMessage]);
  (0, import_react29.useEffect)(() => {
    const handleMessageNew = (event) => {
      if (!isOwnMessage(event.message)) {
        return setMessageDeliveryStatus(void 0);
      }
      return setMessageDeliveryStatus(
        "sent"
        /* SENT */
      );
    };
    channel.on("message.new", handleMessageNew);
    return () => {
      channel.off("message.new", handleMessageNew);
    };
  }, [channel, isOwnMessage]);
  (0, import_react29.useEffect)(() => {
    if (!isOwnMessage(lastMessage)) return;
    const handleMessageDelivered = (event) => {
      if (event.user?.id !== client.user?.id && lastMessage && lastMessage.id === event.last_delivered_message_id)
        setMessageDeliveryStatus(
          "delivered"
          /* DELIVERED */
        );
    };
    const handleMarkRead = (event) => {
      if (event.user?.id !== client.user?.id)
        setMessageDeliveryStatus(
          "read"
          /* READ */
        );
    };
    channel.on("message.delivered", handleMessageDelivered);
    channel.on("message.read", handleMarkRead);
    return () => {
      channel.off("message.delivered", handleMessageDelivered);
      channel.off("message.read", handleMarkRead);
    };
  }, [channel, client, isOwnMessage, lastMessage]);
  return {
    messageDeliveryStatus
  };
};
var ChannelListItemContext = import_react29.default.createContext({
  channel: null
});
var useChannelListItemContext = () => (0, import_react29.useContext)(ChannelListItemContext);
var ChannelListItem = (props) => {
  const {
    active,
    channel,
    channelUpdateCount,
    getLatestMessagePreview: getLatestMessagePreview$1 = getLatestMessagePreview
  } = props;
  const { ChannelListItemUI: ChannelListItemUI$1 = ChannelListItemUI } = useComponentContext();
  const {
    channel: activeChannel,
    client,
    isMessageAIGenerated,
    setActiveChannel
  } = useChatContext("ChannelPreview");
  const { t: t2, userLanguage } = useTranslationContext("ChannelPreview");
  const { displayImage, displayTitle, groupChannelDisplayInfo } = useChannelPreviewInfo({
    channel
  });
  const [lastMessage, setLastMessage] = (0, import_react29.useState)(
    channel.state.messages[channel.state.messages.length - 1]
  );
  const [latestMessagePreview, setLatestMessagePreview] = (0, import_react29.useState)(
    () => getLatestMessagePreview$1(channel, t2, userLanguage, isMessageAIGenerated)
  );
  const [unread, setUnread] = (0, import_react29.useState)(0);
  const { messageDeliveryStatus } = useMessageDeliveryStatus({
    channel,
    lastMessage
  });
  const isActive = typeof active === "undefined" ? activeChannel?.cid === channel.cid : active;
  const { muted } = useIsChannelMuted(channel);
  (0, import_react29.useEffect)(() => {
    const handleEvent = (event) => {
      if (!event.cid) return setUnread(0);
      if (channel.cid === event.cid) setUnread(0);
    };
    client.on("notification.mark_read", handleEvent);
    return () => client.off("notification.mark_read", handleEvent);
  }, [channel, client]);
  (0, import_react29.useEffect)(() => {
    const handleEvent = (event) => {
      if (channel.cid !== event.cid) return;
      if (event.user?.id !== client.user?.id) return;
      setUnread(channel.countUnread());
    };
    channel.on("notification.mark_unread", handleEvent);
    return () => {
      channel.off("notification.mark_unread", handleEvent);
    };
  }, [channel, client]);
  const refreshUnreadCount = (0, import_react29.useMemo)(
    () => (0, import_lodash3.default)(() => {
      if (muted) {
        setUnread(0);
      } else {
        setUnread(channel.countUnread());
      }
    }, 400),
    [channel, muted]
  );
  (0, import_react29.useEffect)(() => {
    refreshUnreadCount();
    setLatestMessagePreview(
      getLatestMessagePreview$1(channel, t2, userLanguage, isMessageAIGenerated)
    );
    const handleEvent = (event) => {
      const deletedMessagesInAnotherChannel = event.type === "user.messages.deleted" && event.cid && event.cid !== channel.cid;
      if (deletedMessagesInAnotherChannel) return;
      setLastMessage(
        channel.state.latestMessages[channel.state.latestMessages.length - 1]
      );
      setLatestMessagePreview(
        getLatestMessagePreview$1(channel, t2, userLanguage, isMessageAIGenerated)
      );
      refreshUnreadCount();
    };
    channel.on("message.new", handleEvent);
    channel.on("message.updated", handleEvent);
    channel.on("message.deleted", handleEvent);
    client.on("user.messages.deleted", handleEvent);
    channel.on("message.undeleted", handleEvent);
    channel.on("channel.truncated", handleEvent);
    return () => {
      channel.off("message.new", handleEvent);
      channel.off("message.updated", handleEvent);
      channel.off("message.deleted", handleEvent);
      client.off("user.messages.deleted", handleEvent);
      channel.off("message.undeleted", handleEvent);
      channel.off("channel.truncated", handleEvent);
    };
  }, [
    channel,
    client,
    refreshUnreadCount,
    channelUpdateCount,
    getLatestMessagePreview$1,
    t2,
    userLanguage,
    isMessageAIGenerated
  ]);
  const channelPreviewContextValue = (0, import_react29.useMemo)(() => ({ channel }), [channel]);
  if (!ChannelListItemUI$1) return null;
  return (0, import_jsx_runtime4.jsx)(ChannelListItemContext.Provider, { value: channelPreviewContextValue, children: (0, import_jsx_runtime4.jsx)(
    ChannelListItemUI$1,
    {
      ...props,
      active: isActive,
      displayImage,
      displayTitle,
      groupChannelDisplayInfo,
      lastMessage,
      latestMessagePreview,
      messageDeliveryStatus,
      muted,
      setActiveChannel,
      unread
    }
  ) });
};
var ThreadListItemUI = ({
  resetHighlighting,
  ...props
}) => {
  const { client } = useChatContext();
  const thread = useThreadListItemContext();
  const selector2 = (0, import_react29.useCallback)(
    (nextValue) => ({
      channel: nextValue.channel,
      deletedAt: nextValue.deletedAt,
      latestReply: nextValue.replies.at(-1),
      ownUnreadMessageCount: client.userID && nextValue.read[client.userID]?.unreadMessageCount || 0,
      parentMessage: nextValue.parentMessage,
      participants: nextValue.participants,
      replyCount: nextValue.replyCount
    }),
    [client]
  );
  const {
    channel,
    deletedAt,
    latestReply,
    ownUnreadMessageCount,
    parentMessage,
    participants,
    replyCount
  } = useStateStore(thread.state, selector2);
  const { displayTitle: channelDisplayTitle } = useChannelPreviewInfo({ channel });
  const { t: t2 } = useTranslationContext("ThreadListItemUI");
  const { activeThread, setActiveThread } = useThreadsViewContext();
  const avatarProps = deletedAt ? void 0 : {
    imageUrl: latestReply?.user?.image,
    userName: latestReply?.user?.name || latestReply?.user?.id
  };
  const displayInfo = (0, import_react29.useMemo)(() => {
    if (!participants) return [];
    return participants.slice(0, 3).map((participant) => ({
      id: participant.user?.id ?? void 0,
      imageUrl: participant.user?.image,
      // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
      userName: participant.user?.name || participant.user.id
    }));
  }, [participants]);
  (0, import_react29.useEffect)(() => {
    if (!resetHighlighting) return;
    const reset2 = resetHighlighting;
    const timeout = setTimeout(() => {
      reset2();
    }, 2e3);
    return () => clearTimeout(timeout);
  }, [resetHighlighting]);
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-list-item-container", children: (0, import_jsx_runtime4.jsxs)(
    "button",
    {
      "aria-pressed": activeThread === thread,
      className: clsx_default("str-chat__thread-list-item", {
        "str-chat__thread-list-item--highlighted": typeof resetHighlighting !== "undefined"
      }),
      "data-thread-id": thread.id,
      onClick: () => setActiveThread(thread),
      role: "option",
      ...props,
      children: [
        (0, import_jsx_runtime4.jsx)(Avatar, { size: "xl", ...avatarProps }),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list-item__content", children: [
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list-item__content-leading", children: [
            (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__thread-list-item__title", children: channelDisplayTitle }),
            (0, import_jsx_runtime4.jsx)(
              SummarizedMessagePreview,
              {
                latestMessage: parentMessage,
                participantCount: participants?.length
              }
            )
          ] }),
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list-item__content-trailing", children: [
            (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list-item__reply-information", children: [
              (0, import_jsx_runtime4.jsx)(AvatarStack, { displayInfo, size: "sm" }),
              (0, import_jsx_runtime4.jsx)("span", { className: "str-chat__thread-list-item__reply-count", children: t2("replyCount", { count: replyCount }) })
            ] }),
            (0, import_jsx_runtime4.jsx)(
              Timestamp,
              {
                customClass: "str-chat__thread-list-item__timestamp",
                timestamp: latestReply?.created_at
              }
            )
          ] })
        ] }),
        ownUnreadMessageCount > 0 && (0, import_jsx_runtime4.jsx)(Badge, { size: "md", variant: "primary", children: ownUnreadMessageCount })
      ]
    }
  ) });
};
var ThreadListItemContext = (0, import_react29.createContext)(void 0);
var useThreadListItemContext = () => (0, import_react29.useContext)(ThreadListItemContext);
var ThreadListItem = ({
  thread,
  threadListItemUIProps
}) => {
  const { ThreadListItemUI: ThreadListItemUI$1 = ThreadListItemUI } = useComponentContext();
  return (0, import_jsx_runtime4.jsx)(ThreadListItemContext.Provider, { value: thread, children: (0, import_jsx_runtime4.jsx)(ThreadListItemUI$1, { ...threadListItemUIProps }) });
};
var ThreadListEmptyPlaceholder = () => {
  const { t: t2 } = useTranslationContext("ThreadListEmptyPlaceholder");
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list-empty-placeholder", children: [
    (0, import_jsx_runtime4.jsx)(IconMessageBubbles, {}),
    (0, import_jsx_runtime4.jsx)("p", { children: t2("Reply to a message to start a thread") })
  ] });
};
var selector$2 = (nextValue) => ({
  isLoading: nextValue.pagination.isLoading,
  unseenThreadIds: nextValue.unseenThreadIds
});
var ThreadListUnseenThreadsBanner = () => {
  const { client } = useChatContext();
  const { t: t2 } = useTranslationContext();
  const { isLoading, unseenThreadIds } = useStateStore(client.threads.state, selector$2);
  if (!unseenThreadIds.length) return null;
  return (0, import_jsx_runtime4.jsxs)(
    "button",
    {
      className: clsx_default("str-chat__unseen-threads-banner", {
        "str-chat__unseen-threads-banner--loading": isLoading
      }),
      disabled: isLoading,
      onClick: () => client.threads.reload(),
      children: [
        !isLoading && (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          (0, import_jsx_runtime4.jsx)(IconRefresh, {}),
          (0, import_jsx_runtime4.jsx)("span", { children: t2("ThreadListUnseenThreadsBanner/unreadThreads", {
            count: unseenThreadIds.length
          }) })
        ] }),
        isLoading && (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
          (0, import_jsx_runtime4.jsx)(LoadingIndicator, {}),
          (0, import_jsx_runtime4.jsx)("span", { children: t2("ThreadListUnseenThreadsBanner/loading") })
        ] })
      ]
    }
  );
};
var selector$1 = (nextValue) => ({
  isLoadingNext: nextValue.pagination.isLoadingNext
});
var ThreadListLoadingIndicator = () => {
  const { LoadingIndicator: LoadingIndicator$1 = LoadingIndicator } = useComponentContext();
  const { client } = useChatContext();
  const { isLoadingNext } = useStateStore(client.threads.state, selector$1);
  if (!isLoadingNext) return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-list-loading-indicator", children: (0, import_jsx_runtime4.jsx)(LoadingIndicator$1, {}) });
};
var ThreadListHeader = () => {
  const { t: t2 } = useTranslationContext();
  const { HeaderEndContent } = useComponentContext();
  const { activeThread } = useThreadsViewContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list__header", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-list__header__title", children: t2("Threads") }),
    activeThread && HeaderEndContent && (0, import_jsx_runtime4.jsx)(HeaderEndContent, {})
  ] });
};
var selector = (nextValue) => ({
  isLoading: nextValue.pagination.isLoading,
  threads: nextValue.threads
});
var computeItemKey = (_2, item) => item.id;
var useThreadList = () => {
  const { client } = useChatContext();
  (0, import_react29.useEffect)(() => {
    const handleVisibilityChange = () => {
      if (document.visibilityState === "visible") {
        client.threads.activate();
      }
      if (document.visibilityState === "hidden") {
        client.threads.deactivate();
      }
    };
    handleVisibilityChange();
    document.addEventListener("visibilitychange", handleVisibilityChange);
    return () => {
      client.threads.deactivate();
      document.removeEventListener("visibilitychange", handleVisibilityChange);
    };
  }, [client]);
};
var useThreadHighlighting = (threadManager) => {
  const [threadsToHighlight, setThreadsToHighlight] = (0, import_react29.useState)({});
  (0, import_react29.useEffect)(() => {
    const unsubscribe = threadManager.state.subscribeWithSelector(
      (state) => state.threads,
      (nextThreads, previousThreads) => {
        if (!previousThreads) return;
        const resetByThreadId = {};
        for (const thread of nextThreads) {
          if (previousThreads.includes(thread)) continue;
          resetByThreadId[thread.id] = () => {
            setThreadsToHighlight((pv) => {
              const copy2 = { ...pv };
              delete copy2[thread.id];
              return copy2;
            });
          };
        }
        setThreadsToHighlight(resetByThreadId);
      }
    );
    return unsubscribe;
  });
  return threadsToHighlight;
};
var ThreadList = ({ virtuosoProps }) => {
  const { client } = useChatContext();
  const {
    NotificationList: NotificationListFromContext = NotificationList,
    ThreadListEmptyPlaceholder: ThreadListEmptyPlaceholder$1 = ThreadListEmptyPlaceholder,
    ThreadListItem: ThreadListItem$1 = ThreadListItem,
    ThreadListLoadingIndicator: ThreadListLoadingIndicator$1 = ThreadListLoadingIndicator,
    ThreadListUnseenThreadsBanner: ThreadListUnseenThreadsBanner$1 = ThreadListUnseenThreadsBanner
  } = useComponentContext();
  const { isLoading, threads } = useStateStore(client.threads.state, selector);
  const resetByThreadId = useThreadHighlighting(client.threads);
  useThreadList();
  if (isLoading && !threads.length) {
    return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list-container", children: [
      (0, import_jsx_runtime4.jsx)(ThreadListHeader, {}),
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__thread-list str-chat__thread-list--loading", children: (0, import_jsx_runtime4.jsx)(LoadingChannels, {}) })
    ] });
  }
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__thread-list-container", children: [
    (0, import_jsx_runtime4.jsx)(ThreadListHeader, {}),
    (0, import_jsx_runtime4.jsx)(ThreadListUnseenThreadsBanner$1, {}),
    (0, import_jsx_runtime4.jsx)(
      ln,
      {
        atBottomStateChange: (atBottom) => atBottom && client.threads.loadNextPage(),
        className: "str-chat__thread-list",
        components: {
          EmptyPlaceholder: ThreadListEmptyPlaceholder$1,
          Footer: ThreadListLoadingIndicator$1
        },
        computeItemKey,
        data: threads,
        itemContent: (_2, thread) => (0, import_jsx_runtime4.jsx)(
          ThreadListItem$1,
          {
            thread,
            threadListItemUIProps: {
              resetHighlighting: resetByThreadId[thread.id]
            }
          }
        ),
        ...virtuosoProps
      }
    ),
    (0, import_jsx_runtime4.jsx)(NotificationListFromContext, { panel: "thread-list" })
  ] });
};
var useSystemNotifications = (options) => {
  const { client } = useChatContext();
  const selector2 = (0, import_react29.useCallback)(
    (state) => {
      const withSystemTag = state.notifications.filter(hasSystemNotificationTag);
      const notifications2 = options?.filter ? withSystemTag.filter(options.filter) : withSystemTag;
      return { notifications: notifications2 };
    },
    [options?.filter]
  );
  const { notifications } = useStateStore(client.notifications.store, selector2);
  return notifications;
};
var IconsBySeverity = {
  error: IconExclamationMark,
  info: null,
  loading: IconRefresh,
  success: IconCheckmark,
  warning: IconExclamationTriangleFill
};
var DefaultNotificationIcon = ({ notification }) => {
  if (!notification.severity) return null;
  const Icon = IconsBySeverity[notification.severity] ?? null;
  return Icon && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__notification-icon", children: (0, import_jsx_runtime4.jsx)(Icon, {}) });
};
var Notification = (0, import_react29.forwardRef)(
  ({
    className,
    entryDirection,
    Icon = DefaultNotificationIcon,
    notification,
    onDismiss,
    showClose = false,
    transitionState
  }, ref) => {
    const { removeNotification } = useNotificationApi();
    const { t: t2 } = useTranslationContext();
    const displayMessage = t2("translationBuilderTopic/notification", {
      notification,
      value: notification.message
    });
    const handleDismiss = () => {
      if (onDismiss) {
        onDismiss();
        return;
      }
      removeNotification(notification.id);
    };
    const isPersistent = !notification.duration;
    const severity = notification.severity;
    return (0, import_jsx_runtime4.jsxs)(
      "div",
      {
        "aria-live": "polite",
        className: clsx_default(
          "str-chat__notification",
          entryDirection && `str-chat__notification--enter-from-${entryDirection}`,
          transitionState === "enter" && "str-chat__notification--is-entering",
          transitionState === "exit" && "str-chat__notification--is-exiting",
          severity && `str-chat__notification--${severity}`,
          severity === "loading" && "str-chat__notification--loading",
          className
        ),
        "data-testid": "notification",
        ref,
        children: [
          (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__notification-content", children: [
            Icon && (0, import_jsx_runtime4.jsx)(Icon, { notification }),
            (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__notification-message", children: displayMessage })
          ] }),
          notification.actions && notification.actions.length > 0 && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__notification-actions", children: notification.actions.map((action, index6) => (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "outline",
              className: "str-chat__notification-action",
              inverseTheme: true,
              onClick: () => {
                action.handler();
              },
              size: "sm",
              variant: "secondary",
              children: action.label
            },
            index6
          )) }),
          (showClose || isPersistent) && (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "ghost",
              "aria-label": "Dismiss",
              circular: true,
              className: "str-chat__notification-close-button",
              inverseTheme: true,
              onClick: handleDismiss,
              size: "sm",
              variant: "secondary",
              children: (0, import_jsx_runtime4.jsx)(IconXmark, {})
            }
          )
        ]
      }
    );
  }
);
Notification.displayName = "Notification";
var ENTER_TRANSLATION = {
  bottom: { x: "0%", y: "100%" },
  left: { x: "-100%", y: "0%" },
  right: { x: "100%", y: "0%" },
  top: { x: "0%", y: "-100%" }
};
var EXIT_ANIMATION_MS = 340;
var isEnterFrom = (value) => value === "bottom" || value === "left" || value === "right" || value === "top";
var getNotificationEnterFrom = (notification, fallbackEnterFrom) => {
  if (!notification) return fallbackEnterFrom;
  const metadataEnterFrom = notification.metadata?.entryDirection;
  if (isEnterFrom(metadataEnterFrom)) return metadataEnterFrom;
  const originEnterFrom = notification.origin.context?.entryDirection;
  if (isEnterFrom(originEnterFrom)) return originEnterFrom;
  return fallbackEnterFrom;
};
var NotificationList = ({
  className,
  enterFrom = "bottom",
  fallbackPanel,
  filter: filter2,
  panel,
  verticalAlignment = "bottom"
}) => {
  const { Notification: NotificationComponent = Notification } = useComponentContext();
  const { removeNotification, startNotificationTimeout } = useNotificationApi();
  const exitTimeoutRef = (0, import_react29.useRef)(null);
  const latestNotificationRef = (0, import_react29.useRef)(null);
  const listRef = (0, import_react29.useRef)(null);
  const observedElementRef = (0, import_react29.useRef)(null);
  const startedTimeoutIdsRef = (0, import_react29.useRef)(null);
  if (!startedTimeoutIdsRef.current) {
    startedTimeoutIdsRef.current = /* @__PURE__ */ new Set();
  }
  const [displayedNotification, setDisplayedNotification] = (0, import_react29.useState)(
    null
  );
  const [transitionState, setTransitionState] = (0, import_react29.useState)("enter");
  const combinedFilter = (0, import_react29.useCallback)(
    (notification2) => {
      if (hasSystemNotificationTag(notification2)) return false;
      return filter2 ? filter2(notification2) : true;
    },
    [filter2]
  );
  const notifications = useNotifications({
    fallbackPanel,
    filter: combinedFilter,
    panel
  });
  const nextNotification = notifications[0] ?? null;
  const dismiss = (0, import_react29.useCallback)(
    (id3) => {
      startedTimeoutIdsRef.current?.delete(id3);
      removeNotification(id3);
    },
    [removeNotification]
  );
  (0, import_react29.useEffect)(() => {
    const notificationIds = new Set(notifications.map(({ id: id3 }) => id3));
    startedTimeoutIdsRef.current?.forEach((id3) => {
      if (!notificationIds.has(id3)) {
        startedTimeoutIdsRef.current?.delete(id3);
      }
    });
  }, [notifications]);
  (0, import_react29.useEffect)(() => {
    latestNotificationRef.current = nextNotification;
  }, [nextNotification]);
  (0, import_react29.useEffect)(
    () => () => {
      if (exitTimeoutRef.current) {
        window.clearTimeout(exitTimeoutRef.current);
      }
    },
    []
  );
  (0, import_react29.useEffect)(() => {
    if (!displayedNotification) {
      if (!nextNotification) return;
      setDisplayedNotification(nextNotification);
      setTransitionState("enter");
      return;
    }
    if (displayedNotification.id === nextNotification?.id) return;
    if (transitionState === "exit") return;
    setTransitionState("exit");
    exitTimeoutRef.current = window.setTimeout(() => {
      setDisplayedNotification(latestNotificationRef.current);
      setTransitionState("enter");
      exitTimeoutRef.current = null;
    }, EXIT_ANIMATION_MS);
  }, [displayedNotification, nextNotification, transitionState]);
  const notification = displayedNotification;
  const notificationEnterFrom = getNotificationEnterFrom(notification, enterFrom);
  (0, import_react29.useEffect)(() => {
    const element4 = observedElementRef.current;
    if (!element4 || !notification || transitionState === "exit") return;
    const startTimeout = () => {
      if (!startedTimeoutIdsRef.current || startedTimeoutIdsRef.current.has(notification.id))
        return;
      startedTimeoutIdsRef.current.add(notification.id);
      startNotificationTimeout(notification.id);
    };
    if (typeof IntersectionObserver === "undefined") {
      startTimeout();
      return;
    }
    const observer = new IntersectionObserver(
      (entries) => {
        const [entry] = entries;
        if (!entry?.isIntersecting) return;
        startTimeout();
        observer.disconnect();
      },
      {
        root: listRef.current,
        threshold: 0.5
      }
    );
    observer.observe(element4);
    return () => {
      observer.disconnect();
    };
  }, [notification, startNotificationTimeout, transitionState]);
  if (!notification) return null;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      "aria-label": "Notifications",
      className: clsx_default(
        "str-chat__notification-list",
        `str-chat__notification-list--enter-from-${notificationEnterFrom}`,
        `str-chat__notification-list--position-${verticalAlignment}`,
        panel && `str-chat__notification-list--${panel}`,
        className
      ),
      "data-testid": "notification-list",
      ref: listRef,
      role: "region",
      style: {
        "--str-chat__notification-list-enter-x": ENTER_TRANSLATION[notificationEnterFrom].x,
        "--str-chat__notification-list-enter-y": ENTER_TRANSLATION[notificationEnterFrom].y
      },
      children: [
        (0, import_jsx_runtime4.jsx)(
          "div",
          {
            "aria-hidden": true,
            className: "str-chat__notification-list__edge str-chat__notification-list__edge--top"
          }
        ),
        (0, import_jsx_runtime4.jsx)(
          NotificationComponent,
          {
            entryDirection: notificationEnterFrom,
            notification,
            onDismiss: () => dismiss(notification.id),
            ref: (element4) => {
              observedElementRef.current = element4;
            },
            showClose: !notification.duration,
            transitionState
          },
          notification.id
        ),
        (0, import_jsx_runtime4.jsx)(
          "div",
          {
            "aria-hidden": true,
            className: "str-chat__notification-list__edge str-chat__notification-list__edge--bottom"
          }
        )
      ]
    }
  );
};
var AudioAttachmentUI = ({ audioPlayer }) => {
  const { FileSizeIndicator: FileSizeIndicator$1 = FileSizeIndicator } = useComponentContext();
  const dataTestId = "audio-widget";
  const rootClassName2 = "str-chat__message-attachment-audio-widget";
  const { durationSeconds, isPlaying, progress, secondsElapsed } = useStateStore(audioPlayer?.state, audioPlayerStateSelector$1) ?? {};
  return (0, import_jsx_runtime4.jsxs)("div", { className: rootClassName2, "data-testid": dataTestId, children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-audio-widget--play-controls", children: (0, import_jsx_runtime4.jsx)(PlayButton, { isPlaying: !!isPlaying, onClick: audioPlayer.togglePlay }) }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment-audio-widget--data", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-audio-widget--text-first-row", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-audio-widget--title", children: audioPlayer.title }) }),
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-audio-widget--text-second-row", children: durationSeconds ? (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)(
          DurationDisplay,
          {
            duration: durationSeconds,
            isPlaying: !!isPlaying,
            secondsElapsed
          }
        ),
        (0, import_jsx_runtime4.jsx)(ProgressBar, { progress: progress ?? 0, seek: audioPlayer.seek })
      ] }) : (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
        (0, import_jsx_runtime4.jsx)(FileSizeIndicator$1, { fileSize: audioPlayer.fileSize }),
        (0, import_jsx_runtime4.jsx)(ProgressBar, { progress: progress ?? 0, seek: audioPlayer.seek })
      ] }) })
    ] }),
    (0, import_jsx_runtime4.jsx)(FileIcon, { className: "str-chat__file-icon", mimeType: audioPlayer.mimeType }),
    (0, import_jsx_runtime4.jsx)(DownloadButton, { assetUrl: audioPlayer.src, suggestedFileName: audioPlayer.title })
  ] });
};
var audioPlayerStateSelector$1 = (state) => ({
  durationSeconds: state.durationSeconds,
  isPlaying: state.isPlaying,
  progress: state.progressPercent,
  secondsElapsed: state.secondsElapsed
});
var Audio2 = (props) => {
  const {
    attachment: { asset_url, duration: duration2, file_size, mime_type, title }
  } = props;
  const { message, threadList } = useMessageContext() ?? {};
  const audioPlayer = useAudioPlayer({
    durationSeconds: duration2,
    fileSize: file_size,
    mimeType: mime_type,
    requester: message?.id && `${threadList ? message.parent_id ?? message.id : ""}${message.id}`,
    src: asset_url,
    title,
    waveformData: props.attachment.waveform_data
  });
  return audioPlayer ? (0, import_jsx_runtime4.jsx)(AudioAttachmentUI, { audioPlayer }) : null;
};
var defaultAttachmentActionsDefaultFocus = {
  giphy: "send"
};
var UnMemoizedAttachmentActions = (props) => {
  const { actionHandler, actions, defaultFocusedActionValue, id: id3, text: text22 } = props;
  const { t: t2 } = useTranslationContext("UnMemoizedAttachmentActions");
  const buttonRefs = (0, import_react29.useRef)([]);
  const handleActionClick = (event, name2, value) => actionHandler?.(name2, value, event);
  const knownActionText = (0, import_react29.useMemo)(
    () => ({
      Cancel: t2("Cancel"),
      Send: t2("Send"),
      Shuffle: t2("Shuffle")
    }),
    [t2]
  );
  const focusIndex = (0, import_react29.useMemo)(() => {
    if (!defaultFocusedActionValue) return null;
    const index6 = actions.findIndex(
      (action) => action.value === defaultFocusedActionValue
    );
    return index6 >= 0 ? index6 : null;
  }, [actions, defaultFocusedActionValue]);
  (0, import_react29.useEffect)(() => {
    if (focusIndex === null) return;
    const button = buttonRefs.current[focusIndex];
    if (button && document.activeElement !== button) {
      button.focus();
    }
  }, [focusIndex]);
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-actions", children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment-actions-form", children: [
    (0, import_jsx_runtime4.jsx)("span", { children: text22 }),
    actions.map((action, index6) => (0, import_jsx_runtime4.jsx)(
      Button,
      {
        appearance: "ghost",
        className: clsx_default(
          `str-chat__message-attachment-actions-button str-chat__message-attachment-actions-button--${action.style}`
        ),
        "data-testid": `${action.name}`,
        "data-value": action.value,
        onClick: (event) => handleActionClick(event, action.name, action.value),
        ref: (element4) => {
          buttonRefs.current[index6] = element4;
        },
        variant: "secondary",
        children: action.text ? knownActionText[action.text] ?? t2(action.text) : null
      },
      `${id3}-${action.value}`
    ))
  ] }) });
};
var AttachmentActions = import_react29.default.memo(
  UnMemoizedAttachmentActions
);
var rootClassName = "str-chat__message-attachment__voice-recording-widget";
var audioPlayerStateSelector = (state) => ({
  canPlayRecord: state.canPlayRecord,
  durationSeconds: state.durationSeconds,
  isPlaying: state.isPlaying,
  playbackRate: state.currentPlaybackRate,
  progress: state.progressPercent,
  secondsElapsed: state.secondsElapsed
});
var VoiceRecordingPlayerUI = ({ audioPlayer }) => {
  const { FileSizeIndicator: FileSizeIndicator$1 = FileSizeIndicator } = useComponentContext();
  const {
    canPlayRecord,
    durationSeconds,
    isPlaying,
    playbackRate,
    progress,
    secondsElapsed
  } = useStateStore(audioPlayer?.state, audioPlayerStateSelector) ?? {};
  return (0, import_jsx_runtime4.jsxs)("div", { className: rootClassName, "data-testid": "voice-recording-widget", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__voice-recording-widget__play-button-container", children: (0, import_jsx_runtime4.jsx)(PlayButton, { isPlaying: !!isPlaying, onClick: audioPlayer.togglePlay }) }),
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__voice-recording-widget__metadata", children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment__voice-recording-widget__audio-state", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__voice-recording-widget__timer", children: durationSeconds ? (0, import_jsx_runtime4.jsx)(
        DurationDisplay,
        {
          duration: durationSeconds,
          isPlaying: !!isPlaying,
          secondsElapsed,
          showRemaining: true
        }
      ) : (0, import_jsx_runtime4.jsx)(
        FileSizeIndicator$1,
        {
          fileSize: audioPlayer.fileSize,
          maximumFractionDigits: 0
        }
      ) }),
      (0, import_jsx_runtime4.jsx)(
        WaveProgressBar,
        {
          progress,
          seek: audioPlayer.seek,
          waveformData: audioPlayer.waveformData || []
        }
      )
    ] }) }),
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__voice-recording-widget__right-section", children: (0, import_jsx_runtime4.jsxs)(
      PlaybackRateButton,
      {
        disabled: !canPlayRecord,
        onClick: audioPlayer.increasePlaybackRate,
        children: [
          "x",
          playbackRate?.toString()
        ]
      }
    ) })
  ] });
};
var VoiceRecordingPlayer = ({
  attachment,
  playbackRates
}) => {
  const { t: t2 } = useTranslationContext();
  const {
    asset_url,
    duration: duration2 = 0,
    file_size,
    mime_type,
    title = t2("Voice message"),
    waveform_data
  } = attachment;
  const { message, threadList } = useMessageContext() ?? {};
  const audioPlayer = useAudioPlayer({
    durationSeconds: duration2 ?? 0,
    fileSize: file_size,
    mimeType: mime_type,
    playbackRates,
    requester: message?.id && `${threadList ? message.parent_id ?? message.id : ""}${message.id}`,
    src: asset_url,
    title,
    waveformData: waveform_data
  });
  return audioPlayer ? (0, import_jsx_runtime4.jsx)(VoiceRecordingPlayerUI, { audioPlayer }) : null;
};
var QuotedVoiceRecording = ({ attachment }) => {
  const { FileSizeIndicator: FileSizeIndicator$1 = FileSizeIndicator } = useComponentContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: rootClassName, "data-testid": "quoted-voice-recording-widget", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__voice-recording-widget__metadata", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__voice-recording-widget__audio-state", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment__voice-recording-widget__timer", children: attachment.duration ? (0, import_jsx_runtime4.jsx)(
      DurationDisplay,
      {
        duration: attachment.duration,
        isPlaying: false,
        secondsElapsed: void 0
      }
    ) : (0, import_jsx_runtime4.jsx)(
      FileSizeIndicator$1,
      {
        fileSize: attachment.file_size,
        maximumFractionDigits: 0
      }
    ) }) }) }),
    (0, import_jsx_runtime4.jsx)(FileIcon, { mimeType: attachment.mime_type })
  ] });
};
var VoiceRecording = ({ attachment, isQuoted }) => isQuoted ? (0, import_jsx_runtime4.jsx)(QuotedVoiceRecording, { attachment }) : (0, import_jsx_runtime4.jsx)(VoiceRecordingPlayer, { attachment });
var MAX_VISIBLE_THUMBNAILS = 4;
var BASE_IMAGE_PROP_KEYS = [
  "className",
  "crossOrigin",
  "decoding",
  "draggable",
  "fetchPriority",
  "height",
  "loading",
  "onError",
  "onLoad",
  "ref",
  "showDownloadButtonOnError",
  "sizes",
  "srcSet",
  "style",
  "title",
  "useMap",
  "width"
];
var ModalGallery = ({
  className,
  closeOnBackgroundClick = true,
  items,
  modalClassName
}) => {
  const {
    BaseImage: BaseImage$1 = BaseImage,
    Gallery: Gallery$1 = Gallery,
    Modal = GlobalModal
  } = useComponentContext();
  const [modalOpen, setModalOpen] = (0, import_react29.useState)(false);
  const [selectedIndex, setSelectedIndex] = (0, import_react29.useState)(0);
  const usesDefaultBaseImage = BaseImage$1 === BaseImage;
  const closeModal = (0, import_react29.useCallback)(() => {
    setModalOpen(false);
  }, []);
  const handleThumbnailClick = (0, import_react29.useCallback)((index6) => {
    setSelectedIndex(index6);
    setModalOpen(true);
  }, []);
  const itemCount = items.length;
  const visibleItems = items.slice(0, MAX_VISIBLE_THUMBNAILS);
  const overflowCount = itemCount - MAX_VISIBLE_THUMBNAILS;
  return (0, import_jsx_runtime4.jsxs)(import_jsx_runtime4.Fragment, { children: [
    (0, import_jsx_runtime4.jsx)(
      "div",
      {
        className: clsx_default("str-chat__modal-gallery", className, {
          "str-chat__modal-gallery--three-images": itemCount === 3,
          "str-chat__modal-gallery--two-images": itemCount === 2
        }),
        children: visibleItems.map((item, index6) => {
          const isLastVisible = index6 === MAX_VISIBLE_THUMBNAILS - 1;
          const showOverlay = isLastVisible && overflowCount > 0;
          return (0, import_jsx_runtime4.jsx)(
            ThumbnailButton,
            {
              BaseImage: BaseImage$1,
              baseImageUsesDefaultBehavior: usesDefaultBaseImage,
              index: index6,
              item,
              itemCount,
              onClick: () => handleThumbnailClick(index6),
              overflowCount,
              showOverlay
            },
            index6
          );
        })
      }
    ),
    (0, import_jsx_runtime4.jsx)(
      Modal,
      {
        className: clsx_default("str-chat__gallery-modal", modalClassName),
        onClose: closeModal,
        open: modalOpen,
        children: (0, import_jsx_runtime4.jsx)(
          Gallery$1,
          {
            closeOnBackgroundClick,
            GalleryUI,
            initialIndex: selectedIndex,
            items,
            onRequestClose: closeModal
          }
        )
      }
    )
  ] });
};
var ThumbnailButton = ({
  BaseImage: BaseImage3,
  baseImageUsesDefaultBehavior,
  index: index6,
  item,
  itemCount,
  onClick,
  overflowCount,
  showOverlay
}) => {
  const { t: t2 } = useTranslationContext();
  const imageUrl = item.imageUrl;
  const [isLoadFailed, setIsLoadFailed] = (0, import_react29.useState)(false);
  const [isImageLoading, setIsImageLoading] = (0, import_react29.useState)(Boolean(imageUrl));
  const [retryCount, setRetryCount] = (0, import_react29.useState)(0);
  const {
    onError: itemOnError,
    onLoad: itemOnLoad,
    ...baseImageProps
  } = getBaseImageProps(item);
  const showRetryIndicator = isLoadFailed && !showOverlay;
  const showLoadingIndicator = isImageLoading && !showRetryIndicator && !showOverlay;
  const handleButtonClick = () => {
    if (showRetryIndicator) {
      setIsLoadFailed(false);
      setIsImageLoading(true);
      setRetryCount((currentRetryCount) => currentRetryCount + 1);
      return;
    }
    onClick();
  };
  const buttonLabel = showRetryIndicator ? t2("aria/Retry upload") : itemCountAwareLabel({ imageIndex: index6 + 1, itemCount, t: t2 });
  return (0, import_jsx_runtime4.jsxs)(
    "button",
    {
      "aria-label": buttonLabel,
      className: clsx_default("str-chat__modal-gallery__image", {
        "str-chat__modal-gallery__image--load-failed": showRetryIndicator,
        "str-chat__modal-gallery__image--loading": showLoadingIndicator
      }),
      onClick: handleButtonClick,
      type: "button",
      children: [
        item.videoThumbnailUrl ? (0, import_jsx_runtime4.jsx)(VideoThumbnail, { alt: t2("User uploaded content"), src: item.videoThumbnailUrl }) : (0, import_jsx_runtime4.jsx)(
          BaseImage3,
          {
            ...baseImageProps,
            alt: item.alt ?? t2("User uploaded content"),
            onError: (event) => {
              setIsImageLoading(false);
              setIsLoadFailed(true);
              itemOnError?.(event);
            },
            onLoad: (event) => {
              setIsImageLoading(false);
              setIsLoadFailed(false);
              itemOnLoad?.(event);
            },
            src: imageUrl,
            ...baseImageUsesDefaultBehavior ? { showDownloadButtonOnError: false } : {}
          },
          retryCount
        ),
        showLoadingIndicator && (0, import_jsx_runtime4.jsx)(
          "div",
          {
            "aria-hidden": "true",
            className: "str-chat__modal-gallery__image-loading-overlay",
            "data-testid": "str-chat__modal-gallery__image-loading-overlay",
            children: (0, import_jsx_runtime4.jsx)(LoadingIndicator, {})
          }
        ),
        showRetryIndicator && (0, import_jsx_runtime4.jsx)(
          "div",
          {
            "aria-hidden": "true",
            className: "str-chat__modal-gallery__image-load-failed-overlay",
            "data-testid": "str-chat__modal-gallery__image-load-failed-overlay",
            children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__modal-gallery__image-retry-indicator", children: (0, import_jsx_runtime4.jsx)(IconRetry, {}) })
          }
        ),
        showOverlay && (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__modal-gallery__placeholder", children: [
          "+",
          overflowCount
        ] })
      ]
    }
  );
};
var itemCountAwareLabel = ({
  imageIndex,
  itemCount,
  t: t2
}) => itemCount === 1 ? t2("Open image in gallery") : t2("Open gallery at image {{ index }}", {
  index: imageIndex
});
var getBaseImageProps = (item) => {
  const baseImageProps = {};
  for (const key of BASE_IMAGE_PROP_KEYS) {
    const value = item[key];
    if (value !== void 0) {
      baseImageProps[key] = value;
    }
  }
  return baseImageProps;
};
var ImageComponent = (galleryItem) => {
  const { ModalGallery: ModalGallery$1 = ModalGallery } = useComponentContext();
  return (0, import_jsx_runtime4.jsx)(ModalGallery$1, { items: [galleryItem], modalClassName: "str-chat__image-modal" });
};
var UnMemoizedSafeAnchor = (props) => {
  const { children, className, download, href, rel, target } = props;
  if (!href) return null;
  const sanitized = (0, import_sanitize_url.sanitizeUrl)(href);
  return (0, import_jsx_runtime4.jsx)(
    "a",
    {
      className,
      download,
      href: sanitized,
      rel,
      target,
      children
    }
  );
};
var SafeAnchor = import_react29.default.memo(UnMemoizedSafeAnchor);
var UnableToRenderCard = ({ type }) => {
  const { t: t2 } = useTranslationContext("Card");
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default("str-chat__message-attachment-card", {
        [`str-chat__message-attachment-card--${type}`]: type
      }),
      children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-card--content", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-card--text", children: t2("this content could not be displayed") }) })
    }
  );
};
var CardRoot = ({ cardUrl, children, type }) => {
  const className = clsx_default(
    `str-chat__message-attachment-card str-chat__message-attachment-card--${type}`
  );
  return cardUrl ? (0, import_jsx_runtime4.jsx)(
    SafeAnchor,
    {
      className,
      href: cardUrl,
      rel: "noopener noreferrer",
      target: "_blank",
      children
    }
  ) : (0, import_jsx_runtime4.jsx)("div", { className, children });
};
var CardHeader = (props) => {
  const { dimensions, image: image3, image_url, thumb_url, title } = props;
  return image3 ? (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__message-attachment-card--header str-chat__message-attachment-card-react--header",
      "data-testid": "card-header",
      children: (0, import_jsx_runtime4.jsx)(
        BaseImage,
        {
          alt: title || image3,
          "data-testid": "image-test",
          src: image_url ?? thumb_url,
          title: title || image3,
          ...dimensions
        }
      )
    }
  ) : null;
};
var CardContent = (props) => {
  const { og_scrape_url, text: text22, title, title_link } = props;
  const url = title_link || og_scrape_url;
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment-card--content", children: [
    title && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-card--title", children: title }),
    text22 && (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-card--text", children: text22 }),
    url && (0, import_jsx_runtime4.jsxs)(
      "div",
      {
        className: "str-chat__message-attachment-card--source-link",
        "data-testid": "card-source-link",
        children: [
          (0, import_jsx_runtime4.jsx)(IconLink, {}),
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-card--url", children: url })
        ]
      }
    )
  ] });
};
var UnMemoizedCard = (props) => {
  const { giphy, image_url, og_scrape_url, thumb_url, title, title_link, type } = props;
  const { giphyVersion: giphyVersionName } = useChannelStateContext("");
  const cardUrl = title_link || og_scrape_url;
  let image3 = thumb_url || image_url;
  const dimensions = {};
  if (type === "giphy" && typeof giphy !== "undefined") {
    const giphyVersion = giphy[giphyVersionName];
    image3 = giphyVersion.url;
    dimensions.height = giphyVersion.height;
    dimensions.width = giphyVersion.width;
  }
  if (!title && !cardUrl && !image3) {
    return (0, import_jsx_runtime4.jsx)(UnableToRenderCard, {});
  }
  return (0, import_jsx_runtime4.jsxs)(CardRoot, { cardUrl, type, children: [
    (0, import_jsx_runtime4.jsx)(CardHeader, { ...props, dimensions, image: image3 }),
    (0, import_jsx_runtime4.jsx)(CardContent, { ...props })
  ] });
};
var Card = import_react29.default.memo(UnMemoizedCard);
var FileAttachment = ({ attachment }) => {
  const { AttachmentFileIcon, FileSizeIndicator: FileSizeIndicator$1 = FileSizeIndicator } = useComponentContext();
  const FileIconComponent = AttachmentFileIcon ?? FileIcon;
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__message-attachment-file--item",
      "data-testid": "attachment-file",
      children: [
        (0, import_jsx_runtime4.jsx)(
          FileIconComponent,
          {
            className: "str-chat__file-icon",
            fileName: attachment.title,
            mimeType: attachment.mime_type
          }
        ),
        (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment-file--item__info", children: [
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-file--item__first-row", children: (0, import_jsx_runtime4.jsx)(
            "div",
            {
              className: "str-chat__message-attachment-file--item__name",
              "data-testid": "file-title",
              children: attachment.title
            }
          ) }),
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-file--item__data", children: (0, import_jsx_runtime4.jsx)(FileSizeIndicator$1, { fileSize: attachment.file_size }) })
        ] }),
        (0, import_jsx_runtime4.jsx)(
          DownloadButton,
          {
            assetUrl: attachment.asset_url,
            suggestedFileName: attachment.title
          }
        )
      ]
    }
  );
};
var Giphy = ({ attachment }) => {
  const { giphyVersion: giphyVersionName, imageAttachmentSizeHandler } = useChannelStateContext();
  const { BaseImage: BaseImage$1 = BaseImage } = useComponentContext();
  const { t: t2 } = useTranslationContext();
  const usesDefaultBaseImage = BaseImage$1 === BaseImage;
  const imageElement = (0, import_react29.useRef)(null);
  const [attachmentConfiguration, setAttachmentConfiguration] = (0, import_react29.useState)(void 0);
  const imageDescriptors = (0, import_react29.useMemo)(
    () => toGalleryItemDescriptors(attachment, { giphyVersionName }),
    [attachment, giphyVersionName]
  );
  const alt = imageDescriptors && imageDescriptors.alt;
  const dimensions = imageDescriptors && imageDescriptors.dimensions;
  const imageUrl = imageDescriptors && imageDescriptors.imageUrl;
  const title = imageDescriptors && imageDescriptors.title;
  const resolvedImageUrl = attachmentConfiguration?.url || imageUrl;
  const imageStyleVariables = (0, import_react29.useMemo)(() => {
    const originalHeight = Number(dimensions?.height);
    const originalWidth = Number(dimensions?.width);
    return {
      "--original-height": String(originalHeight > 1 ? originalHeight : 1e6),
      "--original-width": String(originalWidth > 1 ? originalWidth : 1e6)
    };
  }, [dimensions?.height, dimensions?.width]);
  (0, import_react29.useLayoutEffect)(() => {
    if (!imageElement.current || !imageAttachmentSizeHandler) return;
    const config = imageAttachmentSizeHandler(attachment, imageElement.current);
    setAttachmentConfiguration(config);
  }, [attachment, imageAttachmentSizeHandler]);
  if (!imageUrl) return null;
  return (0, import_jsx_runtime4.jsxs)("div", { className: clsx_default(`str-chat__message-attachment-giphy`), children: [
    (0, import_jsx_runtime4.jsx)(
      BaseImage$1,
      {
        alt: alt ?? title ?? t2("User uploaded content"),
        height: dimensions?.height,
        ref: imageElement,
        src: resolvedImageUrl,
        style: imageStyleVariables,
        width: dimensions?.width,
        ...usesDefaultBaseImage ? { showDownloadButtonOnError: false } : {}
      }
    ),
    (0, import_jsx_runtime4.jsx)(GiphyBadge, {})
  ] });
};
var GiphyBadge = () => (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__giphy-badge", children: [
  (0, import_jsx_runtime4.jsx)(IconGiphy, {}),
  "Giphy"
] });
var ExternalLinkIcon = () => (0, import_jsx_runtime4.jsx)("svg", { fill: "currentColor", viewBox: "0 0 16 16", xmlns: "http://www.w3.org/2000/svg", children: (0, import_jsx_runtime4.jsx)("path", { d: "M12.586 2H10a1 1 0 1 1 0-2h5a1 1 0 0 1 1 1v5a1 1 0 1 1-2 0V3.414l-6.793 6.793a1 1 0 0 1-1.414-1.414L12.586 2zM6 1a1 1 0 1 1 0 2H3a1 1 0 0 0-1 1v9a1 1 0 0 0 1 1h8.967a1 1 0 0 0 1-.99L13 9.99a1 1 0 1 1 2 .02l-.033 3.023a3 3 0 0 1-3 2.967H3a3 3 0 0 1-3-3V4a3 3 0 0 1 3-3h3z" }) });
var Geolocation = ({
  GeolocationAttachmentMapPlaceholder = DefaultGeolocationAttachmentMapPlaceholder,
  GeolocationMap,
  location: location2
}) => {
  const { channel, client } = useChatContext();
  const { t: t2 } = useTranslationContext();
  const [stoppedSharing, setStoppedSharing] = (0, import_react29.useState)(
    !!location2.end_at && new Date(location2.end_at).getTime() < (/* @__PURE__ */ new Date()).getTime()
  );
  const timeoutRef = (0, import_react29.useRef)(void 0);
  const isMyLocation = location2.user_id === client.userID;
  const isLiveLocation = !!location2.end_at;
  (0, import_react29.useEffect)(() => {
    if (!location2.end_at) return;
    clearTimeout(timeoutRef.current);
    timeoutRef.current = setTimeout(
      () => setStoppedSharing(true),
      new Date(location2.end_at).getTime() - Date.now()
    );
  }, [location2.end_at]);
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__message-attachment-geolocation",
      "data-testid": "attachment-geolocation",
      children: [
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-geolocation__location-preview", children: GeolocationMap ? (0, import_jsx_runtime4.jsx)(GeolocationMap, { latitude: location2.latitude, longitude: location2.longitude }) : (0, import_jsx_runtime4.jsx)(GeolocationAttachmentMapPlaceholder, { location: location2 }) }),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-geolocation__status", children: isLiveLocation ? stoppedSharing ? t2("Location sharing ended") : isMyLocation ? (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment-geolocation__status--active", children: [
          (0, import_jsx_runtime4.jsx)(
            Button,
            {
              appearance: "outline",
              className: "str-chat__message-attachment-geolocation__stop-sharing-button",
              onClick: () => channel?.stopLiveLocationSharing(location2),
              size: "sm",
              variant: "secondary",
              children: t2("Stop sharing")
            }
          ),
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-geolocation__status--active-until", children: t2("Live until {{ timestamp }}", {
            timestamp: t2("timestamp/LiveLocation", { timestamp: location2.end_at })
          }) })
        ] }) : (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__message-attachment-geolocation__status--active", children: [
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-geolocation__status--active-status", children: t2("Live location") }),
          (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-geolocation__status--active-until", children: t2("Live until {{ timestamp }}", {
            timestamp: t2("timestamp/LiveLocation", { timestamp: location2.end_at })
          }) })
        ] }) : t2("Current location") })
      ]
    }
  );
};
var DefaultGeolocationAttachmentMapPlaceholder = ({
  location: location2
}) => (0, import_jsx_runtime4.jsxs)(
  "div",
  {
    className: "str-chat__message-attachment-geolocation__placeholder",
    "data-testid": "geolocation-attachment-map-placeholder",
    children: [
      (0, import_jsx_runtime4.jsx)(IconLocation, {}),
      (0, import_jsx_runtime4.jsx)(
        "a",
        {
          className: "str-chat__message-attachment-geolocation__placeholder-link",
          href: `https://maps.google.com?q=${[location2.latitude, location2.longitude].join()}`,
          rel: "noreferrer",
          target: "_blank",
          children: (0, import_jsx_runtime4.jsx)(ExternalLinkIcon, {})
        }
      )
    ]
  }
);
var UnsupportedAttachment = () => {
  const { t: t2 } = useTranslationContext("UnsupportedAttachment");
  return (0, import_jsx_runtime4.jsxs)(
    "div",
    {
      className: "str-chat__message-attachment-unsupported",
      "data-testid": "attachment-unsupported",
      children: [
        (0, import_jsx_runtime4.jsx)(IconUnsupportedAttachment, {}),
        (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__message-attachment-unsupported__metadata", children: (0, import_jsx_runtime4.jsx)(
          "div",
          {
            className: "str-chat__message-attachment-unsupported__title",
            "data-testid": "unsupported-attachment-title",
            children: t2("Unsupported attachment")
          }
        ) })
      ]
    }
  );
};
var VisibilityDisclaimer = () => {
  const { t: t2 } = useTranslationContext();
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__visibility-disclaimer", children: [
    (0, import_jsx_runtime4.jsx)(IconEyeFill, {}),
    t2("Only visible to you")
  ] });
};
var VideoAttachment = ({
  attachment,
  VideoPlayer: VideoPlayer$1 = VideoPlayer
}) => {
  const { shouldGenerateVideoThumbnail, videoAttachmentSizeHandler } = useChannelStateContext();
  const videoElement = (0, import_react29.useRef)(null);
  const [attachmentConfiguration, setAttachmentConfiguration] = (0, import_react29.useState)();
  const [showVideo, setShowVideo] = import_react29.default.useState(!shouldGenerateVideoThumbnail);
  (0, import_react29.useLayoutEffect)(() => {
    if (videoElement.current && videoAttachmentSizeHandler) {
      const config = videoAttachmentSizeHandler(
        attachment,
        videoElement.current,
        shouldGenerateVideoThumbnail
      );
      setAttachmentConfiguration(config);
    }
  }, [
    videoElement,
    videoAttachmentSizeHandler,
    attachment,
    shouldGenerateVideoThumbnail
  ]);
  const renderThumbnailFirst = Boolean(
    attachment.thumb_url && shouldGenerateVideoThumbnail
  );
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: "str-chat__player-wrapper",
      "data-testid": "video-wrapper",
      ref: videoElement,
      style: getCssDimensionsVariables(attachment.thumb_url || ""),
      children: renderThumbnailFirst && !showVideo ? (0, import_jsx_runtime4.jsx)(
        VideoThumbnail,
        {
          alt: attachment.title,
          "data-testid": "image-test",
          onPlay: () => setShowVideo(true),
          src: attachment.thumb_url,
          title: attachment.title
        }
      ) : (0, import_jsx_runtime4.jsx)(
        VideoPlayer$1,
        {
          isPlaying: renderThumbnailFirst,
          thumbnailUrl: attachmentConfiguration?.thumbUrl,
          videoUrl: attachmentConfiguration?.url
        }
      )
    }
  );
};
var AttachmentWithinContainer = ({
  attachment,
  children,
  componentType
}) => {
  const isGAT = isGalleryAttachmentType(attachment);
  let extra = "";
  if (!isGAT && !isSharedLocationResponse(attachment)) {
    extra = componentType === "card" && !attachment?.image_url && !attachment?.thumb_url ? "no-image" : attachment?.actions?.length ? "actions" : "";
  }
  const classNames = clsx_default(
    "str-chat__message-attachment str-chat__message-attachment-dynamic-size",
    {
      [`str-chat__message-attachment--${componentType}`]: componentType,
      [`str-chat__message-attachment--${attachment?.type}`]: attachment?.type,
      [`str-chat__message-attachment--${componentType}--${extra}`]: componentType && extra,
      "str-chat__message-attachment--svg-image": isSvgAttachment(attachment),
      "str-chat__message-attachment-with-actions": extra === "actions"
    }
  );
  return (0, import_jsx_runtime4.jsx)("div", { className: classNames, children });
};
var AttachmentActionsContainer = ({
  actionHandler,
  attachment,
  AttachmentActions: AttachmentActions$1 = AttachmentActions,
  attachmentActionsDefaultFocus
}) => {
  if (!attachment.actions?.length) return null;
  const defaultFocusedActionValue = attachment.type && attachmentActionsDefaultFocus?.[attachment.type];
  return (0, import_jsx_runtime4.jsx)(
    AttachmentActions$1,
    {
      ...attachment,
      actionHandler,
      actions: attachment.actions,
      defaultFocusedActionValue,
      id: attachment.localMetadata?.id || "",
      text: attachment.text || ""
    }
  );
};
var MediaContainer = (props) => {
  const { attachments: mediaAttachments } = props;
  if (!mediaAttachments.length) return null;
  if (mediaAttachments.length > 1) {
    return (0, import_jsx_runtime4.jsx)(
      GalleryContainer,
      {
        ...props,
        attachment: { items: mediaAttachments, type: "gallery" }
      }
    );
  }
  const mediaAttachment = mediaAttachments[0];
  const { attachments: _attachments, ...rest } = props;
  if (isVideoAttachment(mediaAttachment, SUPPORTED_VIDEO_FORMATS$1)) {
    return (0, import_jsx_runtime4.jsx)(VideoContainer, { attachment: mediaAttachment, ...rest });
  }
  return (0, import_jsx_runtime4.jsx)(ImageContainer, { attachment: mediaAttachment, ...rest });
};
var CardContainer = (props) => {
  const { attachment, Card: Card$1 = Card } = props;
  const componentType = "card";
  if (attachment.actions && attachment.actions.length) {
    return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment", children: [
      (0, import_jsx_runtime4.jsx)(Card$1, { ...attachment }),
      (0, import_jsx_runtime4.jsx)(AttachmentActionsContainer, { ...props })
    ] }) });
  }
  return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsx)(Card$1, { ...attachment }) });
};
var GiphyContainer = (props) => {
  const { attachment, Giphy: Giphy$1 = Giphy } = props;
  const componentType = "giphy";
  if (attachment.actions && attachment.actions.length) {
    return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment", children: [
      (0, import_jsx_runtime4.jsx)(VisibilityDisclaimer, {}),
      (0, import_jsx_runtime4.jsx)(Giphy$1, { attachment }),
      (0, import_jsx_runtime4.jsx)(AttachmentActionsContainer, { ...props })
    ] }) });
  }
  return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsx)(Giphy$1, { attachment }) });
};
var FileContainer = (props) => {
  const { attachment } = props;
  if (isVoiceRecordingAttachment(attachment)) {
    return (0, import_jsx_runtime4.jsx)(VoiceRecordingContainer, { ...props });
  }
  if (isAudioAttachment(attachment)) {
    return (0, import_jsx_runtime4.jsx)(AudioContainer, { ...props });
  }
  if (!attachment.asset_url || !isFileAttachment(attachment, SUPPORTED_VIDEO_FORMATS$1)) {
    return null;
  }
  return (0, import_jsx_runtime4.jsx)(OtherFilesContainer, { ...props });
};
var GalleryContainer = ({
  attachment,
  ModalGallery: ModalGallery$1 = ModalGallery
}) => {
  const items = (0, import_react29.useMemo)(
    () => attachment.items.reduce((acc, attachment2) => {
      const item = toGalleryItemDescriptors(attachment2);
      if (item) acc.push(item);
      return acc;
    }, []),
    [attachment.items]
  );
  return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType: "gallery", children: (0, import_jsx_runtime4.jsx)(ModalGallery$1, { items }, "gallery") });
};
var ImageContainer = (props) => {
  const { attachment, Image: Image2 = ImageComponent } = props;
  const componentType = "image";
  const imageElement = (0, import_react29.useRef)(null);
  const { imageAttachmentSizeHandler } = useChannelStateContext();
  const [attachmentConfiguration, setAttachmentConfiguration] = (0, import_react29.useState)(void 0);
  (0, import_react29.useLayoutEffect)(() => {
    if (imageElement.current && imageAttachmentSizeHandler) {
      const config = imageAttachmentSizeHandler(attachment, imageElement.current);
      setAttachmentConfiguration(config);
    }
  }, [imageElement, imageAttachmentSizeHandler, attachment]);
  const imgUrlFromAttachment = attachment.image_url || attachment.thumb_url || "";
  const imageConfig = {
    ...toGalleryItemDescriptors({
      ...attachment,
      image_url: attachmentConfiguration?.url || imgUrlFromAttachment
    }),
    ref: imageElement,
    style: getCssDimensionsVariables(imgUrlFromAttachment)
  };
  if (attachment.actions && attachment.actions.length) {
    return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment", children: [
      (0, import_jsx_runtime4.jsx)(Image2, { ...imageConfig }),
      (0, import_jsx_runtime4.jsx)(AttachmentActionsContainer, { ...props })
    ] }) });
  }
  return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsx)(Image2, { ...imageConfig }) });
};
var OtherFilesContainer = ({
  attachment,
  File: File2 = FileAttachment
}) => {
  if (!attachment.asset_url) return null;
  return (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType: "file", children: (0, import_jsx_runtime4.jsx)(File2, { attachment }) });
};
var AudioContainer = ({
  attachment,
  Audio: Audio$1 = Audio2
}) => (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType: "audio", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment", children: (0, import_jsx_runtime4.jsx)(Audio$1, { attachment }) }) });
var VoiceRecordingContainer = ({
  attachment,
  isQuoted,
  VoiceRecording: VoiceRecording$1 = VoiceRecording
}) => (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType: "voiceRecording", children: (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment", children: (0, import_jsx_runtime4.jsx)(VoiceRecording$1, { attachment, isQuoted }) }) });
var VideoContainer = (props) => {
  const { attachment, Media } = props;
  const componentType = "media";
  return attachment.actions?.length ? (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__attachment", children: [
    (0, import_jsx_runtime4.jsx)(VideoAttachment, { attachment, VideoPlayer: Media }),
    (0, import_jsx_runtime4.jsx)(AttachmentActionsContainer, { ...props })
  ] }) }) : (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType, children: (0, import_jsx_runtime4.jsx)(VideoAttachment, { attachment, VideoPlayer: Media }) });
};
var GeolocationContainer = ({
  Geolocation: Geolocation$1 = Geolocation,
  location: location2
}) => (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment: location2, componentType: "geolocation", children: (0, import_jsx_runtime4.jsx)(Geolocation$1, { location: location2 }) });
var UnsupportedAttachmentContainer = ({
  attachment,
  UnsupportedAttachment: UnsupportedAttachment$1 = UnsupportedAttachment
}) => (0, import_jsx_runtime4.jsx)(AttachmentWithinContainer, { attachment, componentType: "unsupported", children: (0, import_jsx_runtime4.jsx)(UnsupportedAttachment$1, { attachment }) });
var ATTACHMENT_GROUPS_ORDER = [
  "media",
  "giphy",
  "card",
  "geolocation",
  "file",
  "unsupported"
];
var Attachment = (props) => {
  const {
    attachmentActionsDefaultFocus = defaultAttachmentActionsDefaultFocus,
    attachments,
    ...rest
  } = props;
  const groupedAttachments = (0, import_react29.useMemo)(
    () => renderGroupedAttachments({
      attachmentActionsDefaultFocus,
      attachments,
      ...rest
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [attachments, attachmentActionsDefaultFocus]
  );
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__attachment-list", children: ATTACHMENT_GROUPS_ORDER.reduce(
    (acc, groupName) => [...acc, ...groupedAttachments[groupName]],
    []
  ) });
};
var renderGroupedAttachments = ({
  attachments,
  ...rest
}) => {
  const mediaAttachments = [];
  const containers = attachments.reduce(
    (typeMap, attachment) => {
      if (isSharedLocationResponse(attachment)) {
        typeMap.geolocation.push(
          (0, import_react29.createElement)(
            GeolocationContainer,
            {
              ...rest,
              key: `geolocation-${typeMap.geolocation.length}`,
              location: attachment
            }
          )
        );
      } else if (attachment.type === "giphy") {
        typeMap.card.push(
          (0, import_jsx_runtime4.jsx)(
            GiphyContainer,
            {
              ...rest,
              attachment
            },
            `giphy-${typeMap.giphy.length}`
          )
        );
      } else if (isScrapedContent(attachment)) {
        typeMap.card.push(
          (0, import_jsx_runtime4.jsx)(
            CardContainer,
            {
              ...rest,
              attachment
            },
            `card-${typeMap.card.length}`
          )
        );
      } else if (isImageAttachment(attachment) || isVideoAttachment(attachment, SUPPORTED_VIDEO_FORMATS$1)) {
        mediaAttachments.push(attachment);
      } else if (isAudioAttachment(attachment) || isVoiceRecordingAttachment(attachment) || isFileAttachment(attachment, SUPPORTED_VIDEO_FORMATS$1)) {
        typeMap.file.push(
          (0, import_jsx_runtime4.jsx)(
            FileContainer,
            {
              ...rest,
              attachment
            },
            `file-${typeMap.file.length}`
          )
        );
      } else {
        typeMap.unsupported.push(
          (0, import_jsx_runtime4.jsx)(
            UnsupportedAttachmentContainer,
            {
              ...rest,
              attachment
            },
            `unsupported-${typeMap.unsupported.length}`
          )
        );
      }
      return typeMap;
    },
    {
      card: [],
      file: [],
      geolocation: [],
      giphy: [],
      media: [],
      unsupported: []
    }
  );
  if (mediaAttachments.length) {
    containers.media.push(
      (0, import_jsx_runtime4.jsx)(MediaContainer, { ...rest, attachments: mediaAttachments }, "media-container")
    );
  }
  return containers;
};
var isMobile = () => /Mobi/i.test(navigator.userAgent);
var isSafari2 = () => {
  if (typeof navigator === "undefined") return false;
  return /^((?!chrome|android).)*safari/i.test(navigator.userAgent || "");
};
var isFirefox = () => {
  if (typeof navigator === "undefined") return false;
  return navigator.userAgent?.includes("Firefox");
};
var isChrome = () => {
  if (typeof navigator === "undefined") return false;
  return navigator.userAgent?.includes("Chrome");
};
var browser = () => {
  if (isChrome()) return "chrome";
  if (isFirefox()) return "firefox";
  if (isSafari2()) return "safari";
  return "other";
};
var useLiveLocationSharingManager = ({
  client,
  getDeviceId,
  watchLocation
}) => {
  const manager = (0, import_react29.useMemo)(() => {
    if (!client) return null;
    return new LiveLocationManager({
      client,
      getDeviceId: getDeviceId ?? (() => `web-${isMobile() ? "mobile" : "desktop"}-${browser()}-${client.userID}`),
      watchLocation
    });
  }, [client, getDeviceId, watchLocation]);
  (0, import_react29.useEffect)(() => {
    if (!manager) return;
    manager.init();
    return () => {
      manager.unregisterSubscriptions();
    };
  }, [manager]);
  return manager;
};
function useChannelHeaderOnlineStatus() {
  const { t: t2 } = useTranslationContext();
  const { client } = useChatContext();
  const { channel, watcherCount = 0 } = useChannelStateContext();
  const { member_count: memberCount = 0 } = channel?.data || {};
  const [watchers, setWatchers] = (0, import_react29.useState)(
    () => Object.assign({}, channel?.state?.watchers ?? {})
  );
  (0, import_react29.useEffect)(() => {
    if (!channel) return;
    const subscription = channel.on("user.watching.start", (event) => {
      setWatchers((prev) => {
        if (!event.user?.id) return prev;
        if (prev[event.user.id]) return prev;
        return Object.assign({ [event.user.id]: event.user }, prev);
      });
    });
    return () => subscription.unsubscribe();
  }, [channel]);
  if (!memberCount) return null;
  const isDmChannel = memberCount === 1 || memberCount === 2 && Object.values(channel?.state?.members ?? {}).some(
    ({ user }) => user?.id === client.user?.id
  );
  if (isDmChannel) {
    const hasWatchers = Object.keys(watchers).length > 0;
    return hasWatchers ? t2("Online") : t2("Offline");
  }
  return `${t2("{{ memberCount }} members", { memberCount })}, ${t2("{{ watcherCount }} online", { watcherCount })}`;
}
var ChannelHeaderSubtitle = () => {
  const { channelConfig } = useChannelStateContext("ChannelHeaderSubtitle");
  const { client } = useChatContext("ChannelHeaderSubtitle");
  const { typing = {} } = useTypingContext("ChannelHeaderSubtitle");
  const onlineStatusText = useChannelHeaderOnlineStatus();
  const typingInChannel = Object.values(typing).filter(
    ({ parent_id, user }) => user?.id !== client.user?.id && !parent_id
  );
  const hasTyping = channelConfig?.typing_events !== false && typingInChannel.length > 0;
  if (!hasTyping && !onlineStatusText) return null;
  return (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__channel-header__data__subtitle", children: (0, import_jsx_runtime4.jsx)(
    "span",
    {
      className: "str-chat__subtitle-content-transition",
      children: hasTyping ? (0, import_jsx_runtime4.jsx)(TypingIndicatorHeader, {}) : onlineStatusText
    },
    hasTyping ? "typing" : "default"
  ) });
};
var ChannelHeader = (props) => {
  const { Avatar: Avatar2 = ChannelAvatar, image: overrideImage, title: overrideTitle } = props;
  const { channel } = useChannelStateContext();
  const { HeaderStartContent } = useComponentContext();
  const { displayImage, displayTitle, groupChannelDisplayInfo } = useChannelPreviewInfo({
    channel,
    overrideImage,
    overrideTitle
  });
  return (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-header", children: [
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__channel-header__start", children: HeaderStartContent && (0, import_jsx_runtime4.jsx)(HeaderStartContent, {}) }),
    (0, import_jsx_runtime4.jsxs)("div", { className: "str-chat__channel-header__data", children: [
      (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__channel-header__data__title", children: displayTitle }),
      (0, import_jsx_runtime4.jsx)(ChannelHeaderSubtitle, {})
    ] }),
    (0, import_jsx_runtime4.jsx)("div", { className: "str-chat__channel-header__end", children: (0, import_jsx_runtime4.jsx)(
      Avatar2,
      {
        className: "str-chat__avatar--channel-header",
        displayMembers: groupChannelDisplayInfo?.members,
        imageUrl: displayImage,
        size: "lg",
        userName: displayTitle
      }
    ) })
  ] });
};
var useChat = ({
  client,
  defaultLanguage = "en",
  i18nInstance
}) => {
  const [translators, setTranslators] = (0, import_react29.useState)({
    t: defaultTranslatorFunction,
    tDateTimeParser: defaultDateTimeParser,
    userLanguage: "en"
  });
  const [channel, setChannel] = (0, import_react29.useState)();
  const [mutes, setMutes] = (0, import_react29.useState)([]);
  const [latestMessageDatesByChannels, setLatestMessageDatesByChannels] = (0, import_react29.useState)({});
  const clientMutes = client.user?.mutes ?? [];
  const appSettings = (0, import_react29.useRef)(null);
  const getAppSettings = () => {
    if (appSettings.current) {
      return appSettings.current;
    }
    appSettings.current = client.getAppSettings();
    return appSettings.current;
  };
  (0, import_react29.useEffect)(() => {
    if (!client) return;
    const version = "14.0.1";
    const userAgent = client.getUserAgent();
    if (!userAgent.includes("stream-chat-react")) {
      client.setUserAgent(`stream-chat-react-${version}-${userAgent}`);
    }
    client.threads.registerSubscriptions();
    client.polls.registerSubscriptions();
    client.reminders.registerSubscriptions();
    client.reminders.initTimers();
    return () => {
      client.threads.unregisterSubscriptions();
      client.polls.unregisterSubscriptions();
      client.reminders.unregisterSubscriptions();
      client.reminders.clearTimers();
    };
  }, [client]);
  (0, import_react29.useEffect)(() => {
    setMutes(clientMutes);
    const handleEvent = (event) => {
      setMutes(event.me?.mutes || []);
    };
    client.on("notification.mutes_updated", handleEvent);
    return () => client.off("notification.mutes_updated", handleEvent);
  }, [clientMutes?.length]);
  (0, import_react29.useEffect)(() => {
    let userLanguage = client.user?.language;
    if (!userLanguage) {
      const browserLanguage = window.navigator.language.slice(0, 2);
      userLanguage = isLanguageSupported(browserLanguage) ? browserLanguage : defaultLanguage;
    }
    const streami18n = i18nInstance || new Streami18n({ language: userLanguage });
    streami18n.registerSetLanguageCallback(
      (t2) => setTranslators((prevTranslator) => ({ ...prevTranslator, t: t2 }))
    );
    streami18n.getTranslators().then((translator) => {
      setTranslators({
        ...translator,
        userLanguage: userLanguage || defaultLanguage
      });
    });
  }, [i18nInstance]);
  const setActiveChannel = (0, import_react29.useCallback)(
    async (activeChannel, watchers = {}, event) => {
      if (event && event.preventDefault) event.preventDefault();
      if (activeChannel && Object.keys(watchers).length) {
        await activeChannel.query({ watch: true, watchers });
      }
      setChannel(activeChannel);
    },
    []
  );
  (0, import_react29.useEffect)(() => {
    setLatestMessageDatesByChannels({});
  }, [client.user?.id]);
  return {
    channel,
    getAppSettings,
    latestMessageDatesByChannels,
    mutes,
    setActiveChannel,
    translators
  };
};
var useReportLostConnectionSystemNotification = () => {
  const { t: t2 } = useTranslationContext();
  const { client } = useChatContext();
  const { addSystemNotification, removeNotification } = useNotificationApi();
  const connectionLostNotificationIdRef = (0, import_react29.useRef)(null);
  (0, import_react29.useEffect)(() => {
    if (!t2 || !client) return;
    const dismissConnectionLostNotification = () => {
      if (!connectionLostNotificationIdRef.current) return;
      removeNotification(connectionLostNotificationIdRef.current);
      connectionLostNotificationIdRef.current = null;
    };
    const handleConnectionChanged = ({ online }) => {
      if (!online) {
        if (connectionLostNotificationIdRef.current) return;
        connectionLostNotificationIdRef.current = addSystemNotification({
          duration: 0,
          emitter: "Chat",
          message: t2("Waiting for network…"),
          severity: "loading",
          type: "system:network:connection:lost"
        });
        return;
      }
      dismissConnectionLostNotification();
    };
    client.on("connection.changed", handleConnectionChanged);
    return () => {
      client.off("connection.changed", handleConnectionChanged);
      dismissConnectionLostNotification();
    };
  }, [addSystemNotification, client, removeNotification, t2]);
};
var useCreateChatContext = (value) => {
  const {
    channel,
    channelsQueryState,
    client,
    customClasses,
    getAppSettings,
    isMessageAIGenerated,
    latestMessageDatesByChannels,
    mutes,
    searchController,
    setActiveChannel,
    theme,
    useImageFlagEmojisOnWindows
  } = value;
  const channelCid = channel?.cid;
  const channelsQueryError = channelsQueryState.error;
  const channelsQueryInProgress = channelsQueryState.queryInProgress;
  const clientValues = `${client.clientID}${Object.keys(client.activeChannels).length}${Object.keys(client.listeners).length}${client.mutedChannels.length}
  ${client.user?.id}`;
  const mutedUsersLength = mutes.length;
  const chatContext = (0, import_react29.useMemo)(
    () => ({
      channel,
      channelsQueryState,
      client,
      customClasses,
      getAppSettings,
      isMessageAIGenerated,
      latestMessageDatesByChannels,
      mutes,
      searchController,
      setActiveChannel,
      theme,
      useImageFlagEmojisOnWindows
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      channelCid,
      channelsQueryError,
      channelsQueryInProgress,
      clientValues,
      getAppSettings,
      searchController,
      mutedUsersLength,
      isMessageAIGenerated
    ]
  );
  return chatContext;
};
var useChannelsQueryState = () => {
  const [error2, setError] = (0, import_react29.useState)(null);
  const [queryInProgress, setQueryInProgress] = (0, import_react29.useState)("uninitialized");
  return {
    error: error2,
    queryInProgress,
    setError,
    setQueryInProgress
  };
};
var NetworkConnectionNotificationReporter = () => {
  useReportLostConnectionSystemNotification();
  return null;
};
var Chat = (props) => {
  const {
    children,
    client,
    customClasses,
    defaultLanguage,
    i18nInstance,
    isMessageAIGenerated,
    searchController: customChannelSearchController,
    theme = "messaging light",
    useImageFlagEmojisOnWindows = false
  } = props;
  const {
    channel,
    getAppSettings,
    latestMessageDatesByChannels,
    mutes,
    setActiveChannel,
    translators
  } = useChat({
    client,
    defaultLanguage,
    i18nInstance
  });
  const channelsQueryState = useChannelsQueryState();
  const searchController = (0, import_react29.useMemo)(
    () => customChannelSearchController ?? new SearchController({
      sources: [
        new ChannelSearchSource(client),
        new UserSearchSource(client),
        new MessageSearchSource(client)
      ]
    }),
    [client, customChannelSearchController]
  );
  const chatContextValue = useCreateChatContext({
    channel,
    channelsQueryState,
    client,
    customClasses,
    getAppSettings,
    isMessageAIGenerated,
    latestMessageDatesByChannels,
    mutes,
    searchController,
    setActiveChannel,
    theme,
    useImageFlagEmojisOnWindows
  });
  if (!translators.t) return null;
  return (0, import_jsx_runtime4.jsx)(ChatProvider, { value: chatContextValue, children: (0, import_jsx_runtime4.jsx)(TranslationProvider, { value: translators, children: (0, import_jsx_runtime4.jsxs)(ModalDialogManagerProvider, { children: [
    (0, import_jsx_runtime4.jsx)(NetworkConnectionNotificationReporter, {}),
    children
  ] }) }) });
};
var useCreateChatClient = ({
  apiKey,
  options,
  tokenOrProvider,
  userData
}) => {
  const [chatClient, setChatClient] = (0, import_react29.useState)(null);
  const [cachedUserData, setCachedUserData] = (0, import_react29.useState)(userData);
  if (userData.id !== cachedUserData.id) {
    setCachedUserData(userData);
  }
  const [cachedOptions] = (0, import_react29.useState)(options);
  (0, import_react29.useEffect)(() => {
    const client = new StreamChat(apiKey, void 0, cachedOptions);
    let didUserConnectInterrupt = false;
    const connectionPromise = client.connectUser(cachedUserData, tokenOrProvider).then(() => {
      if (!didUserConnectInterrupt) setChatClient(client);
    });
    return () => {
      didUserConnectInterrupt = true;
      setChatClient(null);
      connectionPromise.then(() => client.disconnectUser()).then(() => {
        console.log(`Connection for user "${cachedUserData.id}" has been closed`);
      });
    };
  }, [apiKey, cachedUserData, cachedOptions, tokenOrProvider]);
  return chatClient;
};
var UnMemoizedWindow = (props) => {
  const { children, thread: propThread } = props;
  const { thread: contextThread } = useChannelStateContext("Window");
  return (0, import_jsx_runtime4.jsx)(
    "div",
    {
      className: clsx_default("str-chat__main-panel", {
        "str-chat__main-panel--thread-open": contextThread || propThread
      }),
      children
    }
  );
};
var Window = import_react29.default.memo(UnMemoizedWindow);
export {
  ACTIONS_NOT_WORKING_IN_THREAD,
  AIStateIndicator,
  AIStates,
  ATTACHMENT_GROUPS_ORDER,
  AddCommentPrompt,
  Alert,
  AmountBar,
  Attachment,
  AttachmentActions,
  AttachmentActionsContainer,
  AttachmentPreviewList,
  AttachmentSelector,
  AttachmentSelectorButton,
  AttachmentWithinContainer,
  Audio2 as Audio,
  AudioContainer,
  AudioPlayer,
  AudioRecorder,
  Avatar,
  AvatarStack,
  Badge,
  BaseContextMenuButton,
  BaseImage,
  BinIcon,
  Button,
  Callout,
  Card,
  CardContainer,
  Channel,
  ChannelActionContext,
  ChannelActionProvider,
  ChannelAvatar,
  ChannelHeader,
  ChannelList,
  ChannelListContext,
  ChannelListContextProvider,
  ChannelListItem,
  ChannelListItemActionButtons,
  ChannelListItemTimestamp,
  ChannelListItemUI,
  ChannelListUI,
  ChannelSearchResultItem,
  ChannelStateContext,
  ChannelStateProvider,
  Chat,
  ChatContext,
  ChatProvider,
  ChatView,
  ChatViewChannelsSelectorButton,
  ChatViewContext,
  ChatViewSelectorButton,
  ChatViewThreadsSelectorButton,
  CheckSignIcon,
  Checkmark,
  CircularProgressIndicator,
  CommandChip,
  CommandItem,
  ComponentContext,
  ComponentProvider,
  ContextMenu,
  ContextMenuBackButton,
  ContextMenuBody,
  ContextMenuButton,
  ContextMenuContent,
  ContextMenuHeader,
  ContextMenuRoot,
  CooldownTimer,
  DateSeparator,
  DefaultAttachmentSelectorComponents,
  DefaultSearchResultItems,
  DialogAnchor,
  DialogManager,
  DialogManagerProvider,
  DialogPortalDestination,
  DialogPortalEntry,
  DownloadButton,
  Dropdown,
  DurationDisplay,
  EmojiContextMenuButton,
  EmoticonItem,
  EmptyStateIndicator,
  EndPollAlert,
  ErrorBadge,
  EventComponent,
  FILE_ICON_GRAPHIC_CLASSNAME,
  FILE_ICON_NO_LABEL_CLASSNAME,
  FieldError,
  FileAttachment,
  FileContainer,
  FileDragAndDropContent,
  FileIcon,
  FileInput,
  FileSizeIndicator,
  Gallery,
  GalleryContainer,
  GalleryContext,
  GalleryUI,
  Geolocation,
  GeolocationContainer,
  GiphyContainer,
  GiphyPreviewMessage,
  GlobalModal,
  GroupAvatar,
  Header$1 as Header,
  IconArchive,
  IconArrowDown,
  IconArrowDownCircle,
  IconArrowLeft,
  IconArrowUp,
  IconArrowUpRight,
  IconAttachment,
  IconAudio,
  IconBell,
  IconBellOff,
  IconBolt,
  IconBookmark,
  IconBookmarkRemove,
  IconCamera,
  IconCheckmark,
  IconCheckmark1Small,
  IconChecks,
  IconChevronDown,
  IconChevronLeft,
  IconChevronRight,
  IconClock,
  IconCommand,
  IconCopy,
  IconDelete,
  IconDownload,
  IconEdit,
  IconEmoji,
  IconEmojiAdd,
  IconExclamationCircleFill,
  IconExclamationMark,
  IconExclamationMarkFill,
  IconExclamationTriangleFill,
  IconEyeFill,
  IconFile,
  IconFlag,
  IconGiphy,
  IconImage,
  IconLeave,
  IconLink,
  IconLoading,
  IconLocation,
  IconMessageBubble,
  IconMessageBubbleFill,
  IconMessageBubbles,
  IconMicrophoneSolid,
  IconMinus,
  IconMinusCircle,
  IconMore,
  IconMute,
  IconNoSign,
  IconNotification,
  IconPauseFill,
  IconPin,
  IconPlayFill,
  IconPlus,
  IconPlusSmall,
  IconPoll,
  IconQuote,
  IconRefresh,
  IconReorder,
  IconReply,
  IconRetry,
  IconSearch,
  IconSend,
  IconThread,
  IconThreadFill,
  IconTranslate,
  IconTrophy,
  IconUnpin,
  IconUnsupportedAttachment,
  IconUpload,
  IconUser,
  IconUserAdd,
  IconUserCheck,
  IconUserRemove,
  IconVideo,
  IconVideoFill,
  IconVoice,
  IconXCircle,
  IconXmark,
  IconXmarkSmall,
  ImageComponent,
  ImageContainer,
  ImagePlaceholder,
  InfiniteScroll,
  LinkPreviewCard,
  LinkPreviewList,
  LoadMoreButton,
  LoadMorePaginator,
  LoadingChannel,
  LoadingChannels,
  LoadingErrorIndicator,
  LoadingIndicator,
  MAX_MESSAGE_REACTIONS_TO_FETCH,
  MESSAGE_ACTIONS,
  MediaContainer,
  MediaRecordingState,
  Message,
  MessageActions,
  MessageAlsoSentInChannelIndicator,
  MessageBlocked,
  MessageBounceModal,
  MessageBouncePrompt,
  MessageBounceProvider,
  MessageComposer2 as MessageComposer,
  MessageComposerContext,
  MessageComposerContextProvider,
  MessageComposerUI,
  MessageContext,
  MessageDeletedBubble,
  MessageDeliveryStatus,
  MessageEditedIndicator,
  MessageList,
  MessageListContext,
  MessageListContextProvider,
  MessageProvider,
  MessageReactions,
  MessageReactionsDetail,
  MessageReactionsDetailLoadingIndicator,
  MessageRepliesCountButton,
  MessageSearchResultItem,
  MessageStatus,
  MessageText,
  MessageTimestamp,
  MessageTranslationIndicator,
  MessageTranslationViewContext,
  MessageTranslationViewProvider,
  MessageUI,
  ModalContext,
  ModalContextProvider,
  ModalDialogManagerProvider,
  ModalGallery,
  NewMessageNotification,
  Notification,
  NotificationList,
  NotificationTranslationTopic,
  NumericInput,
  OPTIONAL_MESSAGE_ACTIONS,
  OtherFilesContainer,
  PauseIcon,
  PinIndicator,
  PlayButton,
  PlayIcon,
  PlaybackRateButton,
  Poll,
  PollActions,
  PollAnswerList,
  PollContent,
  PollContext,
  PollCreationDialog,
  PollHeader,
  PollOptionList,
  PollOptionSelector,
  PollOptionsFullList,
  PollProvider,
  PollResults,
  PollVote,
  PollVoteListing,
  PopperTooltip,
  ProgressBar,
  Prompt,
  QuickMessageActionsButton,
  QuotedMessage,
  QuotedMessagePreview,
  QuotedMessagePreviewUI,
  QuotedVoiceRecording,
  ReactionSelector,
  RecordingPermission,
  RecordingPermissionDeniedNotification,
  RecordingTimer,
  ReminderNotification,
  Root,
  SUPPORTED_VIDEO_FORMATS$1 as SUPPORTED_VIDEO_FORMATS,
  SYSTEM_NOTIFICATION_TAG,
  SafeAnchor,
  ScrollToLatestMessageButton,
  Search,
  SearchBar,
  SearchContext,
  SearchContextProvider,
  SearchResults,
  SearchResultsHeader,
  SearchResultsPresearch,
  SearchSourceResultList,
  SearchSourceResultListFooter,
  SearchSourceResults,
  SearchSourceResultsContext,
  SearchSourceResultsContextProvider,
  SearchSourceResultsEmpty,
  SearchSourceResultsLoadingIndicator,
  SendButton,
  ShareLocationDialog,
  SimpleAttachmentSelector,
  SpriteImage,
  DefaultStartRecordingAudioButton as StartRecordingAudioButton,
  StreamedMessageText,
  Streami18n,
  SuggestPollOptionPrompt,
  SuggestionList,
  SuggestionListItem,
  SwitchField,
  SwitchFieldDescription,
  SwitchFieldLabel,
  SwitchFieldTitle,
  TextInput,
  TextInputFieldSet,
  TextareaComposer,
  Thread,
  ThreadContext,
  ThreadHeader,
  ThreadList,
  ThreadListItem,
  ThreadListItemUI,
  ThreadProvider,
  ThreadStart,
  Tooltip,
  TranslationBuilder,
  TranslationContext,
  TranslationProvider,
  TranslationTopic,
  TypingContext,
  TypingIndicator,
  TypingIndicatorHeader,
  TypingProvider,
  UNREAD_MESSAGE_SEPARATOR_CLASS,
  UnMemoizedLoadMorePaginator,
  UnreadMessagesNotification,
  UnreadMessagesSeparator,
  UnsupportedAttachment,
  UnsupportedAttachmentContainer,
  UploadIcon,
  UploadProgressIndicator,
  UploadedSizeIndicator,
  UserContextMenuButton,
  UserItem,
  UserSearchResultItem,
  VideoContainer,
  Viewer,
  VirtualizedMessageList,
  VoiceRecording,
  VoiceRecordingContainer,
  VoiceRecordingPlayer,
  WaveProgressBar,
  Window,
  WithAudioPlayback,
  WithComponents,
  WithDragAndDropUpload,
  addNotificationTargetTag,
  areMessagePropsEqual,
  areMessageUIPropsEqual,
  countEmojis,
  createIcon,
  deTranslations,
  defaultAllowedTagNames,
  defaultAttachmentActionsDefaultFocus,
  defaultAttachmentSelectorActionSet,
  defaultChatViewSelectorItemSet,
  defaultComponents$1 as defaultComponents,
  defaultDateTimeParser,
  defaultMessageActionSet,
  defaultReactionOptions,
  defaultRegisterAudioPlayerError,
  defaultRenderMessages,
  defaultTranslatorFunction,
  displayDuration,
  divMod,
  downSample,
  elementIsPlaying,
  emojiMarkdownPlugin,
  emojiToUnicode,
  enTranslations,
  esTranslations,
  escapeRegExp,
  extractSortValue,
  findLastPinnedChannelIndex,
  frTranslations,
  getChannel,
  getChannelDisplayImage,
  getCssDimensionsVariables,
  getGroupChannelDisplayInfo,
  getGroupStyles,
  getImages,
  getIsFirstUnreadMessage,
  getLastReceived,
  getLatestMessagePreview,
  getMessageActions,
  getNonImageAttachments,
  getNotificationTargetPanel,
  getNotificationTargetPanels,
  getNotificationTargetTag,
  getReadByTooltipText,
  getTextareaCaretRect,
  getTranslatedMessageText,
  getWholeChar,
  handleActionWarning,
  hasMoreMessagesProbably,
  hasSystemNotificationTag,
  hiTranslations,
  htmlToTextPlugin,
  imageToLink,
  insertIntro,
  isChannelArchived,
  isChannelPinned,
  isChrome,
  isDate,
  isDateSeparatorMessage,
  isDayOrMoment,
  isFirefox,
  isGalleryAttachmentType,
  isIntroMessage,
  isLanguageSupported,
  isLocalMessage,
  isMessageBlocked,
  isMessageBounced,
  isMessageDeleted,
  isMessageEdited,
  isMessageErrorRetryable,
  isNetworkSendFailure,
  isNotificationForPanel,
  isNotificationTargetPanel,
  isNumberOrString,
  isSafari2 as isSafari,
  isSvgAttachment,
  isUserMuted,
  itTranslations,
  jaTranslations,
  keepLineBreaksPlugin,
  koTranslations,
  makeDateMessageId,
  makeIntroMessage,
  mapEmojiMartData,
  mapToUserNameOrId,
  markDownRenderers,
  matchMarkdownLinks,
  mentionsMarkdownPlugin,
  messageCodeBlocks,
  messageHasAttachments,
  messageHasGiphyAttachment,
  messageHasQuotedMessage,
  messageHasReactions,
  messageHasSingleAttachment,
  messageTextHasEmojisOnly,
  missingUseFlagHandlerParameterWarning,
  missingUseMuteHandlerParamsWarning,
  modalDialogId,
  modalDialogManagerId,
  moveChannelUpwards,
  nlTranslations,
  plusPlusToEmphasis,
  processMessages,
  ptTranslations,
  reactionHandlerWarning,
  remarkIgnoreMarkdown,
  renderPreviewText,
  renderText,
  resampleWaveformData,
  ruTranslations,
  shouldConsiderArchivedChannels,
  shouldConsiderPinnedChannels,
  toBaseImageDescriptors,
  toGalleryItemDescriptors,
  trTranslations,
  unicodeToEmoji,
  upSample,
  useAIState,
  useActionHandler,
  useActiveAudioPlayer,
  useActiveThread,
  useAttachmentManagerState,
  useAttachmentsForPreview,
  useAudioPlayer,
  useBaseMessageActionSetFilter,
  useCanCreatePoll,
  useChannelActionContext,
  useChannelDisplayName,
  useEditMessageHandler as useChannelEditMessageHandler,
  useChannelListContext,
  useChannelListItemContext,
  useChannelMembersState,
  useChannelMembershipState,
  useMentionsHandlers as useChannelMentionsHandler,
  useChannelPreviewInfo,
  useChannelStateContext,
  useChat,
  useChatContext,
  useChatViewContext,
  useComponentContext,
  useConnectionRecoveredListener,
  useContextMenuContext,
  useCooldownRemaining,
  useCreateChatClient,
  useDeleteHandler,
  useDialog,
  useDialogAnchor,
  useDialogIsOpen,
  useDialogManager,
  useDialogOnNearestManager,
  useDropdownContext,
  useEnrichedMessages,
  useFlagHandler,
  useGalleryContext,
  useLastReadData,
  useLegacyThreadContext,
  useLiveLocationSharingManager,
  useMarkUnreadHandler,
  useMentionsHandler,
  useMessageBounceContext,
  useMessageComposerBindings,
  useMessageComposerContext,
  useMessageComposerController,
  useMessageComposerHasSendableData,
  useMessageContentIsEmpty,
  useMessageContext,
  useMessageListContext,
  useMessageListElements,
  useMessageListScrollManager,
  useMessageReminder,
  useMessageTextStreaming,
  useMessageTranslationViewContext,
  useModalContext,
  useModalDialog,
  useModalDialogIsOpen,
  useModalDialogManager,
  useMuteHandler,
  useNearestDialogManagerContext,
  useNewMessageNotification,
  useNotificationApi,
  useNotificationTarget,
  useNotifications,
  useOpenThreadHandler,
  useOpenedDialogCount,
  usePaginatedChannels,
  usePinHandler,
  usePollAnswerPagination,
  usePollContext,
  usePollOptionVotesPagination,
  usePrependedMessagesCount,
  useReactionHandler,
  useReactionsFetcher,
  useReportLostConnectionSystemNotification,
  useRetryHandler,
  useScrollLocationLogic,
  useSearchContext,
  useSearchSourceResultsContext,
  useShouldForceScrollToBottom,
  useStateStore,
  useSystemNotifications,
  useThreadContext,
  useThreadList,
  useThreadListItemContext,
  useThreadsViewContext,
  useTranslationContext,
  useTypingContext,
  useUserHandler,
  useUserRole,
  validateAndGetMessage
};
//# sourceMappingURL=stream-chat-react.js.map
